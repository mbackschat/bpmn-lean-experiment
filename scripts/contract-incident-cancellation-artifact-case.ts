/** Exact retained-evidence route for the incident-gated root cancellation schedule. */
export const serviceTaskIncidentCancellationArtifactCases = Object.freeze([
  Object.freeze({
    scenarioRelativePath:
      "scenarios/service-task-incident-cancellation/scenario.json",
    evidenceRelativePath:
      "scenarios/service-task-incident-cancellation/cibseven-evidence.json",
  }),
]);
