/** Exact retained-evidence route for the configured Service Task incident schedule. */
export const serviceTaskIncidentArtifactCases = Object.freeze([
  Object.freeze({
    scenarioRelativePath:
      "scenarios/service-task-incident/scenario.json",
    evidenceRelativePath:
      "scenarios/service-task-incident/cibseven-evidence.json",
  }),
]);
