import BpmnSemantics.Experiments.CheckedSourceScenario

/-! # Direct checked-graph token game

This umbrella module exposes the provisional source-side state, transition relation, and scenario evaluator. It cannot depend on Semantic Process lowering or execution because it sits below that implementation in the import graph.
-/
