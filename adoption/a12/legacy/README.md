# Frozen A12 adoption baseline

This directory preserves project-authored files exactly as they existed at immutable pre-extraction target `02330ad0f980a5fc282cc0aa93600a9632b86c3e`. It is optional adoption evidence, not a product source root, current contract reader, build input, runtime dependency, semantic authority, or A12 distribution.

[The manifest](manifest.json) binds every original path, frozen path, and SHA-256. Current validators do not read the frozen artifacts. The adoption oracle must execute the original validator and projector from the immutable target in an isolated Git export, then compare their projections with the current overlay-aware generation after only the approved identity translation.
