# CIB Seven 2.2.0 User Task assignment and form metadata draft profile

This draft profile composes the Boolean User Task completion-data policy with one exact literal group candidate and one exact typed generated-form field. The passive neutral metadata is observed on the open User Task and does not change occurrence identity, completion admission, submitted values, authorization, claiming, rendering, or validation.

The [profile artifact](profile.json) pins CIB Seven `2.2.0` at the reviewed source revision and selects `CIB-EXT-0011` for the literal group candidate plus `CIB-EXT-0012` for the generated-form field identity and `string` or `boolean` type. The [User Task assignment and form metadata proposal](../../docs/capsules/USER-TASK-ASSIGNMENT-FORM-METADATA-PROPOSAL.md) owns the exact source restriction, neutral contract, evidence obligations, and exclusions.

Its status remains `draft`. Results must not be reported as general BPMN resource assignment, a form engine, authorization behavior, immutable CIB compatibility, or BPMN Process Execution Conformance.
