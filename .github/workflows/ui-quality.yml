name: Product 2 UI quality

on:
  pull_request:
    paths:
      - ".github/workflows/ui-quality.yml"
      - "package.json"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - "platform/apps/web/**"
      - "platform/contracts/**"
      - "platform/ui-kit/**"
      - "showcase/platform-ui-quality/**"
      - "scripts/playwright-loopback-ports.test.ts"
      - "scripts/playwright-loopback-ports.ts"
  push:
    branches:
      - main
    paths:
      - ".github/workflows/ui-quality.yml"
      - "package.json"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - "platform/apps/web/**"
      - "platform/contracts/**"
      - "platform/ui-kit/**"
      - "showcase/platform-ui-quality/**"
      - "scripts/playwright-loopback-ports.test.ts"
      - "scripts/playwright-loopback-ports.ts"
  workflow_dispatch:
    inputs:
      regenerate_baselines:
        description: Generate the optional wide Process Diagram screenshot candidate
        required: false
        default: false
        type: boolean
      release_acceptance:
        description: Run the real M1 through M4 and MUE Preview Alpha browser acceptance lanes
        required: false
        default: false
        type: boolean

permissions:
  contents: read

concurrency:
  group: product-2-ui-quality-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  ui-quality:
    name: ui-quality (desktop Chromium)
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright@sha256:c091b21d9fae78c76e85cd4356431e9b018402f172a214fc7d7a5e9a7e29d8ac
      options: --init
    timeout-minutes: 10
    env:
      CI: "true"
      PLAYWRIGHT_BROWSERS_PATH: /ms-playwright
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Trust checked-out workspace
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Resolve pinned toolchain versions
        id: pins
        run: |
          eval "$(./scripts/pinned-toolchain.sh)"
          {
            printf 'node=%s\n' "$required_node_version"
            printf 'pnpm=%s\n' "$required_pnpm_version"
          } >> "$GITHUB_OUTPUT"

      - name: Set up pnpm
        uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9
        with:
          version: ${{ steps.pins.outputs.pnpm }}

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ steps.pins.outputs.node }}
          cache: pnpm

      - name: Install workspace dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify Product 2 UI quality
        if: github.event_name != 'workflow_dispatch' || (inputs.regenerate_baselines != true && inputs.release_acceptance != true)
        run: ./scripts/pnpm.sh run test:pre-push:ui

      - name: Verify Product 2 M1 through M4 and MUE Preview Alpha release acceptance
        if: github.event_name == 'workflow_dispatch' && inputs.release_acceptance == true && inputs.regenerate_baselines != true
        run: ./scripts/pnpm.sh run test:release:product2

      - name: Generate candidate wide Diagram baseline
        if: github.event_name == 'workflow_dispatch' && inputs.regenerate_baselines == true
        run: ./scripts/pnpm.sh run test:ui-quality:update-snapshots

      - name: Upload candidate wide Diagram baseline
        if: github.event_name == 'workflow_dispatch' && inputs.regenerate_baselines == true
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: product-2-ui-quality-baseline-candidates
          if-no-files-found: error
          retention-days: 14
          path: showcase/platform-ui-quality/e2e/snapshots/

      - name: Upload UI-quality failure evidence
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: product-2-ui-quality-failure
          if-no-files-found: ignore
          retention-days: 14
          path: |
            showcase/platform-ui-quality/playwright-report/
            showcase/platform-ui-quality/test-results/
            showcase/mue-preview-alpha/playwright-report/
            showcase/mue-preview-alpha/test-results/
