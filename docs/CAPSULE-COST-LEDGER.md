# Capsule cost ledger

**Status:** Maintained closure-measurement ledger.

## Purpose and denominator

This ledger retains reproducible nonblank code and documentation churn for every completed semantic capsule or bounded product/enabling increment whose baseline and closure commits were recorded. It does not measure semantic value, proof strength, test independence, JSON evidence volume, generated output, or wall time. [PLAN.md](PLAN.md) owns only current sequencing; Git and the owning specifications retain implementation history.

The denominator begins when commit-bounded measurement became mandatory. Earlier work with no clean baseline remains explicitly unknown rather than reconstructed from a mixed span. Run `node scripts/capsule-cost.ts <baseline-commit> <closure-commit>` as specified by [TESTING-SPEC.md](TESTING-SPEC.md#capsule-closure-review).

## Measurements

The 2026-09-20 [commit-binding correction](TESTING-SPEC.md#commit-boundaries) migrated every existing module row to the SHA-256 of its original measured Git blob after verifying identical current bytes. Existing peaks, elapsed readings, conditions, and historical measurement identities were retained; this was no new Docker measurement. Future measurements bind their mounted source and completed receipt before the implementation commit. This removes the source-commit/evidence-commit cycle without changing the fixed resource gate or claiming dependency-closure freshness from one module digest.

The retained M6 closure gate ran on 2026-08-16 and exited zero. It covered Lean, the semantic core, BPMN source, protocol/schema/canonical bytes, Workflow Query, client/API, both pinned CIB releases, differential comparison, 219 Temporal adapter tests, retained history replays, and the complete registered pipeline. The separate path-scoped Product 2 browser evidence covered the 1280/1600 structured Human Work journey. This is correctness evidence, not an uncontended performance baseline or a production-scalability claim.

The runtime-state identity-bound preflight is resource evidence, not a closure-churn row. The governed rerun on 2026-08-24 used the local image `bpmn-lean-audit:v4.31.0-arm64` at image ID `sha256:4df22c7a1ec8ba8c4c2795fdea1269d7b9cadade4e65b22e595047509aa27950`, Lean 4.31.0 commit `68218e876d2a38b1985b8590fff244a83c321783`, `LEAN_NUM_THREADS=1`, `--cpus=1`, `--memory=3g`, and `--memory-swap=3g`; the applied cgroup values were `memory.max=3221225472` and `memory.swap.max=0`. The Linux-container enforcement backend bind-mounted the exact commit read-only and mounted a separate Linux `.lake` cache. Each cache was prewarmed through the named target, then only that target's `.olean`, `.ilean`, hash, trace, C, and setup artifacts were removed, leaving a warm dependency closure and a cold measured target. The exact measured command inside the constrained container was `/usr/bin/time -v ./scripts/lake.sh build <target>`; the outer command was `docker run --rm --memory=3g --memory-swap=3g --cpus=1 --mount type=bind,source=<exact-commit-checkout>,target=/work,readonly --mount type=bind,source=<commit-specific-linux-cache>,target=/work/.lake --workdir /work bpmn-lean-audit:v4.31.0-arm64 bash -lc '<metadata>; /usr/bin/time -v ./scripts/lake.sh build <target>; <cgroup-readback>'`. GNU `ru_maxrss` and the cgroup peak are retained separately because the cgroup is the hard process-tree controller.

The Activity data-output capsule re-measured `BpmnSemantics.UserTaskMetadataConformance`, `BpmnSemantics.ParallelUserTaskMetadataCompositionConformance`, and the new `BpmnSemantics.ActivityDataOutputConformance` at commit `d78cafd3` under that identical setup, then re-measured the new module alone at `de92c7c5` when a checkpoint-review correction changed its source bytes, and additionally measured the parallel-metadata module at parent commit `3dc827ac` to attribute its figure. That baseline run is the reason the row is recorded as roughly unchanged rather than as a threefold regression: the module already measured 3,272,540 KiB before this capsule, against the 2,718,336 KiB the ledger still carried from `d878f38e`. Two runs at the head commit bracketed the figure at 3,091,880 and 3,282,876 KiB under ambient macOS background load, and the higher run is the one recorded together with its own elapsed reading.

The composed Activity data-input/output checkpoint measured its new conformance owner and re-measured the changed User Task metadata consumer at immutable commit `1326f543c8d5933033bad2299586f4aea1ac9400` on 2026-09-05 under the same pinned image, one-CPU allowance, network isolation, warm dependency closure, cold exact-target rebuild, unchanged 3 GiB hard limit, and no-additional-swap policy. Other host processes were active, so elapsed time remains context rather than a trend; both runs received 95% CPU and required no retry. `ActivityDataInputOutputConformance` exited zero in 9.26 seconds at 2,361,096 KiB GNU RSS and 2,045,648,896 bytes cgroup peak. `UserTaskMetadataConformance` exited zero in 7.82 seconds at 1,741,508 KiB GNU RSS and 1,420,500,992 bytes cgroup peak. Both retained zero swaps and zero cgroup `low`, `high`, `max`, `oom`, `oom_kill`, `oom_group_kill`, and `sock_throttled` events; neither reaches the 90% disclosure threshold.

The composed checkpoint's semantic corrections were remeasured at immutable commit `64d0cb869b22fab101a33ca5513c408efa0b820d` on 2026-09-05 under the identical pinned, network-disabled, one-CPU, 3 GiB/no-additional-swap protocol above. Each exact target was rebuilt cold against its warm dependency closure. The Activity IO result newly crosses the 90% GNU-RSS disclosure threshold; an unchanged-bound repeat confirmed the near-cap classification, and the executable ledger conservatively retains the higher RSS tuple and its own elapsed time. The worst cgroup peak leaves 494,194,688 bytes below the hard limit. Every run exited zero with zero swaps and zero cgroup memory-event counters. The first Activity IO run's pressure `some` and `full` totals rose from 276 to 8,410 microseconds; the repeat and metadata run retained zero pressure totals. Elapsed readings remain host-load context, not an uncontended trend.

| Corrected target at `64d0cb86` | User / system / elapsed | GNU maximum RSS | Cgroup peak | Output SHA-256 |
|---|---|---:|---:|---|
| `BpmnSemantics.ActivityDataInputOutputConformance`, retained run | 14.05 s / 0.71 s / 15.20 s | 2,983,524 KiB | 2,727,030,784 bytes | `5faef559483db37c550cc99b3827dd1ab55695d7b7bfc15f485a54e89ec6ead8` |
| `BpmnSemantics.ActivityDataInputOutputConformance`, repeat | 24.51 s / 1.87 s / 28.54 s | 2,904,720 KiB | 2,638,110,720 bytes | `e81cb2ac6e9aea9eddbdfe200fa14237fbba0ed16d24e3e2b640f199032181e5` |
| `BpmnSemantics.UserTaskMetadataConformance` | 8.06 s / 0.31 s / 8.78 s | 1,825,220 KiB | 1,499,840,512 bytes | `2a2ad900892415a78088f85838410d9229043a47ff9f5e5faf2abad7eee4ca57` |

The next composed-input correction at `305b5893702c7999430c5b2ed62882eeba6aff30` passed the local complete Lean library but failed the unchanged Linux controller on 2026-09-05. Its dependency prewarm ended with the conformance child killed at code 137, cgroup peak 3,221,225,472 bytes, `max=445281`, `oom=1`, and `oom_kill=1`. An isolated cold-target retry against the now-warm dependencies exited zero in 21.27 seconds at 3,121,316 KiB GNU RSS, but again reached the exact cgroup ceiling with `max=3797`; that exit-zero result is rejected as resource evidence. Both retain zero swap. The repeated ceiling therefore cannot be attributed only to dependency prewarming.

The diagnostic correction separates unchanged admission, malformed-state refusal, and successful-lifetime decisions while keeping their exact source and runtime fixture definitions in one shared owner. Merely separating admission first still left only 64,397,312 bytes of cgroup margin. The completed separation passed in 17.57 seconds at 1,965,732 KiB GNU RSS and 2,293,329,920 cgroup bytes with every controlled event and pressure total zero. These are working-tree diagnostics, not immutable cost rows. The conservation check compares all 55 declaration bodies and all 24 theorem statements/proofs against `305b5893`; only shared conformance-fixture visibility changes. No semantic rule, proof proposition, kernel-decision obligation, or memory limit changes.

The separated proof owners were each rebuilt cold at immutable commit `8a16c7a2b06074b91ea714a995e9dd6112b82712` on 2026-09-05 against the warm dependency closure under the identical pinned, network-disabled, one-CPU, 3 GiB/no-additional-swap protocol. All three exited zero with zero swaps, zero cgroup memory-event counters, and zero pressure totals before and after the build. None reaches the 90% GNU-RSS disclosure threshold; the highest cgroup peak leaves 1,641,037,824 bytes below the unchanged hard ceiling. These exact-target measurements replace the composed owner's near-cap executable row; the failed unsplit and intermediate diagnostic evidence above remains retained.

| Separated target at `8a16c7a2` | User / system / elapsed | GNU maximum RSS | Cgroup peak | Output SHA-256 |
|---|---|---:|---:|---|
| `BpmnSemantics.ActivityDataInputOutputAdmissionConformance` | 4.53 s / 0.49 s / 5.48 s | 1,422,344 KiB | 1,063,088,128 bytes | `426303799d9f52f7b7211a0b9cabb9cf35d7ab24b0c37febe511bec7b526b85d` |
| `BpmnSemantics.ActivityDataInputOutputRefusalConformance` | 6.05 s / 0.35 s / 6.79 s | 1,918,280 KiB | 1,580,187,648 bytes | `27c0c1ee960ef4e828863426225f272768e14f1cfb3f25456dc254f2b90d709d` |
| `BpmnSemantics.ActivityDataInputOutputConformance` | 5.32 s / 0.17 s / 5.87 s | 1,847,624 KiB | 1,501,843,456 bytes | `89927f70db8174aa90ba17afbf58b58c124e72137f6d00d2a3d52021cd7180f4` |

The external-review E1 experiment on 2026-09-05 isolated elaboration mode under the same pinned image, one CPU, one Lean thread, network isolation, warm dependency closure, and unchanged 3 GiB/no-additional-swap controller. The refusal baseline uses exact commit `91042eb0`; its variant adds only `set_option Elab.async false`. The unsplit variant restores the original 24-theorem owner from `305b5893` under a temporary module name and adds the same option. All three builds exited zero with zero swaps, controlled memory events, and pressure totals. The large within-target reduction identifies asynchronous elaboration as a material contributor to accumulation; it does not establish the cost of every other proof family. The variants below are reproducible diagnostics, not immutable replacement rows.

| E1 target and source variant | User / system / elapsed | GNU maximum RSS | Cgroup peak | Output SHA-256 |
|---|---|---:|---:|---|
| Refusal at `91042eb0`, unchanged | 7.00 s / 0.61 s / 8.84 s | 2,027,092 KiB | 2,166,878,208 bytes | `28f1ac2f7e51597102d4b34953b5ec0b35423b38c2a8e660c477a4bbc87f84a4` |
| Same refusal, asynchronous elaboration disabled | 7.09 s / 0.28 s / 7.78 s | 833,632 KiB | 447,098,880 bytes | `8838da178e20b7016873fa2f898ba51d7cc636eb8e576eb4dda7514fb5fcd2b1` |
| Original unsplit owner from `305b5893`, asynchronous elaboration disabled | 15.62 s / 0.61 s / 16.89 s | 1,081,752 KiB | 722,513,920 bytes | `f78131be374997b8d812cd0ef826b6c8868879dca67fc97a2375380d38c8a323` |

The resulting correction keeps the split owners, disables asynchronous elaboration locally, and replaces the composed fixture's repeated lowering/start/activation/completion chain with literal values locked to those computations by kernel equality proofs. Existing refusal statements use the unchanged generic rejection law; the profile and capability facts are proved once. Independent mutations of the literal Program's Start output and the active wait's owner fail the corresponding equality locks. All 24 original consumer theorem statements remain byte-identical.

The complete candidate library gate on 2026-09-05 passed in 23:55.85 under the same controller: GNU maximum RSS 3,141,140 KiB, cgroup peak 3,215,540,224 bytes, zero swaps, and zero `memory.events` counters. Unlike the focused E1 runs, its `memory.pressure` totals were 823,678 microseconds for both `some` and `full`; its 5,685,248-byte controller margin is narrow. This full-library result establishes successful compilation of the unchanged propositions and their consumers, not a general memory-margin claim for unrelated proof families.

The corrected consumers were remeasured at immutable commit `c6aeb5834bfd2b7f752fcbc6fbef6b74d26790de` on 2026-09-05. Each target's own generated artifacts were removed from its warm dependency closure. The unchanged controller recorded zero swaps, memory events, and pressure totals in every run; these are the replacement rows in [the executable cost record](../scripts/lean-module-cost.ts).

| Exact target at `c6aeb583` | User / system / elapsed | GNU maximum RSS | Cgroup peak | Output SHA-256 |
|---|---|---:|---:|---|
| `BpmnSemantics.ActivityDataInputOutputAdmissionConformance` | 3.09 s / 0.33 s / 3.88 s | 967,732 KiB | 594,030,592 bytes | `91c833b13837f7d12a2c563735be15f0cf2deee55f7f531ecda29742b9a37c05` |
| `BpmnSemantics.ActivityDataInputOutputRefusalConformance` | 2.40 s / 0.34 s / 3.18 s | 736,440 KiB | 335,446,016 bytes | `4a1dcaa56eff751c851ca65e610487dda3d51b4f8cb32185470dc2c88dd7c1c1` |
| `BpmnSemantics.ActivityDataInputOutputConformance` | 1.43 s / 0.23 s / 2.03 s | 967,484 KiB | 574,992,384 bytes | `90402cd6ba4ead083aa926455548d57025fb9e0371812538a100101d3c6e2f3e` |

The Compensation ownership/dependency regression owner was measured cold at immutable commit `7b0b5151f2b3dae4b7759b30e39d8326ec378321` on 2026-09-05 under the same warm-dependency, one-CPU, network-disabled 3 GiB/no-additional-swap controller. `BpmnSemantics.CompensationTriggerHandlerAmbiguityConformance` exited zero at 3.44 s user / 0.41 s system / 5.14 s elapsed, 821,532 KiB GNU RSS, and 1,076,166,656 bytes cgroup peak, with zero swaps, memory-event counters, and pressure totals. Source SHA-256: `e72f879e10cb862b812061982d49b7a72ece98cd68c953a18ba79be3b0cdd568`; output SHA-256: `d719729c42a7f1d69891f92897360cec1b311bbc06144deb6af76eecfde8d2c7`. The same slice's complete Lean gate passed in 18:19.46 at 3,205,300 KiB GNU RSS and 3,099,238,400 bytes cgroup peak; it recorded zero swaps and memory events, but 376,424 microseconds in both pressure totals. The focused cost row does not claim zero pressure for that full-library run.

The [empty-state capacity repair](capsules/COMPENSATION-EMPTY-STATE-CAPACITY-REPAIR-SPEC.md) was measured at immutable source commit `4b2a304fca1b0264256cffc18fbecebdc6a3baaf` on 2026-09-05 with the same controller and cold exact-target artifacts over warm dependencies. `BpmnSemantics.CompensationTriggerHandlerProgramContractConformance` passed at 2.01 s user / 0.32 s system / 2.60 s elapsed, 889,480 KiB GNU RSS, and 505,503,744 cgroup bytes, with zero swaps, memory events, and pressure totals. Source SHA-256: `7110c3fafb19578d54ec2ffd935606d6e2f1d63e55fc73dbead7b2a2e425aff5`; output SHA-256: `067767176c119259a51828ae28e2f5de4e683d19c090494a58839eab69909a64`. Its complete 296-target Lean gate passed in 26:48.63 at 3,175,172 KiB GNU RSS and 3,006,087,168 cgroup bytes with zero swaps and memory events; both pressure totals were 191,224 microseconds.

| Commit | Exact target | Cache state | User / system / elapsed | GNU maximum RSS | Cgroup peak | Verdict | Output SHA-256 |
|---|---|---|---|---:|---:|---|---|
| `67503432ed56769fe7ad61fe1a3a5e7e8ae6cf1d` | `BpmnSemantics.RuntimeStateWellFormedConformance` | Warm dependency closure; exact target artifacts removed after successful prewarm | 30.04 s / 2.27 s / 33.20 s | 3,269,656 KiB | 3,221,225,472 bytes | Lake exit 1 after Lean child exit 137 at the hard memory limit | `10260f0e3b8e36da6c83cc4ebf31d259c6ab4811f2f22de427e53afbb7ee362a` |
| `07820fba191851920cd051880fb69fa08cac63a5` | `BpmnSemantics.RuntimeStateIdentityBoundConformance` | Warm dependency closure; exact target artifacts removed after successful prewarm | 7.09 s / 0.26 s / 7.55 s | 1,688,084 KiB | 1,331,347,456 bytes | Exit 0 | `0275b5349b25c487d8c8e4d8dc06f56ca76878a0e6f088b611cb60aba849a5ae` |
| `07820fba191851920cd051880fb69fa08cac63a5` | `BpmnSemantics.RuntimeStateActivityConformance` | Warm dependency closure; exact target artifacts removed after successful prewarm | 21.01 s / 0.65 s / 21.91 s | 3,063,168 KiB | 2,811,039,744 bytes | Exit 0 | `cec9729bf023ace9ec0b29621e812ac8a85b640048421d0fbfd3b0b1a0d90ee9` |
| `07820fba191851920cd051880fb69fa08cac63a5` | `BpmnSemantics.RuntimeStateWellFormedConformance` | Warm dependency closure; exact target artifacts removed after successful prewarm | 22.73 s / 0.81 s / 23.76 s | 2,831,344 KiB | 2,590,326,784 bytes | Exit 0 | `381d34e63516176db82e63b9cd0e4f4e0d88781e071efdd202473ed8b29bbed7` |

The unsplit parent owner hits the exact hard ceiling, while each checkpoint owner passes independently and both split owners remain close enough to the bound to make the separation material. This selects the proposal's consumer-bound narrowing: User Task, Timer, and Activity land now, while Message, Effect, Event race, Call, and ordinary Scope remain explicitly absent.

### Compensation snapshot resource-ceiling incident

The Compensation Event Sub-Process snapshot checkpoint repeated the same governed protocol on 2026-09-01: `docker run --rm --network=none --memory=3g --memory-swap=3g --cpus=1 ... /usr/bin/time -v ./scripts/lake.sh build <target>`, with `memory.max=3221225472`, no additional swap, `LEAN_NUM_THREADS=1`, a warm dependency closure, and only the named target's own generated artifacts removed before an immutable measurement. Diagnostic rows below were taken from the evolving working tree and therefore are design evidence rather than reproducible commit measurements; immutable rows name the exact commit whose source bytes were measured. GNU maximum RSS and cgroup peak remain separate because Docker Desktop's VM-level `ru_maxrss` can exceed the hard cgroup charge without an OOM kill. Unrelated host processes can perturb elapsed time and the VM's reported peak, so an anomalous near-bound result was retried under the unchanged ceiling; no pass was obtained by raising or disabling it.

The first breach was not a capacity shortage. Adding snapshot validation and a three-arm transition dispatcher to shared evaluator owners made declaration-free legacy fixtures re-reduce snapshot-only work for every operation. Moving the snapshot path into focused modules restored exact declaration-free delegation. A second breach showed that storing a generic `InternalCommutation.sortBy` computation inside a `Program` made every kernel-decided use of that program repeat the sort; explicit canonical snapshot atoms removed that amplification. A third class came from compound concrete fixtures: an Error interruption proposition repeatedly reconstructed and decided the program, start, purge, and recovery chain as one term. Named validated intermediate states and smaller proof owners preserved the same facts while bounding each reduction. The full-library probe then exposed a pre-existing, independent zero-margin `MessageStartConformance` monolith; splitting its unchanged laws by admission, closure, identity, and scenario made the aggregate cheap and gave every proof slice material headroom.

| Stage | Source boundary | Exact target | Exit | GNU maximum RSS | Cgroup peak | Deciding observation |
|---|---|---|---:|---:|---:|---|
| Shared-dispatch regression | Diagnostic working tree | `BpmnSemantics.SubProcessBoundaryTimerConformance` | 137 | 3,218,228 KiB | Hard 3 GiB ceiling reached | A declaration-free legacy target reduced the newly shared snapshot validator and dispatcher. |
| Focused fast-path correction, first retry | Diagnostic working tree | `BpmnSemantics.SubProcessBoundaryTimerConformance` | 0 | 2,687,260 KiB | Not retained | The same target passed under the unchanged ceiling after restoring a declaration-free path. |
| Broader minimal snapshot integration | Diagnostic working tree | `BpmnSemantics.CompensationEventSubProcessSnapshotIntegrationConformance` | 0 | 2,921,736 KiB | Not retained | Minimal end-to-end snapshot integration fit, at 3:11.90 elapsed. |
| Expanded unsplit snapshot integration | Diagnostic working tree | `BpmnSemantics.CompensationEventSubProcessSnapshotIntegrationConformance` | 1 | 3,101,876 KiB | Below the ceiling | The build retained only about 43 MiB of GNU-RSS margin and exposed false witnesses; the proof owner was split instead of accepting that margin. |
| First split lifecycle witness | Diagnostic working tree | `BpmnSemantics.CompensationEventSubProcessSnapshotLifecycleIntegrationConformance` | 1 | 2,747,188 KiB | Not retained | The root tuple failed only `programWellFormed`; the Error tuple had an invalid program, committed start, one retained snapshot, and a rejected interruption. |
| Generic-sort design | Diagnostic working tree | `BpmnSemantics.CompensationEventSubProcessSnapshotLifecycleIntegrationConformance` | 137 | 3,308,348 KiB | Hard 3 GiB ceiling reached | Embedding generic `InternalCommutation.sortBy` in the program repeated canonicalization in every kernel reduction. |
| Focused atom owner | Diagnostic working tree | `BpmnSemantics.SemanticProcess.CompensationEventSubProcessSnapshotInternalCommutation` | 0 | 683,948 KiB | Not retained | Snapshot-specific reservation, promotion, and purge atoms removed the shared-program sort. |
| Focused evaluator owner | Diagnostic working tree | `BpmnSemantics.SemanticProcess.TransitionAttempt` | 0 | 693,896 KiB | Not retained | The three-result attempt evaluator compiled before shared-owner restoration. |
| Focused trace owner | Diagnostic working tree | `BpmnSemantics.SemanticProcess.CompensationEventSubProcessSnapshotTransitionTrace` | 0 | 715,456 KiB | Not retained | Replay and closure stayed cheap before the final law set. |
| Explicit root canonicalization | Diagnostic working tree | `BpmnSemantics.CompensationEventSubProcessSnapshotRootClosureConformance` | 0 | 2,924,548 KiB | Not retained | An explicit canonical snapshot list replaced generic sort while preserving the root-order witness. |
| Compound Error witness | Diagnostic working tree | `BpmnSemantics.CompensationEventSubProcessSnapshotErrorInterruptionConformance` | 137 | 3,340,420 KiB | Hard 3 GiB ceiling reached | One compound proposition reconstructed the whole Error lifecycle. |
| Same-module Error fact split | Diagnostic working tree | `BpmnSemantics.CompensationEventSubProcessSnapshotErrorInterruptionConformance` | 137 | 3,320,832 KiB | Hard 3 GiB ceiling reached | Merely naming facts in the same owner did not stop repeated reduction. |
| Declaration-aware Error start fact | Diagnostic working tree | `BpmnSemantics.CompensationEventSubProcessSnapshotErrorInterruptionConformance` | 0 | 3,208,868 KiB | Below the ceiling | The pass had unsafe near-zero margin and was rejected as an adequate correction. |
| Explicit Error program state | Diagnostic working tree | `BpmnSemantics.CompensationEventSubProcessSnapshotErrorProgramConformance` | 0 | 1,902,468 KiB | Not retained | A separately validated program bounded the first reduction. |
| Explicit Error outcome state | Diagnostic working tree | `BpmnSemantics.CompensationEventSubProcessSnapshotErrorOutcomeConformance` | 0 | 2,549,344 KiB | Not retained | The interruption result was decided from the validated predecessor. |
| Explicit Error purge state | Diagnostic working tree | `BpmnSemantics.CompensationEventSubProcessSnapshotErrorPurgeConformance` | 0 | 2,545,464 KiB | Not retained | Snapshot removal was decided independently. |
| Explicit Error recovery state | Diagnostic working tree | `BpmnSemantics.CompensationEventSubProcessSnapshotErrorRecoveryConformance` | 0 | 2,545,616 KiB | Not retained | Recovery was decided independently. |
| Error aggregate after extraction | Diagnostic working tree | `BpmnSemantics.CompensationEventSubProcessSnapshotErrorInterruptionConformance` | 0 | 1,754,260 KiB | Not retained | The aggregate stopped owning the reductions later measured in five exact modules. |
| Bounded full-library probe | Diagnostic working tree | `BpmnSemantics` | 1 | 3,420,192 KiB | At least one child reached the hard ceiling | `ParallelUserTaskMetadataCompositionConformance` and `MessageStartConformance` were killed; `CommittedExecutionPublication` also exposed a type mismatch. Elapsed was 12:36.56. |
| Parallel-metadata isolated retry | Diagnostic working tree | `BpmnSemantics.ParallelUserTaskMetadataCompositionConformance` | 0 | 3,395,384 KiB | Below the hard process-tree charge | The retry confirmed a noisy pre-existing near-cap target; its established comparison baseline is 3,282,876 KiB. |
| Publication compatibility repair | Diagnostic working tree | `BpmnSemantics.CommittedExecutionPublication` | 0 | 3,252,168 KiB | Below the ceiling | The temporary compatibility repair compiled; restoring the shared base source later eliminated the need for it. |
| Focused snapshot lifecycle, final design | Diagnostic working tree | `BpmnSemantics.CompensationEventSubProcessSnapshotLifecycleIntegrationConformance` | 0 | 2,974,572 KiB | Below the ceiling | The focused API and exact legacy delegation removed shared amplification. |
| Focused snapshot root, final design | Diagnostic working tree | `BpmnSemantics.CompensationEventSubProcessSnapshotRootClosureConformance` | 0 | 2,929,356 KiB | Below the ceiling | The root proof remained the checkpoint's near-cap owner and required immutable remeasurement. |
| Focused snapshot Error aggregate, final design | Diagnostic working tree | `BpmnSemantics.CompensationEventSubProcessSnapshotErrorInterruptionConformance` | 0 | 2,712,404 KiB | Below the ceiling | Split predecessor owners bounded the aggregate. |
| Focused snapshot atomicity, final design | Diagnostic working tree | `BpmnSemantics.CompensationEventSubProcessSnapshotAtomicityConformance` | 0 | 1,944,748 KiB | Below the ceiling | Whole-command refusal and trace atomicity fit with material margin. |
| Focused snapshot aggregate, warm diagnostic | Diagnostic working tree | `BpmnSemantics.CompensationEventSubProcessSnapshotIntegrationConformance` | 0 | 626,000 KiB | Not retained | The aggregate became import-only rather than a second proof owner. |
| Focused trace after universal laws | Diagnostic working tree | `BpmnSemantics.SemanticProcess.CompensationEventSubProcessSnapshotTransitionTrace` | 0 | 670,172 KiB | 164,728,832 bytes | Result erasure, emitted-trace replay, noncommitted no-trace, no-trace no-lifecycle, and atomic-refusal laws remained cheap. |
| Focused command admission | Diagnostic working tree | `BpmnSemantics.SemanticProcess.CompensationEventSubProcessSnapshotCommandAdmission` | 0 | 629,084 KiB | Not retained | Validation runs once and declaration-free commands delegate exactly. |
| Focused commutation owner | Diagnostic working tree | `BpmnSemantics.SemanticProcess.CompensationEventSubProcessSnapshotInternalCommutation` | 0 | 667,264 KiB | Not retained | The extended atom vocabulary stays outside the shared `InternalStateAtom`. |
| Complete focused snapshot aggregate rebuild | Diagnostic working tree | `BpmnSemantics.CompensationEventSubProcessSnapshotIntegrationConformance` | 0 | 2,928,900 KiB | 2,665,435,136 bytes | The final design retained roughly 530 MiB of cgroup margin with no pressure or OOM event. |

The full-library probe's Message Start kill was reproducible enough to reject as host noise but variable enough to require retaining every retry. Before extraction, its earlier immutable row at `826231ab` was 3,116,780 KiB in 33.77 seconds. An observed failure reached 3,438,640 KiB. A partial three-dependency extraction passed at 3,151,392 KiB in 45.37 seconds, but a target-only retry was killed at 3,328,788 KiB with cgroup peak 3,221,225,472 bytes and `memory.events` recording `max=1641`, `oom=1`, `oom_kill=1`. After the first helper extraction, one run passed at 3,296,972 KiB with `max=2835` and no OOM, while an identical retry was killed at 3,345,072 KiB with `max=41975`, `oom=1`, `oom_kill=1`. Restoring the shared atom type while snapshot and cancellation hooks remained in shared owners produced a warm-dependency pass at 3,333,188 KiB with `max=6007`, followed by a target-only kill at 3,363,148 KiB with `max=7046`, `oom=1`, `oom_kill=1`. After every shared hook owner matched `HEAD`, a run that also rebuilt `Execution`, `Scenario`, and fixtures passed at 3,402,388 KiB with `max=452`, and a target-only retry passed at 3,226,700 KiB with `max=6319`; that was still effectively zero healthy margin. The accepted correction is commit `71b45760`, which split the unchanged laws and reduced the cold aggregate to 1,745,976 KiB by GNU time and 1,810,862,080 bytes by cgroup, with no pressure or OOM event.

| Immutable commit | Exact target | Elapsed | GNU maximum RSS | Cgroup peak | Verdict |
|---|---|---:|---:|---:|---|
| `71b45760` | `BpmnSemantics.MessageStartAdmissionConformance` | 9.54 s | 1,810,064 KiB | 1,410,764,800 bytes | Exit 0 |
| `71b45760` | `BpmnSemantics.MessageStartClosureConformance` | 6.87 s | 1,681,372 KiB | 1,240,563,712 bytes | Exit 0 |
| `71b45760` | `BpmnSemantics.MessageStartIdentityConformance` | 7.80 s | 1,539,460 KiB | 1,090,801,664 bytes | Exit 0 |
| `71b45760` | `BpmnSemantics.MessageStartScenarioConformance` | 6.56 s | 1,419,604 KiB | 971,304,960 bytes | Exit 0 |
| `71b45760` | `BpmnSemantics.MessageStartConformance` | 0.35 s | 618,008 KiB | 119,660,544 bytes | Exit 0 |
| `bd2bef03` | `BpmnSemantics.CompensationEventSubProcessSnapshotLifecycleIntegrationConformance` | 19.33 s | 2,050,248 KiB | 1,746,440,192 bytes | Exit 0 |
| `bd2bef03` | `BpmnSemantics.CompensationEventSubProcessSnapshotRootClosureConformance` | 33.59 s | 2,929,256 KiB | 2,704,228,352 bytes | Exit 0; above the 90% GNU-RSS disclosure threshold |
| `bd2bef03` | `BpmnSemantics.CompensationEventSubProcessSnapshotErrorProgramConformance` | 6.54 s | 1,755,796 KiB | 1,309,077,504 bytes | Exit 0 |
| `bd2bef03` | `BpmnSemantics.CompensationEventSubProcessSnapshotErrorReadyStateConformance` | 5.99 s | 1,898,152 KiB | 1,452,879,872 bytes | Exit 0 |
| `bd2bef03` | `BpmnSemantics.CompensationEventSubProcessSnapshotErrorOutcomeConformance` | 9.94 s | 2,545,104 KiB | 2,112,049,152 bytes | Exit 0 |
| `bd2bef03` | `BpmnSemantics.CompensationEventSubProcessSnapshotErrorPurgeConformance` | 11.64 s | 2,546,956 KiB | 2,115,612,672 bytes | Exit 0 |
| `bd2bef03` | `BpmnSemantics.CompensationEventSubProcessSnapshotErrorRecoveryConformance` | 10.55 s | 2,545,196 KiB | 2,112,237,568 bytes | Exit 0 |
| `bd2bef03` | `BpmnSemantics.CompensationEventSubProcessSnapshotAtomicityConformance` | 9.30 s | 1,894,208 KiB | 1,492,148,224 bytes | Exit 0 |
| `bd2bef03` | `BpmnSemantics.CompensationEventSubProcessSnapshotErrorInterruptionConformance` | 0.67 s | 642,024 KiB | 133,312,512 bytes | Exit 0 |
| `bd2bef03` | `BpmnSemantics.CompensationEventSubProcessSnapshotIntegrationConformance` | 0.47 s | 622,884 KiB | 121,970,688 bytes | Exit 0 |
| `1fda9213` | `BpmnSemantics.CompensationEventSubProcessSnapshotAdmissionConformance` | 6.92 s | 1,464,728 KiB | 1,085,808,640 bytes | Exit 0; checkpoint-review correction remeasurement |
| `1fda9213` | `BpmnSemantics.CompensationEventSubProcessSnapshotAtomicityConformance` | 11.81 s | 1,847,580 KiB | 1,538,920,448 bytes | Exit 0; checkpoint-review correction remeasurement |
| `1fda9213` | `BpmnSemantics.UserTaskMetadataConformance` | 13.36 s | 1,748,740 KiB | 1,412,497,408 bytes | Exit 0; checkpoint-review correction remeasurement |
| `6208f2e4` | `BpmnSemantics.CompensationEventSubProcessSnapshotLifecycleIntegrationConformance` | 20.52 s | 2,126,608 KiB | 1,911,873,536 bytes | Exit 0; second correction-audit remeasurement |
| `6208f2e4` | `BpmnSemantics.ServiceTaskIncidentRetryConformance` | 23.34 s | 2,432,344 KiB | 2,138,685,440 bytes | Exit 0; controlled proof-regression remeasurement |

These immutable rows are the acceptance evidence. The root-closure target retains about 493 MiB of cgroup headroom even though its GNU-RSS figure crosses the record's conservative 90% disclosure threshold; every other new snapshot owner is below that threshold. The first three checkpoint-review correction measurements and both second-audit remeasurements used their own commit-specific Linux cache after each named target had been prewarmed and only its own generated artifacts removed. All five reported `memory.events` values of zero for `high`, `max`, `oom`, `oom_kill`, and `oom_group_kill`. Their elapsed figures were plausible for the unchanged one-CPU lane, so host-contention retries were not needed. The later lifecycle measurement covers the quantified root/child/completion and regional-cancellation laws plus the pre-closure Timer discriminator; the Service Task incident measurement binds the exhaustive proof repair forced by declaration-aware admission. The governing correction is therefore structural rather than a larger allowance: focused snapshot admission, transition, trace, and commutation owners; exact declaration-free delegation laws; explicit validated predecessor states; and split proof owners. Raising the ceiling by 0.5 GiB would make the rejected generic-sort, compound-fixture, or zero-margin monolith designs appear green without removing their amplification, so the 3 GiB bound remains unchanged.

#### Parallel metadata and cyclic-consumer continuation

The first post-checkpoint full-library repair did not end with the snapshot owners. `ParallelUserTaskMetadataCompositionConformance` was killed under the same 3 GiB controller after the full-library probe made its accumulated exact-composition laws cold. An unchanged-limit isolated retry again ended with Lean code 137 after 46 seconds. The correction split admission, runtime, and closure laws into independent owners and made the original module an import-only aggregate; all 19 public theorem names were preserved. The first focused rows were already bounded—1,770,820 KiB for admission, 1,991,556 KiB for runtime, 2,530,968 KiB for closure, and 619,796 KiB for the aggregate—but the first complete build still touched the exact cgroup ceiling with `memory.events max=1940`, so the repair was not accepted on those focused exits alone.

That complete build exposed a second mechanism rather than another memory limit: four cyclic-control-flow consumers depended on the concrete shape of a private snapshot-bypassing evaluator. Splitting the parallel owner changed elaboration enough to expose the hidden dependency. The first correction failed because `fireWithoutCompensationSnapshots?` was private; the second chose `List ConditionalOutput` where the public contract requires `List ConditionalCandidate`; the third used `BpmnElementOrigin` where the route requires `BpmnSequenceFlowOrigin`. The accepted repair expressed the cyclic fixtures through public declaration-free constructor laws. The base cyclic target then passed, a complete test predicted and exposed the same issue in `CyclicControlFlowExecutionConformance`, and the same public-law correction repaired execution, reachability, and step-completeness without publishing or duplicating the private evaluator.

| Boundary | Exact target | Elapsed | GNU maximum RSS | Cgroup peak | Verdict |
|---|---|---:|---:|---:|---|
| Diagnostic split | `BpmnSemantics.ParallelUserTaskMetadataCompositionAdmissionConformance` | 13.17 s | 1,770,820 KiB | 2,125,529,088 bytes | Exit 0 |
| Diagnostic split | `BpmnSemantics.ParallelUserTaskMetadataCompositionRuntimeConformance` | 13.32 s | 1,991,556 KiB | 1,679,495,168 bytes | Exit 0 |
| Diagnostic split | `BpmnSemantics.ParallelUserTaskMetadataCompositionClosureConformance` | 19.02 s | 2,530,968 KiB | 2,274,824,192 bytes | Exit 0 |
| Diagnostic split | `BpmnSemantics.ParallelUserTaskMetadataCompositionConformance` | 0.67 s | 619,796 KiB | 213,323,776 bytes | Exit 0; import-only aggregate |
| Diagnostic cyclic repair | `BpmnSemantics.SemanticProcess.CyclicControlFlowConformance` | 3:19.41 | 2,040,052 KiB | 1,744,367,616 bytes | Exit 0 after three failed type/private-boundary attempts |
| Diagnostic cyclic family | base, execution, reachability, and step-completeness | 3:38.32 | 1,986,988 KiB | 1,733,664,768 bytes | Exit 0 |
| `223486be` | `BpmnSemantics.ParallelUserTaskMetadataCompositionAdmissionConformance` | 11.73 s | 1,802,080 KiB | 1,496,846,336 bytes | Exit 0; no pressure event |
| `223486be` | `BpmnSemantics.ParallelUserTaskMetadataCompositionRuntimeConformance` | 13.87 s | 2,076,884 KiB | 1,778,135,040 bytes | Exit 0; no pressure event |
| `223486be` | `BpmnSemantics.ParallelUserTaskMetadataCompositionClosureConformance` | 24.56 s | 2,439,560 KiB | 2,213,457,920 bytes | Exit 0; no pressure event |
| `223486be` | `BpmnSemantics.ParallelUserTaskMetadataCompositionConformance` | 1.41 s | 615,444 KiB | 211,906,560 bytes | Exit 0; import-only aggregate |
| `223486be` | `BpmnSemantics.SemanticProcess.CyclicControlFlowConformance` | 16.19 s | 2,087,352 KiB | 1,810,960,384 bytes | Exit 0; no pressure event |
| `223486be` | `BpmnSemantics.SemanticProcess.CyclicControlFlowExecutionConformance` | 5.53 s | 790,732 KiB | 394,293,248 bytes | Exit 0; no pressure event |
| `223486be` | `BpmnSemantics.SemanticProcess.CyclicControlFlowReachabilityConformance` | 3.99 s | 775,300 KiB | 390,721,536 bytes | Exit 0; no pressure event |
| `223486be` | `BpmnSemantics.SemanticProcess.CyclicControlFlowStepCompletenessConformance` | 3.44 s | 825,916 KiB | 441,712,640 bytes | Exit 0; no pressure event |

After those repairs, a complete Lean build passed in 6:01.61 at 3,128,672 KiB GNU RSS and 3,030,597,632 bytes cgroup peak with no pressure event. The complete Lean test also exited zero, but it reached the exact 3,221,225,472-byte cgroup ceiling and recorded `memory.events max=32353`; an exit-zero test at zero process-tree margin was treated as a design failure, not as closure evidence.

#### Whole-library near-cap audit and exact-pressure repair

The zero-margin complete test triggered an isolated census of every disclosed near-cap conformance target under the unchanged controller. Elapsed time was treated as noisy because unrelated host processes were active; cgroup ceiling events were not. Every suspicious exact-cap result was retried before selecting a structural change.

| Exact target | First GNU RSS / cgroup peak | Retry GNU RSS / cgroup peak | Classification |
|---|---:|---:|---|
| `SequentialMultiInstanceConformance` | 3,343,496 KiB / 3,121,868,800 bytes | 3,303,628 KiB / exact ceiling, `max=43` | Unsafe proof monolith |
| `FlowNodeOccurrenceLifecycleConformance` | 3,335,824 KiB / exact ceiling, `max=441` | 3,243,788 KiB / exact ceiling, `max=358` | Unsafe proof monolith |
| `RuntimeStateWellFormedConformance` | 3,106,528 KiB / 3,180,548,096 bytes | 3,133,532 KiB / exact ceiling, `max=33` | Unsafe proof monolith |
| `RuntimeStateActivityConformance` | 3,202,508 KiB / 3,112,075,264 bytes | 2,974,532 KiB / 2,994,561,024 bytes | Initially variable; exact ceiling with `max=120` after fixture narrowing proved it unsafe |
| `TimerStartConformance` | 2,953,420 KiB / 2,797,490,176 bytes | Not required | Retained near-cap disclosure |
| `MessagePayloadCatchConformance` | 3,070,632 KiB / 2,805,493,760 bytes | Not required | Retained near-cap disclosure |
| `SequentialMultiInstanceProgramBindingConformance` | 2,963,568 KiB / 2,795,372,544 bytes | Not required | Retained near-cap disclosure |
| `CallActivityConformance` | 3,200,492 KiB / 2,976,841,728 bytes | Not required | Retained near-cap disclosure |
| `CompensationEventSubProcessSnapshotRootClosureConformance` | 2,926,744 KiB / 2,950,213,632 bytes | 2,923,776 KiB / 2,715,987,968 bytes | Retained near-cap disclosure; unchanged-limit retry restored about 482 MiB cgroup margin |
| `TerminateEndEventConformance` | 2,813,976 KiB / 2,662,846,464 bytes | Not required | Retained near-cap disclosure |

The selected root mechanism was proof ownership, not file size or host capacity. Sequential Multi-Instance mixed runtime/lifecycle, refusal/bound, and canonical-JSON reductions around one shared lifecycle chain. Runtime well-formedness mixed ordinary invariant, Event-Based Gateway, and successor reductions, while its Activity owner still mixed boundary-Activity and sequential-controller reductions. Flow-node lifecycle imported thirteen semantic owners and evaluated every happy-path, projection-validity, cancellation, and rejection family in one target. Commit `da6e6477` moved unchanged declarations into sibling proof owners, preserved the original namespaces and import paths as declaration-free aggregates, and registered every aggregate in the import-only source guard. The only compile correction added the exact `TransitionTrace` import required by the moved `TracedStimulusResult` helpers.

| Immutable commit | Exact target | Elapsed | GNU maximum RSS | Cgroup peak | Verdict |
|---|---|---:|---:|---:|---|
| `da6e6477` | `BpmnSemantics.SequentialMultiInstanceRuntimeConformance` | 20.11 s | 2,509,372 KiB | 2,238,963,712 bytes | Exit 0; no pressure event |
| `da6e6477` | `BpmnSemantics.SequentialMultiInstanceRefusalConformance` | 10.21 s | 1,791,356 KiB | 1,443,962,880 bytes | Exit 0; no pressure event |
| `da6e6477` | `BpmnSemantics.SequentialMultiInstanceCanonicalJsonConformance` | 1.91 s | 833,720 KiB | 430,080,000 bytes | Exit 0; no pressure event |
| `da6e6477` | `BpmnSemantics.SequentialMultiInstanceConformance` | 0.65 s | 619,012 KiB | 213,352,448 bytes | Exit 0; import-only aggregate |
| `da6e6477` | `BpmnSemantics.RuntimeStateActivityOccurrenceConformance` | 18.29 s | 2,116,976 KiB | 1,838,526,464 bytes | Exit 0; no pressure event |
| `da6e6477` | `BpmnSemantics.RuntimeStateControllerConformance` | 16.00 s | 2,089,244 KiB | 1,793,007,616 bytes | Exit 0; no pressure event |
| `da6e6477` | `BpmnSemantics.RuntimeStateActivityConformance` | 0.55 s | 618,664 KiB | 213,245,952 bytes | Exit 0; import-only aggregate |
| `da6e6477` | `BpmnSemantics.RuntimeStateWellFormedInvariantConformance` | 14.62 s | 2,048,816 KiB | 1,740,648,448 bytes | Exit 0; no pressure event |
| `da6e6477` | `BpmnSemantics.RuntimeStateWellFormedEventRaceConformance` | 5.49 s | 1,390,688 KiB | 1,026,326,528 bytes | Exit 0; no pressure event |
| `da6e6477` | `BpmnSemantics.RuntimeStateWellFormedSuccessorConformance` | 14.57 s | 1,832,044 KiB | 1,526,501,376 bytes | Exit 0; no pressure event |
| `da6e6477` | `BpmnSemantics.RuntimeStateWellFormedConformance` | 0.55 s | 619,360 KiB | 214,175,744 bytes | Exit 0; import-only aggregate |
| `da6e6477` | `BpmnSemantics.FlowNodeOccurrenceLifecycleDeltaConformance` | 2.21 s | 639,644 KiB | 234,483,712 bytes | Exit 0; no pressure event |
| `da6e6477` | `BpmnSemantics.FlowNodeOccurrenceLifecycleHappyPathConformance` | 13.87 s | 2,172,304 KiB | 1,850,167,296 bytes | Exit 0; no pressure event |
| `da6e6477` | `BpmnSemantics.FlowNodeOccurrenceLifecycleProjectionConformance` | 9.89 s | 1,699,276 KiB | 1,362,251,776 bytes | Exit 0; no pressure event |
| `da6e6477` | `BpmnSemantics.FlowNodeOccurrenceLifecycleProgramValidityConformance` | 16.10 s | 2,333,280 KiB | 2,064,138,240 bytes | Exit 0; no pressure event |
| `da6e6477` | `BpmnSemantics.FlowNodeOccurrenceLifecycleCancellationConformance` | 14.06 s | 2,086,828 KiB | 1,787,998,208 bytes | Exit 0; no pressure event |
| `da6e6477` | `BpmnSemantics.FlowNodeOccurrenceLifecycleRejectionConformance` | 1.07 s | 751,784 KiB | 346,161,152 bytes | Exit 0; no pressure event |
| `da6e6477` | `BpmnSemantics.FlowNodeOccurrenceLifecycleConformance` | 0.66 s | 620,612 KiB | 214,188,032 bytes | Exit 0; import-only aggregate |

The repaired complete Lean test passed in 5.18 seconds at 939,468 KiB GNU RSS and 1,266,122,752 bytes cgroup peak; the complete Lean build passed in 18.18 seconds at 2,308,556 KiB and 1,988,366,336 bytes. Both reported zero for `high`, `max`, `oom`, `oom_kill`, and `oom_group_kill`. These warm whole-graph times are not compared with the earlier multi-minute runs because their cache state differed and the host was active; the material result is the absence of pressure with more than 1 GiB of cgroup margin. The ceiling remains 3 GiB. Increasing it would have hidden four independently reproduced ownership defects and removed the oracle that forced their common structural correction.

#### Snapshot invariant and cgroup acceptance correction

The second semantic-checkpoint correction audit found that the child-entry, bounded-child-entry, and scope-completion lifecycle theorems established only the expected snapshot collection shape, not the quantified aggregate state invariant of every applied successor. Commit `7b3ca41f` adds a fail-closed `applyValidSnapshotSuccessor` boundary and separate `attemptInternalOperation_enterScope_applied_stateValid`, `attemptInternalOperation_enterBoundedScope_applied_stateValid`, and `attemptInternalOperation_completeScope_applied_stateValid` theorems. Extracting that boundary into `InternalOperationAttempt.lean` was required when `Transition.lean` reached 811 nonblank lines; the transition owner returned to 777 without weakening the 800-line review target.

The same audit exposed a control-plane gap: GNU RSS rows alone could not reject a command that exited zero at the exact cgroup ceiling. [`lean-memory-acceptance.ts`](../scripts/lean-memory-acceptance.ts) now rejects a nonzero command exit, `memory.peak` greater than or equal to 3,221,225,472 bytes, or any nonzero `high`, `max`, `oom`, `oom_kill`, or `oom_group_kill` event. Its tests include the exact-bound, exit-zero adversary. A 13:28.92 diagnostic full test is retained as rejected evidence: it exited zero at 3,157,844 KiB GNU RSS but reached the exact cgroup ceiling with `max=103`.

The unchanged-limit isolation pass then exercised every cold consumer implicated by that diagnostic result. Elapsed time is reported but not compared because other host processes were active; the cgroup peaks and zero-event results decide acceptance.

| Diagnostic target | Elapsed | GNU maximum RSS | Cgroup peak | Memory events |
|---|---:|---:|---:|---|
| `TerminateEndEventConformance` | 36.54 s | 2,928,144 KiB | 2,793,156,608 bytes | All zero |
| `SequentialMultiInstanceProgramBindingConformance` | 33.46 s | 3,013,432 KiB | 2,875,363,328 bytes | All zero |
| `CallActivityConformance` | 27.19 s | 3,294,196 KiB | 3,091,030,016 bytes | All zero |
| `TimerStartConformance` | 24.65 s | 3,079,472 KiB | 2,874,978,304 bytes | All zero |

| Standing watch set | Remeasurement trigger | Required response |
|---|---|---|
| `CallActivityConformance`, `SequentialMultiInstanceProgramBindingConformance`, `TimerStartConformance`, and `TerminateEndEventConformance` | A capsule adds or changes a reduction family consumed by any listed target | Remeasure each affected exact target under the unchanged 3 GiB ceiling with every controlled memory event zero. If margin is unsafe, first isolate asynchronous elaboration and repeated fixture construction as in the E1 experiment above; use local synchronous elaboration, kernel-locked literal fixtures, and reusable laws where they remove measured amplification. Split by proof ownership only for residual accumulation; a single over-bound obligation needs its own reduction repair. Do not budget fixture sites or raise the limit. |

The accepted complete receipts are immutable at `7b3ca41f`. The warm-closure test ran in 0.65 seconds at 120,048 KiB GNU RSS and 136,794,112 bytes cgroup peak; its log SHA-256 is `aaec15959030baf4dae2b0327c50fdb5b8a765e3f80ad93b82629f4339833d58`. The warm-closure build ran in 0.45 seconds at 119,768 KiB and 36,450,304 bytes; its log SHA-256 is `748a58696eab89272da962e2f88d514187abf2aab87a66985dfedc4e14ee0a13`. Both exited zero with every controlled memory event zero. The validator binds these receipts as closure evidence while the rejected cold diagnostic and isolated cold-consumer rows remain visible; no larger limit or exit-code-only exception was introduced.

#### External-review fixture cost binding

The external-review corrections were remeasured at immutable commit `eed22392635ba31cc485367a0a1bf460b6d1c8f4` on 2026-09-06 under the identical pinned image and 3 GiB, no-additional-swap, one-CPU protocol above. The corrected [User Task fixture](../BpmnSemantics/UserTaskInteractionConformance.lean) and [Compensation collision fixture](../BpmnSemantics/CompensationTriggerHandlerRuntimeConformance.lean) were each rebuilt alone after warming their dependencies and moving only that target's generated artifacts out of the isolated cache. Both exited zero with zero swaps, every cgroup memory-event counter zero, and zero memory-pressure totals; neither reaches the 90% disclosure threshold. These records replace the stale source bindings in the executable cost registry without changing either fixture or the memory policy.

| Corrected target at `eed22392` | Elapsed | GNU maximum RSS | Cgroup peak | Output SHA-256 |
|---|---:|---:|---:|---|
| `UserTaskInteractionConformance` | 7.14 s | 1,516,764 KiB | 1,154,441,216 bytes | `ac68ba3162e016bb0893f29b7b19c45b039915b48fcf8532420a8140c4378002` |
| `CompensationTriggerHandlerRuntimeConformance` | 4.78 s | 1,292,220 KiB | 909,676,544 bytes | `2d07cdd47b74a2bab9fed4dbf6c477cc49f67613e8784f97ca1f2b0815bf5303` |

The composed declaration-dispatch correction was remeasured at immutable commit `8d3927f6bf7c1336ac69a6fb5c7e07d1af24194b` on 2026-09-06 under the same pinned image, one CPU, network isolation, and unchanged 3 GiB/no-additional-swap controller. After warming the dependency closure, each exact target's eight generated artifacts were moved aside and rebuilt alone. The changed refusal owner and all four standing-watch consumers exited zero with zero swaps, memory-event counters, and memory-pressure totals. The largest cgroup peak leaves 1,994,100,736 bytes below the limit. Elapsed time remains host-load context; no uncontended performance trend is claimed.

| Dispatch-correction target at `8d3927f6` | Elapsed | GNU maximum RSS | Cgroup peak | Output SHA-256 |
|---|---:|---:|---:|---|
| `ActivityDataInputOutputRefusalConformance` | 2.41 s | 731,312 KiB | 328,609,792 bytes | `e88d8a55197363f4eac99e11aabc090f1ac44dd761782be5cc1115d7acda342f` |
| `CallActivityConformance` | 23.17 s | 1,346,800 KiB | 1,040,437,248 bytes | `022df2035793221edf5a2b2d70bdb2fa5b1d1d5db3e6d4d0f77948d8a4e8eb48` |
| `SequentialMultiInstanceProgramBindingConformance` | 29.75 s | 1,548,508 KiB | 1,195,859,968 bytes | `55e8c9f1b624321cc89a041c1afdbff63ca54cca6c209efb0698b5a7707674bf` |
| `TimerStartConformance` | 22.32 s | 1,416,936 KiB | 1,103,904,768 bytes | `b9f654b21521188648308ae5e0028767eb161fe6b22fa999f96e9fbeb7a05b15` |
| `TerminateEndEventConformance` | 31.07 s | 1,543,944 KiB | 1,227,124,736 bytes | `2a3ba7d46f11f2f67117cf66bbf993af57d1b1c2b5ba9b3e53a9a4b958691ea2` |

#### Compensation trigger-handler module-cost binding

The compensation trigger-handler proof owners were initially measured from immutable commit `ef739572` with a warm dependency closure and only each measured target's own generated artifacts removed. Other host processes were active, so elapsed time is recorded but not compared; every command exited zero without swap, and every cgroup `high`, `max`, `oom`, `oom_kill`, and `oom_group_kill` event remained zero.

| Target | Elapsed | GNU maximum RSS | Cgroup peak |
|---|---:|---:|---:|
| `CompensationTriggerHandlerRuntimeContractConformance` | 0.68 s | 705,308 KiB | 176,893,952 bytes |
| `CompensationTriggerHandlerRuntimeConformance` | 2.93 s | 1,086,024 KiB | 710,311,936 bytes |
| `CompensationTriggerHandlerTransitionConformance` | 11.19 s | 1,892,392 KiB | 1,473,748,992 bytes |
| `CompensationTriggerHandlerCompletionConformance` | 27.28 s | 2,966,900 KiB | 2,705,903,616 bytes |
| `CompensationActivityRetentionConformance` | 6.68 s | 1,916,172 KiB | 1,468,231,680 bytes |
| `CompensationEventSubProcessSnapshotAtomicityConformance` | 6.55 s | 1,977,500 KiB | 1,534,824,448 bytes |

The completion owner crosses the conservative 90% GNU-RSS disclosure threshold and is therefore listed in the executable near-cap registry. Its cgroup peak retained 515,321,856 bytes of hard-limit headroom with no pressure event. The fixed 3 GiB ceiling remains unchanged; the owner is viable but must not absorb another proof family without remeasurement or structural separation.

Commit `3020899b` added the separate cancellation conformance owner. Its immutable cold-target measurement passed in 0.58 seconds at 614,680 KiB GNU RSS and 175,321,088 bytes cgroup peak. Because the changed relation became a dependency of the near-cap completion owner, that target was remeasured rather than treated as unchanged: the first run passed in 27.48 seconds at 2,787,884 KiB and 2,478,338,048 bytes, but crossed below the disclosure threshold relative to the preceding record, so it was repeated. The conservative retained run passed in 27.07 seconds at 2,938,028 KiB and 2,623,070,208 bytes. Both completion runs and the cancellation run had no swap or controlled pressure/OOM event. The completion owner therefore remains disclosed and retains 598,155,264 bytes of cgroup headroom under the unchanged ceiling.

Correction commit `7126d5db` added the nonempty-trigger validity witness to `CompensationTriggerHandlerTransitionConformance`, so that target was remeasured against the immutable source revision. The first cold-target run passed in 11.89 seconds at 1,833,352 KiB GNU RSS and 2,024,046,592 bytes cgroup peak, with 90% CPU allocation and 1,570 major page faults. The identical retry passed in 11.19 seconds at 1,892,392 KiB and 1,473,748,992 bytes, with 98% CPU allocation and no major page fault; that more comparable retry supplies the executable row, while this paragraph retains the first run's higher process-tree charge. Both runs had no swap and zero `high`, `max`, `oom`, `oom_kill`, and `oom_group_kill` events. The target remains below the near-cap threshold under the unchanged 3 GiB ceiling.

#### Compensation source checkpoint proof-owner repair

The first compensation source checkpoint implementation at immutable commit `02cf3d3a` placed all kernel-reduced admission, JSON, compatibility, and definition-binding assertions in `BpmnSemantics.CompensationSourceLoweringConformance`. Its first bounded container build also had to warm stale dependencies, so it is not an accepted cold-target cost row, but the target's own elaboration then ran for 734 seconds and exited `137` under the fixed 3 GiB process-tree ceiling. The last observation before termination recorded `memory.current=3,220,975,616`, `memory.peak=3,221,225,472`, and `memory.events max=4,108,930`; `high`, `oom`, and `oom_kill` were zero at that observation, but the final event file was not captured and is not inferred.

The first repair separated shared fixtures, admission, JSON, compatibility, and binding owners. The admission, JSON, compatibility, and import-only aggregate targets passed, but the binding owner still collected fifteen independent kernel decisions in one file and exited `137` after 197 seconds. Splitting conjunctions into named theorems had therefore not repaired the mechanism: the reduction work still accumulated under one proof owner.

Commit `b4544cf6` splits the binding obligations again by invariant family: valid Program shape, exact parser-graph references, trigger and restored-binding identity, storage-limit joins, and execution-limit joins. Every original obligation remains kernel checked, while the binding and checkpoint aggregates become import-only. Raising the memory ceiling was rejected because it would hide proof-ownership coupling and remove the existing pressure signal without creating a stable bound.

All ten accepted measurements below use immutable commit `b4544cf6`, the pinned `bpmn-lean-audit:v4.31.0-arm64` image with image ID `sha256:4df22c7a1ec8ba8c4c2795fdea1269d7b9cadade4e65b22e595047509aa27950`, one CPU, no network, the unchanged 3 GiB memory and no-additional-swap bounds, a warm dependency closure, and removal only of the named target's generated artifacts. Other machine processes were active, so elapsed time is retained as context rather than treated as a regression signal; CPU allocation remained between 86% and 99%, and no retry was required.

| Target | Elapsed | GNU maximum RSS | Cgroup peak |
|---|---:|---:|---:|
| `CompensationSourceAdmissionConformance` | 20.54 s | 2,863,952 KiB | 2,486,214,656 bytes |
| `CompensationSourceJsonConformance` | 12.15 s | 2,326,880 KiB | 1,910,824,960 bytes |
| `CompensationSourceCompatibilityConformance` | 0.69 s | 667,864 KiB | 156,762,112 bytes |
| `CompensationSourceBindingReferenceConformance` | 31.89 s | 2,725,808 KiB | 2,412,118,016 bytes |
| `CompensationSourceBindingTriggerConformance` | 20.36 s | 2,379,680 KiB | 2,019,799,040 bytes |
| `CompensationSourceBindingStorageLimitConformance` | 33.68 s | 2,701,156 KiB | 2,419,683,328 bytes |
| `CompensationSourceBindingExecutionLimitConformance` | 25.21 s | 2,574,272 KiB | 2,271,854,592 bytes |
| `CompensationSourceBindingValidProgramConformance` | 11.43 s | 1,833,660 KiB | 1,414,615,040 bytes |
| `CompensationSourceBindingConformance` | 0.50 s | 635,228 KiB | 127,651,840 bytes |
| `CompensationSourceLoweringConformance` | 0.59 s | 635,272 KiB | 129,077,248 bytes |

Every accepted target exited zero with zero swap and every controlled `low`, `high`, `max`, `oom`, `oom_kill`, `oom_group_kill`, and `sock_throttled` event at zero. `CompensationSourceAdmissionConformance` alone crosses the GNU-RSS 90% disclosure threshold and is now ratcheted as near-cap; its cgroup peak retains exactly 735,010,816 bytes of headroom. The two failed layouts remain part of the evidence because the hard ceiling exposed proof-reduction ownership, not source-file length or missing memory.

#### Compensation start-capacity assurance

Commit `8ab3ba5f` extends `CompensationSourceCompatibilityConformance` with Program-derived start admission and changes its source bytes, so the old 667,864 KiB row at `b4544cf6` cannot remain its executable ratchet. The replacement uses the same pinned image, one CPU, no network, warm dependency closure, exact-target artifact removal, 3,221,225,472-byte memory limit, and zero additional swap.

The first concrete boundary used 7,510 and 7,511 ASCII value bytes. Production encoders produced snapshot/execution pairs of 8,192/18,257 and 8,193/18,259 bytes; the target exited `137` at `memory.peak=3,221,225,472`, with `oom=1` and `oom_kill=1`. Removing that value case and retaining the 875/876-byte identity boundary, whose pairs were 4,131/20,467 and 4,135/20,487 bytes, reproduced the same exact-ceiling OOM. A compact literal representation and then a single exact-side proposition each reproduced the exact-ceiling OOM, excluding source generation, conjunction size, and the rejected half as causes.

Removing both long concrete reductions passed under the unchanged limit in 10.80 seconds at cgroup peak 2,082,803,712 bytes with every controlled pressure/OOM event zero. The accepted design retains the universal arbitrary-start implication, production encoders, ordinary command-path state preservation, and escaped/multibyte decisions in Lean; independently written TypeScript and Product 1 witnesses own the full inherited-limit exact-fit/+1 cases. Neither a larger limit nor `native_decide` is selected.

The final cold-target build with short exact/one-under representative decisions passed in 15.00 seconds at 1,938,140 KiB GNU RSS and 2,324,123,648 cgroup bytes. `memory.max=3,221,225,472`, `memory.swap.max=0`, and every controlled event was zero. The output SHA-256 is `9f4d36fbf9c61fcb1a3525478c8c4a557b7752dce4a47af3a2c46af4cd24fb76`; this immutable-source measurement supplies the replacement executable row and remains below the near-cap threshold. The [start-data specification](capsules/COMPENSATION-DURABILITY-START-DATA-REPAIR-SPEC.md#lean-assurance-correction-after-the-hard-ceiling) owns the semantic assurance split.

Cold-review correction commit `05244cf7` routes that short exact/one-under decision through ordinary command admission and therefore changes the target bytes again. After warming the dependency closure and recoverably moving only the target's seven generated products out of the cache, the exact committed source rebuilt under the same fixed ceiling in 14.78 seconds at 2,023,220 KiB GNU RSS and 2,228,940,800 cgroup bytes. `memory.max=3,221,225,472`, `memory.swap.max=0`, every controlled event and both pressure totals were zero, and the build emitted no signal. The source SHA-256 is `359f8b0c3747a6b5d91919c4b20456ded8bb5980ef5e22caebb0fc1f788e0d58` and the output SHA-256 is `506a17fa0c72ad1a5fd0dcbe3db3030ab12b6ea9a884af451901e1a9ae997b66`; this measurement replaces only the executable ratchet row, while every preceding OOM and design-disposition fact remains retained above.

#### Message key-correlation population module-cost binding

The answer-free two-instance population conformance target was measured from immutable commit `b39031028942b005452179f98e7dc46ff912106d` on 2026-09-03 under the same pinned image, one-CPU, one-Lean-thread, no-network, 3 GiB memory, and no-additional-swap protocol. The exact commit archive was bind-mounted read-only with a separate commit-specific Linux cache; after a successful target prewarm, only the target's eight `.olean`, `.ilean`, hash, trace, C, and setup products were recoverably moved out of that cache. `BpmnSemantics.EnginePopulationScenarioConformance` then rebuilt in 0.85 seconds at 669,784 KiB GNU RSS and 257,982,464 cgroup bytes, with 72% CPU allocation, zero swap, every controlled memory event zero, and both pressure totals zero. Other host processes were active, so elapsed time is retained only as context. The source SHA-256 is `41a697fbc7b70acb4a836981e8a848dd60c76b496cf6b7b85aad21b08d60af82` and the output SHA-256 is `8f3f61f4752786d47d01f9b56dcba1320c14b9805d9f3b06f559ff28fb16f798`. The target remains far below the near-cap disclosure threshold; capsule churn and comparative reflection remain closure obligations rather than being inferred from this module measurement.

#### Runtime invariant and committed-initialization correction

On 2026-09-06, the changed runtime/Activity witnesses and new regional cleanup/quiescence witnesses were measured from immutable commit `0958697d26126cbffcfdb889a92dd03b10adacf7`. The [initialization repair](capsules/RUNTIME-INITIALIZATION-ASSURANCE-REPAIR-SPEC.md) was measured both there and after the proof-preserving `Elab.async false` correction at `6d231a2bb71dff679bff13497111f402083f2464`. The exact commit archives used the same pinned image, one CPU, one Lean thread, no network, 3 GiB memory ceiling, and no additional swap. Each measurement retained the warm dependency closure and invalidated only the target's eight owned products.

| Module | Commit | Elapsed | GNU RSS | Cgroup peak | Output SHA-256 |
|---|---|---:|---:|---:|---|
| `ScopeCancellationConformance` | `0958697d` | 0.88 s | 623,244 KiB | 189,927,424 bytes | `d162a04a70f04000dc6877f0f5cbd2f3fe8f5cade3aa14880357b467ad7d8515` |
| `ScopeCompletionConformance` | `0958697d` | 0.54 s | 587,916 KiB | 152,301,568 bytes | `52f24f079f3bef04d1e2b038704f71267c96217c0a169699d50abd430a51873f` |
| `RuntimeStateActivityOccurrenceConformance` | `0958697d` | 14.63 s | 1,212,052 KiB | 873,422,848 bytes | `a65297d70a42e0e53e2d00271a16ee7cf36c40dadc15e8c54fa15cfc4e9a61ce` |
| `RuntimeStateWellFormedInvariantConformance` | `0958697d` | 21.72 s | 1,164,872 KiB | 841,728,000 bytes | `687933e7a55093f01db5da0fd3ca8ed3924d0c00b771ca7214153dedfc99ff9a` |
| `RuntimeStateInitializationConformance` | `0958697d` | 17.12 s | 2,888,164 KiB | 2,631,020,544 bytes | `2383b8f00a966eb86252ca58f31b8a05dbfae61ff6606aac2288ab92477a8873` |
| `RuntimeStateInitializationConformance` | `6d231a2b` | 15.20 s | 1,013,076 KiB | 658,264,064 bytes | `391e31bde64e3c0d8a3902d2cce9ab503fa8b087c15ddfb33628238fc50ac311` |

Every measured target passed with zero swap, zero controlled memory events, and zero pressure totals. Synchronous initialization elaboration preserves every declaration, proposition, and proof while reducing the immutable cgroup peak from 2,631,020,544 to 658,264,064 bytes; this removes its near-cap classification without weakening kernel checking. The other four measurements replace or add the source-bound executable rows in [the module-cost record](../scripts/lean-module-cost.ts).

The preceding complete 304-module library command exited zero in 19:32.31 but failed the strict resource-acceptance rule, with 3,102,096 KiB GNU RSS, a 3,221,225,472-byte cgroup peak, 2,108 `max` events, no OOM/kill or swap, and 1,123,162 microseconds in each pressure total. That whole-library run used the maintained candidate Lean source tree with warm prior outputs; it is not a cold immutable per-module measurement. The final incremental library gate also passed after synchronous initialization elaboration. Whole-library pressure is retained separately from the zero-event module measurements; neither result closes general invariant preservation or the remaining review findings.

The standing-watch trigger above requires remeasurement of all four consumers of the changed runtime reduction families. The immutable `6d231a2b` baselines below reproduced high resident-set cost; commit `fe934e3cd2959d0528a4657c41d620e7054c6f34` adds only local `Elab.async false` to those four files, preserving every preceding declaration, proposition, and proof byte. The final cold-target measurements use that immutable commit and the same warm-dependency, exact-target invalidation protocol. Every row exited zero with zero swap and controlled memory events. Baseline pressure totals were 177,227 microseconds for Call Activity, 1,718 for Sequential Multi-Instance, zero for Timer Start, and 14,408 for Terminate; every final pressure total was zero. Elapsed times are contextual because other host processes were active.

| Module | Commit | Elapsed | GNU RSS | Cgroup peak | Output SHA-256 |
|---|---|---:|---:|---:|---|
| `CallActivityConformance` | `6d231a2b` | 36.31 s | 3,139,004 KiB | 3,010,551,808 bytes | `1d61f482a41a35654d284bb359980abc5a7b03b8e9c5d1588501ec2a0352763f` |
| `CallActivityConformance` | `fe934e3c` | 40.43 s | 1,344,932 KiB | 1,025,175,552 bytes | `e85d49a7c8ab2d9320a30cb8a1cec782d285f4deaed71ad8a85d1220e427726d` |
| `SequentialMultiInstanceProgramBindingConformance` | `6d231a2b` | 42.88 s | 3,032,140 KiB | 2,890,510,336 bytes | `9d49f686fba3b7b1c365f7106c0692594cbb94abc63ff25d96bad7d3f3b247f9` |
| `SequentialMultiInstanceProgramBindingConformance` | `fe934e3c` | 31.96 s | 1,554,672 KiB | 1,252,052,992 bytes | `294c7305f13181e9c05462ecea3e868e5a5e8300c4a4b355107ed948dcaf448f` |
| `TimerStartConformance` | `6d231a2b` | 24.79 s | 2,904,536 KiB | 2,662,248,448 bytes | `9bda3af0c971803755ead6626aaa32fc56db2f889cc7f82a1f002285f7a385fe` |
| `TimerStartConformance` | `fe934e3c` | 22.93 s | 1,418,872 KiB | 1,107,382,272 bytes | `378f15a5fc2e38db5c155192cd7eab985adfd89faa627c97d594646cc18bf2c6` |
| `TerminateEndEventConformance` | `6d231a2b` | 35.93 s | 3,056,104 KiB | 2,915,860,480 bytes | `9f22ca33b1151fabaa322eda2b8b4406ef07b78425212e6a52d431b63f7fee0d` |
| `TerminateEndEventConformance` | `fe934e3c` | 33.14 s | 1,541,560 KiB | 1,273,778,176 bytes | `998c8820405c83f6964ba32af75965f27f0cd44249dfbe4a02b127e1729bc3ff` |

The four final measurements replace their executable module-cost rows and remove their near-cap disclosures; the standing-watch set and its trigger remain in force. A complete 304-module library pass on the final maintained Lean source bytes passed in 4:13.63 at 2,422,024 KiB GNU RSS and 2,182,316,032 cgroup bytes, with zero controlled memory events, pressure totals, and swap. The immutable `fe934e3c` archive then passed both complete commands after the four isolated cold-target runs, reusing their outputs: `./scripts/lake.sh test` took 0.75 seconds at 121,676 KiB and 41,410,560 cgroup bytes, output SHA-256 `6c53dad040c90367e6583c9a37d2c5280034e1adf837a205778b077b75139d40`; `./scripts/lake.sh build` took 0.44 seconds at 121,680 KiB and 41,287,680 cgroup bytes, output SHA-256 `bcbbf734a71ec4050d6aa326781f706485403162ed088573c5c34673008ff5b0`. Both warm complete receipts exited zero with all controlled memory events, pressure totals, and swap zero and replace the accepted tuples in [the cgroup acceptance record](../scripts/lean-memory-acceptance.ts). The rejected exact-ceiling result remains visible above; the fixed 3 GiB limit is unchanged.

#### Internal closure atomicity correction

On 2026-09-06, the [atomicity repair](capsules/INTERNAL-CLOSURE-ATOMICITY-REPAIR-SPEC.md) was calibrated from immutable commit `e9c9db0fd20f3c969fc2783f3b3554797eeea5cc`. The ten changed conformance owners and all four standing-watch consumers were measured, with Timer Start counted once. The pinned image, one CPU, one Lean thread, no network, fixed 3 GiB ceiling, and no additional swap remain unchanged. Each measurement used a warm dependency closure and invalidated only the exact target's eight generated products.

| Module | Elapsed | GNU RSS | Cgroup peak | Output SHA-256 |
|---|---:|---:|---:|---|
| `EventBasedGatewayConformance` | 17.19 s | 2,721,196 KiB | 2,460,598,272 bytes | `6b744ba0a8b9e813db4a1f26ca798b84eb362c37c467756d8feddbd4e1d97135` |
| `InclusiveGatewayConformance` | 21.32 s | 2,813,784 KiB | 2,584,272,896 bytes | `c9404e25bc35a0d65ce1a2dca490af039909791c5f62689818a8ec654855a239` |
| `InternalClosureAtomicityConformance` | 12.95 s | 1,217,720 KiB | 883,953,664 bytes | `9bec1eb0ee3b4178b0390457b539127fc8021fcb5aa57cf8c751f6da15f03757` |
| `InternalCommutationConformance` | 8.73 s | 1,923,892 KiB | 1,592,741,888 bytes | `c0093d0e486dbe71c00a4ce082ff0bb57be12f630139e58fb3ed0bec354b5067` |
| `MessageStartClosureConformance` | 6.92 s | 1,699,144 KiB | 1,361,661,952 bytes | `2419fea9731c936d1a2772abe0238d04deee80d171cfd746e9330029c1336dc4` |
| `SemanticProcess.CyclicControlFlowConformance` | 9.87 s | 1,892,268 KiB | 1,614,888,960 bytes | `7697ee346c1ebcbfe91d3e052d50f43a30da4e28de7c07f34c14d833a363a064` |
| `SemanticProcess.CyclicControlFlowExecutionConformance` | 1.90 s | 806,972 KiB | 421,711,872 bytes | `8c82281a4bf8e6761dbf398c6b50ac9ab40b786976493b4a11c259e37e0b18e9` |
| `SemanticProcess.CyclicControlFlowReachabilityConformance` | 3.58 s | 842,656 KiB | 461,123,584 bytes | `c179480ff546575e49b236613b97c24b3e6bfe7135bd1648d51581f94e5e52b8` |
| `TimerStartConformance` | 20.95 s | 1,416,564 KiB | 1,101,373,440 bytes | `e1bcd9937049ad1da7fd0b17536eb32e5507a417f96634dd7c78fdc2c079a901` |
| `UserTaskMetadataConformance` | 8.91 s | 1,720,408 KiB | 1,392,812,032 bytes | `281dae39127f6a7950827ee708d96c385850dfdb883e65545d552c6989f50a4e` |
| `CallActivityConformance` | 22.33 s | 1,350,512 KiB | 1,054,326,784 bytes | `90ff47c5dca30e9b9765f55ae9f75dcc79f16234e067837d29a130389f620d2d` |
| `SequentialMultiInstanceProgramBindingConformance` | 26.90 s | 1,550,520 KiB | 1,193,320,448 bytes | `74a4ef07af87fe1b793ff7e84ee279e83012b35ced0e22a7e8fb6c679848f452` |
| `TerminateEndEventConformance` | 30.44 s | 1,541,904 KiB | 1,260,503,040 bytes | `cf3b08d8dec637b59b1934438ef5e6aada8dc607c71bceae25f19e70a073cd93` |

Every module exited zero with zero swap, controlled memory events, and pressure totals. The highest cgroup peak was 2,584,272,896 bytes, leaving 636,952,576 bytes below the unchanged ceiling; no measured module reaches the GNU-RSS disclosure threshold. These measurements replace the exact source-bound tuples in [the module-cost record](../scripts/lean-module-cost.ts). Elapsed readings remain host-load context.

The preceding complete maintained-source library build passed in 18:58.10 at 2,988,396 KiB GNU RSS and 2,909,528,064 cgroup bytes, with zero controlled memory events and swap. Its pressure totals were 230,056 microseconds; that nonzero pressure is retained separately from the zero-pressure immutable measurements. This build used the candidate source that became `1c8510ea`, before the subsequent comment-only cleanup, and is not an isolated immutable module measurement.

After calibration, the immutable archive passed both complete commands with warm outputs: `./scripts/lake.sh test` took 0.46 seconds at 121,408 KiB GNU RSS and 41,607,168 cgroup bytes, output SHA-256 `5fa968103e69ea2b59b487fccfe92da90795ac0ecb94a672aed7248f70e0f94a`; `./scripts/lake.sh build` took 0.34 seconds at 121,644 KiB and 41,705,472 cgroup bytes, output SHA-256 `9d79233d5ddf6e2906a58cd18d2425a18ea73a5549a691b59bdb4930ed76c96f`. Both exited zero with zero controlled memory events, pressure totals, and swap and replace [the cgroup acceptance record](../scripts/lean-memory-acceptance.ts). These are checkpoint resource receipts; Temporal classification, complete Product 1 integration, and governed review remain separate obligations.

#### Package-wide theorem elaboration memory correction

On 2026-09-08, the composed arming footprint checkpoint's complete working-tree library build after `539ec9ac` compiled all 322 jobs successfully but failed resource acceptance. It took 27:13.68 at 2,990,012 KiB GNU RSS, reached the exact 3,221,225,472-byte cgroup ceiling, recorded 539 `max` events and 1,880,779 microseconds in each pressure total, and recorded no OOM, kill, or swap. Output SHA-256: `5dc21491554f96f8d10a85f9bc2d070df9de2f647b5f709258cb009980f8b412`. A subsequent warm run cannot close this pressure failure; the owner explicitly required its correction.

The mechanism is retained asynchronous theorem elaboration, which the single Lean thread does not remove. In the pinned Lean `v4.31.0` distribution, `Lean/Elab/MutualDef.lean` selects asynchronous theorem-body tasks when `Elab.async` is enabled; those tasks retain elaboration state and snapshots. `Lean/Elab/Tactic/Decide.lean` already disables async for the auxiliary declaration used by `decide +kernel`, so the correction concerns the surrounding theorem tasks, not removal or weakening of a kernel decision. `Lean/AddDecl.lean` uses the same kernel-checking operation in both scheduling branches. The earlier E1 and standing-consumer corrections above identified the same contributor, but per-module overrides left other fixtures using the default.

Two cold-target comparisons held the maintained source, dependencies, pinned image, CPU/thread count, network isolation, 3 GiB ceiling, and swap policy fixed. Each variant inserted only `set_option Elab.async false`; removing that inserted line reproduces every original source byte, including every theorem statement and proof. Each run invalidated only the target's eight generated products and retained its warm dependencies. Both defaults recorded pressure; both synchronous variants recorded zero controlled memory events, zero pressure totals, and no swap. These are working-tree diagnostics, not immutable module-cost rows.

| Target and mode | Elapsed | GNU RSS | Cgroup peak | Each pressure total | Output SHA-256 |
|---|---:|---:|---:|---:|---|
| Message payload, default | 28.77 s | 3,093,752 KiB | 2,963,697,664 bytes | 179,104 µs | `290bc7b5d628596545111cf38fd240cede2a1a1c29f656e37e6337f065cd6682` |
| Message payload, synchronous | 28.44 s | 1,510,044 KiB | 1,222,635,520 bytes | 0 µs | `736efcf4c7243e53aa44bd179f67246caa03cb620ef878d918f01ffcaf547b20` |
| Snapshot root closure, default | 17.49 s | 2,952,724 KiB | 2,933,153,792 bytes | 5,602 µs | `968e895aa80f2db32d127b9eee71860cd05a00d849df48b37686c53cc17cb25e` |
| Snapshot root closure, synchronous | 17.35 s | 2,944,444 KiB | 2,710,491,136 bytes | 0 µs | `e051f3ab5c1fec7b6d11066058afe4697dfd32b114068606640712bcc383145d` |

The Message-payload result separates accumulated theorem tasks from a necessary single reduction peak. The smaller snapshot-root reduction leaves its individual large proof as a residual risk, so synchronous elaboration is not claimed to guarantee a memory bound by itself. [lakefile.toml](../lakefile.toml) now selects `leanOptions = { Elab.async = false }` at package scope. Lake fingerprints that effective option for every repository module build and supplies it to the language server. The [verification-entrypoint guard](../scripts/verification-entrypoint.test.ts) rejects absence, enabling, and placement after a library table, which would fail to supply the package default. Existing local overrides remain; the package setting changes no theorem, fixture, memory limit, or thread pin.

The first genuinely cold full-library run with that package default compiled all 322 jobs in 23:34.40 at 2,941,520 KiB GNU RSS and 2,820,386,816 cgroup bytes. All controlled memory events and swap were zero, but both pressure totals reached 52,632 microseconds, so resource acceptance still failed. Sampling first observed that pressure after the snapshot-root closure target; the sample interval alone does not prove attribution. All 348 recorded source inputs remained byte-identical across the run, and all 320 generated module setups contained Boolean `Elab.async=false`. Output SHA-256: `7fb701d2113271ef0cc80aef12fd4415fc7855275bfeb253dd3135973ec72a19`.

The unchanged synchronous snapshot-root target then reproduced pressure in isolation. Its selected-root theorem decides nine facts together and refers five times to a result that expands three production commands; the unselected-root theorem repeats the same mechanism across three facts. Separately deciding the selected theorem's conjuncts lowered residency but exceeded the unchanged 200,000-heartbeat limit, so that form was discarded. The correction instead materializes each complete `StimulusResult` through a kernel equality and rewrites the next command with its checked predecessor. The [snapshot-root owner](../BpmnSemantics/CompensationEventSubProcessSnapshotRootClosureConformance.lean) retains all six public execution definitions and both public theorem statements byte-for-byte. Its private values preserve every runtime field and result flag; the original propositions still use `decide +kernel` after rewriting those complete equalities.

| Snapshot-root diagnostic | Exit | Elapsed | GNU RSS | Cgroup peak | Each pressure total | Output SHA-256 |
|---|---:|---:|---:|---:|---:|---|
| Unchanged synchronous target | 0 | 18.02 s | 2,946,452 KiB | 2,697,547,776 bytes | 127,640 µs | `f6b122a09189857926aa2b6e88e17c1f9222ffc188a894e26f06b19ac1619d2d` |
| Separate conjunct decisions; heartbeat refusal | 1 | 36.89 s | 2,433,692 KiB | 2,342,072,320 bytes | 0 µs | `3de17af5fbfe6ab87e2f859a1bbdaa9ecf53ee3b7c88792b2cdaf64df2f2b18d` |
| Kernel-locked complete intermediate results | 0 | 22.50 s | 1,495,236 KiB | 1,139,458,048 bytes | 0 µs | `cc0359e1910afc2259c377e99c0496dc581befec761dc06a5d6bc98846d0546c` |

Every diagnostic retained the same 3 GiB ceiling, one CPU/thread, synchronous package default, network isolation, warm dependency closure, and cold target. All controlled memory events and swap were zero. Independent mutations of the literal Child Task wait owner and the promoted snapshot's root frame were rejected at their complete-result equality checks. Their output SHA-256 values are `35c6f782e89399f65490962a32ce556f341f471843f5f83008350f5d2c215191` and `b93330619f950289708bc912a81498bc0a3ba09752b5fe10c9e367610992b211`, respectively. These are working-tree diagnostics; immutable module-cost replacement remains separate.

The second genuinely cold full-library run passed all 322 jobs in 23:34.16 at 2,561,396 KiB GNU RSS and 2,454,470,656 cgroup bytes, leaving 766,754,816 bytes below the unchanged ceiling. Every controlled memory event, both pressure totals, and swap were zero. All 348 recorded source inputs remained byte-identical across the build, and all 320 generated module setups retained Boolean `Elab.async=false`. Output SHA-256: `a22210437126ab93770838a8047b3597fe81609269fc095657e35aee1aabf3a1`. The complete companion `./scripts/lake.sh test` passed against those warm outputs in 0.57 seconds at 121,436 KiB GNU RSS and 42,070,016 cgroup bytes, again with every event and pressure total zero; its output SHA-256 is `59a7c33035dd9ce0ac7546046bb9dccf7b6744e75716a693139323c62ae73190`. The cold result supplies resource acceptance for this correction; the companion warm command does not replace it.

This memory repair is non-material to the semantic review boundary: public execution definitions, theorem propositions, profiles, observations, and admission remain unchanged. The verification-entrypoint and contributor-setup guards cover the native package setting; Lean source-contract, import-boundary, source-hygiene, link, plan, and reviewability guards cover the fixture correction. Kernel equality locks and their separating mutations preserve the complete results. The composed arming footprint checkpoint still requires its separate independent semantic review.

#### Composed arming footprint checkpoint calibration

On 2026-09-08, immutable commit `5fa6dfe36214b6620ee90d588726a90c951c206d` supplied the read-only source archive for the measurements below. All 348 source inputs match the accepted cold full-library run above byte-for-byte. Each module retained the warm dependency closure and invalidated only its eight owned generated products; the pinned image, one CPU/thread, network isolation, 3 GiB ceiling, and no-additional-swap policy were unchanged. The selection covers the new canonical Activity-order witness, changed runtime and commutation consumers, all four standing-watch consumers, and all remaining near-cap targets.

| Module (`BpmnSemantics.` prefix) | Elapsed | GNU RSS | Cgroup peak | Output SHA-256 |
|---|---:|---:|---:|---|
| `ActivityActivationOrderConformance` | 1.66 s | 607,400 KiB | 845,103,104 bytes | `0a64a22b3043985df9340abc50fc56008e07c3a4ed8b712b015f2a3a18b367af` |
| `CallActivityConformance` | 23.60 s | 1,345,716 KiB | 1,045,606,400 bytes | `42dc8c7f41f482653b6b56fb669b94b9c969c5cecf3045edad62a0e5d9bb903f` |
| `CompensationEventSubProcessSnapshotLifecycleIntegrationConformance` | 15.25 s | 1,933,028 KiB | 1,656,119,296 bytes | `a19e78326a3d01c0248eefd64b4c26b6edf0f1866b6d415f3616cd23bff7ec8a` |
| `CompensationEventSubProcessSnapshotRootClosureConformance` | 20.08 s | 1,489,244 KiB | 1,225,306,112 bytes | `bbf0a1cacac672f35de204b21627877cbc238184c3cf79ca7d9033740bb69485` |
| `CompensationSourceAdmissionConformance` | 18.21 s | 2,447,360 KiB | 2,190,393,344 bytes | `61b115e04638762688e749ebe148e0f6361113d4a43a473ddf6024772632385b` |
| `CompensationTriggerHandlerCompletionConformance` | 25.83 s | 1,485,228 KiB | 1,177,042,944 bytes | `5120fe0e7e1db374d2d867970d760d18b2535579c22c6e3574395bad875190bd` |
| `InternalCommutationConformance` | 8.99 s | 1,242,848 KiB | 903,319,552 bytes | `3bbddc75baac4eac5b7e9177fe84f57a96fc3c331244d91d1538275cb3123ea1` |
| `MessagePayloadCatchConformance` | 25.93 s | 1,508,268 KiB | 1,215,041,536 bytes | `0830688b14e3209b8224e86304523035d7237143cb6360752826464261f261f7` |
| `RuntimeStateWellFormedInvariantConformance` | 20.24 s | 1,183,920 KiB | 845,213,696 bytes | `f19100b4e1fe8d42d54ae5168bae1b290cb32dbcc6a0cb36aaa2f4728809fea8` |
| `SequentialMultiInstanceProgramBindingConformance` | 31.01 s | 1,546,980 KiB | 1,246,470,144 bytes | `c94baa7d3503d1bf6192c6479a78c43b81817e0367a6de2d8849f4c0ab607604` |
| `TerminateEndEventConformance` | 30.56 s | 1,544,784 KiB | 1,240,502,272 bytes | `b4322512580a22c87fbcae5ea278ab94f25f11c9a7e013b61a20608f612cb257` |
| `TimerStartConformance` | 23.25 s | 1,416,140 KiB | 1,103,757,312 bytes | `0face0a8984680cd457c455eda188c71501d5c883d2d035287a98c0aa1dba131` |

Every command exited zero with zero controlled memory events, both pressure totals zero, and no swap. The source-bound rows replace [the module-cost record](../scripts/lean-module-cost.ts); its derived near-cap set is now empty. Historical measurements of unchanged targets remain historical evidence, and the standing-watch trigger remains in force. Elapsed time is contextual: source archive faults and competing host work prevent a controlled speed comparison.

The immutable archive also passed `./scripts/lake.sh build` against those warm outputs in 0.34 seconds at 121,872 KiB GNU RSS and 41,222,144 cgroup bytes; output SHA-256: `4f567059c83dca3b1b9ffbbc39d5b9d271d0162dfbde274d951b9dc03891cd21`. All controlled memory events, pressure totals, and swap were zero.

The immutable archive also passed `./scripts/lake.sh test` against those warm outputs in 0.54 seconds at 121,856 KiB GNU RSS and 43,171,840 cgroup bytes; output SHA-256: `fe812e937bf9d73be49a2b1a69a77b28790dc06a28d06c9c556cb3c6fa69f8cd`. All controlled memory events, pressure totals, and swap were zero.

These two warm tuples replace [the cgroup acceptance record](../scripts/lean-memory-acceptance.ts). They do not replace the genuinely cold full-library acceptance above. This calibration closes the resource checkpoint; independent semantic review and production batch integration remain separate obligations.

#### Finite prepared arming checkpoint calibration

On 2026-09-08, immutable commit `1fb503c64f686eaee95ba22e7444ed5775dfc827` supplied the read-only archive for the new finite arming fixture and all four standing-watch consumers. Each measurement used the pinned image, one CPU/thread, no network, the unchanged 3 GiB/no-additional-swap controller, warm dependencies, and removal of only the target's eight generated products. The first new-target invocation after invalidation reported success without compiling the target; its 0.25-second result is excluded. The retained retry and every watch measurement explicitly rebuilt the named module.

| Module | Elapsed | GNU RSS | Cgroup peak | Output SHA-256 |
|---|---:|---:|---:|---|
| `FiniteInternalArmingConformance` | 8.96 s | 1,311,400 KiB | 963,710,976 bytes | `a372343c039cb110b1b392ded116ef3603892af3420546f3a7da492c5d753040` |
| `CallActivityConformance` | 22.57 s | 1,347,112 KiB | 1,035,366,400 bytes | `6b4ff9720ee9229c1f2434f7501de3c68aac423b3b06fe97271eba539d7f5b10` |
| `SequentialMultiInstanceProgramBindingConformance` | 27.36 s | 1,548,380 KiB | 1,253,380,096 bytes | `07dd293d5c77e1337f05c9275e3d50cfedac69b4a990ce09d191afd90791feec` |
| `TimerStartConformance` | 22.77 s | 1,419,104 KiB | 1,106,575,360 bytes | `c29a18e830711dec71d145e29a0baa8ed8852d6f173af1fa7844bdb297855644` |
| `TerminateEndEventConformance` | 31.99 s | 1,549,756 KiB | 1,287,991,296 bytes | `368fb04cc63138b9a611337f3d9119aaaaece63e47590a2c6a2aaffc797401ac` |

Every retained command exited zero with zero controlled memory events, pressure totals, and swap. These source-bound rows update [the executable module-cost record](../scripts/lean-module-cost.ts); none reaches the near-cap threshold. Elapsed times remain host-load context. The measurements do not replace the full-library acceptance record or establish independent semantic approval.

#### Place-wide token-owner census calibration

On 2026-09-08, immutable commit `21b4c1de92c39d021142b665d4cfccd9251dd8b4` supplied the read-only source archive for the changed census witness, finite arming consumer, and all four standing-watch modules. The pinned image, one CPU/thread, no network, fixed 3 GiB ceiling, and no-additional-swap policy remain unchanged. Each target retained warm dependencies and moved aside only its eight generated products inside the same container that ran Lean. Acceptance required observed artifact absence and an explicit build of the named module. A preceding host-side invalidation returned success in 0.24 seconds without compiling the target; that result is excluded, as is the analogous skipped invocation recorded in the finite arming calibration above.

| Module | Elapsed | GNU RSS | Cgroup peak | Output SHA-256 |
|---|---:|---:|---:|---|
| `InternalCommutationConformance` | 10.91 s | 1,250,512 KiB | 910,233,600 bytes | `00b1e3ba63d806b1d5cc072a9237d81ded4b677849f769ae2c3b2ef2f225839e` |
| `FiniteInternalArmingConformance` | 9.90 s | 1,315,448 KiB | 970,739,712 bytes | `df1fab088c9636d6ab7b97f1a47de748695f49635d4cd9bae7ed872cbd7c57bc` |
| `CallActivityConformance` | 29.76 s | 1,346,324 KiB | 1,040,130,048 bytes | `fd036cd60b1e46bc07e2fb23505a76e1b3fc0789857bf0246b3a2fd136e2c099` |
| `SequentialMultiInstanceProgramBindingConformance` | 33.36 s | 1,552,572 KiB | 1,196,924,928 bytes | `8d32e9f0ba9fad9522cce215f2966cf0fdac3d102b3cefc97aeb0fa07fc6a333` |
| `TimerStartConformance` | 25.67 s | 1,415,592 KiB | 1,124,364,288 bytes | `e52b0774dfbe76ba6d2d00207349429bcb96eeb067948b11fafd2e91c7b40ace` |
| `TerminateEndEventConformance` | 38.71 s | 1,550,232 KiB | 1,306,763,264 bytes | `df21f167e40a9ef6f6f5ca36a5444c24cc83fb5afc8eb46f20e655b861ad76c9` |

Every retained command exited zero below the hard ceiling with zero controlled memory events, pressure totals, and swap. These rows replace the corresponding [module-cost records](../scripts/lean-module-cost.ts); none is near-cap. GNU RSS remains comparable within the series, while elapsed times reflect host load and the container-side invalidation is included in cgroup accounting. The checkpoint establishes no new full-library resource acceptance or semantic approval.

#### Canonical token-storage calibration

On 2026-09-08, immutable commit `f7d96debd3c215149730b3db5299bd9d0a3cc826` supplied the read-only archive for the new token-storage witness, both commutation consumers, the four standing-watch consumers, and three changed cyclic-control-flow witnesses. All 357 archived Lean/build inputs were checked against that commit. The pinned image, one CPU/thread, no network, fixed 3 GiB ceiling, and no-additional-swap policy remain unchanged. Each target retained warm dependencies and moved aside only its eight generated products inside the measurement container; every retained invocation explicitly rebuilt the named module.

| Module | Elapsed | GNU RSS | Cgroup peak | Output SHA-256 |
|---|---:|---:|---:|---|
| `CanonicalTokenStorageConformance` | 4.60 s | 1,216,092 KiB | 846,581,760 bytes | `f6fad239d7ce20ce265d89f33bb10c83546623453ce00bcdce2f2743b3244b79` |
| `InternalCommutationConformance` | 10.31 s | 1,273,148 KiB | 933,793,792 bytes | `20cf31d517420252b68c3d28454f88c70381ec1ba969d2a960f358cf537842c5` |
| `FiniteInternalArmingConformance` | 10.18 s | 1,337,416 KiB | 1,009,618,944 bytes | `b633ba2d9d3779037906339af9a7e0d90a68df9bb2d2ad3fa539f54995bb2ac2` |
| `CallActivityConformance` | 25.74 s | 1,345,652 KiB | 1,049,415,680 bytes | `85ef8e867d8bd77b63c0fdd4c1e4d689311ac65d4edccb80bbc4449ad93d3eb3` |
| `SequentialMultiInstanceProgramBindingConformance` | 28.93 s | 1,550,868 KiB | 1,195,577,344 bytes | `c50be9dc7c3751c3719a04705bee9d1e784181f862037e6adc6a995e6ccd4e6f` |
| `TimerStartConformance` | 22.01 s | 1,417,392 KiB | 1,107,922,944 bytes | `a0ed91182c41563ad9549c470f59893239be055ee8eeba4fd3a1cfe02b03d273` |
| `TerminateEndEventConformance` | 33.25 s | 1,595,728 KiB | 1,284,681,728 bytes | `8cf1e5b54a99982db30aa1977f9e5ecb8b5eabaa2ef3ae2208dbb542bc33901a` |
| `SemanticProcess.CyclicControlFlowConformance` | 10.15 s | 1,072,920 KiB | 710,578,176 bytes | `3eddf1559257c357f91462facd199d2f765d945e7ae5b0e03e9c45f135532a01` |
| `SemanticProcess.CyclicControlFlowExecutionConformance` | 3.07 s | 695,004 KiB | 295,247,872 bytes | `3b9a5f1cc7ef2e02567b384b21aef2ad455178c4f3fa7b79bd14af1d905e16ee` |
| `SemanticProcess.CyclicControlFlowReachabilityConformance` | 3.98 s | 734,464 KiB | 336,646,144 bytes | `bff2b3825725c676c78b07b647ade8f7dd8d93fa45e8911ee6b411613e4a29f9` |

Every retained command exited zero below the hard ceiling with zero controlled memory events, pressure totals, and swap. These rows replace the corresponding [module-cost records](../scripts/lean-module-cost.ts); none is near-cap. GNU RSS and cgroup charging remain separate measurements, and elapsed times reflect host load. This target-only calibration does not replace the cold full-library acceptance or establish independent semantic approval.

#### Inclusive pairing calibration

On 2026-09-08, immutable commit `565ff325eec927f5367be6fe16d160c578ab70ae` supplied the read-only archive for the new pairing witness, the existing Inclusive Gateway consumer, and all four standing-watch consumers of the changed structural validator. All 360 archived Lean/build inputs match that commit. The pinned image, one CPU/thread, no network, fixed 3 GiB ceiling, and no-additional-swap policy remain unchanged. Each target retained warm dependencies and moved aside only its eight generated products inside the measurement container; every retained invocation explicitly rebuilt the named module.

| Module | Elapsed | GNU RSS | Cgroup peak | Output SHA-256 |
|---|---:|---:|---:|---|
| `InclusiveGatewayPairingConformance` | 3.43 s | 836,304 KiB | 414,973,952 bytes | `a966643491f93ee8ff51ab7d15fdef2989f6a0e91c1fa549738f0148f222e9c4` |
| `InclusiveGatewayConformance` | 27.63 s | 1,435,552 KiB | 1,141,964,800 bytes | `1de9b43eb472fa128cd474b3193c7636469df26d14b4b4a081a8b1ee070514ee` |
| `CallActivityConformance` | 25.96 s | 1,347,480 KiB | 1,039,626,240 bytes | `1ac7c302baef4d83ca8a94f79c933ae28e5c42df248c5f41f285cbab2d056a3c` |
| `SequentialMultiInstanceProgramBindingConformance` | 37.44 s | 1,557,340 KiB | 1,246,167,040 bytes | `58acb65caca0ce173a57d2ca9333241a08438b5d5c8ec749229cfeffed7cb7d8` |
| `TimerStartConformance` | 25.38 s | 1,416,408 KiB | 1,121,857,536 bytes | `761ccefedf2615b0653937832533ee2d4e3f6b77270ed34b31916cd628e08104` |
| `TerminateEndEventConformance` | 31.99 s | 1,604,368 KiB | 1,331,187,712 bytes | `ebfdf901892b95a3b4e1964bb81d50c82a64390b1db2168a938f512b1dcfe8e3` |

Every retained command exited zero below the hard ceiling with zero controlled memory events, pressure totals, and swap. These source-bound rows update [the executable module-cost record](../scripts/lean-module-cost.ts); none is near-cap. Elapsed times remain host-load context. The initial container launch failed before Lean because the read-only source archive lacked the nested cache mount point; creating that empty directory changed no source input. These isolated measurements do not replace full-library acceptance or establish semantic approval.

#### Finite mixed local-control calibration

On 2026-09-09, immutable commit `5a335c21673f97f6b993755e5804b4dfa0d62f5b` supplied the read-only archive for the new mixed witness and the four standing consumers of the changed shared proof inputs. All 394 archived Lean/build inputs match that commit. The pinned image, one CPU/thread, no network, unchanged 3 GiB ceiling, and no-additional-swap policy remain fixed. Each target retained warm dependencies and moved aside only its eight generated products inside the container; every retained invocation explicitly rebuilt the named module.

| Module | Elapsed | GNU RSS | Cgroup peak | Output SHA-256 |
|---|---:|---:|---:|---|
| `FiniteInternalTransitionConformance` | 7.48 s | 1,154,012 KiB | 797,900,800 bytes | `e15bc0042aa61348abc36d97bf699f0658762703c4623e68fd7ba9359f690aef` |
| `CallActivityConformance` | 23.28 s | 1,347,552 KiB | 1,041,088,512 bytes | `8c3911ef4fb523aa08ff4c7a6ad15b5d97ec1160dad9470ab9000ac124a120ee` |
| `SequentialMultiInstanceProgramBindingConformance` | 28.15 s | 1,549,028 KiB | 1,242,087,424 bytes | `2b64254ce5902a986c76ad3ae4326280eee676a2d8847d763f80878cfff1ed40` |
| `TimerStartConformance` | 21.72 s | 1,417,632 KiB | 1,115,271,168 bytes | `1bdf4f0639300e44b67f2b829dbf71075c969a860f3740dbe5bf1f78ae2a91c9` |
| `TerminateEndEventConformance` | 31.57 s | 1,595,884 KiB | 1,285,562,368 bytes | `c120987c200626aedc2bbad3a19f038a3963d78080a949640593a1907363f8b1` |

Every retained command exited zero with zero controlled memory events, pressure totals, and swap. The source-bound rows update [the executable module-cost record](../scripts/lean-module-cost.ts); none is near-cap. The mixed witness consumes quantified arbitrary-permutation laws instead of repeatedly reducing complete executions. These measurements establish resource acceptance at this proof checkpoint, not production integration or a performance trend.

#### Mixed local-control production calibration

On 2026-09-09, immutable commit `cbb78a1179031d02a7edd99f6ced70d6872fe76c` supplied the read-only archive for the new production-closure example, four direct batch/publication consumers, and all four standing-watch consumers. All 394 archived root Lean/build inputs match both that commit and the measured sources. The pinned image, one CPU/thread, no network, unchanged 3 GiB ceiling, and no-additional-swap policy remain fixed. Each measurement retained warm dependencies and moved aside only the target's eight generated products inside the container; every retained invocation explicitly rebuilt the named target.

| Module | Elapsed | GNU RSS | Cgroup peak | Output SHA-256 |
|---|---:|---:|---:|---|
| `MixedLocalControlClosureConformance` | 32.32 s | 1,567,812 KiB | 1,846,767,616 bytes | `9b2c3a052b1da8e299e7d58015f2977c10aaaaa431eb7ebdd5e235846e767219` |
| `FiniteInternalArmingConformance` | 9.35 s | 1,343,692 KiB | 1,026,576,384 bytes | `fb7ca9aa01113ca532912dea62aba1995296d2eb5839b85df8752f554db5e684` |
| `InternalClosureAtomicityConformance` | 14.90 s | 1,261,436 KiB | 926,498,816 bytes | `5398eb623487676b94c9a620d9af81ec03ab5ff46351165160252fcd6bb8bae8` |
| `CommittedExecutionPublicationConformance` | 17.13 s | 1,478,996 KiB | 1,154,965,504 bytes | `755a7ee59e885d30384f9754890e9dc0eab71e987aebe3b5d9d105b2f3f04cf8` |
| `InternalCommutationConformance` | 9.83 s | 1,269,404 KiB | 918,433,792 bytes | `28f74973a309536e8bae22e0ffb716df5ada66b9df51c7f0a386e94d685ef7ad` |
| `CallActivityConformance` | 23.67 s | 1,355,584 KiB | 1,058,447,360 bytes | `361fa030d47a71a62ee3f35ddd41dfd566f9d42e90ddc4ec10b58e199da3a972` |
| `SequentialMultiInstanceProgramBindingConformance` | 38.62 s | 1,557,084 KiB | 1,285,394,432 bytes | `93cb6533898775133bf32dc2bd64a124a7e60a8df608599590482be25b56b9ac` |
| `TimerStartConformance` | 27.30 s | 1,422,720 KiB | 1,128,316,928 bytes | `6af9205208545787c136375a8a971d5f294330e6ed3dc7a45df66c4cd68b5657` |
| `TerminateEndEventConformance` | 43.32 s | 1,612,012 KiB | 1,368,268,800 bytes | `2bf6c7b135ecc54703192599c752ad0f6a1b7a07649f4caba6c1f2914da20882` |

Every retained command exited zero with zero controlled memory events, pressure totals, and swap. None reaches the near-cap threshold; [the executable module-cost record](../scripts/lean-module-cost.ts) binds every row to the immutable source. Elapsed readings include host contention and source-page faults and establish no performance trend. The complete native Lean gate also passes on these production sources; independent production review and complete repository verification remain separate obligations.

#### Mixed local-control production verification

On 2026-09-09, independent production review approved correction `34e38132` as recorded in [the receipt](INTERNAL-COMMUTATION-PROPOSAL.md#independent-cold-review-receipt). Complete verification passes at clean commit `9fddbaca`. The retained attempts distinguish a corrected test typing defect from two runtime-harness failures; no test deadline, clock margin, VM flag, dependency pin, or semantic limit changed during these diagnostics.

| Target | Command or exact diagnostic | Exit | Elapsed | Output SHA-256 |
|---|---|---:|---:|---|
| `8c3f818c` | `test:pre-push`: harness typecheck refused three new test typing errors, corrected at `caa56e48` | 1 | 9,678 ms | `787240ddf34d17ca3a19799965aa34d459c76366ac745afa1b9bb3d30e4891c6` |
| `caa56e48` | `test:pre-push`: the SMI natural path consumed 2,622 ms of its 5,000 ms host Timer, exceeding the 50% margin after successful task completions | 1 | 559,534 ms | `c8dc0244bed85a9467aabd9d2cb902c8288e34ba6118969474f8113a967e2e03` |
| `caa56e48` | isolated `sequential-multi-instance-refinement.temporal-serial-test.ts`, unchanged deadline and margin | 0 | 18,422 ms | `de088f026f18faf110e07628406d18df54b78761dbb2e2545ea9b7dc59ba4c1a` |
| `34e38132` | `test:pre-push`: the direct-VM managed-wait test process terminated with `SIGSEGV` in V8 `ClearStaleLeftTrimmedPointerVisitor::VisitRootPointers` during mark/compact collection | 1 | 246,169 ms | `f35aa4a229b5b34aa801bbffc58ac67fcac8aeba30feb16516efb877f047b61e` |
| `34e38132` | isolated `managed-wait-host-wakeup.test.ts` under the existing `--no-parallel-scavenge` flag | 0 | 2,775 ms | `23314a35b02a416742b26c6d8d7a419f37cdf6cf6a846f9f2e96980b6bdd2388` |
| `34e38132` | `test:temporal:built:concurrent`, all 332 tests unchanged | 0 | 76,802 ms | `e7c8a66c25be12fc3704a74f4e3871fa77f1ae7a9d472dc9235b77e637ed79d7` |
| `9fddbaca` | clean complete `test:pre-push`, all 72 pipeline cases agree | 0 | 549,784 ms | `060ac75fae528e1de3924934df0a42e99c9b8a0191d4eea6cc2cca747b623429` |

The macOS native reports captured at 2026-09-09 02:42:24 and 2026-09-08 16:10:34 local time have the same signal, invalid address, and first sixteen native frames. The earlier report predates this production integration. The isolated and concurrent passes establish diagnostic recovery; only the final complete run establishes repository acceptance, and none establishes a V8 fix. The final pipeline's 57,040 ms exceeds its 36,000 ms soft feedback target while remaining below the unchanged pathology ceiling; host load rose from 0.60 to 1.99 per core, so elapsed time establishes no performance trend.

#### Canonical scope-storage working-tree checkpoint

On 2026-09-09, the [scope and counter-storage prerequisite](INTERNAL-COMMUTATION-PROPOSAL.md#canonical-scope-and-counter-storage-checkpoint) reproduced actual child-entry, Call-invocation, and race-arming order differences before replacing the shared Lean prepend writers. The correctly typed red kernel run exited 1 in 2,147 ms, output SHA-256 `2e9044e418e0c3269d79d146cc647f376587c9bec3a0e89743fe63294a3b971a`; all four initial assertions failed for the intended storage mechanism.

Each changed kernel fixture passes in a mutable candidate mirror under `memory.max=3221225472`, zero swap allowance, and zero controlled memory events and pressure totals. GNU elapsed includes any dependency rebuilds. These are pre-commit resource checks, not immutable calibration or committed-source cost binding.

| Kernel target | GNU elapsed | Maximum RSS (KiB) | Cgroup peak (bytes) | Output SHA-256 |
|---|---:|---:|---:|---|
| `ScopeStorageOrderConformance` | 3.38 s | 688,308 | 933,367,808 | `5e15f9ea47d7e2462c3467166e3330d9e1329fcebd3bb2a77397bf76d0d8ee72` |
| `SubProcessBoundaryTimerConformance` | 172.32 s | 1,357,552 | 1,707,782,144 | `ec247e61f68907cd24f9a1671a58f6253762496ccae7c0de284ca9d575bf1119` |
| `CompensationEventSubProcessSnapshotRootClosureConformance` | 60.27 s | 1,963,476 | 1,674,342,400 | `fe0ff3af072580c666478e73d50937a1ac5727c838be2d36c2abacdfbc495278` |

The full rebuilds exposed an old child-first expected list in the boundary-Timer witness, then the same scope/counter ordering in the dependent Compensation witness's cached complete result. Both corrections preserve occurrence and counter values; complete-result equality remains checked. Existing aggregate-preservation proofs retain their public contracts.

| Working-tree gate | Exit | Elapsed | Output SHA-256 |
|---|---:|---:|---|
| Semantic core, all 897 tests | 0 | 17,822 ms | `f463b653fdcd6fa749b2d2ee6e380aae57e0ee010cad030f73aac5efd540bf85` |
| Six shared Lean proof-consumer roots | 0 | 31,953 ms | `6c46c563865adf143f2372efaa609d921e1920c8581b7fcd6fc283b32698cd1a` |
| Full Lean library: old boundary-Timer expectation | 1 | 1,107,808 ms | `b484b489b58a664cb8423777ed3c90abe19f59f291deb0f7d0559c0fa8ef10c9` |
| Full Lean library: dependent cached Compensation result | 1 | 144,910 ms | `50e092b81856a4bc04e916e580c70d9fc554680030d039a97244ae8955cefeac` |
| Full Lean library after both corrections, all 370 jobs | 0 | 22,674 ms | `329ba22813eb2dc5091dea8650aca5a10742cdb0a1ee94320ebb72670efcaac4` |
| Prepared differential pipeline, all 72 cases agree | 0 | 54,793 ms | `9d71d0eb437c189e9489d49040951b1511422d7b479d3aff305f434b1e6440f3` |

The pipeline reports `dirty=true` against base `669f2bf9`, so it is working-tree correctness evidence. Its 53,435 ms warm measurement exceeds the unchanged 36,000 ms soft feedback target while remaining below the pathology ceiling; load per core rises from 0.65 to 1.53, so it establishes no performance trend. At the owner's requested next-commit stop, committed-source calibration, immutable churn accounting, independent checkpoint review, and the complete clean gate remained resume obligations. On resumption, the module-cost guard reproduced the missing new row and stale source bindings for both corrected fixtures before calibration supplied their immutable replacements.
#### Canonical scope-storage immutable calibration

On 2026-09-18, immutable commit `a97a4ea18029bcbbc607e83e8712b83ecaa8a9ed` supplied the read-only archive for the new scope-storage witness, both corrected fixtures, five token/commutation/batch consumers, and all four standing-watch consumers. All 396 archived Lean/build inputs match that commit and were rechecked after every measurement. The pinned image, one CPU/thread, network isolation, unchanged 3 GiB ceiling, and zero swap allowance remain fixed. The dependency closure was built once; each fresh measurement container rebuilt only its named target after any existing eight target products were moved aside in the separate cache. The measured invocation was `/usr/bin/time -v timeout --kill-after=10s 1800 ./scripts/lake.sh build <target>`.

| Module | Elapsed | GNU RSS | Cgroup peak | Build-output SHA-256 |
|---|---:|---:|---:|---|
| `ScopeStorageOrderConformance` | 1.61 s | 690,964 KiB | 274,489,344 bytes | `c772bbff08aedc11fe6d74ef2a0f6bb8c7e6a3e9c51c5470c2e49bf36ef2db29` |
| `SubProcessBoundaryTimerConformance` | 20.71 s | 1,354,732 KiB | 1,009,967,104 bytes | `f9ac97fdb6ba41442923ff740ecee6d20c54c34bc2261b424650509da99c6bf8` |
| `CompensationEventSubProcessSnapshotRootClosureConformance` | 26.49 s | 1,515,000 KiB | 1,239,867,392 bytes | `a7b1e09eb1ff882388c3c6f9d6ae452fbaa6a29782814cc9c308898211e8af4d` |
| `CanonicalTokenStorageConformance` | 5.85 s | 1,226,740 KiB | 864,915,456 bytes | `31f9e296eede5fb5409f6dab664163c97d45d066e9eb312e094bfdfd254c2c66` |
| `InternalCommutationConformance` | 10.27 s | 1,273,704 KiB | 931,323,904 bytes | `6cccb2c213b127d0283d34c068681e72dc99b7b8b77ae34fa8a1ba58c88b4d60` |
| `FiniteInternalArmingConformance` | 9.13 s | 1,344,572 KiB | 1,006,256,128 bytes | `17c4e91a5aef109d8a063e30ad98d3f37da51dcb02a83b45dbd4d95a0db0b41f` |
| `FiniteInternalTransitionConformance` | 8.05 s | 1,147,776 KiB | 799,182,848 bytes | `6b7f4f27689bf2fd26e5842ad182151dc3b65f95e0c8d99ecabb9e5c1fffffbe` |
| `MixedLocalControlClosureConformance` | 31.71 s | 1,588,064 KiB | 1,339,084,800 bytes | `62fc42b5c4f3d15f68e2ef8cd5409078c6260c6ec8b75c9d6b2dc3d40af565ed` |
| `CallActivityConformance` | 25.36 s | 1,361,428 KiB | 1,069,346,816 bytes | `5b778c133c57a5bd141060a82cf9ff2af6c600b41b0b3115f2ca256431595f0c` |
| `SequentialMultiInstanceProgramBindingConformance` | 28.82 s | 1,563,696 KiB | 1,208,778,752 bytes | `b93d31862c60aee4cbec6afd79e74f8f24d23426b3b610c09e8dd9757e0d6297` |
| `TimerStartConformance` | 21.89 s | 1,425,300 KiB | 1,133,105,152 bytes | `4d5111671f5195f697640d39ef4da4ddc2b038d2f9f7cbf10ebb2ea44f3e1acc` |
| `TerminateEndEventConformance` | 35.48 s | 1,661,408 KiB | 1,386,967,040 bytes | `252b6e9c927d7ca891a72f7ecc59b5c5494cbb8e7b7a9bef4922d0c1913bf4d8` |

Every invocation exited zero, its durable command receipt passed the separate receipt assertion, and all controlled memory events, pressure totals, and swaps remained zero. The highest cgroup peak leaves 1,834,258,432 bytes below the hard ceiling; none reaches the GNU-RSS near-cap threshold. GNU RSS and cgroup accounting remain separate series, and elapsed readings establish no performance trend. [The executable module-cost record](../scripts/lean-module-cost.ts) binds all twelve rows to the immutable source. This evidence-only increment changes no semantic claim and opens no separate review cycle; module-cost/source binding, cost reproduction, documentation guards, and the complete infrastructure gate govern it. Independent review of the scope-storage implementation and clean complete repository verification remain separate obligations.


#### Scope-creation selection calibration

On 2026-09-18, `BpmnSemantics.InternalScopeCreationSelectionConformance` was rebuilt from immutable commit `bced19a5c11e258e6506b5caef043735933c5d3e`. All 398 archived Lean/build inputs match that commit and were checked before and after measurement. The pinned image, one CPU/thread, network isolation, 3 GiB ceiling, and zero swap allowance match the preceding calibration. Only the target's eight generated products were moved aside; its dependency closure remained warm. The invocation was `/usr/bin/time -v timeout --kill-after=10s 1800 ./scripts/lake.sh build BpmnSemantics.InternalScopeCreationSelectionConformance`.

The immutable run exited zero in 2.23 seconds at 658,084 KiB GNU RSS and 905,416,704 cgroup bytes, with every controlled memory event, pressure total, and swap count zero. Build-output SHA-256 is `19dc4d5c8fdc98c3199a8cdb88fecb7599e5a610b7d2ac286f2fabd94a22cc2b`; the durable receipt passed its separate assertion. [The cost record](../scripts/lean-module-cost.ts) binds this exact source. Elapsed time includes archive page faults and establishes no performance trend.

The first bounded working-tree build also rebuilt stale dependencies: it passed in 52.84 seconds at 667,652 KiB GNU RSS and 946,212,864 cgroup bytes, with zero controlled memory events, pressure totals, and swap. The complete native Lean gate passed all 371 jobs in 1,324.61 seconds on identical Lean/build inputs. Existing evaluator files differ only in the visibility of the shared scope-counter reader and Call sorter; their bodies are unchanged. These results add no new full-library memory-acceptance or independent semantic-review claim.

#### Scope-creation preparation calibration

On 2026-09-18, immutable commit `93cfcbf9c34de63dfa745f11815cd1003efd509f` supplied the new preparation witness, four commutation/batch consumers, and all four standing-watch consumers. All 402 archived Lean/build inputs match that commit and were checked before and after each measurement. The pinned image, one CPU/thread, network isolation, 3 GiB ceiling, and zero swap allowance remain unchanged. After warming the dependency closure, each fresh container rebuilt only its named target with its eight generated products moved aside. The measured invocation was `/usr/bin/time -v timeout --kill-after=10s 1800 ./scripts/lake.sh build <target>`.

| Module | Elapsed | GNU RSS | Cgroup peak | Build-output SHA-256 |
|---|---:|---:|---:|---|
| `InternalScopeCreationPreparationConformance` | 1.78 s | 721,184 KiB | 316,637,184 bytes | `f93691c23995dc4c0c82915bc85f102060d4414947506458225cc837cd2fe2c5` |
| `InternalCommutationConformance` | 9.16 s | 1,271,768 KiB | 928,616,448 bytes | `05d9425e321f1c125aac4d367e3f6b4d96e0b3f77dfa5eeebfcba3a798fc35d4` |
| `FiniteInternalArmingConformance` | 8.83 s | 1,344,100 KiB | 1,000,226,816 bytes | `6b2671644cfada0cbccb9da136ebbb76a042e16a4b4853c57a49abac5e25bc5a` |
| `FiniteInternalTransitionConformance` | 6.77 s | 1,160,420 KiB | 797,130,752 bytes | `920baa1bdab4ccf6c6cc245c8b832e2e6abfc879c222a55876ef9e33d197622a` |
| `MixedLocalControlClosureConformance` | 28.99 s | 1,570,176 KiB | 1,365,401,600 bytes | `203f1c94a6c728c539439d2db6baa8bfd4e5d9e93e38bdb324e67ccf77fa4075` |
| `CallActivityConformance` | 24.50 s | 1,362,324 KiB | 1,072,025,600 bytes | `4e709467c35d3c459368a5e00dfb58a79a67c1f47ea5a15d56361078738a9830` |
| `SequentialMultiInstanceProgramBindingConformance` | 27.41 s | 1,557,692 KiB | 1,273,290,752 bytes | `f7536ef452b495907da4800198863b2ac02323b8b7487d9dcdf0b28851f19ef0` |
| `TimerStartConformance` | 21.83 s | 1,428,084 KiB | 1,125,556,224 bytes | `4d5111671f5195f697640d39ef4da4ddc2b038d2f9f7cbf10ebb2ea44f3e1acc` |
| `TerminateEndEventConformance` | 31.22 s | 1,663,504 KiB | 1,381,236,736 bytes | `2a0597c17261b11be62d00bdee182c91f551cc5cbdb71a6a795de2b1b3a292b7` |

Every invocation exited zero, its durable receipt passed the separate assertion, and all controlled memory events, pressure totals, and swaps remained zero. The highest cgroup peak leaves 1,839,988,736 bytes below the hard ceiling; no row reaches the GNU-RSS near-cap threshold. [The executable cost record](../scripts/lean-module-cost.ts) binds all nine measurements to the immutable source. GNU RSS and cgroup charging remain separate series, and elapsed readings establish no performance trend.

The first successful bounded working-tree witness passed in 2.62 seconds at 727,636 KiB GNU RSS and 321,511,424 cgroup bytes with zero events, pressure, and swap. Two earlier fixture-elaboration failures remain retained and are not acceptance evidence. The complete native Lean gate passed all 375 jobs in 1,228.65 seconds on identical Lean/build inputs. This resource-record increment is non-material and governed by module-cost/source binding, cost reproduction, documentation guards, and the complete infrastructure gate; it creates no full-library memory-acceptance or independent semantic-review claim.

#### Scope-creation position calibration

On 2026-09-18, immutable commit `d7fcdcb0345004243801c2bc9eb158b2fc57e23a` supplied `BpmnSemantics.InternalScopeCreationValidityConformance`. All 410 archived Lean/build inputs match that commit and were checked before and after measurement. The pinned image, one CPU/thread, network isolation, 3 GiB ceiling, and zero swap allowance remain unchanged. The dependency closure stayed warm; the target's eight generated products were moved aside and their absence checked inside the measurement container before `/usr/bin/time -v timeout --kill-after=10s 1800 ./scripts/lake.sh build BpmnSemantics.InternalScopeCreationValidityConformance`.

The confirmed target rebuild exited zero in 12.13 seconds at 1,726,820 KiB GNU RSS and 1,437,876,224 cgroup bytes, with every controlled memory event, pressure total, and swap count zero. Build-output SHA-256 is `52ba21ef7071d1001f7aed8a2f913a86262ff4b0ec9ad8cdae95d8d6f49ba8ad`; the durable receipt passed its separate assertion. [The executable cost record](../scripts/lean-module-cost.ts) binds this exact source. An earlier attempt reported success without rebuilding the target and was rejected by the rebuild assertion; it supplies no resource evidence. The first bounded working-tree build also passed, including stale dependencies, at 2,363,330,560 cgroup bytes. These measurements establish neither a performance trend nor new full-library memory acceptance. This evidence-only increment is non-material and governed by module-cost/source binding, cost reproduction, documentation guards, and the complete infrastructure gate; independent semantic review remains separate.

#### Scope-creation Compensation calibration

On 2026-09-18, immutable commit `52a843677d9f05a8ed002fbad1f3558504e5716d` supplied `BpmnSemantics.ScopeCreationCompensationValidityConformance`. All 413 archived Lean/build inputs match that commit and were checked before and after measurement. The pinned image, one CPU/thread, network isolation, 3 GiB ceiling, and zero swap allowance remain unchanged. The dependency closure stayed warm; the target's eight generated products were moved aside and their absence checked inside the measurement container before `/usr/bin/time -v timeout --kill-after=10s 1800 ./scripts/lake.sh build BpmnSemantics.ScopeCreationCompensationValidityConformance`.

The confirmed target rebuild exited zero in 4.40 seconds at 1,049,176 KiB GNU RSS and 1,331,363,840 cgroup bytes, with every controlled memory event, pressure total, and swap count zero. Build-output SHA-256 is `13416e5e4aa13e9024d5feacbc63112a2ffb73b495aa859a35709e6a46296760`; the durable receipt passed its separate assertion. [The executable cost record](../scripts/lean-module-cost.ts) binds this exact source. The first bounded working-tree build also passed, including stale dependencies, at 1,528,086,528 cgroup bytes. These measurements establish neither a performance trend nor new full-library memory acceptance. This evidence-only increment is non-material and governed by module-cost/source binding, cost reproduction, documentation guards, and the complete infrastructure gate; independent semantic review remains separate.

#### Scope-creation preparation frame calibration

On 2026-09-18, immutable commit `ba020ca9950f6f23f53beaed4c31ebcedec62a2d` supplied `BpmnSemantics.InternalScopeCreationFrameConformance`. All 417 archived Lean/build inputs match that commit and were checked before and after measurement. The pinned image, one CPU/thread, network isolation, 3 GiB ceiling, and zero swap allowance remain unchanged. The dependency closure stayed warm; the target's eight generated products were moved aside and their absence checked inside the measurement container before `/usr/bin/time -v timeout --kill-after=10s 1800 ./scripts/lake.sh build BpmnSemantics.InternalScopeCreationFrameConformance`.

The confirmed target rebuild exited zero in 23.24 seconds at 1,297,740 KiB GNU RSS and 966,393,856 cgroup bytes, with every controlled memory event, pressure total, and swap count zero. Build-output SHA-256 is `442d501d598dcd5e1742f052fc47d680774de48ea6af9f59c3114571dbc6387c`; the durable receipt passed its separate assertion. [The executable cost record](../scripts/lean-module-cost.ts) binds this exact source. The first successful bounded working-tree build passed at 1,635,737,600 cgroup bytes after correcting the fixture's unregistered graph-policy identity; the fixture explicitly refuses source-profile admission. These measurements establish neither a performance trend nor new full-library memory acceptance. This evidence-only increment is non-material and governed by module-cost/source binding, cost reproduction, documentation guards, and the complete infrastructure gate; independent semantic review remains separate.

#### Scope-creation commutation calibration

On 2026-09-18, immutable commit `997309c7143ed0649df72d95435fec9666ffc439` supplied the expanded `BpmnSemantics.InternalScopeCreationFrameConformance`. All 419 archived Lean/build inputs match that commit and were checked before and after measurement; the capture's symbolic `HEAD` was resolved against those exact bytes and the measurement receipt's full commit identity. The pinned image, one CPU/thread, network isolation, 3 GiB ceiling, and zero swap allowance remain unchanged. With the dependency closure warm, the target's eight generated products were moved aside and their absence checked inside the container before `/usr/bin/time -v timeout --kill-after=10s 1800 ./scripts/lake.sh build BpmnSemantics.InternalScopeCreationFrameConformance`.

The confirmed rebuild exited zero in 34.77 seconds at 1,361,200 KiB GNU RSS and 1,750,228,992 cgroup bytes, with every controlled memory event, pressure total, and swap count zero. Build-output SHA-256 is `d547179cb567c51159f07a7b1a3a58d172842e7eecdb56f1076a7901572170c7`; the durable receipt passed its separate assertion. [The executable cost record](../scripts/lean-module-cost.ts) binds the exact measured source. The first bounded working-tree build passed in 38.62 seconds at 1,770,733,568 cgroup bytes. The root's complete Lean gate passed all 392 jobs. These measurements establish neither a performance trend nor new full-library memory acceptance. This resource-record increment is non-material; independent semantic review and complete repository verification remain separate.

#### Scope-creation control-position publication calibration

On 2026-09-18, immutable commit `27d619c0267b1d365129edce2a19931fec91fc14` supplied the expanded `BpmnSemantics.InternalScopeCreationFrameConformance`. All 421 archived Lean/build inputs match that commit and were checked before and after measurement. The pinned image, one CPU/thread, network isolation, 3 GiB ceiling, and zero swap allowance remain unchanged. With the dependency closure warm, the target's eight generated products were moved aside and their absence checked inside the container before `/usr/bin/time -v timeout --kill-after=10s 1800 ./scripts/lake.sh build BpmnSemantics.InternalScopeCreationFrameConformance`.

The confirmed rebuild exited zero in 53.30 seconds at 1,364,012 KiB GNU RSS and 1,796,804,608 cgroup bytes, with every controlled memory event, pressure total, and swap count zero. Build-output SHA-256 is `916f0656136a50f9d8dd08efd560b447feea86439d4bd8c6fece591b58d340ab`; the durable receipt passed its separate assertion. [The executable cost record](../scripts/lean-module-cost.ts) binds the exact measured source. The first successful bounded working-tree build passed in 46.18 seconds at 1,120,387,072 cgroup bytes after two retained import/syntax failures. The root's complete Lean gate passed all 394 jobs. These measurements establish neither a performance trend nor new full-library memory acceptance. This resource-record increment is non-material; independent semantic review and complete repository verification remain separate.

#### Scope-creation lifecycle publication calibration

On 2026-09-19, immutable commit `b589ab0a97951a4cf581eb7cf91684e849bcd93c` supplied the expanded `BpmnSemantics.InternalScopeCreationFrameConformance`. All 426 archived Lean/build inputs match that commit and the current source before and after measurement. The pinned image, one CPU/thread, network isolation, 3 GiB ceiling, and zero additional swap remain unchanged. The dependency closure stayed warm; only the target's eight generated products were moved aside, and their absence was checked inside the container before the measured build.

The confirmed rebuild exited zero in 72.62 seconds at 1,368,940 KiB GNU RSS and 1,157,017,600 cgroup bytes, with every memory event, pressure total, and swap count zero. Build-output SHA-256 is `ac38dd099aa812b2eb2c46e83afb78164ad9c18a77618cc08311e756f9fb97f5`; the durable receipt passed its separate assertion. [The module-cost guard](../scripts/lean-module-cost.test.ts) rejects the previous fixture source and requires this committed measurement before complete verification. This non-material evidence increment also records the implementation cost; semantic checkpoint review remains separate.

The successful bounded working-tree build passed in 96.36 seconds at 1,162,391,552 cgroup bytes, followed by the complete native Lean gate with 399 jobs. The preceding bounded run retained a rejection-fixture syntax failure and exited one at 1,945,378,816 cgroup bytes; an earlier socket-permission failure occurred before Lean started. The working-tree archive included four frozen legacy inputs outside the target closure; the immutable archive retains only the 426 current Lean/build inputs. These measurements establish neither a performance trend nor new full-library memory acceptance.

#### Scope-creation mixed-batch calibration

On 2026-09-19, immutable commit `10e774c5c2ecc1ddb6901b3e3d5a982f80228ad9` supplied both changed mixed-batch conformance owners. All 430 archived Lean/build inputs match that commit and current source before and after measurement. The pinned image, one CPU/thread, network isolation, 3 GiB ceiling, and zero additional swap remain unchanged. Each measured target had a warm dependency closure and its own eight generated products moved aside; the container checked their absence before building.

| Target | Elapsed | GNU maximum RSS | Cgroup peak | Build-output SHA-256 |
|---|---:|---:|---:|---|
| `BpmnSemantics.FiniteInternalTransitionConformance` | 8.31 s | 1,170,672 KiB | 1,493,434,368 bytes | `6dc2b1b2ff62a5f2349f180cc74d655d19bcf85e068b471ab87a7ccd2f89e253` |
| `BpmnSemantics.ScopeCreationMixedBatchConformance` | 80.59 s | 1,980,156 KiB | 1,970,282,496 bytes | `2e7e5485860914dff6c0d1faeddc86ba379b8dd4697b143fffe443a4b6be085b` |

Both builds exited zero with every memory event, pressure total, and swap count zero; their durable receipts passed separate assertions. The [module-cost guard](../scripts/lean-module-cost.test.ts) requires a new source-bound row and replacement of the changed finite-batch row before complete verification. This non-material evidence increment also updates the reproducible churn row and its affected rank denominators. The [commit rules](TESTING-SPEC.md#commit-boundaries) now distinguish measurement timing from binding to committed bytes, allowing successful unchanged pre-commit measurements to supply that evidence without a post-commit rerun.

Before the implementation commit, the complete 403-job Lean gate and all 940 semantic-core tests passed. The bounded production witness passed in 83.33 seconds at 2,011,357,184 cgroup bytes. Three earlier bounded runs retained fixture type-annotation failures, with peaks of 2,272,514,048, 2,291,757,056, and 2,222,071,808 bytes; a cache-mount failure occurred before Lean started. The finite witness's initial dependency rebuild passed in 61.89 seconds at 1,494,188,032 cgroup bytes. These measurements establish neither an uncontended performance trend nor new full-library memory acceptance.

#### Regional review-checkpoint calibration

On 2026-09-20, both new regional projection conformance targets were measured before implementation commit `4d4d8d76ddba14266264d62ea13b255bd70ec576`. All 513 archived Lean/build inputs were checked against the working tree and that commit. The pinned image, one CPU/thread, network isolation, 3 GiB ceiling, and zero additional swap remain unchanged. Each isolated measurement followed dependency prewarming and removal of only its target's eight generated products; the container checked their absence before building.

| Target | Elapsed | GNU maximum RSS | Cgroup peak | Build-output SHA-256 |
|---|---:|---:|---:|---|
| `BpmnSemantics.BoundaryTimerProjectionConformance` | 8.45 s | 1,218,364 KiB | 878,329,856 bytes | `385b1962aae7b5a3fc559463dae6d3fcf098bb93269457ae18badb176fe5eaca` |
| `BpmnSemantics.InternalRegionalCancellationProjectionConformance` | 70.22 s | 2,156,844 KiB | 2,187,943,936 bytes | `e0b004a29afa277a8d05ed067a7e43b84221f6bb687213dc91f2b7b5186b2176` |

Both commands exited zero with every memory event, pressure total, and swap count zero; their durable receipts passed separate assertions. Dependency prewarming also passed, at 2,952,351,744 cgroup bytes with zero events, pressure, and swap. An earlier mountpoint failure occurred before Lean started. These measurements supply the two source-bound module-cost rows, not new full-library memory acceptance or semantic approval.

The complete pre-commit semantic gate passed all 482 Lean jobs and 1076 core tests. Its first run exposed an implicit whole-state conversion exceeding the unchanged 200,000-heartbeat limit in [data arming](../BpmnSemantics/SemanticProcess/InternalDataArmingFrames.lean). Explicit operation-level matcher equality preserves every theorem statement and passes the narrow build in about one second; the subsequent complete gate reused successful unaffected outputs. Regional batching and the remaining proof obligations stay open for the owner-requested architectural review and required semantic-checkpoint review.

#### Call pairing calibration

On 2026-09-19, immutable commit `b304150ebab88f047b9e80094d6c5a579a9f8490` supplied `BpmnSemantics.CallActivityPairingConformance`. All 422 archived Lean/build inputs matched the commit and current source before and after measurement. The pinned image, one CPU/thread, network isolation, 3 GiB ceiling, and zero additional swap remain unchanged. After warming the dependency closure, only the target's eight generated products were moved aside; the confirmed isolated rebuild exited zero in 3.54 seconds at 1,081,832 KiB GNU RSS and 701,947,904 cgroup bytes. Every memory event, pressure total, and swap count was zero.

Build-output SHA-256 is `1b2acd871190eb6449a239e8c418dc1c8ba882cf4315e1a41c895cccc164dc73`; the durable receipt passed its separate assertion. [The module-cost guard](../scripts/lean-module-cost.test.ts) requires this missing source-bound row before complete verification. This evidence correction is non-material and leaves semantic review separate. The preceding dependency build passed in 255.55 seconds at 1,770,995,712 cgroup bytes; an earlier container mount failure exited before Lean started. No new full-library memory acceptance is claimed.

| Increment | Boundary | Code | Documentation | Elapsed | Comparison consequence |
|---|---|---:|---:|---|---|
| [Scoped runtime data](capsules/SCOPED-DATA-SPEC.md) | `08d8b84..3b2e44d` | `+540/-73` | `+134/-11` | Unknown | First atomic runtime-representation replacement; later scope work should not be compared as if it were a small local semantic clause. |
| [Profile-parameterized admission](PROFILE-PARAMETERIZED-ADMISSION-SPEC.md) | `e553b4b..dbfd702` | `+1760/-492` | `+160/-52` | Unknown | Larger than scoped data because it replaced source, Lean, core, host admission, and differential evidence, while removing recurring topology predicates. |
| [Intermediate Catch Message](capsules/INTERMEDIATE-CATCH-MESSAGE-SPEC.md) | `28d9a35..e273374` | `+3181/-82` | `+227/-119` | Unknown | Adds source references, a transition family, a public observation field, and durable Signal/result ingress together; not comparable to a same-contract mechanism reuse. |
| [User Task completion data](capsules/USER-TASK-COMPLETION-DATA-SPEC.md) | `8a5f3ac..5255888` | `+651/-94` | `+84/-63` | Unknown | Reuses the existing User Task occurrence and Update lifecycle while adding the bounded simulated-form patch contract. |
| [Process-start data](capsules/PROCESS-START-DATA-SPEC.md) | `416df39..07e7f17` | `+289/-77` | `+104/-36` | Unknown | Smallest measured data increment; reuses Process scope and the existing start boundary. |
| [Runnable Temporal MVP](RUNNABLE-TEMPORAL-MVP-SPEC.md) | `9b58437..32df044` | `+950/-132` | `+58/-22` | Unknown | Adds the first strict external-runtime config, command, event/result, exit, and orchestration boundary. |
| [Runnable MVP product surface](RUNNABLE-TEMPORAL-MVP-SPEC.md) | `c036868..4a12629` | `+1609/-653` | `+78/-174` | Unknown | Lifts the product command from one profile to all fifteen by replacing the single-task actor with an enabled-interaction driver, registering the effect Activity, and adding per-profile examples; adds no BPMN semantics, so its cost is host and configuration work rather than a semantic mechanism. Larger than the original runnable MVP increment because it replaces that command boundary wholesale instead of extending it. |
| [Bounded Product 1 Workflow chain](archived/TEMPORAL-WORKFLOW-CHAIN-BOUNDS-PROPOSAL.md) | `4431f13..18e1907` | `+23074/-1866` | `+1182/-856` | Unknown | The recorded figures are a contaminated conservative upper bound rather than a pure Workflow-chain attribution: the contiguous range includes the unrelated documentation-control-plane program, with `scripts/` alone contributing `+3921/-790`. No subtraction is applied. The nearest same-layer comparator is the Runnable MVP product surface above because both change Product 1 Temporal hosting without adding BPMN meaning, but this mixed range is not a clean cost comparison. The H2 contract itself spans recovery, atomic capacity, continuation, publication traversal, deployment admission, and forced Message, Timer, and effect refinement evidence; later semantic work must reuse that host boundary instead of reopening a Workflow-chain lane per BPMN family. |
| [Ordinary embedded Sub-Process completion](capsules/EMBEDDED-SUBPROCESS-COMPLETION-SPEC.md) | `5b34977..a59f8a3` | `+5266/-1698` | `+283/-158` | Unknown | Adds the first definition/runtime occurrence ownership model and atomically replaces all consumers; future nested-scope capsules should reuse this foundation and be materially smaller unless they add another cross-layer contract. |
| [Embedded Sub-Process Error propagation](capsules/SUBPROCESS-ERROR-PROPAGATION-SPEC.md) | `be7845d..1449771` | `+3370/-398` | `+218/-133` | Unknown | Materially smaller than the ordinary completion foundation while adding a new checked-node and transition family across every evidence lane; regional cancellation and direct-parent handler resolution reuse the existing scope-occurrence model as intended. The closure boundary includes the review corrections that extend the Temporal bypass through its stale-command discriminator and align every evidence claim with its actual witness strength. |
| [Message-addressed Receive Task](capsules/RECEIVE-TASK-MESSAGE-SPEC.md) | `2f39cc2..65d4076` | `+3805/-788` | `+504/-191` | Unknown | Code additions increased beyond User Task completion data, Intermediate Catch Message, and the preceding Sub-Process Error propagation capsule; documentation additions are rank 7 of 104 by documentation, while code additions are rank 33 of 104 by code. That code standing was rank 2 when this row was written; the Event-Based Gateway race, Call Activity, the interrupting Activity boundary Timer, and the A12 product-boundary increment have all since been recorded above it, which is why the claim is stated as a checkable rank rather than as an ordinal phrase that a later row can silently falsify. The range includes the atomic closed-channel replacement, checked-source corrections, retained-CIB lane, and closure evidence across every target. The closure removes one repeated process weight by sharing Message Temporal support, server, and Workflow bundle; later direct-Message consumers must reuse that boundary rather than repeat this cost. |
| [Inclusive Gateway selected-branch synchronization](capsules/INCLUSIVE-GATEWAY-SPEC.md) | `263433e..24d726b` | `+3123/-151` | `+115/-45` | Unknown | Smaller than the nearest recorded Embedded Sub-Process Error propagation increment in both code additions (`3123` versus `3370`) and documentation additions (`115` versus `218`). The measured range still crosses source admission, two reusable operation kinds, hidden owner-scoped runtime state, Lean relations and laws, independent TypeScript semantics, four registered standards-only differential cases, Temporal Worker-replacement/replay evidence, and closure reflection; the absence of a CIB lane and reuse of User Task hosting keep it below that comparator without implying a general Inclusive Gateway implementation. |
| [Event-Based Gateway Message/Timer race](capsules/EVENT-BASED-GATEWAY-SPEC.md) | `8efec58..378a0c8` | `+4606/-63` | `+140/-60` | Unknown | Code additions are rank 29 of 104 by code and documentation additions are rank 29 of 104 by documentation. Both exceeded the Inclusive Gateway comparator this row was written against (`4606` versus `3123` code, `140` versus `115` documentation), and that comparison still holds; the code standing was rank 2 when written, with Call Activity, the interrupting Activity boundary Timer, and the A12 product-boundary increment recorded above it since. The range adds one checked Gateway and hidden occurrence family like that comparator, but also owns cancellable durable Timer scheduling, activation-batch fail-closed detection, direct pinned-SDK premise evidence, four live histories, and hostile scheduler mutations. These mechanisms account for the changed surface but do not discount the measured increase. |
| [Bounded called-Process Call Activity](capsules/CALL-ACTIVITY-SPEC.md) | `dee189c..bd0f0d3` | `+5801/-392` | `+160/-71` | Unknown | Code additions exceed the ordinary embedded Sub-Process foundation (`5801` versus `5266`), while documentation additions are lower (`160` versus `283`). The contiguous lifecycle range includes the separately owned `97750ec` review-process improvement, so the measured code increase is a conservative upper bound for Call Activity rather than a pure feature attribution; no subtraction is used to make the comparison look cheaper. The feature itself adds a second root definition, distinct semantic instance identity, paired invocation/return, source and wire replacement, Lean/core/Temporal evidence, and registered differential artifacts while reusing the existing User Task Update host. |
| [Interrupting Activity boundary Timer](capsules/ACTIVITY-BOUNDARY-TIMER-SPEC.md) | `7325fee..426bab8` | `+5521/-838` | `+363/-31` | Unknown | Code additions are rank 25 of 104 by code, behind Call Activity at `+5801` and the A12 product-boundary increment, and ahead of ordinary embedded Sub-Process completion at `+5266`. Documentation additions are rank 11 of 104 by documentation, behind Message-addressed Receive Task at `+504`. The code figure is consistent with a capsule that adds one composite transition family, a durable host deadline scheduler, a direct-VM harness, and two registered schedules. The documentation figure is the one worth attention, because unlike the Receive Task row above it this capsule replaced no wire contract and added no retained-CIB lane, so its prose is process output rather than a wider artifact surface. Three governed review stages including one rejected closure and one rejected closure retry, multi-round correction cycles, five new process-assessment findings, three new executable guards, and three recorded evidence exclusions or deferrals each produced durable prose. Code cost fell against the nearest comparator while documentation cost rose by more than double, so the reflection rule applies on the measurement that did not fall: the process weight selected for removal before the next capsule is the duplicated host readiness-and-schedulability shape, which this capsule copied from `event-race-readiness-scheduler.ts` under an explicit plan instruction and which produced a lost-command defect whose symptom pointed at a missing deadline withdrawal instead. Two complete consumers now exist, so one shared owner replaces the copy rather than anticipating a third. |
| [Lean comment discipline](archived/LEAN-COMMENT-DISCIPLINE-PROPOSAL.md) | `cc1ae8f..35ae276` | `+1607/-1117` | `+26/-12` | Unknown | About one quarter of the code additions in the nearest source-ownership hygiene increment (`9f1f046..7674a02`, `+6210/-5144` code and `+18/-6` documentation). Most churn is ownership-preserving Lean extraction plus the focused scanner and tests; the selected comment backfill remains small and no density target is inferred from either measurement. |
| [Interrupting Sub-Process boundary Timer](capsules/SUBPROCESS-BOUNDARY-TIMER-SPEC.md) | `c29db82..5a795c7` | `+3970/-1002` | `+428/-62` | Unknown | The nearest comparator is the interrupting Activity boundary Timer above, which shares the trigger, the `PT1S` literal, the victory shape, and the host hazard, and differs only in what the deadline bounds. Code additions fell against it (`3970` versus `5521`) and that fall is the reusable-mechanism claim being paid off rather than a smaller feature: this capsule added no new host scheduler, no new direct-VM harness, and no new durable timer ownership, instead parameterizing the sibling's scheduler over a host-family descriptor and reusing the harness unchanged. Removals rose (`1002` versus `838`) because that generalization deletes the copied module rather than leaving it beside a third. Documentation additions rose as measured (`428` versus `363`), **but this range is contaminated and the figure is an upper bound rather than an attribution.** Eighteen of its forty commits are an unrelated Lean kernel-decide, memory, and thread-pin episode plus MVP-guide renames and process-guard work; the closure review measured their contribution at `+137` documentation and `+668` code, which puts this capsule's own documentation addition near `291` against the `363` comparator, so documentation did not actually rise. No subtraction is applied to the recorded figures, exactly as the Call Activity row above declines to subtract its own contaminating commit, but the inference must not be read as a clean attribution. The selected weight below is therefore retained as a genuine documentation subtraction that stands on its own merit, not as an action the reflection rule compelled. The process weight selected for removal before the next capsule is the **per-capsule diary that [PLAN.md](PLAN.md)'s `Exact resume point` has become**: that section now carries superseded resume narrative for several closed capsules whose specifications and Git history already own it, so every capsule pays to append to a section nobody prunes, and this capsule added five paragraphs to it. Its owner definition is the *current* checkpoint and resume point, and [CLAUDE.md](../CLAUDE.md#milestone-and-capsule-reflection) already forbids a retrospective diary per capsule; the next capsule prunes closed-capsule narrative to its owning specification before adding its own. |
| [Non-interrupting boundary Timer](capsules/NON-INTERRUPTING-BOUNDARY-TIMER-SPEC.md) | `736b9fc..1c41cdd` | `+3578/-713` | `+313/-20` | Unknown | The nearest comparator is the interrupting Sub-Process boundary Timer above, which shares the trigger, the `PT1S` literal, and the host-family scheduler, and differs in what firing does to the host. Both measures fell: code `3578` versus `3970`, documentation `313` versus `428`. Documentation fell against a contaminated comparator whose own row estimates its clean figure near `291`, so against that estimate this capsule's prose is level rather than lower, and no reduction is claimed from the raw pair alone. The code fall is the reusable-mechanism claim paying off a second time: the third `BoundedDeadlineFamily` is a descriptor beside two existing ones, the direct-VM harness and the boundary-deadline assertions were reused unchanged, and no new host scheduler, timer ownership, or wire contract was added. The range also carries two corrections that are not this capsule's feature, both surfaced by its own reviews: the parser boolean-coercion class defect across five `supported` requirement rows, and the explicit-metamodel-default admission defect across 36 corpus instances. Neither is subtracted. Cost fell on both axes, so the reflection rule compels no removal; the weight the previous row selected was executed rather than deferred, and this capsule's `Exact resume point` records only its current state. |
| [Preserve-only admission](PRESERVE-ONLY-ADMISSION-SPEC.md) | `d888ef0..06a8b08` | `+3053/-407` | `+206/-17` | Unknown | Code additions are rank 48 of 104 by code and documentation additions are rank 19 of 104 by documentation. The nearest comparator by layer is [profile-parameterized admission](PROFILE-PARAMETERIZED-ADMISSION-SPEC.md) at `+1760/-492` code and `+160/-52` documentation, which this proposal extends rather than replaces: both change source admission, profile artifacts, and differential evidence, and neither adds a transition family, a Lean lane, or a host mechanism. **Both measures rose**, code by 73% and documentation by 29%, and the largest single reason is that this capsule closed two admission holes the allowlists could not see — foreign attributes stored in the non-enumerable `$attrs`, and resolved references never checked against their declared target type — each of which needed a rule, its diagnostics, and its own seeded-defect suite rather than an allowlist entry. Cost did not fall, so the reflection rule applies. The process weight selected for removal before the next capsule is **installing a document-wide admission rule inside each profile source reader**: this capsule installed the reference-target rule in two of four readers and shipped the omission through a semantic-checkpoint review, then had to touch all four readers again when the diagnostic contract changed. The removal is executed rather than promised: the rule now runs once above the reader dispatch, its derived boolean wrapper is deleted, and a case per dispatch path pins the class. It bears on the measure that rose, because reader-by-reader installation is what made both admission rules wide changes. The profile-parameterized foreign-attribute rule stays per profile, which is the honest distinction rather than the same omission preserved. |
| [Interchange admission composition](INTERCHANGE-ADMISSION-SPEC.md) | `adec37e..42e62e5` | `+1775/-260` | `+88/-46` | Unknown | The nearest layer comparator is [profile-parameterized admission](PROFILE-PARAMETERIZED-ADMISSION-SPEC.md) at `+1760/-492` code and `+160/-52` documentation because both change profile admission, Lean and core profile identity, CIB and differential evidence, and Temporal hosting without adding a transition family or public observation. Code additions rose by 15, about 0.9%, while documentation additions fell by 72, 45%. The increment adds a total profile-by-surface value-domain contract, one closed preservation composition, independently bound source/twin and declaration/dispatch guards, registered CIB and corpus evidence, narrow Lean/core reuse, Product 1 Worker-replacement/replay evidence, and one release-pipeline case. The code-axis process weight exposed by the complete gate was remembering to add a runnable Product 1 example for every registered profile; the measured range removes it by making the focused port-free example guard derive exact coverage from the registered profile catalog. No semantic, proof, CIB, or hosting lane is removed to manufacture a larger reduction. Closure-review, receipt, and graduation commits are excluded. |
| [Per-element admission diagnostics](PRESERVE-ONLY-ADMISSION-SPEC.md) | `312684f..fb4b7dc` | `+1153/-110` | `+43/-38` | Unknown | Code additions are rank 67 of 104 by code and documentation additions are rank 64 of 104 by documentation. The nearest comparator by layer is preserve-only admission above: both change source admission diagnostics across the same four dispatch paths without adding a transition family, Lean account, CIB relationship, or host mechanism. Both additions fell, code by 62% and documentation by 79%. This increment reused the existing diagnostic union and public result, replaced projector-local key lists with one shared inventory, and kept accepted artifacts exact, so the smaller surface is the intended mechanism reuse rather than a narrower dispatch denominator. The measured span includes both rejected review corrections and the ordinary checkpoint receipt. Because both measures fell, the reflection rule requires no further process-weight removal. |
| [A12 add-on product boundary](A12-ADD-ON-BOUNDARY-SPEC.md) | `2e109b0..adf757b` | `+16959/-682` | `+194/-76` | Unknown | The nearest comparator by changed layers is profile-parameterized admission at `+1760/-492` code and `+160/-52` documentation: both cross source/profile admission, checked and Semantic Process identity, Lean, the core, host admission, and differential evidence without adding a transition family. Documentation additions exceed the comparator by `34`, while the full code figure is about 9.6 times the comparator. The measurement deliberately includes `13794` code additions under the immutable frozen legacy source tree required by the owner's no-discard decision; subtracting them would make the retained evidence obligation disappear from the cost record. The remaining `3165` additions cover the live overlay, neutral-profile, cross-language identity, live non-null-overlay Temporal replay, CIB, preservation, and product-boundary mechanisms. The removable process weight was hand-maintained or manifest-seeded guard coverage: the target replaces it with a baseline-derived typed business/catalog inventory, fixed original dependency roots, exact route derivation, and seeded coherent mutations, so a future boundary correction does not pay repeated reviewer rounds for the same coverage class. The following status-binding commit changes only lines already counted as documentation replacements, so it does not change either measured total. |
| [Resumption-bounded cyclic control flow](capsules/CYCLIC-CONTROL-FLOW-SPEC.md) | `7529150..de866bf` | `+5795/-283` | `+354/-41` | Unknown | The profile-parameterized admission comparator is `+1760/-492` code and `+160/-52` documentation, while the closer gateway transition-family comparator, Inclusive Gateway selected-branch synchronization, is `+3123/-151` code and `+115/-45` documentation. The Exclusive Gateway conditional-routing cost remains unknown under this ledger's historical-baseline rule. Both measured additions rose against either recorded comparator. The range adds the first profile-selected cyclic graph policy, a general declarative Exclusive Merge relation, strict source and wire replacement, material Lean path, actual-reachability, step-completeness, and closure proofs, a baseline-preservation oracle, one registered answer-free scenario, differential mutation evidence, and a Worker-replacement/stale-refusal/replay witness. No removable code process weight was found: deleting one of those independent mechanisms or evidence lanes would weaken the reviewed claim rather than simplify its delivery. Graduation removed the proposal-only implementation owner and headroom inventory, whose repeated manual completion consumed four proposal audits and became stale during implementation. Future proposals still derive their live inventory through `what-binds` and the exhaustive build guards, but closed specifications retain only the resulting contract and evidence. |
| [Message Start Event](capsules/MESSAGE-START-EVENT-SPEC.md) | `8442e1a..c31c7c1` | `+3584/-138` | `+165/-97` | Unknown | The approved nearest comparator is resumption-bounded cyclic control flow above because both increments cross checked source, IL, Lean, the TypeScript core, strict artifacts, registered answer-free evidence, and live Temporal hosting. Message Start additions fell by 38% in code (`3584` versus `5795`) and 53% in documentation (`165` versus `354`). The reduction is mechanism reuse rather than a narrower evidence denominator: the capsule reuses whole-graph acyclic admission, root-token production, the existing User Task lifecycle, the concrete Temporal start client, replay support, and the standards-only differential lane, while adding a distinct source, IL, trigger, and runnable configuration arm. It adds no new Workflow command handler, CIB lane, subscription, or broker. Both measured additions fell, so the reflection rule requires no process-weight removal. |
| [Runtime-state invariant](RUNTIME-STATE-INVARIANT-SPEC.md) | `6a674db..6456b26` | `+2856/-641` | `+129/-30` | Unknown | The nearest recorded comparator is Message Start Event above, the most recent capsule crossing Lean, the TypeScript core, and Temporal hosting together. Both measures fell, code by 20% (`2856` versus `3584`) and documentation by 22% (`129` versus `165`), **but the fall is scope rather than mechanism reuse and must not be read as the latter.** This increment adds no checked source, no IL node, no operation kind, no wire contract, no public observation field, no scenario, and no CIB lane, so it changed strictly fewer layers than every capsule comparator in this table; a smaller figure is what that predicts. The documentation figure is also net of graduation: the range creates a proposal and then deletes it, so only the resulting specification is counted, and the specification was written from the implemented source rather than extracted from the proposal's accumulated review prose, which took it from 4,254 to 2,225 words at equal contract coverage before restorations brought it to 2,392. That method change is the one durable subtraction this increment makes on the documentation axis, and it is recorded here because the raw fall would otherwise be credited to scope alone. What this table does not measure is the increment's actual dominant cost: two semantic-checkpoint correction rounds and four closure rounds, three of them owner-authorized past the standing two-round bound. Churn fell while review cost was the highest recorded for any capsule, so this row is evidence that nonblank churn is not a proxy for closure effort on a cross-cutting invariant whose surface is claims rather than code. Because both measured axes fell, the reflection rule compels no further removal; the process weight identified instead is recorded in [the process-assessment ledger](PROCESS-ASSESSMENT-LEDGER.md): the review target lived outside the tree for its whole closure, so no gate and no reviewer diff could see a correction until it landed. |
| [Timer Start Event](capsules/TIMER-START-EVENT-SPEC.md) | `fa79688..61b9bb7` | `+3711/-141` | `+149/-98` | Unknown | The approved nearest comparator is Message Start Event above because both increments cross checked source, start IL, Lean, the independent core, strict artifacts, registered answer-free evidence, runnable configuration, and Temporal start hosting. Timer Start code additions rose by `127`, about 3.5%, while documentation additions fell by `16`, about 9.7%. The increment reuses triggered-root semantics, the User Task lifecycle, the Workflow implementation, strict wire machinery, replay support, and the standards-only differential lane. Its additional code establishes the distinct Temporal Schedule service boundary, Worker absence through the due occurrence, service-returned opaque Workflow/Run identity, and meaningful stored-action/direct-start mutations. No removable code process weight exists without weakening the reviewed claim, so the reflection rule records the reason rather than manufacturing a deletion. |
| [Terminate End Event](capsules/TERMINATE-END-EVENT-SPEC.md) | `3b368a2..c92cd27` | `+3689/-199` | `+111/-76` | Unknown | The approved nearest comparator is Embedded Sub-Process Error propagation at `+3370/-398` code and `+218/-133` documentation because both increments change checked source, regional cancellation, Lean, TypeScript, strict wires, registered evidence, and Temporal Worker-replacement hosting. Code additions rose by `319`, about 9.5%, while documentation additions fell by `107`, about 49.1%. The increase establishes complete represented-owner-family cancellation, root and nested proof boundaries, three answer-free scenario and differential discriminators, and a test-owned durable global-cancellation mutation. Removing any of those mechanisms would weaken the reviewed claim, so no removable code process weight is manufactured. The existing metamodel-default guard exposed the explicit-false source contradiction before registration, while proposal and checkpoint corrections stayed inside the governed review cycle; no new process-assessment entry is warranted. |
| [Configured Task extension](capsules/CONFIGURED-GENERIC-TASK-SPEC.md) | `6026fae..fe234e9` | `+3276/-593` | `+82/-55` | Unknown | The nearest mechanism comparator is the historical Service Task effect increment at `fe83605..77eeb1b`, measured reproducibly at `+4069/-284` code and `+180/-97` documentation. Configured Task additions fell by 20% in code and 54% in documentation because it preserves a distinct checked Task type and exact extension binding while reusing the existing `awaitEffect`, effect stimulus, runtime state, Activity transport, handler registry, and User Task continuation. Code removals rose because the checkpoint extracted shared profile-shape and effect-origin owners before feature growth instead of extending near-limit mixed owners. Both additions fell, so the reflection rule requires no process-weight removal. |
| [Service Task incident and retry](capsules/SERVICE-TASK-INCIDENT-RETRY-SPEC.md) | `43228e3..be46057` | `+6038/-966` | `+185/-118` | Unknown | The nearest mechanism comparator is the historical Service Task effect increment at `+4069/-284` code and `+180/-97` documentation because this capsule preserves its exact source, checked graph, IL, effect occurrence, Activity transport, and successful completion while adding the configured failure-and-resumption path. Code additions rose by 1,969 and documentation additions by 5. The code increase establishes a new strict incident state and two commands across TypeScript and Lean, independent configured CIB job/incident evidence, raw-to-canonical fidelity, one four-target differential schedule, and live Temporal report, retry, race, Worker-replacement, history, replay, and post-retry-host-failure evidence. The implementation removed duplicated process weight by extracting the shared retained-Update recovery lifecycle and by making one immutable Activity policy own both retry attempts and technical-result routing instead of copying the User Task client or duplicating retry numbers in the crowded Workflow. No further mechanism can be removed without dropping an independently failing semantic, CIB, proof, or durability boundary, so the reflection records the increase rather than manufacturing a narrower claim. The range includes its first measurement-binding commit; this corrective rebinding and later graduation commits are excluded. |
| [Service Task incident-scoped Process cancellation](capsules/SERVICE-TASK-INCIDENT-CANCELLATION-SPEC.md) | `0f043cd..9007232` | `+6438/-354` | `+122/-113` | Unknown | The nearest comparator is Service Task incident and retry above because this successor reuses its exact BPMN source, checked graph, IL effect occurrence, incident identity, Activity result policy, and retained-Update recovery while adding one incident-gated root lifecycle command. Code additions rose by 400 while documentation additions fell by 63. The code increase covers incident-aware transitive subtree cleanup and preservation in TypeScript and Lean, strict cancelled state and receipt, configured CIB external deletion with positive `EXTERNALLY_TERMINATED` history, a four-target cancellation schedule, and live Temporal Worker replacement, ordinary completion, retained closure receipt, replay, and native-termination discrimination. The implementation also removed two recurring process costs before closure: the CIB runner's validation responsibilities moved into a focused owner before the near-limit runner grew, and the differential pipeline now has one typed adapter-lifecycle relation instead of scenario-specific post-terminal assumptions in both comparison and summary code. Graduation removes the proposal-only implementation headroom inventory. Removing another independent semantic, proof, CIB, or durability discriminator would weaken the selected claim, so no narrower cost is manufactured. Measurement-binding and graduation commits are excluded. |

| [Product 2 definition scheduling](BPM-PLATFORM-DEFINITION-SCHEDULING-SPEC.md) | `24901a5..232a352` | `+8606/-149` | `+52/-20` | Unknown | The nearest recorded product-host comparator is the [Runnable MVP product surface](RUNNABLE-TEMPORAL-MVP-SPEC.md) at `+1609/-653` code and `+78/-174` documentation. Code additions rose because this increment adds a strict public contract, exact-version capability projection, crash-recoverable SQLite lifecycle, handle-free Schedule host, HTTP and UI surfaces, live cancellation and replay evidence, and browser acceptance; documentation additions fell because it reuses the approved Product 2 and Timer Start boundaries. Cost did not fall, so the reflection rule applies. The removable process weight found during implementation was test-only error-constructor injection through the production web API to accommodate Node module resolution; it was removed in the measured range by importing the existing errors directly through explicit source paths under the no-emit type gate. Sharing the M1 and M2 composition hosts would require a new cross-milestone package or dependency and is not a genuine subtraction from this increment. |
| [Product 2 Message Start ingress](BPM-PLATFORM-MESSAGE-INGRESS-SPEC.md) | `4cf0de0..3c11413` | `+7506/-176` | `+49/-21` | Unknown | The nearest product-host comparator is Product 2 definition scheduling at `+8606/-149` code and `+52/-20` documentation. Message ingress reuses exact-version deployment, the SQLite lifecycle pattern, server and UI composition, the existing direct Message Start Workflow, and Worker-replacement, history, replay, and browser evidence. Its new mechanism is the complete operation-addressed public capability plus a durable no-redispatch receipt that reports retained ambiguity honestly. Code additions fell by 1,100 and documentation additions by 3, so both addition measures fell and the reflection rule requires no process-weight removal. |
| [Product 2 Process-instance search](BPM-PLATFORM-PROCESS-INSTANCE-SEARCH-SPEC.md) | `cb71827..8a87cf4` | `+4802/-7` | `+62/-30` | Unknown | The nearest product-host comparator is Product 2 Message Start ingress above because both increments add a strict public contract, durable SQLite lifecycle, three-layer server and web composition, live Temporal discrimination, and browser acceptance. Process-instance search reuses the three existing confirmed-start producers and exact deployed-definition identity while adding an independent append-only index, classified same-ID races, stable keyset paging, exact filters, and private-fact exclusion. Code additions fell by 2,704 while documentation additions rose by 13. The increase is the independently documented `operate` module and its truthful identity-only/search-loss boundary plus the new global HTTP/UI and showcase gate owners; removing any of those current contracts or registries would make the implemented surface less reviewable, so no documentation process weight can be removed without weakening the closure record. |
| [Boolean Process data](capsules/BOOLEAN-PROCESS-DATA-SPEC.md) | `2cd983c..3a2f52f` | `+2806/-252` | `+91/-63` | Unknown | The nearest comparator is User Task completion data at `+651/-94` code and `+84/-63` documentation because both increments use the same completion command, Process-scope merge, CIB public-service overlay, Lean and independent TypeScript execution, differential comparison, and Temporal Update lifecycle. Boolean code additions rose by 2,155 and documentation additions rose by 7. The increases reflect an additive value constructor crossing every exhaustive TypeScript, Lean, Java/CIB, retained-evidence, command-identity, and Temporal encoding boundary, profile-aware refusal at deployment and live ingress, a mandatory Java protocol extraction, registered evidence, and distinct durable stringification and outside-core mutations. The range conservatively includes the separately guarded 27-line `what-binds` worktree-rename correction. Removing a cross-language boundary, independent stringification/refusal witness, or the selected extension's authority and evidence record would weaken reviewability or the claim, so no code or documentation process weight is manufactured. The separate guard correction removes the implementation friction it exposed before the next increment. |
| [User Task assignment and form metadata](capsules/USER-TASK-ASSIGNMENT-FORM-METADATA-SPEC.md) | `007fc8c..4f332dd` | `+4127/-182` | `+104/-62` | Unknown | The nearest comparator is Boolean Process data because both increments cross the same User Task wire, profile, CIB, Lean, semantic-core, differential, and Temporal layers. Code additions rose by 1,321 and documentation additions by 13. This increment adds exact namespace-aware source admission, a passive checked/IL/runtime/public metadata value, independently retained Task Service and Form Service evidence, and candidate, field-key, field-type, source-projection, and live Query-omission discriminators while reusing the existing completion transition and Temporal Update mechanism. The implementation paid the proposal's selected removal before growth by extracting raw CIB evidence types from the near-limit artifact owner and placing public-service observation in a separate Java projector. No additional code or documentation process weight can be removed without dropping an independent source, public-service, proof, or durable-projection boundary, so none is manufactured. |
| [Parallel User Task metadata composition](capsules/PARALLEL-USER-TASK-METADATA-COMPOSITION-SPEC.md) | `6388f99..25d03e0` | `+3522/-111` | `+79/-64` | Unknown | The nearest mechanism comparator is User Task assignment and form metadata above because this composition crosses the same profile, CIB, Lean, semantic-core, differential, and Temporal boundaries while reusing its passive metadata representation and completion transition. Code additions fell by 605 and documentation additions by 25 even though this increment also adds a retained real-world model and a production-backed Product 2 journey. The measured range is a conservative upper bound because it includes the separate serialized-Lake and narrow-Lean-import corrections; nothing is subtracted. The reduction is the intended payoff from composing the existing exact balanced-parallel topology and metadata mechanisms rather than introducing another runtime representation, command, public field, or Temporal primitive. Both addition measures fell, so the reflection rule requires no process-weight removal. |
| [Product 2 human work](BPM-PLATFORM-HUMAN-WORK-SPEC.md) | `63d3ca9..e736a7e` | `+13689/-258` | `+103/-62` | Unknown | The nearest product-host comparator is Product 2 Process-instance search at `+4802/-7` code and `+62/-30` documentation because both increments consume the same three confirmed-start producers, add a strict public contract and independent SQLite state, compose HTTP and React surfaces, and retain live Temporal plus browser evidence. Human-work additions rose by 8,887 code lines and 41 documentation lines because the increment adds private exact host addressing, fresh all-or-error task observation, actor policy, claim/release/completion recovery, typed detail, same-transaction audit outbox, a reusable UI kit, and full mutation evidence rather than an identity-only index. The closure corrections add retained-claim generation checks, real browser transport-loss and indeterminate retry identity, cross-store audit recovery, every observation classification, audit-silent hidden-task mutations, privacy-safe host diagnostics, both cross-type form mismatches, and discriminating recursive private-fact scans. The implementation removed two obsolete parallel mechanisms before closure: the checkpoint-only Work store and the legacy producer recording port were deleted after the shared durable confirmed-publication lifecycle took ownership. Removing another independent failure boundary would weaken the product contract, so no further process weight is manufactured. The following measurement-binding and status commits are excluded from the implementation boundary. |
| [Product 2 incident operations](BPM-PLATFORM-INCIDENT-OPERATIONS-SPEC.md) | `d1188cb..914950e` | `+13373/-543` | `+97/-54` | Unknown | The nearest product-host comparator is Product 2 human work because both increments consume confirmed Process registrations, add private Product 1 locators and fresh all-or-error aggregation, authorize exact state-changing actions, persist retry-safe recovery and audit, compose strict HTTP and React workspaces, and retain real Temporal, replay, browser, and four-width evidence. Incident-operations additions fell by 316 code lines and 6 documentation lines while adding current incident classification, content-bound Retry/Cancel, terminal corroboration, and an Operations workspace. The reduction comes from reusing the confirmed-publication lifecycle, identity policy, audit boundary, UI kit, and existing incident semantics instead of creating parallel platform infrastructure. Both addition measures fell, so the reflection rule requires no further process-weight removal. |
| [Committed execution publication](capsules/COMMITTED-EXECUTION-PUBLICATION-SPEC.md) | `f0a002d..63ad14f` | `+13750/-261` | `+71/-42` | Unknown | The approved comparators are Product 2 incident operations above for the Product 1 Query plus Product 2 projection/UI path and resumption-bounded cyclic control flow at `+5795/-283` code and `+354/-41` documentation for repeated semantic execution. Against incident operations, code additions rose by 377, about 2.8%, while documentation additions fell by 26, about 26.8%. Against cyclic control flow, code additions rose by 7,955 while documentation additions fell by 283. The code increase establishes replay-complete evaluator-root trace and position accounts in TypeScript and Lean, strict canonical wire, retention-bounded Workflow publication, live replay, transactional gap-detecting projection and rebuild, and History/Diagram/export adoption across both products. It reuses confirmed locators, Operations authorization, SQLite transaction patterns, existing diagrams, and the path-scoped UI-quality lane. The removable process weight exposed by the hosted gate was independently configured parent and child deadlines that let macOS kill healthy nested CIB work; the measured range replaces that mechanism with strict parent-over-child deadline ordering and a planted class guard. Cohesive closure, protocol, and Product 2 decoder extractions prevent the near-limit owners from becoming recurring integration costs. The following measurement-binding and graduation commits are excluded. |
| [Flow-node occurrence metrics](capsules/FLOW-NODE-OCCURRENCE-METRICS-SPEC.md) | `6520448..7a776b3` | `+12836/-147` | `+34/-20` | Unknown | The approved comparators are committed execution publication above for the additive Product 1 observation, Query, projection, and diagram-backed UI path, and Product 2 incident operations at `+13373/-543` code and `+97/-54` documentation for fresh all-or-error aggregation and an Operations surface. Flow-node metrics code additions fell by 914, about 6.6%, against E1 and by 537, about 4.0%, against incident operations; documentation additions fell by 37 and 63 respectively. The measured range establishes exhaustive Lean and TypeScript occurrence lifecycle projection, replay-stable command-batch time, strict E1-aligned publication, live Worker-replacement and replay evidence, exact-version transactional projection, bounded complete-population aggregation, authorization-first HTTP, and Frequency/Duration diagram and table presentation. It reuses E1 revision and cursor identity, confirmed Process registrations, Operations authorization, SQLite transaction patterns, and the existing bpmn-js workspace. Both addition measures fell against both same-layer comparators, so the reflection requires no manufactured product-code deletion. Integration did expose repeated package-graph builds across otherwise focused gates; the later build-once/reuse-only correction and executable efficiency guard are deliberately outside this capsule measurement. Closure-review, measurement-binding, and graduation commits are excluded. |
| [Product 2 operator history and audit export](BPM-PLATFORM-OPERATOR-HISTORY-AUDIT-EXPORT-SPEC.md) | `eec10da..cb2a026` | `+3608/-216` | `+22/-19` | Unknown | The nearest product-only comparator is Product 2 Process-instance search at `+4802/-7` code and `+62/-30` documentation because both add a strict public contract, confirmed-instance route, server and web composition, privacy boundary, and browser acceptance. Operator-history additions fell by 1,194 code lines and 40 documentation lines while adding bounded snapshots to two existing audit stores, an Operations-authorized canonical attachment, independent execution/audit availability, and 1280/1600 acceptance. The reduction comes from reusing the existing audit events, outboxes, confirmed-instance registry, authorization policy, canonical JSON mechanism, Process-detail shell, and UI kit rather than adding another producer, store, or chronology. Both addition measures fell, so the reflection requires no manufactured process-weight removal. Graduation and receipt bookkeeping are excluded. |
| [Product 2 initial web bundle](ARCHITECTURE.md#user-interface) | `3550bdf..ed1c0ea` | `+262/-130` | `+10/-8` | Unknown | The nearest web-surface comparator is Product 2 operator history and audit export above because it is the immediately preceding increment that grew the same application and browser evidence. Bundle-boundary additions are smaller by 3,346 code lines and 12 documentation lines because this increment changes no public contract or product behavior: it defers the existing Definitions, Operations, About, and BPMN viewer modules through native Vite and React boundaries, adds one manifest-based initial-graph guard, and retains the existing browser journeys. The complete initial Work JavaScript graph fell from 809,893 bytes to 464,930 bytes without raising the warning threshold. Both addition measures fell, so the reflection requires no process-weight removal. |
| [Product 2 structured Human Work](BPM-PLATFORM-STRUCTURED-HUMAN-WORK-SPEC.md) | `0c5e746..88a999e` | `+6520/-1052` | `+138/-105` | Unknown | The nearest product comparator is Product 2 human work above because both increments own definition-bound task detail, completion validation, durable retry recovery, HTTP and React surfaces, Work audit, and live browser evidence. Structured Human Work additions fell by 7,169 code lines while adding six regular field kinds, multiple resolution actions, action-dependent input, an immutable exact-source catalog, Zod-backed server validation, canonical patch computation, epoch-3 persistence, and a retained real-world journey. Documentation additions rose by 35 because this increment also records the explicit Product 1/Product 2 form-authority split and the catalog projection boundary. The range reuses the existing claim, completion, audit, authorization, UI kit, and engine command mechanisms, and removes the closed-variant cloning from the old Work mutation owner instead of extending its switch. It keeps the Work inbox eager while moving optional workspaces, task detail, structured form, and diagram code behind natural boundaries; the like-for-like complete default Work static-import graph is 462,859 bytes, down from 464,930 before M6. The remaining documentation increase describes durable public contracts and exclusions rather than duplicated implementation sequencing, so the reflection requires no manufactured process-weight removal; graduation removed proposal-only owner and headroom material after closure review. |

| [Activity occurrence ownership](ACTIVITY-OCCURRENCE-OWNERSHIP-SPEC.md) | `44131ca..a601896` | `+2909/-673` | `+114/-68` | Unknown | The nearest comparator is the runtime-state invariant above at `+2856/-641` code and `+129/-30` documentation, because both cross Lean, the TypeScript core, and the Temporal adapter while adding a runtime representation rather than a BPMN capability. Code additions rose by 53, about two percent, and documentation additions fell by 15. This row has now been measured from the wrong place twice and is corrected here against the receipt rather than re-remembered. The first version stopped at `55ab76e` and missed four later documentation commits, which the closure review caught. Its replacement figures, `+3085/-690` code and `+118/-62` documentation over `44131ca..7e1c17f`, reproduce from neither that range nor any other endpoint of this capsule, so they were taken from a working tree rather than from a commit pair; [the reproduction guard](../scripts/capsule-cost-reproduction.test.ts) is what makes that otherwise invisible state a gate failure. The endpoint is now the closure-audit commit recorded in the capsule's [review receipt](ACTIVITY-OCCURRENCE-OWNERSHIP-SPEC.md#independent-cold-review-receipt), which is what the update rule means by measuring to the closure target, and the graduation and next-step commits after it are excluded. The direction of both comparisons survives the correction. The weight identified against the measure that did not fall is the single-language census: the capsule enumerated six derivation sites in TypeScript and the reviewer found that the reference interpreter still ran the retired mechanism, which turned one migration into two passes and made the second pay for reworking two declarative relations and their proofs. The removal is the two-language guard enumeration, which makes the omission a gate failure instead of a review finding, so the next capsule touching this mechanism cannot repeat the split. |
| [Activity body turnover](ACTIVITY-OCCURRENCE-OWNERSHIP-SPEC.md#stable-semantic-rules) | `30bd89a..0971fc1` | `+2276/-429` | `+168/-33` | Unknown | The nearest comparator is Activity occurrence ownership above at `+2909/-673` code and `+114/-68` documentation, because this amendment extends that exact account and reuses its record, guards, and families. Code additions fell by 806, about 28 percent, and documentation additions rose by 38, about 33 percent. The first version of this row recorded `96672f6..d9163f1`, whose baseline is the previous capsule's own closure tail plus unrelated infrastructure work, and whose figures reproduced from neither that range nor any other; the baseline here is the commit before the proposal landed. The fall is worth reading precisely, because it happened while paying for a proof this account had never needed: 1,173 of the 2,103 added code lines are Lean, and 953 of those are the two owners this capsule created, the collection-order theory and the preservation proof. That theory is reusable and the next preservation lane should not pay it again; if the following capsule's Lean cost is comparable, the theory did not generalise and that is the signal to read. The rise is documentation, and it is the review record itself: three governed reviews, the last of which refused closure, produced findings corrected in place. **Weight removed before the next capsule:** the runtime-and-proof map's per-capsule status sections had grown to fill its reviewed budget, so a raise sized to the whole remaining set replaces a per-capsule negotiation that had already, once before, changed what the status owner asserted. That bears on the documentation axis, which is the one that rose. It was the map's second raise, so the next capsule to exhaust that budget splits the map under a new area ID rather than negotiating a third. The endpoint is the commit that recorded the approved closure, which is the target the update rule means; the graduation that folded this contract into its parent specification, and any later measurement-binding commit, are excluded |
| [Runtime-state identity bound](RUNTIME-STATE-INVARIANT-SPEC.md#layer-1-lifecycle-and-structure) | `67503432..32a1fef8` | `+555/-246` | `+68/-41` | Unknown | The nearest comparator is the runtime-state invariant above at `+2856/-641` code and `+129/-30` documentation because this amendment extends that exact predicate across Lean, the independently written TypeScript core, and the Workflow-continuation admission boundary while adding no source, IL, profile, public observation, or transition family. Code additions fell by 2,301, about 81 percent, and documentation additions fell by 61, about 47 percent. The reduction comes from reusing the invariant aggregate, command and continuation gates, Activity turnover mechanism, and existing preservation schedules while adding one cohesive three-family bound owner, exact per-family negatives, a continuation refusal witness, and the hard-bounded Lean fixture split. Both addition measures fell, so the reflection requires no manufactured process-weight removal. The endpoint is the semantic-checkpoint correction audit, which contains the complete implementation and corrected evidence boundary; closure receipt and proposal graduation bookkeeping are excluded. |
| [Activity identity issuing discipline](RUNTIME-STATE-INVARIANT-SPEC.md#layer-3-monotonicity) | `28133cd0..2b91e761` | `+1019/-23` | `+38/-27` | Unknown | The nearest comparator is the runtime-state identity bound above because this amendment extends the same invariant across Lean and the independently written TypeScript core while adding no source, IL, profile, public observation, transition family, or host mechanism. Code additions rose by 464 while documentation additions fell by 30. The increase is the exact cost of closing a two-state completeness class the one-state bound could not decide: a criterion-based pair predicate, strict freshness at both Lean issuer roots, exact predecessor witnesses for every current preserving and removing Lean writer, an independent TypeScript pair oracle over all four issuers, the three-state exact-reissue negative, a real activation-two rearm, and a seeded production-writer census. Removing a writer witness, the independent target, or the seeded completeness guard would weaken the claim, so no code process weight can be removed without manufacturing a cheaper but incomplete result. The reusable census is the selected prevention mechanism: a future Activity writer must classify itself and carry its required evidence instead of reopening a repository-wide integration sweep. The endpoint is the first-green implementation commit; checkpoint receipt and later Sequential Multi-Instance hosting work are excluded. |
| [Sequential Multi-Instance User Task](capsules/SEQUENTIAL-MULTI-INSTANCE-SPEC.md) | `f3802b7c..d814cac4` | `+26860/-2289` | `+1071/-153` | Unknown | The nearest same-mechanism comparator is the interrupting Activity boundary Timer above because both own one User Task host and one managed lifetime deadline. Both additions rose, code from `5521` to `26860` and documentation from `363` to `1071`. The range is a conservative composite lifecycle cost rather than a local Multi-Instance attribution: Sequential Multi-Instance forced the separately reviewed Activity occurrence ownership, body-turnover, runtime-identity, issuing-discipline, and closure-correction work before it could close ordered repetition, data mediation, public progress, two-language execution, production capacity, continuation, live natural/interrupted refinement, replay, and corpus evidence. No prerequisite is subtracted and no code or evidence lane can be removed without weakening the established claim. The process weight removed before the next material capsule is serial deep-first completion without an earlier breadth checkpoint: the adopted MUE sequence now delivers Alpha by reusing this reviewed engine contract without duplicating semantic policy, then gives every remaining MUE family its own separately reviewed vertical risk slice before any of them deepens for Beta. Named mechanism boundaries remain separate, so another composite prerequisite span cannot hide late cross-family risk or accumulate its review prose inside one capsule. The endpoint is the clean gated closure-correction commit; cost binding, receipt bookkeeping, and graduation are excluded. |
| [Parallel Multi-Instance User Task](capsules/PARALLEL-MULTI-INSTANCE-SPEC.md) | `25dd98cd..17e23590` | `+21035/-593` | `+454/-148` | Unknown | The nearest same-mechanism comparator is Sequential Multi-Instance above because both close one collection-driven User Task, direct String mediation, public progress, a lifetime Timer, Lean/core agreement, production Workflow-chain refinement, capacity, replay, and retained whole-model evidence. Parallel additions fell by 5,825 code lines, about 21.7%, and 617 documentation lines, about 57.6%, while adding atomic concurrent generation, all/first completion, sibling termination, complete permutation and preservation matrices, all-policy commutation, a first-policy trace non-law, coalesced task/Timer refusal, and strict Product 2 copied-contract compatibility. The range conservatively retains the supporting Activity body-claim invariant and the proof and verification corrections encountered after implementation rather than subtracting prerequisites. Reuse of the sequential data graph, Activity ownership, managed deadline, Workflow chain, E1/E2 publication, corpus machinery, and internal commutation vocabulary accounts for the reduction. Both addition measures fell, so the reflection requires no manufactured process-weight removal. Cost binding, closure receipts, and graduation are excluded. |
| [Activity data-input mediation](capsules/ACTIVITY-DATA-INPUT-MEDIATION-SPEC.md) | `f9f3f7bb..7349964e` | `+5824/-492` | `+368/-104` | Unknown | Code additions are rank 22 of 104 by code and documentation additions are rank 10 of 104 by documentation. The capsule's own obligation names User Task completion data at `+651/-94` code and `+84/-63` documentation as the nearest data comparator, and both measures rose sharply against it. The layer count is the reason rather than the scope: that increment reused an existing occurrence and Update lifecycle and predates quantified Lean preservation laws, real-service refinement witnesses, retained whole-model corpus rows, and Product 2 copied-contract adoption, all of which this capsule owes. Against the immediately preceding capsule, Parallel Multi-Instance at `+21035/-593` and `+454/-148`, both measures fell. The range is conservative and subtracts no prerequisite: it contains the discriminated local-owner replacement that every effect consumer moved through, and also ten commits from three unrelated increments measurable at `13a8de20..e4c2970e` as `+855/-236` code and `+115/-55` documentation. The process weight removed is the Work module's verbatim republication of Product 1's open-task shape, which made every engine observation field a mirrored Product 2 contract edit; narrowing through an allowlist closes that class and bears directly on the code measure that rose. |
| [MUE Preview Alpha](MUE-PREVIEW-ALPHA-SPEC.md) | `d814cac4..40c740ae` | `+4182/-168` | `+181/-168` | Unknown | The nearest same-layer comparator is Product 2 incident operations at `+13373/-543` code and `+97/-54` documentation because both add strict product transport, recovery, Operations presentation, real Temporal hosting, browser acceptance, and replay over already-owned BPMN semantics. Alpha code additions fell by 9,191 through reuse of the closed Sequential Multi-Instance, confirmed-start, execution-publication, UI-kit, Workflow-chain, and replay mechanisms. Documentation additions rose by 84 because Alpha selected a new cross-product command and observation consumer and paid proposal plus semantic-checkpoint review before its showcase lane. The process weight removed before Beta is repeating that product-level proposal across seven breadth slices: each slice stays with its named semantic or hosting owner and opens a capsule only for a new proposition, while one later Beta integration owner carries the coherent catalog and Product 2 journey. The endpoint is the complete production-backed showcase implementation; closure receipt and PLAN bookkeeping are excluded. |
| [Internal commutation Beta checkpoint](INTERNAL-COMMUTATION-PROPOSAL.md) | `9b80c0ed..a34df385` | `+10702/-370` | `+33/-19` | Unknown | The nearest runtime/proof comparator is Activity body turnover at `+2276/-429` code and `+168/-33` documentation because both replace a cross-family RuntimeState invariant in Lean and the independently written TypeScript core without adding source, profile, wire, or host capability. Code additions rose by 8,426 while documentation additions fell by 135. The code increase establishes one closed four-family footprint vocabulary, canonical collection replacement at every producer, opposite-arm enabledness, intermediate well-formedness and open projection, raw-state equality, actual transition/lifecycle publication equality, and an independently structured both-order oracle. The implementation removes the would-be sixteen constructor-pair proof programme by centralizing the reusable footprint and prepared-patch account; deleting another proof surface would weaken the selected claim rather than remove process weight. Later MUE families must reuse this owner or open a new proposition instead of copying a family-local commutation rule. The endpoint is the approved semantic-checkpoint correction; the broader `INTERNAL-COMMUTATION` closure remains open. |
| [Multi-Instance outer-lifetime deadline correction](capsules/SEQUENTIAL-MULTI-INSTANCE-SPEC.md) | `fde60915..42f152de` | `+507/-196` | `+35/-32` | Unknown | It covers the [parallel capsule](capsules/PARALLEL-MULTI-INSTANCE-SPEC.md) equally. The nearest comparator is the MUE Preview Alpha row above at `+4182/-168` code, because both change a closed capsule's surface without adding a BPMN proposition. This increment is an order of magnitude smaller because it advances no semantic family: it moves one admitted constant through the layers that already bound it. What the figure understates is the search: the constant appears as a lexeme in checked source, as milliseconds in the program, and as a remaining duration in the host, so no single term found every site and three of five surfaced from failing gates rather than from planning. The process weight removed before the next increment is the shared per-family constant itself: the two lowerings and the deadline checker now read each family's own value instead of restating one, so the next lexeme change is a one-line edit in the owner that names it. |
| [Activity data-output mediation](capsules/ACTIVITY-DATA-OUTPUT-MEDIATION-SPEC.md) | `0b06ca91..92d523bb` | `+3701/-401` | `+199/-21` | Unknown | Code additions are rank 36 of 104 by code and documentation additions are rank 20 of 104 by documentation. The nearest comparator is its own sibling, [Activity data-input mediation](capsules/ACTIVITY-DATA-INPUT-MEDIATION-SPEC.md) at `+5824/-492` code and `+368/-104` documentation, and both measures fell against it, by 36 and 46 percent. The layers crossed are identical, so the difference is what the input capsule had already paid for: the discriminated Activity-occurrence local owner, the profile-parameterized admission arm, the Activity-writer census, the published-collection projection, and the Product 2 copied-enum precedent all existed, leaving one transition pair, one source reader, one profile, three scenarios, and one retained model to add on top of them. Because the measure fell, no process weight is owed under the reflection rule. |
| [Message payload catch mediation](capsules/MESSAGE-PAYLOAD-CATCH-MEDIATION-SPEC.md) | `3f54d8f1..0c34aace` | `+6062/-575` | `+109/-41` | Unknown | The nearest same-mechanism comparator is Activity data-output mediation above because both select one required scalar direct output, association-decided Process routing, atomic fill and disposal, checked Lean and independent TypeScript accounts, three answer-free schedules, real-service refinement, and one retained whole model. Message payload code additions rose by 2,361, about 64%, while documentation additions fell by 90, about 45%. Unlike the Activity capsule's reuse of the existing User Task completion Update, this increment adds a new public stimulus and enabled-interaction arm through every strict wire boundary, payload-sensitive Signal command and recovery identity, forced continuation, Worker replacement, and replay. The code-axis process weight removed in the measured range is the temporary staged semantic catalog: once full Temporal refinement existed, the registered cases moved into the one pipeline catalog and the parallel dispatch/guard path was deleted. Cost binding, closure receipts, and graduation are excluded. |
| [Interrupting Activity boundary Message](capsules/ACTIVITY-BOUNDARY-MESSAGE-SPEC.md) | `79406df6..0b3c3cda` | `+7669/-756` | `+112/-72` | Unknown | The nearest same-mechanism comparator is the interrupting Activity boundary Timer at `+5521/-838` code and `+363/-31` documentation because both atomically arm one User Task with one interrupting handler, select distinct winner routes, publish exact lifecycles, register two schedules, and carry direct-VM plus production Temporal scheduling and replay evidence. Code additions rose by 2,148, about 39%, while documentation additions fell by 251, about 69%. The code increase replaces Timer-only Activity-handler ownership across its complete producer/consumer census, carries the complete admission, default-exclusion, evidence, and product-example registrations, and adds continuation-safe fail-closed settlement that excludes ledger-suppressed retries and identity conflicts from semantic races. The reusable process weight removed in the range is Message-first runner batching: `StimulusOrder` now drives completion, Message, and Timer inputs from the declared answer-free schedule, so a later mixed-ingress family reuses one order-preserving runner instead of adding a family-specific path. No semantic, proof, host, or evidence lane is subtracted. Cost binding, closure receipts, and graduation are excluded. |
| [Message key correlation](capsules/MESSAGE-KEY-CORRELATION-SPEC.md) | `79f6f416..2254b158` | `+64265/-4341` | `+1780/-174` | Unknown | This contiguous programme span is a conservative upper bound, not a pure correlation attribution: independently governed Compensation and CI/resource-repair work interleaved after proposal approval, and no subtraction is applied. The separately reproducible private-ingress core at `5938bfb3..cee9109f` is `+11026/-99` code and `+69/-27` documentation. Even that clean slice exceeds Message payload catch (`+6062/-575` code, `+109/-41` documentation) and Activity boundary Message (`+7669/-756`, `+112/-72`) in code additions because it adds cross-Workflow registration, complete barrier discovery, fixed result reservation, FIFO settlement, same-target recovery, quarantine, and continuation rather than another Process-local Signal path. No semantic, proof, recovery, or independent-evidence lane can be removed without weakening the selected claim. The process weight removed is recollection-based production-start enrollment: an AST-derived exact constructor census now requires generic, exact-definition, Message Start, and scheduled starts to carry the same content-bound Workflow-chain host input, and four independent deletion mutations turn an omitted sibling into an architecture-gate failure instead of another integration sweep. Closure receipt and graduation are excluded. |
| [MUE Preview Beta](MUE-PREVIEW-BETA-SPEC.md) | `553adfb3..b7ac924d` | `+391/-14` | `+30/-15` | Unknown | The nearest same-layer comparator is MUE Preview Alpha at `+4182/-168` code and `+181/-168` documentation because both integrate already-reviewed engine behavior into a Product 2 preview and a production release gate. Beta additions fell by 3,791 code lines, about 90.6%, and 151 documentation lines, about 83.4%, because it composes the existing Alpha and correlated-Message production journeys and adds only a static disclosure surface, exact integration guard, and responsive acceptance rather than another transport, actor, host, or replay boundary. The range conservatively includes the two Product 2 test-contract alignments exposed by the clean complete gate and subtracts nothing. Both addition measures fell, so the reflection requires no manufactured process-weight removal. Closure receipt, graduation, and tag bookkeeping are excluded. |
| [Internal closure atomicity correction](capsules/INTERNAL-CLOSURE-ATOMICITY-REPAIR-SPEC.md) | `19679a31..d6f1a14e` | `+1390/-517` | `+79/-33` | Unknown | The range ends at the implemented host correction and excludes later cost/review bookkeeping. Against the required empty-state capacity checkpoint at `2dcbe680..ec66bc7a` (`+1028/-137` code, `+87/-10` documentation), code additions rise by 362 and documentation additions fall by eight; that comparator has checkpoint approval, not closure approval. The nearest completed capsule crossing Lean, core, and Temporal is Activity data-output (`+3701/-401`, `+199/-21`), and both additions are lower here because this repair adds no source admission, transport, profile, or corpus capability. The removed process weight is reliance on representative rollback examples: general laws cover both Lean result producers, and the production-loop probe observes failure after real recovery admission. Shared-consumer corrections and resource records remain in the measured range without subtraction. |
| [Runtime initialization assurance repair](capsules/RUNTIME-INITIALIZATION-ASSURANCE-REPAIR-SPEC.md) | `65d568f7..6d231a2b` | `+2099/-283` | `+59/-51` | Unknown | This is the conservative combined initialization and runtime-ownership correction at `0958697d`, followed by synchronous witness elaboration; no regional-cancellation or predicate work is subtracted. Against empty-state capacity, code additions rise by 1071 and documentation additions fall by 28. The source-derived theorem-owner import guard removes filename-by-filename assurance enrollment, while the public admission-frame lemmas avoid proving initialization against an intermediate raw state. The fixed-limit measurements above distinguish proof cost from this mixed implementation churn. |
| [Composed Activity data-input/output implementation](capsules/ACTIVITY-DATA-INPUT-OUTPUT-MEDIATION-SPEC.md) | `be358c78..d69a3df4` | `+15292/-1486` | `+864/-305` | Unknown | The contiguous range starts at proposal approval and retains the interleaved six external-review repairs without subtraction, so it is a conservative programme bound rather than pure composed-data attribution. Both additions exceed Activity data-output (`+3701/-401` code, `+199/-21` documentation), the nearest capsule crossing the same layers. The separate registration/evidence slice `b4fdfb82..d69a3df4` is `+687/-71` code and `+67/-39` documentation; it reuses the reviewed source/runtime/proofs and passive User Task host. Closure removes temporary checkpoint-profile enrollment from the catalog consumers, including redundant admitted-set and type-union membership, while retaining every independent semantic and host lane. Cost binding, independent closure review, and graduation are excluded. |

| [Mixed local-control production integration](INTERNAL-COMMUTATION-PROPOSAL.md#local-control-preparation-prerequisite) | `c5d0107c..34e38132` | `+1444/-826` | `+42/-21` | Unknown | This production-only range begins after the independently approved quantified-proof prerequisite; it is not the cost of the complete commutation account. The nearest production checkpoint comparator is the first Internal Commutation checkpoint at `+10702/-370` code and `+33/-19` documentation. Code additions fall by 9,258 and documentation additions rise by nine. Integration reuses the proved prepared update, finite-prefix publication bridge, and whole-command rollback. Six unchanged definition blocks move out of the trace dependency closure so the bounded production witness can elaborate without importing its own caller; no independent proof or evidence lane is removed. The documentation increase records the implemented boundary and its exclusions. Verification and receipt bookkeeping are excluded. |

| [Canonical scope and counter storage](INTERNAL-COMMUTATION-PROPOSAL.md#canonical-scope-and-counter-storage-checkpoint) | `669f2bf9..a97a4ea1` | `+475/-52` | `+46/-22` | Unknown | This implementation checkpoint adds canonical collection laws, actual-writer discriminators, and aggregate checks without adding a transition family. The nearest completed representation comparator is scoped runtime data (`+540/-73` code, `+134/-11` documentation): both replace internal storage across Lean and the independent TypeScript core while preserving public observations. Code additions fall by 65 and documentation additions by 88 because this checkpoint reuses the existing collection-order algebra and ordered TypeScript writers. Both addition measures fall, so no process-weight removal is owed. Immutable calibration, review, and verification bookkeeping are excluded; this row claims neither checkpoint approval nor final commutation closure. |

| [Fresh scope ownership and Call reachability](INTERNAL-COMMUTATION-PROPOSAL.md#scope-creation-preparation-prerequisite) | `fcbd1a38..c6edad18` | `+339/-2` | `+5/-2` | Unknown | This Lean proof prerequisite preserves exact owner lookup after fresh insertion and bounded Call reachability without assuming successor validity. The nearest collection-law checkpoint is canonical scope and counter storage above (`+475/-52` code, `+46/-22` documentation). Code additions fall by 136 and documentation additions by 41; this slice reuses the existing insertion algebra and executable closure rather than changing runtime bodies. Both addition measures fall, so no process-weight removal is owed. Measurement binding and review bookkeeping are excluded; aggregate preservation and final commutation closure remain open. |

| [Call-association preservation under scope creation](INTERNAL-COMMUTATION-PROPOSAL.md#scope-creation-preparation-prerequisite) | `598f7365..eb3b0ccd` | `+525/-0` | `+4/-3` | Unknown | The nearest quantified prerequisite is fresh scope ownership and Call reachability above (`+339/-2` code, `+5/-2` documentation). Code additions rise by 186 and documentation additions fall by one. The increase supplies the exact root/record association and Program-admission obligations that singleton lookup alone does not prove. The proof reuses the existing insertion and reachability laws and proves its private characterization equivalent to the unchanged predicate. Consumer-local admission lemmas avoid modifying shared admission owners and repeating their dependency-wide rebuild. Measurement binding and review bookkeeping are excluded; aggregate preservation and final commutation closure remain open. |

| [Runtime-position preservation under scope creation](INTERNAL-COMMUTATION-PROPOSAL.md#scope-creation-preparation-prerequisite) | `d17b0bd3..d7fcdcb0` | `+501/-0` | `+4/-3` | Unknown | The nearest quantified prerequisite is Call-association preservation above (`+525/-0` code, `+4/-3` documentation). Code additions fall by 24 and documentation additions remain equal. This increment reuses the existing token, insertion, and association laws; its consumer-local characterization is proved equivalent to the unchanged position predicate and avoids modifying shared predicate owners. The retained finite witnesses exercise complete admitted preparations without claiming general aggregate validity. Measurement binding and review bookkeeping are excluded; Compensation/incident preservation and final commutation closure remain open. |
| [Aggregate runtime-validity preservation under scope creation](INTERNAL-COMMUTATION-PROPOSAL.md#scope-creation-preparation-prerequisite) | `23e6b81d..52a84367` | `+401/-0` | `+4/-3` | Unknown | The nearest quantified prerequisite is runtime-position preservation above (`+501/-0` code, `+4/-3` documentation). Code additions fall by 100 and documentation additions remain equal. The aggregate composes the existing position, insertion, token, and Call-order laws while preserving the unchanged Compensation validators through exact owner lookups. Nonempty predicate witnesses reuse existing Compensation fixtures without claiming source reachability or a newly admitted composition. Measurement binding and review bookkeeping are excluded; complete preparation frames and final commutation closure remain open. |
| [Independent scope-creation preparation frames](INTERNAL-COMMUTATION-PROPOSAL.md#scope-creation-preparation-prerequisite) | `f111c37c..ba020ca9` | `+819/-0` | `+5/-2` | Unknown | The nearest quantified prerequisite is aggregate runtime-validity preservation above (`+401/-0` code, `+4/-3` documentation). Code additions rise by 418 and documentation additions by one. This increment proves unchanged complete preparations across four creator pairings, including global population exclusions that exact-identity freshness cannot establish. To avoid four separate proof and fixture lanes, all pairings consume shared population/read-frame laws and one parameterized constructed fork graph; the symmetric result reuses the one-direction theorem. The fixture jointly establishes structural premises and explicitly rejects profile admission. Measurement binding and review bookkeeping are excluded; exact commutation, accepted publication, and production batching remain open. |
| [Independent scope-creation commutation](INTERNAL-COMMUTATION-PROPOSAL.md#scope-creation-preparation-prerequisite) | `0b8a5544..997309c7` | `+367/-0` | `+4/-2` | Unknown | The nearest quantified prerequisite is preparation-frame preservation above (`+819/-0` code, `+5/-2` documentation). Code additions fall by 452 and documentation additions by one. This increment composes those frames with existing token, scope, counter, and validity laws; only the partial-key Call sorter needs new anchor-uniqueness and canonical-equality algebra. The existing constructed graph now supplies both-order complete-state and final-validity witnesses plus an equal-key/different-payload exclusion. Both addition measures fall, so no process-weight removal is owed. Measurement binding and review bookkeeping are excluded; accepted publication and production batching remain open. |

| [Scope-creation control-position correspondence](INTERNAL-COMMUTATION-PROPOSAL.md#scope-creation-preparation-prerequisite) | `f6b99ac6..27d619c0` | `+319/-0` | `+4/-2` | Unknown | The nearest quantified prerequisite is complete-state commutation above (`+367/-0` code, `+4/-2` documentation). Code additions fall by 48 and documentation additions remain equal. Both creator kinds reuse one multiplicity argument, one fresh-scope projection law, and the existing constructed graph. Consumer-local algebra leaves shared projection bodies and their existing consumers unchanged. Neither addition measure rises, so no process-weight removal is owed. Measurement binding and review bookkeeping are excluded; lifecycle acceptance and production batching remain open. |

| [Scope-creation accepted lifecycle](INTERNAL-COMMUTATION-PROPOSAL.md#scope-creation-preparation-prerequisite) | `be60e283..b589ab0a` | `+1559/-0` | `+6/-3` | Unknown | The nearest publication checkpoint is control-position correspondence above (`+319/-0` code, `+4/-2` documentation). Both additions rise because lifecycle acceptance requires structural Program binding, old wait/Scope/Call projection preservation, fresh-anchor insertion, and actual candidate agreement. The four supporting proof owners share one outcome, one parameterized fixture matrix, and one checkpoint review instead of separate helper commits and reviews. Existing runtime validators, projection bodies, and cross-target semantics remain unchanged. The complete pair theorem composes predecessor-derived frames and acceptance without assuming intermediate projectability. Resource evidence and review bookkeeping are excluded; production scope/region batching remains open. |

| [Mixed scope-creation batch integration](INTERNAL-COMMUTATION-PROPOSAL.md#scope-creation-preparation-prerequisite) | `7613956c..10e774c5` | `+1372/-33` | `+10/-6` | Unknown | The nearest production comparator is mixed local-control integration above (`+1444/-826` code, `+42/-21` documentation); additions fall by 72 and 32 respectively. This range includes the new mixed pair proofs as well as production dispatch, finite induction, and accepted publication. Child/Call variants share one production fixture, and the pair helpers share one integration commit and review boundary. All supporting edits precede one complete affected-package gate; the shared transition-type change still invalidates downstream Lean caches. No proof or evidence obligation is removed. Resource binding and review bookkeeping are excluded; regional removal and final closure remain open. |

## Repair closure costs

These immutable checkpoint and closure-target ranges record the reviewed repairs; their specification receipts own approval. Closure-target rows retain all intervening work without subtraction; the earlier rows preserve the narrower implementation attribution. Subsequent cost binding, receipt, and graduation bookkeeping are excluded. Elapsed authoring time is unknown, and the resource measurements above remain separate.

| Increment | Boundary | Code | Documentation | Elapsed | Comparison consequence |
|---|---|---|---|---|---|
| [Compensation start-data repair](capsules/COMPENSATION-DURABILITY-START-DATA-REPAIR-SPEC.md) | `535e2293..29c35194` | `+1443/-234` | `+44/-32` | Unknown | The required source/lowering comparator is its implementation/calibration checkpoint `5fc90395..5480b22e`, which measures `+4199/-779` code and `+90/-58` documentation. Both addition measures fall because the repair reuses the exact source, lowering, snapshot, handler, and host transports and adds only Program-derived start admission with capacity and reachability discriminators. No process-weight removal is owed. Trigger durability supplies the composed live evidence; its separate closure does not silently approve this repair's receipt. |
| [Compensation empty-state capacity repair](capsules/COMPENSATION-EMPTY-STATE-CAPACITY-REPAIR-SPEC.md) | `2dcbe680..ec66bc7a` | `+1028/-137` | `+87/-10` | Unknown | Against the preceding start-data repair, code additions fall by 415 and documentation additions rise by 43. This range includes shared conformance calibration without subtraction. The correction reuses both canonical encoders and adds no runtime exception; its three-reader and exact-boundary evidence cannot be removed. The documentation increase records a separate admission contradiction and its review boundary, so no redundant process weight is identified for removal. |
| [Compensation start-data repair closure target](capsules/COMPENSATION-DURABILITY-START-DATA-REPAIR-SPEC.md) | `535e2293..32b76106` | `+33814/-2526` | `+2159/-609` | Unknown | This contiguous range includes later Compensation durability, runtime/initialization, commutation, assurance, and Temporal deployment/recovery corrections without subtraction. Both addition measures exceed the required source/lowering checkpoint comparator (`+4199/-779` code, `+90/-58` documentation) and the nearest completed data-admission capsule, Activity data-output (`+3701/-401`, `+199/-21`). This is gross programme churn, not attribution to start admission. The narrower checkpoint row above isolates the repair and falls on both addition measures against source/lowering. The repair reuses production snapshot/frontier constructors and canonical encoders rather than maintaining an admission-only byte formula; removing the capacity, reachability, or host evidence would weaken its contract. |
| [Compensation empty-state capacity repair closure target](capsules/COMPENSATION-EMPTY-STATE-CAPACITY-REPAIR-SPEC.md) | `2dcbe680..32b76106` | `+9532/-1265` | `+574/-162` | Unknown | This contiguous range includes later runtime/initialization, commutation, assurance, and Temporal deployment/recovery corrections without subtraction. Both additions fall against the preceding start-data closure-target range, but the ranges overlap and this is not a measure of repair efficiency. Against the nearest completed admission/invariant repair, runtime initialization (`+2099/-283` code, `+59/-51` documentation), both additions rise because the gross range retains those intervening increments. The narrower checkpoint row above preserves the capacity correction's attribution and comparison. Both encoders, all three readers, and the unchanged initialization theorem remain necessary; no redundant process weight is identified for removal. |
| [Native Worker deployment repair](TEMPORAL-WORKER-DEPLOYMENT-REPAIR-SPEC.md) | `6b08baf3..9628d6fb` | `+3619/-244` | `+236/-93` | Unknown | This contiguous range ends at the smoke-tested implementation and includes later command-recovery, deadline, composed-data, and measurement corrections without subtraction; subsequent evidence/receipt-only commits are excluded. It is an upper bound on native deployment attribution. The nearest same-layer completed comparator is Workflow-chain hosting at `+23074/-1866` code and `+1182/-856` documentation, itself explicitly mixed. Both additions fall by reusing native deployment routing, existing queues, immutable bundles, and the published start/recovery contracts. The repair removes procedural mixed-poller exclusion as the maintained production mechanism. The specification receipt owns the approved closure; the container smoke and complete verifier are green. |
| [Call pairing reverse-uniqueness correction](capsules/CALL-ACTIVITY-SPEC.md#checked-graph-and-semantic-process-il) | `36cce36d..d6ef4c6d` | `+81/-3` | `+6/-2` | Unknown | The nearest standalone-admission repair comparator is Compensation empty-state capacity at `+1028/-137` code and `+87/-10` documentation. Both addition measures fall because this correction restores an existing conjunct and reuses the original Call fixture and independent TypeScript predicate. Balanced many-to-one populations discriminate the missing check without claiming a full Program-admission bypass. The existing scope-separation theorem keeps its contract; only its conjunction projection changes. No process-weight removal is owed. Cost and review bookkeeping are excluded. |

## Receipt timing observation, 2026-09-20

`node scripts/summarize-command-receipts.ts .cache/scope-mixed-batch-20260919` reproduces the reviewed mixed scope/regional window from 630 complete standard receipts: 2026-09-19 16:15:01.411 UTC through 2026-09-20 12:40:30.456 UTC. Modification-time intervals yield **20.425 hours span**, **2.537 hours union**, and **2.605 hours summed command duration**. All 630 lack lane labels; no agent count or engineering-time attribution follows. The summary exposes seven nonstandard measurement directories as incomplete standard-receipt candidates and ignores eighteen directories without receipt evidence; exit two denotes that partial input, not a failed measured build. This mixed-window observation cannot replace capsule-specific unknown elapsed costs or establish a performance trend. [The summary reader](../scripts/summarize-command-receipts.ts) and [its overlap/incomplete-evidence tests](../scripts/summarize-command-receipts.test.ts) own reproduction.

## Kernel-fixture pilot, 2026-09-20

The one-module pilot used [Internal Commutation conformance](../BpmnSemantics/InternalCommutationConformance.lean) at `df8a6c2d`. All 23 original propositions were retained byte-for-byte. Twenty-two were replaced in a scratch snapshot by `#guard (decide (<proposition>))`; `complete_pair_publication_is_order_independent` retained its original proof through the general commutation law because its publication result has no executable `Decidable` equality instance. A ground proposition without consumers is therefore not automatically an executable fixture. The other Lean sources contain no references to these 23 theorem names.

| Variant | Elapsed seconds | GNU peak RSS, KiB | Cgroup peak, bytes | Exit |
|---|---:|---:|---:|---:|
| Original kernel proofs | 10.86 | 1,288,936 | 939,388,928 | 0 |
| 22 guards and one retained proof | 3.11 | 1,206,968 | 829,521,920 | 0 |

Both runs used Lean 4.31.0, image `sha256:4df22c7a1ec8ba8c4c2795fdea1269d7b9cadade4e65b22e595047509aa27950`, one Lean thread, one CPU, no network, a read-only source mount, a 3,221,225,472-byte memory limit, and zero permitted swap. Dependency caches were warmed identically; each measured run rebuilt only the target. All controlled memory events, pressure totals, and actual swap were zero. RSS and cgroup charging remain separate accounting measures. The original target's new measured peak updates its [cost row](../scripts/lean-module-cost.ts); no production Lean source changed.

The local evidence root is `/tmp/bpmn-fixture-pilot-20260920`: `inputs.json` hashes all 513 source/build inputs and each original proposition; `kernel/cold-evidence/measurement.json` hashes to `a836939c202072965c700b568492e38cfd346628584fc39f77726a391fb1dc53`, and `guard/evidence-03/measurement.json` to `3bede60427ea119c152cad08abfe35fd881b8c70267b565da87ce4327af00ea7`. Mounted target hashes are respectively `a02dd231e707535714fa9f6f453b6a8ca6059f89123b8f8aadb10737ac868d00` and `41acf3bed8b3abfce9fd8e8faca507e95dc054750b08001c4195f3aac9948a04`. Reproduction replaces only the 22 whole theorem declarations/proofs and makes attached doc comments ordinary comments; definitions, imports, proposition text, and the remaining proof stay unchanged. The initial dependency-warming run and two failed scratch conversions are excluded. Negating the first guard's enabledness proposition then failed specifically because the expression did not evaluate to true.

Coverage is preserved in this pilot, but the 22 guards trust Lean's evaluator/compiler rather than supplying kernel-checked proof terms, cannot be cited as theorems, and cannot supply downstream hypotheses. The retained theorem keeps its original boundary. The single observation saves 7.75 seconds with only about 6% lower RSS; it establishes neither a suite-wide saving nor a remedy for the shared-atom memory ceiling. Retain the production kernel lane. Reopen selective conversion when a particular fixture's measured cost justifies that explicit trust tradeoff; do not convert by consumer count alone.

## Architecture and workflow review follow-up, 2026-09-20

These implementation ranges end at `fd214cd8`, before the following closure records. Reproduce them with `node scripts/capsule-cost.ts <baseline> fd214cd8`. The broader range includes the narrower repair without subtraction; the two rows must not be added together.

| Increment | Boundary | Code | Documentation | Elapsed | Comparison consequence |
|---|---|---|---|---|---|
| [PMI output-capacity and proof/evidence correction](capsules/PARALLEL-MULTI-INSTANCE-SPEC.md#independent-cold-review-receipt) | `8220c4c4..fd214cd8` | `+169/-250` | `+50/-26` | Unknown | The nearest completed aggregate-admission repair is Compensation empty-state capacity above (`+1028/-137` code, `+87/-10` documentation). Both addition measures fall because this repair reuses the existing collection predicate, profile, transition, and host. The range also retains proof/evidence corrections and agent-capacity guidance. Shared collection lemmas remove duplicate proof bodies; no semantic, kernel, differential, or host evidence is removed. |
| [Architecture and workflow review follow-up](TESTING-SPEC.md#commit-boundaries) | `e409a7c0..fd214cd8` | `+1090/-432` | `+92/-37` | Unknown | The nearest completed cross-layer correction is internal closure atomicity (`+1390/-517` code, `+79/-33` documentation). Code additions fall and documentation additions rise, but this mixed review programme is not a like-for-like productivity comparison. The source/receipt binding removes circular evidence commits; the gate suite remains complete. The narrower repair row isolates the later semantic correction. Subsequent closure records are excluded. |

The complete clean-implementation command `./scripts/pnpm.sh run test:pre-push:verify` passed at `83dcd3e3` in 504.320 seconds, recorded by `/tmp/bpmn-review-followup-complete` with output SHA-256 `cb0d849159e55ebfe132046792b2a15b7efd970bf009af643d0dc9bb6cfd1b6f`. This command's interval is both its span and busy duration; it is not the programme's elapsed time. The later `fd214cd8` instruction-only change passed all 572 infrastructure tests in `/tmp/bpmn-agent-thread-policy-infrastructure`. Earlier complete-run failures remain in the [repair receipt](capsules/PARALLEL-MULTI-INSTANCE-SPEC.md#independent-cold-review-receipt); focused reruns alone did not establish closure.

The [representation assessment](INTERNAL-COMMUTATION-PROPOSAL.md#representation-and-reuse-assessment-2026-09-20) retains the production account after the locality pilot and consolidates identical collection proofs. The [fixture pilot](#kernel-fixture-pilot-2026-09-20) retains kernel checking. The [regional investigation](INTERNAL-COMMUTATION-PROPOSAL.md#regional-preparation-prerequisite) records its admission premises and reopen witness without retiring RC obligations. [Compensation registration prerequisites](capsules/COMPENSATION-TRIGGER-HANDLER-PROPOSAL.md#public-registration-prerequisites) own snapshot-batching and runner gaps. The corrected [evidence-lane account](TESTING-SPEC.md#evidence-lanes) separates lowering comparison, transcription checks, and shared account risk; the PMI receipt owns the output-capacity repair and stronger slot-order claim.

Workflow changes retain complete gates, bind measurements before committing, record review targets with verdicts, require deliverable-based reassessment before substantial proof prerequisites, and add terminal failure locators and optional receipt labels. [Gate selection remains unadopted](TESTING-SPEC.md#three-level-verification-policy) because historical receipts cannot reconstruct changed inputs. [Receipt timing](#receipt-timing-observation-2026-09-20) is reproducible without engineering-effort or agent-count inference. The owner left the regional checkpoint trial pending in the RC queue on 2026-09-20; these fixes do not claim trial success, zero ceremony commits, or measured scheduling improvement. Its prospective observations remain receipt span/union, contemporaneously recorded repeat work, commit purpose, and unchanged assurance.

## Message-boundary projection correction, 2026-09-20

The independently reviewed outcome is `MBP-EMPTY-TASK-01`, with implementation range `ec217b50..8c5b2818`: restore independent Task/Message/Activity population checks under the existing [Message-boundary contract](capsules/ACTIVITY-BOUNDARY-MESSAGE-SPEC.md#state-command-and-observation-distinctions). Two complete-runtime/projector probes reproduced the discrepancy; TypeScript already refused both states. The correction changes lambda grouping and simplifies the two regional preservation consumers, while a quantified empty-population law guards the entire refusal class. It introduces no fixture conversion, representation migration, runtime writer, or capability. The [review receipt](INTERNAL-COMMUTATION-PROPOSAL.md#independent-cold-review-receipt) owns independent approval and the subsequent complete integration result; this cost range excludes the following evidence record.

| Increment | Boundary | Code | Documentation | Elapsed | Comparison consequence |
|---|---|---|---|---|---|
| [Message-boundary empty-population projection correction](capsules/ACTIVITY-BOUNDARY-MESSAGE-SPEC.md#state-command-and-observation-distinctions) | `ec217b50..8c5b2818` | `+75/-54` | `+12/-5` | Pre-commit receipt span 2252.798 s; interval union 1872.520 s | The nearest validator correction is Call pairing reverse uniqueness (`+81/-3` code, `+6/-2` documentation). Code additions fall and documentation additions rise; the latter includes resource evidence and the regional consumer boundary. The quantified law and two regressions replace a first-Task proof dependency. Existing kernel fixtures and complete gates remain unchanged; these figures measure churn, not quality or engineering effort. |

The pre-commit timing summary uses 19 completed receipts from the first complete Message probe through the full affected Lean gate, retaining original `command.txt`/`exit-status` modification times. Reproduce it with `node scripts/summarize-command-receipts.ts /tmp/bpmn-message-projection-precommit-receipts`; `sources.json` maps those timestamp-preserving copies to their original receipts. The five nonzero exits are the expected red discriminator, an unsuccessful scratch proof, two documentation-budget failures, and a container mount failure before Lean started. The initial doctor, unreceipted short commands, later review/integration, and separate handler-publication investigation are outside this timing scope. Labels identify authored command groups, not agents; receipt span and union imply neither engineering effort nor critical-path delay.

The unchanged `BpmnSemantics.ActivityBoundaryMessageConformance` passed a cold-target build against the corrected dependencies in **29.58 seconds**, at **1,402,596 KiB GNU RSS** and **1,113,640,960 cgroup bytes**, with all controlled events and pressure totals zero. The container used `bpmn-lean-audit:v4.31.0-arm64`, one Lean thread, one CPU, `memory.max=3221225472`, zero additional swap, no network, read-only source, and an isolated warm dependency cache. Its target source SHA-256 remains `a92aba0f28f4f827daf02c4970c4341a5fce2c699b9916c599e5db15d1b61e2a`; that module's theorem statements and kernel fixtures are unchanged. This is resource acceptance for changed dependencies, not a like-for-like performance improvement over the historical measurement.

Evidence is retained under `/tmp/bpmn-message-projection-resource`: `inputs.json` binds all 513 source/build inputs with SHA-256 `ed7d1fec044866d2ad776ad6735fa8dbb62c811ec127b2bbc587fb715145510f`, and every source matches implementation target `8c5b2818`. The cold receipt's combined SHA-256 is `5f270f2ca2408e7f481597c0c6d281f126d0444569f1ec2d90b730dd8c501da2`, over raw bytes concatenated as `command.txt`, `git-head`, `output.log`, `exit-status`, with no added separators; its output hashes to `f5177f370c84151fa8ab686cff9ae5277a294e0c5aa426302d4bdb5ad28afeae`. The first warm-up failed before Lean started because the read-only snapshot lacked the cache mount point; creating that directory allowed the same bounded warm-up. Both receipts remain. Task-owned containers remove themselves on exit; reusable images and the original cache are preserved.

Reassessment: the correction stays in three Lean owners and the existing TypeScript test owner; Complete/Return preservation derives the Task from the reverse record check rather than growing an empty-Task workaround. The 1596.951-second full Lean run owns most recorded command duration because the shared validator invalidated its consumer closure. Two avoidable documentation-budget failures show that the existing focused document guards should run immediately after status edits, before the complete suite; no new policy machinery or weakened gate is needed. The next publication discriminator was reproduced before writing further proofs, using the existing retention fixture. Commit purpose is outcome for `8c5b2818` and required evidence for this review/cost record. Regional trial success, a productivity trend, and general selected-domain equality are not claimed.

## Regional handler-publication correction, 2026-09-20

The independently approved `RHP-HANDLER-01` correction reuses predecessor Activity withdrawal to publish the exact handlers already removed by cancellation. [Its review receipt](INTERNAL-COMMUTATION-PROPOSAL.md#independent-cold-review-receipt) owns the immutable target, approval, and clean integration; the [regional prerequisite](INTERNAL-COMMUTATION-PROPOSAL.md#regional-preparation-prerequisite) owns the finite evidence and unchanged 3 GiB measurement. The implementation range excludes this subsequent evidence record.

| Increment | Boundary | Code | Documentation | Elapsed | Comparison consequence |
|---|---|---|---|---|---|
| [Regional handler publication](INTERNAL-COMMUTATION-PROPOSAL.md#regional-preparation-prerequisite) | `2e419714..40ce8c00` | `+264/-46` | `+12/-9` | Pre-commit receipt span 2978.312 s; interval union 2295.118 s | The nearest Message-boundary projection correction above changed `+75/-54` code and `+12/-5` documentation. This correction changes both semantic targets, the retained checker, and five kernel-decided witnesses; code additions rise and documentation additions are equal. Existing representation, raw removal, profiles, and quantified preparation assumptions remain unchanged. These figures measure churn, not productivity or quality. |

Reproduce timing with `node scripts/summarize-command-receipts.ts /tmp/bpmn-handler-precommit-receipts`; `sources.json` maps timestamp-preserving copies to 32 original completed receipts. Eleven nonzero exits include intended publication/completeness reds, scratch-proof and TypeScript errors, fixture setup, stale documentation budgets, and sandbox/container preconditions. Doctor, unreceipted short commands, review, and post-commit integration are excluded. The 1601.592-second full Lean command rebuilt the shared lifecycle consumer closure. Receipt labels do not identify agents; span and union establish neither effort nor critical-path delay.

The first clean integration receipt `/tmp/bpmn-handler-clean-integration` ended nonzero after 258.450 seconds; output SHA-256 is `faec67cd803141597d2916abe3552ac46b576e16f88165eba84519b7ac4b360b`. macOS recorded a contemporaneous Node `EXC_BAD_ACCESS`/`SIGSEGV` in V8 `ClearStaleLeftTrimmedPointerVisitor::VisitRootPointers` and garbage-collection frames. An earlier same-day report, predating this correction, has the same leading stack. Selected report fields and source hashes are retained in `/tmp/bpmn-handler-v8-crash-evidence.json`, SHA-256 `77f596be1803fab2117d2793d34c69cc56ece517f80db5df04d33e3b323548b6`. This diagnoses a native crash, not its upstream cause or a runtime fix.

The affected four-test file passed separately, followed by all 332 concurrent Temporal tests with the original `--no-parallel-scavenge` flag and concurrency four, changing only the diagnostic reporter. The latter asserted receipt is `/tmp/bpmn-handler-temporal-concurrent-diagnostic`, output SHA-256 `351a73b68b43729e777af1efe260bf99e65ac8ea5386533ba5e363d610cd60dd`. The subsequent complete integration passed without source, gate, concurrency, or timeout changes; both full attempts remain recorded. Focused diagnostics alone were not accepted as integration evidence.

Reassessment: the first discriminator exercised actual publication and the retained template; adding the independent retained checker exposed a second omission before commit. Future publication discriminators should cover all three boundaries from the first red, while keeping the Message child-scope exclusion explicit. Existing Activity withdrawal avoids another representation or pair-proof prerequisite. Commit purpose is outcome for `40ce8c00` (including its resource row) and required evidence for this acceptance record; no separate cost or pre-review binding commit was needed. The review's pre-existing `AOO-JOIN-03` advisory is recorded as a separate bounded correction requiring its own red witness. Regional trial closure remains pending, with no scheduling-improvement claim.

## Actual Timer-publication correction, 2026-09-21

The independently approved `AOO-JOIN-03` correction replaces the remaining Task/Timer ordinal join in actual lifecycle publication with the existing Activity record and exact body identity. [The review receipt](INTERNAL-COMMUTATION-PROPOSAL.md#independent-cold-review-receipt) owns approval and clean integration; the [regional prerequisite](INTERNAL-COMMUTATION-PROPOSAL.md#regional-preparation-prerequisite) owns the finite witness boundary and unchanged 3 GiB resource measurement. This implementation range excludes the subsequent acceptance record.

| Increment | Boundary | Code | Documentation | Elapsed | Comparison consequence |
|---|---|---|---|---|---|
| [Actual Timer publication](INTERNAL-COMMUTATION-PROPOSAL.md#regional-preparation-prerequisite) | `faf33f52..0ba40aef` | `+72/-23` | `+8/-4` | Pre-commit receipt span 2041.015 s; interval union 1645.177 s | The nearest handler-publication correction above changed `+264/-46` code and `+12/-9` documentation. Both addition measures fall: this correction changes one lookup and reuses existing record/body-turnover fixtures, while TypeScript production behavior remains unchanged. Census controls and an explicit historical cost-test fixture guard the exposed failure classes. Churn measures neither productivity nor quality. |

Reproduce timing with `node scripts/summarize-command-receipts.ts /tmp/bpmn-timer-pairing-precommit-receipts`; `sources.json` maps timestamp-preserving copies to 19 completed receipts. Five nonzero exits comprise the intended ordinal-join, actual-publication, and mixed-comparison reds, one scratch-probe type error, and the historical cost-test fixture's accidental inheritance of a live receipt. Doctor, unreceipted short commands, review/integration, and the separate record-retirement investigation are excluded. The 1474.574-second full Lean gate rebuilt the shared lifecycle consumer closure; no unchanged-input repetition or critical-path saving is inferred. All receipts lack lane labels, so no agent count follows.

Reassessment: existing Activity lookup and body replacement avoid a new representation or pair-proof prerequisite. The enrolled lifecycle owner exposed the Lean guard's whole-line exemption; operand-scoped matching and mixed-comparison controls close that hole while retaining the separate TypeScript lexical limitation. Historical cost tests now construct historical rows explicitly instead of inheriting whichever live measurement occupies row zero. Complete gates and kernel checking remain unchanged. Commit purpose is outcome for `0ba40aef` (including its resource row) and required evidence for this acceptance record. The independently reproduced record-retirement defect is the next bounded repair; regional trial success and general state preservation are not claimed.

## Activity-record retirement correction, 2026-09-21

The independently approved repair maintains Activity records alongside the waits consumed by both bounded victories, monitored completion, and monitored Timer firing. [Its review receipt](INTERNAL-COMMUTATION-PROPOSAL.md#independent-cold-review-receipt) owns approval and clean integration; the [regional prerequisite](INTERNAL-COMMUTATION-PROPOSAL.md#regional-preparation-prerequisite) owns the complete-command and constructed-frame evidence and both unchanged 3 GiB resource measurements. The implementation range excludes this subsequent acceptance record.

| Increment | Boundary | Code | Documentation | Elapsed | Comparison consequence |
|---|---|---|---|---|---|
| [Activity-record retirement](INTERNAL-COMMUTATION-PROPOSAL.md#regional-preparation-prerequisite) | `441b7400..44ae1775` | `+209/-8` | `+5/-3` | Pre-commit receipt span 2419.683 s; interval union 1932.617 s | The preceding Timer-publication correction changed `+72/-23` code and `+8/-4` documentation. Code additions rise because this repair changes two transition families, their declarative successors, and eight writer classifications; documentation additions fall. Existing exact-identity filters and record maps preserve the representation, original transition premises, and public routes. TypeScript production behavior remains unchanged. Churn measures neither productivity nor quality. |

Reproduce timing with `node scripts/summarize-command-receipts.ts /tmp/bpmn-activity-retirement-precommit-receipts`; `sources.json` maps timestamp-preserving copies to 19 completed receipts. Seven nonzero exits retain the negative kernel fixtures, their isolation check, the writer-census discriminator, two intermediate proof errors, one fixture-syntax error, and a sandbox loopback refusal. The latter passed with the required local-port access and no source change. Doctor, earlier controls at `0ba40aef`, unreceipted short commands, review, and integration are excluded. The full Lean gate took 1570.363 seconds because both transition modules feed the shared execution closure. No lane labels exist; neither agent counts nor effort or critical-path delay follow from these intervals.

Reassessment: one outcome groups both manifestations of the missing record-maintenance mechanism, with complete-command validity checks and separate unrelated-record controls. Existing subset and identity-witness lemmas avoid a new store or preservation framework; the constructed controls make no validity or reachability claim. The final infrastructure invocation should have retained the known loopback permission from its first invocation, and a truncated hash in the review prompt was corrected immediately against the unchanged generated packet; the reviewer independently regenerated it exactly. These were avoidable execution/transcription detours, not reasons to weaken the checks or add another policy layer. Commit purpose is outcome for `44ae1775`, including both measured cost rows, and required evidence for this acceptance record. General transition preservation, regional trial success, and scheduling improvement remain unclaimed.

## Private Timer alias correction, 2026-09-21

The independently approved repair classifies private boundary Timers before matching untagged public anchors in actual and prepared publication. The retained checker independently uses Program declarations, including the external Timer-firing route. [The review receipt](INTERNAL-COMMUTATION-PROPOSAL.md#independent-cold-review-receipt) owns approval and clean integration; the [correction account](INTERNAL-COMMUTATION-PROPOSAL.md#private-timer-alias-correction) owns the constructed Task/Message witnesses and unchanged 3 GiB measurement. The range includes the required receipt-digest correction and excludes this acceptance record.

| Increment | Boundary | Code | Documentation | Elapsed | Comparison consequence |
|---|---|---|---|---|---|
| [Private Timer alias publication](INTERNAL-COMMUTATION-PROPOSAL.md#private-timer-alias-correction) | `cd2c28e9..5bc50736` | `+290/-89` | `+12/-7` | Pre-commit receipt span 2538.595 s; interval union 1692.989 s | The preceding Activity-record retirement changed `+209/-8` code and `+5/-3` documentation. Both additions rise because this correction covers both evaluators and the independent retained checker, with two kernel witnesses and a separate firing-route discriminator. Existing live-host and declaration classifiers preserve the representation, runtime predicates, and raw cancellation. No redundant evidence lane is identified for removal. Churn measures neither productivity nor quality. |

Reproduce timing with `node scripts/summarize-command-receipts.ts /tmp/bpmn-private-alias-precommit-receipts`; `sources.json` maps timestamp-preserving copies to 27 completed receipts. Eleven nonzero exits retain separating probes and regressions, invalid intermediate fixtures, type/import errors, and guarded headroom corrections. The native full Lean gate took 1276.184 seconds; the separate cold-target Docker measurement took 16.02 seconds. Unreceipted short commands, review, integration, and the later metadata correction are outside these pre-commit intervals. All receipts lack lane labels; neither agent counts nor effort or critical-path delay follow from these timings.

Reassessment: executable Task and Message counterexamples rejected the false cancellation/publication correspondence before more proof code was written. One shared selection correction covers actual and prepared publication, while the retained checker uses its existing state-free Program boundary. No new state representation or preservation framework was needed. The review caught a transcription error between the packet's output-log hash and the cost row's complete-receipt hash; the latter was recomputed from all four retained files and checked directly before committing its correction. Existing measurement and integration evidence remained usable. Commit purpose is outcome for `df253c4e`, required correction for `5bc50736`, and required evidence for this acceptance record. General preservation, regional trial success, and scheduling improvement remain unclaimed.

## Error and Terminate accepted publication, 2026-09-21

The independently approved checkpoint derives actual successor projection and lifecycle acceptance for both cancellation families. [Its review receipt](INTERNAL-COMMUTATION-PROPOSAL.md#independent-cold-review-receipt) owns approval, hypothesis checks, and clean integration; the [proof account](INTERNAL-COMMUTATION-PROPOSAL.md#error-and-terminate-accepted-publication) owns the exact premises and remaining obligations. This implementation range excludes the subsequent acceptance record.

| Increment | Boundary | Code | Documentation | Elapsed | Comparison consequence |
|---|---|---|---|---|---|
| [Error/Terminate accepted lifecycle](INTERNAL-COMMUTATION-PROPOSAL.md#error-and-terminate-accepted-publication) | `7d0e0a53..5c3570c7` | `+1106/-25` | `+11/-7` | Pre-commit receipt span 3655.591 s; interval union 1534.146 s | The nearest quantified lifecycle checkpoint is scope-creation acceptance (`+1559/-0` code, `+6/-3` documentation). Code additions fall by 453; documentation additions rise by five to reconcile earlier open claims and describe the two cancellation dispositions. Both Error and Terminate share the wait, scope, Message-census, and filter machinery. Existing ancestry and attachment lemmas are reused. These ranges establish different operation-family guarantees; churn measures neither productivity nor proof quality. |

Reproduce timing with `node scripts/summarize-command-receipts.ts /tmp/bpmn-cancellation-acceptance-precommit-receipts`; `sources.json` maps timestamp-preserving copies to 36 completed receipts. Sixteen nonzero exits retain fourteen intermediate proof failures and two documentation-budget failures. The complete semantic command took 1390.596 seconds, including the shared proof-consumer rebuild and core tests. Doctor, unreceipted short commands, independent review, its requested probe, and clean integration are excluded. No receipt carries a lane label; neither agent counts nor effort or critical-path delay follow from these intervals. No kernel fixture or executable reduction body changed, and no new Docker measurement is claimed.

Reassessment: one outcome groups the component proofs with actual acceptance for child Error and both Terminate roots. Public identity uniqueness is derived after private Timer classification, and Message pairing reuses one multiplicity-preserving census law instead of pair-specific proofs. Five stale open claims now link to the single acceptance account; the runtime map reuses its existing publication bullet, removing duplicate status growth on the documentation axis. The first status edit exceeded the map budget; the existing focused guards caught it, and subsequent map edits must be measured before the complete suite. All command verdicts were separately asserted, and the required cold review preceded further implementation. Self-assessment found no escaped mechanism requiring a new process-ledger row. Commit purpose is outcome for `5c3570c7` and required evidence for this acceptance record. The next task checks aggregate-validity gaps before new proof machinery; regional trial closure remains pending.

## Error and Terminate runtime preservation, 2026-09-21

The independently approved [aggregate preservation checkpoint](INTERNAL-COMMUTATION-PROPOSAL.md#error-and-terminate-runtime-preservation) preserves the complete existing predicate for actual prepared successors. Its [review receipt](INTERNAL-COMMUTATION-PROPOSAL.md#independent-cold-review-receipt) owns independent approval and clean integration. This implementation range excludes the subsequent acceptance record.

| Increment | Boundary | Code | Documentation | Elapsed | Comparison consequence |
|---|---|---|---|---|---|
| [Error/Terminate runtime preservation](INTERNAL-COMMUTATION-PROPOSAL.md#error-and-terminate-runtime-preservation) | `cdd5bc11..0f4e8a51` | `+595/-0` | `+5/-2` | Pre-commit receipt span 1326.210 s; interval union 82.791 s | The nearest same-family proof checkpoint is accepted lifecycle above (`+1106/-25` code, `+11/-7` documentation): additions fall by 511 and six. The earlier aggregate scope-creation checkpoint is smaller (`+401/-0` code, `+4/-3` documentation); cancellation additionally needs retained Compensation ownership and the declaration-derived empty Terminate case. These are different guarantees, not a productivity or proof-quality comparison. Both cancellation families share one aggregate composition and reuse existing collection and bidirectional Multi-Instance laws. |

Reproduce timing with `node scripts/summarize-command-receipts.ts /tmp/bpmn-cancellation-runtime-precommit-receipts`; `sources.json` maps timestamp-preserving copies to 25 completed receipts. Eleven nonzero exits retain intermediate proof/probe failures. The complete semantic gate took 20.117 seconds against the warm dependency closure; infrastructure took 45.054 seconds. Doctor, unreceipted commands, independent review, its requested committed-target probe, and clean integration are excluded. No receipt identifies a delegated lane, engineering effort, or critical-path delay. No executable reduction body or kernel fixture changed; historical resource measurements are not measurements of the two new proof owners.

Reassessment: inspect the existing component inventory before adding proof machinery. The audit found complete Sequential and Parallel Multi-Instance binding preservation already available; the missing Compensation cases now share exact owner-census and running-state frame laws. One outcome groups both actual-operation theorems with their support, without helper-only commits or new representation/predicate changes. The complete semantic, infrastructure, retained controls, cold review, and clean integration gates passed. Commit purpose is outcome for `0f4e8a51` and required evidence for this record. Complete/Return aggregate preservation is the next bounded deliverable; the regional trial remains pending.

## Complete and Return runtime preservation, 2026-09-21

The independently approved [Complete/Return checkpoint](INTERNAL-COMMUTATION-PROPOSAL.md#complete-and-return-runtime-preservation) preserves the unchanged aggregate predicate for actual prepared successors and corrects the retained Activity-owner edge in both targets. Its [review receipt](INTERNAL-COMMUTATION-PROPOSAL.md#independent-cold-review-receipt) owns independent approval and clean integration. This implementation range excludes the subsequent acceptance record.

| Increment | Boundary | Code | Documentation | Elapsed | Comparison consequence |
|---|---|---|---|---|---|
| [Complete/Return runtime preservation](INTERNAL-COMMUTATION-PROPOSAL.md#complete-and-return-runtime-preservation) | `f49f6fd7..f725745d` | `+1042/-7` | `+16/-8` | Pre-commit receipt span 3122.316 s; interval union 709.540 s | The nearest aggregate checkpoint is Error/Terminate above (`+595/-0` code, `+5/-2` documentation): additions rise by 447 and eleven. Complete/Return includes distinct root-terminal and child-removal cases, retained Compensation cleanup, Call cleanup, and the reproduced owner correction. Existing census, canonical-order, and bidirectional Multi-Instance laws are reused. These different guarantees and the defect repair prevent treating the increase as a productivity or proof-quality comparison. |

Reproduce timing with `node scripts/summarize-command-receipts.ts /tmp/bpmn-completion-precommit-receipts`; `sources.json` maps timestamp-preserving copies to 50 completed receipts. Eighteen nonzero exits retain sixteen intermediate Lean failures, the two-case TypeScript red command, and the Docker mountpoint setup failure. The complete semantic gate took 136.578 seconds; three infrastructure runs totalled 138.239 seconds. Doctor, unreceipted commands, independent review, its committed-target probes, and clean integration are excluded. No receipt identifies a delegated lane, engineering effort, or critical-path delay. Both transitive kernel-fixture consumers were remeasured under the unchanged 3 GiB protocol, with their full source/receipt bindings retained in the executable cost rows and the checkpoint account.

Reassessment: the component audit reused existing proofs and exposed a missing owner check instead of adding another invariant representation. One outcome groups the three actual-operation guarantees, shared support, both-target owner correction, and resource evidence. Late assurance-wording corrections caused extra infrastructure runs; finish the owned-claim scan and all status/resource edits before the final gate under the existing commit rule. This calls for applying the current rule, not another policy. Complete package gates, controls, cold review, and clean integration passed. Commit purpose is outcome for `f725745d` and required evidence for this acceptance record. The next bounded task audits complete preparation frames against existing laws before selecting new proof work; the regional trial remains pending.

## Local-control/regional preparation frame, 2026-09-21

The independently approved [preparation frame](INTERNAL-COMMUTATION-PROPOSAL.md#local-control-preservation-of-regional-preparation) retains the complete regional artifact after independent local control. The [review receipt](INTERNAL-COMMUTATION-PROPOSAL.md#independent-cold-review-receipt) owns approval and clean integration; this range excludes the acceptance record.

| Increment | Boundary | Code | Documentation | Elapsed | Comparison consequence |
|---|---|---|---|---|---|
| [Local-control/regional preparation frame](INTERNAL-COMMUTATION-PROPOSAL.md#local-control-preservation-of-regional-preparation) | `dadd06d3..e185caca` | `+557/-0` | `+7/-2` | Pre-commit receipt span 1968.453 s; interval union 151.201 s | The preceding regional aggregate checkpoint added 1042 code and sixteen documentation lines. Additions fall by 485 and nine, but complete preparation equality and aggregate validity are different guarantees. Existing token/branch filters, position validity, open projection, and preparation assembly supply the reused machinery; this is no productivity or proof-quality comparison. |

Reproduce timing with `node scripts/summarize-command-receipts.ts /tmp/bpmn-regional-local-control-precommit-receipts`; `sources.json` maps timestamp-preserving copies to 33 completed receipts. Twenty-four nonzero exits retain the missing-theorem red, intermediate Lean proof/probe failures, two TypeScript fixture-construction failures, and one documentation-budget failure. The complete semantic gate took 20.367 seconds; two infrastructure runs totalled 90.755 seconds. Doctor, unreceipted commands, independent review, its committed-target probe, and clean integration are excluded. Receipt duration does not establish engineering effort or critical-path delay. No executable reduction body or kernel fixture changed, so no new Docker resource measurement was required or claimed.

Corrected reassessment: this forward frame is a prerequisite of the complete pair outcome, not a separate outcome under the existing grouping rule; [Finding 63](PROCESS-ASSESSMENT-LEDGER.md#finding-63) records the recurrence. Avoidable probe iterations came from constructing malformed graphs and guessing Lean syntax/API names; inspect the existing fixture and declaration contracts before composing the next discriminator. Complete package gates, cold review, and clean integration passed. The historical measurement remains valid; its separate review boundary was unnecessary.

## Regional preservation of local-control preparation, 2026-09-21

The independently approved [reverse preparation frame](INTERNAL-COMMUTATION-PROPOSAL.md#regional-preservation-of-local-control-preparation) retains the complete local-control artifact after actual independent regional execution. The [review receipt](INTERNAL-COMMUTATION-PROPOSAL.md#independent-cold-review-receipt) owns approval and clean integration; the measured range excludes this acceptance record.

| Increment | Boundary | Code | Documentation | Elapsed | Comparison consequence |
|---|---|---|---|---|---|
| [Regional preservation of local-control preparation](INTERNAL-COMMUTATION-PROPOSAL.md#regional-preservation-of-local-control-preparation) | `b23f68a3..ecd535a0` | `+551/-1` | `+7/-4` | Pre-commit receipt span 1016.166 s; interval union 118.821 s | The preceding forward frame added 557 code and seven documentation lines. The reverse direction reuses the complete local read interface, regional removal masks, and completion field laws. Similar churn does not measure productivity or proof quality; no representation migration or new runtime behavior was required. |

Reproduce timing with `node scripts/summarize-command-receipts.ts /tmp/bpmn-local-regional-reverse-precommit-receipts`; `sources.json` maps timestamp-preserving copies to nineteen completed receipts. Seven nonzero exits retain the missing-theorem red, four intermediate Lean proof failures, and two documentation-budget failures, including the focused reproduction. The complete semantic gate took 21.392 seconds; two infrastructure runs totalled 90.535 seconds. Doctor, unreceipted reads, independent review, its exact-target probe, and clean integration are excluded. Receipt duration establishes neither engineering effort nor critical-path delay. No executable reduction body or kernel fixture changed, so no Docker remeasurement was required or claimed.

Corrected reassessment: this reverse frame is another prerequisite of the complete pair outcome; [Finding 63](PROCESS-ASSESSMENT-LEDGER.md#finding-63) records why its separate review boundary was unnecessary. The existing read interface avoids repeating the selector proof; predicate-filter laws preserve exact order and multiplicity. The missed PLAN backstop shows that document headroom checks must include the routed control-plane and plan guards, not only document reviewability. Their focused correction passed before the complete rerun. The historical measurement and gate evidence remain valid.

## Regional/local-control pair commutation, 2026-09-21

The independently approved [pair outcome](INTERNAL-COMMUTATION-PROPOSAL.md#regionallocal-control-commutation-and-accepted-publication) derives exact final equality and accepted publication in both orders from predecessor facts. The [review receipt](INTERNAL-COMMUTATION-PROPOSAL.md#independent-cold-review-receipt) owns approval and clean integration; the measured range excludes this acceptance record.

| Increment | Boundary | Code | Documentation | Elapsed | Comparison consequence |
|---|---|---|---|---|---|
| [Regional/local-control pair commutation](INTERNAL-COMMUTATION-PROPOSAL.md#regionallocal-control-commutation-and-accepted-publication) | `9656fd33..ab29d3ca` | `+593/-1` | `+6/-3` | Pre-commit receipt span 1078.557 s; interval union 81.194 s | The preceding reverse frame added 551 code and seven documentation lines. This outcome reuses both approved preparation frames and individual preservation/publication laws, adding exact commutation and actual acceptance in both orders. The different guarantees preclude a productivity comparison; no representation migration or runtime behavior change was required. |

Reproduce timing with `node scripts/summarize-command-receipts.ts /tmp/bpmn-regional-local-pair-precommit-receipts`; `sources.json` maps timestamp-preserving copies to twenty completed receipts. Nine nonzero exits retain the missing-theorem red and intermediate proof/probe failures. The complete semantic gate took 20.851 seconds and infrastructure 44.780 seconds. Unreceipted reads, independent review, its exact-target probe, thread-capacity waiting, and clean integration are excluded. Receipt duration establishes neither engineering effort nor critical-path delay. No executable reduction body or kernel fixture changed, so no Docker remeasurement was required or claimed.

Reassessment: one complete pair outcome combines exact state commutation and accepted publication across all regional/local-control combinations. The shared cancellation record update avoids repeating per-field proofs. Failed proof iterations included avoidable guessed type names and missing imports; inspect existing declarations before composing proofs rather than adding another process rule. Keep the representation and complete gates, and reuse this algebra for the next family pair. Commit purpose is outcome for `ab29d3ca` and required evidence for this record. Regional trial closure remains pending.

## Regional pair review-cycle baseline, 2026-09-21

The completed regional/local-control outcome spans both preparation frames and the final pair theorem/publication. The [review receipts](INTERNAL-COMMUTATION-PROPOSAL.md#independent-cold-review-receipt) identify the immutable targets and successful clean complete gates:

| Target | Contribution | Cold-review cycles | Clean full-gate runs |
|---|---|---:|---:|
| `e185caca` | Forward preparation frame | 1 | 1 |
| `ecd535a0` | Reverse preparation frame | 1 | 1 |
| `ab29d3ca` | Exact state equality and accepted pair publication | 1 | 1 |
| Completed outcome | All three contributions | 3 | 3 |

These counts exclude focused builds and pre-commit package/infrastructure gates. For the regional/arming outcome, report the same two counts at completion alongside receipt span/union and code measurements. Required early semantic reviews remain required when a new claim or risk justifies them; fewer cycles alone establish neither quality nor elapsed-time savings. [Finding 63](PROCESS-ASSESSMENT-LEDGER.md#finding-63) owns the corrected process assessment. The regional trial remains pending.

## Regional/arming outcome preparation, 2026-09-21

The [regional/arming outcome](INTERNAL-COMMUTATION-PROPOSAL.md#regionalarming-outcome) groups both preparation frames, exact commutation, successor validity, and actual accepted publication into one implementation target. The [independent review](INTERNAL-COMMUTATION-PROPOSAL.md#independent-cold-review-receipt) approved without findings. No helper-only checkpoint or completeness guard was added. Clean integration passed at `4234d029` after two separate testkit cleanup corrections. The completed pair used one cold-review cycle and four clean full-gate attempts, three failed and one successful, against the [three-review/three-successful-gate baseline](#regional-pair-review-cycle-baseline-2026-09-21). Grouping reduced review cycles; the failures prevented a reduction in gate runs. Neither comparison establishes elapsed savings.

| Increment | Boundary | Code | Documentation | Elapsed | Comparison consequence |
|---|---|---|---|---|---|
| [Regional/arming pair](INTERNAL-COMMUTATION-PROPOSAL.md#regionalarming-outcome) | `eba2eb81..1f490780` | `+2550/-61` | `+29/-5` | Complete pre-commit receipt span 14794.974 s; interval union 2224.000 s | Both preparation directions and the consumer theorems share one review target. Data ownership, bounded completion, and publication add different guarantees from the local-control pair; line counts establish neither productivity nor quality. |
| [Integration Worker cleanup](../packages/temporal-adapter/testkit/src/bypass-mutation.ts) | `1f490780..4234d029` | `+156/-38` | `+22/-14` | Correction receipt span 1244.543 s; interval union 775.523 s | Two independently reproduced testkit lifecycle faults are corrected separately. This range contains no Lean or semantic-core changes and is not additional proof volume. |

The complete pre-commit snapshot at `/tmp/bpmn-regional-arming-complete-precommit-receipts` extends the earlier 99-receipt cutoff with the complete semantic gate and corrected infrastructure run: 101 receipts, 52 nonzero exits. The semantic gate took 1,776.210 seconds, including rebuilt dependencies after shared theorem visibility changed. No executable reduction body or kernel fixture changed. Reproduce with `node scripts/summarize-command-receipts.ts <receipt-root>`; each snapshot's `sources.json` maps timestamp-preserving copies to original receipts.

Through successful integration, `/tmp/bpmn-regional-arming-through-integration-receipts` contains 127 completed receipts, 61 nonzero exits, span 35,700.836 seconds, and interval union 4,892.319 seconds. It includes pre-commit work, independent checks, the exact-target Lean probe, all four integration attempts, isolated diagnostics, and both Worker corrections; acceptance-document validation is excluded. The correction-only snapshot at `/tmp/bpmn-regional-arming-worker-correction-receipts` contains 17 receipts and five nonzero exits. The earlier third-attempt snapshot remains retained. The overall span includes an owner-directed stop and resumption. Missing lane labels establish no delegation count, and neither interval measures engineering effort or critical-path delay.

The first integration attempt (`/tmp/bpmn-regional-arming-clean-integration`, output SHA-256 `645ae5c2143c76d9f0099ba6e2ff8b0c66de237e5e5b8ee92be543d4415d992d`) failed in a bounded-scope test process with a contemporaneous native V8 `SIGSEGV`. The selected crash evidence at `/tmp/bpmn-regional-arming-crash-evidence.json`, SHA-256 `de335e5ea7e4879cceff354f8aaab9e1a1c7986d0c95d8928b880c3f883c2319`, matches the leading stack frames of two pre-target reports. The unchanged isolated test passed 4/4. The second attempt (`/tmp/bpmn-regional-arming-clean-integration-retry`, output SHA-256 `8698e082ebb7e6792102969c420abc248003b8b020398f91f848165fba1dcbe1`) passed Temporal but exceeded the pipeline's 10-second replay deadline. An external timing diagnostic then passed all 72 cases and replayed 83 histories in 926.481 ms; its corrected receipt is `/tmp/bpmn-regional-arming-pipeline-replay-diagnostic-2`. The initial diagnostic's wrong package-resolution root is retained as a failed receipt. Neither diagnosis establishes a native-crash or replay-timeout fix, and no deadline was raised.

The third attempt (`/tmp/bpmn-regional-arming-clean-integration-final`, output SHA-256 `76c4283f1e9b2f43cde0702b1fb2897320839f41c44e547ea2ddea47840a3d18`) exposed a concrete mutation-Worker lifecycle defect. Bundling took 29,629 ms, outlasting the 20-second startup deadline; the abandoned creation subsequently registered a Worker that had no caller to shut it down. Two later probes reused its queue and failed native registration. After all tests finished, the stranded test child continued polling a stopped server; root sent `SIGTERM`, then `SIGKILL` when graceful termination did not finish. The parent recorded 329 passes, three failures, and exit 1. The [startup correction](../packages/temporal-adapter/testkit/src/bypass-mutation.ts) retains the deadline, shuts down late-created Workers through SDK `runUntil`, and isolates probe queues. Both mutation-runner paths reproduce the late-cleanup and queue-collision class before the fix; normal-startup controls and seven real mutation witnesses pass afterward. Startup cleanup landed at `364ba7f0`. A separately reproduced termination failure also skipped Worker shutdown; nested cleanup fixes that at `4234d029` while retaining the termination error. Both corrections passed complete Temporal and infrastructure gates before commit. The subsequent clean integration passed in 545.752 seconds; the [review receipt](INTERNAL-COMMUTATION-PROPOSAL.md#independent-cold-review-receipt) binds its exact target and output. No Lean or semantic-core bytes changed after the reviewed proof target.

Reassessment: the existing selector read interface now serves local control and arming; canonical insertion/filter algebra and individual publication laws avoid a new state-store representation. Data arming still needs explicit freshness and Activity-local ownership arguments, while bounded completion removes a parent-owned deadline: neither is covered by plain token disjointness. Failed iterations include incorrect Lean destructuring/unfolding and guessed lemma names; inspect the existing declarations before composing the next pair. The first infrastructure run also caught a seven-word map overrun and a sandbox-denied loopback bind; shorten the owned status and rerun with the required local access, preserving both failures. No executable reduction body or kernel fixture changed, so no new Docker resource measurement is required. The regional trial remains pending.

## Regional/regional pair preparation, 2026-09-21

The [regional/regional outcome](INTERNAL-COMMUTATION-PROPOSAL.md#regionalregional-outcome) groups complete preparation frames, literal canonical state equality, successor validity, and accepted publication in one implementation target. Independent review approved after one same-reviewer ledger correction; clean integration passed at `aa497c93`. The completed outcome used one cold-review cycle, one correction audit, and two clean full-gate attempts, one failed and one successful, against the [three/three baseline](#regional-pair-review-cycle-baseline-2026-09-21). No helper-only review or commit was opened; these counts establish neither proof quality nor elapsed savings.

| Increment | Boundary | Code | Documentation | Elapsed | Comparison consequence |
|---|---|---|---|---|---|
| [Regional/regional pair](INTERNAL-COMMUTATION-PROPOSAL.md#regionalregional-outcome) | `72612d6c..cafc8018` | `+3757/-4` | `+13/-2` | Pre-commit receipt span 6188.874 s; interval union 482.401 s | Sixteen ordered family combinations and bounded completion share one consumer. Exact graph, cancellation, and lifecycle preservation add different obligations from the arming pair; line counts establish neither productivity nor quality. |

Reproduce timing with `node scripts/summarize-command-receipts.ts /tmp/bpmn-regional-pair-precommit-receipts`; `sources.json` maps timestamp-preserving copies to 67 completed receipts, including 37 nonzero exits. The complete semantic gate passed in 38.293 seconds; the final Lean gate and corrected 573-check infrastructure gate passed. The temporary Lean probe passes twenty joint-predecessor/state/publication checks and both axiom reports. Its first failures were an unavailable convenience function and omission of Lean's existing Terminate profile tag, not a change to selection. The initial infrastructure run caught missing module descriptions and sandbox-denied loopback access; the correction preserves both failures. Final documentation validation, review, and clean integration are outside this timing cutoff. No executable reduction body or kernel fixture changed, so no new Docker resource measurement is required.

The first clean integration attempt at `cafc8018` stopped at the cost-rank guard: adding this row changed the measured population from 93 to 94 and three historical code ranks. The author added the final row after the complete infrastructure run, then omitted the cost-comparison guard from the final documentation checks. The independent reviewer confirmed this as the sole required edit. The historical claims were corrected together and the complete 573-check infrastructure gate passed before `aa497c93`; no proof or runtime change was involved. The failed receipt `/tmp/bpmn-regional-pair-clean-integration` is retained. This correction is required evidence, not another proof checkpoint.

Through successful integration, `/tmp/bpmn-regional-pair-through-integration-receipts` retains 76 completed receipts, 39 nonzero exits, span 7384.760 seconds, and interval union 1154.062 seconds. It includes exact-target reviewer probes, both integration attempts, and the ledger correction gate; unreceipted reviewer reads/tests and acceptance-document validation are excluded. `sources.json` retains the timestamp-preserving input map. These intervals establish neither engineering effort nor critical-path delay. The [review receipt](INTERNAL-COMMUTATION-PROPOSAL.md#independent-cold-review-receipt) binds the successful 524.261-second gate and its contended pipeline timing; no performance improvement is claimed.

Reassessment: shared removal laws and individual preservation/publication results support the complete pair without a new state-store framework. The largest additional boundary is exact cancellation lifecycle preservation, including private handlers and historical Call ownership; root disjointness alone cannot supply it. Proof iteration still included guessed simplifier behavior and avoidable probe API errors. Keep subsequent work tied to its complete consumer, inspect existing selector restrictions before constructing probes, and include every changed owner's reported guard in final validation. Regional trial closure remains pending; this increment's commit purpose is outcome.

## Regional/scope-creation pair preparation, 2026-09-22

The [regional/scope-creation outcome](INTERNAL-COMMUTATION-PROPOSAL.md#regionalscope-creation-outcome) groups both complete preparation frames, exact canonical state equality, successor validity, and actual accepted publication from baseline `23388fc2`. Independent review approved without findings and clean integration passed at `2c4a7bb5`. The completed outcome used one cold-review cycle and two clean full-gate attempts, one failed and one successful, against the [three/three baseline](#regional-pair-review-cycle-baseline-2026-09-21). No helper-only checkpoint was opened; fewer cycles establish neither proof quality nor elapsed savings.

| Increment | Boundary | Code | Documentation | Elapsed | Comparison consequence |
|---|---|---|---|---|---|
| [Regional/scope-creation pair](INTERNAL-COMMUTATION-PROPOSAL.md#regionalscope-creation-outcome) | `23388fc2..2c4a7bb5` | `+2228/-0` | `+12/-2` | Pre-documentation receipt span 2482.235 s; interval union 92.020 s | Both preparation directions, exact state equality, validity, and accepted publication share one consumer boundary. The preceding regional/regional pair added 3757 code and thirteen documentation lines; the different insertion/removal obligations preclude a productivity comparison. Existing graph and collection laws supply the shared machinery. |

The pre-documentation snapshot at `/tmp/bpmn-regional-scope-creation-precommit-receipts` contains 38 completed receipts, 21 nonzero exits, span 2,482.235 seconds, and interval union 92.020 seconds. Its timestamp-preserving `sources.json` mapping reproduces through `node scripts/summarize-command-receipts.ts <receipt-root>`. The complete Lean/core gate passed in 23.074 seconds with 1,142 core tests; the seventeen-case Lean probe checks joint hypotheses, actual executions, multiplicity, both preparation directions, publication, and containing-region refusal. Both consumer axiom reports retain only the existing allowlist. Documentation validation, review, and clean integration are outside this cutoff; missing lane labels establish no delegation count, and neither interval measures engineering effort or critical-path delay.

The first infrastructure attempt caught an inferred test-publication type error; an explicit type fixes it. The next caught a 251-word resume section against its 250-word limit. The owned next-action paragraph now fits with margin. Both failed receipts remain retained; complete core tests pass after the annotation change. Validate these small document/type contracts before the next complete gate rather than spending another full run discovering them.

The first clean integration attempt failed the existing Sequential Multi-Instance host-clock margin after successful natural completions: 2859 ms exceeded the 2500 ms margin on a 5000 ms Timer. Its receipt `/tmp/bpmn-regional-scope-creation-pre-push` records 444.435 seconds and output SHA-256 `ed69d5f7e6bc2da429db822577333227ec06d4da1814bd239b528ff3968eae1f`. The unchanged isolated witness passed in 14.091 seconds at `/tmp/bpmn-regional-scope-creation-smi-isolated`, output SHA-256 `20ba0d754d687614fa95026bc33f5d39f033f18c3978c19598a2622d15020e8f`. This failure class predates the target, as recorded in [mixed local-control verification](#mixed-local-control-production-verification). No deadline, margin, runtime code, or VM flag changed. External CPU work was observed after the failure; that observation does not establish its cause. The successful complete retry establishes integration acceptance, not a timing-defect fix or performance improvement.

Through successful integration, `/tmp/bpmn-regional-scope-creation-through-integration-receipts` retains 47 completed receipts, 24 nonzero exits, span 4241.477 seconds, and interval union 1269.515 seconds. Its timestamp-preserving `sources.json` mapping includes both complete attempts and the isolated diagnostic; unreceipted reviewer checks and acceptance-document validation are excluded. No lane labels were present. These intervals establish neither engineering effort nor critical-path delay. Commit purpose is outcome for `2c4a7bb5` and required evidence for this acceptance record; regional trial closure remains pending.

Reassessment: existing graph leastness, selector transport, individual preservation/publication laws, and insertion/filter algebra support the complete consumer without a new state-store representation. The Call census needs the existing structural invocation-owner rule, not merely association validity; the proof derives it explicitly. Failed iterations include incorrect Lean rewriting, type inference, and one forbidden umbrella-build spelling, which the wrapper refused before work. No executable reduction body or kernel fixture changed, so no new Docker resource measurement is required. The author reused an implementation thread whose old name did not match its new task, contrary to the task-shaped naming rule; subsequent delegation must use correctly named lanes while reserving review capacity. Regional trial closure remains pending.

## Finite regional batch integration, 2026-09-22

The [finite regional outcome](INTERNAL-COMMUTATION-PROPOSAL.md#finite-regional-batch-outcome) composes the approved pair laws in the existing finite induction and production dispatchers. Complete preparation, prefix validity, literal canonical state equality, accepted publication, and whole-command refusal share one implementation target, `63b20e13`. Independent review approved without findings and clean integration passed. This outcome used one cold-review cycle and one clean full-gate run, against the [three/three pair baseline](#regional-pair-review-cycle-baseline-2026-09-21); no helper-only checkpoint was opened. Regional trial closure remains pending.

| Increment | Boundary | Code | Documentation | Elapsed | Comparison consequence |
|---|---|---:|---:|---|---|
| [Finite regional batch integration](INTERNAL-COMMUTATION-PROPOSAL.md#finite-regional-batch-outcome) | `587ec3dd..63b20e13` | `+934/-338` | `+38/-16` | Pre-commit receipt span 4992.692 s; interval union 3777.904 s | The nearest finite integration comparator is mixed scope-creation batching (`+1372/-33` code, `+10/-6` documentation): code additions fall by 438 while documentation additions rise by 28. This range also records six resource measurements and updates the shared IL contract. Approved pair laws supply the induction cases; publication values and individual acceptance have shared owners below the dispatcher. Grouping the complete consumer removes the earlier helper-only review cycles without removing assurance. Different obligations and external CPU contention prevent a productivity or timing comparison. |

Through the final requested reviewer probe, `/tmp/bpmn-finite-regional-through-review-receipts` retains 45 completed receipts, 13 nonzero exits, span 6120.754 seconds, and interval union 4777.193 seconds. Its timestamp-preserving `sources.json` mapping includes the complete integration run and reviewer-requested checks; unreceipted static review and acceptance-document validation are excluded. No lane labels were present. The owner reported external CPU contention during native Lean and integration verification. These intervals establish neither engineering effort, critical-path delay, nor a performance trend. Commit purpose is outcome for `63b20e13` and required evidence for this acceptance record.

All 577 Lean/build inputs in `.cache/finite-regional-batch-20260922/source-v1-sha256.json` match both the immutable read-only snapshot and the working tree. The pinned Lean/image, one CPU/thread, no network, fixed 3,221,225,472-byte controller, and zero additional swap remain unchanged. The first controlled canonical-consumer dependency build passes in 792.69 seconds at 1,727,508 KiB GNU RSS and 2,104,602,624 cgroup bytes. Earlier native narrow proof builds are not resource evidence. The seventeen-case Lean probe passes in 166.33 seconds at 758,408 KiB RSS and 375,357,440 cgroup bytes; output is retained in the owning proposal. Both have zero controlled memory events, pressure totals, and swap.

The six cold measurements below retain warm dependencies and move aside only each exact target's generated products inside its container, verifying absence before rebuilding. Every run explicitly compiles its named target and exits zero with every memory event, pressure total, and swap count zero. Mounted-source SHA-256 and complete command-receipt SHA-256 bind the replacement [module-cost rows](../scripts/lean-module-cost.ts). The watch targets consume the changed dispatcher; their source files are unchanged. Historical rows remain in Git. The worst cgroup peak leaves 1,115,013,120 bytes below the fixed ceiling; elapsed differences from historical runs are not a controlled performance comparison.

| Exact target | Elapsed | GNU maximum RSS | Cgroup peak | Build-output SHA-256 |
|---|---:|---:|---:|---|
| `FiniteInternalTransitionConformance` | 14.12 s | 1,223,912 KiB | 935,297,024 bytes | `5071330f06219baa6b6d9a0af526f60c77c370ee8c1dcb537d7d15653a99fadc` |
| `ScopeCreationMixedBatchConformance` | 134.28 s | 2,002,316 KiB | 2,106,212,352 bytes | `dcf3f877c8a445ff4d4b4cab9463b6b279d07c4daeaa213b0c39892272f9be83` |
| `CallActivityConformance` | 42.14 s | 1,382,800 KiB | 1,086,783,488 bytes | `0b029c153fd7aa56bf14dab9ba19bf552c3c2dff57ed9ef686b61bfd0bfc482a` |
| `SequentialMultiInstanceProgramBindingConformance` | 42.45 s | 1,588,420 KiB | 1,252,679,680 bytes | `2d165d5ffef9aee60d2457b69514c86d424a501d89414b182e0610027cee90c9` |
| `TimerStartConformance` | 23.96 s | 1,457,220 KiB | 1,191,743,488 bytes | `46df5182dafa32afbe3b37bb5a29420a14ccb3cd3775ccf23e68a6c48d8b0d35` |
| `TerminateEndEventConformance` | 48.85 s | 1,692,992 KiB | 1,617,563,648 bytes | `e05fe7d2d970836ce9458f1456790998b77e41561489cd0a798d43e41582a5e7` |

Reassessment: reusing the approved pairs avoids new per-field preservation owners. Separating publication values and individual acceptance from the dispatcher removes an import cycle without adding a state-store framework. Root completion requires a derived protected-control argument for nontrivial batches; a new runtime exclusion or an assumed intermediate invariant would weaken the result. The complete core gate passes 1,166 tests. Failed iterations include Lean interface/destructuring corrections, a bounded fixture's leftover merge, and a private admission witness whose successful closure needs nine steps rather than eight. The first infrastructure run caught the expected stale resource row and sandbox-denied loopback access; a later run caught one incorrectly formatted implementation-map route. The correction uses the existing structural route form; failed receipts remain retained. Future shared-dispatch/proof-layout changes must begin in the existing bounded Linux cache under the already-required ceiling, rather than preceding resource validation with native proof builds.

## Ordinary End batch integration, 2026-09-22

The [ordinary End outcome](INTERNAL-COMMUTATION-PROPOSAL.md#ordinary-end-batch-outcome) spans `71151bcce06756aa3b2790446d8f6904955836a6` through approved correction `ae78a5341b7ba98908741b250c02c168512af87b`. Complete preparation, mixed-family frames, finite state/publication, successor validity, and command rollback remained one outcome: one cold review, one same-thread correction audit, and two clean full-gate runs, the first failing at a stale source oracle and the second passing. No helper-only checkpoint was opened; the [three-review/three-gate baseline](#regional-pair-review-cycle-baseline-2026-09-21) remains the comparator. Regional trial closure remains pending.

| Increment | Baseline..endpoint | Nonblank code | Nonblank documentation | Elapsed | Comparison and consequence |
|---|---|---:|---:|---|---|
| [Ordinary End batch integration](INTERNAL-COMMUTATION-PROPOSAL.md#ordinary-end-batch-outcome) | `71151bcc..ae78a534` | `+1527/-28` | `+29/-9` | Root receipt span 7727.722 s; interval union 3229.828 s | Against finite regional integration (`+934/-338` code, `+38/-16` documentation), code additions rise by 593 and documentation additions fall by nine. End adds a prepared family and its mixed pair laws; the preceding integration reused completed pair owners. Token-patch and fixed regional-action reuse avoid another aggregate preservation layer. The avoidable cost was discovering an existing source refusal oracle only during integration: check retained source-level expectations for each newly batchable operation before the first implementation commit. |

The 73 completed root receipts through clean integration are `/tmp/bpmn-merge-end-baseline`, `/tmp/bpmn-end-batch-*`, and `/tmp/bpmn-end-source-correction-*`, selecting only directories with complete receipt files. Their span and interval union use `command.txt` and `exit-status` modification times; 28 record failed attempts. These figures include diagnostics, resource checks, compilation iterations, and the failed integration, but exclude the independent reviewer's commands and subsequent acceptance-document checks. They measure command intervals, not engineering effort or critical-path delay.

The first clean gate at `aa5ab5ad` failed because the retained admitted End/arming source test still expected rollback. Correction `ae78a534` preserves its two raw execution orders and checks exact committed state/publication at fuel five and whole-command rollback at fuel zero, two, three, and four. All 817 source tests and 573 infrastructure checks pass; one correction harness run caught the test's nullable owner and was fixed with an explicit assertion. The final pre-commit infrastructure attempt also required local-loopback authorization after a sandbox-only `EPERM`. No production or Lean input changed during correction. Clean integration passed in 557.643 seconds with 72 agreeing cases, 83 replayed histories, and a clean target. Its 53.715-second pipeline exceeded the 36-second soft target; no performance improvement is claimed.

All 581 archived Lean/build inputs in `.cache/end-batch-20260922/source-v7-sha256.json` match the final read-only snapshot and worktree. The six final `/tmp/bpmn-end-batch-*-cold-v2` receipts retain the pinned Lean/image, one CPU/thread, no network, fixed 3,221,225,472-byte controller, and zero additional swap. Dependencies were prewarmed; each final run moved aside only its own generated target products and compiled exactly that target. Every memory event, pressure total, and swap count is zero. Mounted-source and complete command-receipt digests bind the replacement [module-cost rows](../scripts/lean-module-cost.ts). The largest cgroup peak leaves 1,253,859,328 bytes below the unchanged ceiling. External CPU contention prevents a timing-trend inference.

| Exact target | Elapsed | GNU maximum RSS | Cgroup peak | Build-output SHA-256 |
|---|---:|---:|---:|---|
| `FiniteInternalTransitionConformance` | 10.18 s | 1,358,004 KiB | 1,073,033,216 bytes | `4fa12f969d7f7752c4b0c1f02ef8ec7b2dac5900c98dfeaa17269507d42961f7` |
| `ScopeCreationMixedBatchConformance` | 76.64 s | 1,999,244 KiB | 1,967,366,144 bytes | `bd2c0a99fe308a6347b273788d89f10abb240080dd6b943a49d71e5c69b24542` |
| `CallActivityConformance` | 21.86 s | 1,389,356 KiB | 1,098,457,088 bytes | `e5516aec04c0d1a4e4f0ff1ea012913e350a75045f1a2e526aefa050dcf631c6` |
| `SequentialMultiInstanceProgramBindingConformance` | 26.00 s | 1,587,628 KiB | 1,300,836,352 bytes | `b129c9e8bbe315f682ac846a2af4f3b62b346eb975b42563a4fbb920ee86af84` |
| `TimerStartConformance` | 20.42 s | 1,456,780 KiB | 1,178,267,648 bytes | `f8a7fe276ecae9907e742882a42bc6d455f9d5805932b7de33229df32ddb8472` |
| `TerminateEndEventConformance` | 31.34 s | 1,693,972 KiB | 1,420,865,536 bytes | `96c4f290752b7f86f1a8720dd9eaa71b0f53358330a39c188418a226ac3449e0` |

The earlier `cold-v1` series remains diagnostic: Call, Timer, and Terminate also rebuilt prerequisites, and the End proof modules subsequently gained their required semantic-scope documentation. Those runs do not replace the final warm-dependency measurements. The initial bounded dispatcher attempt omitted the pin-reader script from its snapshot and failed before Lean; the corrected snapshots retain that input. Compilation corrections discharged the End snapshot-refusal case, protected-control projection, and publication-time equality without changing the finite consumer's statement. Three infrastructure runs failed only on the runtime map's word limit; directly checking the exported document contract before rerunning corrected that avoidable feedback loop. The fourth run passed all 573 checks. Failed receipts remain retained.

The complete pre-commit `./scripts/lake.sh test` passed all 554 targets in `/tmp/bpmn-end-batch-lean-full`. Its dependency rebuild follows the added prepared-transition constructor; the shared finite theorem's statement is unchanged. The owner reported external CPU contention and subsequently released it during this run, so its elapsed time is not a performance comparator.

Reassessment: End uses the existing token-patch validity/commutation laws and regional relative-increment atom. Regional frames reuse fixed token actions and ownership-closed selection rather than adding another field-by-field invariant layer. The End publication adapter belongs beside the prepared dispatcher; placing it in the lower shared publication owner would unnecessarily make earlier pair proofs depend on End preparation. The final lower owner is unchanged. All 1,184 semantic-core tests pass, including the eighteen End discriminators. The concrete Lean witness jointly establishes preparation, predecessor validity and projectability before applying the generic permutation theorem; it establishes no registered-profile reachability. The End and finite-consumer axiom reports contain only `propext`, `Classical.choice`, and `Quot.sound`.

## Exact Merge frontier integration, 2026-09-22

The [exact Merge outcome](INTERNAL-COMMUTATION-PROPOSAL.md#exact-merge-frontier-outcome) starts at `146fd0f88db0e976ae6a3e1688ea2dae483a8e44`. Complete offered-input discovery, mixed-family preservation, canonical batch state/publication, private scheduled consumption, and atomic refusal remain one outcome. Supporting modules have opened no helper-only review. The shared token-patch laws serve Merge and the existing local-control/End consumers; regional framing reuses the existing ownership and fixed-action account rather than adding RuntimeState fields or another aggregate preservation layer. No public profile or Temporal scheduling admission changes.

The complete semantic-core gate passes 1,202 tests and the source gate passes 817 tests. Twenty answer-free shared scheduling cases compare both interpreters' actual outcomes, failure precedence, rollback, replay, frontier counts, and publication. Extending that witness to every replayed predecessor exposed a stale Lean diagnostic that reported zero enabled operations for a two-alternative Merge; discovery now supplies both execution and that count. The additional schedule that accumulates two Merge outputs exposed Lean closure's fallback to a broader raw End relation. Frontier discovery now applies the already-reviewed single-unit End selector, preserving the TypeScript restriction and retaining the distinct stable-running result as an explicit private witness.

The first bounded full Lean run failed in existing admission proofs because the refactor hid the result-only admission boundary behind a private evaluator. Restoring that boundary and sharing the scheduled-closure result preserved the theorem statements. The next run found Configured Task mutation and User Task metadata regressions: singleton execution had acquired publication validation as an execution prerequisite. Two raw-lowering probes reproduced rollback both for the pass-through mutation and for noncanonical operation storage; restoring singleton result/publication separation made both commit their expected states. The original conformance assertions are unchanged. This is a reason to include existing wrong-lowering and symbolic-metadata consumers in the focused closure check before a full rebuild, not to weaken their contracts or add a process document.

Resource chronology remains explicit. `.cache/exact-merge-20260922/proof-integration-v1` passed the combined proof build at 2,096,467,968 cgroup bytes with zero controlled events and pressure totals. `full-library-v2` failed the admission proofs at 2,990,469,120 bytes, also with zero events and pressure. `full-library-v3` took 1,647.62 seconds, failed the two conformance consumers, reached the exact 3,221,225,472-byte ceiling, recorded `max=1401`, and recorded 397,201 microseconds in each pressure total; no OOM occurred, and this is not accepted resource evidence. The focused singleton correction passed both unchanged consumers and the cancellation-projection consumer in 218.91 seconds at 2,954,567,680 bytes with every controlled memory event zero; its pressure totals were 147,631 microseconds each, so it establishes no zero-pressure result. That diagnostic snapshot predates the End-discovery correction and does not bind the final source.

The final `source-v5` snapshot passed all 567 targets in `./scripts/lake.sh test` in 1,479.79 seconds, at 2,841,940 KiB RSS and 3,004,313,600 cgroup bytes, below the unchanged 3,221,225,472-byte limit. Every controlled memory event was zero; each pressure total was 73,265 microseconds, so this is not a zero-pressure claim. The asserted receipt is `/tmp/bpmn-exact-merge-full-lean-linux-3`, complete receipt SHA-256 `00332fd2b3770d11eda03c8259fd02378ed529ebaa3538f1e855fc7a6cf9d8ba`. The 599-entry input manifest `.cache/exact-merge-20260922/source-v5-sha256.json`, SHA-256 `b183480c38f8709301521feb7a5bf887fdeed00f3c02c6252dc12dbcde4552de`, matches both archived and working-tree source/build inputs. This is a dependency-invalidated full-library run with warm unchanged inputs, not a from-scratch library measurement.

Cold-target calibration uses the same final snapshot and fixed limit, with dependencies warm and only each measured target's own products removed. `FiniteInternalTransitionConformance` passed in 11.43 seconds at 1,379,540 KiB RSS and 1,119,379,456 cgroup bytes, with all controlled events and pressure totals zero; its asserted receipt is `/tmp/bpmn-exact-merge-finite-cold-linux-1`. The unchanged-source cancellation consumer passed in 105.65 seconds at 2,841,852 KiB RSS and 2,945,003,520 cgroup bytes, with all controlled events zero and each pressure total 74,463 microseconds; its asserted receipt is `/tmp/bpmn-exact-merge-cancellation-cold-linux-1`. At that revision, both mounted-source hashes and complete receipt digests were bound in [the cost rows](../scripts/lean-module-cost.ts). That Cancellation measurement exceeded the existing 90% RSS disclosure threshold and required a `nearCapModules` disclosure; neither the threshold nor the memory limit changed. Its changed dependency closure is the reason for remeasurement despite unchanged fixture bytes.

All 19 inspected Merge, mixed-pair, finite-publication, scheduling, rollback, and replay theorem axiom reports contain only `propext`, `Classical.choice`, and `Quot.sound`; the asserted receipt is `/tmp/bpmn-exact-merge-axioms-linux-1`. The focused TypeScript gate, complete packages, harness types, and gate-wiring checks retain their receipts under `/tmp/bpmn-exact-*`. The first documentation check caught an overlong PLAN resume point; the shortened resume passes its existing contract. Failed receipts remain alongside successful ones. Receipt intervals describe commands, not engineering effort or critical-path delay; this increment supplies no performance trend.

Final infrastructure verification exposed two cost-policy fixtures that replaced the production rows while inheriting the production near-cap list, plus the stale executable-root expectation after adding the scheduled interpreter. The two independent fixture failures reproduce the same coupling defect. Synthetic measurement fixtures now carry their own below-cap rows and empty disclosures; the live derived-disclosure assertions are unchanged. The executable-root guard includes the new root and its pipeline invocation. All 53 focused cost/entrypoint checks pass; `/tmp/bpmn-exact-schedule-infrastructure-final-1` retains the three original failures.

| Increment | Boundary | Code | Documentation | Elapsed | Comparison consequence |
|---|---|---:|---:|---|---|
| [Exact Merge and private scheduling integration](INTERNAL-COMMUTATION-PROPOSAL.md#exact-merge-frontier-outcome) | `146fd0f8..28bac900` | `+3057/-573` | `+54/-28` | Precommit receipt span 12121.442 s; interval union 4689.129 s; separate UI recovery 124.759 s | Against ordinary End integration (`+1527/-28` code, `+29/-9` documentation), this outcome adds exact alternatives and transactional scheduling across the existing finite consumer. One cold review, two same-thread correction audits, one complete-gate attempt, and one failed-lane recovery replace helper-only checkpoints. This comparison measures grouping, not equivalent semantic scope or productivity; the regional trial remains pending. |

The frozen precommit receipt set `/tmp/bpmn-exact-frontier-precommit-receipts`, with its original-path inventory in `sources.json`, contains 100 completed commands at baseline `146fd0f8`, including 40 failures. The table's precommit interval measurements exclude review, postcommit integration, and correction receipts. These are command observations, not engineering effort, critical-path delay, or an agent count. The [review receipt](INTERNAL-COMMUTATION-PROPOSAL.md#independent-cold-review-receipt) owns the original integration failure and successful browser recovery. All selected lanes are now evidenced, while the original failed umbrella receipt remains unchanged. Recovery reused successful lanes whose code and build inputs remained byte-identical; the later documentation passed infrastructure verification.

Reflection: updating the implementation account without sweeping its current IL, atomicity, and map owners caused avoidable correction work; the first correction still missed a sibling unsupported-claim sentence. Include those owners and their actual changed sections in the initial claim sweep and generated packet. One correction infrastructure run also failed solely because the sandbox denied its loopback listener; the authorized rerun passed without source changes. Finally, inspect the selected integration lanes' pinned executables before starting their expensive predecessors: the absent browser was discoverable before the complete gate. Preserve passed unchanged lanes when resolving that environment failure; do not repeat bounded Lean measurements for documentation corrections. These are applications of existing ownership and proportionate-verification rules, not a new process document.

## Standalone Activity-data arming, 2026-09-22

The [standalone arming outcome](INTERNAL-COMMUTATION-PROPOSAL.md#standalone-activity-data-arming-outcome) starts at `7c509b9627ef75da85fa6981366dfd5cceee8414`. Input-only and output-only arming reuse the composed variant's complete lifetime patch, predecessor frames, and finite/mixed publication consumers. Generalizing its existing activation-preservation proof adds one shared insertion theorem and keeps the composed theorem's statement; it avoids a second field-by-field preservation proof. There is no new RuntimeState field, atom domain, public profile, wire shape, or Temporal mechanism. Preparation, successor validity, exact state/publication, and production integration remain one review outcome.

| Increment | Baseline..endpoint | Nonblank code | Nonblank documentation | Elapsed | Comparison and consequence |
|---|---|---:|---:|---|---|
| [Standalone Activity-data arming](INTERNAL-COMMUTATION-PROPOSAL.md#standalone-activity-data-arming-outcome) | `7c509b96..486ac42f` | `+1130/-687` | `+42/-17` | Root receipt span 9775.762 s; interval union 6937.496 s | Ordinary End batching measured `+1527/-28` code and `+29/-9` documentation. This increment extends existing data owners and adds no proof module; the measured churn includes dedenting the shared preservation proof. Different guarantees and compilation conditions prevent an effort or performance comparison. |

One cold review, one same-thread correction audit, and one clean complete Product 1 run closed the outcome. The 45 complete `/tmp/bpmn-standalone-arming-*` receipts through `/tmp/bpmn-standalone-arming-correction-infrastructure-1` supply the span and interval union from command/status modification times; fifteen have nonzero status. They exclude delegated implementation/reviewer commands and later acceptance checks. The separately disclosed zero-exit resource rejection remains failed acceptance evidence. These intervals measure commands, not engineering effort or critical-path delay.

Reflection: the shared insertion theorem preserves the composed statement and avoids duplicate invariant machinery. The remaining avoidable correction was an unchanged source-contract paragraph contradicting its edited evidence row, recorded as [Finding 15, instance 25](PROCESS-ASSESSMENT-LEDGER.md#finding-15). The same-reviewer audit verified the historical-facade qualification and current-owner link. The correction changed documentation only, so passed semantic, resource, and clean integration evidence was retained rather than rebuilt. Next-family work first checks the existing common activation and frame owners before adding another preservation layer; no new process rule or reduced gate is introduced.

The complete semantic-core and source-package gates pass 1,221 and 817 tests respectively. The TypeScript red witness reproduces independent standalone arming rollback before dispatch integration. The root's 61-case integrated gate and the mixed-family Lean consumer pass; 32 focused documentation, ownership, and census checks pass. The first infrastructure run caught a test-only enum-widening annotation; the corrected run passed 581 checks and failed only the expected stale fixture measurement. With final measurements recorded, `/tmp/bpmn-standalone-arming-infrastructure-3` passes all 582 checks. The first bounded fixture attempt found a non-synthesized match-proposition decision instance and a record-layout parse error; both were corrected without changing production semantics. Failed receipts remain retained.

The first complete bounded Lean run, `/tmp/bpmn-standalone-arming-full-lean-1`, reached its unchanged 1,800-second deadline and exited 124 after reporting target 405 of 567. Its 2,816,712,704-byte cgroup peak stayed below 3 GiB; every controlled memory event was zero, while both pressure totals were 397,925 microseconds. The command is failed evidence, not full-gate acceptance. During the run the host load average rose from 65.41 to 95.95 and other applications were active; the Error-propagation fixture took 412 seconds against 38 seconds in the preceding calibration. These observations do not isolate a code regression from host contention. The retry keeps the same source snapshot, controller, deadline, and successful build cache; no input or guarantee is relaxed.

The retry `/tmp/bpmn-standalone-arming-full-lean-2` checked all 567 targets and exited zero in 1,136.37 seconds, but is rejected resource evidence: `memory.peak` reached exactly 3,221,225,472 bytes and `memory.events.max` was 799. OOM counters stayed zero, GNU RSS was 2,788,356 KiB, and both pressure totals were 876,165 microseconds. Exit zero does not override the [cgroup acceptance rule](#snapshot-invariant-and-cgroup-acceptance-correction); the required response is isolated cold-consumer checks under the same controller before any complete warm acceptance receipt.

All 599 archived Lean/build inputs in `.cache/standalone-data-arming-20260922/source-v2-sha256.json` match the final read-only snapshot and worktree; the manifest SHA-256 is `24580f6ea6807294f53505f7c24a59773d1e8457fcf0585794939e5fb481ca61`. The ten isolated cold-consumer checks below retain Lean 4.31.0, image `sha256:4df22c7a1ec8ba8c4c2795fdea1269d7b9cadade4e65b22e595047509aa27950`, one CPU/thread, no network, the fixed 3,221,225,472-byte controller, and zero additional swap. Dependencies are warm; each run moves aside only its target's generated products. Mounted-source and complete command-receipt digests bind the replacement [module-cost rows](../scripts/lean-module-cost.ts). Every controlled memory event and swap count is zero. Cancellation records 164,365 microseconds in each pressure total; the other nine record zero. The largest cgroup peak leaves 349,736,960 bytes below the ceiling. The derived RSS near-cap set is empty; this is no performance-improvement claim.

| Exact target | Elapsed | GNU maximum RSS | Cgroup peak | Build-output SHA-256 |
|---|---:|---:|---:|---|
| `FiniteInternalArmingConformance` | 22.63 s | 1,486,952 KiB | 1,225,752,576 bytes | `2c4792d62ece3f9268fb26879f3b26eb139e4dd409774b76822b18c92d1076d1` |
| `FiniteInternalTransitionConformance` | 14.21 s | 1,364,756 KiB | 1,754,128,384 bytes | `1ec820da34b74ae62d712a1fa336fe6976a1f4379f8e00b78820c1c18f6ef58a` |
| `ScopeCreationMixedBatchConformance` | 87.55 s | 2,110,356 KiB | 2,221,199,360 bytes | `e1eb4f8b9f725c222b00a60b2b3891c66a1ec49be84edba3474f82d6e6c96394` |
| `InternalRegionalCancellationProjectionConformance` | 107.47 s | 2,812,648 KiB | 2,871,488,512 bytes | `d6d0251574bbe0e64dc66ae8d3c333681b98c6427d1e7a758924d72a21cede65` |
| `CallActivityConformance` | 29.36 s | 1,392,412 KiB | 1,199,734,784 bytes | `cd8d32ec90b3661dc9e88c1bd730c2021686d32a257a0b0af609c7c5f46c6c8f` |
| `SequentialMultiInstanceProgramBindingConformance` | 27.55 s | 1,592,064 KiB | 1,332,031,488 bytes | `fd176cd6f1291729818c3602788f596f08f0f61879c094080a7e923fb776eaf9` |
| `TimerStartConformance` | 21.34 s | 1,461,540 KiB | 1,179,779,072 bytes | `10094b79a4735fe63b7a89b7abceaa3f583ef068a4c461d786191661f5e44d03` |
| `TerminateEndEventConformance` | 33.17 s | 1,721,768 KiB | 1,474,985,984 bytes | `fbadcb88b55df702c03739c911ae962c3f0a94f0191b5b8a4309215fc85127bb` |
| `InternalRegionalPrivateTimerAliasConformance` | 18.21 s | 2,743,196 KiB | 2,527,932,416 bytes | `3be4fc010335f59e94125b94eb4c02d528d0f63ef72c55cce9e365e1fb42edf5` |
| `SubProcessErrorPropagationConformance` | 40.77 s | 2,367,304 KiB | 2,281,492,480 bytes | `d95bb480b2b6181081ef9241941214f0abe5de2b0d555a102f2431264e0430cf` |

After those checks, complete warm `lake test` and `lake build` pass in 1.08 and 0.65 seconds at 54,063,104 and 52,277,248 cgroup bytes. Their output digests are `e4db54a92f50b40579602b990e1d7cee300bf329aba8e0cba8201fd3b366989f` and `3d87b3ff498c3b6a4efc1404e05b599b4ac3acd2b18739368eb0f0b354514145`. The ten-declaration axiom audit passes in 0.96 seconds at 359,002,112 bytes, output `815637e7cd3fbf929ded66508cd46607f1ce9fe0d6a532db27c64f0afc30f2ec`; it reports only `propext`, `Classical.choice`, and `Quot.sound` for shared/composed preservation, preparation refinement, validity, pair publication, finite publication, and the standalone universal witness. All three warm receipts have zero controlled events, pressure totals, and swap. This establishes isolated cold-consumer plus complete warm acceptance under the existing protocol, not a successful cold whole-graph memory bound.

## Timer-task arming, 2026-09-22

| Increment | Baseline..endpoint | Nonblank code | Nonblank documentation | Elapsed | Comparison and consequence |
|---|---|---:|---:|---|---|
| [Timer-task arming](INTERNAL-COMMUTATION-PROPOSAL.md#timer-task-arming-outcome) | `bbaeb866..e8c1aa72` | `+4902/-110` | `+51/-12` | Precommit receipt span 13703.313 s; interval union 3946.923 s; separate clean integration 2363.728 s | Standalone Activity-data arming measured `+1130/-687` code and `+42/-17` documentation. Both group complete outcomes, but Timer-task arming adds the joint handler/Activity proof family and a Process-owner correction. The scopes differ, so this is no productivity or performance comparison. |

One cold review, no required correction audit, and one clean full integration run accepted the outcome. The frozen 166 complete root precommit receipts in `/tmp/bpmn-timer-task-precommit-receipts` retain their original paths in `sources.json`; 73 have nonzero status. The summary uses command/status modification times and excludes independent reviewer commands, integration, acceptance checks, and the next-family probes. It measures command intervals, not engineering effort or critical-path delay. The bounded complete-Lean failures below remain separate rejected evidence rather than successful full runs. The enum advisory is corrected with acceptance; implementation cost excludes that follow-up.

Execution reflection: the clean integration umbrella selected all five gate families from 486 unpushed paths. Re-evaluating the existing `selectedPrePushGates` function on `bbaeb866..e8c1aa72` selects only the complete Product 1 gate for its 52 changed paths. Invoking the umbrella at this checkpoint therefore repeated unrelated platform, PostgreSQL, showcase, and browser work. For the next comparable checkpoint, use the existing complete Product 1 entry point after checking the increment's selection; keep the full umbrella before a push. This corrects command selection without changing any gate, selector, or evidence requirement. The pipeline's 52.670 seconds exceed its 36-second soft target; no speed improvement is claimed. Regional trial closure remains pending.

The [Timer-task outcome](INTERNAL-COMMUTATION-PROPOSAL.md#timer-task-arming-outcome) groups both interrupting and non-interrupting arming, all existing mixed-family pair laws, successor validity, and canonical finite-batch publication into one review target. It reuses the existing TypeScript Activity preparation and activation, the regional atom domain, ordered insertion/filter laws, and finite executor. Its shared frame extractions preserve the existing consumers' theorem statements. The selected owner's Process identity corrects the two Lean activation paths without widening source/profile or Temporal admission.

The new `InternalTimerTaskBatchConformance` target passes a cold-target build with warm dependencies in **44.45 seconds**, at **2,007,128 KiB GNU RSS** and **1,944,612,864 cgroup bytes**. Every controlled memory event, pressure total, and swap count is zero. The run retains Lean 4.31.0, the pinned `bpmn-lean-audit:v4.31.0-arm64` image, one CPU/thread, no network, read-only source, the fixed 3,221,225,472-byte controller, and zero additional swap. Its asserted receipt is `/tmp/bpmn-timer-task-fixture-cold-4`; [the executable cost row](../scripts/lean-module-cost.ts) binds the mounted source and complete command receipt. At that measurement, all 618 inputs in `.cache/timer-task-arming-20260922/source-v3-manifest.json` matched the worktree; manifest SHA-256 is `d6abc5c3075c0632f5ac921de950dbfb9072b738552ba97820aa421cda3b8df5`. This is an isolated target measurement, not full-library resource acceptance or a performance comparison.

The retained failures distinguish infrastructure setup from fixture defects. The first container could not create its cache mountpoint on read-only source; the next build compiled the changed dependency closure and exposed record-layout syntax and an ambiguous sorter name. The following attempt rejected the fixture's 2,000 ms standalone Timer because the existing structural account admits 1,000 ms. Diagnostic evaluation then confirmed the corrected Program, runtime state, projection, three prepared operations, and committed command before the kernel retry. No production validator or memory limit was relaxed.

The first complete Lean run, `/tmp/bpmn-timer-task-full-lean-1`, stopped at the unchanged 1,800-second deadline after reporting target 570 of 590. It exited 124 in 1,800.06 seconds at 2,743,192 KiB GNU RSS, reached the exact 3,221,225,472-byte cgroup ceiling, and recorded `max=1034`. OOM counters and swaps were zero; both pressure totals were 886,443 microseconds. This is failed verification and rejected resource evidence. Successful cached products remain available to finish the unchanged source; the [existing acceptance protocol](#snapshot-invariant-and-cgroup-acceptance-correction) still requires isolated cold-consumer measurements and complete accepted gates without raising the limit or deadline.

The cache-preserving continuation `/tmp/bpmn-timer-task-full-lean-2` found one omitted exhaustive match arm in the existing `FiniteInternalTransitionConformance` family-label fixture, not a failed expected semantic result. It exited 1 in 14.23 seconds at 1,154,908,160 cgroup bytes with zero controlled events. Adding the Timer-task label keeps every fixture expectation unchanged. Its isolated cold rebuild passes in 12.86 seconds at 1,376,408 KiB RSS and 1,098,285,056 cgroup bytes, with zero events, pressure, and swaps. The corrected `source-v4` snapshot differs only in that fixture; its 618-input manifest SHA-256 is `bdf9de23dc00af2f1a88de43f9cf74dd34adebcd3e0e6c24a7e179ca7f54bc1d`.

The nine final cold-consumer measurements below use that snapshot and the unchanged isolated-target protocol. Every controlled memory event and swap count is zero. Cancellation projection records 327,285 microseconds in each pressure total; private-Timer aliasing records 8,283; the other seven record zero. Cancellation projection crosses the existing 90% GNU-RSS disclosure threshold and is recorded in `nearCapModules`; its cgroup peak leaves 256,876,544 bytes below the hard ceiling. Mounted-source and complete command-receipt digests bind every updated [cost row](../scripts/lean-module-cost.ts).

| Exact target | Elapsed | GNU maximum RSS | Cgroup peak | Output SHA-256 |
|---|---:|---:|---:|---|
| `FiniteInternalTransitionConformance` | 12.86 s | 1,376,408 KiB | 1,098,285,056 bytes | `fa6eea6e064593c167b6a33e1a999956972980c4aac5ed90ff4e32aee1025a19` |
| `CallActivityConformance` | 31.25 s | 1,398,692 KiB | 1,149,374,464 bytes | `005b8fb9e749ee7af3624179b91f3ff49e3909aba3e92a28e85757a7a6ac052b` |
| `SequentialMultiInstanceProgramBindingConformance` | 28.10 s | 1,595,216 KiB | 1,261,617,152 bytes | `9d44b32e4d972a54a493309e86409a1fdfd81da0b7367111adb1131be1bee48f` |
| `TimerStartConformance` | 29.87 s | 1,438,340 KiB | 1,162,399,744 bytes | `c21700dd70cf5dff49817b164f2eedca75b315d7990bf765ebee037b7885a823` |
| `TerminateEndEventConformance` | 40.78 s | 1,722,640 KiB | 1,458,466,816 bytes | `0d1f490518b507d29b9344807762b1ee65b491589b92054f457384b23ed67807` |
| `InternalRegionalCancellationProjectionConformance` | 111.86 s | 2,835,716 KiB | 2,964,348,928 bytes | `4f22094f18a033de81f35b4b0d85d14dc705177d7378c4bf647a8052b925c6ef` |
| `InternalRegionalPrivateTimerAliasConformance` | 16.97 s | 2,735,872 KiB | 2,562,113,536 bytes | `54eeda38c91fd8552b5fca6b2e2d67f9ec2cbe64eed2cd5af630d4b774f15df5` |
| `ActivityBoundaryTimerConformance` | 20.59 s | 1,250,204 KiB | 935,514,112 bytes | `0e4d4255db529aa66b33b5223226b30f11b3511c520ee9108403f8905f425a0c` |
| `NonInterruptingBoundaryTimerConformance` | 18.65 s | 1,292,472 KiB | 978,010,112 bytes | `a0ffb305358a9b43f613a06687affc687c621da3a8691a31762e815a6baf64c8` |

Complete warm `lake test` and `lake build` pass all 590 and 591 targets in 2.15 and 0.68 seconds, at 475,598,848 and 53,571,584 cgroup bytes. Their output SHA-256 digests are `3d18e16035999f587201fdf707b0680ce7a0b54b0f54a6f0232618df4fbb3e5a` and `88cb29cbe4a711a4a0b76963e0b9968fefe1b474ac26bd45b935bb92572a91d3`. The ten-declaration axiom audit passes in 1.77 seconds at 383,098,880 bytes, output `0b7cd32540a46824307d45e601e236efdc5d97dea31ec0020398b9780ea2056f`; it reports only `propext`, `Classical.choice`, and `Quot.sound` for owner identity, preparation refinement, runtime/open-set preservation, actual publication, regional pair publication, and finite permutation guarantees. All three complete receipts have zero controlled memory events, pressure totals, and swaps. This establishes isolated cold-consumer plus complete warm acceptance; the earlier full-run timeout and memory rejection remain visible.

The complete TypeScript semantic-core gate passes **1,245 tests**, including both Timer-task variants in the existing regional pair and scope/local-control permutation matrices. Every tested prefix retains complete preparation, runtime/projection validity, exact final state, and actual publication. The called-Process identity regression now checks a structurally valid graph and valid predecessor/successor; it needed distinct deadline routing, one merged called End, and canonical scope ordering. Failed intermediate fixture receipts remain retained. The initial infrastructure run passed 580 checks and failed two map-word-budget checks; shortening only the edited summary and linking its existing owner fixed them. The next run caught a test-only loss of the nonempty merge-input tuple type after sorting; an explicit two-element tuple annotation fixes that without changing the tested operation. The ten focused Timer-task tests and final complete infrastructure gate pass, with all 582 infrastructure assertions green in `/tmp/bpmn-timer-task-infrastructure-3`.

Reflection: the finite executor and publication theorem required no new induction or publication format. Complete mixed-family evidence stays in this one outcome rather than becoming separate helper checkpoints. The 22 new Timer-task proof modules contain 4,092 nonblank lines at this measurement; that is a size observation, not an efficiency or quality score. The substantial regional preparation cost remains visible, and no unmeasured abstraction savings are claimed. The bounded fixture and TypeScript matrix errors show that fixture admission must be checked explicitly before their execution results are used as semantic evidence. Extending the prepared-transition sum also requires the existing finite-frontier classification fixture in the focused compile set: its exhaustive match caught the omitted constructor only at the end of the full run. This applies the existing complete-consumer rule without adding a process guard or weakening the full gate.

## Bounded Sub-Process Activity identity correction, 2026-09-22

The owner-approved [identity correction](INTERNAL-COMMUTATION-PROPOSAL.md#bounded-sub-process-arming-outcome) uses the Sub-Process BPMN element for its Activity record and counter while retaining the child definition-scope identity. The new fixture retains the two valid Program/predecessor counterexamples, and three existing fixtures preserve canonical issuance, duplicate-body refusal, and exact snapshot lifecycle state. This is resource evidence for the in-flight outcome, not a closed checkpoint or a complete integration verdict.

Measurements use the immutable `.cache/bounded-scope-identity-20260922/source-v3` snapshot, the existing pinned ARM64 image, one CPU/thread, no network, 3,221,225,472 cgroup bytes, and no additional swap. All 624 archived Lean/toolchain input files were compared byte-for-byte with the worktree. Each retained row below has warm dependencies and a cold target. Mounted-source hashes and the complete receipt-byte hashes bind the [module-cost rows](../scripts/lean-module-cost.ts); historical provenance is retained. All memory-event counters, pressure totals, and swap counts are zero, and none of these four modules reaches the existing RSS near-cap threshold.

| Target | Receipt under `/tmp/` | Elapsed | GNU RSS KiB | Cgroup bytes |
|---|---|---:|---:|---:|
| `BoundedScopeActivityIdentityConformance` | `bpmn-bounded-identity-cold-2` | 12.63 s | 1,992,220 | 1,724,231,680 |
| `ActivityActivationOrderConformance` | `bpmn-bounded-activity-order-cold-1` | 0.65 s | 608,860 | 182,452,224 |
| `ActivityBodyClaimUniquenessConformance` | `bpmn-bounded-body-claims-cold-2` | 7.79 s | 1,464,712 | 1,137,766,400 |
| `CompensationEventSubProcessSnapshotRootClosureConformance` | `bpmn-bounded-snapshot-root-cold-2` | 20.63 s | 1,565,576 | 1,281,593,344 |

The first container attempts recorded sandbox socket denial and a missing nested cache mount point before any Lean execution. The first executing identity-fixture run rebuilt dependencies and then failed kernel reduction of the fixture's `List.mergeSort`; replacing it with the existing kernel-reducible `SemanticProcess.sortBy` produced the retained green run without changing production semantics. Body-claim and snapshot first runs included stale dependencies, so they are functional evidence rather than isolated target costs; their dependency-warm repeats supply the table. No memory ceiling or evaluator-trust exception changed. The corrected arming soundness and issuance-law axiom audit reports only `propext`, `Classical.choice`, and `Quot.sound` in `/tmp/bpmn-bounded-identity-ambiguity-1`.

The separately approved declaration-ambiguity check extends the identity fixture with ordinary and Sequential Multi-Instance User Task refusals on valid raw Programs and predecessors. Its current isolated measurement supersedes only the first table row: `/tmp/bpmn-bounded-ambiguity-kernel-final` builds the cold target from immutable `source-v4` with warm dependencies in 30.97 s at 1,991,084 KiB RSS and 1,793,449,984 cgroup bytes. The same 3 GiB, one-thread protocol retains zero memory events, pressure, and swap; `/tmp/bpmn-bounded-ambiguity-kernel-1` is the preceding successful dependency-building run. The source and receipt digests in the module-cost row bind the expanded fixture. Both core negative witnesses fail before the check in `/tmp/bpmn-bounded-ambiguity-red-1`, the focused gate passes all 12 cases after it, and the complete core package passes all 1,264 tests in `/tmp/bpmn-bounded-ambiguity-package-1`. These finite results do not themselves prove aggregate preservation or close the bounded outcome; the proposal owns the separate quantified proof and remaining obligations.

The owner-directed checkpoint on 2026-09-23 stops further family-pair expansion for an external architecture assessment. It preserves the corrections and completed proof work but does not close the bounded outcome or authorize the TypeScript-only batch integration as a cross-target result. The [bounded outcome owner](INTERNAL-COMMUTATION-PROPOSAL.md#bounded-sub-process-arming-outcome) records the remaining obligations and the explicit unfinished-commit exception. No closed-outcome cost row or independent semantic approval is claimed. The initial checkpoint infrastructure run caught oversized PLAN and runtime-map status passages; focused document checks corrected both before the final gate, with failed evidence retained.

The first complete Lean checkpoint run, `/tmp/bpmn-bounded-checkpoint-full-lean-1`, timed out after 1,800.05 seconds with 481 of 608 targets completed. Its GNU maximum RSS was 2,741,848 KiB; cgroup peak reached the fixed 3,221,225,472-byte ceiling, with 473 `max` events and 832,694 microseconds of both `some` and `full` pressure. OOM, OOM-kill, and swap counts were zero. This is a failed full-gate attempt, not zero-pressure resource evidence. The unchanged-source continuation retains its completed cache and the same controller, timeout, and thread/CPU limits; it does not establish cold-package performance.

The continuation `/tmp/bpmn-bounded-checkpoint-full-lean-2` passes the complete 608-target Lean gate in 396.98 seconds at 2,712,784 KiB GNU RSS and 2,457,411,584 cgroup bytes. Every memory-event counter and swap count is zero; `some` and `full` pressure each total 30,923 microseconds, so this is not a zero-pressure isolated calibration. All 636 source/toolchain inputs match the immutable `source-v1` snapshot and worktree; its manifest SHA-256 is `583bb0d19e0918c5ea410a847633784697f6f6a1ebe39c178ca5543fb99c1a4b`. The complete 1,264-test core gate passes in `/tmp/bpmn-bounded-checkpoint-core-1`, and doctor verification passes in `/tmp/bpmn-bounded-checkpoint-doctor`. Final infrastructure receipt: `/tmp/bpmn-bounded-checkpoint-infrastructure-2`. These are pre-commit package checks, not a clean Product 1 integration or semantic acceptance verdict.

## Ordinary snapshot arming, 2026-09-23

The [Compensation registration prerequisite](capsules/COMPENSATION-TRIGGER-HANDLER-PROPOSAL.md#public-registration-prerequisites) is one complete Start-to-publication outcome, based at `990d2edd406d2d50c9c05659e45e84ebc3b16434`. Its independently approved implementation endpoint is `3644020e9a5b3cb630a1090c951421bbf89fe4a2`. The retained red receipt `/tmp/bpmn-snapshot-arming-red.Shwc0n` admits the reviewed source, reaches the two ordinary Task operations, and then rolls back as ambiguous with no publication. The correction retains complete preparations in the focused snapshot-aware runner and derives actual-attempt, full-state, projectability, finite-permutation, refusal-frame, and accepted-publication guarantees from predecessor facts. The generic snapshot exclusion is unchanged.

The complete semantic package passes all Lean targets and 1,264 core tests in `/tmp/bpmn-snapshot-semantic-complete`. The new kernel fixture covers six ordinary arming families, stale preparations, duplicate conflicts, the exact source Start, and insufficient-fuel rollback. The final isolated cold fixture `/tmp/bpmn-snapshot-fixture-calibration` passes in 27.27 seconds at 2,554,572 KiB GNU RSS and 2,306,441,216 cgroup bytes. The actual-runner proof and its refusal-frame dependency pass in `/tmp/bpmn-snapshot-runner-resource` in 9.96 seconds at 791,756 KiB RSS and 427,094,016 cgroup bytes. Both use the pinned Lean/image, one CPU/thread, no network, the fixed 3,221,225,472-byte controller, and zero additional swap; all controlled memory events, pressure totals, and swap counts are zero. The fixture's mounted-source and complete receipt digests bind its [module-cost row](../scripts/lean-module-cost.ts). All 643 Lean/build inputs match the archived read-only source and worktree; `.cache/snapshot-arming-20260923/source-v1-sha256.json` has SHA-256 `ceaf319bac33713997d51635e27a6ee7f8700172540a953d5e6bbc2d2c71ebac`.

| Capsule | Range | Code | Documentation | Elapsed evidence | Comparison and assessment |
|---|---|---:|---:|---|---|
| [Ordinary snapshot arming](capsules/COMPENSATION-TRIGGER-HANDLER-PROPOSAL.md#public-registration-prerequisites) | `990d2edd..3644020e` | `+1822/-67` | `+12/-4` | Precommit receipt span 3149.752 s; interval union 510.253 s | Standalone Activity-data arming measured `+1130/-687` code and `+42/-17` documentation. This outcome adds the focused snapshot attempt/refusal/publication connection and reuses ordinary pair laws. The difference is not equivalent scope or an effort comparison. One complete outcome replaces helper-only commits; later proof work requires a selected RC admission dependency. |

The frozen precommit receipt set `/tmp/bpmn-snapshot-precommit-receipts` contains 34 completed commands through the axiom audit, including fifteen failures. Its original-path inventory preserves receipt modification times: span 3,149.752 seconds, interval union 510.253 seconds. It excludes delegated commands, unreceipted investigation, and the later infrastructure/review/integration checks. These are command intervals, not engineering effort or a performance trend. All five consumer axiom reports in `/tmp/bpmn-snapshot-axioms` contain only `propext`, `Classical.choice`, and `Quot.sound`.

Earlier receipts retain a missing Docker mount point, invalid constructed-fixture channels, Lean proof/destructuring corrections, and a malformed negative-test indentation. The first final-source resource run also rebuilt dependencies; the later cold-target calibration above is the isolated measurement. None required a changed memory limit, native decision shortcut, admitted profile, or weakened invariant.

The cold reviewer approved the immutable implementation with no findings. The requested exact-target probe `/tmp/bpmn-snapshot-review-probe` passes all actual-frontier, forged-preparation, refusal-precedence, and arbitrary-permutation obligations. Including its necessary atomicity dependency rebuild, it takes 40.56 seconds at 2,001,036 KiB RSS and 2,414,993,408 cgroup bytes under the unchanged controller; every controlled memory event, pressure total, and swap count is zero. Both theorem axiom reports use standard axioms only. Clean Product 1 integration passed in 733.630 seconds with all 72 pipeline cases agreeing and 83 histories replayed; the [review receipt](capsules/COMPENSATION-TRIGGER-HANDLER-PROPOSAL.md#independent-cold-review-receipt) binds exact outputs. Host load reached 65.22 during serial Temporal checks, and the pipeline recorded 34.36 at start and 26.05 at end on eight cores. These contended timings are not comparable performance measurements.

Reassessment: this outcome spends its proof work on an executable source failure and reuses the ordinary pair laws. The actual dispatcher requires disabled-operation frames because its canonical refusal scan is wider than the selected batch; assuming prefix refusal absence would have left the implementation connection unproved. A new aggregate invariant and further family-pair expansion are excluded from this outcome. Compared with standalone Activity-data arming, this correction crosses the already separate snapshot publication/attempt account, so the number of proof owners does not measure equivalent scope or productivity. After independent acceptance, remaining work is selected strictly by current RC admission and the subscription/Transaction capabilities still to be implemented. The regional trial remains pending.

## Snapshot pipeline correspondence, 2026-09-24

The [Compensation correspondence prerequisite](capsules/COMPENSATION-TRIGGER-HANDLER-PROPOSAL.md#public-registration-prerequisites) starts at `abab7694`. It repairs the actual scenario dispatcher, includes handler effects in observations, and reuses the accepted snapshot replay/publication account for complete command schedules. It adds no proof owner, kernel-decision fixture, representation, profile, or host mechanism. Its independently approved implementation endpoint is `55cebd6fa18cbc38f5bc1eaefc10fdfda2b0fc8a`.

The separating receipt `/tmp/bpmn-compensation-pipeline-red-3` admits the checked source but returns a Lean harness failure at Start while TypeScript completes. The earlier two receipts retain malformed private provenance inputs and are harness setup failures, not semantic reds. A second consumer failure reproduces in `/tmp/bpmn-compensation-baseline-publication`: the baseline publication owner, interpreted under a separate diagnostic namespace, returns no publication for the focused runner's committed five-transition Start. The publication comparison also caught a diagnostic distinction: no committed trace is not a replayable empty committed command; rejection separately preserves the exact received state. The final focused receipt `/tmp/bpmn-compensation-pipeline-final-focused` passes all 36 complete source schedules and the ordinary-emitter control. The complete semantic receipt `/tmp/bpmn-compensation-pipeline-semantic` passes the Lean library and 1,264 core tests; the differential and infrastructure receipts pass 128 and 582 tests respectively. Clean integration evidence belongs to the acceptance receipt.

The nearest outcome is ordinary snapshot arming (`990d2edd..3644020e`, `+1822/-67` code), which established the reusable preparation/attempt proof connection. This increment consumes that account at the scenario and JSON boundaries instead of expanding pair proofs. Its finite comparisons preserve the distinction between private checkpoint evidence and public registration, including the unresolved standards-only provenance schema. No speed or effort comparison is inferred from different scopes or contended command timings.

| Increment | Boundary | Code | Documentation | Elapsed | Comparison consequence |
|---|---|---:|---:|---|---|
| [Snapshot pipeline correspondence](capsules/COMPENSATION-TRIGGER-HANDLER-PROPOSAL.md#public-registration-prerequisites) | `abab7694..55cebd6f` | `+249/-26` | `+13/-5` | Clean Product 1 integration 544.552 s | Ordinary snapshot arming added 1,822 code lines and twelve documentation lines. This consumer correction reuses its evaluator and replay account without a new proof owner. Code additions fell; documentation additions rose by one. Direct inclusion in the existing verification pipeline avoids a new package-script layer and unrelated Product 2 gates. The outcomes have different scope, so churn is not a productivity comparison. |

The cold reviewer approved the immutable target without findings. All ten independent publication/observation checks pass at that target, as do the 36 complete private schedules, 72 registered pipeline cases, and 83 replayed histories. The [acceptance receipt](capsules/COMPENSATION-TRIGGER-HANDLER-PROPOSAL.md#independent-cold-review-receipt) binds clean integration and probe outputs. The full semantic gate took 1,086.109 seconds because the shared Scenario import invalidated existing Lean consumers; no new proof owner or kernel-decision fixture was introduced. That dependency rebuild and the registered pipeline's 54.615-second run are disclosed costs, not a performance trend.

Reassessment: finish the real consumer connection before expanding infrastructure. The complete schedule comparison exposed a missing handler projection and a replay-diagnostic distinction that the isolated Start witness could not detect. Remaining scheduling work must follow selected subscription/Transaction admission and reachable frontiers. Public registration still needs an honest provenance decision; its private inputs are not schema-valid registered artifacts. Existing packet generation, package gates, and direct pipeline execution supplied the necessary evidence without another abstraction.

## Subscription checkpoint resource qualification, 2026-09-24

The [approved subscription checkpoint](capsules/REPEATABLE-EVENT-SUBSCRIPTIONS-SPEC.md#scheduling-proof-domain-amendment) was measured before its implementation commit, against baseline `13abab7dc8e8bdb93ff28bb1e2758b8510a381ed`; semantic review later accepted correction `2c0f74e8` and clean integration `b7bef016`. The four new kernel fixtures cover bounded admission, repeated Message delivery, recurring Timer replacement and overflow rollback, and exact retained-versus-removed scope cleanup. Eight changed consumers are requalified separately. This is resource evidence, not semantic acceptance or a completed capsule cost range.

The archived source and input hashes are `/tmp/bpmn-subscription-checkpoint-resource-final/source` and `source-hashes.json`; each isolated target starts cold against warm dependencies. All runs use the pinned `bpmn-lean-audit:v4.31.0-arm64` image (`sha256:4df22c7a1ec8ba8c4c2795fdea1269d7b9cadade4e65b22e595047509aa27950`), one CPU/thread, no network, a read-only source mount, the unchanged 3,221,225,472-byte memory controller, and no additional swap. Source and complete receipt digests are retained in the [module-cost registry](../scripts/lean-module-cost.ts).

| Target | Receipt under `/tmp/` | Elapsed | GNU RSS KiB | Cgroup bytes |
|---|---|---:|---:|---:|
| `RepeatableEventSubscriptionAdmissionConformance` | `bpmn-subscription-final-admission-cold-3` | 9.57 s | 1,377,844 | 1,033,465,856 |
| `RepeatableMessageLifetimeConformance` | `bpmn-subscription-final-message-lifetime-cold` | 5.14 s | 1,229,212 | 889,335,808 |
| `RecurringTimerLifetimeConformance` | `bpmn-subscription-final-RecurringTimerLifetimeConformance-cold-2` | 9.83 s | 1,327,064 | 1,000,923,136 |
| `SubscribedScopeCancellationConformance` | `bpmn-subscription-final-SubscribedScopeCancellationConformance-cold-2` | 0.43 s | 587,232 | 150,982,656 |
| `ActivityBoundaryTimerConformance` | `bpmn-subscription-final-ActivityBoundaryTimerConformance-cold` | 17.51 s | 1,280,148 | 960,266,240 |
| `SubProcessBoundaryTimerConformance` | `bpmn-subscription-final-SubProcessBoundaryTimerConformance-cold` | 28.54 s | 1,514,392 | 1,260,630,016 |
| `NonInterruptingBoundaryTimerConformance` | `bpmn-subscription-final-NonInterruptingBoundaryTimerConformance-cold` | 17.76 s | 1,319,948 | 1,018,011,648 |
| `InternalRegionalCancellationProjectionConformance` | `bpmn-subscription-final-InternalRegionalCancellationProjectionConformance-cold-final` | 108.60 s | 2,818,852 | 3,156,688,896 |
| `ScopeCancellationConformance` | `bpmn-subscription-final-ScopeCancellationConformance-cold` | 1.35 s | 614,460 | 384,294,912 |
| `UserTaskMetadataConformance` | `bpmn-subscription-final-UserTaskMetadataConformance-cold-final` | 16.77 s | 1,290,752 | 1,299,922,944 |
| `SequentialMultiInstanceProgramBindingConformance` | `bpmn-subscription-consumer-sequential-cold-final` | 51.50 s | 1,609,256 | 1,390,055,424 |
| `FiniteInternalTransitionConformance` | `bpmn-subscription-consumer-finite-cold` | 16.20 s | 1,425,944 | 1,192,255,488 |

All controlled memory-event counters and swap counts are zero. The regional cancellation fixture records 170,839 microseconds in both pressure totals and reaches 3,156,688,896 cgroup bytes, about 98% of the fixed ceiling. Its headroom is tight; no zero-pressure or resource-improvement claim is made. The other eleven targets record zero pressure totals. The registry's empty near-cap list uses its existing GNU-RSS threshold and does not erase the separate cgroup observation.

The regional fixture initially failed because its Terminate expectation still cancelled the selected body's parent-owned handlers. Read-only evaluation confirmed valid before/after states and exact actual publication for both Message and Timer variants, while Error-removal controls retained their original cancellation behavior. The correction implements the approved `ESL-RETAIN-01` expectation without changing production semantics or weakening projection checks. Earlier receipts also retain a fixture-construction syntax failure and dependency-building runs; those are not the isolated costs above. The complete native Lean gate `/tmp/bpmn-subscription-checkpoint-full-lean` ended with six consumer failures: four admission destructurings, the corresponding quantified cancellation classifier, and a fixture classifier missing the two new prepared variants. The four pure proof-consumer repairs pass `/tmp/bpmn-subscription-full-consumer-repairs-focused` (129 jobs); the Sequential Multi-Instance and finite-batch fixture repairs receive the final two cold measurements above. Their 438-job dependency warm-up in `/tmp/bpmn-subscription-consumer-resource-warm` is excluded from isolated target times.

The two final fixtures use the separate immutable `/tmp/bpmn-subscription-consumer-resource/source` snapshot. Its 679-input manifest SHA-256 is `4663c62507242c00e9c0504ad1fbcb2f5ac1f9c4a1e139241cb009f07222baf2`. Compared with the first snapshot, only the six named consumer repairs differ; the first ten fixtures' complete transitive import closures remain byte-identical, checked in `/tmp/bpmn-subscription-checkpoint-resource-final/fixture-closure-audit.json`. The resource evidence therefore remains bound without pretending that the two complete snapshots are identical.  The complete 651-job native Lean gate passes in `/tmp/bpmn-subscription-checkpoint-full-lean-corrected`. The final infrastructure run then catches one plain `decide` in the Sequential Multi-Instance repair; the required `decide +kernel` correction passes its focused hygiene guard and the refreshed cold target above. The previous 50.85-second measurement remains historical. The corrected source is archived in `source-v2`, with manifest SHA-256 `899629414fd62fa4beacee20b85e192808cb0b3ede6f685e866850f05eac2e38`; only that fixture differs from `source`. The final 651-job native Lean gate passes in `/tmp/bpmn-subscription-checkpoint-full-lean-final`; the fresh 27-schedule Lean/core pipeline and eleven-theorem axiom audit also pass. Complete infrastructure verification passes all 582 checks in `/tmp/bpmn-subscription-checkpoint-infrastructure-ready`; independent checkpoint review was still open at that measurement boundary. The first full registered pipeline `/tmp/bpmn-subscription-checkpoint-registered-pipeline-ready` times out at the unchanged 103,600 ms outer limit while a separate workspace runs a parallel Lean build; CIB batches report 52.6–68.6 seconds against their 30-second soft target. This is incomplete regression evidence, not a semantic failure or a passing timing result. After that build finishes, `/tmp/bpmn-subscription-checkpoint-registered-pipeline-retry` reuses the completed artifacts and passes all 72 registered cases in 61.41 seconds with the same timeout. Host load remains above the comparison threshold, so this establishes regression correctness without a comparable performance claim.

## Subscription hosting outcome, 2026-09-24

The coherent outcome after `b7bef016` is the [approved subscription hosting contract](capsules/REPEATABLE-EVENT-SUBSCRIPTIONS-SPEC.md#temporal-hosting-implementation-checkpoint): complete-profile admission, activation ordering, native Timer handoff, physical deadline continuation, failure classification and accepted-input settlement. It reuses the existing activation barrier, durable Timer owner, command recovery and publication/capacity boundaries. No Lean source or measured kernel fixture changes in this outcome; previous resource qualifications are not presented as new measurements. Registration and closure remain separate unfinished work.

Separating failures preceded implementation: host admission rejected the entire new profile; direct-VM replay without the production patch marker exercised legacy enrollment; and the first live SDK-flag oracle incorrectly required the first started task after Signal ingress to complete. Actual history contained an explicitly failed `UNHANDLED_COMMAND` task followed by successful handling, so the oracle now requires each task's disposition and checks the first successful activation. A missing-disposition mutation rejects. These fixture/oracle corrections preserve the selected production schedule and do not weaken replay or flag requirements.

The expanded live witness failed once as a native process, without a JavaScript assertion: `/tmp/bpmn-subscription-live-retried-task-flags`. The matching macOS diagnostic `node-2026-09-24-135717.ips`, captured at 13:57:16 +0200, records `EXC_BAD_ACCESS`/`SIGSEGV` at address `0xe`, with `v8::internal::ClearStaleLeftTrimmedPointerVisitor::VisitRootPointers` above the stack/root iteration and mark-compact frames. The unchanged-source, unchanged-runtime rerun `/tmp/bpmn-subscription-live-replay-process-diagnostic` passes in 24.544 seconds, including replay of all 17 Runs. This diagnoses a native runtime failure and establishes successful rerun evidence; it claims neither a runtime fix nor reliability improvement.

The focused production direct-VM receipt `/tmp/bpmn-subscription-host-locus-matrix` passes eight tests in 6.50 seconds. Complete semantic-core and client receipts `/tmp/bpmn-subscription-host-core-package` and `/tmp/bpmn-subscription-host-client-complete` pass 1,283 and 177 tests; the Temporal workspace build and harness types also pass. The first complete Temporal gate exposed four obsolete whole-profile fence expectations after precise admission was enabled; the seven corrected protocol checks pass. The corrected complete Temporal receipt `/tmp/bpmn-subscription-host-temporal-complete-corrected` passes 392 component, 343 concurrent, 18 hosted and 26 serial tests. The first infrastructure run found undeclared compiler imports in Workflow tests, two test imports tied to build-output declarations, and sandbox-denied loopback binding. A test-only workspace dependency and source-typed/package-entry imports correct the two implementation defects; the complete gate reruns with local networking. Pnpm refreshed only that workspace lockfile entry; its registry policy checks required network access even under the attempted offline lockfile-only command. The corrected complete receipt `/tmp/bpmn-subscription-host-infrastructure-corrected` passes all 584 checks and harness types in 58.40 seconds of test execution. Timing is correctness evidence, not a controlled performance comparison.

The corrected import-focused run `/tmp/bpmn-subscription-host-test-imports-corrected` passes the failure/scheduler and dependency/source-policy checks but loses its direct-VM process during the final locus matrix. Diagnostic `node-2026-09-24-141358.ips`, captured at 14:13:57 +0200, records the same address `0xe`, `SIGSEGV` and V8 root-visitor/mark-compact stack as the live failure above. The unchanged direct-VM file passes all eight tests in 6.822 seconds in `/tmp/bpmn-subscription-host-direct-vm-import-diagnostic`. Both failed processes remain unresolved native-runtime evidence; no retry loop or runtime-setting change masks them.

Reassessment: one host outcome groups the binding, scheduler, private diagnostic and live recovery evidence, avoiding helper-only commits. Extending the existing direct-VM harness with replay patch notifications exposed the real production branch without duplicating hosting. Existing admission and failure oracles caught stale assumptions early. The next useful work is ordered multi-Timer pipeline support and registered business-model evidence, followed by closure; further proof infrastructure and exhaustive family-pair expansion remain unselected.

## Subscription registration outcome, 2026-09-24

The outcome after hosting commit `61353901` is the [registered subscription pipeline and corpus](capsules/REPEATABLE-EVENT-SUBSCRIPTIONS-SPEC.md#registered-pipeline-and-corpus). It extends the existing ordered scenario runner, preserves legacy single-Timer evidence, and registers fourteen neutral schedules over eight application-review sources. Four capability rows distinguish repeated Messages, recurring User Task/Sub-Process Timers, and one-shot monitored Sub-Process Timers. The retained models share the existing `subprocess-termination` clone family and gain no browser-catalog eligibility. No Lean source, semantic account, proof contract or measured kernel fixture changes in this outcome.

Red evidence `/tmp/bpmn-subscription-runner-preflight-red` reproduces the second-firing refusal at both actual admitted Activity loci; `/tmp/bpmn-subscription-runner-unit-red` retains the runner and missing history-oracle failures. `/tmp/bpmn-subscription-capability-red` separates recurring from one-shot coverage and exposes the missing repeated-Message classification. The green runner, capability and exact catalog-mutation receipts pass before the complete pipeline. `/tmp/bpmn-subscription-registration-pipeline` passes all 86 registered cases in 80.32 seconds; its completion load is 2.80 per core, so the result is correctness evidence without a comparable performance claim. `/tmp/bpmn-subscription-registration-contracts` passes 109 checks, `/tmp/bpmn-subscription-registration-differential` passes 144, and `/tmp/bpmn-subscription-registration-about` verifies canonical About rendering. Aggregate pipeline/hosted deadlines change only by the existing per-case and 50% hosted-margin formulas as the catalog grows; per-case limits remain unchanged.

Avoidable witness corrections remain visible: `/tmp/bpmn-subscription-runner-live` used the wrong server cache and hit the existing 40-second startup deadline; the next two receipts exposed a nonexistent runtime status field and an omitted scenario observation contract. Reusing the existing CLI cache owner, checking `control.kind`, and supplying the required observation requests produce `/tmp/bpmn-subscription-runner-live-observations-corrected`, green in 7.55 seconds with zero/two native firings at both loci and four replays. The first catalog pass also exposed a fixed 72-case unit assertion coupled to the live aggregate budget; retaining 72 as a fixed input with its exact 93,600 ms expected result removes that stale coupling while the separate catalog guard still enforces the current total.

The initial status and infrastructure receipts retain an overlong resume paragraph, an outdated runner headroom measurement and a shortened delegated section. `/tmp/bpmn-subscription-registration-status-guards-corrected` passes all 48 focused checks after correction. The infrastructure gate was launched before those known documentation failures were corrected, so that duplicate failure run was avoidable. The corrected complete infrastructure receipt passes all 585 checks, and the complete Temporal receipt passes 392 component, 347 concurrent, 19 hosted and 26 serial tests. The next execution improvement is concrete: run the narrow fixture type/contract preflight before starting its service, and settle known focused failures before opening the complete gate. No new process framework or weaker verification is introduced.

The complete corpus receipt `/tmp/bpmn-subscription-registration-corpus-complete` passes all 17 static, retained-execution and population-refinement checks in 76.95 seconds. The web consumer receipt `/tmp/bpmn-subscription-registration-web-complete` passes five UI-kit and 147 web checks. The corpus gate ran with unchanged repository inputs and reused the already built production graph; no redundant Lean or TypeScript build was started. The earlier registered-pipeline pass overlapped documentation/catalog test-script edits and therefore is not clean immutable acceptance; the committed integration gate remains required.


## Subscription closure cost, 2026-09-24

The contiguous implementation interval begins at the retained semantic baseline `13abab7dc8e8bdb93ff28bb1e2758b8510a381ed` and ends at closure correction `b7eefa969`. It includes the pending-source ownership guard and clock-sensitive witness serialization corrections discovered during implementation; neither is subtracted. The interval also includes cost binding, public catalog/example registration and the repeated-Message driver correction; only final receipt recording and graduation are outside it. Elapsed engineering time was not retained and remains unknown.

| Increment | Boundary | Code | Documentation | Elapsed | Comparison consequence |
|---|---|---:|---:|---|---|
| [Repeatable subscription lifetime](capsules/REPEATABLE-EVENT-SUBSCRIPTIONS-SPEC.md) | `13abab7d..b7eefa969` | `+15317/-1900` | `+280/-134` | Unknown | The nearest Message-boundary account changes `+7669/-756` code and `+112/-72` documentation; the non-interrupting Timer account changes `+3578/-713` and `+313/-20`. Subscription code additions exceed both; documentation exceeds Message-boundary and falls below non-interrupting Timer. This interval adds reusable lifetime at multiple loci, retained-body correction, bounded quantified scheduling integration, ordered hosting and deadline continuation. The process weight removed is repeated family-specific runner/build work: all fourteen registrations reuse ordered stimulus delivery, one native-Timer accounting owner and the existing built corpus graph. No proof, independent evidence, capacity or review guarantee is removed. Different scopes and contended measurements establish no speedup. |

The [registration reflection](#subscription-registration-outcome-2026-09-24) identifies avoidable reruns and their concrete correction. Closure uses the already accepted semantic checkpoint and the complete downstream evidence together; it does not reopen byte-identical proof families merely to repeat a green gate. Clean immutable integration and the same-reviewer closure audit are accepted; RC dependency reconciliation against Compensation/Transaction admission remains.

The first closure review at `9caca887` requires completing public catalog/example registration and reconciling invariant/requirement owners. Exact-target integration fails the existing source-profile artifact guard before Temporal verification: the new artifact exists while its identity remains outside `SemanticProfileId`. `/tmp/bpmn-subscription-catalog-correction-focused` passes the corrected catalog, independent notation negatives, exhaustive product pre-start examples and requirement checks. Registering the profile also makes the older regional-exclusion guard classify this profile under its separately reviewed subscription frontier account, rather than silently applying the old exact-shape argument.

The runnable example exposes a product command-identity defect: two declared Messages to one persistent subscription reuse the occurrence-only command ID and the second is recovered as a retry. `/tmp/bpmn-subscription-driver-command-identity-red` separates the plain and payload driver cases; the corrected response-position binding preserves published occurrence identities and deterministic retries, with all 21 focused driver checks green. The example uses sequential handlers because the driver deliberately refuses ambiguous same-element occurrences. The asserted `/tmp/bpmn-subscription-correction-product-live` receipt passes actual second delivery with the existing live mechanism suite. This correction changes no engine subscription rule, public occurrence construction, proof claim or host scheduling policy.

The exact-target integration at `b7eefa969` first failed in the subscription serial witness with a native V8 crash: `/tmp/bpmn-subscription-closure-correction-clean-integration`, exit 1. The macOS report `node-2026-09-24-154007.ips` binds PID 75677 at 15:40:01.3428 +0200 to `EXC_BAD_ACCESS` at address `0xe`, with `ClearStaleLeftTrimmedPointerVisitor` and root marking on the faulting stack; the reviewer independently corroborated it. The unchanged rerun `/tmp/bpmn-subscription-closure-clean-integration-runtime-rerun` passes complete `test:pre-push:verify`, exit 0, clean exact target, output SHA-256 `acf92524aec275752edadc6fb5f96e709299426ddc7e63e7e13df9a24ee08c46`. All 86 registered cases agree and 97 live histories replay. The 77.488-second warm pipeline exceeds its 36-second soft target under recorded contention; this is correctness evidence, not a speedup. The reviewer approved the correction with all three findings closed. Successful rerun does not resolve the native runtime defect.

## Compensation RC admission assessment, 2026-09-24

The bounded assessment measures `0a9cb75a..2c52c622`. It changes no production or Lean code and introduces no profile. Elapsed engineering time was not retained.

| Increment | Boundary | Code | Documentation | Elapsed | Comparison consequence |
|---|---|---:|---:|---|---|
| [Compensation scheduling admission](INTERNAL-COMMUTATION-PROPOSAL.md#compensation-checkpoint-dependency-assessment) | `0a9cb75a..2c52c622` | `+154/-0` | `+17/-2` | Clean Product 1 integration 670.599 s | The preceding snapshot pipeline correspondence changed `+249/-26` code and `+13/-5` documentation. This assessment adds topology widening controls and complete fixed-model path exploration, consuming the accepted Start/publication result without another proof owner. Distinct scopes and contended gates establish no productivity or speed ratio. |

Independent review approves the exact checked-source exclusion argument with no findings. Both topology refusal controls fail under the isolated predicate-bypass mutation; the positive exploration remains green. An initial harness typecheck rejected two possibly undefined IDs; captured checked values correct that before the complete package gates. Clean integration retains all 86 registered agreements and 97 replayed histories. Its 79.631-second warm pipeline exceeds the 36-second soft target under load per core rising from 1.042 to 2.790, so this is correctness evidence only.

Reassessment: classifying reachable command frontiers resolved the current capability dependency with existing proof and hosting guarantees. Continue with public registration and its concrete multi-effect harness obligation; reopen scheduling only when Transaction or wider admission supplies a required frontier. Preserve the raw Lean/TypeScript domain mismatch and the pending regional trial. This assessment does not close either broader boundary.

## Public Compensation registration, 2026-09-24

The outcome begins after `baf51fc7` and preserves the accepted exact source, semantic account, proof contracts and dedicated scheduler. It enrolls the normative profile, six neutral scenarios, four contextual corpus capability rows, About disclosure and one runnable example together. The existing ordered runner gains real multi-effect Activity execution and terminal failed-receipt reconciliation. No Lean source or kernel fixture changes; the private 36-case comparison remains intact. The immutable endpoint is the registration review target; clean integration and independent review remain separate acceptance obligations.

| Increment | Boundary | Code | Documentation | Elapsed | Comparison consequence |
|---|---|---:|---:|---|---|
| Public Compensation registration review target | `baf51fc7..953f5d64` | `+828/-61` | `+101/-49` | Engineering time unknown | The nearest subscription registration interval `61353901..b7eefa969` measures `+588/-50` code and `+122/-54` documentation. Compensation adds more code and less documentation while introducing an ordered multi-Activity driver; subscriptions reused the ordered stimulus driver. Both reuse existing semantic and proof accounts. Different scopes establish no productivity or speed claim. |

Registration exposed stale exhaustive profile assertions in lowering, value domains, heartbeat policy, the Boolean-profile census and regional-exclusion classification. Their corrections preserve production rules and explicitly route Compensation to its own topology assessment. Source, core and differential package receipts pass 992, 1283 and 157 checks respectively; corpus/disclosure passes 91. The example passes its 14 compile/admission checks. These are working-tree package results, not clean integration evidence.

Two complete Temporal attempts failed in different VM-bearing files before hosted qualification. The macOS reports `node-2026-09-24-172506.ips` and `node-2026-09-24-173137.ips` record PIDs 81825 and 15922, `SIGSEGV` at `0xe`, and `ClearStaleLeftTrimmedPointerVisitor::VisitRootPointers` above root marking/mark-compact frames. The first subscription file passes all eight checks in isolation; the TAP diagnostic instead loses the oversized-command file to the same native signature. This matches [Node issue 62393](https://github.com/nodejs/node/issues/62393), but identifies no upstream fix. The existing `--no-parallel-scavenge` flag does not prevent these crashes; successful rerun evidence cannot establish runtime reliability.

The later complete Temporal attempt again crashes in the subscription VM file; the three selected macOS reports are retained in `/tmp/bpmn-compensation-registration-native-crash-evidence.json`, SHA-256 `72fbd5b61167364cb9e1bff00f9851e885a1ae5cc6a943a8e29fad56783f99ea`. Running the registered pipeline concurrently with hosted and infrastructure qualification was avoidable contention: Maven phases exceeded 46 seconds and the enclosing pipeline hit its unchanged 129,600 ms command deadline. The isolated pipeline then exposed a legacy single-Service-Task probe assertion on the new ordered handler path; its correction retains exact durable request/result checks for every Compensation Activity and leaves the existing single-effect counters intact.

The corrected isolated pipeline passes all 92 registered comparisons and replays 103 live histories in `/tmp/bpmn-compensation-registration-pipeline-green`. Its 103,970 ms warm run exceeds the unchanged 46,000 ms soft target but passes the 119,600 ms hard ceiling; host load rises from 1.34 to 3.91 per core, so this is correctness evidence, not a speed claim. The complete hosted lane passes 20 checks, contracts pass 109, and web qualification passes five UI-kit and 147 web checks. Status-map word-budget overshoots caused avoidable focused reruns before the infrastructure gate accepted those maps.

All three native reports reach `Builtins_BaselineOutOfLinePrologue`. Disabling Sparkplug in the two affected VM files passes all 14 checks in 35,197 ms; the complete concurrent Temporal lane then passes all 356 checks in 94,710 ms with the same flag and unchanged file concurrency of four. The separately asserted receipts are `/tmp/bpmn-compensation-v8-no-sparkplug-diagnostic` and `/tmp/bpmn-compensation-temporal-concurrent-no-sparkplug`. The concurrent test command retains `--no-sparkplug`; other lanes and production retain their existing flags. The command guard first failed on the missing flag. This avoids the observed baseline-entry path in the test lane without a runtime-version change, smaller workload, increased deadline, or claim of an upstream V8 fix.

The complete serial Temporal lane passes 26 checks in 265,449 ms, retaining the existing Compensation durability, cancellation and eleven-Run replay witnesses. The unchanged private Lean/core comparison passes all 36 result/publication cases in 11.93 seconds. Their asserted receipts are `/tmp/bpmn-compensation-registration-hosted-serial` and `/tmp/bpmn-compensation-registration-private-correspondence`; output digests are `054ace2be9afe21ffef1989f03132bbd3871f796b9251ad4eb62b0020cb4d719` and `0ad532fc5dc32260bbd09029cd05b963895291ee37236b0763a725dabb7475a1`. The corrected differential package passes 157 checks, the feedback guard passes 19, and the complete platform-policy gate passes 52. The final catalog audit also narrows an inherited blanket Event Sub-Process exclusion to preserve the newly disclosed Compensation exception.

After the research doctor verified all registered external inputs, the complete reuse-only model-corpus gate passed its 17 checks in 85.07 seconds. It executes all retained models through their claimed targets, preserves population refinement/replay, and confirms the generated map and separate browser-readiness boundary. The asserted receipt is `/tmp/bpmn-compensation-registration-model-corpus`. Qualification reuses unchanged package outputs and runs heavy hosting, corpus and pipeline work separately; no build-cache purge, broader proof lane or relaxed deadline is needed.

Avoidable harness corrections remain recorded: assuming ActivityTaskStarted event order described callback start order, and using the legacy two-argument Workflow envelope for failed terminal receipts. The corrected driver uses a live callback barrier plus durable schedule ordering and the existing production envelope. The infrastructure gate also caught a catalog test importing the direct Lean-command helper despite only compiling source; using the existing catalog-facing target exports restores the intended lane boundary. The remaining work reuses built artifacts for package qualification and leaves clean committed integration separate; it adds no proof programme or gate weakening.

Cold closure review of `953f5d64` found two required corrections: a complement-of-Running test incorrectly classified `NotStarted` as terminal in both harness consumers, and unchanged owner passages still denied registration. The root reproduced the former before replacing both consumers with one explicit Completed/Cancelled/Failed classifier. The separating test preserves both nonterminal statuses and absent state while retaining exact receipt requirements for all three terminal statuses. The owner reconciliation preserves historical checkpoint evidence, current exact registration, and Product 2/Transaction exclusions. These were avoidable pre-review omissions; future registration scans must include declaration and admission owners as well as status maps.

The reviewer-requested live witness passes all six scenarios and replays in `/tmp/bpmn-compensation-registration-review-live`, 14.047 seconds. Clean integration at `953f5d64` finishes with exit 1 in 741.97 seconds: infrastructure, core, source, differential and the 392/356/20 Temporal component/concurrent/hosted checks pass, but the serial lane passes 25 of 26. The existing parallel Multi-Instance natural witness consumes 3,931 ms of its 5,000 ms host deadline, exceeding the unchanged 50% margin. External CPU work remains active during qualification; no semantic mismatch or native crash is reported by this run. The failure receipt `/tmp/bpmn-compensation-registration-clean-integration` remains retained; neither the margin nor the deadline is relaxed, and complete qualification remains outstanding.

Correction qualification passes harness type checking, the complete build graph and all 392 component checks, then fails the concurrent lane: 352 of 357 reported checks pass in 429.160 seconds. `/tmp/bpmn-compensation-registration-correction-temporal` retains four deadline failures (Boolean mutation bundling, Call mutation Query, subscription VM initialization and Error-bypass mutation) and a distinct native `InternalCallbackScope::Close` assertion on `execution_async_id` in the managed-wait VM file. At 16:30 UTC the eight-core host reports load averages 343.542/253.249/144.977, retained in `/tmp/bpmn-compensation-correction-host-load.json`; external build/test processes were observed, but that does not establish the native assertion's cause. The run finishes naturally before an attempted task-only interrupt; no process is interrupted. Heavy qualification waits for a quiet host, and the correction stays uncommitted until the affected package and infrastructure gates pass. No timeout, semantic deadline, concurrency setting or proof guarantee is weakened.

The macOS report `node-2026-09-24-182812.ips` binds PID 46658 and parent test runner 40547 to `SIGABRT` in `InternalCallbackScope::Close`, distinct from the earlier `0xe` root-marking crashes. Original report SHA-256: `aa2efd8824d486be874e781bb7b3a8fa48c1f4bb422a249314f3f171deecd69a`; selected fields are retained in `/tmp/bpmn-compensation-callback-crash-evidence.json`. The installed direct-VM harness uses a one-second SDK execution bound, and a separate subscription test explicitly reports that bound expiring. This evidence does not establish that the timeout caused the native assertion; no runtime or SDK correction is claimed.

The narrow correction preflight caught a one-word map-budget excess and an invalid test-only nullable instance identity; both are corrected. A valid `NotStarted` state reproduces the original defect against immutable `953f5d64` in `/tmp/bpmn-compensation-terminal-valid-state-red-final`. Earlier scratch-source probes failed package/built-in import resolution and are retained separately; they are not red semantic evidence. The corrected fixture covers all five statuses and absent state, with explicit terminal receipt mismatches. Documentation/review-policy qualification passes 41 checks; complete qualification and the same-reviewer correction audit remain outstanding.

The owner reserved a quiet validation window and confirmed available CPU. The fresh doctor passes; the complete Temporal receipt `/tmp/bpmn-compensation-registration-correction-temporal-quiet` exits 1 in 359.94 seconds with 392/358/20 component/concurrent/hosted checks passing and 25 of 26 serial checks passing. The recurring-subscription witness reaches its first handler, replaces the Worker, then fails to observe a second handler within the existing 20-second publication bound. At 21:03 UTC host load is 4.305/6.037/7.253 on eight CPUs. Neither earlier native crash signature recurs in this invocation; this does not establish a runtime fix. The distinct quiet-host failure requires diagnosis before correction qualification can close. The final owner scan also finds and corrects stale registration exclusions in the adapter README and the IL specification’s unsupported and maintained-obligation lists.

The bounded subscription diagnostics did not reproduce the quiet failure. A complete serial diagnostic passes subscriptions but fails the unchanged Sequential Multi-Instance margin at 2,870/5,000 ms (57.4%) in `/tmp/bpmn-subscription-serial-failure-capture`; 25 of 26 checks pass in 232.14 seconds. An independent audit identifies a separate publication-poll defect: each 16-page attempt restarts at revision zero. Retained 17- and 33-page controls reproduce that defect before the one-line cursor-lifetime correction and pass afterward; this does not establish the cause of either live failure. The final focused receipt passes 15 checks and harness type checking passes. The two live subscription/SMI checks pass in 37.69 seconds with diagnostic timing only; complete qualification remains distinct. Scratch import resolution, fixture typing, and a mistyped package command cost additional failed probes, not semantic evidence. A full attempt without local-server permission fails in 37.49 seconds with localhost restrictions; its host-enabled successor reuses the completed build.

After the cursor correction, `/tmp/bpmn-compensation-pagination-temporal-host-qualified` passes every Temporal phase: 392 component, 360 concurrent, 20 hosted and 26 serial checks in 375.76 seconds. The preceding build and harness type check pass; the separate infrastructure receipt `/tmp/bpmn-compensation-correction-infrastructure-quiet` passes all 590 checks in 51.14 seconds. Both final receipts are asserted successful. The recurring-subscription and Multi-Instance witnesses pass with their original bounds, but the earlier intermittent failures remain unexplained. The pagination repair is committed separately at `d1abe982`; terminal classification and owner reconciliation proceed to the original reviewer, with clean committed integration still required.

## Unknown historical measurements

The [Exclusive Gateway conditional-routing capsule](capsules/EXCLUSIVE-GATEWAY-CONDITION-SPEC.md) has no clean pre-capsule baseline and its multi-commit span contains unrelated toolchain work. Its code, documentation, and elapsed cost remain unknown. Other work completed before this policy is outside the measured denominator unless an already-recorded exact boundary exists in its owner.

## Update rule

Record the baseline before implementation. At closure, create the implementation endpoint only after the complete applicable gate passes, verify the measurement from those two commits, add or update one row here, and compare it with the nearest increment that changed the same layers. Follow [the commit boundaries](TESTING-SPEC.md#commit-boundaries): record cost with the required closure evidence and receipt rather than creating an automatic cost-only commit after every proof prerequisite. [The reproduction guard](../scripts/capsule-cost-reproduction.test.ts) still recomputes every row's four figures from its immutable range; neither a working-tree estimate nor the previous capsule's tail is an admissible substitute. Do not keep the same measurement as a second live fact in [PLAN.md](PLAN.md).
