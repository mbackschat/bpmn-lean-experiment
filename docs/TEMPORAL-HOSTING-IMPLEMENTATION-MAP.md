# Temporal hosting implementation map

This detail map owns exact current Product 1 protocol, client, Workflow, Worker, runner, testkit, replay, durable-hosting, and Workflow-chain capacity status. Root routing and cross-area claims remain in [`implementation-status-router`](IMPLEMENTATION-MAP.md).

## Current boundary

One Temporal Workflow Execution chain durably hosts one semantic Process instance. Product starts are handle-free, Run identity stays private, public results are closed, and production chain enrollment enforces the implemented budgets before speculative exposure, scheduling, or terminal return. Forced payload-free and payload-bearing Intermediate Catch Message, Intermediate Catch Timer, and Service Task effect rollover are evidenced, and Horizon 2 is closure-reviewed.

The implemented bounded Workflow-chain contract owns project Event History, payload, pending-operation, publication, chain, and recovery budgets; the safe rollover checkpoint; complete carried state; exact command-result and publication continuity; handle-free public start; stop-the-world deployment compatibility; and forced evidence. Every later repeating BPMN mechanism inherits this host boundary rather than reopening it. Temporal Run identity remains private.

Product 1 privately traverses paired E1 and occurrence-publication segments across Continue-As-New through SHA-bound descriptors and immutable latest-Run selection. Recovery, RuntimeState, paired publication, stimulus, Update, accepted-input queue, effect Activity, retained per-Run trace/publication, pending-Timer, Query-response, terminal-result, Event History, aggregate continuation, and 128-Run bounds are active before speculative exposure, scheduling, or return. Retry and conflict precede lifetime capacity, semantic terminal state and accepted-handler draining precede terminal-envelope capacity, and no public contract or host identity changes.

Activity boundary Message has isolated admission, E1/E2 validation, direct-VM scheduling, and real-service Signal/Update refinement through forced Continue-As-New, Worker replacement, typed coalescence failure, exact history assertions, and replay.

The independently approved Message key-correlation semantic checkpoint extends protocol and validation types far enough to preserve the complete global address, correlated interaction, candidate facts, and target delivery across shared boundaries. Correction target `1ce28ed5` is the approved boundary. The first downstream hosting slice adds one ingress Workflow per canonical complete-address hash, fixed production configuration, an immutable identity/configuration Query, and a client operation that validates that Query after successful start, concurrent duplicate, or lost response.

The second slice adds private content-bound prepare/finalize Updates and deterministic pending/active/quarantined registration state. Pending blocks the scan transition; an installed barrier defers prepare without recording; exact retries retain one fact; changed content fails nonretryably; and finalize activates only the prepared fact. Count, combined canonical-byte, Activity-request, and future 768-byte result-envelope boundaries return typed candidate capacity. The real Workflow retains pending state across Worker replacement, finalizes it, and replays.

The third slice stages the first direct payload successor that creates a correlated candidate. Before prepare succeeds, Process RuntimeState, trace, E1, and E2 remain exact pre-state facts. Prepare acknowledgement atomically installs the staged successor and makes the exact Process candidate Query current; only then may the Worker finalize the same transaction, and the opening Message outcome remains unpublished until that acknowledgement returns. Deferred scans and Activity/ingress failures retain the exact phase; candidate capacity and quarantine become distinct retained Process-host Message failures with no semantic result or speculative publication.

The fourth slice adds private content-bound begin/finish scan Updates and one Worker-owned fanout Activity. The ingress installs the complete active-record barrier before scheduling network I/O; the Activity describes and Queries every locator, accepts only running Workflows returning their byte-exact candidate in barrier order, and emits no partial vector. A complete vector remains barrier-held until exact finish, while a failed or exhausted fanout leaves the same scan identity and barrier installed so later prepare remains `deferredByScan`. Live success, closed-Workflow failure, retained-barrier retry, and both Workflow histories replay.

The fifth slice adds a private content-bound publication Update and identity-bound status Query. Its synchronous validator and handler atomically install one bounded payload queue entry and one fixed 768-byte future-result reservation before acknowledgement; command-id conflict precedes capacity, while count, canonical queue bytes, ledger count, charged bytes, and the reservation encoding fail without state or ordinal. Only the ingress main loop removes the FIFO head, assigns the next contiguous positive ordinal, and begins its content-bound scan. A live one-in-flight plus 63-queued witness races two final-slot Updates and observes exactly one accepted reservation, one unaccepted capacity refusal, byte-identical Temporal deduplication, changed-content conflict, and replay.

The sixth slice invokes the existing pure exact-cardinality matcher only after the complete barrier-held vector passes exact validation. `noMatch` and `ambiguous` replace the current fixed reservation in place, preserve every Process, finish only that scan, and permit the next FIFO ordinal afterward. `unique` writes the exact sole target into both the in-flight record and its ledger reservation, retains the barrier, and returns the stored target without rematching on retry. A live two-publication zero-candidate witness proves ordered settlement, barrier release, later registration, and replay; the concurrent last-slot witness remains held by a pending registration so admission capacity cannot race semantic drain.

The seventh slice sends one content-bound correlated-delivery Update through a bounded Worker Activity to only the retained Process locator. The Process revalidates the exact selected subscription through ordinary semantic admission; response loss resolves against the same Workflow ID and command identity without issuing another Update or rematching. A committed result removes that exact locator, replaces the reserved result, and releases its barrier before the next ordinal.

Any valid semantic refusal or closed/unknown contradiction instead stores target-identified infrastructure inconsistency and quarantines the same locator in place; later admission returns address quarantine without consuming a queue or ledger slot, while a publication accepted before quarantine receives its already reserved result against that retained target. The real-service witness covers commit, valid target contradiction, accepted-before-quarantine settlement, later refusal, three Worker generations, and replay of both Process histories plus ingress.

The eighth slice adds a versioned correlation-ingress continuation envelope over the complete registration and publication states and an explicit candidate-fanout or target-delivery phase. Exact incoming and outgoing validation binds queued and in-flight content identity, assigned-before-barrier pending registration, installed barrier identity, selected-target retention, and strict registration/ledger/FIFO relations. The separately transported address, configuration, and continuation sum under an exact 896 KiB aggregate; Run 128 is admitted and a required Run 129 fails before retained-state access.

Production rollover reuses the 8,000-Event and 8 MiB Workflow-chain triggers, drains accepted handlers, and crosses only with no Activity result or backoff Timer in flight. The live serial witness forces pending registration, exhausted fanout, and exhausted selected-target delivery across three two-Run chains, replaces the Worker, preserves ordinals and queued results, resumes the selected target without rematching, and replays all six Runs.

Independent review approved continuation target `1dd34c29` after correction `7fe0e016`. Incoming continuation now rechecks the same exact aggregate enforced on successor emission, every non-null retained phase rejects address quarantine, and the publication validator admits only a settled prefix, one optional in-flight reservation, and a queued suffix. Exact 917,504-byte restore remains valid; one byte over and each impossible combined state fail through the continuation-invalid boundary.

The ninth slice adds the Product 1 publication client and engine projection. It validates the complete command before ingress creation, resolves the content-bound private status before submitting an Update, admits an explicit Query-safe identity-conflict arm, and after admission polls only the reserved record. Known validator capacity returns the closed nonsemantic capacity arm; lost Update or status evidence returns infrastructure-indeterminate without another allocation attempt. Only settled semantic commit or target inconsistency exposes a target, and the engine declaration contains no Workflow or Process locator. Independent public-boundary review approved correction `f627d06e`, which structurally keeps an unsettled retained target private.

The registered profile and host gate admit only the exact isolated two-catch Program, and the product runner invokes the target-free Product 1 publication operation. A separate answer-free population contract binds unique, zero, ambiguous, and cross-definition schedules across Lean, the pure TypeScript core, and Temporal.

The real-service witness starts both Process instances, reaches the correlated waits through ordinary payload delivery, replaces the Worker before the unique publication, loses the first target-delivery Activity response after Process commit, requires an identical retry request, compares the complete public result and both Process states with the core, and replays every Process and ingress history.

The unique case is now a protected retained corpus route with generated capability and Product 2 About-page disclosure. The Product 2 operational journey, cost/reflection, and closure remain ahead.

The independently approved boundary Compensation retention and Event Sub-Process snapshot checkpoints change optional Program and committed `RuntimeState` bytes already carried by the Workflow. The trigger/handler slice adds pure-core trigger/frontier/completion/failure transitions, optional trigger/effect-wait collections with frozen context, private lifecycle anchors, and a typed failed Process result. Exact core command, entry, completion, and cancellation paths derive these private facts or semantic refusal.

Strict continuation structurally decodes all four Compensation collections and validates them against the Program. Publication validation and Workflow continuation preserve private anchors without exposing them; terminal protocol, Workflow, client, and runner surfaces propagate failure, while Product 2 rejects it. Host admission now admits only the exact well-formed draft checkpoint and retains `compensationSchedulerUnavailable` for every trigger-bearing near-miss. A Compensation-only one-second Activity heartbeat timeout makes running-sibling cancellation deliverable while terminal return waits for acknowledgement.

The checkpoint-only source profile constructs the exact declaration-bearing Program and requires one Program-derived String Process-start binding. Product 1 rejects malformed and snapshot- or execution-capacity-exceeding starts as semantic-process unsupported, while the exact valid start reaches the dedicated Workflow scheduler. It preflights an entire maximal frontier before its first Activity, uses a definition/trigger/handler/effect-bound retry key, starts every member before observing a callback, orders one activation's results canonically, fences Continue-As-New until scheduled Activities are reconciled, and drains removed siblings before terminal return.

Live success crosses a pre-schedule Run boundary, replaces the Worker, retries B with one mutation and the same frozen request, runs B/C concurrently, delays A until B, and replays all six Runs. Live failure publishes the exact failed state after C's typed result, requests and awaits B cancellation before completing with the failed receipt, never starts A, and replays all five Runs. Independent closure review approved target `b951330c` after correction `dfae0d9f`; registration and public capability remain absent.

## Implemented

### Temporal adapter

- One semantic-lifetime Workflow receiving the start stimulus and admitted Semantic Process program
- handle-free `started | rejected` production start after semantic and host admission; success exposes only the Process-instance ID
- caller-configured Worker/client lifecycle with content-derived Workflow and command identities
- one core-owned semantic loop with committed-state Query projection, User Task Update ingress, one Message Signal carrying distinct payload-free and payload-bearing command arms, durable Timer wakeup, bounded effect Activity, and retained result recovery
- protocol, Workflow-recovery, continuation, publication, and testkit validation understand the registered correlated global address and per-instance delivery arm; host admission accepts only the exact isolated profile shape, and the target-free Product 1 operation is driven by the maintained runner
- strict continuation structurally carries all four optional Compensation collections, validates declaration-bound runtime state including deferred Event Sub-Process context, and rejects failed state as non-resumable; protocol publication validation and Workflow continuation preserve the pure core's private trigger and handler occurrence anchors; terminal protocol, Workflow receipt, client, and runner surfaces preserve the distinct failed Process result
- exact Compensation host admission and scheduling for the well-formed draft checkpoint: Product 1 admits its Program-derived String start, refuses malformed and capacity-exceeding starts before another `client.start`, preflights each complete frontier before scheduling, content-binds retry identity, releases same-activation results canonically, fences in-flight rollover, and drains sibling cancellation through an exact one-second heartbeat policy; live success/failure evidence retains pre-schedule continuation, replacement, retry, frozen input, dependency order, failed-state publication, cancellation acknowledgement, both terminal receipts, Event History order, and replay of all eleven Runs; trigger-bearing near-misses retain the typed scheduler-unavailable refusal
- one correlation-ingress Workflow per complete address, with a domain-separated SHA-256 Workflow ID, `REJECT_DUPLICATE` start, fixed production capacity configuration, unconditional exact echo validation after every start outcome, lost-response recovery, explicit test cleanup, and replay
- private content-bound candidate registration across ingress and Process Workflows: pending blocks scans, a barrier makes prepare return `deferredByScan` without queueing or recording, exact retry returns the retained phase, changed content under one transaction id fails nonretryably, quarantine remains distinct from absence, and finalize atomically changes the same pending record to active; the Process stages its exact successor and paired E1/E2 publication until prepare, installs them before its exact candidate Query can authorize finalize, withholds the opening command outcome until finalization, preserves either phase through continuation, and maps capacity/quarantine to retained nonsemantic Message failures; live success and deferred/capacity refusal histories replay, while this registration path publishes no global result and admits no correlated profile
- barrier-linearized private candidate scans: begin/finish Update identities bind exact content, pending registration blocks begin, the barrier precedes Activity scheduling, one Worker Activity describes and Queries every finalized locator, closed or non-exact Process state fails the whole vector, and only the exact complete ordered result can clear the barrier; live success and exhausted closed-Workflow fanout retain the same barrier, defer later prepare, and replay without publishing a global Message result
- private publication admission, order, and semantic settlement: the complete command drives the Update identity, acceptance synchronously pairs a bounded payload queue entry with a fixed ledger reservation, exact retry is service-deduplicated, changed content under one command id conflicts before capacity, refusal stores no identity or ordinal, and the main loop alone assigns contiguous FIFO ordinals before one scan; only a complete exact vector reaches the pure matcher, zero or ambiguity replaces that ordinal's reservation and exact barrier before the next scan, and uniqueness retains one exact target and barrier without delivery; focused order, candidate-order invariance, target-retry, concurrent production-limit last-slot, live zero-candidate, later-registration, and replay witnesses are green, while no public result path is enabled
- same-target private delivery and recovery: the selected publication, active locator, Process identity, subscription occurrence, payload, and ordinal form one bounded Activity request and one exact Process Update; the existing Workflow-chain recovery relation reuses the same Workflow ID and never rematches, semantic commit removes only that locator, and valid refusal or closed/unknown contradiction quarantines it in place while later admission consumes no durable slot and already accepted queued work settles against the retained quarantined target; focused exact-envelope, response-loss, settlement, quarantine, Worker-replacement, and replay witnesses are green
- strict correlation-ingress continuation: one versioned envelope carries complete registration and publication states plus the exact fanout-or-target phase; separately encoded arguments enforce a 896 KiB aggregate and 128 Runs; incoming and outgoing validation reject malformed state, substituted queued or in-flight content, orphan barriers, lost targets, and Run 129; stable production rollover drains handlers and carries no Activity result or backoff Timer; live pending-registration, failed-fanout, and selected-target two-Run chains preserve ordinals and queued results through Worker replacement and replay without rematching
- exact duplicate, changed-content conflict, semantic refusal, closed/unknown separation, and committed-state-only acknowledgement
- durable hosting for every closed family named in [`implementation-status-owner:ENGINE-RUNTIME-PROOF`](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md#current-boundary), keeping embedded and called Process work inside one Workflow rather than assigning BPMN meaning to Child Workflows
- passive `mergeExclusive` host admission; the finite cycle witness reuses User Task Update, survives replacement, recovers one result, refuses a stale occurrence, takes both back-edges and the default exit, observes no rollover suggestion, and replays without a new host mechanism
- family-parameterized boundary-deadline scheduling with distinct typed shared-activation refusals, plus the managed Event-Based Gateway race and passive Message subscription class
- isolated payload-free Activity boundary Message admission, E1/E2 validation, and activation-tagged Signal/Update scheduling with typed co-readiness refusal and pending-callback rollover fencing; every Message-delivery or completion callback wakes an exact-pair wait so semantic admission can settle wrong-channel, payload-bearing, non-empty, or stale inputs instead of hiding them behind the scheduler; direct-VM evidence covers both winners and shared-activation refusal, while the real service covers both winners after forced continuation and Worker replacement, exact stale-Signal rejection with stable published state, both coalesced callers settling, the nonretryable failure type in the Workflow result and Event History, host-mechanism exclusions, and replay of every Run
- a registered sequential Multi-Instance managed-deadline class with its own typed refusal, exact isolated admission, program-aware operation/owner/controller/Activity/User-Task/one-Timer binding, stable semantic deadline identity through inner-task turnover, zero-item absence, and a chain-level prohibition on Continue-As-New while any managed boundary-deadline Timer is armed; the private exact-version real-service capacity owner retains and replays the maximal natural and interrupted topologies, proves exact-16 fit and count-only exact-17 refusal, classifies every final History Event, and measures maxima of 87 Events, 568,902 deterministic conservative History-envelope bytes, 10 activation Events, and 246,799 canonical activation payload bytes; raw service History-size observations are monotone and bounded rather than exact retained facts
- program-aware E1 validation for the optional sequential Multi-Instance progress field, including its exact recursive identities, count equations, task binding, canonical order, old-program absence, and terminal emptiness; transport validation remains representation-only, while producer validation requires the field exactly when the Program declares the operation
- program-aware E2 binding for the exact generated task and lifetime boundary Event under their declaring scope, including one unique sequential-operation successor after completion and explicit exclusion of the synthetic outer/controller occurrence; iteration cardinality and identity remain independently decided by semantic completeness
- continuation decoding for the optional sequential Multi-Instance controller collection through the existing plain-data, structural, stable-checkpoint, identity-bound, and semantic well-formedness gates, plus terminal-receipt acceptance of only the program-required empty Multi-Instance projection
- live natural and interrupted sequential Multi-Instance refinement through the production Workflow chain: pre-arming Continue-As-New, one lifetime Timer, Worker replacement, accepted-result recovery, exact task turnover, stale-task refusal, natural output aggregation or interrupted output absence, E1/E2 publication, terminal receipt equality, production-bound comparison, and replay of every Run
- registered Activity data-input hosting with no new mechanism: the family arms no deadline and schedules no Activity, so durability alone must preserve the copied Activity-local value; one real service runs the present-string, explicit-null, and absent-source scenarios on one compiled program, replaces the Worker between arming and completion, publishes the exact one-binding input collection through the existing open-task Query, terminates the unproductive unavailable-source Run without fabricating any transition, and replays both completed Runs
- registered Activity data-output hosting with no new mechanism either: entry is token-only and the write is a function of committed state and the content-bound completion rather than of delivery count; one real service runs the supplied-string, supplied-null, and omitted-output scenarios on one compiled program, replaces the Worker while all three tasks are open, requires the published canonical trace to name the associated Process `Property` rather than the submitted `DataOutput` id, refuses the omitted-output completion and terminates that Run without writing anything, and replays both completed Runs
- registered parallel Multi-Instance hosting through the same Workflow-chain and managed-deadline mechanisms, with program-aware controller/Activity/all-child/one-Timer readiness, strict continuation and E1/E2 validation, FIFO accepted completion order, distinct coalesced task/Timer refusal, pre-arming rollover, Worker replacement, accepted-result recovery, zero/all/first/task-first/Timer-first schedules, exact terminal receipts, paired publication traversal, and replay of every Run
- retained parallel capacity evidence for natural, Timer-interrupted, and one-transition early-completion topologies at the exact 16-item limit, with 17 refused before mutation; deterministic serializer maxima are 11 activation Events and 128,654 conservative History-envelope bytes, while the pinned real service records maxima of 89 Run Events, 384,535 conservative History-envelope bytes, seven activation Events, and 19,991 canonical activation-payload bytes
- MUE Preview Alpha Product 2 acceptance over that published contract: the showcase actor reads the public committed trace and current interaction before each content-bound User Task Update, the interrupted branch waits for the production deadline scheduler, and acceptance reads Event History only after termination to check exact Update/Timer facts and replay every actual Run
- Worker replacement, accepted-result recovery, exact Query and history assertions, replay, cleanup, and a separately bundled semantic or host-bypass mutation for each distinct mechanism
- stop-the-world Workflow deployment admission: SHA-256 identities bind exact executable bundle bytes and fleet-visible Worker identities; the gate orders Product 1 ingress fencing, graceful old-Worker stop, empty authoritative poller inventory, candidate replay, candidate-only Workflow and Activity pollers, and ingress reopen; every failure stays fenced, a started bad candidate is stopped, and no old bundle is restarted automatically
- deployment compatibility evidence: the candidate replays both a retained two-argument legacy history and all three v1 Runs before polling; the old bundle fails the closed v1 Run on the `bpmn-workflow-chain-v1` marker; mixed old/new or incomplete poller inventories are refused; the legacy open User Task then resumes under the candidate and reaches the exact semantic-core terminal state without changing public Product 1, BPMN, or CIB facts
- forced payload-free Intermediate Catch Message rollover: a wrong-channel rejection closes Run 1; the exact subscription survives into Run 2, its valid delivery opens the trailing User Task in Run 3, and duplicate/conflict recovery adds no semantic transition; exact receipt, pure trace, Signal bytes, and replay preserve the standards-only profile and unchanged CIB premise
- forced Message payload catch rollover through the same Signal: payload-free refusal closes Run 1 with the subscription intact; a replacement Worker commits the scalar payload while changed content conflicts; the associated `Property` survives into Run 3; exact receipt, pure trace, Signal bytes, host-mechanism exclusions, and replay preserve Account P1 and payload-sensitive identity
- forced Intermediate Catch Timer rollover: Run 1 closes before scheduling the committed definition-derived `PT1S` Timer; a fresh successor defers the still-triggered Event History threshold until it retains work, preserving the exact Timer occurrence, 1,000 ms deadline, and logical time zero; Run 2 records exactly one matched Timer-started/Timer-fired pair, derives one `fireTimer` stimulus, reaches the pure semantic-core terminal trace, and replays both Runs under the registered standards-only Timer profile and its unchanged `CIB-AGR-0004`/`CIB-CFG-0001` classifications
- forced Service Task effect rollover: Run 1 closes with the committed effect occurrence, neutral descriptor, empty request arguments, and content-bound transport key before any Activity is scheduled; Run 2 exposes the same semantic wait while one probe Activity is held in flight, then records exactly one schedule/start/completion lifecycle, derives one `completeEffect` stimulus, reaches the pure semantic-core terminal trace with one external mutation, and replays both Runs under the registered success-only profile and unchanged `CIB-EXT-0001`/`CIB-CFG-0002` classifications
- canonical start/completion data, effect transport, interaction/task projection, closed v1 receipts, private exact legacy normalization, and strict external-runtime configuration
- one live product example per distinct host mechanism, with reused mechanisms checked without a Temporal service and optional time-skipping calibration outside default verification
- direct `client.start` hosting for exact registered Message Start identity through the ordinary Workflow start input, with wrong Interface Operation rejection before Workflow creation, service acceptance while no Worker polls, later User Task completion, no Signal Event, exact history replay, and a test-owned Signal-With-Start discriminator
- test-owned one-action Temporal Schedule hosting for the registered Timer Start profile, with pre-Schedule zero-create refusal, Worker absence through the due occurrence, an exact stored Workflow-ID base kept distinct from the opaque service-returned Workflow/Run identity, base-as-execution refusal, exact downstream User Task completion, ten-event history inspection, action exhaustion, stored-action and direct-start mutations, and replay
- passive hosting for the registered Terminate End profile through existing User Task Updates, including Worker replacement after Trigger, committed-result recovery, Outer-only Query, stale Sibling refusal with exact state preservation, terminal completion, 20-event history inspection and replay, plus a test-owned Workflow whose wrong global cancellation closes instead of publishing Outer
- configured Task hosting through the existing Probe Activity and User Task Update path, including Worker replacement during the active Activity attempt, idempotent result reconciliation, exact terminal state, history inspection and replay, plus a test-owned effect bypass that exposes the User Task early
- registered Boolean completion through the existing Update path, with Worker replacement, old-profile refusal then valid completion, tagged projections, history/replay, and stringification/outside-core mutations
- registered User Task assignment/form metadata through the existing Query and Update path, with Worker replacement, exact passive projection, Boolean completion, metadata-free terminal state, history/replay, old-profile control, a Query-omission mutation, and a same-task source field-type variation bound in the Workflow-start program and Query
- registered interchange composition through the unchanged Product 1 start and content-bound User Task Update path, with exact String/Null data, Worker replacement before completion, retained Update result, terminal state and trace equality with the semantic core, notation absent from public observation, exact history mechanism exclusions, and replay
- registered Service Task incident hosting with an unchanged bare semantic Activity result union, exact successor-only one-attempt policy, host-only `technicalFailure`, Workflow-derived report command, committed incident Query, content-bound retained retry Update, exact effect restoration, Worker replacement, terminal result recovery, two-command race, typed post-retry host failure, history assertions, and replay
- registered incident-cancellation hosting with the shared one-attempt Activity/report policy, content-bound exact-root/exact-incident Update, Worker replacement while the incident is open, retained accepted-result recovery, typed cancelled receipt through ordinary Workflow completion, distinct late `processClosed`, preserved committed data, history and replay, and native-termination/completed-receipt mutations
- a Workflow revision accumulator/cursor Query, strict producer validation, representation-free client, opaque-locator engine API, and real retention evidence covering positive cursors, Worker replacement, terminal retrieval, repeated activation identity, pure Query history stability, and exact replay
- production Product 1 enrollment for generic, exact-definition, Message Start, and one-shot Schedule Process starts in the exact `bpmn-workflow-chain-v1` branch through a fixed Initial host envelope, with explicit production budgets, committed RuntimeState, bounded command recovery, and publication heads/current/open anchors carried across Runs; retained direct two-argument Workflow starts remain outside the patch for exact pre-v1 replay compatibility
- chain-relative recovery for User Task, Message, Retry, and Cancel through a private identity-echoing latest-Run Query, preserving retry/conflict precedence, capacity/closure classification, Call Activity identity separation, and public host-identity privacy
- production capacity: 512 recovery entries; atomic 64 KiB state, paired batches, and stimuli; 1,500 Updates/eight in flight; a 64-entry/256 KiB queue; refusals precede transport, acceptance, or evaluation; exact fills request rollover; failures omit Run IDs/candidates; failed histories replay
- effect Activity capacity: 64 KiB request before scheduling and result before return with Workflow revalidation; fixed 16 KiB cause-free exhaustion detail; oversized-result live failure at revision 3 replays
- retained per-Run capacity: exact incremental canonical accounting for trace plus aligned E1/E2 batches, a hard 2 MiB ceiling before mutation, and proactive rollover with room for the closing candidate plus one fence-racing Signal; a production-bound witness grows only rejected-command trace, crosses one rollover, preserves the open occurrence and early result, completes, and replays both Runs below the Event History trigger
- pending-Timer capacity: committed Timers are checked against 64 before scheduling; 65 fails through typed chain capacity, while admitted profiles retain one live Timer
- Event History capacity: lower-only v1 count and byte limits with production values of 8,000 Events and 8 MiB; deterministic SDK-suggestion, count, then byte trigger selection at the stable checkpoint; every trigger is deferred in a fresh continuation Run until that Run retains work, preventing an empty Continue-As-New loop without weakening continuation-input bounds; an executable Update/Signal, Timer, Activity-result, Workflow Task, and continuation cost table that fits one maximum admitted activation inside the 10,240-Event and 10 MiB warning margins; and live count-triggered three-Run plus byte-triggered two-Run witnesses that preserve the registered cyclic User Task trace, outcomes, occurrence identities, terminal receipt, and replay without using Event History as BPMN or CIB state
- aggregate continuation capacity: each of the six separately transported canonical payloads keeps its individual bound, their exact byte lengths sum under 448 KiB before successor emission and incoming admission, exact fit succeeds, and one byte over is reported without adding a synthetic array wrapper; successor overflow remains typed chain capacity while malformed or oversized incoming continuation remains a distinct fail-closed continuation defect
- fail-closed continuation admission applies the semantic core's gated runtime-state defects to carried state. A declared resumable User Task checkpoint is accepted with its activation counter at one and refused after only that counter entry is removed, independently observing the implemented identity bound at the Run boundary without assigning Event History or Run identity any BPMN meaning
- the independently checkpoint-approved Activity-family issuing amendment changes no host mechanism or carried shape: existing continuation state already transports each Activity occurrence beside its high-water mark. Its semantic pair oracle and guarded writer discharge prevent a retired Activity identity from being reintroduced after restore or ordinary evaluation; Timer and task issuing disciplines remain separate open obligations
- Workflow-chain lifetime capacity: a valid required 128th Run carries 127 contiguous retained descriptors plus complete state, recovery, and publication; a required 129th successor fails nonretryably as typed chain capacity before the current segment directory is read or extended; an incoming ordinal 129 remains a distinct fail-closed continuation defect; descriptor-directory and Run limits remain independent and no ordinal or Run ID enters a public result
- private paired E1/E2 per-Run publication segments with strict descriptor/directory validation and SHA-bound continuation; immutable latest selection followed by an exact selected-Run Query; Run-local pages; selected retained-Run loss mapped to `unavailable`; legacy fallback only for `QueryNotRegisteredError`; and live evidence over Run 1 `0..8`, Run 2 `8..12`, Run 3 `12..16`, cursor pages `0/4/8/12/16`, two occurrence identities spanning Run boundaries, unchanged terminal receipt, no public Workflow/Run identity, and replay of all three Runs
- retained open flow-node occurrences carry `retainedOpen[].attachedHandlers`, a closed tagged Timer-or-Message list. Admitted families consume their exact tags, and only Message victory emits the instantaneous Boundary Event. The private state crosses Continue-As-New, reaches no public artifact, and counts against the existing 64 KiB continuation-and-directory bound
- that field has one writer, which derives it from the committed post-state after every command for every retained entry, and the continuation decoder recomputes the same derivation and refuses a payload that disagrees, so the restore boundary is where a forged pairing is caught
- it carries **no version marker**, which the pre-release policy permits while no immutable history baseline is approved. At the first durable baseline a retained accumulator written before the field existed decodes as absent rather than empty and the decoder's exact-key check refuses it, so that baseline owes either a migration for the retained set or an explicit statement that no pre-field history is supported
- selected-Run Query-response capacity: a 192 KiB canonical UTF-8 ceiling at producer and client boundaries; the producer returns the largest complete aligned E1/E2 batch prefix that fits, never splits a command batch or Run segment, and withholds current/open snapshots until the page reaches the immutable head; exact-fit, one-byte-lower, oversized-client, malformed-pair, deterministic-remainder, and real BPMN cycle evidence preserve the original semantic-core batches unchanged
- terminal-result capacity: the exact private v1 receipt-plus-recovery envelope is measured under a 192 KiB canonical UTF-8 ceiling immediately before return; exact production-bound bytes return unchanged, while one byte over becomes nonretryable typed chain capacity after every accepted handler has its semantic result; the retained failed Run resolves the closing result and identity conflict before returning the exact terminal receipt for unseen work, exposes no Run identity or recovery material publicly, and replays

## Explicitly absent

### Temporal adapter

- retained result beyond Temporal retention
- production canonical-observation API
- protocol that imposes caller order on concurrent distinct commands
- semantic policy copies in the Workflow
- structured or collection-valued Message payloads, payload on another Event or Task locus, key-based/global correlation beyond the exact registered single-property profile, modeled Message throw, and other public cross-Workflow Message routing
- Message ingress broker/router, definition-version fanout, or multi-target publication receipt
- committed Event History fixtures
- engine-global task discovery through Search Attributes; Product 2 instead owns its current-task projection from published engine facts
- Compensation profile registration, public capability, corpus, or Product 2 support
- any new Temporal primitive for User Task metadata
- a second semantic incident, arbitrary retry count or backoff, exception/cause projection, cancellation beyond the exact incident-gated root command, or Product 2 incident ingress
- any patch branch other than the exact `bpmn-workflow-chain-v1` checkpoint enrollment
- legacy representation fallback beyond the exact decode-only pre-v1 terminal-receipt normalization seam
- production history baseline
- general Worker versioning
- expression evaluation beyond pure Simple Boolean v1
- integer values outside exact M6 User Task completion, String-list values outside M6 completion and the exact profile-gated Multi-Instance Process-start bindings, nested or heterogeneous values, or general effect faults/Error propagation beyond the direct-parent internal slice
- Activity heartbeats beyond the exact one-second Compensation cancellation-delivery policy
- host cancellation recovery and exceptional child-scope interruption or propagation beyond the exact direct-parent Error slice
- timer forms/races/cancellation beyond the exact capsule
- post-retention publication reconstruction or archive
- Compensation and Event Sub-Process hosting beyond the exact draft-profile scheduler and carried-state boundaries above
- Message payload beyond the exact scalar Intermediate Catch Event slice, public hosted key-based/global publication, modeled throw, Message Flow, and other Message Event loci
- Multi-Instance hosting beyond the exact registered sequential and parallel User Task slices, including repeated or nested controllers, migration after an immutable history baseline, and a Workflow-chain policy that rolls over while a managed lifetime Timer is armed
- immutable profile or production Event History compatibility
- task inbox

## Evidence owners

The separate protocol, client, Workflow, Worker, runner, and testkit packages under [`packages/temporal-adapter/`](../packages/temporal-adapter/), [production lifecycle specification](TEMPORAL-PROCESS-LIFECYCLE-SPEC.md), and [Temporal evidence map](TEMPORAL-TEST-EVIDENCE-MAP.md) bind these claims. The [archived Workflow-chain proposal](archived/TEMPORAL-WORKFLOW-CHAIN-BOUNDS-PROPOSAL.md) retains the selected-design and review rationale.

Workflow-chain readiness waits use the shared 20-second host-operation deadline rather than a fixed query-attempt count, so variable remote observation cost cannot shorten their allowed wall-clock interval. The correction is non-material under [the negative case](TESTING-SPEC.md#independent-cold-review-gate), changes no production or semantic contract, and is covered by the fake-clock [workflow-chain support guard](../packages/temporal-adapter/testkit/test/workflow-chain-test-support.test.ts) plus the complete Temporal package gate.

The production refinement witnesses are not yet reliable under host load, and that budget correction covered only half the class. Measured on 2026-08-29 at engine head `3530da83`, `./scripts/pnpm.sh run test:temporal:built` failed 2 of 5 runs and the composed `test:pre-push:verify` 1 of 3.

Three different tests failed with three different symptoms. The Sequential Multi-Instance natural path lost its scenario's real `PT1S` lifetime Timer to three completion round trips, then observed the engine correctly reject an interrupted task. The Parallel Multi-Instance serial witness exceeded the 20-second readiness deadline. The Workflow-chain Timer-rollover file died before its first result with no captured cause. The same tests pass in isolation and the hosted batch alone passed 3 of 3, so the mechanism is host load rather than any semantic change.

These witnesses run on `createCachedLocalEnvironment`, a real host clock, so a model deadline shorter than the client work inside it makes a verdict depend on machine speed, which [the semantic invariants](../CLAUDE.md#semantic-invariants) require to be an explicit input instead.

Raising a budget did not close it, because the deadline was the retained scenario's own, whose exact bytes bind the Lean, differential, and corpus lanes where logical time makes it free. Instrumentation on a quiet host measured the natural path at 328 to 355 ms of its 1,000 ms window, of which Worker replacement alone cost about 150 ms. The recorded four-core hosted-runner profile is about twice as slow as that machine, which leaves roughly 700 ms against 1,000 ms before any concurrency at all.

The owner approved raising both Multi-Instance outer-lifetime deadlines to `PT5S` on 2026-08-29. That is a profile-capability change rather than a timeout: the admitted lexeme, the checked graph, both lowerings, Lean admission and structural validation, the host admission class, and every retained schedule and digest move together.

Duration alone would only move the same race further away, so both natural witnesses now assert their own margin against [the host-clock deadline ceiling](../packages/temporal-adapter/testkit/test/host-clock-deadline-margin.ts). A path that consumes more than half its host-armed deadline fails while headroom remains and reports the fraction, instead of surfacing later as the semantic symptom the schedule caused. The measured budget is the armed remainder `deadlineMs - logicalTimeMs` taken at the arming observation, not the absolute deadline, and each span runs to the closing completion because that round trip races the timer exactly as the earlier ones do.

The readiness half of the remaining findings has a mechanism rather than only a symptom. The open-task poll spent its own allowance on the model's armed deadline, so [`waitForOpenUserTaskIds`](../packages/temporal-adapter/testkit/test/temporal-worker-test-support.ts) now takes that deadline separately instead of subtracting it from the poll budget. The Parallel Multi-Instance serial witness has since run at 13.9 seconds against its 20-second deadline in both the focused Temporal gate and the clean-committed verification entry point.

The rollover finding has neither a cause nor a fix, only non-reproduction: the Workflow-chain Timer-rollover file died before its first result with no captured cause, and has not done so again. Treat a single red Temporal run as uninformative and re-run before concluding, and treat green runs on a host carrying other work as equally weak evidence that a load-dependent failure is gone.

The deadline change was independently reviewed. A context-cold sub-agent reviewed the raised deadline at `b2205b2e` under `fork-turns-none` and returned `approve-with-required-edits`; the same reviewer audited the correction at `ffbf7b24` and `32d321aa` in a warm follow-up and returned `approve`.

Its decisive check was one the author's own gates could not make: `$ref` siblings are ignored under draft-07, so the five per-family `durationMs` constants would have been silent no-ops under the wrong dialect, and the reviewer compiled the exact schema bytes under `Ajv2020` to prove each site accepts its own deadline and refuses the sibling's. This capsule pair is already closed, so the verdict is recorded here rather than as a fourth stage in either capsule's three-stage receipt.

## Nearest unsupported claims

- **Post-retention publication:** reconstruction or archive remains outside the bounded Workflow-chain contract, whose public result is `unavailable` after the selected retained Run disappears.
- **Workload isolation:** complete Horizon 3 with queue and Worker isolation, backpressure, tenant fairness, capacity observability, shared-store failover, representative mixed-model tests, and published throughput, latency, saturation, recovery, and cost evidence. More Workers or queue partitions alone do not constitute this result.
- **Hosting breadth — correlation mechanism:** contract validation, ingress identity/recovery, the registration transaction, Process-side staged commit, exact candidate Query, barrier-linearized complete fanout, resolver recovery, typed capacity/quarantine failure, both-phase Process continuation, FIFO publication queue/ledger reservation, semantic-rejection settlement, unique-target retention, same-target delivery/recovery, contradiction quarantine, strict ingress continuation/capacity, the public engine/client operation, and runner binding are present.
- **Hosting breadth — correlation evidence:** four-schedule population comparison, Worker replacement, response-loss recovery, replay, retained whole-model coverage, and capability disclosure are present. The Product 2 operational journey, cost/reflection, and closure remain.
- **Hosting breadth — other families:** Message payload beyond the exact scalar Intermediate Catch Event slice and other public Message routing, general Worker versioning, Activity heartbeats beyond the exact Compensation policy, Search Attributes, general cancellation recovery, and broader timer, effect, and expression families remain absent.
- **Compensation hosting:** exact admission, same-handler retry identity, complete-frontier scheduling, activation-local canonical reconciliation, in-flight Run fencing, heartbeat-delivered cancellation drain, pre-schedule Continue-As-New, Worker replacement, retry, completed/failed receipt, Event History ordering, and replay evidence are independently closure-reviewed for the private draft checkpoint. Profile registration, public capability, corpus, and Product 2 support remain absent.
