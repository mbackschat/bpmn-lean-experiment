# Process assessment ledger

**Status:** Maintained process-finding ledger.

## Purpose and denominator

This ledger retains every observed weakness in *how* work was carried out, separately from whether its claims were sound. [The capsule reflection](../CLAUDE.md#milestone-and-capsule-reflection) already requires turning each escaped issue into a reusable review question or an executable guard, and [the closure review](TESTING-SPEC.md#capsule-closure-review) repeats that requirement. Neither kept a record, so a finding could be answered with fresh prose, forgotten, and repeated — and a repeat was indistinguishable from a first occurrence. This ledger exists to make recurrence visible and therefore actionable.

The denominator is process findings, not defects. A finding belongs here when it cost real work or produced a wrong intermediate result **and** an existing guard did not catch it. A defect that a gate rejected is not a finding: the guard already worked. Semantic claims, evidence strength, and common-mode risks stay with [the owning capsule](capsules/README.md); reproducible cost stays in [the capsule cost ledger](CAPSULE-COST-LEDGER.md); implementation status stays in [IMPLEMENTATION-MAP.md](IMPLEMENTATION-MAP.md).

This is not a retrospective diary. Rows are findings with dispositions, one row per distinct mechanism, and no row narrates a session.

## Self-assessment questions

Answer these after a capsule or milestone closes, and again at each session handoff. A question whose honest answer is "I do not know" is itself a finding.

1. **Bounds.** Which executable guard, oracle, registry, or size ceiling already constrained this work, and did I enumerate them before the first edit or discover one mid-change?
2. **Recall.** Which structural claim did I state without reading its source in this session? Name the claim, not the intent to be careful.
3. **Rework.** What did I write and then discard? Was the deciding fact available before I started?
4. **Measurement.** Is every number I reported reproducible by a named command, and was any timing figure taken under competing load? Which resource dimensions did I actually measure, and did I draw a cost conclusion from fewer dimensions than the change can consume?
5. **Evidence reading.** Did I read each gate outcome from the run's own output, or from a summary, notification, or impression of it?
6. **Prose versus guard.** Which correction from the previous increment did I rely on prose to enforce, and did that prose actually bind me?
7. **Recurrence.** Does anything above already appear below? If so, its recorded disposition failed and must be escalated.
8. **Siblings.** For each correction I made, which other copies of the corrected fact did I open? Name the files and sections I actually read, not the intent to have been thorough. If the fact had more than one copy, why does it still have more than one?

## Escalation rule

A finding recorded a second time refutes its own prose disposition: the rule existed and did not bind. The second occurrence therefore requires an **executable guard**, not a better-worded reminder — unless no repository fact can observe the class at all, which is what `unguardable` records. That exception exists because the rule as first written assumed every recurring finding is guardable, and a finding about how results are *reported* is not: the repository cannot see a claim made in prose about itself. [The executable check](../scripts/document-reviewability.test.ts) enforces this — a row at two or more instances whose disposition is not `executable guard` fails the infrastructure gate.

Dispositions are a closed set:

| Disposition | Meaning | Evidence cell must link |
|---|---|---|
| `executable guard` | A gate now rejects the class, not only the reported instance | at least one executable guard or script |
| `review question` | Added to the questions above or to a reviewer prompt; admissible only at a single instance | the document recording it |
| `accepted risk` | Understood, not worth a guard, and explicitly tolerated; admissible only at a single instance | the document recording the acceptance |
| `unguardable` | No repository fact can detect the class, so no gate can enforce it; admissible at any instance count but must state why | the reusable question standing in for the guard |

## Findings

| Finding | First observed | Instances | Disposition | Evidence |
|---|---|---:|---|---|
| Existing executable bounds were not enumerated before planning an atomic change, so an oracle, a module ceiling, and a registry obligation each surfaced mid-implementation. The fourth instance shows the limit of the disposition: `what-binds` was run before editing the adapter schedulers and not before adding a check to a guard file that was already at 578 of 600 nonblank lines, so the module ceiling surfaced again mid-change. The enumerator reports bounds when invoked and cannot require its own invocation; the size gate rejected the result before commit, which is why this is a count rather than a new row. The fifth instance shows the same limit on the *add* half of the rule, which says every path a change will add or grow: four owners the change created — a 242-line family runtime, a replacement admission module, an extracted cancellation owner, and a new Lean module — were absent from the capsule's owner inventory, so the recomputing guard measured only the rows already present and could not report the ones nobody wrote. The sixth instance shows a third way the rule is evaded without being broken: `what-binds` was run on `scenarios/` before the first edit, correctly, but the change then grew to include the Temporal adapter when a red pipeline revealed the family had no host lane, and the enumerator was not re-run for the newly in-scope paths. `workflow-implementation.ts` was therefore edited to 591 of 600 nonblank lines and its 9-line headroom discovered afterwards rather than planned around. The rule reads as a precondition on a change whose extent is known at planning time; a change whose extent is discovered by a failing gate has no such moment, so the trigger belongs on scope growth rather than only on first edit | [Interrupting Activity boundary Timer](capsules/ACTIVITY-BOUNDARY-TIMER-SPEC.md) capsule | 6 | `executable guard` | [change-binding enumerator](../scripts/what-binds.test.ts), [capsule binding inventory](../scripts/document-reviewability.test.ts), [module-size headroom](../scripts/source-hygiene.test.ts) |
| Which module or conjunct enforced a behavior was asserted without opening or probing it, and the claim was wrong. The second and third instances are the same mechanism one layer deeper, and both were refuted by one probe: a capsule stated that its attachment rule requires the unique Sub-Process host, while that rule constrains only the host's kind and scope and the *operation multiset* at scenario and start admission is what requires the identity — moving the deadline to a same-scope User Task compiles and lowers to another family's program. The adjacent negative test then credited its rejection to the host-kind conjunct when the same-scope conjunct is what fails, so the case carried none of the weight its comment assigned it. The fourth instance landed *inside the correction closing the second and third*, which is the strongest evidence that this class does not yield to intention: a new owner-inventory row credited a semantic-core admission merge with rejecting a managed race beside a bounded wait, where that rejection is `managedTotal === 1` in the adapter's host classifier and already existed at the baseline. The false reason was the adjacent row's true reason, transplanted into a module that decides program well-formedness and nothing about host capability. `unguardable` states a limit rather than a preference: no repository fact distinguishes a prose or comment sentence that names the right conjunct from one that names a neighbouring conjunct of the same predicate, because both compile and both leave the test green, which is why question 2 stands in for the guard. One half *is* mechanizable and is the cheaper defence: seeding a conjunct mutation per negative case, which this capsule did for the host-kind allowlist — replacing its closing `false` with `true` fails exactly the two host-kind cases — and did not do for the child-task case, where it would have shown the same-scope conjunct failing before review did. The fifth instance moves the class from code to the normative corpus and shows it is at its worst inside a correction: told that two tables sit in a different clause than cited, the correction invented a reason — "a Boundary Event is a kind of Intermediate Event" — that `BPMN20.cmof` refutes by deriving `BoundaryEvent` and `IntermediateCatchEvent` alike from `CatchEvent`, on the same page the capsule was already citing. A wrong citation is checkable and a wrong rationale reads as understanding, so the invented explanation was worse than the error it explained. Two further rounds then corrected the replacement's sub-heading, each time by walking backward from the table to the nearest heading — the same weak method whose failure had just been demonstrated | [Interrupting Activity boundary Timer](capsules/ACTIVITY-BOUNDARY-TIMER-SPEC.md) capsule | 5 | `unguardable` | [question 2](#self-assessment-questions) |
| A gate outcome was read from a task notification rather than the run's own exit line. The second instance shows the mechanism is not only *where* the outcome is read but how the run is invoked: the complete gate was launched as `./scripts/verify.sh > log 2>&1; echo "EXIT=$?"`, so the compound command's own status was the `echo`'s and the harness reported exit 0 over a gate that had failed with exit 1. The failure was real — a `fire?` signature change had left the `Experiments` tree broken, which plain `lake build` does not compile — and it was found only by grepping the log for failure markers. `unguardable` states a limit rather than a preference: no repository fact can observe which line an agent read, nor whether a status came from the gate or from a trailing command in the same invocation. The practical rule that follows is narrower than question 5 and belongs with it: invoke a gate as the last command of its invocation, so its exit status is the invocation's | [Interrupting Activity boundary Timer](capsules/ACTIVITY-BOUNDARY-TIMER-SPEC.md) capsule | 2 | `unguardable` | [question 5](#self-assessment-questions) |
| A structural requirement was asserted from a proxy measurement rather than a demonstrated need, so an extraction was recorded as required for a file the work never grows | [Interrupting Activity boundary Timer](capsules/ACTIVITY-BOUNDARY-TIMER-SPEC.md) capsule | 1 | `review question` | [question 1](#self-assessment-questions) |
| A recorded conclusion outlived the measurement that made it true, because the premise was not stored beside it. The third instance is inside the guard's reach and the fourth is outside it: a capsule's fifteen headroom figures were written before the implementation that moved them and the recomputing guard rejected all fifteen, while a resume-point paragraph then restated one of those figures from recall as 22 where it measures 23. The guard recomputes figures in the capsule inventory it parses, not every number a planning document repeats about the same owner, so the second copy is the exposure. Duplicating a measured figure into prose is the mechanism; the disposition stays `executable guard` because the primary copy is guarded and the fix for the secondary copy is to link the owner rather than restate its number | [Interrupting Activity boundary Timer](capsules/ACTIVITY-BOUNDARY-TIMER-SPEC.md) capsule | 4 | `executable guard` | [recomputed owner headroom](../scripts/document-reviewability.test.ts) |
| A verification claim was stated more broadly than the gates actually run: twice against stale built output from a narrower build target, once by calling the tree green while an unrun focused gate was red, once by committing on `lake build` while the complete gate additionally builds the `Experiments` tree | [Interrupting Activity boundary Timer](capsules/ACTIVITY-BOUNDARY-TIMER-SPEC.md) capsule | 4 | `unguardable` | [question 5](#self-assessment-questions) |
| A registered profile was landed without the live example its oracle requires, turning the complete gate red after the focused gates passed | [Interrupting Activity boundary Timer](capsules/ACTIVITY-BOUNDARY-TIMER-SPEC.md) capsule | 1 | `review question` | [question 1](#self-assessment-questions) |
| A collection-contributing match over a semantic variant used a wildcard arm, so a new family silently contributed nothing instead of failing to compile: a boundary Event was unreachable because its Activity edge was keyed on trigger kind, and a composite family's task and timer waits were missing from every public projection. The fourth and fifth instances are the same mechanism inside the checked-graph validator, and both were found by the first Lean fixture ever to lower a bounded scope: `oneCompletionStrategyPerScope` counted child-scope entry through `.enterScope` alone, so a bounded child scope read as one nobody enters, and `operationRespectsScopes` sent `.enterBoundedScope` to a catch-all requiring every output to be owner-scoped, which its child entry is not by construction. The profile's own admitted program shape already expected that operation, so the validator and the profile disagreed while every gate stayed green — the failure surfaced as an unexplained `programWellFormed` rejection rather than as a missing case. The lesson these two add is where the class hides: a wildcard in a *validator* inverts, reading as a pass for the sibling arm and a rejection for the new family, and no fixture existed to run the lane that would have shown it | [Interrupting Activity boundary Timer](capsules/ACTIVITY-BOUNDARY-TIMER-SPEC.md) capsule | 5 | `executable guard` | [exhaustive variant inventories](../scripts/lean-source-contracts.test.ts) |
| A wall-time figure was measured while a review sub-agent competed for the same cores, making it non-comparable | [Interrupting Activity boundary Timer](capsules/ACTIVITY-BOUNDARY-TIMER-SPEC.md) capsule | 1 | `review question` | [contended-measurement rule](TESTING-SPEC.md#default-verification) |
| One external pin was restated in every consumer instead of derived from a single owner, so a host tool upgrade blocked the complete gate and no gate could have caught a bump that stranded a stale copy | [Interrupting Activity boundary Timer](capsules/ACTIVITY-BOUNDARY-TIMER-SPEC.md) capsule | 1 | `executable guard` | [pin owner agreement](../scripts/pinned-toolchain.test.ts) |
| A shared harness assertion compared a proxy for a fact only one component observes — the state the host actually paused at — so each new evidence shape adds another scenario-name exception rather than correcting the proxy; that assertion now carries five | [Interrupting Activity boundary Timer](capsules/ACTIVITY-BOUNDARY-TIMER-SPEC.md) capsule | 1 | `review question` | [question 1](#self-assessment-questions) |
| Normative references were written into a durable profile artifact, its scenarios, and a Lean fixture without opening the pinned corpus: two named clauses that exist but describe other subjects, and one workflow-pattern row absent from BPMN 2.0.2 entirely. The third instance shows the guard's *coverage* was the hole rather than its predicate: a new profile shipped `Clause 10.5.4` and `10.5.5` for its start and end events, where 10.5.5 is Event Definitions, and the resolution gate stayed green because its glob was `scenarios/*/*.scenario.json`, which never matches a primary `scenarios/*/scenario.json` — eleven of them had never been checked. The author then compounded it by reading clause numbers out of a heading regex over the Markdown conversion, which silently omits clauses the conversion renders as plain lines, and concluding that three clauses a reviewer named did not exist; searching the body text showed all three do. Two rules follow, both cheap: a guard over a file class states which globs it covers and is checked against the actual file inventory, and a claim that a clause is absent is made from the body text rather than from an extraction of headings | [Interrupting Activity boundary Timer](capsules/ACTIVITY-BOUNDARY-TIMER-SPEC.md) capsule | 3 | `executable guard` | [normative reference resolution](../scripts/normative-reference-resolution.test.ts) |
| A new guard's first formulation did not reject the defect it was written for, and was only found by seeding that exact defect: an unreferenced schema branch credited as reachable, a helper enum read as an empty value list, and a fabricated table row accepted on its table number alone. The fourth instance survived a first correction and reached a governing document: the kernel-decide guard's prefix alternation admitted only line-start, `by`, and the bracket forms, so `cases outcome <;> decide` was invisible to it, the sweep it certified left that site plain, and [CLAUDE.md](../CLAUDE.md) then asserted the guard rejects a tactic-position `decide` without `+kernel`. Its per-line `+kernel` exemption had the same shape, exempting the plain half of a line carrying both forms. Seeding one form of a defect certifies that form only, so a guard over a syntactic class needs a case per position of the class. The fifth instance moves the same shape from syntax to call sites: the guard answering the 8 GB Lean build asserted that `verify.sh` and `pnpm.sh` derive and export `LEAN_NUM_THREADS`, which is true and was never the class — the documented experiment gates, the experiment reproduction blocks, and every `lake build` typed directly stayed unpinned, covered only by a prose caveat asking the reader to export it themselves. The author of that caveat then relied on remembering it. A guard scoped to the sites the fix touched certifies those sites only, so the fix is to remove the unpinned form from the vocabulary — one wrapper owning the pin, and a scan rejecting a bare `lake` subcommand anywhere it could be copied and run. The sixth instance is the harness rather than the predicate: the guard written to reject a gate depending on an optional external cache spawns `node --test` against an emptied external root and asserts exit `0`, and the spawned runner inherited `NODE_TEST_CONTEXT` from its parent, believed it was already a test child, reported over IPC, and exited `0` with a failing assertion inside. Seeding the exact defect is what exposed it, and the tell was wall time — 50 ms for what should have been a process spawn — so a new guard's seeded run should be checked for having actually done its work, not only for its verdict. The seventh instance is the same mechanism with no guard involved at all, twice in one session, which is what makes it the class rather than a property of guards: a seeded-defect sweep over the semantic core edited the TypeScript source and ran the tests against the previously built `dist`, so all four seeds reported green while nothing had changed; and a seeded-defect sweep over Lean counted failures with a grep pattern that never matched the build's actual output, so four rejected seeds were reported as unrejected. Both were caught by re-running one case and reading the raw output. The rule that follows is narrower than "check the seed took": a seeded run must be confirmed against the artifact the assertion actually reads and against the runner's own unfiltered output, because a proxy for the verdict fails silently in exactly the direction that looks like success. The eighth instance happened *after* the seventh was written into this row, in the same session, and is the row's own earlier lesson violated rather than a new mechanism: a guard added to refuse a `cancelActivity` lexeme that names no interruption disposition matched only the double-quoted XML attribute spelling, so `cancelActivity='1'` — schema-valid, meaning *true* — stayed admitted under the non-interrupting profile and lowered to the monitored operation. Five parameterized cases certified five *values* in the one *position* the author had in mind. The kernel-decide prefix alternation recorded above is the same failure in the same artifact type, a regex over source text that enumerated some syntactic positions and missed others, and the checkpoint reviewer found this one by varying the delimiter rather than the value. Enumerating values is what feels like coverage; enumerating positions is what the class requires, and prose saying so did not bind its own author an hour later. The ninth instance is the same shape in a guard written to close a *different* instance of a neighbouring row, one round earlier: a scenario-to-profile containment check reduced each reference to its numeral and discarded kind and qualifier, so a profile declaring `Table 13.2` authorized a scenario citing `Clause 13.2` — two subjects that both resolve, which is exactly the pair the check exists to separate. The checkpoint reviewer found it; the author's own anti-vacuity case had varied the *numeral* and never the kind, which is the enumerating-values-not-positions failure this row already records at instance eight. Correcting it then surfaced a second lesson the seeded case had also missed in the opposite direction: comparing full identity made four pre-existing scenarios fail for citing `Table 13.3 WCP-7` under a profile declaring `Table 13.3`, because a qualifier narrows rather than departs and an unqualified declaration authorizes its own rows. A containment predicate needs a case per *direction* of the relation, not only per position of the syntax | [Interrupting Activity boundary Timer](capsules/ACTIVITY-BOUNDARY-TIMER-SPEC.md) capsule | 9 | `executable guard` | [seeded-mutation checks in the reference guard](../scripts/normative-reference-resolution.test.ts), [anti-vacuity in the schema coverage guard](../scripts/contract-schema-coverage.test.ts), [no bare Lean invocation](../scripts/verification-entrypoint.test.ts) |
| A live status fact was restated away from its owner and outlived the change that falsified it: an ordered-work item kept a blocked label for eleven commits after the same document recorded it unblocked, a capsule Status section copied an absence inventory that three landed lanes made wrong, and a capsule denied the existence of a semantic-core test that the implementation map linked in the same commit. The third instance also refuted the first disposition's reach: the guard binds statements inside one document and cannot see a capsule contradicting a different owner, so the correction removes the duplicated claims rather than re-synchronizing them. The fourth instance is the same mechanism at a stage boundary: the Sub-Process boundary Timer capsule was handed to closure review with a Status paragraph still denying the scenarios, seeded mutations, and Temporal witness that the same commit contained, contradicting both its own owner-inventory row and two routed owner documents, and the cold reviewer found it rather than any guard. It also shows why the count matters more than the prose: the third instance had already recorded *delete the copy* as the correction, and the copy was nonetheless re-synchronized twice during this capsule instead of removed | [Interrupting Activity boundary Timer](capsules/ACTIVITY-BOUNDARY-TIMER-SPEC.md) capsule | 4 | `executable guard` | [ordered-work status agreement](../scripts/plan-status-consistency.test.ts) |
| A checklist item was corrected only where attention already was, leaving its siblings unaudited: a capsule's Required evidence list was amended for the two scenarios under discussion while two other Required items stayed listed and absent, a stale claim was fixed in the owning map but not in the capsule that copied it, one Required-Lean list was corrected while the sibling list and matrix row restating the same obligation were not, and the correction for that then declared itself the single owner while one sibling enumeration survived. The root cause is stating one obligation in several places, so each correction collapses the duplication instead of synchronizing it. `unguardable` states a limit rather than a preference: a gate can compare two statements it is told to compare, which is why the intra-document status guard exists, but it cannot decide whether two differently worded passages assert the same obligation, and the mechanism is a property of which copies the corrector opened rather than a detectable artifact state. Four consecutive instances refuted the prose disposition, two of them *inside* a correction for this same mechanism: one declared itself the single owner while a sibling enumeration survived, and one explained a rank movement by naming two of the three rows that caused it. Neither is reachable by a gate — the first needs semantic equivalence between differently worded passages, the second is prose about which rows sit above a figure — which is why question 8 asks for the files actually read. The fifth instance is the sharpest yet: a review correction reworked a guard table's preamble and added a row to it while leaving a *different* row in that same table asserting a timing rule the same change had just superseded, so attention reached the table without reaching its contents. The sixth instance happened inside a correction commit closing a review finding about this very class: renaming the joint logical-time bound so it no longer claimed to separate the arms was done at the theorem and its docstring, while three prose sites in three documents kept calling it "the logical-time separation law", one of them a capsule owner row that then appended the correction and so disagreed with itself. The warm correction audit found them, and a sibling sweep found a fourth the audit had not listed. The class stays `unguardable` because no repository fact distinguishes a label from a claim, but the sweep is cheap and belongs in the correction itself rather than in the next review round. The seventh instance is three siblings at once, in the commit that answered a reviewer's finding about a *different* row: a blast-radius count and a misattributed decision reference were corrected in two owners and left standing in a third, and the sentence that declared a fact owned elsewhere was followed four lines later by a paragraph restating it — the same self-contradiction this row already records at instance five. The commit incremented the partial-measurement row correctly and left this one untouched while committing three fresh instances of it, which is the mechanism applying to the ledger itself. The sweep this row prescribes is one command — grep the corrected phrase across `docs/` before committing — and running it afterwards found all three in one pass. The eighth instance is the same shape at a locus that inverts its usual direction: a closure correction retracted a routing claim at the theorem, its docstring, and the capsule bullet, while the copy that survived sat in [IMPLEMENTATION-MAP.md](IMPLEMENTATION-MAP.md), the owner of proof status, so the stale sentence was the *authoritative* one and the corrected capsule read as the deviation. The closure reviewer found it and confirmed the count belongs on this row rather than a new one, because in-correction position is already part of this row's shape and a second row would understate an `unguardable` count that the escalation rule reads. What the instance adds to the prescription is the direction: the sibling sweep must reach the owning document, not only the documents that copy from it | [Interrupting Activity boundary Timer](capsules/ACTIVITY-BOUNDARY-TIMER-SPEC.md) capsule | 8 | `unguardable` | [question 8](#self-assessment-questions) |
| A same-change obligation a capsule wrote for itself was never performed, in the one place the capsule had already predicted no gate would look. It stated "the ledger requirement is new: `BPMN-BOUNDARY-TIMER-01`", scheduled the row for its implementation, and recorded that the closed-slice guard is one-directional so nothing would fail from the identifier's absence. The row was then missed at implementation and again at graduation, and the capsule's own prediction is what makes this worse than an oversight: a named blind spot was documented rather than closed. Checking the siblings kept the guard honest — three other capsules cite no requirement at all because they dispose a CIB extension or a runtime representation, so requiring a row from every capsule would have manufactured dispositions instead of enforcing stated ones | [Interrupting Activity boundary Timer](capsules/ACTIVITY-BOUNDARY-TIMER-SPEC.md) capsule | 1 | `executable guard` | [capsule-cited requirement resolution](../scripts/document-reviewability.test.ts) |
| A cost conclusion was drawn from one resource dimension while the change consumed another, and the unmeasured dimension took the host: converting 40 Lean fixtures to `decide +kernel` was measured in CPU only, declared affordable at about 5% of gate CPU, and then a branch added to the `fireTimer` dispatcher that every kernel-decided timer fixture reduces through drove one build past 7 GB of resident memory. Two halves compound and both are recorded: the kernel reduces in RAM so `+kernel` trades memory as well as elaborator time, and a dispatcher branch is re-reduced by each downstream decided fixture rather than once. `review question` is admissible at one instance and the question now asks which dimensions were measured; the mechanism is not a repository fact, because no committed artifact records that a build was run without a memory ceiling | Interrupting Sub-Process boundary Timer capsule, observed while landing the Lean deadline arm | 1 | `review question` | [question 4](#self-assessment-questions) |
| A performance change was landed on a one-dimensional measurement and its second dimension took the host: converting every decided fixture to `decide +kernel` was justified by a −48% CPU figure, and the same change made a clean `lake build` peak at 7978 MB across four concurrent `lean` processes, because kernel reduction holds its terms in resident memory while Lake scales its build pool with core count. Three follow-on errors compounded it and each is part of the mechanism: the regression was twice attributed to the wrong commit by reasoning about authorship instead of reading timestamps, a polling watchdog was trusted to bound a process that allocates gigabytes between samples, and two conversions were reverted before the actual lever was found, so the revert cost CPU without recovering memory. The owner's recollection of the nine-hour window located the cause faster than three of the agent's own analyses | Kernel-decide conversion `ee193a3`, observed one day later | 1 | `executable guard` | [pinned gate parallelism](../scripts/verification-entrypoint.test.ts) |
| A correction-audit loop ran three rounds with nothing bounding it, because each round closed its predecessor's findings while the correction itself introduced a new required defect. Finding counts fell — eight, then two, then one — so each round looked like convergence while the loop had no terminating condition at all: every correction is fresh prose and fresh prose can carry a fresh defect. The cost was roughly 640,000 reviewer tokens on one planning document, and the owner stopped it rather than any rule. The bound is now explicit at two audits per stage, after which remaining advisory findings are applied unaudited and open required findings go to the owner with the finding stated. It is recorded as a review question rather than an executable guard because the receipt stores one correction-audit commit and no round count, so no repository fact distinguishes a first audit from a third | Non-interrupting boundary Timer proposal review | 1 | `review question` | [two-round audit bound](TESTING-SPEC.md#independent-cold-review-gate) |
| A gate in the default lane required an input that this repository's own cache lock marks optional and disposable, so its verdict depended on which machine ran it. The normative-reference guard read the Markdown conversion of the OMG PDF, passed every local run, and failed both hosted platforms on the first push after it landed; the `verify` scope is declared complete with only the hash-bound official corpus, so that conversion is never provisioned there. Two properties hid it for two days: the conversion exists on the maintainer's machine, and no push ran the workflow between the guard landing and the failure. The fix removes the dependency instead of provisioning the artifact, because provisioning would have added a PDF-conversion toolchain to every runner to reproduce a file whose bytes depend on the converter version. References now resolve against a tracked project-authored label digest, checked equivalent to the previous regex over all 128 declared references and 576 probes before the swap | Normative-reference guard `3d86091`, observed on CI two days later | 1 | `executable guard` | [optional-cache independence](../scripts/contributor-setup.test.ts) |
| A repository-wide prohibition was added to a governing document without measuring whether the repository already violated it, so the rule was false in the commit that introduced it: "`native_decide` remains excluded, because it moves trust to the compiler" landed while 56 tactic sites in six modules were live, five of them in maintained Conformance modules, each adding a `native_decide` axiom to its theorem's footprint. Writing a prohibition beside a real improvement is the mechanism — the same commit correctly removed the elaborator from the trusted path for 231 sites, and the adjacent sentence generalized that into an absolute the tree contradicts. A new rule about the contents of the repository must be introduced together with the count it claims, which is what makes it a ratchet instead of an aspiration. The second instance fell outside that guard's reach, which covers the original `native_decide` claim only: a documentation-wide prohibition on counting artifacts in prose was written into the writing rules from a one-document sample, then withdrawn when completing the sample measured five of six counts across [CLAUDE.md](../CLAUDE.md) and [TESTING-SPEC.md](TESTING-SPEC.md) as exact, with the single error one fact duplicated into both owners rather than a counting failure. It was withdrawn before it landed, so no false rule shipped, but the deciding measurement was one command away before the rule was written | Interrupting Sub-Process boundary Timer capsule, observed at its semantic checkpoint | 2 | `executable guard` | [recorded native_decide exception set](../scripts/lean-source-contracts.test.ts), [question 3](#self-assessment-questions) |
| A composite identity was built on a safety assumption never checked against the domain's own validator: two host schedulers keyed durable timer ownership on parts joined by `U+0000`, beneath a comment asserting that no identifier value could then forge another key, while the shared wire domain admits that scalar — so moving the separator into an identifier produced a byte-identical key for two different committed timers. The comment was the defect's own justification, which is why review did not catch it; the guard rejects the observable shape instead, because whether a string becomes a key is not statically decidable while a separator join is | [Event-Based Gateway](capsules/EVENT-BASED-GATEWAY-SPEC.md) capsule, observed during the shared host readiness extraction | 1 | `executable guard` | [separator-joined composite keys](../scripts/source-hygiene.test.ts) |
| A comparative claim about a maintained table was written without reading the column it compares against: a documentation figure called the largest in its ledger while a larger row sat four lines above, a code figure placed between "the two nearest comparators" while a third lay between them, and a replacement rank and row count both guessed rather than computed. The guard checks the one unambiguous claim form and caught the third instance on its first run; free-form comparative prose stays a review obligation because "nearest recorded" cannot be read as nearest-in-value without churning five historical rows on a contested reading. The fourth instance was outside that guard's reach and is recorded as such rather than as covered: a capsule's own guard table was introduced with "two rows are new assertion work and are marked" while exactly one row carried the mark. The guard binds rank claims in the cost ledger, not a count claim a capsule makes about a table it owns, so this instance was caught by review. The fifth instance is the most instructive, because it occurred *inside* the guard written to answer this class: a graduated specification's stale headroom table was described as carrying "three wrong figures out of four" after only the first five of its eight rows were measured, and all seven stale rows were found by the reviewer rather than the author. The lesson is narrower than "read the table": a partial measurement produced a confident total, so the count must come from the same command that produced the comparison, never from the rows that happened to be on screen. The sixth instance is that lesson without a table at all: a repository-wide conversion was reported as "all 199 tactic-position sites across 25 modules" where the guard's own predicate counts 232, while the same paragraph's "131 of the 199" sub-figure was exactly right — so a real measurement of one part sat beside a total that no command produces. The seventh instance is the same shape with the count *inherited* rather than sampled, which is the cheapest way to acquire one: a fix for a parser-coercion defect was recorded as reaching three `supported` requirement rows because the report that found it said three, and the reviewer of the fix measured five — the same coercion fools any reader that admits on the coerced value, and `instantiate` is a second such attribute in two more readers, both of whose requirement rows state the qualifier the defect breaks. A borrowed total reads exactly like a measured one in the sentence it lands in, and the command that would have produced it — grep the readers for a comparison against `false` — is the same command that established the defect. The eighth instance is the same shape in a plan entry that cites its own evidence: an ordered-work item recorded four admitted-set sentences across three closed specifications as "wording precision, not a defect", and named a probe as the reason. That probe had compared `0` against `false` only. Probing the other clause in the same sentences refuted the premise, because one of those specifications states that lexical `cancelActivity="true"` is tolerated and the implementation rejects it. The entry's own words carry the tell: "verified by probe" was attached to one clause while the conclusion covered all four | [Interrupting Activity boundary Timer](capsules/ACTIVITY-BOUNDARY-TIMER-SPEC.md) capsule | 8 | `executable guard` | [cost-ledger rank claims](../scripts/capsule-cost-comparison.test.ts), [graduated-specification headroom figures](../scripts/document-reviewability.test.ts) |
| An infrastructure change was reported as landed on the strength of a green run, where green was insensitive to whether the change did anything. The corpus cache was declared to remove the third-party download from every build; `actions/cache` rejects a glob containing a `..` segment, so the restore reported an ordinary miss, the post-job save wrote the refusal as a warning, and nothing was ever stored — on both platforms, invisibly, because a cache is a pure optimization and its absence cannot fail a gate. `gh cache list` showed the entry missing and is the observation that should have preceded the report. The distinguishing feature against the guard-formulation row is that no defect was seeded and none could be: the artifact rejects nothing, so its only honest evidence is the effect itself. The guard rejects the segment in any workflow cache path, which is the one repository fact available; the resolved value behind an expression is not, so the workflow's absolute-root derivation is anchored separately. The second instance is the same insensitivity inside a *seeded* check, which is the form that looks most like diligence: a test locking the pinned parser's boolean coercion asserted only `notEqual(status, Accepted)`, which the guard's own refusal satisfies as readily as the coercion's, and the seed run to certify it flipped the expected outcome rather than mutating the mechanism. Flipping an expectation tests the assertion; only mutating the guard reaches what the assertion claims to observe, and that mutation left the test green. The reviewer's structural correction generalizes past this case: an assertion that a source is *admitted* cannot go vacuous, because both hazards turn accept into reject, while a reject-assertion can — so a lock should prefer the admit shape, and a reject shape needs the exact diagnostic rather than the bare non-acceptance. The third instance is a *predicted* detection that was never tested, recorded in a capsule preflight as "registering the scenarios is what will expose it if it does not" about a boundary deadline reaching its host scheduler. Making the family's `ownsDeadline` never match left the complete Temporal gate green at 147 of 147, including both registered schedules and the Worker-absence run, because the generic fallback path and the scheduler agree on every separated order and a registered schedule can express nothing else. The prediction had been inherited from a sibling's lane rule whose red gate came from an *absent host* rather than an unclaimed deadline, so a real observation was carried across to a mechanism it never covered. A claim that a gate detects something is itself a claim, and the only thing that establishes it is mutating the mechanism and watching that gate fail; this one was caught before closure by doing exactly that, and the missing witness it exposed now fails three assertions under the same mutation | Corpus cache `994e6eb`, observed on the run that shipped it | 3 | `executable guard` | [relative-segment cache paths](../scripts/contributor-setup.test.ts) |

## Update rule

At each capsule closure and each session handoff, answer the questions above and either add a row or record that none applied. When a mechanism already has a row, increment its instance count instead of adding a near-duplicate — the count is the signal the escalation rule reads. Keep the finding text describing the mechanism, not the incident, so a second occurrence in different code still matches the same row.

A row is retained after its guard lands. Removing it would hide the recurrence history that justified the guard, and the guard itself is the reason the count stops growing.
