# Repeatable Event subscription lifetime proposal

## Status

Lifecycle: draft
Review: pending

## Question and boundary

How do the selected RC Message and Timer subscriptions remain available for the lifetime of their catching locus, allow overlapping non-interrupting handler work, resolve completion/trigger races explicitly, and disappear with exactly the scope or Activity that owns them?

This proposal owns the subscription-lifetime account required by [the RC content boundary](../PLAN.md#mue-release-candidate-critical-path). Its previously approved lifetime account is retained, but the [scheduling proof-domain amendment](#scheduling-proof-domain-amendment) requires a new cold proposal review before dependent implementation continues. No production capability is established. A root-only repeated-handler example cannot substitute for the selected catch, Activity-boundary, and nested-cleanup boundary. Existing profiles and their same-activation refusals remain unchanged.

The [dependency findings](../INTERNAL-COMMUTATION-PROPOSAL.md#subscription-dependency-findings) establish the starting point: exact Activity ownership already exists; ordinary User Task identities can distinguish overlapping handler instances; the one-shot Timer owner cannot directly replace a fired identity; and the current rollover fence prevents continuation while that owner remains armed. These are implementation constraints, not a selected scheduling policy.

## Normative basis and compatibility

[BPMN 2.0.2](https://www.omg.org/spec/BPMN/2.0.2/PDF), with locally verified provenance in [the reference owner](../reference/bpmn-2.0.2/README.md), supplies the semantic basis. Clause 13.5.2 makes a normal-flow catch consume an occurrence and continue. Clause 13.5.3 distinguishes interrupting and non-interrupting boundaries. Table 10.91 permits concurrent instances of a non-interrupting handler; Table 10.101 distinguishes duration, date, and recurring-interval Timer expressions. Clause 13.2 governs completion in the presence of remaining work. The existing [Sub-Process Timer account](SUBPROCESS-BOUNDARY-TIMER-SPEC.md) owns the reviewed nested-cancellation interpretation; this proposal must preserve it.

Repeated boundary triggering is different from repeatedly entering a normal-flow catch. A normal-flow catch consumes its current wait; another control-flow arrival creates another wait. A non-interrupting boundary remains associated with its still-active host and may produce another handler path without completing the earlier path.

No CIB target, subscription compatibility relationship, or oracle result is selected. The profile reuses only the already reviewed User Task lifecycle and identity premises, `CIB-AGR-0001` and `CIB-OP-0001`, under their existing [register boundaries](../CIB-BPMN-RELATION-REGISTER.md). This follows the [Message-boundary profile](../../profiles/bpmn-2.0.2-activity-boundary-message-draft/README.md) and [monitored-Timer profile](../../profiles/bpmn-2.0.2-non-interrupting-boundary-timer-draft/README.md): naming reused premises is not evidence for repetition, concurrent handlers, nested cleanup, or a CIB race policy. Existing key-correlation and payload profiles retain their own contracts. A new requirement for a CIB-specific delivery, buffering, expression, or race rule must return to the register rather than becoming implicit policy here.

## Required surface and source admission

The scope is the selected RC capability, not a new general BPMN profile:

| Locus | Required lifetime obligation | Existing contract retained |
|---|---|---|
| Intermediate Catch Message and Message-addressed Receive Task | One wait per reached occurrence, exact consumption, fresh identity on later arming, complete owner cleanup | [Intermediate catch](INTERMEDIATE-CATCH-MESSAGE-SPEC.md), [Receive Task](RECEIVE-TASK-MESSAGE-SPEC.md), and separately selected payload/correlation accounts |
| Intermediate Catch Timer | One exact deadline per reached occurrence, stale-fire refusal, withdrawal with its owner | [Intermediate Timer](INTERMEDIATE-CATCH-TIMER-SPEC.md) |
| Message boundary on an admitted Activity host | Interrupting victory withdraws the host; non-interrupting delivery preserves the subscription and can create overlapping paths | [Current User Task boundary](ACTIVITY-BOUNDARY-MESSAGE-SPEC.md); other host kinds require an explicit admission decision |
| Timer boundary on an admitted Activity host | Preserve one-shot interruption/monitoring, add recurring non-interrupting firing with exact successor identity and deadline | [User Task interruption](ACTIVITY-BOUNDARY-TIMER-SPEC.md), [monitoring](NON-INTERRUPTING-BOUNDARY-TIMER-SPEC.md), and [Sub-Process interruption](SUBPROCESS-BOUNDARY-TIMER-SPEC.md) |
| Subscription inside an admitted embedded scope | Normal host completion withdraws future triggers without cancelling already spawned sibling paths; regional cancellation removes all work in the cancelled region and preserves outside work | [Activity ownership](../ACTIVITY-OCCURRENCE-OWNERSHIP-SPEC.md) and [Terminate](TERMINATE-END-EVENT-SPEC.md) |

Source admission uses the existing profile-parameterized graph validation with the following capability restrictions, rather than another whole-topology disjunct. These restrictions belong to the new profile; existing profiles retain their admission. The presence of an existing operation in the IL grants neither source nor Temporal admission.

| Capability | Proposed admission and reason |
|---|---|
| Process and scopes | One private executable Process with one None Start, optionally one ordinary embedded Sub-Process with its own None Start. No second child scope, deeper nesting, Call Activity, Event Sub-Process, Transaction, or Multi-Instance body enters this profile. This retains the existing ordinary embedded-scope depth. |
| Passive work | Ordinary empty-submission User Tasks, payload-free operation-addressed Intermediate Catch Messages, Message-addressed Receive Tasks, and exact `PT1S` Intermediate Catch Timers. Payload, data mediation, correlation-key, effect, incident, and other Task profiles remain independently available, without being composed into this new profile. |
| Control flow | Acyclic fork forests in each scope: one incoming flow per ordinary node, no incoming flow at Start/Boundary Events, one outgoing flow per nonterminal node except divergent Parallel Gateways, at least two distinct outgoing flows at each such Gateway, and one incoming flow at each None or Terminate End. No merge, join, conditional branch, implicit multi-incoming Task, or control-flow loop. Each boundary route is a distinct additional root in the same scope as its host. All nodes and flows satisfy the existing reference, ownership, reachability, and co-reachability checks. |
| Activity boundary | At most one Boundary Event in the definition. Its host is an ordinary User Task for Message or Timer, or the embedded Sub-Process for Timer. Both interruption values are admitted explicitly. Message-boundary child-scope bodies remain excluded; a Message boundary on a User Task *inside* the child is admitted. |
| Timer population | At most one Timer Event Definition in the entire definition, counting normal-flow and boundary loci together. A recurring boundary therefore coexists with passive waits, never another Timer or effect. Timer waits cannot appear downstream of a repeated boundary route, where repetition could instantiate concurrent occurrences of that one definition. |
| Repeated handler path | Its fork-forest descendants may contain ordinary User Tasks, payload-free Message catches/Receive Tasks, and None/Terminate Ends. They cannot contain a Sub-Process, another boundary host, or a Timer. This permits overlapping ordinary handler instances without introducing concurrent activations of a child scope or managed Timer. |
| Termination | A Terminate End is directly reached from an externally resumed wait or a triggered Boundary Event, never through an automatic fork/chain after that resumption. This prevents a fresh internal fork from offering containing-scope termination against new sibling arming as an undeclared observable choice. Normal End branches may still finish independently. |
| Internal closure | Every Start and single external resumption must satisfy the burst bound below before source/Program admission. Size alone, XSD validity, and acyclicity do not establish this bound. |

The scope and Timer cardinalities preserve the selected existing host loci while admitting the missing lifetime compositions. They do not dispose broader BPMN behavior. Multiple Message subscriptions may coexist through distinct catch nodes or repeated handler instances, each with its own published address. The one boundary subscription stays bound to its one host occurrence.

### Resumption and closure bound

The production closure limit is eight internal operations as inspected on 2026-09-24 in [the runtime owner](../../packages/semantic-core/src/semantic-process-runtime.ts). This proposal does not raise it. Admission must bound the entire burst, not the longest individual branch: a fork arms every branch, so summing only path length undercounts work.

Compute a bound for Start and for the output of every externally resumable wait or Boundary Event. Traverse each newly marked fork-forest branch until its first wait is armed, counting the arm and every preceding internal operation. Entering a Sub-Process also traverses its child entry. If that child burst leaves no live wait, include its completion and continue through the parent's normal output. For a resumption inside the child, additionally consider the case where all other child work has already quiesced, so completion and the parent's continuation belong to the same burst. Count each possible ancestor completion, including root completion. A Terminate contributes its own operation, the retained scope's completion, and any parent continuation; previously waiting cancelled work contributes no extra internal execution. Compare the maximum complete-burst bound with the runtime limit, including the evaluator's exact fuel convention.

No admitted burst can reach one operation twice: ordinary control has no merges or cycles, each newly marked forest branch has disjoint descendants, and repeated handlers introduce one new external resumption at a time. Earlier handler instances are already at stable waits and do not add an unbounded internal tail to the new trigger. The compiler, standalone Program admission, and Lean's new-profile admission must independently enforce the same structural restrictions; the known unequal legacy raw-Program admission domains are not evidence for this new account. The burst checker needs a separating over-limit fork witness and a boundary-equal witness before implementation approval.

Stable running states have a finite collection of passive User Task and Message waits, optionally the single managed Timer, optionally the single child scope, and at most one Activity record for the boundary host. No live-token-only state is an accepted stable resumption. The host must support every such admitted population; one-scheduler/one-wait assumptions in the current adapter must not silently narrow this set.

The proposed recurrence grammar is `timeCycle` containing `R/PT1S` on a non-interrupting boundary, with no `timeDate` or `timeDuration` sibling. Elsewhere the admitted Timer form remains `timeDuration` containing `PT1S`. This isolates recurrence using the existing duration magnitude; it does not claim general ISO-8601 expression support. Omitted `cancelActivity` normalizes to true; explicit `true`, `false`, and `0` retain their specified values. The existing [source coercion guard](../../packages/bpmn-source/src/compile.ts) still refuses `1`, entity-encoded, and whitespace-padded forms because the pinned parser does not preserve their value. They are deferred valid BPMN spellings, not different interruption semantics. The draft's earlier blanket normalization wording would have required an unrelated XML decoding change; this capsule selects no such change. Finite recurrence counts, calendar arithmetic, expressions, multiple boundaries on one host, and recurrence on a Timer Start are not silently selected. Whether this expression boundary and the final host/graph capabilities discharge the full RC row is an explicit proposal-review question, not inferred from a passing witness.

## Definition and runtime representation

The proposed profile identity is `bpmn-2.0.2-repeatable-event-subscriptions-draft`. The two missing operation kinds are `awaitMessageMonitoredUserTask`, carrying the existing Message-bounded Task's input, task, and boundary-Message fields, and `enterMonitoredScope`, carrying the existing bounded scope's input, child entry, child-scope identity, and boundary Timer. Their non-interrupting meaning is carried by the operation kind, consistent with the existing monitored User Task. Existing interrupting operations are reused in the new profile with the admitted composition rules.

The immutable Timer arm gains one optional closed field, `recurrence: "repeating"`, admitted only on the two monitored Timer operations under this profile. Absence retains one-shot meaning and every old definition's exact bytes. `durationMs: 1000` supplies both first delay and recurrence period; no second period or count is stored. The existing monitored User Task operation therefore needs no additional operation kind solely for recurrence. Checked graph projection, schema, Lean constructor/codec, lowering, and independent Program admission must agree on the absent-versus-repeating distinction.

The checked `timerBoundaryEvent` keeps its common identity, attachment, interruption, and output fields, with exactly one expression field: existing `durationLiteral: "PT1S"` or new `cycleLiteral: "R/PT1S"`. Both fields together, neither, or any other literal reject. The new arm requires the new profile and non-interrupting disposition. Lean represents this choice with a closed duration/cycle sum carrying the original string and independently normalizes it; the old JSON spelling remains unchanged. Checked `messageBoundaryEvent.interruption` widens to the existing two-value disposition only under this profile. These are checked-source distinctions, not parser objects or a normalized duration passed off as source evidence.

RuntimeState, Activity identity, and attached-handler shapes need no new collection or subscription store. Recurring Timer state is its existing exact wait/deadline plus the immutable recurrence declaration. Persistent Message state is its existing exact wait plus the new operation's lifetime rule. The existing independent counters allocate handler Task/Message occurrences and replacement Timer occurrences. The proposal changes no public stimulus arm; source identity and semantic-profile identity continue to distinguish admitted meaning.

The public E1 operation-kind enumeration adds the two new operation names. E2 uses its existing scope, User Task, subscription-wait, and instantaneous Boundary Event forms. This is an additive public enum migration through every strict reader, including Product 2; absence of a new field does not make that migration optional.

## Proposed lifetime and identity contract

`ESL-OWN-01` — Reuse the existing Activity record as the ownership edge between an exact body and its attached waits. A firing never reconstructs ownership from equal activation ordinals. The record persists while its body remains live, including after a one-shot handler has been consumed.

`ESL-MESSAGE-01` — A non-interrupting Message boundary keeps one subscription identity for the lifetime of its host occurrence. Each distinct accepted Message command consumes one Message occurrence, produces one boundary-path token, and leaves the subscription available. Two distinct commands targeting that live subscription may both commit while the first handler path remains active. Reusing a command identity with identical content follows the existing command-recovery contract; it is not a second Message occurrence. Conflicting content remains an identity conflict. A delivery after withdrawal is stale and preserves semantic state.

This choice avoids requiring callers to rediscover a new subscription between every pair of Messages. It also avoids making two already accepted, distinct deliveries compete for a one-shot subscription accidentally. Normal-flow Message catches and interrupting boundaries retain their consuming behavior. Payload and key-correlation bindings cannot be inherited by the new boundary arm without their own explicit source and data account.

`ESL-TIMER-01` — Each recurring firing consumes its exact Timer occurrence and atomically installs the next occurrence while preserving the host. The successor draws a fresh Timer activation from the existing high-water counter and replaces the old tagged attachment in the same Activity record. Its logical deadline is the preceding deadline plus the admitted period, not the time at which handler work finishes. The old firing identity cannot fire the successor. Overflow or capacity refusal rolls back the complete command, including the new path and replacement wait.

`ESL-SPAWN-01` — Every committed non-interrupting trigger produces one token on its boundary Sequence Flow. Handler paths use ordinary occurrence allocation and token multiplicity; they do not reuse the host's task identity or overwrite an earlier handler occurrence. Handler completion does not close the subscription or complete the host.

`ESL-CLOSE-01` — Normal host completion atomically withdraws its remaining attached subscriptions and retires its Activity record. Already spawned paths live in their declared containing scope and remain runnable. Process or Sub-Process completion still requires all remaining work to quiesce.

`ESL-CANCEL-01` — Cancelling a region withdraws every subscription and spawned path owned inside that region, plus attached waits whose Activity body is removed even when the wait itself is parent-owned. Work outside that ownership closure survives. Cancellation never rewinds activation counters. Removing an inner region is not permission to cancel a handler path already running outside that region.

### Retained Sub-Process body amendment

The admitted Sub-Process-host/inner-Terminate composition requires resolving [the documented `AOO-RETAINED-BODY-01` gap](../INTERNAL-COMMUTATION-PROPOSAL.md#regionalregional-outcome). Both [the core cancellation owner](../../packages/semantic-core/src/semantic-process-scope-cancellation.ts) and [the Lean owner](../../BpmnSemantics/SemanticProcess/ScopeCancellation.lean) currently withdraw an Activity whenever its child-scope body belongs to the selected region, even when Terminate deliberately retains that exact scope for normal completion. The resulting live child has lost the record and deadline required by bounded completion. Weakening the missing-pair refusal would conceal the ownership error.

`ESL-RETAIN-01` proposes the replacement withdrawal criterion: remove a record when its owner is in the cancelled region, or when its child-scope body is actually among the scopes selected for removal. Under the `retain` disposition, the exact selected root is excluded from the latter set; its descendants are not. Under `remove`, the selected root and all descendants are included. Classify these sets from the predecessor and explicit disposition before mutation, never by differencing successor state. Task-body records owned inside the retained root still disappear because their owner is cancelled.

Consequently, terminating the contents of a Timer-bound Sub-Process retains its parent-owned Activity record and attached deadline through that internal step. Ordinary child completion then withdraws both through the exact declaration and publishes the normal continuation. Removing the Sub-Process itself still withdraws both immediately. A handler already spawned in the parent survives child-content termination; a handler inside the cancelled child does not. No external callback can interleave between Terminate and the subsequent completion in one command closure.

This is a proposed material amendment to `AOO-CANCEL-01`, not a claim that its current contract already means this. Independent proposal review must cover the changed withdrawal predicate, prepared regional read/write sets, retained publication associations, and both semantic accounts before any implementation. The existing Activity owner and accepted guarantees remain authoritative until that amendment is approved and implemented atomically.

At `1c8a5aa5`, the root re-ran the constructed bounded-scope diagnostic with `ChildEnd` replaced by Terminate: both predecessor and successor pass runtime validity and open projection; the child scope survives, its Activity/deadline disappear, and actual bounded completion returns no successor. The whole-subtree removal control removes all three. Receipt `/tmp/bpmn-subscription-retained-body-probe` records the result. This confirms the existing composition gap, not an admitted-profile failure or new capability.

## Commands and publication

The semantic core continues to receive an explicit ordered sequence of stimuli. Completion then trigger and trigger then completion are distinct valid schedules; the first can prevent handler creation, while the second can leave a handler running after host completion. They are a checked non-law of commutation, not candidates for independent batching.

`ESL-ORDER-01` selects an explicit new-profile host order. First preserve activation-batch order using the existing pinned activation-tagging/drain mechanism. Within one closed batch, process accepted User Task completions first, Message deliveries second, Timer firings third, and incident commands fourth. The Message class includes `DeliverMessage`, `DeliverPayloadMessage`, and `DeliverCorrelatedPayloadMessage`; the incident class includes `RetryIncident` and `CancelIncidentProcess`. Within each class, compare command identities with the existing canonical string order, including across the two incident kinds. The complete ordered batch is fixed before its first semantic command executes; each command then runs its full closure and publishes or rejects before the next begins. Deduplicated retries and identity conflicts are resolved by the existing recovery ledger before scheduling and do not become new contenders.

Completion-first is a profile choice: when both inputs are co-ready, finish already completed work before spawning another reminder path. It is not a BPMN-wide priority or a claim about physical arrival time. An earlier activation's trigger still precedes a later activation's completion. Two distinct Messages in one batch both reach a still-live non-interrupting subscription; sorting does not collapse them. An invalid completion still reaches semantic admission, rejects without mutation, and cannot suppress a later valid trigger. Completions of sibling tasks are included because their closure can terminate the subscription's containing scope. All accepted ingress for this profile passes through the same ordering boundary, including stale and unrelated inputs; a side queue must not bypass it.

Transport validators already accept well-shaped payload/correlated Messages and incident Updates without establishing their semantic applicability. Their inclusion in these classes grants no payload, correlation, or incident capability: the new profile rejects those commands semantically through the existing result and external recovery paths. The scheduler must drain already accepted commands even if an earlier command completes the process or requests rollover, and settle every accepted Update handler before terminal return or Continue-As-New. Start initializes the execution before ingress scheduling; effects cannot produce host callbacks in this profile. The listed classes therefore cover every accepted external command and native callback available to this profile.

The core's two sequential schedules remain distinct, and the host's co-ready policy selects one allowed schedule explicitly. Legacy profiles retain their typed same-activation refusal. The new policy neither imports the SDK's callback order nor changes public/private internal scheduling admission. Its live refinement witness must permute Signal/Update/Timer callback order, test multiple same-subscription deliveries, and show the same committed order, every caller's settlement, and replay.

Publication keeps three facts distinct: the open subscription, the instantaneous caught Boundary Event, and the handler's ordinary running occurrences. The [current Message occurrence tests](../../packages/semantic-core/test/activity-boundary-message-occurrence.test.ts) already distinguish the wait anchor from the transition anchor. For the proposed repeated Message arm, the subscription wait remains open across deliveries while each committed delivery receives a distinct transition-anchored event occurrence. Timer replacement publishes the successor open deadline, and each caught event remains instantaneous. A retry, stale callback, or rolled-back command creates no new committed occurrence or publication revision.

## Temporal hosting and refinement preflight

Message ingress uses the existing content-bound Signal/result recovery path and task completion uses the existing Update path. Neither transport acceptance nor a native Timer callback is a semantic commit. No Temporal Activity, Child Workflow, external effect, or new message broker is required solely to repeat a boundary handler.

The state relation must bind committed semantic state, command recovery, publication, and each managed native Timer to its exact live semantic Timer identity. A firing handoff has three distinct states: the old callback is reconciled; the semantic successor wait exists; and a native Timer for that successor is installed. Only the first and third are host facts. There must be no externally visible semantic gap between consumption and replacement.

The verified SDK pin in [SOURCES.md](../SOURCES.md) supplies the required primitives: [Workflow globals](https://github.com/temporalio/sdk-typescript/blob/2595d1b62cf5c3ff1748df0df2f9b303902bb31c/packages/workflow/src/global-overrides.ts) obtain `Date.now()` from activation time; [Timer implementation](https://github.com/temporalio/sdk-typescript/blob/2595d1b62cf5c3ff1748df0df2f9b303902bb31c/packages/workflow/src/workflow.ts) clamps nonpositive sleep to one millisecond and removes a cancelled Timer completion before emitting its cancellation command. Resolving a cancellation promise is not proof that the server has already persisted its command.

The whole-activation ordering premise is conditional. The pinned [VM host](https://github.com/temporalio/sdk-typescript/blob/2595d1b62cf5c3ff1748df0df2f9b303902bb31c/packages/worker/src/workflow/vm-shared.ts) splits Signals from other jobs on legacy replay without `ProcessWorkflowActivationJobsAsSingleBatch` (internal flag 2), draining microtasks between those batches. An inline microtask drain therefore cannot implement `ESL-ORDER-01` on those histories. New executions use and record that flag when Signal-containing activations need it; replay then preserves the required single batch. Activations without Signals take the single-batch path unconditionally and need no invented flag record. This new profile is admitted only on new pinned-SDK executions and their histories, with the existing [SDK activation-premise guard](../../packages/temporal-adapter/testkit/test/event-race-sdk-activation-premise.test.ts) and a real-service Signal/Update/Timer history/replay assertion enforcing that boundary. No old history is relabelled with the new profile, and no legacy policy is changed. Additional microtask turns are not a substitute for the flag.

The proposed host handoff clears the reconciled old native owner before installing its successor, retaining the owner's stale-callback suppression. A requested Continue-As-New may occur at that intervening host checkpoint only after accepted ingress and callbacks are settled. The committed successor wait must cross the Run boundary, so rollover cannot discard the next occurrence or reapply the prior firing.

`ESL-HANDOFF-01` selects a private due-time binding for the new profile. When a semantic Timer first becomes committed, before yielding or considering rollover, bind its exact identity and logical deadline to replay-safe Workflow time plus its remaining semantic delay. On recurring replacement, advance the old binding's physical due time by the same period as the semantic deadline and bind the new Timer identity. Do not restart the period from callback completion or from the successor Run's start.

The [private continuation metadata](../../packages/temporal-adapter/protocol/src/workflow-continuation.ts) gains a profile-specific optional field `subscriptionTimer`, required on successors of this profile and absent for every legacy profile. Its closed value is `{ protocol: "bpmn-lean.subscription-timer.v1", timer: null | { id: TimerOccurrenceId, logicalDeadlineMs: number, dueTimeMs: number } }`. Null is required exactly when committed state has no Timer. A non-null binding must match the sole committed Timer's complete identity and logical deadline, and both time values must be nonnegative safe integers. An absent, extra, wrong-version, wrong-identity, or wrong-logical-deadline binding refuses continuation. This private additive pre-release field is covered by existing individual and aggregate continuation budgets; it appears in no public receipt, observation, variable, or publication. Check newly computed due-time arithmetic before committing the affected command; unrepresentable arithmetic is an infrastructure invariant failure retaining the last committed publication, never a fabricated BPMN failure.

Before installing the successor native Timer, compute `max(1, dueTimeMs - workflowNowMs)`. The positive minimum yields a durable wakeup even when overdue. Missed recurring intervals are caught up one semantic firing and full command closure at a time, with fresh identities and capacity checks; they are neither collapsed into one event nor expanded in an unbounded synchronous loop. Physical lateness remains hosting delay and never replaces the exact semantic deadline in a firing stimulus.

An accepted native firing which does not commit needs an explicit disposition. If an earlier command in its fixed batch has withdrawn the exact Timer, its stale rejection is expected and produces no replacement. If the same exact Timer remains live after semantic rejection, fail with the existing `BpmnHostCapabilityInvariantViolation`, retaining the rejected cause privately. Core rollback or closure/projection failure instead retains `BpmnSemanticClosureFailure`; host capacity refusal retains `BPMN_WORKFLOW_CHAIN_CAPACITY_EXHAUSTED` and its existing budget details. All three preserve the last committed semantic state and publication. Do not re-arm the consumed native callback, mint another command ID, or spin on an overdue deadline: that would retry without a selected semantic retry rule and can repeatedly encounter the unchanged refusal. `FireTimer` is explicitly excluded from external command recovery; no Timer result is recovered from that ledger.

Preserve the core cause at the existing single-evaluation boundary in [advanceScenario](../../packages/semantic-core/src/scenario.ts). Its private `HarnessFailureScenarioStep` gains a required diagnostic with a closed `stage: "commandClosure" | "observationProjection"`, the actual `CommandResult.outcome`, and the existing `internalStepBoundExceeded` and `ambiguousInternalChoice` booleans. The first failure branch copies those facts before converting to observation-free HarnessFailure; the projection branch records the same result with its distinct stage. The Workflow consumes that diagnostic in its existing HarnessFailure branch before any publication or outcome recording, binding the failure details to the exact firing command, Timer identity, logical deadline, and last committed publication revision. It must not reevaluate the command. The semantic-rejection path uses the existing Terminal step's rejected outcome; the host-capacity path uses the existing candidate-preflight failure and capacity owner, preserving its budget, bound, observed value, and publication revision. The scheduler treats these dispositions as terminal host failures, never as permission to install a successor native Timer.

These diagnostics cross only the private core-to-host result and infrastructure-failure boundary. `runScenario` and public observation/publication consumers continue emitting the existing HarnessFailure with no command observation on rollback; public command receipts, ScenarioResult JSON, recovery eligibility, and continuation state gain no failed-Timer result. Separate witnesses must distinguish an expected stale rejection, unexpected rejection while the same Timer remains live, core rollback (including closure/overflow refusal), projection failure, and host-capacity refusal. Each must assert the original typed cause, unchanged committed publication, no invented semantic result or recovery entry, and no re-arming after a host failure. This is the bounded host's failure boundary, not a liveness guarantee beyond capacity.

Rollover must also work before an outstanding Timer fires. Fence new ingress through the existing lifecycle, close the current activation batch, and distinguish an already recorded firing from an unfired native owner. Resolve any accepted firing through the fixed batch; otherwise detach/cancel the native owner with generation-based stale-callback suppression. Drain all accepted commands, without installing another native Timer while fenced, and only then await caller-handler settlement. Waiting for handlers before processing their accepted commands would deadlock. Carry the resulting committed wait and due-time binding. A late callback from the detached owner cannot create a second firing, and cancellation cannot erase a firing from an accepted batch. The successor re-arms the carried identity against the carried due time. This extends rollover only for the new profile and retains every existing capacity bound and legacy fence. Actual native cancellation and replay witnesses must establish this handoff; the mock owner probe alone cannot.

The smallest real-service witness must fire at least twice while the host and first handler remain active, replace the Worker, force continuation between firings, recover a retried command, then complete the host and prove that future triggers stop while both spawned paths can finish. A nested cancellation witness must independently preserve outside work. Replay covers every Run, and native Timer cancellation must not become a BPMN cancellation fact.

### Diagnostic preflight results

The 2026-09-24 preflight at source commit `1c8a5aa5` tested the proposed mechanisms outside production. Receipts below are local diagnostics; they do not register a profile, retained scenario, or refinement claim.

| Question | Observation and limit |
|---|---|
| Can the selected source shapes fit the unchanged closure limit? | Six XSD-valid candidate models cover nested Message/Timer boundaries, normal catches, Receive Task, and a recurring Timer on a Sub-Process with outside work. The proposed lowering cost model gives a maximum burst of seven; separate exact-eight and over-limit-nine fork controls distinguish the boundary. The actual closure kernel accepts eight operations and atomically refuses nine; an admitted existing parallel Start commits with its exact fuel and rolls back with one fewer. Receipts: `/tmp/bpmn-subscription-source-bursts`, `/tmp/bpmn-subscription-source-bound-xsd`, `/tmp/bpmn-subscription-closure-bound-probe`. Production compilation of the new profile remains required. |
| Does the retained-body defect affect a valid predecessor? | A constructed valid and projectable bounded child state loses its parent-owned Activity and Timer after inner Terminate while retaining the child. Its successor still validates and projects, but exact ordinary completion refuses the absent Activity record. Removing the whole child is the control. Receipt: `/tmp/bpmn-subscription-retained-body-probe`. This reproduces the mechanism; it is not source admission for the composition. |
| Does the SDK support the selected order? | All 24 permutations of completion, two Messages, and Timer pass on current execution and on flagged replay: 48 direct-VM cases. Same-class IDs include U+E000 and U+10000 to distinguish canonical scalar ordering from UTF-16 ordering. Updates settle and both Messages remain. An unflagged replay control produces the legacy split and fails the proposed whole-batch order, establishing the flag premise above. Receipt: `/tmp/bpmn-subscription-host-preflight-final`. |
| Can later callbacks remain queued while earlier callers are unsettled? | Four additional direct-VM runs cover current/flagged replay and reversed callback order across three activations. All three Update callers remain pending until the final drain; the queue retains activation order even when later command IDs sort earlier, then settles all callers. Receipt: `/tmp/bpmn-subscription-queued-batches-preflight`. The same run repeats the ordering and handoff cases above. This is a readiness-buffer test; production command recovery and capacity accounting remain required. |
| Can a Timer cross the proposed rollover checkpoint? | The same direct-VM receipt exercises the actual native Timer owner: detach an unfired Timer at Workflow time 400 for due time 1,000; preserve either callback order for a firing at 1,000 and carry its successor due at 2,000; restore the carried deadline with a 300 ms remaining delay or a 1 ms overdue wakeup. This tests cancellation, callback acceptance, and delay calculation, not the new semantic transitions, continuation codec, live service, or retained-history replay. |

The first prototype used a noncanonical macOS `/tmp` entry path and failed bundling; canonicalizing the diagnostic path fixed setup. The next exposed the unflagged replay premise rather than justifying a new scheduling barrier. These failures remain recorded in `/tmp/bpmn-subscription-host-preflight` and `/tmp/bpmn-subscription-host-preflight-2`.

### Continuation and host consumer inventory

| Boundary | Required change and deciding check |
|---|---|
| Private metadata and budgets | [Continuation protocol](../../packages/temporal-adapter/protocol/src/workflow-continuation.ts) adds the closed optional outer field and its strict inner decoder. Structural decoding admits only the specified shape; validation against the Program and committed state enforces required-for-new/forbidden-for-legacy and exact Timer binding. `workflowContinuationBudgetViolation` already measures the complete host object under `PublicationContinuationAndSegmentDirectoryBytes` and includes it in `ContinueAsNewCarriedArgumentsBytes`; both measurements must demonstrably include the new bytes without raising either bound. |
| Successor producer and incoming validator | [Workflow continuation](../../packages/temporal-adapter/workflow/src/workflow-chain-continuation.ts), specifically `buildWorkflowChainSuccessor`, `validateContinuationArguments`, and restored host state, carries and validates the binding before native Timer installation. Test a non-null binding, the exact null case, omitted new-profile field, added legacy-profile field, unknown inner key, stale identity, wrong logical deadline, unsafe physical due time, and individual/aggregate overflow. Existing argument positions and publication/recovery validation remain unchanged. |
| Ingress and native Timer ownership | [Command ingress](../../packages/temporal-adapter/workflow/src/workflow-command-ingress.ts) routes every accepted completion, Message variant, and incident Update to the new-profile readiness owner; [Workflow implementation](../../packages/temporal-adapter/workflow/src/workflow-implementation.ts) drains it and routes native firings through the same boundary. Reuse [activation-tagged readiness](../../packages/temporal-adapter/workflow/src/activation-tagged-readiness.ts), canonical command-ID ordering, and the [durable Timer owner](../../packages/temporal-adapter/workflow/src/durable-timer-owner.ts). The new-profile scheduler owns the sole Timer even at a normal-flow catch. A passive sibling completion must remain processable while that Timer is pending; the generic Timer path currently awaits the Timer directly and cannot supply this contract. Mixed batches must include both incident kinds and payload/correlated Messages alongside valid subscription work, including terminal completion and rollover, and prove semantic rejection, external recovery, and settlement of every accepted caller without a generic-queue bypass. |
| Failed-firing cause | [Scenario evaluation](../../packages/semantic-core/src/scenario.ts) produces the private HarnessFailure diagnostic from its one traced evaluation; the existing Workflow HarnessFailure branch consumes it before publication. Its rejected-live-Timer path passes exact firing details through [host invariant failure](../../packages/temporal-adapter/workflow/src/host-invariant.ts); candidate and retention capacity refusals keep [the capacity owner](../../packages/temporal-adapter/workflow/src/workflow-chain-capacity.ts) and existing failure details. [Publication state](../../packages/temporal-adapter/workflow/src/execution-publication-state.ts) and `runScenario` retain their observation-free HarnessFailure handling. Update [core closure atomicity](../../packages/semantic-core/test/closure-atomicity.test.ts), [transition publication](../../packages/semantic-core/test/semantic-transition-publication.test.ts), and [host closure classification](../../packages/temporal-adapter/workflow/test/closure-failure-classification-probe.ts) for the added private diagnostic, then discriminate every failed-firing class above through the scheduler. [Recovery eligibility](../../packages/temporal-adapter/workflow/src/workflow-chain-continuation.ts) continues to exclude `FireTimer`; no new wire schema or Timer recovery entry is introduced. |
| Wakeup and rollover | [Host readiness](../../packages/temporal-adapter/workflow/src/workflow-host-readiness.ts) and [rollover safety](../../packages/temporal-adapter/workflow/src/workflow-rollover-safety.ts) admit the new profile's fenced, settled, detached checkpoint. The existing legacy schedulers retain their refusal and armed-deadline veto. The checkpoint must prove no accepted command or callback remains unprocessed before handler settlement and successor construction. |
| Pre-start capability | [Host admission](../../packages/temporal-adapter/protocol/src/host-admission.ts) requires the complete new-profile structural/burst check and exactly the selected wait population. Its present blanket split-plus-managed-wait refusal must remain for legacy profiles. Adding an operation to the passive classification or allowing a token split alone is insufficient: the new scheduler must cover every admitted catch, boundary, sibling, and retained-child state. |

These host changes are prerequisites of the capability, not optional optimization. They require the existing [host-admission tests](../../packages/temporal-adapter/testkit/test/host-admission.test.ts), [continuation-state tests](../../packages/temporal-adapter/testkit/test/workflow-continuation-state.test.ts), and [aggregate-budget tests](../../packages/temporal-adapter/protocol/test/workflow-continuation-aggregate.test.ts), followed by direct-VM and real-service/replay witnesses. Public semantic state, RuntimeState continuation shape, correlation continuation, and public command receipts gain no Timer host-time field.

## Reachable scheduling obligations

These are obligations to derive from the final admitted graph, not assertions that a constructed state is source-reachable:

| Frontier | Required classification |
|---|---|
| Another trigger while an earlier handler waits | External trigger commits first; ensuing ordinary handler arming must issue a distinct occurrence and preserve all existing work |
| Host completion versus its next trigger | Dependent external choice; never an internal commutation law |
| Handler arming alongside an independently marked ordinary branch | Reuse the accepted ordinary-arming account if its exact ownership and snapshot hypotheses hold |
| Composite host arming alongside independent work | Required by the admitted fork forest: Message-boundary User Task arming can coexist with a passive sibling; Timer-bound or monitored child entry can coexist with a parent sibling. Close those complete atomic preparations; the legacy one-path exclusion no longer applies to the new profile. |
| Handler termination, child quiescence, and containing-region cancellation | Classify exact read/write and ownership overlap; do not infer independence from different element IDs |
| Multiple tokens for one handler operation | Preserve multiplicity and sequential fresh allocation; do not treat one operation offered twice as two disjoint prepared operations |
| Inner Terminate followed by subscribed child completion | Required by the selected Sub-Process Timer locus; close `ESL-RETAIN-01` and its exact ordinary-completion/publication consequence before admission |

For every allowed wait-set shape, the final account must establish a resumption surface, bounded internal closure, and separate Temporal host admission. Snapshot and Transaction combinations remain unresolved in [the RC assessment](../INTERNAL-COMMUTATION-PROPOSAL.md#rc-admission-dependency-assessment); this draft neither excludes them from RC nor opens a generic family-pair proof programme.

## Lean assurance lane

Lane shape: proved

Evidence: the planned lane extends the independently stated lifetime relations represented today by [Message-boundary laws](../../BpmnSemantics/SemanticProcess/MessageBoundedTaskLaws.lean), [monitored Timer semantics](../../BpmnSemantics/SemanticProcess/MonitoredTask.lean), and [regional cancellation](../../BpmnSemantics/SemanticProcess/ScopeCancellation.lean). Those files are existing prerequisites, not evidence that repetition is implemented. No new theorem is claimed by this draft.

Required quantified propositions are exact body/owner preservation through a non-interrupting trigger; fresh Timer replacement without reissuing an identity; preservation of arbitrarily many already spawned handler occurrences; complete withdrawal of future subscriptions on host completion; and exact removal of the cancelled ownership closure with an outside-frame law. Hypotheses name well-formed predecessor ownership, live identity uniqueness, admitted definition binding, and capacity; they may not assume the desired successor validity or removal result. Each new transition family requires a declarative relation, executable evaluator, and explicit dispatcher/constructor soundness bridge.

The following proposition groups bound the lane. All quantify over arbitrary well-formed predecessor collections within the selected admission and capacity bounds; no fixed list of two handlers stands in for the frame proposition.

| Group | Required conclusion and owner |
|---|---|
| Message lifetime | A successful repeat delivery preserves the exact host body, Activity identity, attached Message identity, and every pre-existing ordinary handler wait; it adds exactly one correctly owned output token. Completion withdraws that subscription and preserves spawned paths. Extend the relation/evaluator pattern of [Message-bounded Task](../../BpmnSemantics/SemanticProcess/MessageBoundedTask.lean) in the new `MessageMonitoredTask.lean` owner, with quantified laws in `MessageMonitoredTaskLaws.lean`. |
| Timer replacement | A successful firing removes exactly the old Timer and tagged attachment, issues one fresh identity above the predecessor counter, advances both the logical deadline and counter exactly once, and frames the host and all prior handler waits. One-shot absence retains consumption without replacement. A new `RecurringBoundaryTimer.lean` relation/evaluator serves both body kinds; `RecurringBoundaryTimerLaws.lean` owns freshness and frame laws. |
| Monitored child lifetime | Arming creates the exact Activity/child/Timer ownership triple; firing preserves it except for the selected Timer replacement; ordinary child completion removes only the live attachment and record, leaving parent-owned handler work. New `MonitoredScope.lean` and `MonitoredScopeLaws.lean` reuse [bounded scope arming](../../BpmnSemantics/SemanticProcess/BoundedScopeArming.lean) and its identity basis. |
| Regional retention and removal | Under `retain`, the selected child root and its external Activity record/attachment survive inner Terminate, while all body-owned work and removed descendants disappear. Under `remove`, the body and externally attached waits disappear. The outside frame and counters remain exact in both cases. Amend [ScopeCancellation](../../BpmnSemantics/SemanticProcess/ScopeCancellation.lean), [regional Activity removal](../../BpmnSemantics/SemanticProcess/InternalRegionalActivityRemoval.lean), and their cancellation validity/projection consumers. |
| Internal scheduling within selected admission | Close complete Message-host and bounded/monitored child preparations under the [amended proof domain](#scheduling-proof-domain-amendment). Reuse the existing [prepared transition sum](../../BpmnSemantics/SemanticProcess/InternalPreparedTransition.lean), [bounded preparation](../../BpmnSemantics/SemanticProcess/InternalBoundedScopePreparation.lean), and [finite execution frontier](../../BpmnSemantics/SemanticProcess/InternalExecutionFrontier.lean). Independence preserves complete preparation, exact successor validity/projectability, and canonical publication; dependent offers retain refusal and rollback. |
| Actual execution bridge | Connect each new declarative constructor to the evaluator, dispatch, complete command closure, and E1/E2 result. The existing [transition dispatcher](../../BpmnSemantics/SemanticProcess/Transition.lean), [execution](../../BpmnSemantics/SemanticProcess/Execution.lean), and [scenario projection](../../BpmnSemantics/SemanticProcess/Scenario.lean) remain the consumers. Finite fixtures run the compiled new source through those consumers; helper-only agreement is insufficient. |

Each capability claim must carry a concrete admitted source and reachable frontier. Proof-integration obligations additionally cover arbitrary well-formed predecessors within the admitted structural surface, as specified below. The at-most-one boundary and child restrictions exclude two distinct composite hosts. Message-host arming may coexist with a plain Timer on another branch; a Timer-host excludes that second Timer. Child entry can coexist with parent passive arming or Ends. Child completion may coexist with outside work, while retained inner Terminate and its subsequent completion are dependent steps. Effect, Call, data-mediated Task, Multi-Instance, Event race, and Compensation snapshot pair expansion remains excluded; their existing guarantees remain unchanged.

Effort bound: each proposition group gets one direct reuse pass against the existing relation/frame machinery and one focused correction pass for a demonstrated missing lemma or ownership split. Failure to close after those two passes records the exact residual proposition and the attempted boundary in the runtime/proof map and returns that group to review; it does not authorize an aggregate invariant, generic family-pair programme, assumed successor validity, or weaker claim. Every new kernel-decided fixture retains the fixed 3 GiB cold-target gate; a memory failure triggers a smaller proof-owner split, never a larger limit or native-decision replacement. Measure the complete consumer closure before acceptance.

Finite Lean/core schedules separately check source-to-result publication, both completion/trigger orders, repeated Messages to one subscription, stale Timer identity, and nested cleanup. They cannot replace the quantified multiplicity or cancellation laws. New `RepeatableEventSubscriptionAdmissionConformance`, `RepeatableMessageLifetimeConformance`, `RecurringTimerLifetimeConformance`, and `SubscribedScopeCancellationConformance` modules separate source/limit decisions, repeated delivery, replacement, and regional reduction costs. They are planned owners, not current evidence. The lane does not claim general fairness, a global reachable-state preservation theorem, or a Temporal liveness theorem.

### Scheduling proof-domain amendment

The existing [pair and preservation theorems](../../BpmnSemantics/SemanticProcess/InternalTransitionBatch.lean) and [canonical finite-batch publication theorem](../../BpmnSemantics/SemanticProcess/InternalTransitionCanonicalBatchPublication.lean) quantify over arbitrary well-formed predecessors and prepared lists. They do not assume source reachability. Adding unrestricted composite constructors therefore demands more than the source-frontier witnesses above. This amendment changes the new preparations' proof boundary and evidence strategy; it changes no BPMN lifetime meaning, source capability, scheduling choice, or Temporal contract.

New integrated Message-host and Lean bounded/monitored-child preparations require the immutable Program admission certificate: the exact subscription profile identity, generic structural validity, and the complete selected operation/forest/ownership/cardinality/burst predicate. A profile-name comparison alone is insufficient. The executable preparation and its `Prepared` proposition must enforce the same certificate. Existing constructor predicates, footprint independence, theorem hypotheses and conclusions remain unchanged; no global admission or reachability premise is added. Raw family helpers remain separate from integrated preparation. Because the certificate depends only on the immutable Program, every revalidated finite prefix retains it.

TypeScript already integrates raw `EnterBoundedScope` preparation at baseline `9bb287ba`; Lean's accepted prepared sum does not. Preserve that existing TypeScript behavior outside the new profile. Apply the new admission check to the subscription-profile path and newly integrated Message/monitored-child paths, and identify this restricted surface as the new common assurance domain. Do not claim equal legacy raw-preparation domains or silently restrict the older path.

The new quantified domain is arbitrary well-formed predecessors of certificate-bearing Programs, including intermediate states within an already selected batch. Each pair below requires complete preparation preservation, exact commutation, successor validity/projectability and accepted-publication frames when independent, or a predecessor-derived proof that simultaneous independent preparation is impossible. Finite witnesses cannot replace these propositions.

| New preparation | Other preparation | Exact obligation |
|---|---|---|
| Message host, either disposition | Ordinary User Task, payload-free Message/Receive Task, or sole plain Timer | Complete atomic Task/Message/Activity arming and both-order frames. |
| Message host, either disposition | Divergent Parallel control, ordinary child entry, or None End | Preserve exact tokens, scope census, identities and publication in both orders. |
| Message host, either disposition | Regional completion or Terminate | Prove the independent outside-region cases and overlapping-region refusal over valid predecessors; absence from a discovered source frontier is insufficient. |
| Bounded/monitored child entry | Ordinary User Task, payload-free Message/Receive Task, divergent Parallel control, or None End | Complete parent Activity/child/Timer arming and both-order frames. |
| Bounded/monitored child entry | Regional completion or Terminate | Classify joint preparation and independence from the sole-child declaration, live-body census and occurrence region. Any surviving independent case requires the same frame laws. |
| Either new preparation | Another boundary composite, another child entry where the sole child is already claimed, or an excluded operation family | Derive impossibility from the admission certificate, exact operation membership and preparation/independence predicates. Do not encode an arbitrary family blacklist as footprint conflict. |

The excluded families are Effect, Call/Return, data-mediated work, Multi-Instance, Event race, Merge/Join/conditional control, Error and Compensation snapshots. A second Timer is excluded when the boundary owns the sole Timer. Two preparations of one composite operation are not assumed distinct: shared input and occurrence-census conflicts must establish dependent refusal. Ordinary child entry alongside a Message host remains required and is not covered by the sole-child exclusion.

Acceptance includes a structurally valid raw Program containing a new host plus an excluded family: its raw helper may prepare, but its new integrated preparation must refuse the absent admission certificate. A second discriminator constructs a well-formed admitted Program state with outside Message arming and quiescent child completion; either exact complete preparation rejects it for an existing invariant, or the regional pair law must cover it. Preserve the old raw bounded-scope TypeScript control. Check single-step validity/projectability and publication before lifting pair results through unchanged finite-prefix and permutation contracts; no successor property may be assumed.

This amendment renews one direct reuse pass and one focused correction pass for each of four bounded groups: admission-certificate integration and exclusion derivation; new single-step validity/publication; the enumerated Message pairs; and the enumerated child-entry pairs. Finite-batch integration reuses existing lifting theorems. A residual after that bound returns to review with its exact proposition; no aggregate invariant or further family set is authorized.

## Separating evidence and common-mode risks

| Rule | Deciding positive and negative | Independent evidence required |
|---|---|---|
| `ESL-OWN-01` | Host, handler, and Activity counters deliberately differ; a missing or multiply claimed attachment refuses | Source binding, Lean relation, core validity, publication completeness |
| `ESL-MESSAGE-01` | Two distinct commands to the same live subscription create two paths; retrying one command creates none | Lean/core execution, public recovery, Worker replacement and replay |
| `ESL-TIMER-01` | Two firings produce fresh identities and fixed logical spacing; the first identity cannot fire again | Lean counter/deadline law, core mutation, durable handoff and continuation witness |
| `ESL-SPAWN-01` | The second handler starts before the first completes; either completion order preserves the host | Quantified frame law plus whole-model Lean/core/publication schedules |
| `ESL-CLOSE-01` | Host completion removes the live subscription and leaves two spawned paths | Exact state and E1/E2 publication in both semantic accounts and the live host |
| `ESL-CANCEL-01` | Nested cancellation removes inside work and parent-owned attachments, retaining an outside sibling | Quantified ownership-closure law, source-reachable nested witness, replay |

Required mutations retain a stale attachment, reuse a Timer identity, replace a Message subscription after each delivery, delete a running handler on normal host completion, retain a cancelled nested wait, reset the next Timer's delay at rollover, and let callback order select a winner. The common-mode risk is copying a one-shot victory transition into the repeat path and obtaining agreement because both targets accidentally consume the same long-lived subscription.

No CIB column is claimed. Registered profiles, answer-free scenarios, retained whole business models, capability/restriction rows, generated corpus coverage, and Product 2 disclosures are required at capability closure. A private fixture does not satisfy those obligations. Broader event families, new Task subclasses, new Multi-Instance hosts, and Event Sub-Processes are excluded from this proposal; the separate selected Compensation/Transaction work remains required for RC.

## Artifact registration

Use the existing normative-authority profile branch and the existing neutral-scenario schema. The profile lists `CIB-AGR-0001` and `CIB-OP-0001` solely for the reused User Task premises described above. Each retained business-model scenario includes ordinary human work and records the already inspected pristine CIB Seven `2.2.0` revision `5a45b47ea22688d774de97277c3ff7013f54fdd2` and `engine/src/main/java/org/cibseven/bpm/engine/impl/bpmn/behavior/UserTaskActivityBehavior.java` as research provenance for that work. Its `performExecution` creates the task and its `signal` leaves the task; this source supplies no subscription-repeat, concurrency, or race-policy evidence. No CIB target, result file, or comparison column is added.

This is the pattern already used by the [registered Message-boundary scenarios](../../scenarios/activity-boundary-message/README.md). The [artifact verifier](../../scripts/contract-artifacts.ts) separately validates normative-authority profiles and CIB evidence, while the [relationship guard](../../scripts/contract-artifact-projections.test.ts) still requires real registered IDs. The draft's earlier inference that this capability necessarily needed a shared schema change was too broad: its retained User Task models have truthful existing provenance. No source-admission restriction is added to manufacture that provenance, no empty/placeholder CIB reference is accepted, and the separate Compensation registration question is not resolved by analogy.

Capability closure registers answer-free scenarios and whole business models for repeated reminder Messages, recurring deadline reminders, subscribed child termination with outside work, and normal catch/Receive Task cleanup. Each model binds exact XML, profile, pipeline cases, capability/restriction rows, generated corpus coverage, and Product 2 About disclosure. Browser-catalog eligibility requires its complete journey; corpus inclusion alone does not supply it.

## Review questions and implementation obligations

1. Does the selected catch/host/recurrence boundary discharge the selected RC subscription row without presenting its restrictions as general BPMN meaning? The exact `R/PT1S` cycle, one boundary, one child, and one Timer limits need explicit review. Broader compliant behavior remains deferred.
2. Is the whole-burst admission argument sound for every permitted fork forest, including immediate child completion, repeated handlers already waiting, and Terminate followed by parent continuation? Source/XSD diagnostics and the existing kernel's exact-limit controls are feasibility evidence. Production source/Program admission and independently lowered Lean cases must still implement and discriminate the bound.
3. Does `ESL-RETAIN-01` preserve Activity-body ownership through the dependent inner-Terminate/ordinary-completion sequence? Review it as a material amendment to the existing cancellation rule. Source-reachable completion and publication are required, not merely a valid constructed successor.
4. Do the flag premise, complete ingress routing, fixed batches, and due-time handoff preserve the public schedule through Worker replacement and every successor Run? Direct-VM feasibility does not replace production recovery/capacity tests, actual cancellation, recorded flags, retained histories, and replay.

The lifetime account and migration inventory were approved at `5677738d64732f9b39050421f53e93a3f6e08e55`; the current proof-domain amendment requires a new cold proposal review. The next coherent implementation outcome remains the semantic checkpoint: exact checked-source/Program admission through independent Lean/core lifetime execution and E1/E2 publication, including retained-body cancellation and the bounded scheduling proof domain above. Helper repairs remain part of that complete outcome. Reassess each proposition group at its stated effort bound before adding prerequisites. Artifact registration and the production Temporal lane follow checkpoint review.

These questions belong to independent review, not a separate request for owner confirmation.

## Versioning consequences

Use additive semantic-profile and operation admission. Preserve existing profile bytes, consuming Message semantics, one-shot Timer semantics, coalescence refusals, public scheduling refusal, and the [pinned deployment/history contract](../TEMPORAL-PROCESS-LIFECYCLE-SPEC.md#workflow-chain-production-contract). The new nested private continuation version belongs only to this new profile; old Workers do not receive its histories or successor arguments. Existing deployment pinning remains required. Exact incoming validation must reject a missing new-profile field and an added legacy-profile field, with no mixed-version fallback.

The owner inventory was derived with [what-binds](../../scripts/what-binds.ts) and the live operation/checked-node readers. The implementation must satisfy [schema coverage](../../scripts/contract-schema-coverage.test.ts), [artifact projections](../../scripts/contract-artifact-projections.test.ts), [Activity joins](../../scripts/activity-occurrence-join.test.ts), [writer census](../../scripts/activity-occurrence-writer-census.test.ts), [collection-removal completeness](../../scripts/runtime-collection-removal-completeness.test.ts), [operation census](../../scripts/semantic-operation-consumer-census.test.ts), [commutation census](../../scripts/internal-commutation-census.test.ts), [publication coverage](../../scripts/execution-publication-contract-coverage.test.ts), [canonical ordering](../../scripts/canonical-ordering.test.ts), [host admission](../../packages/temporal-adapter/testkit/test/host-admission.test.ts), [corpus policy](../../scripts/bpmn-corpus-policy.test.ts), [reviewability](../../scripts/document-reviewability.test.ts), and [independent review policy](../../scripts/independent-review-policy.test.ts).

### Producer and consumer migration

The existing [operation-variant matrix](ACTIVITY-DATA-INPUT-OUTPUT-MEDIATION-SPEC.md#complete-operation-variant-producerconsumer-matrix) supplies the shared consumer baseline. The table below selects the subscription delta; it does not transfer the predecessor capsule's semantic account. `Change` includes strict refusal and exhaustive classification, not just execution. `No change` requires predecessor gates and exact legacy artifact bytes. The operation census must include the new variants and their current readers in this capsule's inventory; its existing Activity-data-only reader predicate is not evidence of subscription completeness.

| Boundary | Existing owners and disposition |
|---|---|
| Profile identity and graph policy | Change: [catalog](../../packages/semantic-core/src/semantic-profile-catalog.ts), [profile selection](../../packages/semantic-core/src/semantic-process-profile.ts), [checked shape](../../packages/semantic-core/src/checked-process-profile-shape.ts), [Program shape](../../packages/semantic-core/src/semantic-program-profile-shape.ts), [graph policy](../../packages/semantic-core/src/semantic-process-graph-policy.ts), and [Lean shape catalog](../../BpmnSemantics/SemanticProcess/ProfileShapeCatalog.lean) select the bounded capability predicates rather than an exact whole-model multiset. Existing profile cases retain their predicates. |
| Value and observation selection | Change: [TypeScript value domain](../../packages/semantic-core/src/semantic-profile-value-domain.ts), [Lean value domain](../../BpmnSemantics/SemanticProcess/ValueDomain.lean), [observation selection](../../packages/semantic-core/src/semantic-profile-observations.ts), and [preservation capability](../../packages/bpmn-source/src/preservation-capability.ts) classify the new profile explicitly. No change: scalar value domain, empty User Task submissions, observation request variants, or parser-warning policy. |
| Source projection | Change: [element projection](../../packages/bpmn-source/src/checked-element-projection.ts), [Message boundary](../../packages/bpmn-source/src/message-boundary-event-source.ts), and [Timer boundary](../../packages/bpmn-source/src/timer-boundary-event-source.ts) retain exact expression/disposition evidence and reject mixed expressions, unselected hosts, and parser warnings. [Compilation dispatch](../../packages/bpmn-source/src/compilation-dispatch.ts) recognizes only the new identity. |
| Checked wire and admission | Change: [checked contract](../../packages/semantic-core/src/checked-process-contract.ts), [checked schema](../../contracts/schemas/checked-process.schema.json), [source admission](../../packages/bpmn-source/src/checked-process-admission.ts), [graph admission](../../packages/bpmn-source/src/checked-process-graph-admission.ts), [Lean checked decoder](../../BpmnSemantics/SemanticProcessJson/CheckedProcess.lean), [Lean checked admission](../../BpmnSemantics/SemanticProcess/CheckedProcessAdmission.lean), and [Lean checked graph validation](../../BpmnSemantics/SemanticProcess/CheckedGraphValidation.lean) carry the disjoint duration/cycle arm and enforce forest, locus, multiplicity, and burst restrictions. |
| IL and lowering | Change: [TypeScript contract](../../packages/semantic-core/src/semantic-process-contract.ts), [Lean contract](../../BpmnSemantics/SemanticProcessContract.lean), [Program schema](../../contracts/schemas/semantic-process.schema.json), [TypeScript lowering](../../packages/bpmn-source/src/semantic-process-lowering.ts), [Lean lowering](../../BpmnSemantics/SemanticProcess/Lowering.lean), and [Lean Program decoder](../../BpmnSemantics/SemanticProcessJson/Program.lean) preserve both new operation kinds and optional recurrence. Old absent-field JSON bytes remain exact. |
| Raw Program admission | Change: [operation admission](../../packages/semantic-core/src/semantic-process-operation-admission.ts), [Program graph admission](../../packages/semantic-core/src/semantic-process-graph-admission.ts), [bounded wait admission](../../packages/semantic-core/src/bounded-wait-admission.ts), [Lean structural validation](../../BpmnSemantics/SemanticProcess/ProgramStructuralValidation.lean), and [Lean graph validation](../../BpmnSemantics/SemanticProcess/GraphValidation.lean) reject forged recurrence on interrupting/legacy operations, ambiguous declarations, invalid ownership, and over-limit forests independently of source compilation. |
| Execution dispatch and family semantics | Change: [core runtime](../../packages/semantic-core/src/semantic-process-runtime.ts), [Message dispatch](../../packages/semantic-core/src/semantic-process-message.ts), [Message-bounded Task](../../packages/semantic-core/src/semantic-process-message-bounded-task-runtime.ts), [monitored Task](../../packages/semantic-core/src/semantic-process-monitored-task-runtime.ts), [bounded scope](../../packages/semantic-core/src/semantic-process-bounded-scope-runtime.ts), and the Lean owners named in the assurance lane dispatch exact consuming versus persistent behavior. New core owners are `semantic-process-message-monitored-task-runtime.ts`, `semantic-process-recurring-boundary-timer-runtime.ts`, and `semantic-process-monitored-scope-runtime.ts`; existing owner extraction is limited to shared exact arming/ownership mechanics. |
| Runtime validity and body joins | Change: [core well-formedness](../../packages/semantic-core/src/runtime-state-well-formedness.ts), [Lean well-formedness](../../BpmnSemantics/SemanticProcess/RuntimeStateWellFormed.lean), and [Activity arming](../../packages/semantic-core/src/semantic-process-activity-arming.ts) bind the new kinds to exact live bodies and tagged attachments. No change: RuntimeState fields, counter shapes, and Activity body/handler representations. Missing-pair refusal remains mandatory. |
| Regional cancellation and preparation | Change: [core cancellation](../../packages/semantic-core/src/semantic-process-scope-cancellation.ts), [regional preparation](../../packages/semantic-core/src/internal-transition-regional-preparation.ts), [Lean cancellation](../../BpmnSemantics/SemanticProcess/ScopeCancellation.lean), [regional preparation](../../BpmnSemantics/SemanticProcess/InternalRegionalPreparation.lean), and the Activity-removal/validity/projection consumers implement the predecessor-derived removed-scope distinction. All existing cancellation collection fields remain classified. |
| Internal census and identity declarations | Change: [core family census](../../packages/semantic-core/src/internal-commutation-census.ts), [wait census](../../packages/semantic-core/src/internal-transition-wait-census.ts), [footprints](../../packages/semantic-core/src/internal-transition-footprint.ts), and [Lean census](../../BpmnSemantics/SemanticProcess/InternalCommutationCensus.lean) classify both new composite operations and all three identity domains of monitored scope arming. Repetition adds no internal firing operation; firing remains an external stimulus. |
| Prepared execution and publication | Change: [core batch](../../packages/semantic-core/src/internal-transition-batch.ts), [arming batch](../../packages/semantic-core/src/internal-transition-arming-batch.ts), [attempt](../../packages/semantic-core/src/internal-transition-attempt.ts), [bounded preparation](../../packages/semantic-core/src/internal-transition-bounded-scope-preparation.ts), [Lean prepared sum](../../BpmnSemantics/SemanticProcess/InternalPreparedTransition.lean), and existing bounded/Timer-task preparation, validity, and publication owners implement the amended admission certificate and enumerated proof domain. Unsupported combinations keep typed refusal; legacy preparation paths remain intact. |
| Open occurrences and external completeness | Change: [open set](../../packages/semantic-core/src/flow-node-occurrence-open-set.ts), [boundary starts](../../packages/semantic-core/src/flow-node-occurrence-boundary-starts.ts), [lifecycle](../../packages/semantic-core/src/flow-node-occurrence-lifecycle.ts), [publication completeness](../../packages/semantic-core/src/flow-node-occurrence-publication-completeness.ts), and [external completeness](../../packages/semantic-core/src/flow-node-occurrence-publication-external-completeness.ts) distinguish persistent wait anchors from each delivery's instantaneous event. The corresponding Lean BoundaryStarts, Lifecycle, ProgramValidity, WaitProgramValidity, and UserTaskProgramValidity owners carry the same constructor coverage. |
| Trace and strict publication readers | Change: [Lean transition record](../../BpmnSemantics/SemanticProcess/TransitionRecord.lean), [scenario projection](../../BpmnSemantics/SemanticProcess/Scenario.lean), [publication JSON](../../BpmnSemantics/SemanticProcessJson/Publication.lean), [publication schema](../../contracts/schemas/semantic-publication.schema.json), [protocol validation](../../packages/temporal-adapter/protocol/src/semantic-publication-validation.ts), [Program binding](../../packages/temporal-adapter/protocol/src/flow-node-occurrence-publication-program-validation.ts), and [Product 2 enum copy](../../platform/contracts/src/execution-publications.ts) accept the two new E1 values and validate the existing E2 forms. No change: publication envelope, pagination, hashing, or public occurrence identity format. |
| Temporal production | Change: every producer/reader in the [host inventory](#continuation-and-host-consumer-inventory), with a new `subscription-readiness-scheduler.ts` owning the selected batch and sole Timer. No change: legacy schedulers' semantics, command recovery identity, receipt variants, or production capacity values. The new owner routes all admitted wait-set shapes, not merely exact host/trigger pairs. |
| Scenario driver and history oracle | Change: [runner support](../../packages/temporal-adapter/testkit/src/runner-support.ts) currently refuses a second firing, and [history evidence](../../packages/temporal-adapter/testkit/src/harness-evidence.ts) currently requires exactly one Timer-started/fired pair. The new profile gets an ordered firing list and a per-Run native-start/fire/cancel reconciliation against exact accepted semantic firings; successor re-arming and shortened/overdue delays are accounted for. [Runner](../../packages/temporal-adapter/testkit/src/runner.ts) and [ordered stimulus delivery](../../packages/temporal-adapter/testkit/src/ordered-stimulus-delivery.ts) wait for each exact command boundary. Timers remain host-driven. Preserve the legacy single-firing oracle, and retain a bypass mutation which has the right semantic trace but no native Timer evidence. |
| Artifact and capability registration | Change: [artifact cases](../../scripts/contract-artifact-cases.ts), [Activity writer records](../../scripts/activity-occurrence-writer-records.ts), [capability extraction](../../scripts/executable-model-capabilities.ts), [corpus catalog](../../model-corpus/mvp-capabilities.ts), generated corpus coverage, and About disclosure bind the new operation and recurrence capabilities atomically with retained whole models. No change: profile/scenario provenance schemas, existing CIB results, or oracle-answer separation. |
| Excluded family readers | No semantic change: Compensation snapshot/trigger declarations and validation, Call, effects, data mediation, and Multi-Instance accounts reject the new operations outside their selected domains. Their exhaustive matches must compile and their retained cases remain green. Snapshot, regional, scope-creation, and Timer-task legacy laws may need constructor-exhaustion adjustments; those adjustments do not authorize new family-pair claims. |

The profile-specific forest/burst predicates belong in new cohesive `repeatable-subscription-admission.ts` and `RepeatableSubscriptionAdmission.lean` owners, reached by both checked and Program admission at their respective representations. The strict continuation Timer decoder belongs in `subscription-timer-binding.ts` under the Temporal protocol package; native time and cancellation stay in the Workflow scheduler. These planned owners separate existing boundaries rather than introducing another runtime representation or policy registry.

### Owners this implementation grows

| Existing owner | Measured headroom before the 800-line review target |
|---|---:|
| [Monitored Task runtime](../../packages/semantic-core/src/semantic-process-monitored-task-runtime.ts) | 540 |
| [Message-bounded Task runtime](../../packages/semantic-core/src/semantic-process-message-bounded-task-runtime.ts) | 525 |
| [Durable Timer owner](../../packages/temporal-adapter/workflow/src/durable-timer-owner.ts) | 697 |
| [Workflow continuation](../../packages/temporal-adapter/workflow/src/workflow-chain-continuation.ts) | 104 |
| [Message boundary source](../../packages/bpmn-source/src/message-boundary-event-source.ts) | 724 |
| [Timer boundary source](../../packages/bpmn-source/src/timer-boundary-event-source.ts) | 693 |
| [Lean monitored Task](../../BpmnSemantics/SemanticProcess/MonitoredTask.lean) | 254 |
| [Lean Message-bounded Task](../../BpmnSemantics/SemanticProcess/MessageBoundedTask.lean) | 361 |
| [Core regional cancellation](../../packages/semantic-core/src/semantic-process-scope-cancellation.ts) | 578 |
| [Lean regional cancellation](../../BpmnSemantics/SemanticProcess/ScopeCancellation.lean) | 578 |
| [Checked contract](../../packages/semantic-core/src/checked-process-contract.ts) | 423 |
| [IL contract](../../packages/semantic-core/src/semantic-process-contract.ts) | 178 |
| [Source lowering](../../packages/bpmn-source/src/semantic-process-lowering.ts) | 134 |
| [Lean contract](../../BpmnSemantics/SemanticProcessContract.lean) | 11 |
| [Lean transition](../../BpmnSemantics/SemanticProcess/Transition.lean) | 127 |
| [Lean Program JSON](../../BpmnSemantics/SemanticProcessJson/Program.lean) | 87 |
| [Product 2 operation enum](../../platform/contracts/src/execution-publications.ts) | 345 |
| [Temporal scenario runner](../../packages/temporal-adapter/testkit/src/runner.ts) | 255 |
| [Lean prepared sum](../../BpmnSemantics/SemanticProcess/InternalPreparedTransition.lean) | 521 |
| [Lean finite-batch consumer](../../BpmnSemantics/SemanticProcess/InternalTransitionBatch.lean) | 93 |
| [Lean canonical batch publication](../../BpmnSemantics/SemanticProcess/InternalTransitionCanonicalBatchPublication.lean) | 703 |
| [Core prepared batch](../../packages/semantic-core/src/internal-transition-batch.ts) | 618 |

The Lean contract's 11-line margin cannot hold the selected additions. Extract its existing checked-source declarations through `CheckedProcess`, including their shared identity types, to `BpmnSemantics/CheckedProcessContract.lean`; the current Semantic Process contract imports that cohesive owner and continues to own the IL operation sum. Preserve namespace, declaration meaning, and old codecs, and update exact-source guards to read the new checked owner rather than weakening their declaration checks. This is a representation-owner extraction, not new proof infrastructure. The continuation owner needs extraction only if its growth exceeds the measured 104-line margin. Other new cohesive owners are named above; closure cost is measured against the current Message-boundary and non-interrupting Timer increments in [the cost ledger](../CAPSULE-COST-LEDGER.md).

## Independent cold-review receipt

| Stage | Review target | Isolation | Verdict | Correction audit |
|---|---|---|---|---|
| Proposal | `not-recorded` | `not-recorded` | `pending` | `not-applicable` |
| Semantic checkpoint | `not-applicable` | `not-applicable` | `not-reached` | `not-applicable` |
| Closure | `not-applicable` | `not-applicable` | `not-reached` | `not-applicable` |
