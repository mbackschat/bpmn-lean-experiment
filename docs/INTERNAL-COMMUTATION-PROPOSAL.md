# Internal commutation proposal

## Status

Lifecycle: implementation-in-progress
Review: approved

## Decision question and boundary

What is the smallest reusable rule that lets bounded internal closure advance every simultaneously enabled, observationally independent operation and represent every residual observable choice without treating program collection order as BPMN scheduling meaning?

The implemented first `INTERNAL-COMMUTATION` Beta risk checkpoint replaced the constructor-specific exception for exactly two distinct `awaitUserTask` operations with one semantic-footprint criterion over an exact two-operation frontier. The checkpoint covers ordinary `awaitUserTask`, `awaitMessage`, `awaitTimer`, and `awaitEffect` arming operations in Lean and the independently written TypeScript core. It added no BPMN source shape, profile capability, Semantic Process operation, RuntimeState field, stimulus, public wire field, Temporal host capability, MUE content ID, or support claim.

The full MUE content obligation remains open after that checkpoint. The approved final-closure account selects larger enabled sets, every current operation family, and explicit choice representation without adding a BPMN construct or newly admitted topology. The independently approved [selected-join readiness amendment](#selected-join-readiness-dependency-amendment) extends only its dependency vocabulary; its implementation precedes local-control batch integration. Earlier approved checkpoints retain their recorded boundaries.

## Existing risk

[The Semantic Process contract](SEMANTIC-PROCESS-IL-SPEC.md#internal-scheduling) requires every material internal choice to be explicit or observationally irrelevant under exact hypotheses. Before this proposal's first Beta checkpoint, production closure recognized only one exact pair of distinct ordinary User Task arms. That check named operation constructors and three identifiers without stating the semantic resources each transition reads and writes. Extending that checklist would repeat the mechanism without establishing why the pair commutes. Current batch coverage belongs to [`implementation-status-owner:ENGINE-RUNTIME-PROOF`](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md#current-boundary); the checkpoint sections below retain their bounded laws and exclusions.

Canonical operation-ID selection is necessary for reproducibility but is not evidence of commutation. Sorting two conflicting transitions merely makes one conflict deterministic. The deciding rule must establish non-interference before the selector may use canonical order.

## Implemented checkpoint semantic-footprint rule

Each candidate transition receives a dynamic `InternalTransitionFootprint` derived from the admitted program, the exact pre-state, and the operation before its successor is selected. The footprint contains canonical sets of tagged semantic atoms, split into `reads`, `writes`, and `publications`. Shared reads are permitted. A pair is non-interfering only when neither side's writes intersect the other side's reads or writes and the publication keys are distinct.

The checkpoint state-atom vocabulary is closed:

- `controlToken(owner, place)` for one exact token unit whose presence enables the operation and whose multiplicity is decremented;
- `scopeOccurrence(owner)` and `runtimeControl(instance)` for owner liveness and the exact running Process instance;
- `logicalTime` for Timer deadline calculation;
- `activation(kind, elementId)` for the per-family counter value that mints the next occurrence;
- `wait(kind, occurrence)` for absence of, and insertion of, the complete User Task, Message, Timer, or effect occurrence;
- `openWaitAnchor(occurrence)` for absence from, and addition to, the actual derived untagged public wait-anchor domain shared by User Task, Message, Timer, effect, and incident occurrences;
- `activityVariableScope(effectOccurrence)` for absence of, and insertion of, the complete effect-owned local scope, including an empty scope;
- `activityVariable(effectOccurrence, name)` for every literal input binding inserted into that local scope.

The closed publication-atom vocabulary is `committedTransition(operationId, kind, origin, owner, logicalTime, positionDelta)`, `flowNodeLifecycle(waitAnchor(occurrence))`, and `publicationPair(operationId, occurrence)`. The lifecycle atom deliberately omits the wait family because the published `SemanticFlowNodeOccurrenceAnchor.wait` contract is keyed only by `(processInstanceId, elementId, activation)`. A User Task and Timer with the same complete occurrence therefore collide even though their private wait collections and activation counters differ. A publication pair binds one transition record and its one lifecycle delta before either receives a batch index. Its canonical sort key is the exact tuple `(operationId, occurrenceKind, processInstanceId, elementId, activation)`, where `occurrenceKind` is the private wait-family discriminator and never part of the public lifecycle anchor.

Occurrence atoms include the state-derived next activation, not only an element ID. Every User Task, Message, Timer, and effect occurrence in the equations uses `owner.processInstanceId`; only the separate `runtimeControl(state.control.instanceId)` read names the top-level running Process instance. Atom tags keep User Task, Message, Timer, effect, control-place, and Activity-local domains distinct. Every set and sort key uses the repository's canonical Unicode scalar ordering, numeric activation ordering, and explicit tagged equality. Locale collation, object enumeration order, and a successor-state diff are excluded from footprint construction.

The constructor equations are exact:

| Constructor | Required reads | Complete writes | Publication bundle |
|---|---|---|---|
| `awaitUserTask` | `runtimeControl(instance)`, `scopeOccurrence(owner)`, `controlToken(owner,input)`, `activation(userTask,taskId)`, and absence of both `wait(userTask,occurrence)` and `openWaitAnchor(occurrence)` | decrement `controlToken(owner,input)`; replace `activation(userTask,taskId)` by the next value; canonically insert `wait(userTask,occurrence)`, thereby adding the derived `openWaitAnchor(occurrence)` | one `committedTransition` carrying the exact operation metadata, owner, unchanged logical time, and the token-to-User-Task position delta; one started `flowNodeLifecycle(waitAnchor(occurrence))`; one `publicationPair(operationId,occurrence)` |
| `awaitMessage` | `runtimeControl(instance)`, `scopeOccurrence(owner)`, `controlToken(owner,input)`, `activation(message,elementId)`, and absence of both `wait(message,occurrence)` and `openWaitAnchor(occurrence)` | decrement `controlToken(owner,input)`; replace `activation(message,elementId)` by the next value; canonically insert `wait(message,occurrence)`, thereby adding the derived `openWaitAnchor(occurrence)` | one `committedTransition` carrying the exact operation metadata, owner, unchanged logical time, and the token-to-Message position delta; one started `flowNodeLifecycle(waitAnchor(occurrence))`; one `publicationPair(operationId,occurrence)` |
| `awaitTimer` | `runtimeControl(instance)`, `scopeOccurrence(owner)`, `controlToken(owner,input)`, `logicalTime`, `activation(timer,elementId)`, and absence of both `wait(timer,occurrence)` and `openWaitAnchor(occurrence)` | decrement `controlToken(owner,input)`; replace `activation(timer,elementId)` by the next value; canonically insert `wait(timer,occurrence)` with deadline `logicalTime + duration`, thereby adding the derived `openWaitAnchor(occurrence)` | one `committedTransition` carrying the exact operation metadata, owner, unchanged logical time, and the token-to-Timer position delta; one started `flowNodeLifecycle(waitAnchor(occurrence))`; one `publicationPair(operationId,occurrence)` |
| `awaitEffect` | `runtimeControl(instance)`, `scopeOccurrence(owner)`, `controlToken(owner,input)`, `activation(effect,elementId)`, and absence of `wait(effect,occurrence)`, `openWaitAnchor(occurrence)`, and `activityVariableScope(occurrence)` | decrement `controlToken(owner,input)`; replace `activation(effect,elementId)` by the next value; canonically insert `wait(effect,occurrence)`, thereby adding the derived `openWaitAnchor(occurrence)`; canonically insert `activityVariableScope(occurrence)` and one `activityVariable(occurrence,name)` per literal input binding | one `committedTransition` carrying the exact operation metadata, owner, unchanged logical time, and the token-to-effect position delta; one started `flowNodeLifecycle(waitAnchor(occurrence))`; one `publicationPair(operationId,occurrence)` |

Current effect input mappings are literals and therefore read no Process or Activity-local runtime variable. Adding a variable-reading expression reopens this table and the atom vocabulary before implementation.

For the first checkpoint, footprint construction returns `none` for every operation outside ordinary `awaitUserTask`, `awaitMessage`, `awaitTimer`, and `awaitEffect`. It also returns `none` unless the selected operation is the unique program operation in its complete wait-family declaring set for the emitted element, that declaration is owned by the selected scope, and the new untagged wait anchor is absent from the exact pre-state. The declarer census includes every ordinary and composite operation that can emit that wait family, including bounded, monitored, Sequential Multi-Instance, Parallel Multi-Instance, event-race, and boundary-host operations even though those operations cannot themselves receive a checkpoint footprint. Program admission now requires that census to resolve every family-tagged declaration uniquely even when no wait is live; the footprint check retains the same rule defensively at its local boundary. The open-anchor census includes existing incident occurrences as well as every live User Task, Message, Timer, and effect wait. An unavailable footprint, more or fewer than two enabled operations, an intersection, an existing or paired untagged wait-anchor collision, a duplicate publication key, or a failed second step retains the existing fail-closed ambiguous-choice result. Separately, an unavailable pre-state or intermediate open-set projection retains the traced boundary's existing no-publication failure rather than counting two equal projection failures as commutation evidence. This is a sufficient rule, not a necessary characterization of every commuting transition.

At the first checkpoint, the Activity data-input operation `awaitDataInputUserTask` and its data-output sibling `awaitDataOutputUserTask` entered the composite wait-and-Activity-arming declarer criterion but received no checkpoint footprint. The legacy footprint facade retains that refusal; complete preparation is owned by the later [standalone Activity-data arming outcome](#standalone-activity-data-arming-outcome). The `LocalDataOwner` discriminator distinguishes `effectOccurrence` and `activityOccurrence` while preserving the checkpoint's effect-arm meaning. The complete data-input footprint includes its Process-variable read, Activity occurrence, local scope, wait, counters, and publication. Shared Process-variable reads are permitted by the existing equation, while a Process-variable write conflicts with that read.

## Checkpoint execution and publication contract

The pair predicate requires an admitted `WellFormedProgram`, a `runtimeStateWellFormed` exact-instance pre-state, successful `projectOpenFlowNodeOccurrences?` projection of that exact program and pre-state, two distinct operations contained in that program, successful independent enabledness witnesses from the same pre-state, complete unique-declarer footprints from the equations above, no write/read or write/write intersection in either direction, and distinct actual publication atoms. Successful open-set projection supplies the current `flowNodeOccurrenceProgramValidity`, association validity, and untagged anchor uniqueness premises that `runtimeStateWellFormed` alone does not establish. Shared reads are permitted.

Given those premises for `left` and `right`, the checkpoint requires all of these facts:

1. firing `left` preserves `right` enabledness and firing `right` preserves `left` enabledness;
2. both two-step executions succeed;
3. both executions produce exactly equal raw RuntimeState values whose affected collections retain canonical order, and both intermediate states remain runtime-well-formed and open-set-projectable;
4. the actual accepted transition and lifecycle facts are defined and identical, not merely two equal candidate values or two equal projection failures;
5. sorting the two unnumbered publication pairs by the complete paired sort key and only then assigning consecutive transition indices produces one identical committed publication without separating a transition from its lifecycle delta.

“Canonical RuntimeState” does not mean equality after a projection or an assertion-time sort. The implementation extends `runtimeStateWellFormed`'s canonical-collection criterion and every production insertion site so User Task, Message, Timer, and effect waits are ordered by complete occurrence identity; their four activation-counter families are ordered by element ID; and Activity-variable scopes are ordered first by `LocalDataOwner` discriminator and then by complete identity within that arm. The invariant applies to every add site for an affected collection, including composite operations outside this checkpoint, rather than only to the four constructors named here.

This is an internal pre-release runtime-representation replacement owned jointly with the [scoped runtime data specification](capsules/SCOPED-DATA-SPEC.md#runtime-replacement). It changes no public wire. Its targeted preservation obligation requires every existing producer, consumer, well-formedness check, fixture, and proof to use the replacement order directly; existing canonical observations and retained Temporal histories must replay unchanged; and no compatibility reader, order-normalizing boundary, or second RuntimeState shape is permitted.

The production evaluator continues to select the canonical lowest operation ID first. The proof makes that selection observationally irrelevant for the admitted pair; the sort does not serve as the proof. No existing committed-transition or lifecycle wire shape changes. Raw evaluator visit order remains private, while the published batch order remains canonical and replay-stable.

An overlapping pair is not forced into either order. It remains `ambiguousInternalChoice` until a later reviewed profile supplies an explicit semantic choice or a stronger checked commutation account.

## Checkpoint Lean assurance lane

The checkpoint's Lean lane is **proved**. Its proof owners are grouped by responsibility:

- Canonical insertion and ordering: [InternalArmingOrder](../BpmnSemantics/SemanticProcess/InternalArmingOrder.lean) owns reusable canonical insertion, ordered distinct-key counter replacement, and Activity-scope order laws.
- Preparation and footprints: [InternalCommutationCore](../BpmnSemantics/SemanticProcess/InternalCommutationCore.lean) owns the private prepared-patch representation, exact pre-state constructor, closed atom projection, and non-interference predicate.
- Program validity: [structural validity](../BpmnSemantics/SemanticProcess/FlowNodeOccurrenceProgramValidityCore.lean), [Effect validity](../BpmnSemantics/SemanticProcess/FlowNodeOccurrenceEffectProgramValidity.lean), [User Task validity](../BpmnSemantics/SemanticProcess/FlowNodeOccurrenceUserTaskProgramValidity.lean), [wait validity](../BpmnSemantics/SemanticProcess/FlowNodeOccurrenceWaitProgramValidity.lean), and [validity frames](../BpmnSemantics/SemanticProcess/FlowNodeOccurrenceProgramValidityFrames.lean) keep family validators private while exposing the structural, wait-family, and insertion-preservation facts needed by the checkpoint.
- Projection structure: [lifecycle order](../BpmnSemantics/SemanticProcess/FlowNodeOccurrenceLifecycleOrder.lean), [Process identity](../BpmnSemantics/SemanticProcess/FlowNodeOccurrenceProcessIdentityProofs.lean), and [projection shape](../BpmnSemantics/SemanticProcess/FlowNodeOccurrenceProjectionShapeProofs.lean) prove canonical lifecycle order, static/runtime owner Process-ID correspondence, and complete projected-anchor shape without duplicating production algorithms.
- Preservation: [commutation projection](../BpmnSemantics/SemanticProcess/InternalCommutationProjection.lean), [state frames](../BpmnSemantics/SemanticProcess/InternalCommutationStateFrames.lean), [runtime preservation](../BpmnSemantics/SemanticProcess/InternalCommutationRuntimePreservation.lean), and [open projection](../BpmnSemantics/SemanticProcess/InternalCommutationOpenProjection.lean) compose those facts into projectability, collection-order, identity, aggregate runtime, and accepted-lifecycle preservation.
- Commutation: the [InternalCommutation facade](../BpmnSemantics/SemanticProcess/InternalCommutation.lean) proves opposite-arm enabledness and exact raw-state commutation.
- Publication: [transition records](../BpmnSemantics/SemanticProcess/InternalCommutationTransitionRecord.lean) and [control-position deltas](../BpmnSemantics/SemanticProcess/ControlPositionDeltaProofs.lean) prove the prepared record and exact public control delta. [InternalCommutationPublication](../BpmnSemantics/SemanticProcess/InternalCommutationPublication.lean) composes them with lifecycle preservation into complete actual-publication equality plus canonical pair numbering.
- Serialization and conformance: [publication JSON](../BpmnSemantics/SemanticProcessJson/Publication.lean) consumes canonical traced order and independently replays it without re-deciding pair order. [InternalCommutationConformance](../BpmnSemantics/InternalCommutationConformance.lean) imports the public proof and publication surfaces as the exact conformance target.

This acyclic responsibility split avoids sixteen constructor-pair proofs and prevents the footprint, validity, state-update, projection, and publication accounts from drifting.

The theorem begins from admitted-program well-formedness, exact-instance runtime-state well-formedness, successful exact pre-state open-set projection, two independently enabled `OperationStep` facts, and the complete prepared-patch footprint predicate. It proves both intermediate states runtime-well-formed and open-set-projectable before proving the opposite operation remains enabled. It then proves both exact patch applications commute and proves equality of the actual accepted transition/lifecycle publication. It must not assume either intermediate invariant, either second `fire?`, equal successors, equal final states, canonical intermediate storage, defined lifecycle projection, or equal publications. Constructor proofs may reuse established transition relations, canonical collection insertion, exact occurrence issuance laws, and the existing flow-node program-validity account. They may not cite finite fixture evaluation as the theorem.

This does not claim arbitrary closure confluence, Church-Rosser, arbitrary-cardinality independence, general BPMN scheduler determinism, or the open run-level checked-source preservation theorem.

## Checkpoint independent TypeScript realization

The TypeScript core defines the same state atom vocabulary, actual untagged lifecycle-anchor collision rule, exhaustive family-declarer census, and non-interference equation without generated Lean code or a copied decision table. Program admission applies that census globally, and footprint construction reuses it for the selected operation. It corrects ordinary Message wait issuance and footprint derivation to use `owner.processInstanceId`, matching the other three families, while preserving top-level `state.control.instanceId` only as the runtime-control read. Production closure classifies the complete two-operation enabled set through that predicate and retains current ambiguity and bound precedence. The traced boundary additionally requires the exact pre-state and both intermediates to remain open-set-projectable before accepting the paired publication.

The independent executable oracle explicitly runs both orders outside the production selector and compares canonical state, transition records, and lifecycle publication. The production code must not implement commutation by running both orders and comparing their results, because that would make the claimed reason a successor-equality test rather than the reviewed footprint rule.

## Selected final-closure account

### RC completion scope amendment

Amendment approved on 2026-09-23 after the owner closed the [proof-architecture review and experiments](experiments/INTERNAL-COMMUTATION-PROOF-ARCHITECTURE-EXPERIMENT.md); the [independent review receipt](#independent-cold-review-receipt) records approval. Previously approved accounts and unfinished outcomes retain their status. This amendment authorizes the RC dependency classification, not acceptance of any unresolved outcome.

The required RC result is safe, deterministic internal closure for the complete owner-selected RC capability surface, including the capabilities to be added by `EVENT-SUBSCRIPTIONS` and `COMPENSATION-TRANSACTIONS`. It is not completion of every possible cross-family combination in raw Programs. The [operation census](#complete-operation-family-census), [RuntimeState census](#prepared-transition-and-region-footprints), and all existing accepted theorem contracts remain exhaustive and unchanged. The broader final-closure account below remains the proposal's generalization target; completing the narrower RC result does not graduate this proposal or claim that target complete.

Required for RC:

- Preserve all existing accepted behavior, proof statements, independent evidence lanes, whole-command rollback, and the current public/Temporal refusal of scheduled mode. Keep the arbitrary finite-batch theorem and canonical state/publication guarantees; no universal theorem is replaced by finite tests.
- Classify each remaining preparation/publication/integration obligation against the actual profile capability predicates, structural validators, lowering, and reachable command frontiers. Record in this owner its capability dependency, deciding evidence, and one of required, structurally excluded from RC, or unresolved. A failed witness search, scenario-corpus enumeration, or an unavailable footprint is not an exclusion argument. A structural exclusion needs an admission argument and an executable widening counterexample that reopens it.
- Complete every required obligation before admitting its capability. An unresolved dependency blocks that capability's admission and RC closure; it does not authorize rejecting previously accepted behavior or silently changing committed outcomes. Each new family retains complete preparation in both directions, successful canonical execution, successor validity/projectability, and accepted publication for its admitted independent frontiers.
- Reconcile the classification after the subscription and Compensation/Transaction accounts select their admission boundaries, and before the RC row can become satisfied. The existing [regional reachability argument](#regional-preparation-prerequisite) is bounded to its registered surface and cannot exclude those future compositions. Preserve the [same-change owners and reopen conditions](#same-change-owners-and-reopen-conditions), the hosting preflight, complete applicable gates, and governed reviews.

The initial unresolved set is explicit; this amendment itself establishes no structural exclusions:

| Obligation | Current disposition | Evidence needed to settle the RC dependency |
|---|---|---|
| Bounded Sub-Process entry: regional pair, accepted publication, finite integration | Unresolved; preserve the [unfinished checkpoint](#bounded-sub-process-arming-outcome) | Capability-level argument for every selected bounded-entry composition, or an admitted reachable frontier requiring the missing complete outcome |
| Message-boundary task arming, Sequential/Parallel Multi-Instance arming, event-race arming | Unresolved; the [composite census](#complete-operation-family-census) remains binding | Classify independent frontiers under the retained profiles and the selected subscription widening; an already satisfied feature row does not supply a missing commutation proof |
| Compensation trigger/frontier preparation and snapshot-enabled combinations | Unresolved; pre-registration dependency | Selected public Compensation/Transaction admission and reachable frontiers, including local control, scope creation, and data arming under snapshot declarations |
| Existing scheduling, finite batches, classifications, and Temporal boundary | Required: retain implemented results and close any remaining evidence gaps | Reconcile the existing [final discriminators](#remaining-final-closure-discriminators) with exact current evidence; private scheduling does not imply public/hosted scheduling |

Optional for RC are proof-only cleanups that demonstrably reduce maintenance while preserving the accepted contract. The two experimental replacements are not selected. Generalization solely for capabilities outside the selected RC surface may remain deliberately open only after its exclusion argument is recorded, with reason and admission-widening reopen trigger in [`implementation-status-owner:ENGINE-RUNTIME-PROOF`](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md#explicitly-absent).

Excluded from this amendment are a new preparation compiler or store representation, a changed conflict/acceptance rule, stronger raw-state validity, sequential fallback for ambiguous frontiers, public scheduled choice, new BPMN scope, and treating failed experiments as evidence that existing proofs are unnecessary. It adds no host primitive or wire change; the [existing Temporal preflight](#temporal-hosting-and-information-preservation-preflight) continues to apply.

### RC admission dependency assessment

Assessment after the accepted [ordinary snapshot arming and pipeline correspondence outcomes](capsules/COMPENSATION-TRIGGER-HANDLER-PROPOSAL.md#independent-cold-review-receipt), updated 2026-09-24. This classifies existing admission under the approved RC amendment; it changes no profile, runtime rule, accepted theorem, or future capability selection. The amendment's initial unresolved set above remains its historical starting point.

For the five current composite profiles below, [exact operation multisets](../packages/semantic-core/src/semantic-program-profile-shape.ts), [profile details](../packages/semantic-core/src/semantic-process-profile.ts), and [graph validation](../packages/semantic-core/src/semantic-process-graph-admission.ts) jointly exclude a concurrent ordinary branch at composite arming. Start contributes one token; these profiles contain no fork, inclusive split, or non-interrupting sibling producer. Graph validation requires one producer and consumer per place, scoped ownership, reachability, and the selected acyclic policy. A preceding ordinary Task temporarily replaces that single control path with one wait. Composite arming consumes its one incoming token atomically; its paired waits, Multi-Instance children, or child scope do not constitute separate internally offered arming operations. Completion or an external winner releases at most one selected continuation; a zero-item Multi-Instance arm may release it immediately. External commands do not interleave with closure, and scope completion cannot bypass live child/wait work. Thus singleton arming does not require the composite to be the first operation.

| Obligation and capability | Current RC disposition | Deciding evidence and reopening condition |
|---|---|---|
| Bounded Sub-Process entry mixed batches under the retained `SubProcessBoundaryTimer` profile | Structurally excluded from this current profile; its unfinished general checkpoint stays unaccepted | Exact nested operation shape has no independent branch before entry; child work is created by entry. The retained [admission controls](../packages/bpmn-source/test/internal-commutation-rc-admission.test.ts) reject a structurally valid fork widening and admit a Task-before-entry control. Reopen for another admitting profile, changed lowering/initialization, or subscription/Transaction composition. |
| Message-boundary Task, Sequential/Parallel Multi-Instance, and event-race mixed arming under their four retained profiles | Structurally excluded from these current profiles | The same one-path argument applies; Message-boundary admission additionally fixes its exact topology. All four fork widenings reject; admitted Task-before-arm controls prevent mistaking first position for the invariant. Reopen when admission or composite execution can produce an independently marked sibling. |
| Ordinary arming under snapshots | Required and accepted for the focused Lean runner | The reviewed source Start and six-family actual-runner proof close this preparation/publication outcome. Preserve generic snapshot exclusion and exact public/Temporal admission. |
| Snapshot execution/publication correspondence for the proposed public Compensation capability | Required and accepted for the private checked-source checkpoint | The [registration prerequisites](capsules/COMPENSATION-TRIGGER-HANDLER-PROPOSAL.md#public-registration-prerequisites) record independently accepted complete scenario/publication correspondence through success, handler failure, and stale rejection. This finite evidence neither registers the capability nor establishes equal raw Lean/TypeScript admission domains. |
| Subscription widening, snapshot local-control/scope/data combinations, trigger concurrency, and bounded Transaction cancellation | Unresolved for the selected future RC capabilities | Settle against each approved capability's exact admission and reachable frontiers before admitting it. No absent witness or current-profile exclusion disposes these future dependencies. |

The admission audit retained a further limit: a same-cardinality trigger-before-join rewire satisfies Lean raw Program/profile/start admission but fails TypeScript structural admission. Therefore, neither the profile multiset alone nor the checked-source trigger-isolation argument may exclude arbitrary Lean snapshot Programs. The [ten executable composite controls](../packages/bpmn-source/test/internal-commutation-rc-admission.test.ts) establish concrete admission/widening discriminators, not an enumeration proof over raw Programs. Preserve all existing finite-batch, canonical publication, private-scheduling, and Temporal-refusal guarantees; do not resume exhaustive family-pair expansion without a selected capability dependency. The RC row stays active until future boundaries are reconciled.

#### Subscription dependency findings

Inspection and bounded probes at `ab89b2e4` on 2026-09-24 identify the next capability-design constraints. These findings select no new semantic account, admission restriction, race winner, or proof exemption. The five current-profile exclusions above remain unchanged.

| RC dependency | Existing mechanism and deciding boundary | Consequence for the subscription proposal |
|---|---|---|
| Handler ownership and repeated occurrence identity | [Activity records](../packages/semantic-core/src/activity-occurrence.ts) already bind tagged Timer/Message occurrences to an exact body. [Monitored firing](../packages/semantic-core/src/semantic-process-monitored-task-runtime.ts) removes the one-shot Timer while preserving its host record. | Reuse this ownership boundary; repetition alone establishes no need for a new subscription store. Select fresh trigger identity, rearming, stale-delivery refusal, and publication lifetime explicitly. |
| Concurrent ordinary handler work | A constructed second boundary token lets the existing ordinary arm retain `HandlerTask#1` and issue `HandlerTask#2`; both completion orders preserve the host and full runtime validity. The current one-shot account cannot generate that second token. | Existing occurrence and quiescence machinery is a reuse candidate, not a repeated-subscription capability or a universal correspondence proof. Establish reachable frontiers under the new source account before assigning any missing commutation law. |
| Durable Timer replacement | The [Timer owner](../packages/temporal-adapter/workflow/src/durable-timer-owner.ts) retains the fired identity until withdrawal reconciliation. Both immediate arming and reconciliation of a different identity refuse, even after firing. | Atomic semantic consumption/rearming needs an explicit host handoff preserving stale-callback suppression; the current one-shot owner cannot be assumed to implement it. |
| Continuation with a recurring deadline | [Rollover safety](../packages/temporal-adapter/workflow/src/workflow-rollover-safety.ts) refuses while a managed Timer or readiness callback is retained. | A proposed recurring account must exhibit a safe successor checkpoint or an explicit reviewed bound; repeatedly installing another deadline cannot silently defer rollover forever. Preserve the existing capacity contract. |
| Completion/trigger races | The [managed scheduler](../packages/temporal-adapter/workflow/src/bounded-deadline-scheduler.ts) and [direct-VM witness](../packages/temporal-adapter/testkit/test/monitored-deadline-temporal.test.ts) refuse competing callback kinds in one activation before choosing a winner. | The RC account must make its allowed semantic schedules and host linearization explicit, including duplicate/rejected callbacks. Host callback order is not an existing portable winner, and old-profile refusal remains binding. |
| Scope cleanup and already spawned paths | [Monitored-task tests](../packages/semantic-core/test/non-interrupting-boundary-timer.test.ts) distinguish host completion, withdrawn deadline, and surviving handler work. Existing regional cancellation owns subtree removal separately. | Select and evidence normal host completion versus containing-scope cancellation over every admitted repeated path. Neither reusing an ownership record nor passing a root-only witness closes nested cleanup. |

The root separately asserted four receipts: `/tmp/bpmn-rc-subscription-baseline` passes 21 existing semantic tests; `/tmp/bpmn-rc-subscription-host-baseline` passes seven ownership/direct-VM tests; `/tmp/bpmn-rc-subscription-runtime-probe-2` checks the constructed two-occurrence state and both completion orders; `/tmp/bpmn-rc-subscription-timer-owner-probe-2` checks the actual Timer owner's replacement/refusal and rollover predicates with only the SDK cancellation scope mocked. The latter two are diagnostic mechanism probes, not admitted source, Lean proof, or live-service refinement evidence. Their initial failures were missing probe imports, not semantic failures.

The [subscription account](capsules/REPEATABLE-EVENT-SUBSCRIPTIONS-PROPOSAL.md) owns the selected lifetime, admission and hosting decisions. Capability claims require admitted source and reachable-frontier witnesses. Quantified scheduling assurance follows its [bounded proof domain](capsules/REPEATABLE-EVENT-SUBSCRIPTIONS-PROPOSAL.md#scheduling-proof-domain-amendment), including necessary obligations over arbitrary well-formed predecessors and finite batch prefixes within that admission. Preserve existing guarantees and the stated effort bound; unrelated family expansion and a generic pair-proof programme remain excluded.

##### Subscription checkpoint dependency reassessment

The private [subscription semantic checkpoint](capsules/REPEATABLE-EVENT-SUBSCRIPTIONS-PROPOSAL.md) implements the approved source/Program admission through independent Lean/core lifetime execution, exact E1/E2 publication, and the selected scheduling obligations. All four bounded proposition groups are closed through direct reuse and focused correction. The complete Lean gate and source-bound pipeline pass; independent semantic-checkpoint review remains required before hosting or registration. No additional aggregate-invariant programme or family-pair expansion is selected.

| Dependency | Disposition at this checkpoint | Deciding boundary |
|---|---|---|
| Message-host arming with ordinary child entry | Required; quantified integration and finite Lean/core evidence pass | The [source-frontier controls](../packages/bpmn-source/test/repeatable-event-subscription-source.test.ts) expose both complete preparations after Start/root fork for both interruption values. The [retained model](../packages/bpmn-source/test/fixtures/repeatable-event-subscriptions/message-host-with-child-entry.bpmn) places the Message host outside the child; the older child-local fixture cannot establish this pair. |
| Message-host and bounded/monitored-child arming with passive arming, divergent control, or None End | Required; selected quantified pairs and unchanged batch/publication lift pass | The [approved scheduling obligations](capsules/REPEATABLE-EVENT-SUBSCRIPTIONS-PROPOSAL.md#reachable-scheduling-obligations) and valid-predecessor proof domain own these cases. |
| Two boundary composites, a second child entry, or excluded Effect/Call/data/MI/Event-race/snapshot composition | Excluded by the new profile's structural surface; no extra proof cell selected | Complete admission, source widening controls and [quantified exclusions](../BpmnSemantics/SemanticProcess/InternalSubscriptionPairExclusions.lean) derive the one-boundary/one-child and excluded-operation consequences. Shared writes make two preparations of the same composite dependent. |
| Message host with regional completion or Terminate | Required over admitted valid predecessors; quantified integration passes | Source-bound valid-intermediate controls construct outside Message arming beside quiescent completion or Terminate. Both interruption values preserve full preparations and yield equal state and accepted publication in both orders. |
| Bounded/monitored-child arming with regional completion or Terminate | Dependent under selected admission and runtime validity | The sole-child declaration and valid live-scope binding imply that prepared entry has no live nonroot scope. Root completion conflicts with its control read; root Terminate covers its live owner. No reachability or successor-validity premise is added. |

The direct reuse pass exposed a support-domain residual: extending [the prepared sum](../BpmnSemantics/SemanticProcess/InternalPreparedTransition.lean) enlarged the raw `Prepared`/`Independent` domain beyond source-frontier exclusions. The [approved proof-domain amendment](capsules/REPEATABLE-EVENT-SUBSCRIPTIONS-PROPOSAL.md#scheduling-proof-domain-amendment) resolves that boundary with an immutable full admission certificate on the two new constructor paths, including application revalidation and Lean's `Prepared` proposition. [Independent refusal controls](../packages/semantic-core/test/internal-transition-subscription-admission.test.ts) reject invalid structure, excluded Effect and legacy-profile inputs while preserving raw helpers and the older TypeScript bounded-scope domain. Footprint conflict semantics and existing theorem contracts remain intact.

The [Message-host preservation theorem](../BpmnSemantics/SemanticProcess/InternalMessageTaskRuntimePreservation.lean) derives the complete unchanged runtime predicate from raw preparation and predecessor validity. [Hosting-owner identity](../BpmnSemantics/SemanticProcess/InternalSubscriptionOwnerIdentity.lean) follows from the selected depth-one forest, Call exclusion and structural projection validator over arbitrary valid predecessors. [Pair projection](../BpmnSemantics/SemanticProcess/InternalMessageTaskPairProjection.lean), [complete open projection](../BpmnSemantics/SemanticProcess/InternalMessageTaskOpenProjection.lean), and [accepted publication](../BpmnSemantics/SemanticProcess/InternalMessageTaskAcceptedPublication.lean) establish atomic Task/Message/Activity insertion, exact lifecycle and token delta, independent counters and predecessor-template preservation without assuming intermediate projectability. The corresponding [child projection](../BpmnSemantics/SemanticProcess/InternalBoundedScopeProjection.lean) and [publication](../BpmnSemantics/SemanticProcess/InternalBoundedScopePublication.lean) cover both boundary dispositions.

The [Message pair dispatcher](../BpmnSemantics/SemanticProcess/InternalMessageTaskTransitionPair.lean) and [child pair dispatcher](../BpmnSemantics/SemanticProcess/InternalBoundedScopeTransitionPair.lean) discharge the enumerated matrix. [Regional Message commutation](../BpmnSemantics/SemanticProcess/InternalMessageTaskRegionalCommutation.lean) derives both complete preparations and literal successor equality; its [publication frame](../BpmnSemantics/SemanticProcess/InternalMessageTaskRegionalPublication.lean) preserves both anchors through the actual atomic projection. The existing single-step theorems moved into [InternalTransitionPreservation](../BpmnSemantics/SemanticProcess/InternalTransitionPreservation.lean) without changing their statements. The unchanged finite-prefix, permutation and accepted canonical-publication lift passes `/tmp/bpmn-subscription-canonical-batch-publication` (424 jobs).

Expanded completion/arming selectors also required migrating existing consumers. The final [bounded selection](../BpmnSemantics/SemanticProcess/InternalBoundedScopeRegionalSelection.lean) and [retention](../BpmnSemantics/SemanticProcess/InternalBoundedScopeRegionalRetention.lean) laws preserve live and consumed deadlines, exact child/Activity/Timer censuses and tagged identity freshness; `/tmp/bpmn-subscription-bounded-regional-migration-final` passes 336 jobs. A raw constructed bounded-child value could previously name different contract and selection operations; [template selection](../BpmnSemantics/SemanticProcess/InternalTransitionPublication.lean) now refuses that mismatch. Valid preparations already bind them, so no valid prepared outcome changes.

The retained-body correction implements the approved `ESL-RETAIN-01`: inner Terminate retains the selected child root and its parent-owned Activity/deadline; ordinary child completion then withdraws both. Descendant/owner cancellation and whole-body removal still withdraw the exact records and handlers. The [Activity ownership specification](ACTIVITY-OCCURRENCE-OWNERSHIP-SPEC.md#stable-semantic-rules) routes the superseded `AOO-CANCEL-01` account to that amendment. Fixed-memory qualification exposed stale Terminate expectations in [the regional publication fixture](../BpmnSemantics/InternalRegionalCancellationProjectionConformance.lean); independent evaluation distinguished both Message/Timer retention cases from unchanged Error-removal controls. The complete Lean gate then exposed four legacy admission destructurings, the corresponding termination classifier and a finite fixture's missing constructor cases. Their repairs retain the consumer theorem contracts and classify actual retained scope disposition.

Current evidence, 2026-09-24:

- `/tmp/bpmn-subscription-checkpoint-full-lean-final`: complete Lean gate, 651 jobs, exit 0. The eleven selected consumer theorems in `/tmp/bpmn-subscription-checkpoint-axiom-audit` use only the standard `propext`, `Classical.choice` and `Quot.sound` axioms.
- `/tmp/bpmn-subscription-checkpoint-pipeline-final`: all 27 source-bound schedules pass independent lowering, command closure, complete outcomes/observations, E1/E2 publication and committed-trace replay, including retained-body cleanup.
- Fresh complete package receipts pass 1,281 core, 869 source, 128 differential, 85 protocol and 167 platform-contract tests. The complete infrastructure gate `/tmp/bpmn-subscription-checkpoint-infrastructure-ready` passes all 582 checks; contract schema/projection checks pass 109 cases and contract-type checks also pass.
- `/tmp/bpmn-subscription-checkpoint-registered-pipeline-retry`: all 72 registered cases pass with unchanged timeout after the contended first attempt; the [cost record](CAPSULE-COST-LEDGER.md#subscription-checkpoint-resource-qualification-2026-09-24) preserves both outcomes.
- [Twelve immutable fixture qualifications](CAPSULE-COST-LEDGER.md#subscription-checkpoint-resource-qualification-2026-09-24) pass under the unchanged 3 GiB ceiling. The regional-publication fixture remains close to that ceiling; no resource-headroom improvement is claimed.

This evidence establishes a private implementation candidate. Public registration, Temporal refinement, retained business corpus/disclosure, governed checkpoint approval, and RC row closure remain open.

### Complete operation-family census

Final closure classifies the complete `SemanticOperation` discriminated union by semantic criterion. A TypeScript exhaustive switch, a Lean exhaustive definition, and one generated documentation guard must fail together when a variant is added without a classification. The classification is not a count and does not infer completeness from the table's prose.

| Operation family | Exact current variants | Final-closure treatment |
|---|---|---|
| Process initiation | `initiate`, `initiateMessage`, `initiateTimer` | Singleton by admitted start kind and `initiationPending`. Exactly one matching initiation may be enabled for one accepted start input. A state or Program that enables two is invalid rather than a schedulable frontier. |
| Ordinary wait arming | `awaitUserTask`, `awaitMessage`, `awaitPayloadMessage`, `awaitCorrelatedPayloadMessage`, `awaitTimer`, `awaitEffect` | Prepared, footprint-bearing, and batchable. The implemented checkpoint equations remain the first six exact instances of the general contract. The correlated arm reads its selected Process Property and publishes one complete candidate atom in addition to the ordinary Message effects. |
| Composite wait and Activity arming | `enterBoundedScope`, `enterMonitoredScope`, `awaitBoundedUserTask`, `awaitMessageBoundedUserTask`, `awaitMessageMonitoredUserTask`, `awaitMonitoredUserTask`, `awaitSequentialMultiInstanceUserTask`, `awaitParallelMultiInstanceUserTask`, `awaitEventRace`, `awaitDataInputUserTask`, `awaitDataInputOutputUserTask`, `awaitDataOutputUserTask` | Prepared, footprint-bearing, and batchable only when their complete controller, Activity record, handler, race, local-variable, counter, wait, and publication effects are represented. Treating the ordinary inner wait as the whole footprint is forbidden. Input-only and composed data arming read one Process binding they never write, accept read/read overlap, and reject write/read overlap. Composed arming performs no output write. Output-only arming has no Process read but still owns the Activity record, empty local scope, separate counters, wait, and publication. The [data arming outcome](#standalone-activity-data-arming-outcome) owns these three variants; the [Timer-task outcome](#timer-task-arming-outcome) owns both attached-Timer User Task variants. Other composite families require their own complete accounts. |
| Scope creation and Call invocation | `enterScope`, `invokeProcess` | Prepared, footprint-bearing, and batchable. Fresh occurrence issuance, parent or caller consumption, child or called entry, Call association, and every created ownership fact are part of the footprint. |
| Local control transformation | `duplicate`, `synchronize`, `choose`, `selectMany`, `synchronizeSelected` | Prepared, footprint-bearing, and batchable. Expression and selected-set reads are explicit. A deterministic condition result is not a scheduler choice. |
| Merge and ordinary End | `mergeExclusive`, `reachNoneEnd` | Prepared, footprint-bearing, and batchable when the selected input is unique. More than one offered input to `mergeExclusive` is an observable local choice and uses the choice contract below rather than list order. |
| Scope return, completion, and interruption | `returnProcess`, `completeScope`, `throwError`, `terminateScope` | Prepared with region-aware footprints. Two such transitions, or one such transition and another operation, are batchable only when their exact affected occurrence closures and parent or caller outputs are disjoint. Ancestor and descendant regions conflict even when their root identifiers differ. |
| Compensation trigger and frontier activation | `triggerCompensation` | The Compensation semantic checkpoint implements trigger construction and simultaneous maximal-frontier activation as one atomic state transformation. It remains unavailable to commutation until this proposal adds its complete preparation, retained-source, trigger/frontier, context, counter, token, and publication footprint; any multi-enabled frontier containing it therefore fails closed. |
| Externally addressed completion | `completeParallelMultiInstanceUserTask` | Never an internal-closure candidate. It remains reachable only through its exact external stimulus path, so returning a footprint is a defect. |

Every future operation variant must enter exactly one criterion above or add a separately reviewed criterion. A variant may be classified singleton only through a structural admission invariant and an executable counterexample that fails when a second candidate becomes reachable. “Unsupported by the footprint helper” is not a final classification for an operation that can occur in a registered internal frontier.

### Prepared transition and region footprints

Every current operation is first evaluated by an exhaustive pre-frontier producer whose result is exactly one of `disabled`, `prepared(transition)`, or `localAlternatives(nonempty canonical transitions)`. Every prepared value carries an exact `InternalAlternative`: `operation(operationId)` for a single-successor operation or `mergeInput(operationId, owner, inputControlPlace)` for one offered `mergeExclusive` input. A multi-offer merge therefore produces one prepared transition per exact offered token owner and input place; it never disappears merely because the current single-successor evaluator returns `none`. Flattening all producer results yields the complete alternative frontier. Selecting one member applies that prepared transition and discards no other member by policy: the evaluator recomputes the whole producer frontier from the successor state.

Every batchable alternative produces one `PreparedInternalTransition` from the admitted Program and exact pre-state before production order is selected. It contains the exact alternative, selected runtime owner, any deterministic branch result, exact state patch, complete semantic reads and writes, and a numbering-free transition/lifecycle publication template. Preparation neither applies the patch nor consults a successor-state difference. The existing ordinary-arm representation becomes one specialization of this owner rather than a parallel mechanism.

For `awaitCorrelatedPayloadMessage`, the prepared footprint reads the exact `processVariable(propertyId)` binding and publishes `correlationCandidate(completeAddress, subscriptionOccurrence, correlationPropertyId, processPropertyId)` in addition to the ordinary Message wait and lifecycle atoms. Equal-key candidates with distinct subscription occurrences do not collide merely because their values agree; a Process-variable write or equal complete candidate atom does. The key value stays protected by the Process-variable read rather than becoming a duplicate RuntimeState atom.

The state-atom vocabulary is selected by the following exhaustive RuntimeState census. Family codes are `P` Process initiation, `W` ordinary wait arming, `A` composite wait or Activity arming, `S` scope creation or Call invocation, `L` local control, `M` merge or ordinary End, `R` scope return/completion/interruption, and `C` compensation trigger/frontier activation. `X` records externally addressed completion or stimulus admission only for same-change preservation; it never becomes a prepared internal alternative. A family code names only the variants that actually use the row, and each prepared owner enumerates its exact keyed atoms. `R/W` means semantic read and write, not RuntimeState representation. `controlTokens`/`tokens`, `userTaskWaits`/`waits`, and `taskActivations`/`activations` are naming differences only.

| TypeScript field | Lean field | Atom, key, and ownership | Readers / writers | Region interaction |
|---|---|---|---|---|
| `control` | `control` | global `runtimeControl` singleton | frontier read `P,W,A,S,L,M,R`; read/write `X`; write `R` | a root completion or cancellation conflicts through this global write; a non-root region does not own it |
| `initiationPending` | `initiationPending` | global `initiationPending` singleton | read `P,R,X`; write `P,X` | root completion conflicts; non-root removal does not |
| `scopeOccurrences` | `scopeOccurrences` | `scopeOccurrence(id)` including `scopeParent(id,parent)`; owned by `id`, with a creation dependency on its parent | read `P,W,A,S,L,M,R,X`; write `A,S,R,X` | insertion into or removal of any member conflicts with that member's region; ancestor and descendant membership overlap |
| `controlTokens` | `tokens` | multiplicity bucket `controlToken(owner,place) -> Nat`, owned by `owner`, plus owner census `tokenOwners(place)`, independent of the selected owner | read/write `P,W,A,S,L,M,R,X`; every owner-selection read uses the census and every insertion/removal that can change it writes the census | every member-owned consumed or produced bucket conflicts with removal of its region; regional token removal also writes each affected place census |
| `userTaskWaits` | `waits` | `userTaskWait(occurrence)`; owned by its runtime owner | read/write `W,A,R,X` | a member-owned wait conflicts with region removal |
| `messageWaits` | `messageWaits` | `messageWait(occurrence)`; owned by its runtime owner | read/write `W,A,R,X` | a member-owned wait conflicts with region removal |
| `timerWaits` | `timerWaits` | `timerWait(occurrence)`; owned by its runtime owner | read/write `W,A,R,X` | a member-owned wait conflicts with region removal |
| `effectWaits` | `effectWaits` | `effectWait(occurrence)`; owned by its runtime owner | read/write `W,R,X` | a member-owned wait conflicts with region removal |
| `effectIncidents` | `effectIncidents` | `effectIncident(incident)` and its suspended `effectWait`; owned by the wait owner | read `W,R,X`; write `R,X` | incident and suspended wait conflict with owner-region removal |
| `selectedBranchSets` | `selectedBranchSets` | `selectedBranch(owner,selectionKey)`, owned by `owner`, plus the proposed owner-free population atom `selectedBranchOwners(selectionKey)` | read/write `L,R`; same-key population reads and every insertion/removal follow the readiness amendment | a member-owned selection conflicts with region removal; removal also writes each affected selection-key population |
| `eventRaces` | `eventRaces` | `eventRace(occurrence)` plus its two wait associations; owned by the race owner | read/write `A,R,X` | race and both associated waits conflict with owner-region removal |
| `calledProcessOccurrences` | `calledProcessOccurrences` | `callOccurrence(callerOccurrence)` plus caller/called-root association; owned jointly by the caller and called root | read/write `S,R` | removal of either endpoint conflicts; region membership follows both transitive scope-parent and Call edges, while disjoint call trees remain separable |
| `activityOccurrences` | `activityOccurrences` | `activityOccurrence(activityId)`, body-member, and attached-handler atoms; owned by the containing scope and containing its body members | read/write `A,R,X` | containing-scope removal, body-member removal, or concurrent body insertion conflicts |
| `sequentialMultiInstanceControllers?` | `sequentialMultiInstanceControllers` | global TypeScript `sequentialControllersPresence` representation atom plus `sequentialController(activityId)` and indexed slots; entries are owned by their Activity and containing scope | read/write `A,R,X`; unrelated patches preserve presence byte-for-byte | Activity or containing-region removal conflicts with entries; no region owns the presence atom |
| `parallelMultiInstanceControllers?` | `parallelMultiInstanceControllers` | global TypeScript `parallelControllersPresence` representation atom plus `parallelController(activityId)` and indexed slots; entries are owned by their Activity and containing scope | read/write `A,R,X`; unrelated patches preserve presence byte-for-byte | Activity or containing-region removal conflicts with entries; no region owns the presence atom |
| `compensationActivityRetentions?` | `compensationActivityRetentions` | global TypeScript presence representation atom plus `compensationActivityRetention(owner)` and immutable outer-Activity records; each register is owned by its root scope occurrence | read/write `P,R`; absent from every current Activity-completion footprint until producer integration reopens the account | root completion removes the matching register; unrelated regions neither own nor mutate it |
| `compensationParentContextRetentions?` | `compensationParentContextRetentions` | global TypeScript presence representation atom plus `compensationParentContextRetention(parent,handler)` and immutable root-to-parent context frames; each record is owned by its exact parent occurrence and containing root | read `R`; write `P,S,R` | parent completion reads and promotes only its exact record; unsuccessful regional removal purges owned records, while context-variable reads conflict through the corresponding variable atoms |
| `compensationTriggers?` | `compensationTriggers` | global TypeScript presence representation atom plus `compensationTrigger(id)`, its owner, canonical handler lifecycles, and dependency edges; each trigger is owned by its exact root scope occurrence | read/write `C,R,X` | owner-region removal conflicts with the complete trigger; terminal failure preserves only the explicitly selected tombstone rather than a live region |
| `compensationHandlerEffectWaits?` | `compensationHandlerEffectWaits` | global TypeScript presence representation atom plus `compensationHandlerEffectWait(effectId)` and its exact trigger/handler join; each wait is owned through that joined trigger and handler | read/write `C,R,X` | removing a trigger or active handler region removes its joined wait, while terminal handler lifecycle and monotonic occurrence counters remain separate |
| `variables` | `variables` | `processVariable(name)` is Process-global; `activityVariable(localDataOwner,name)` and `activityVariableScope(localDataOwner)` are occurrence-owned, where the closed owner discriminator has distinct `effectOccurrence` and `activityOccurrence` arms | read `W,A,L,R,X`; write `W,A,R,X` | Process bindings conflict by name and are not removed with a child region; Activity-local bindings conflict with their discriminated owner's region |
| `taskActivations` | `activations` | monotonic `activation(userTask,elementId)` | read/write `W,A,X` | global keyed high-water mark, never region-owned or rewound; equal keys conflict |
| `messageActivations` | `messageActivations` | monotonic `activation(message,elementId)` | read/write `W,A` | global keyed high-water mark, never region-owned or rewound; equal keys conflict |
| `timerActivations` | `timerActivations` | monotonic `activation(timer,elementId)` | read/write `W,A` | global keyed high-water mark, never region-owned or rewound; equal keys conflict |
| `eventRaceActivations` | `eventRaceActivations` | monotonic `activation(eventRace,elementId)` | read/write `A` | global keyed high-water mark, never region-owned or rewound; equal keys conflict |
| `callActivations` | `callActivations` | monotonic `activation(call,elementId)` | read/write `S` | global keyed high-water mark, never region-owned or rewound; equal keys conflict |
| `effectActivations` | `effectActivations` | monotonic `activation(effect,elementId)` | read/write `W` | global keyed high-water mark, never region-owned or rewound; equal keys conflict |
| `scopeActivations` | `scopeActivations` | monotonic `activation(scope,definitionScopeId)` | read/write `A,S,X` | global keyed high-water mark, never region-owned or rewound; equal keys conflict |
| `activityActivations` | `activityActivations` | monotonic `activation(activity,elementId)` | read/write `A` | global keyed high-water mark, never region-owned or rewound; equal keys conflict |
| `endOccurrences` | `endOccurrences` | global commutative `endIncrement` delta and, only for a predicate that observes the absolute count, `endCount` | relative read/write `M,R`; absolute read only where explicitly classified | region removal never rewinds it; independent `+1` deltas compose, while an absolute-count read conflicts |
| `logicalTimeMs` | `logicalTimeMs` | global `logicalTime` singleton | publication read `P,W,A,S,L,M,R`; read/write `X` | no region owns it; shared reads commute and every write conflicts |

A destructive or completing transition additionally carries an `occurrenceRegion(root,members)` write derived from the exact pre-state ownership closure across both scope-parent and Call associations. A region conflicts with every atom owned by one of its members, and two regions conflict when their member sets intersect. This makes disjoint sibling-region work composable while refusing ancestor/descendant cancellation, a concurrent insertion into a region being removed, a called-root collision, and a parent or caller continuation collision. A generated guard parses both RuntimeState definitions and the table keys and rejects a missing, duplicate, or renamed field; its mutation tests add one field to each language independently. The guard proves census coverage only. Per-field transition mutations prove the chosen key, ownership, direction, and region behavior.

TypeScript stores each token bucket as one row with `multiplicity`; Lean stores one `ControlToken` per unit. TypeScript nests wait and Activity identities where Lean stores the same values flat. TypeScript `ActivityOccurrence` also retains `operationId`, while Lean deliberately derives the unique operation from the admitted Program. The prepared TypeScript value preserves that stored field exactly and the Lean correspondence proves the derivation. TypeScript's optional Multi-Instance collections use absence as a profile-specific exact continuation shape, distinct from present-empty at the representation boundary; Lean's plain lists represent both as the same semantic membership. No independent TypeScript patch may change presence.

Before any token-producing or region family joins production batching, preparation must represent the complete place-wide owner census read by `onlyTokenOwner` as `tokenOwners(place)`. A producer inserting another owner's token at that place can invalidate selection without touching the selected owner's multiplicity bucket; regional removal can change the same census. The census atom supplements owner-specific multiplicity atoms. The [census checkpoint](#place-wide-token-owner-census-checkpoint) implements that prerequisite in the existing preparation domains. Production integration of additional families remains open; the discriminator is grounded in [the runtime owner lookup](../BpmnSemantics/SemanticProcess/RuntimeState.lean) and is outside [the ordinary-arm pair theorem](../BpmnSemantics/SemanticProcess/InternalCommutation.lean)'s producer domain.

Arbitrary raw-state commutation also requires canonical storage at every newly affected Lean insertion site. Final closure replaces prepend-based insertion for distinct control-token buckets, ordinary child scope occurrences, scope activation rows, Call activation rows, and event-race activation rows, as well as any remaining wait, counter, controller, record, or variable-scope collection reached by a prepared family. The TypeScript and Lean insertion oracles compare exact stored values, not observations sorted by the test.

Each prepared-operation owner owes reusable laws in Lean and equivalent executable discriminators in TypeScript over every admitted Program and every runtime-well-formed, canonical, open-set-projectable intermediate state: applying its patch implements the existing operation relation and preserves those premises; applying an independent patch preserves byte-for-byte equality of the complete prepared value, including its footprint and publication template; and applying two independent patches commutes in raw canonical state and numbering-free publication. These laws must remain applicable after every preceding independent patch, so adjacent swaps do not assume their intermediate premises. The executor must apply the classified prepared patch or check equality of the complete re-derived preparation before accepting the step; matching only its operation ID is insufficient. Composite operations may reuse established per-family preservation theorems, but no family may claim a complete footprint by calling its successor and diffing state. These remain final-integration obligations for the remaining families; the finite prepared arming checkpoint below establishes them only for its closed arming domain.

The [Lean region module](../BpmnSemantics/SemanticProcess/InternalCommutationRegion.lean) is imported by [the maintained library root](../BpmnSemantics.lean). That inclusion supplies the bounded ownership-closure definitions; it establishes neither production integration of region families nor a general region-commutation proof.

### Selected-join readiness dependency amendment

The existing [TypeScript selector](../packages/semantic-core/src/semantic-process-inclusive-gateway-runtime.ts) and [Lean selected synchronization](../BpmnSemantics/SemanticProcess/InclusiveGateway.lean) accept exactly one ready record across all owners of the operation's selection key. A record is ready when every expected input has an owned token. The [preparation prerequisite](#local-control-preparation-prerequisite) records two valid-state counterexamples to protecting only the selected record. This amendment changes dependency tracking for that existing selector; it changes no Inclusive Gateway meaning, runtime validity, admitted topology, or public contract.

Add `selectedBranchOwners(selectionKey)` to the closed state-atom vocabulary. It protects the complete same-key record population, including absence, independently of any selected owner. Equality uses the exact selection key and existing tagged Unicode-scalar ordering. No occurrence region owns this atom; regional mutations enumerate the keys they change. The existing `selectedBranch(owner,selectionKey)` atom protects the complete keyed record content. Lean introduces both typed atoms when its preparation vocabulary grows; neither is currently part of its ordinary/composed arming vocabulary.

| Prepared operation | Required selected-record reads | Selected-record writes |
|---|---|---|
| `selectMany` | Absence of the selected owner's `selectedBranch(owner,key)` | Inserted `selectedBranch(owner,key)` and `selectedBranchOwners(key)` |
| `synchronizeSelected` | `selectedBranchOwners(key)`; every pre-state same-key `selectedBranch(owner,key)`; every `controlToken(owner,input)` named by each such record, including absent buckets and inputs of unready records | Removed `selectedBranch(owner,key)` and `selectedBranchOwners(key)` |
| Regional removal | Existing region and operation-specific reads remain required | Every removed record's `selectedBranch(owner,key)` and one `selectedBranchOwners(key)` for each affected key, derived from the exact pre-state ownership closure |

Token writes retain their existing owned-bucket and place-census obligations. The selected join needs no `tokenOwners(place)` read because each record supplies the exact owner; its separate population read protects record insertion and removal. Readiness reads follow each record's actual `expectedInputs`, not only the operation's declared join inputs, because the current runtime invariant does not equate those lists. Atom construction deduplicates repeated dependencies before canonical validation while retaining every distinct owner and key. Complete-preparation equality, not merely unchanged enabledness, remains the frame obligation.

The writer boundary includes Inclusive split insertion, selected-join consumption, [Call cleanup](../packages/semantic-core/src/semantic-process-call-runtime.ts), and [scope cancellation](../packages/semantic-core/src/semantic-process-scope-cancellation.ts), following transitive scope-parent and Call ownership. Termination includes records owned by its retained root. Error, Terminate, and Call-return preparation must enumerate affected populations before any regional family can batch. Scope completion adds no removal write when its existing quiescence condition leaves no record to remove. Initialization and [terminal Compensation cleanup](../packages/semantic-core/src/compensation-trigger-handler-completion.ts), including the [Lean cancellation path](../BpmnSemantics/SemanticProcess/CompensationTriggerHandlerCancellation.lean), remain external or unavailable to batching; any later prepared writer must carry the same population obligation. This amendment admits none of those excluded families.

The separating oracle uses one structurally admitted child-scope Program with two live child activations and one ready same-key record. In the first state, a token producer makes the second existing record ready. In the second, an Inclusive split inserts a second ready record whose expected token already exists. Both predecessor and actual successor must independently pass runtime well-formedness, canonical storage, open-set projection, and control-position projection. The join is prepared before either producer and unavailable afterward; corrected footprints must conflict. Removing the unready-record bucket read must restore the first wrong independence result, while removing the population atom must restore the second. These are admitted-Program state witnesses, not registered-source reachability claims.

With otherwise disjoint dependencies, positive controls retain independence for unrelated selection keys and for token changes to unrelated owned buckets, including a different owner's bucket at the same place. Regional tests bind same-key cleanup through descendants and called roots, plus an unrelated-key control. The existing [local-control tests](../packages/semantic-core/test/internal-commutation-local-control.test.ts), [preparation tests](../packages/semantic-core/test/internal-transition-local-control-preparation.test.ts), [region tests](../packages/semantic-core/test/internal-commutation-region.test.ts), and [cross-language census guard](../scripts/internal-commutation-census.test.ts) remain acceptance constraints. Lean must establish corresponding typed conflict and readiness-frame facts before selected joins enter its batch theorem; complete runtime and accepted-publication preservation remain the broader family obligations above. The unchanged [Temporal preflight](#temporal-hosting-and-information-preservation-preflight) applies: this private dependency correction adds no ingress, persistence, replay, or host-admission mechanism.

### Selected-join dependency checkpoint

Implementation is independently approved at the dependency-helper boundary; the [review receipt](#independent-cold-review-receipt) records its correction audit. The [TypeScript dependency owner](../packages/semantic-core/src/internal-transition-selected-branch-preparation.ts) supplies the same-key population, every existing same-key record, and its actual input buckets to local-control preparation. Inclusive split and join write the population; Error, Terminate, and Call return enumerate populations removed by their predecessor-derived regions. This does not enable another production batch family.

The [selected-join tests](../packages/semantic-core/test/internal-commutation-selected-branch-census.test.ts) retain both admitted-state counterexamples above, the actual-input case outside the operation's declared join inputs, complete-preparation equality for different-owner token changes, and dependency-omission mutations. Regional cleanup fixtures exercise the helper boundary with selected-root, descendant, and called-root records, without claiming admitted-source reachability. Unrelated keys, exact Unicode ordering, and population atoms outside occurrence-region ownership remain precision controls.

[Lean selected-branch dependencies](../BpmnSemantics/SemanticProcess/InternalSelectedBranchDependencies.lean) introduce the typed record/population vocabulary, canonical read/write sets, membership/conflict laws, and derive untouched token buckets from disjoint predecessor dependencies. Those inequalities prove preservation of the actual Inclusive Gateway ready-record filter and its unique-ready result under a token patch. No theorem assumes successor readiness or final equality. Record-patch preparation frames, complete local-control preparation, runtime well-formedness, accepted-publication preservation, regional proofs, and production batch integration remain open.

### Numbering-free publication template

`InternalPublicationTemplate` is private and contains the exact unnumbered committed-transition record plus lifecycle starts and ends whose anchors are `wait`, `scope`, `callActivity`, `compensationTrigger`, `compensationHandler`, or `transitionTemplate`. The two Compensation arms preserve already decided private occurrence pairing but do not supply the missing commutation footprint. A `transitionTemplate` contains the instantaneous occurrence's canonical `(processId, elementId, owner)` key and no `commandId`, `transitionIndex`, or `localIndex`. Within one prepared transition, instantaneous occurrences are canonically sorted by `(processId, elementId, owner.processInstanceId, owner.definitionScopeId, owner.activation)` and equal-key multiplicity receives consecutive local indices only during instantiation. Starts and ends are sorted by anchor-kind order `wait`, `scope`, `callActivity`, `compensationTrigger`, `compensationHandler`, `transitionTemplate`, then by the complete typed anchor payload and terminal kind.

Complete publication templates are sorted by their exact `InternalAlternative`: `operation` before `mergeInput`, then canonical operation ID, then for a merge input its owner Process-instance ID, definition-scope ID, activation, and input-control-place ID. Only after this batch sort does the evaluator assign consecutive transition indices, inject the external command ID, and instantiate each transition template's consecutive local indices. Lean proves that this instantiation is the actual accepted lifecycle delta for the corresponding operation relation. A family whose lifecycle cannot be represented without preassigning a batch index is not prepared and therefore cannot enter an independent batch.

### Arbitrary complete-frontier batches

At each closure step the evaluator derives the complete prepared alternative frontier from one exact state. Zero candidates is stable. Before exposing a choice, it takes every singleton-operation candidate that is independent of every other member of the complete alternative frontier and executes that complete universally independent set as one batch; it then recomputes the frontier. It never selects a favorable independent subset or treats one alternative of a local-choice operation as independently batchable from its siblings. If every candidate belongs to a singleton operation and every pair is independent, the universally independent set is the complete frontier. If no universally independent member remains, the complete canonical alternative frontier is the next observable choice point.

Pairwise independence is sufficient only together with the preparation-frame laws above. Lean proves by adjacent swaps that every permutation of one finite pairwise-independent prepared batch reaches the same raw canonical RuntimeState and the same canonically sorted numbering-free publication. Production executes that complete batch in canonical `InternalAlternative` order, instantiates publication indices only after sorting, and then recomputes the frontier. The TypeScript both-order oracle becomes an arbitrary-permutation oracle outside production selection and must include a three-arm same-family batch and a mixed four-family batch.

The universal-normalization rule removes an operation that commutes with every remaining alternative before a directive is presented. It does not claim a unique schedule for every conflict graph: a connected chain in which `A` conflicts with `B`, `B` conflicts with `C`, and `A` commutes with `C` may still admit different valid directive sequences reaching the same result. The exact presented alternatives and consumed directive sequence remain semantic input, and an executable oracle retains this permitted non-uniqueness rather than silently canonicalizing it.

Closure fuel counts operations, not batches. If the complete batch has more members than remaining fuel, the evaluator reports the existing bound failure before applying any member. No partial batch state or publication is committable. The longest newly admitted batch and one over-limit case are required in both languages.

### Explicit observable choice

The pure semantic layer adds a required closed `InternalSchedulingMode` to the Program with `rejectObservableChoice` and `requireChoiceSchedule`. Every existing profile lowers to `rejectObservableChoice`; this capsule admits no new BPMN topology or profile. The scheduled mode exists so a later reviewed profile can select material nondeterminism without reinterpreting an already admitted Program.

The explicit input is an `InternalChoiceSchedule`, an ordered list of directives carried beside one external `Stimulus` at the pure evaluator boundary. Each directive contains its zero-based choice ordinal, the exact canonical nonempty list of alternatives presented at that point, and one selected member of that list. An alternative is the exact closed `InternalAlternative` union used by preparation and publication: `operation(operationId)` or `mergeInput(operationId, owner, inputControlPlace)`. Exact alternatives are carried rather than a digest, host index, random seed, or collection position.

Choice processing consumes the unified frontier above. An independent batch consumes no directive. A non-independent frontier under `requireChoiceSchedule` exposes its complete canonical `InternalAlternative` list to the next directive and fires only the exact selected prepared member before recomputing every unselected alternative from the successor. This covers a multi-offer `mergeExclusive`, another conflicting operation in the same state, and interactions between them without a second choice mechanism. Under `rejectObservableChoice`, the same frontier retains the existing `ambiguousInternalChoice` result and consumes no directive. Canonical order encodes the choice point and independent publication; it never makes the choice.

The existing `applyStimulus` remains the empty-schedule, result-only entry point. Its `CommandResult` and internal/traced companions carry both `internalStepBoundExceeded` and `ambiguousInternalChoice`. Under the approved [closure atomicity repair](capsules/INTERNAL-CLOSURE-ATOMICITY-REPAIR-SPEC.md#command-contract), either closure flag after committed admission returns `rolledBack` with the exact input RuntimeState before admission, preserves the actual flags, and erases selected steps, batches, transition records, and lifecycle deltas. The harness classifies either flag before observation. Successful stable and admission-rejected results remain exact. The distinct lower-layer `applyStimulusWithSchedule` returns a closed `ScheduledStimulusResult` containing `outcome`, `state`, both closure flags, and either no failure or one exact `InternalChoiceScheduleFailure`. The failure union is `scheduleForbiddenForMode`, `missingDirective`, `ordinalMismatch`, `alternativesMismatch`, `selectedAlternativeMissing`, or `unusedDirective`. A schedule failure returns `rolledBack`, the exact pre-command RuntimeState, `internalStepBoundExceeded=false`, `ambiguousInternalChoice=false`, and empty transition/lifecycle publication. Lean receives the same result and failure unions. The scheduled function and its traced companion remain private to the pure lower layer and are not exported through the engine API until a separately reviewed profile selects scheduled choice.

Precedence is exact. External stimulus admission runs first; `rejected`, `semanticFailure`, or `unsupported` returns its existing result without inspecting or consuming the schedule. After committed admission, `rejectObservableChoice` accepts only an empty schedule: a nonempty schedule rolls back as `scheduleForbiddenForMode`, while an encountered choice with an empty schedule rolls back the whole command with `ambiguousInternalChoice=true`. Under `requireChoiceSchedule`, absence of the next directive is `missingDirective`; a present directive is validated in the fixed order `ordinalMismatch`, then `alternativesMismatch`, then `selectedAlternativeMissing`, and a later predicate is inspected only after every earlier predicate does not match. The first resulting directive defect rolls back the whole command even if admission or earlier internal steps succeeded. Closure-fuel exhaustion is decided before the next batch or choice directive and returns `rolledBack` with the exact input before admission, `internalStepBoundExceeded=true`, the actual ambiguity flag, no schedule failure, and empty selected-step, batch, transition, and lifecycle material; remaining directives are not classified as unused because stable closure was not reached. Only a successful stable closure checks for trailing directives, which roll back as `unusedDirective`. Thus late directive failure cannot leak partial state or publication, an overlapping malformed directive has one cross-language discriminant, and a bound failure is never misreported as a schedule defect. The [exact Merge outcome](#exact-merge-frontier-outcome) implements this precedence in the private lower-layer evaluator; public and Temporal admission remain excluded.

The schedule is semantic input, not RuntimeState, observation, or Temporal metadata; it cannot be inferred from Event History, Workflow task order, SDK readiness, randomness, or program collection order.

### Final assurance and hosting boundary

The final Lean lane is **proved** for the arbitrary independent-batch theorem and for exact schedule consumption. Kernel-decided fixtures remain discriminators, not substitutes for the theorem. TypeScript independently implements the same classification and preparation contracts and retains permutation execution only as a test oracle. A cross-language matrix covers every operation-union classification, every RuntimeState field class, the three-arm and mixed four-arm batches, disjoint sibling regions, overlapping ancestor/descendant regions, same-operation merge alternatives, stale and unused directives, empty-schedule compatibility, and fuel refusal before partial application.

The Temporal adapter continues to host only the existing empty-schedule profiles in this capsule. Host admission accepts only Programs whose `internalSchedulingMode` is `rejectObservableChoice`, and an exhaustive lowering oracle proves every existing profile emits exactly that value. A Program selecting `requireChoiceSchedule` is refused before Workflow start rather than being defaulted or allowed to reach an ambiguous command. If a later profile exports scheduled choice, its command contract must content-bind the complete schedule before Workflow ingress, retain it across retry and recovery, and replay the same choice bytes. Workflow code may pass those bytes to the core but may not construct, repair, reorder, or default them. Independent internal batches remain one atomic evaluator result, and no half-batch or choice directive enters Query, Visibility, E1/E2 publication, or Product 2 projection.

## Temporal hosting and information-preservation preflight

The durable ingress remains the existing content-bound Start, User Task Update, Message delivery, Timer firing, or effect-result command owned by each already admitted profile. This checkpoint changes only pure internal closure after one accepted ingress. It adds no Workflow command, Signal, Timer, Activity, Child Workflow, cancellation scope, retry policy, Task Queue rule, Workflow-chain field, or external-effect lifecycle.

| Concern | Unchanged hosting mechanism and required checkpoint fact |
|---|---|
| Ingress and acknowledgement | The existing content-bound command enters one Workflow handler and is acknowledged only through its existing committed or non-committed result after the whole evaluator call. An internal publication pair has no separate ingress or acknowledgement. |
| Semantic waits, timers, effects, and cancellations | The core continues to own every wait and cancellation fact. Temporal schedules a durable wakeup or I/O only from the committed stable result; no host primitive selects either internal order. |
| Workflow/core state relation | Workflow state contains the exact canonical RuntimeState and committed publication produced by the core. Handler entry, Workflow tasks, Activity attempts, and persistence steps are hidden stuttering steps and cannot expose a half-batch. |
| Serialization and interleaving | The existing Workflow loop serializes accepted Updates, Signals, Timer firings, and Activity results through one evaluator call. Handler readiness or Promise order cannot choose between the two internal operations. |
| Delivery, deduplication, and retry | Existing content-bound command identity, occurrence identity, stale refusal, and retry rules remain unchanged. Duplicate host delivery stutters or returns the existing refusal/result and never replays one member of the pair independently. |
| Completion, failure, cancellation, Continue-As-New, and post-completion commands | Existing terminal receipts, failure mapping, scope cancellation, Workflow-chain rollover, and post-completion refusal apply only to the atomic evaluator result. The checkpoint adds no Continue-As-New field or branch and cannot leave one operation committed when the other fails. |
| Query, Visibility, and external read models | The existing Query exposes only committed publication and canonical observation. Temporal Visibility remains operational metadata, and Product 2 read models consume only the published engine contract. Neither may reconstruct raw evaluator or Program order. |
| Replay and versioning | This repository's [pre-release evolution policy](PROJECT-DESIGN.md#pre-release-evolution-policy) and [contract evolution policy](../contracts/README.md#evolution-policy) select one atomic required-field replacement with no compatibility reader: histories created before `internalSchedulingMode` are unsupported. Every new exact Program and continuation payload is decoded strictly. Histories created with the new bytes must replay byte-identically under Worker replacement, recovery, and Continue-As-New. |

The preserved state relation is exact equality of the canonical semantic state and committed publication after the commuting batch. Program-storage permutation remains a pure-core diagnostic because production admission requires canonical operation-ID order. The smallest host witness uses the exact admitted canonical Program for the existing production parallel User Task profile, retains the core-produced canonical publication through Worker replacement between task completions, recovers one accepted result, crosses a permitted pre-arming Continue-As-New boundary, and replays every Run. It re-runs the exact Program-byte ceiling and the six-argument aggregate continuation ceiling from the [Workflow lifetime contract](TEMPORAL-PROCESS-LIFECYCLE-SPEC.md#workflow-lifetime-contract) and [Workflow-chain production contract](TEMPORAL-PROCESS-LIFECYCLE-SPEC.md#workflow-chain-production-contract), at exact fit and one byte over, because the new required Program field consumes both budgets. Its nearest realistic adapter counterexample reverses or reconstructs the core-produced numbering-free pair before numbering or persistence; the exact transition/lifecycle record and replay oracle must reject that mutation without starting an unadmitted Program.

Host admission must continue to reject an unsupported mixed-wait profile even when its pure internal arming pair has a footprint, because commutation evidence is not source/profile or host-capability admission. A future live mixed-wait witness requires its own profile and host preflight.

## Implemented checkpoint boundary

The Message key-correlation semantic checkpoint extends the implemented ordinary-wait preparation contract with the correlated arm, its Process-property read, complete candidate-publication atom, exhaustive TypeScript and Lean census classification, exact-pair preservation, and commutation witnesses. This extension is implemented and independently approved at correction target `1ce28ed5`, but it does not approve Temporal discovery, ingress, profile registration, or final Internal Commutation closure.

The implemented checkpoint required:

- one shared semantic criterion implemented independently in Lean and TypeScript;
- positive ordinary User Task/User Task and mixed User Task/Timer or Message footprint witnesses;
- one called-owner Message witness proving that issuance, footprint, wait, and lifecycle anchor all carry `owner.processInstanceId` while runtime control remains top-level;
- a realistic same-effect-element counter collision that distinct operation, input, and output IDs do not hide;
- same-family Program-admission refusal, including an out-of-checkpoint composite declarer, and cross-family untagged lifecycle-anchor refusal on structurally well-formed programs;
- one candidate refusal against a pre-existing incident carrying the same untagged public wait anchor;
- both-order state and publication evidence, pure-core canonical program-order permutation evidence, and unchanged ambiguity/bound precedence;
- existing parallel Product 1 Worker-replacement and replay evidence remaining green;
- routed implementation maps, proposal/checkpoint review, focused gates, complete applicable gate, and a reproducible checkpoint cost row.

The checkpoint permitted as optional:

- additional positive pairs among the four supported arming kinds when they use no new atom or proof case.

The checkpoint excluded:

- more than two enabled operations;
- `duplicate`, joins, choices, scope entry/completion, call/return, termination, Error propagation, event-race arming, boundary-host operations, or Multi-Instance operations;
- new BPMN source admission, semantic profile, scenario registration, Product 2 surface, CIB relationship, or Temporal host capability;
- public exposure of footprints, scheduler choices, raw visit order, or Temporal identity;
- Parallel Multi-Instance, completion conditions, general event subscriptions, compensation, transactions, workload isolation, or closure of another MUE content ID.

## Final-closure required, optional, and excluded work

This list retains the broader proposal-completion contract. The [RC completion scope amendment](#rc-completion-scope-amendment) separately bounds milestone obligations without declaring unimplemented parts of this list complete.

Required:

- one executable classification for every current Semantic Operation variant in Lean and TypeScript, plus a guard that rejects an unclassified future variant;
- one field-complete footprint census over both RuntimeState definitions, including owner-aware region conflicts and a guard that rejects an unclassified future state field;
- canonical Lean insertion for every collection reached by a newly prepared family, including token buckets, scope records, scope/Call/event-race activation rows, and every remaining affected wait, controller, record, counter, or variable-scope collection;
- the exhaustive pre-frontier result and unified exact-alternative frontier, including multi-offer merge paired with another conflicting operation;
- prepared transitions for every batchable family in the final census, with exact relation soundness, complete-value independent-frame preservation, pairwise commutation, and actual numbering-free-publication laws;
- one private numbering-free publication-template union, complete cross-family sort key, late transition/local-index instantiation, and accepted-lifecycle proof;
- one arbitrary finite-batch Lean theorem from pairwise independence and the preparation-frame laws, without assuming a canonical execution order or final equality;
- one independently structured TypeScript complete-frontier classifier and arbitrary-permutation oracle that production code does not call;
- the closed scheduling mode, exact typed choice point and directive representation, closed scheduled-result and failure unions, outcome precedence, transactional schedule consumption, empty-schedule compatibility, and fail-closed handling of every missing, stale, invalid, or unused directive;
- a pure evaluator proof and executable oracle that a valid directive selects exactly one offered alternative while an independent batch consumes none;
- exact closure-fuel accounting that refuses an oversized batch before any member is applied;
- host refusal of scheduled mode, exhaustive existing-profile lowering, exact Program and aggregate continuation-byte bounds, the selected pre-field-history non-support rule, and Worker replacement/recovery/Continue-As-New/replay evidence for new Program bytes;
- the updated Semantic Process contract and proof obligations, affected source maps, runtime/proof and assurance maps, Temporal serialization/preflight boundary, PLAN, focused gates, complete applicable gate, governed semantic checkpoint and closure reviews, and a reproducible final-closure cost row.

Optional:

- a subquadratic conflict index after the direct pairwise implementation is proved correct and a realistic frontier demonstrates the need;
- additional permutation fixtures beyond the required same-family three-arm, mixed four-family, and region cases.

Excluded:

- any new BPMN construct, source shape, admitted topology, semantic profile, CIB relationship, Product 2 choice UI, or public engine schedule command;
- implementing compensation, Transactions, generalized subscriptions, data semantics, or workload isolation under this content ID;
- treating an unsupported footprint, canonical operation order, host readiness, randomness, Event History, or a successor-state comparison as semantic choice;
- exposing footprints, prepared patches, raw closure visit order, or private region membership through public observation;
- claiming Church-Rosser, unbounded termination, or confluence beyond one finite complete frontier and the configured closure limit.

## Checkpoint evidence and adversarial oracles

The first Red is a synthetic internally valid state with one ordinary User Task arm and one ordinary Timer or Message arm on distinct owner-token places: current closure reports ambiguity even though both explicit execution orders reach the same canonical state. The second Red is the transition-grounded write/write class separator: two effect arms have distinct operation, input, and output IDs but share one effect element and therefore the same activation-counter, wait-occurrence, and Activity-scope atoms. A shallow identifier checklist can accept it; the footprint rule must reject it before either successor is selected.

Four account-correction Reds separate Program admission from runtime publication safety. One Program mutation gives two distinct ordinary User Task operations the same task identity, and a second makes an ordinary User Task conflict with an out-of-checkpoint composite User Task declarer. Both now fail standalone Program admission before closure classification, proving that the exhaustive census is not limited to footprint-eligible constructors. A third structurally well-formed mixed User Task/Timer program gives both arms the same `(processInstanceId, elementId, activation)`. Their private tagged waits and counters differ and both explicit state steps succeed, but the actual untagged public wait anchor collides and `projectFlowNodeOccurrenceLifecycleDelta` returns `none` for the second start. A fourth independently valid pre-state contains an effect incident whose untagged anchor equals an ordinary candidate's next occurrence. Every affected footprint must be unavailable before successor selection.

A separate called-Process Red arms an ordinary Message wait under a non-root owner. It proves that the actual wait ID, footprint occurrence, lifecycle anchor, and owner all use the called `owner.processInstanceId`; substituting the hosting root `state.control.instanceId` must fail before publication.

A separate predicate-level algebra oracle supplies a left footprint whose `writes` contain `activation(userTask,shared)` and a right footprint whose `reads` contain that atom while every write/write comparison and every publication key remains disjoint. The predicate must reject the pair. This is intentionally abstract evidence for the write/read equation: none of the four current arming constructors supplies a transition-grounded pure write/read conflict, and the checkpoint does not invent one.

Further mutations drop one activation write, tag two counter domains as equal, tag the actual public wait anchor by family, use the hosting root for a called-owner Message occurrence, treat a write/read intersection as harmless, omit a composite declarer, omit incidents from the open-anchor census, accept a second declarer, derive footprints from successor diffs, accept an unsupported operation, compare only final state, accept equal lifecycle projection failures as publication equality, reconstruct or reverse the core publication in the adapter, use locale collation, or classify only the selected first two members of a larger enabled set. Each mutation must fail an oracle outside the changed decision branch.

The focused TypeScript gate owns the closure classifier and both-order oracle. The exact Lean targets are `BpmnSemantics.SemanticProcess.InternalCommutation`, `BpmnSemantics.SemanticProcess.InternalCommutationPublication`, and their importing `BpmnSemantics.InternalCommutationConformance`, with the first kernel-decided fixture build staying with the root under the memory bound. The root runs the complete affected package gate, those exact Lean targets, `test:infrastructure`, every path-selected clean-commit pre-push entry point, and `git diff --check` at the governed checkpoint.

## Final-closure evidence and adversarial oracles

The Activity-data preparation prerequisite retains [exact open-set projection witnesses](../BpmnSemantics/ActivityDataInputOutputProjectionConformance.lean) for admitted input-only, output-only, and composed states. The Lean User Task matcher classifies the closed operation union exhaustively, and Effect-local exactness applies only to the Effect owner arm of `LocalDataOwner`, preserving the existing TypeScript account. Wrong output, task name, live owner, either metadata field, and an orphan Effect scope remain rejected. These are projection regressions; they establish no Activity-data preparation, frame, or commutation law.

The subsequent [User Task validity owner extraction](../BpmnSemantics/SemanticProcess/FlowNodeOccurrenceUserTaskProgramValidity.lean) is non-material: its private matcher and every pre-existing theorem signature remain byte-identical. The existing Lean source-contract, operation-consumer census, source-hygiene, documentation, and complete Lean gates govern this responsibility split; it opens no semantic review cycle.

The [control-position contract extraction](../BpmnSemantics/SemanticProcess/ControlPositionContract.lean) moves the four existing public value declarations unchanged below trace evaluation, allowing predecessor preparation to retain the same publication types without a dependency cycle. Names, fields, and all projection algorithms remain unchanged. The source-contract, import-boundary, source-hygiene, documentation, and complete Lean gates govern this non-material extraction; it opens no semantic review cycle.

The [wire-natural extraction](../BpmnSemantics/WireNatural.lean) keeps the existing integer bound and predicate unchanged under their existing names, while allowing preparation arithmetic to share the decoder's bound without importing JSON decoding. Source-contract, import-boundary, source-hygiene, documentation, and complete Lean gates govern this non-material extraction; it changes no integer or wire contract and opens no semantic review cycle.

The composed input/output User Task arming prerequisite and its footprint and accepted-publication contract are independently checkpoint-approved. The finite prepared arming checkpoint below integrates that contract into production closure and has its own independent approval recorded in [the review receipt](#independent-cold-review-receipt).

- [Predecessor-only preparation and evaluator refinement](../BpmnSemantics/SemanticProcess/InternalDataArmingPreparation.lean), [complete sibling-preparation preservation](../BpmnSemantics/SemanticProcess/InternalDataArmingPreparationPreservation.lean), and [raw patch commutation](../BpmnSemantics/SemanticProcess/InternalDataArmingCommutation.lean) retain the original preparation checkpoint. [Runtime and exact open-projection preservation](../BpmnSemantics/SemanticProcess/InternalDataArmingFrames.lean) preserve unrelated live Multi-Instance controllers through exact declaration disjointness; [accepted lifecycle publication](../BpmnSemantics/SemanticProcess/InternalDataArmingPublication.lean) retains arbitrary transition indices.
- The [data footprint](../BpmnSemantics/SemanticProcess/InternalDataArmingFootprint.lean) names Process-global binding reads, separate Activity and task counters, complete Activity records, tagged Activity-local scopes, body-task claims, and logical-time reads. Shared reads commute; writes intersecting a read or write conflict. Effect and Activity owners remain distinct even when all numeric and string identity coordinates match. [Footprint-derived commutation](../BpmnSemantics/SemanticProcess/InternalDataArmingFootprintCommutation.lean) derives the input/task inequalities and counter ordering needed by the raw laws from noninterference and the admitted predecessor invariant.
- [Mixed preparation frames](../BpmnSemantics/SemanticProcess/InternalDataArmingMixedFrames.lean) and [mixed raw-state commutation](../BpmnSemantics/SemanticProcess/InternalDataArmingMixedCommutation.lean) cover a data arm paired with each existing ordinary arming family. The [execution bridge](../BpmnSemantics/SemanticProcess/InternalDataArmingExecution.lean) connects data patches to actual `fire` under the existing no-snapshot-declaration boundary. The [accepted-publication bridge](../BpmnSemantics/SemanticProcess/InternalDataArmingAcceptedPublication.lean) derives complete record/lifecycle, logical-time, and position bundles.
- The [pair publication laws](../BpmnSemantics/SemanticProcess/InternalDataArmingPairPublication.lean) quantify over an admitted Program, exact instance-bound well-formed predecessor, projectable open set, absent snapshot declaration, successful predecessor preparations, and noninterfering footprints. They derive both intermediate preparations and invariants, actual execution in both orders, final runtime/open-set validity, and one equal canonical accepted E1/E2 publication at any first transition index. They assume no successor preparation, intermediate preservation, final equality, publication equality, or operation-ID inequality. They enable neither the production batch selector nor arbitrary finite batches.
- The independent [TypeScript preparation tests](../packages/semantic-core/test/internal-transition-data-arming-preparation.test.ts) cover unequal counter domains, shared Process reads, Process write conflicts, both raw-state orders, actual accepted publication at arbitrary indices, malformed preparation, and refusal by the legacy ordinary-footprint facade. Six mixed-family witnesses start from admitted and well-formed states, preserve complete opposite preparations, and compare actual accepted publications; equal-coordinate Effect/Activity owners separate the owner tags while a colliding Effect counter rejects noninterference.

The consumer proof exposed a missing canonical Activity-counter invariant. Both runtime validators now reject reversed and duplicate Activity-counter keys. All four Lean writers that formerly prepended a replacement use the existing ordered insertion helper, preserving high-water marks and separate task/Activity issuance; the [kernel writer witness](../BpmnSemantics/ActivityActivationOrderConformance.lean) covers each actual writer, and the [runtime invariant witness](../BpmnSemantics/RuntimeStateWellFormedInvariantConformance.lean) locks the aggregate predicate. Existing preservation proofs carry the strengthened conjunct. This changes no source admission or public observation; it strengthens the internal state contract required for exact raw-state commutation.

Cold-target measurements at `ee17ed58` used warm dependencies, one CPU/thread, no network or swap, and the fixed 3 GiB ceiling. The projection module took 9.54 seconds at 1,288,416 KiB GNU RSS and 946,196,480 cgroup bytes; its parent took 2.22 seconds at 974,464 KiB and 587,448,320 bytes. Both exited zero with every controlled memory event and pressure total zero. The exact output SHA-256 values are `26b1b73c0861aa9b84ddb5385ce0d624dd05c6e83954d71e08605558cb2fe92a` and `67794c1d456f155723760f2eee949d7e225537198dc0aef0f97f97926f791a62`, respectively; [the module-cost record](../scripts/lean-module-cost.ts) binds both source revisions.

The first final-closure Red was a three-arm ordinary User Task frontier whose complete pairwise-independent footprints reach one equal canonical state under all six permutations. Production before the first final-implementation checkpoint reported ambiguity solely because the frontier was larger than two. The second is a four-arm User Task, Message, Timer, and effect frontier. Removing any one family preparation, comparing only the first two candidates, or letting batch order affect publication must fail independently.

### Finite prepared arming checkpoint

This checkpoint integrated complete preparations for ordinary User Task, Message (including payload and correlated payload), Timer, Effect, and composed input/output User Task arming; the later [standalone outcome](#standalone-activity-data-arming-outcome) extends the same account to input-only and output-only arming. Every enabled operation must prepare from the same predecessor and every pair must be independent; unavailable or conflicting members refuse the whole frontier. Before each local patch, execution re-derives and compares the complete preparation, including copied data, separate occurrence counters, footprint, and publication. Operation-ID equality alone cannot authorize it. The existing singleton path is unchanged. Snapshot-declaring Programs remain outside the Lean prepared-batch and quantified proof domain and outside TypeScript data-arming batches. TypeScript preserves its pre-existing ordinary-only batches for snapshot-declaring Programs; this checkpoint adds no Lean correspondence claim for that path.

- [InternalPreparedArming](../BpmnSemantics/SemanticProcess/InternalPreparedArming.lean) owns the closed prepared union and checked patch executor. [InternalArmingBatch](../BpmnSemantics/SemanticProcess/InternalArmingBatch.lean) lifts predecessor-based family laws through every independent prefix and proves raw-state equality by list permutation.
- [InternalArmingBatchPublication](../BpmnSemantics/SemanticProcess/InternalArmingBatchPublication.lean) derives defined accepted publications and proves canonical numbered equality at any starting index. Its erasure and permutation theorems bind the retained [arming batch fold](../BpmnSemantics/SemanticProcess/TransitionTrace.lean) and its paired sort; current production uses the unified mixed-batch function described below. Intermediate validity, opposite preparation, final equality, and publication equality are conclusions rather than premises.
- The independent [TypeScript batch owner](../packages/semantic-core/src/internal-transition-arming-batch.ts) applies retained local patches. [Its executable matrix](../packages/semantic-core/test/internal-transition-arming-batch.test.ts) covers all permutations of three-member frontiers with two composed tasks, four distinct families including correlated Message, shared Process reads, separate task/Activity counters, stale same-ID preparations, conflicts and unsupported trailing members, exact publication at a nonzero index, and late executor refusal. It also compares existing ordinary-only snapshot-declaring batches against the actual operation evaluator in both orders for all six ordinary arming families, while composed preparation and mixed batches refuse snapshot declarations. The [Lean witness](../BpmnSemantics/FiniteInternalArmingConformance.lean) checks an admitted mixed claim frontier, instantiates the universal permutation theorem, and kernel-decides stale preparation refusal, committed paired publication, and complete rollback when fuel is one operation short. It is an internal Program fixture, not checked-source or corpus evidence.

Red was reproduced with the mixed claim Start returning `rolledBack` and `ambiguousInternalChoice=true` despite the complete prepared frontier; production integration makes it commit. The first complete repository gate exposed an overbroad TypeScript snapshot exclusion: it blocked pre-existing ordinary arming and rolled back all three [exact-source Compensation runtime scenarios](../packages/bpmn-source/test/compensation-source-runtime.test.ts). Six family-wide regressions reproduced the mechanism before narrowing the exclusion to composed data arming. The complete TypeScript semantic-core gate passes 776 tests and the complete source gate passes 814 tests on 2026-09-08. The first Lean publication/fuel fixture build passed under the unchanged 3 GiB controller with zero memory events, pressure totals, and swap. [Immutable cost calibration](CAPSULE-COST-LEDGER.md#finite-prepared-arming-checkpoint-calibration) passes for the new fixture and all four standing-watch consumers. The independent correction audit is approved, and the clean complete pre-push gate passes at `ec93ba11322879808ab2512ce48e6340e4ecc841` with agreement across all 72 pipeline cases; output SHA-256 is `ee49893b830c88b2eafc91753f0229597cd8de4cf57c19f1fb95df44174dde7f`. Region-family execution, universal normalization around conflicts, explicit scheduled choice, and final Internal Commutation closure remain open.

### Standalone Activity-data arming outcome

The [complete preparation owner](../BpmnSemantics/SemanticProcess/InternalDataArmingPreparation.lean) retains the actual input-only, output-only, or composed operation and its existing declarative activation relation. Input-bearing variants select exactly one String/Null Process binding and copy it under the DataInput ID; missing, duplicate, or unsupported values refuse preparation. Output-only arming reads no Process binding and installs an empty Activity-local scope. Every variant consumes its owner-specific input token, advances separate task and Activity counters, inserts the joined wait and Activity record without handlers, and owns the fresh local scope even when its binding list is empty. The input-bearing footprint reads the source Property and writes the copied Activity binding; output-only adds neither of those atoms. None writes a Process Property while arming.

The [shared insertion preservation theorem](../BpmnSemantics/SemanticProcess/ActivityDataInputOutputActivationRuntimeStatePreservation.lean) proves runtime validity for the common task/Activity insertion from predecessor facts and the existing unbounded User Task declaration. Its composed activation theorem retains its statement. The data preparation frames derive complete opposite preparation, successor runtime/open-set validity, exact canonical state commutation, and accepted publication through the existing finite and mixed-family owners. No intermediate invariant or final equality is assumed; no new atom domain or RuntimeState field is introduced.

The independently written [TypeScript patch](../packages/semantic-core/src/internal-transition-data-arming-patch.ts) retains zero or one copied bindings and the actual operation variant. [Preparation tests](../packages/semantic-core/test/internal-transition-data-arming-preparation.test.ts) distinguish null from missing input, separate task/Activity counters, shared Process reads, and empty-scope output arming. [Batch tests](../packages/semantic-core/test/internal-transition-arming-batch.test.ts) cover mixed permutations, revalidation, canonical publication, snapshot refusal, and complete rollback; the [input integration regression](../packages/semantic-core/test/internal-commutation-activity-data-input.test.ts) changes the independent frontier from rollback to commit while preserving the legacy facade's refusal. The expanded [Lean fixture](../BpmnSemantics/FiniteInternalArmingConformance.lean) binds both standalone variants to the universal permutation theorem and checks complete publication, missing-input/output-only separation, and one-operation-short fuel rollback.

This outcome changes private preparation and closure realization, not checked-source shapes, registered profiles, public wire contracts, singleton activation, completion, or Temporal admission. Constructed Program witnesses establish no new public reachability. Snapshot-declaring Programs remain excluded from all three data-arming variants and from the quantified Lean batch domain; the prior TypeScript ordinary-only snapshot behavior is retained. Other composite families and final Internal Commutation closure remain open.

### Bounded Sub-Process arming outcome

This outcome completes `enterBoundedScope` preparation as one atomic child entry, parent-owned deadline, and joined Activity record. Its three issuance domains remain separate. Preparation must retain both differently owned token positions, the child/parent relation, complete deadline and Activity payloads, and the existing scope-start publication. The footprint protects both token-owner populations, scope identity and parent facts, the three counters, logical time, the untagged Timer anchor, and the complete Activity association. The [existing hosting preflight](capsules/SUBPROCESS-BOUNDARY-TIMER-SPEC.md#temporal-hosting-and-refinement-preflight) retains its boundary; no firing, completion, source/profile, snapshot, or Temporal admission is widened.

Completion requires both complete preparation directions, exact canonical state equality, successor runtime/open-set validity, and actual accepted publication for every independent prepared family, integrated into the existing finite-batch theorem. The lifecycle starts the Sub-Process scope; its attached deadline does not independently start a Boundary Event. The [retained core regressions](../packages/semantic-core/test/internal-transition-bounded-scope-batch.test.ts) separate valid commuting task and Timer frontiers from the previous rollback, check unequal counters and exact publication, and retain whole-command fuel rollback and snapshot refusal. Constructed Programs establish no registered-profile reachability.

The working [selection](../BpmnSemantics/SemanticProcess/InternalBoundedScopeSelection.lean) reuses ordinary child selection internally and proves refinement to the actual bounded-entry evaluator; the decomposition admits no synthetic Program operation. [Complete preparation](../BpmnSemantics/SemanticProcess/InternalBoundedScopePreparation.lean) reuses the scope-position template and regional atom vocabulary, with fresh Activity issuance and body-claim preservation. [Read frames](../BpmnSemantics/SemanticProcess/InternalBoundedScopePreparationFrames.lean) retain the entire prepared value and derive ordinary child-footprint separation from the joint regional footprint. The [ordinary-wait pair](../BpmnSemantics/SemanticProcess/InternalBoundedScopeOrdinaryFrames.lean) and [local-control pair](../BpmnSemantics/SemanticProcess/InternalBoundedScopeLocalControl.lean) derive both complete preparation directions and literal canonical state equality. [Validity](../BpmnSemantics/SemanticProcess/InternalBoundedScopeRuntimeValidity.lean) reuses the existing scope-creation proof to derive the child insertion's aggregate invariant and the complete operation's runtime-position validity; intermediate validity is a conclusion, never an added premise. It also proves wait-identity uniqueness, live Activity work, and unambiguous Timer attachment. The latter reuses one [joint insertion law](../BpmnSemantics/SemanticProcess/InternalCommutationActivityOwnership.lean) with the existing Timer-task consumer. The existing ordinary scope-creation and Timer-task theorem statements remain unchanged. The Lean hosting-Process restriction remains unchanged. The owner-approved identity correction of 2026-09-22 keys the Activity and its counter by the Sub-Process BPMN element, matching TypeScript; the definition-scope ID still identifies the child scope.

The identity-bound, wait-declaration, live-owner, Activity-identity, and canonical-order preservation laws also compile. Complete preparation checks that the selected operation belongs to the parent scope, matching the existing TypeScript check. After the two approved corrections below, `prepared_bounded_scope_preserves_runtime` composes the child insertion and joined Timer/Activity laws into full `runtimeStateWellFormed` preservation from predecessor validity and successful complete preparation. It derives Multi-Instance separation from the declaration check and assumes neither intermediate nor successor validity. Receipt `/tmp/bpmn-bounded-aggregate-1` records its successful narrow build; open-set validity and publication do not follow from this runtime invariant alone.

Reassessment, 2026-09-22: the preserved definition-scope Activity key prevents the proposed aggregate theorem. Two constructed structurally valid Programs place a Sequential Multi-Instance task inside the child or after the Sub-Process and give the Sub-Process definition ID the same text as that task ID. Both predecessors satisfy `runtimeStateWellFormed`, complete bounded preparation succeeds, and both successors fail `sequentialMultiInstanceProgramBindingsValid`: the new child-body Activity is counted as a task Activity without its controller. The root is [deadline arming](../BpmnSemantics/SemanticProcess/BoundedScopeArming.lean), which uses the definition ID in the shared Activity-element namespace; a scope-to-node bijection does not establish disjointness from other node identifiers. The diagnostic command `./scripts/lake.sh env lean .cache/bounded-scope-activity-key-probe.lean` reproduces both cases; receipt `/tmp/bpmn-bounded-scope-mi-probe-10` records `(Program valid, predecessor valid, successor valid) = (true, true, false)` for each. This is evaluator evidence on constructed Programs, not a kernel theorem or registered-profile reachability claim. The proposed correction is to thread the actual Sub-Process element identity through Lean arming, its relation, and consumers, matching TypeScript's existing key; retain both cases and check declaration ambiguity separately. The owner approved that bounded identity correction on 2026-09-22, superseding the earlier requirement to preserve the definition-scope Activity key. Do not add an assumed successor invariant or silently exclude the counterexamples to finish the proof.

Correction feasibility check: receipt `/tmp/bpmn-bounded-scope-key-correction-probe-1` confirms that replacing only the diagnostic selection's Activity key and counter with the Sub-Process element ID makes both original successors valid. A third structurally valid raw Program gives the Sub-Process and the Multi-Instance operation the same element ID; its predecessor is valid, but the corrected diagnostic successor remains invalid. The key correction alone therefore does not establish the universal claim. The [TypeScript binding](../packages/semantic-core/src/sequential-multi-instance-binding.ts) selects Activity records by retained `operationId`, whereas Lean's reverse binding selects by Activity element ID. Receipt `/tmp/bpmn-bounded-scope-key-core-probe-7`, from `node .cache/bounded-scope-activity-key-probe.ts`, confirms valid TypeScript predecessors and singleton/prepared successors in all three constructed cases. The correction must also settle ambiguous Activity declarations before claiming aggregate preservation; neither a general correspondence theorem nor public reachability follows from these probes. These counterfactual diagnostics preceded the owner-approved identity correction and the subsequent declaration-ambiguity check below.

The approved identity correction is implemented in arming, its declarative relation, snapshot dispatch, and complete-preparation consumers. The two [retained kernel regressions](../BpmnSemantics/BoundedScopeActivityIdentityConformance.lean) now establish valid Programs and predecessors, actual/prepared successor equality, valid successors, and separate scope/Activity counters. The corresponding core regressions and complete 1,261-test package gate pass in `/tmp/bpmn-bounded-identity-semantic-core-1`; the native consumer build passes in `/tmp/bpmn-bounded-identity-consumers-2`. The [resource record](CAPSULE-COST-LEDGER.md#bounded-sub-process-activity-identity-correction-2026-09-22) owns the isolated fixture measurements. Running the corrected evaluator itself in `/tmp/bpmn-bounded-identity-ambiguity-1` confirms both repaired cases and the still-invalid duplicate-element case.

The owner separately approved the declaration-ambiguity restriction on 2026-09-22. Both targets' bounded-scope batch preparations now require that no Program operation declares a User Task wait under the Sub-Process BPMN element ID, using their existing declaration census across all User Task variants. A conflicting declaration refuses preparation even if that task is not currently enabled; an ordinary Timer sharing the element text does not trigger this User Task exclusion. This is an immutable Program check and adds no state-footprint atom. It leaves singleton execution, general Program validation, RuntimeState representation, and registered-profile admission unchanged. Checked BPMN graphs already distinguish node IDs, but these raw Program regressions establish no public admission result. Broadening this batch domain requires an Activity identity account that distinguishes the colliding declarations and proves the same preservation and publication obligations. The ordinary and Sequential Multi-Instance regressions first reproduced acceptance, then passed with refusal in `/tmp/bpmn-bounded-ambiguity-green-1`; checkpoint acceptance remains open.

The [open projection](../BpmnSemantics/SemanticProcess/InternalBoundedScopeProjection.lean) now derives complete occurrence-program validity and exact successor projection from the predecessor: one Sub-Process start is added in canonical order, every existing public wait is retained, and the new bound deadline stays private. Its structural preservation reuses the existing scope-binding proof with the actual bounded child classification; it does not admit a synthetic ordinary-entry operation. The [publication proof](../BpmnSemantics/SemanticProcess/InternalBoundedScopePublication.lean) derives the actual transition record, accepted lifecycle, and token/scope delta, then proves equality with the retained publication template. Ordinary and bounded entry share the existing position-difference calculation; the ordinary theorem's public statement is unchanged. Narrow builds `/tmp/bpmn-bounded-open-2` and `/tmp/bpmn-bounded-publication-3` pass; `/tmp/bpmn-bounded-publication-axioms-1` reports only `propext`, `Classical.choice`, and `Quot.sound` for the complete open-projection and accepted-publication laws and the retained ordinary position theorem.

The [Merge and ordinary End pairs](../BpmnSemantics/SemanticProcess/InternalBoundedScopeTokenPairs.lean) also derive both complete preparation directions and literal canonical state equality, reusing local-control and scope-entry token laws. Their narrow build passes in `/tmp/bpmn-bounded-token-pairs-4`. The complete infrastructure gate passed all 582 assertions in `/tmp/bpmn-bounded-publication-infrastructure-1` before these final token-pair additions; it is not a full-outcome integration verdict.

The [ordinary scope/Call pair](../BpmnSemantics/SemanticProcess/InternalBoundedScopeCreationPair.lean) and [bounded/bounded pair](../BpmnSemantics/SemanticProcess/InternalBoundedScopePair.lean) also preserve complete preparation in both directions and commute in literal canonical RuntimeState. They reuse the existing scope-selection read proof and child-insertion equality; the ordinary scope-pair theorem statements remain unchanged. Bounded/bounded separation additionally protects both Timer and Activity counters, the untagged deadline anchors, and complete Activity associations. Narrow builds `/tmp/bpmn-bounded-creation-3` and `/tmp/bpmn-bounded-self-3` pass. These results assume neither intermediate validity nor final equality and introduce no synthetic Program operation. The affected existing scope/arming consumers and bounded publication pass in `/tmp/bpmn-bounded-creation-consumers-1`; both new pairs and the retained ordinary complete-pair theorem report only the three standard axioms in `/tmp/bpmn-bounded-creation-axioms-1`. All 582 infrastructure assertions pass in `/tmp/bpmn-bounded-creation-infrastructure-1`.

The [Activity-data pair](../BpmnSemantics/SemanticProcess/InternalBoundedScopeDataPair.lean) and [Timer-task pair](../BpmnSemantics/SemanticProcess/InternalBoundedScopeTimerTaskPair.lean) preserve both complete preparations and commute in literal canonical RuntimeState. They protect distinct Activity issuers and wait anchors; the Timer-task pair additionally protects both Timer issuers and joined associations. Narrow builds `/tmp/bpmn-bounded-data-5` and `/tmp/bpmn-bounded-timer-3` pass. The [regional retirement frame](../BpmnSemantics/SemanticProcess/InternalBoundedScopeRegionalFrame.lean) preserves complete bounded preparation after an independent regional operation. The [regional selector and graph laws](../BpmnSemantics/SemanticProcess/InternalBoundedScopeRegionalSelection.lean) preserve the raw selection, exact occurrence region, and existing bounded-withdrawal census after bounded entry; `/tmp/bpmn-bounded-regional-frame-3` and `/tmp/bpmn-bounded-regional-selection-3` pass. Shared scope-read and graph proofs retain their ordinary-entry theorem statements; bounded consumers derive child-position validity from their own preparation and predecessor invariant, without a synthetic Program declaration.

The affected scope/regional consumers, bounded publication, and new pairs pass together in `/tmp/bpmn-bounded-pair-consumers-2`. The six consumer laws audited in `/tmp/bpmn-bounded-pairs-axioms-1`, including the unchanged ordinary regional/scope pair, use only `propext`, `Classical.choice`, and `Quot.sound`. All 582 infrastructure assertions pass in `/tmp/bpmn-bounded-pairs-infrastructure-1`; this is not the complete Product 1 integration gate.

The [regional retention laws](../BpmnSemantics/SemanticProcess/InternalBoundedScopeRegionalRetention.lean) preserve the full cancellation and local-data masks and retain the inserted child, Timer, and Activity. The [ownership selection](../BpmnSemantics/SemanticProcess/InternalBoundedScopeRegionalOwnership.lean) preserves reference closure, Activity-owner closure, and exact local-data ownership, reusing the existing Timer-task insertion argument. Their narrow builds pass in `/tmp/bpmn-bounded-regional-retention-6` and `/tmp/bpmn-bounded-regional-ownership-4`. The ordinary scope and Timer-task public theorem statements remain unchanged.

Owner-directed review checkpoint, 2026-09-23: stop further family-pair expansion and commit the verified unfinished work so external architecture review can use a clean immutable tree. This is an explicit exception to grouping the complete bounded-entry outcome before committing; it is not semantic-checkpoint approval or outcome acceptance. TypeScript currently integrates bounded preparation into its batch dispatcher, while Lean's corresponding finite prepared-transition integration remains open. The checkpoint therefore claims neither complete bounded cross-target batching nor its finite permutation/publication theorem. Remaining obligations are complete regional preparation/publication and state equality, publication composition for all pairs, finite integration, complete bounded fixture/resource evidence, and independent semantic review. The owner subsequently closed the architecture investigation without adopting a replacement; the approved [RC scope amendment](#rc-completion-scope-amendment) now requires dependency classification before further proof implementation. Preserve the identity corrections and adversarial cases.

### Timer-task arming outcome

This outcome groups interrupting `awaitBoundedUserTask` and non-interrupting `awaitMonitoredUserTask` arming under their existing shared activation function. Complete preparation must retain the actual operation, selected input owner, task and Timer waits, Activity record with its attached Timer, three separately issued counter values, and the logical-time-derived deadline. The footprint must protect the input-owner census, consumed token, live scope, runtime control, logical time, all three counters, both untagged public wait anchors, and the Activity/body/handler association. Neither variant creates an Activity-local variable scope or reads Process data. The existing [interrupting](capsules/ACTIVITY-BOUNDARY-TIMER-SPEC.md#temporal-hosting-and-refinement-preflight) and [non-interrupting](capsules/NON-INTERRUPTING-BOUNDARY-TIMER-SPEC.md#temporal-hosting-and-refinement-preflight) hosting preflights retain their scope; this work does not change firing, completion, profile admission, or Temporal admission.

The complete outcome requires both preparation directions, exact canonical state equality, successor runtime/open-set validity, and accepted publication derived from predecessor facts: the state exposes both waits, while the existing lifecycle contract starts only the task anchor, then integration through the existing arbitrary finite and mixed-family batch owners. Snapshot-declaring Programs remain excluded. [The separating core witnesses](../packages/semantic-core/test/internal-transition-timer-task-arming.test.ts) establish valid individual transitions and equal valid successors in both orders. Both constructed independent frontiers rolled back before this integration and now commit; insufficient fuel still rolls back the complete command. They establish no public-profile reachability. The [review receipt](#independent-cold-review-receipt) records acceptance of the complete outcome; helper compilation alone establishes no checkpoint.

Reuse assessment: both variants already use [one Lean activation function](../BpmnSemantics/SemanticProcess/WaitActivation.lean) and [one TypeScript selector](../packages/semantic-core/src/semantic-process-activity-arming.ts). The existing [Activity preparation](../packages/semantic-core/src/internal-transition-activity-arming-preparation.ts) already owns the full state footprint; extend it with retained operation/publication and checked batch dispatch rather than adding a second preparation owner. Existing wait/counter ordering, Activity-association conflicts, and finite batching supply reusable machinery. The data-arming insertion theorem cannot be applied unchanged: it adds a local variable scope and has no attached Timer. Before introducing aggregate preservation machinery, check the joint task/Timer declaration and ownership obligations against the existing frame owners; do not model the pair as two independent ordinary waits or assume intermediate validity.

The [Lean preparation](../BpmnSemantics/SemanticProcess/InternalTimerTaskPreparation.lean) uses the existing regional Activity-association atom, retains all three predecessor issuers, and refines the actual arming operation without assuming successor validity. The reuse check exposed an existing owner-coordinate mismatch in both [interrupting](../BpmnSemantics/SemanticProcess/BoundedTask.lean) and [non-interrupting](../BpmnSemantics/SemanticProcess/MonitoredTask.lean) Lean arming: runtime control names the root instance, while a called Process owns its own waits and Activity. Both arming relations and evaluators now use the selected owner's Process instance, matching TypeScript and the projection identity invariant. Quantified owner/running laws and constructed called-owner regressions cover this distinction; they do not widen public admission.

The proof owners below derive their claims from predecessor preparation and footprint separation.

| Proof owner | Derived guarantee |
|---|---|
| [Timer-task pair](../BpmnSemantics/SemanticProcess/InternalTimerTaskPreparationFrames.lean) and [patch equality](../BpmnSemantics/SemanticProcess/InternalTimerTaskCommutation.lean) | Both complete preparation directions and literal canonical state equality. |
| [Ordinary arming](../BpmnSemantics/SemanticProcess/InternalTimerTaskOrdinaryFrames.lean), [local control](../BpmnSemantics/SemanticProcess/InternalTimerTaskLocalControl.lean), and [Merge/End](../BpmnSemantics/SemanticProcess/InternalTimerTaskTokenCommutation.lean) | Both mixed preparation directions and exact state equality, retaining complete place and selected-join bucket censuses. |
| [Activity-data](../BpmnSemantics/SemanticProcess/InternalTimerTaskDataFrames.lean) and [scope creation](../BpmnSemantics/SemanticProcess/InternalTimerTaskScopeFrames.lean) | Both mixed preparation directions and exact state equality for all three data variants and both scope-creation kinds; independent issuers, Activity body claims, copied bindings, and fresh scopes remain accounted for. |
| [Regional pair](../BpmnSemantics/SemanticProcess/InternalTimerTaskRegionalCommutation.lean) and [accepted publication](../BpmnSemantics/SemanticProcess/InternalTimerTaskRegionalPairPublication.lean) | Return, scope completion, Error, and Terminate preserve both complete preparations and produce identical canonical state in either order; each actual operation accepts the same publication at its assigned index, with successor validity derived from predecessor facts. |
| [Runtime preservation](../BpmnSemantics/SemanticProcess/InternalTimerTaskRuntimePreservation.lean) and [exact open projection](../BpmnSemantics/SemanticProcess/InternalTimerTaskProjection.lean) | Existing collection and Multi-Instance frames compose without intermediate validity; all existing anchors remain and only the task is added, with private Timer classification derived from its joined Activity record. |
| [Per-operation publication acceptance](../BpmnSemantics/SemanticProcess/InternalTimerTaskAcceptedPublication.lean) | The actual transition record, task-only lifecycle delta, and consumed-token position match the existing publication template. |

The [unified transition pair](../BpmnSemantics/SemanticProcess/InternalTimerTaskTransitionPair.lean) integrates these laws through the existing [finite execution](../BpmnSemantics/SemanticProcess/InternalTransitionBatch.lean) and [canonical accepted-publication](../BpmnSemantics/SemanticProcess/InternalTransitionCanonicalBatchPublication.lean) theorems. Timer-task publication remains the existing arming template with only the task lifecycle start. The [shared TypeScript arming fixture](../packages/semantic-core/test/internal-arming-operation-fixture.ts) extends the existing regional and scope/local-control permutation matrices to both Timer-task variants, checking complete preparations, valid prefixes, literal final states, and actual accepted publications. The [kernel-decided fixture](../BpmnSemantics/InternalTimerTaskBatchConformance.lean) covers both variants in a three-operation frontier, unequal issuance counters, stale preparation refusal, task-only lifecycle publication, and whole-command fuel rollback; its isolated build passes the unchanged [resource protocol](CAPSULE-COST-LEDGER.md#timer-task-arming-2026-09-22). The [review receipt](#independent-cold-review-receipt) binds its complete gates and independent approval.

Regional reuse assessment: the existing [retirement law](../BpmnSemantics/SemanticProcess/InternalRegionalArmingFrames.lean) supplies unchanged issuance counters and subset-only resource populations, so both wait-anchor and Activity-association freshness follow without new aggregate preservation machinery. Existing ordered insertion/filter laws and [fresh Timer ownership](../BpmnSemantics/SemanticProcess/InternalCommutationActivityOwnership.lean) supply cancellation commutation. The joined insertion preserves [regional selection](../BpmnSemantics/SemanticProcess/InternalTimerTaskRegionalSelection.lean), [retention masks and footprints](../BpmnSemantics/SemanticProcess/InternalTimerTaskRegionalRetention.lean), and [ownership closure](../BpmnSemantics/SemanticProcess/InternalTimerTaskRegionalOwnership.lean), including retained local-data references. [Complete publication preservation](../BpmnSemantics/SemanticProcess/InternalTimerTaskRegionalPublication.lean) reuses the ordinary/data lifecycle filtering argument. In the opposite direction, the retained Timer-task preparation determines its full accepted template, avoiding another regional Process-lookup proof. Treating the task and attached Timer as separately prepared ordinary operations would omit their Activity association and remains excluded.

### Place-wide token-owner census checkpoint

The private footprint vocabulary now includes owner-free `tokenOwners(place)` alongside owner-specific `controlToken(owner,place)`. Census atoms conflict by exact place identity and carry no occurrence-region owner. They describe a dependency of the existing token collection, not a new RuntimeState field. The [TypeScript shared helper](../packages/semantic-core/src/internal-transition-token-preparation.ts) deduplicates affected places before canonical atom construction; owner-specific output-bucket checks do not imply a census read.

| Existing TypeScript preparation | Census reads | Census writes |
|---|---|---|
| Ordinary, composed-data, boundary-Activity, and event-race arming | Input | Input |
| Sequential/parallel Multi-Instance entry | Input | Input; normal output only for zero items |
| Duplicate, Choose, and SelectMany | Input | Consumed input and actual selected outputs |
| Synchronize | Every input | Every consumed input and output |
| SynchronizeSelected | None; record-supplied owners select exact buckets, with the separate population/readiness correction in the [approved amendment](#selected-join-readiness-dependency-amendment) | Actual selected inputs and output |
| Exact Merge alternative | None; its owner and input are already selected | Selected input and output |
| Ordinary/bounded scope entry and Call invocation | Input | Input and child/called entry |
| Process initiation | None | Actual selected outputs |
| None End | Input, including the evaluator's owner selection | Input |
| Error and Terminate | Input, including the evaluator's owner selection | Every pre-state token place in the removed region; Error also writes the handler output |
| Call return and scope completion | None; exact owner quiescence | Caller/parent output when present, plus token places actually removed by Call return |

Regional writes enumerate the exact pre-state ownership closure through both scope-parent and Call associations. Termination includes tokens owned by its retained root. Multiple regional owners at one place yield one census atom. The existing occurrence-region write separately conflicts with insertion into the region at a previously absent place. Successful return/completion requires regional quiescence; this table does not turn their owner-specific emptiness checks into place-wide reads. Exact Merge alternative preparation is distinct from complete frontier enumeration, whose broader read/frame obligation remains open.

The [Lean footprint core](../BpmnSemantics/SemanticProcess/InternalCommutationCore.lean) adds the same typed census atom to ordinary arming reads and writes; [composed arming](../BpmnSemantics/SemanticProcess/InternalDataArmingFootprint.lean) inherits both. [Quantified membership and conflict laws](../BpmnSemantics/SemanticProcess/InternalCommutation.lean) prove that every writer containing the reader's input census is non-independent of that arming footprint. Existing preparation, frame, finite raw-state, and accepted-publication laws retain their theorem contracts. Lean gains no prepared producer or regional execution family here.

The [TypeScript separating witnesses](../packages/semantic-core/test/internal-commutation-token-owner-census.test.ts) reproduce foreign-owner insertion invalidating ordinary/composed selection while the selected bucket stays exact, a second join-input collision, and Error/Terminate removal through descendant and called ownership. Disjoint-place changes remain independent. Family tests bind the table above. The [kernel witnesses](../BpmnSemantics/InternalCommutationConformance.lean) establish the foreign-owner selection change and removal reversal, then distinguish a declared census writer from the wrong owner-bucket-only footprint. These are helper/proof-boundary witnesses, not new admitted source or corpus claims. The first changed kernel fixture and [immutable calibration of both commutation consumers and four standing-watch modules](CAPSULE-COST-LEDGER.md#place-wide-token-owner-census-calibration) pass under the unchanged 3 GiB controller with zero controlled memory events, pressure totals, and swap. Complete Lean and TypeScript package gates pass on 2026-09-08. Production batching, storage ordering, admission, public wire contracts, and Temporal hosting are unchanged by this prerequisite.

### Canonical token-storage checkpoint

Both representations order stored tokens by `(placeId, owner.processInstanceId, owner.definitionScopeId, owner.activation)`, using Unicode scalar order for each String and numeric order for activation. Lean retains one unit per token, including repeated equal units; TypeScript retains its existing positive-multiplicity bucket representation. The shared Lean insertion helper now maintains this order, and both aggregate runtime predicates check it. Removal preserves the remaining order and exact owner selection. Neither predicate repairs an unordered input by sorting it.

[TokenStorage](../BpmnSemantics/SemanticProcess/TokenStorage.lean) proves insertion permutation, length, arbitrary-predicate counts, and removal sublist/permutation laws. [TokenOrder](../BpmnSemantics/SemanticProcess/TokenOrder.lean) proves complete-key order, order preservation by insertion/removal/filtering, and exact equality from ordered permutations. Two insertions commute for every predecessor, including equal token keys. Mixed insertion/removal and inverse laws require canonical predecessor storage; reinserting a removed unit additionally requires that unit to have been present. Existing aggregate-preservation consumers use these laws without adding a new premise to their public contracts.

The [kernel witnesses](../BpmnSemantics/CanonicalTokenStorageConformance.lean) separate every key coordinate, numeric activations 2/10, Unicode scalar order beyond the BMP, retained equal-unit multiplicity, and foreign-owner selection. Admitted root and child-scope Programs provide independently position-valid reversed-order states that only the aggregate order conjunct rejects. An unordered predecessor explicitly separates mixed update orders, preventing omission of the theorem's canonical-state premise. Existing parallel execution prefixes retain exact token units and aggregate well-formedness through Start, fork, both task-completion orders, and join. The [TypeScript witnesses](../packages/semantic-core/test/canonical-token-storage.test.ts) independently bind the storage facts and exact buckets through those producer paths. This checkpoint adds no prepared producer/regional family, scheduled choice, admitted model, public wire field, or Temporal mechanism; those final-closure obligations remain open.

The complete Lean library build and all 801 TypeScript semantic-core tests pass on 2026-09-08. [Immutable calibration](CAPSULE-COST-LEDGER.md#canonical-token-storage-calibration) passes for the new witness, both commutation consumers, all four standing-watch consumers, and the three changed cyclic witnesses under the unchanged 3 GiB controller, with zero controlled memory events, pressure totals, and swap. The receipt below records independent checkpoint approval and the separate complete repository-wide gate.

### Canonical scope and counter-storage checkpoint

Both runtime aggregates now check scope occurrences in `(processInstanceId, definitionScopeId, activation)` order and include Scope, Call, and event-race activation counters alongside the five already ordered counter families. Strings use Unicode scalar order and activations use numeric order. Shared Lean insertion replaces the remaining prepend writers for ordinary child entry, Call invocation, and event-race arming; the existing TypeScript writers already produce the selected order. Validation rejects inverted input without repairing it. Occurrence identity, counter lookup, high-water retention, uniqueness, and identity-bound coverage are otherwise unchanged.

[ScopeStorageOrder](../BpmnSemantics/SemanticProcess/ScopeStorageOrder.lean) proves scope comparator laws, insertion/filter order preservation, exact membership, distinct-identity insertion commutation, and ordered distinct-key replacement/commutation for all three counters. Its Lean lane is **proved** for those collection laws and **checked** for the [actual writer witnesses](../BpmnSemantics/ScopeStorageOrderConformance.lean): opposite child-entry, Call-invocation, and race-arming orders produce identical complete states with canonical storage and retained unrelated counters. The kernel examples separate all scope-key coordinates, numeric activations 2/10, Unicode BMP/non-BMP ordering, and each newly checked inversion. [Independent TypeScript tests](../packages/semantic-core/test/canonical-scope-storage.test.ts) isolate projectable invalid scope order, aggregate counter inversions, and actual scope/race lifecycle high-water retention. Existing aggregate-preservation consumers retain their public theorem contracts.

The complete Lean library and 897-test TypeScript semantic-core gate pass on 2026-09-09; the complete core gate passes again on 2026-09-18. The [working-tree resource checks](CAPSULE-COST-LEDGER.md#canonical-scope-storage-working-tree-checkpoint) retain the first bounded builds. [Immutable calibration and cost binding](CAPSULE-COST-LEDGER.md#canonical-scope-storage-immutable-calibration) cover the new witness, both corrected cached-order fixtures, five token/commutation/batch consumers, and all four standing-watch consumers at `a97a4ea1`, under the unchanged 3 GiB ceiling with zero controlled memory events, pressure totals, and swap. Independent checkpoint review and the clean complete repository gate pass at `da9657e0`; the [receipt](#independent-cold-review-receipt) records their exact evidence. Scope/region preparation may now consume this prerequisite. It adds no prepared family, source admission, profile, registered model, scheduled choice, public field, or Temporal mechanism; general regional preservation and final closure remain open.

### Scope-creation preparation prerequisite

The private TypeScript [scope-creation preparation](../packages/semantic-core/src/internal-transition-scope-creation-preparation.ts) retains the exact ordinary child-entry or Call-invocation operation, owner, created identity, complete footprint, local issuance patch, and numbering-free publication. Checked application re-derives and compares the whole preparation before applying the retained patch. Identity-first uniqueness checks, immutable control-place ownership and origin checks, safe counters and logical time, fresh scope/output buckets, and refusal of snapshot-declaring Programs bound this prerequisite.

The [patch owner](../packages/semantic-core/src/internal-transition-scope-creation-patch.ts) consumes one input token, produces one created-owner entry token, canonically inserts the scope and any Call association, and updates only the selected issuance counter. It preserves unrelated fields and optional-field presence. Public positions come from immutable provenance: child entry names the Sub-Process, while the called root names the called Process. The lifecycle template starts one Scope or Call Activity anchor; command and transition indices are assigned later.

The [executable matrix](../packages/semantic-core/test/internal-transition-scope-creation-preparation.test.ts) binds admitted single-operation prefixes to the existing runtime successor and actual position/lifecycle projections at a nonzero index. Constructed sibling child entries and distinct Calls check complete preparation frames and exact raw both-order equality without claiming structurally admitted sibling Programs or source reachability. Adversarial cases cover altered preparation facts, owner census, repeated issuance, malformed provenance, identity aliases, unsafe counters/time, and snapshot refusal.

This TypeScript prerequisite is independently approved and completely verified under the [review receipt](#independent-cold-review-receipt).

The Lean [scope-creation selector](../BpmnSemantics/SemanticProcess/InternalScopeCreationSelection.lean) retains the operation, predecessor owner and input, created scope and entry, and complete optional Call association. Its patch canonically inserts the created records and changes only the selected issuance counter. The **proved** lane binds successful predecessor selection to the complete existing `fire?` successor without snapshot declarations, preserves operation identity and running control, advances the child high-water counter, and refuses repeated Call input tokens. Refinement requires successful selection; applying a manually constructed selection supplies no validation guarantee. The existing scope-counter reader and Call sorter are shared by both executable consumers without changing the raw evaluators.

The [kernel witnesses](../BpmnSemantics/InternalScopeCreationSelectionConformance.lean) distinguish nonzero issuance, exact complete-state equality, unrelated counter retention, child token-unit consumption, repeated Call inputs, ambiguous or nonhosting ownership, existing child definitions, aliased caller roots, and nonrunning control. These are constructed predecessor examples, not admitted source models. The complete Lean gate, [immutable resource calibration](CAPSULE-COST-LEDGER.md#scope-creation-selection-calibration), independent checkpoint review, and clean complete repository verification pass under the [review receipt](#independent-cold-review-receipt).

The Lean [complete preparation](../BpmnSemantics/SemanticProcess/InternalScopeCreationPreparation.lean) now retains the selected patch, running instance, canonical read/write atoms, logical time, and numbering-free Scope or Call lifecycle start. It checks exact operation and owner populations before payload matching, unique definition identities and origins, exact created-definition parentage, safe counters/time/token counts, and fresh output buckets. The [position template](../BpmnSemantics/SemanticProcess/InternalScopeCreationPositionDelta.lean) resolves both differently owned token units and the entered scope from immutable Program provenance. Shared atoms now distinguish Scope/Call issuance, complete parentage, and every Call-association field; [dependency laws](../BpmnSemantics/SemanticProcess/InternalScopeCreationDependencies.lean) separate parent presence, parent identity, and return-operation identity.

Checked application compares the complete re-derived preparation before applying its retained patch. The **proved** lane exposes all deciding predecessor facts, retains operation identity, derives refinement to `fire?` from successful preparation, and refuses snapshot declarations, identity aliases, and altered publication time. The [kernel matrix](../BpmnSemantics/InternalScopeCreationPreparationConformance.lean) adds nonvacuous child/Call successors, exact position/lifecycle templates, creation dependencies, altered preparation fields, repeated token units, unsafe issuance, and identity/provenance refusals. These remain constructed helper witnesses. The complete Lean gate, [immutable calibration](CAPSULE-COST-LEDGER.md#scope-creation-preparation-calibration), independent checkpoint review, and clean complete repository verification pass under the [review receipt](#independent-cold-review-receipt).

The Lean [fresh-insertion laws](../BpmnSemantics/SemanticProcess/ScopeInsertionValidity.lean) preserve exact live-owner lookup, `waitOwnersLive`, and `activityRecordsOwnLiveWork` when the inserted complete identity is absent; inserting an existing live identity instead invalidates singleton ownership, regardless of parent payload. [Selector freshness](../BpmnSemantics/SemanticProcess/InternalScopeCreationFreshness.lean) derives that absence from successful child or Call selection and establishes the created scope's exact liveness plus every existing live owner's preservation through the actual retained patch.

The [Call reachability laws](../BpmnSemantics/SemanticProcess/CallReachability.lean) prove seed inclusion, fuel monotonicity, record-extension and permutation membership, and preservation under sorted Call insertion at the evaluator's record-count fuel. The extension proof requires a duplicate-free seed to justify equal-length early termination; the hosting-root singleton satisfies it. A quantified two-edge discriminator reaches the final instance with two fuel units and excludes it with one. These proof prerequisites pass their focused Lean gates, independent checkpoint review, and complete repository verification. Existing executable bodies, TypeScript, profiles, and Temporal behavior are unchanged.

The Lean [Call-association preservation law](../BpmnSemantics/SemanticProcess/ScopeCreationCallAssociations.lean) covers both actual scope-creation selector arms from predecessor association validity, with caller/called definition separation required only for invocation. It preserves exact parentless-root and record counts, Call-anchor uniqueness, and bounded sorted reachability; a quantified omission law rejects a fresh parentless root inserted without its Call record. The [admission facts](../BpmnSemantics/SemanticProcess/InternalScopeCreationAdmissionFacts.lean) derive definition separation from the existing paired-Call contract and successful complete preparation, reject same-definition invocation, and compose the actual preparation's association-preservation theorem without a successor-validity premise. [Call storage-order laws](../BpmnSemantics/SemanticProcess/CallStorageOrder.lean) prove comparator asymmetry and canonical order of the actual sorter, with a reversed-pair discriminator. These quantified laws pass focused Lean gates, independent checkpoint review, and clean complete repository verification under the [review receipt](#independent-cold-review-receipt); executable bodies and all other targets remain unchanged.

The Lean [runtime-position preservation law](../BpmnSemantics/SemanticProcess/InternalScopeCreationPositionValidity.lean) derives the complete existing `runtimePositionValid` predicate after actual child or Call preparation from Program admission, valid predecessor position, and successful preparation. Its private proof characterization is equivalent to the unchanged predicate. Fresh insertion preserves exact owner and parent lookup, the hosting-root census, both truth values of the exclusive hosting/Call-root association, the Call's Process-name payload, and token bindings; existing token removal and insertion laws finish the retained patch. The [kernel witnesses](../BpmnSemantics/InternalScopeCreationValidityConformance.lean) jointly check admitted Programs, valid constructed predecessors with nonzero counters, successful preparation, complete runtime-valid successors, and equality with the existing evaluator. Removing the new child scope rejects position validity; changing only the called Process name preserves association validity but rejects position validity. The [immutable fixture measurement](CAPSULE-COST-LEDGER.md#scope-creation-position-calibration) passes the unchanged 3 GiB ceiling. Independent checkpoint review and clean complete repository verification pass under the [review receipt](#independent-cold-review-receipt).

The Lean [aggregate preservation law](../BpmnSemantics/SemanticProcess/InternalScopeCreationRuntimeValidity.lean) derives the unchanged `runtimeStateWellFormed` predicate after successful actual child or Call preparation from Program admission and a well-formed predecessor. It composes position preservation with incident ownership, existing and newly inserted Call caller liveness, Activity-body ownership, canonical collection order, and every unchanged conjunct. The [Compensation support laws](../BpmnSemantics/SemanticProcess/ScopeCreationCompensationValidity.lean) preserve complete retention-owner filters, active-trigger root counts, and the existing lifecycle and collision checks under fresh insertion. Quantified discriminators separate a same-identity parent-bearing alias from a duplicate parentless root; [nonempty kernel witnesses](../BpmnSemantics/ScopeCreationCompensationValidityConformance.lean) exercise both existing Compensation predicates directly without claiming a newly admitted transition composition. The complete Lean gate, [immutable resource calibration](CAPSULE-COST-LEDGER.md#scope-creation-compensation-calibration), independent checkpoint review, and clean complete repository verification pass under the [review receipt](#independent-cold-review-receipt).

The Lean [complete preparation frame law](../BpmnSemantics/SemanticProcess/InternalScopeCreationFootprintFrames.lean) derives both opposite preparations unchanged from structural Program validity, predecessor runtime validity, two actual successful preparations, and the existing footprint separation predicate. [Selection frames](../BpmnSemantics/SemanticProcess/InternalScopeCreationSelectionFrames.lean) preserve definition-wide child exclusion, instance-wide Call exclusion, exact token-owner populations, and issuance counters; [artifact frames](../BpmnSemantics/SemanticProcess/InternalScopeCreationPreparationFrames.lean) retain the complete footprint and numbering-free publication template. Mixed child/Call pairs use unique static parentage to distinguish their definitions, and Call pairs use derived-instance injectivity plus the approved association-preservation law. No successor preparation or validity premise is assumed.

The [kernel witnesses](../BpmnSemantics/InternalScopeCreationFrameConformance.lean) jointly establish structural Program validity, predecessor validity, and independent preparations for child/child, child/Call, and Call/Call frontiers, then check complete preparation equality in both orders. Different-identity population collisions and changed issuance counters distinguish the required reads. The constructed Programs reuse an existing acyclic graph policy but explicitly fail its separate profile capability gate; neither source admission nor source reachability is claimed. The complete Lean gate, [immutable calibration](CAPSULE-COST-LEDGER.md#scope-creation-preparation-frame-calibration), independent checkpoint review, and clean complete repository verification pass for this frame increment.

The independently approved Lean [complete-state commutation laws](../BpmnSemantics/SemanticProcess/InternalScopeCreationCommutation.lean) derive exact RuntimeState equality for all four independent child/Call pairings from structural Program validity, predecessor runtime validity, actual complete preparations, and declared footprint separation. The complete pair theorem also derives both opposite preparations and every intermediate/final runtime-validity result. Token, scope, and counter laws preserve canonical lists and high-water records. [Call canonical equality](../BpmnSemantics/SemanticProcess/CallRecordCanonicalEquality.lean) uses association-derived caller/element anchor uniqueness, canonical order, and permutation; its partial comparator alone does not identify retained payloads. The extended constructed witnesses compare complete states and final validity in both orders and reject equal-key/different-return-payload aliases. The complete Lean gate, [immutable calibration](CAPSULE-COST-LEDGER.md#scope-creation-commutation-calibration), and clean complete repository verification pass under the [review receipt](#independent-cold-review-receipt).

The Lean [control-position correspondence law](../BpmnSemantics/SemanticProcess/InternalScopeCreationPositionPublication.lean) derives the actual `controlPositionDelta?` result from structural Program validity, predecessor runtime validity, and successful complete preparation. Successor projectability follows from aggregate preservation. Exact private-token counts and immutable origin bindings yield one consumed parent-owned unit and one produced created-owner unit while preserving all other multiplicities. [Fresh scope projection](../BpmnSemantics/SemanticProcess/ScopePositionInsertion.lean) uses permutation and identity freshness to retain the complete entered scope payload and derive no exits, without equating comparator keys with payload equality. The constructed kernel witnesses check child and Call deltas, repeated child-input consumption with one retained unit, unchanged repeated-Call refusal, and wrong produced-owner or scope-parent negatives.

The Lean [accepted-lifecycle law](../BpmnSemantics/SemanticProcess/InternalScopeCreationAcceptedPublication.lean) derives the actual `flowNodeOccurrenceDeltaForOperation?` result from structural Program validity, predecessor runtime validity, successful predecessor open projection, and complete preparation. It proves exactly one Scope or Call start with no ends at arbitrary command and transition indices. Its pair theorem derives acceptance at both successive indices in either independent execution order and one shared final open projection. Neither intermediate projectability, successor validity, candidate acceptance, nor final equality is assumed.

[Structural lifecycle preservation](../BpmnSemantics/SemanticProcess/ScopeCreationLifecycleStructuralValidity.lean) derives exact child-entry and reverse Call-pair populations from the admitted Program. [Wait preservation](../BpmnSemantics/SemanticProcess/ScopeCreationLifecycleValidity.lean) keeps existing owner and boundary-Timer classification checks unchanged. [Projection insertion](../BpmnSemantics/SemanticProcess/InternalScopeCreationOpenProjection.lean) preserves successful old Process lookups and all existing start payloads, derives the new anchor's freshness, and establishes canonical insertion from actual component projections. The new owner intentionally gains a lookup; no universal before/after lookup equality is claimed.

The extended [constructed witnesses](../BpmnSemantics/InternalScopeCreationFrameConformance.lean) check child/child, child/Call, and Call/Call publication in both orders at nonzero indices, then distinguish missing, duplicate, wrong-Process, wrong-owner, and wrong-anchor starts. The fixed-3-GiB first build, complete Lean gate, and [immutable resource calibration](CAPSULE-COST-LEDGER.md#scope-creation-lifecycle-publication-calibration) pass. Complete repository verification and independent approval for this lifecycle checkpoint pass under the [review receipt](#independent-cold-review-receipt). Production scope/region batching and scheduled choice remain open.

The [mixed scope/local-control laws](../BpmnSemantics/SemanticProcess/InternalScopeCreationLocalControlCommutation.lean) and [mixed scope/arming laws](../BpmnSemantics/SemanticProcess/InternalScopeCreationArmingCommutation.lean) preserve both complete predecessor preparations and literal final-state equality. Owner-free place census conflicts exclude input/entry interference even across different owners. The [finite batch induction](../BpmnSemantics/SemanticProcess/InternalTransitionBatch.lean) now includes both scope kinds, derives every prefix's runtime validity and open projection, and reuses the accepted-lifecycle and position laws for actual canonical publication. Scope insertion preserves successful old wait-start queries; the newly created owner invalidates unrestricted lookup equality, so publication frames require actually prepared queries.

The [TypeScript batch dispatcher](../packages/semantic-core/src/internal-transition-batch.ts) uses the existing complete scope preparation and checked application. Its [scope batch witnesses](../packages/semantic-core/test/internal-transition-scope-creation-batch.test.ts) exercise every arming kind, local control, all scope pairings, exact payloads and counters, optional collection presence, actual publication, stale/forged/refused frontiers, and whole-command rollback. Constructed mixed Programs explicitly fail profile admission; singleton controls preserve admitted child and Call execution. The [review receipt](#independent-cold-review-receipt) records passing complete repository verification and independent integration approval after the Semantic Process contract correction.

The [Lean production witness](../BpmnSemantics/ScopeCreationMixedBatchConformance.lean) checks child and Call document-review variants: actual Start admission, a reached scope/local-fork/Timer frontier, eight-step closure to three User Tasks and a deadline, accepted runtime/open/position projections, exact occurrence identities and canonical publication indices, and whole-command rollback at seven steps. Both Programs are structurally valid and explicitly outside profile capability admission. The [resource ledger](CAPSULE-COST-LEDGER.md#scope-creation-mixed-batch-calibration) records both first bounded builds and immutable calibration under the unchanged 3 GiB ceiling, with zero memory events, pressure, and swap; the [module-cost guard](../scripts/lean-module-cost.test.ts) verifies source binding.

The aggregate law preserves the existing identity-bound and hidden-Call declaration coverage without broadening them. The raw TypeScript child selector can use a called owner, whereas Lean ordinary child entry requires the hosting instance; the admitted profiles do not expose that composition, and this checkpoint claims no cross-target correspondence for it. The constructed nonzero-counter predecessors do not establish source reachability or final closure.

### Local control preparation prerequisite

The [TypeScript preparation owner](../packages/semantic-core/src/internal-transition-local-control-preparation.ts) now retains the exact operation, alternative, owner, branch result, local patch, and numbering-free publication for `duplicate`, `synchronize`, `choose`, `selectMany`, and `synchronizeSelected`. The [patch owner](../packages/semantic-core/src/internal-transition-local-control-patch.ts) removes and inserts exact token units, applies one keyed selected-record edit, and derives signed public token deltas from immutable control-place origins. Checked application re-derives and compares the entire preparation. Predecessor arithmetic refuses unavailable consumed units and unsafe output multiplicity. Footprints deduplicate bucket dependencies while patch lists retain every consumed and produced unit, including same-bucket turnover. Repeated-input tests exercise private runtime-predicate-valid records; continuation decoding still requires distinct expected inputs, and no newly reachable source model is claimed.

The [executable matrix](../packages/semantic-core/test/internal-transition-local-control-preparation.test.ts) binds Parallel fork/join and Inclusive split/join single steps to admitted fixture prefixes and compares sixteen synthetic helper-level pairs among Duplicate, Synchronize, Choose, and SelectMany. It checks opposite complete preparations, exact raw state, defined actual publication at a nonzero index, multiplicity, optional-field presence, shared Process reads, foreign-owner input interference, and altered preparation/provenance refusal. Those synthetic pairs do not establish structural Program admission or registered source coverage. The quantified Lean proof boundaries and production integration are described below.

The Lean [TokenPatch owner](../BpmnSemantics/SemanticProcess/TokenPatch.lean) lifts the approved unit-storage laws to finite consumed and produced lists. It proves canonical-order preservation, exact raw patch commutation when neither patch inserts a token unit consumed by the other, and arbitrary-predicate filtering plus place-wide owner-census and owner-selection frames for untouched places. Removal/removal and insertion/insertion commute even for equal keys; these algebraic equalities alone do not establish operation enabledness or permit conflicting footprints to batch.

The [token-validity owner](../BpmnSemantics/SemanticProcess/InternalLocalControlTokenValidity.lean) derives position and aggregate runtime validity from a live owner and static output declarations, and frames the running open-occurrence projection. The [selected-record patch owner](../BpmnSemantics/SemanticProcess/InternalSelectedBranchPatch.lean) proves canonical insert/erase commutation, order preservation, and complete same-key read frames from distinct population keys. The [pairing repair](capsules/INCLUSIVE-GATEWAY-SPEC.md#lean-assurance-lane) supplies declaration uniqueness. These helpers do not establish complete local-control preparation, accepted publication, or production batches.

The [selected-record validity owner](../BpmnSemantics/SemanticProcess/InternalSelectedBranchPatchValidity.lean) additionally proves aggregate runtime validity for a running predecessor. Insertion requires an exact live owner and exactly one key declarer; preservation and removal require no new record premise. The actual running open projection is unchanged from predecessor owner validity and insertion liveness, including when another projection check fails. Symbolic refusal laws reject a dead inserted owner or invalid declarer count. Existing duplicate owner/key records remain permitted by the unchanged runtime predicate; this proof does not select a join or establish a complete prepared transition.

The [branch-selection owner](../BpmnSemantics/SemanticProcess/InternalLocalControlBranchSelection.lean) retains each Exclusive selection's output, origin, and evaluated-prefix variable reads, and each Inclusive selection's exact output/input pairs and all candidate reads. Projection laws preserve the existing evaluators, including failure. Complete branch-selection frames require equal binding populations only for the successful predecessor's recorded reads; false Exclusive prefixes and false Inclusive candidates remain dependencies, while unvisited Exclusive suffixes do not. These are branch-selection laws, not complete operation-preparation or batch laws.

The [local-control selector](../BpmnSemantics/SemanticProcess/InternalLocalControlSelection.lean) retains the exact operation, owned consumed/produced token lists, branch result, and selected-record patch for all five families. Its quantified refinement theorem realizes each retained patch through actual `fire?` under the existing no-snapshot-declaration boundary. Static provenance, multiplicity availability, complete footprints, preparation frames, and publication have separate owners below; raw selected-join readiness alone does not certify consumption multiplicity or unique owner/key storage.

The [complete selection frames](../BpmnSemantics/SemanticProcess/InternalLocalControlSelectionFrames.lean) preserve every retained selector field from predecessor token-owner censuses, evaluated variable populations, selected-key populations, and all same-key records' readiness buckets. Symbolic discriminators show why an unready other owner's absent input and same-key record insertion must both remain dependencies. The theorem assumes neither successor selection nor state validity; the complete preparation owner below consumes these selector frames.

The [local-control position template](../BpmnSemantics/SemanticProcess/InternalLocalControlPositionDelta.lean) derives signed token deltas from immutable owned Sequence Flow origins and full consumed/produced unit counts. It cancels same-place turnover, orders residual entries by Sequence Flow identity, and rejects empty or aliased provenance. Symbolic laws cover repeated-unit residuals, exact turnover, and two distinct places sharing one origin. The [public multiplicity laws](../BpmnSemantics/SemanticProcess/ControlPositionMultiplicity.lean) prove that projection counts every private token unit sharing a public key, including repeated units, and that canonical insertion adds the exact incoming multiplicity. The position-delta correspondence owner below binds these templates to actual accepted publication.

The [static-origin bridge](../BpmnSemantics/SemanticProcess/InternalLocalControlOriginFacts.lean) derives exact projection lookup from successful prepared provenance. The nonempty globally unique Sequence Flow origin identifies exactly one private place, including when other owners occupy that place, and its projected count equals the complete owned private-token count. This uses the existing origin refusal checks and introduces no new admission restriction.

The [template order proof](../BpmnSemantics/SemanticProcess/InternalLocalControlPositionTemplateOrder.lean) derives unique resolved flow keys and strict sorting from successful provenance. The [template facts](../BpmnSemantics/SemanticProcess/InternalLocalControlPositionTemplateFacts.lean) derive canonical positive residual lists, exact signed lookup at every touched place, zero lookup outside touched public keys, and empty scope deltas from successful template construction; none is an additional preparation premise.

The [public difference algebra](../BpmnSemantics/SemanticProcess/ControlPositionDifferenceAlgebra.lean) proves positive residuals, strict key order, and pointwise multiplicity subtraction for the actual `tokenDifference` function. Strict positive lists with equal counts at every public key are exactly equal. Symbolic zero-row and swapped-order counterexamples separate positivity and canonical order from count equality; these helper laws do not assume or establish complete local-control publication correspondence.

The [token-patch multiplicity laws](../BpmnSemantics/SemanticProcess/TokenPatchMultiplicity.lean) derive exact owned counts after finite removal and insertion, preserve every foreign owner's count, and derive both signed residuals when consumption is available. A symbolic repeated-turnover law retains two produced units at one place. Availability remains a separate predecessor obligation because saturating removal alone cannot justify a public consumed residual.

The [position-delta correspondence](../BpmnSemantics/SemanticProcess/InternalLocalControlPositionDeltaProofs.lean) derives exact equality between the retained template and `controlPositionDelta?` over the independently projected predecessor and patched successor. Successful complete preparation supplies available consumption and provenance; untouched public-key counts frame, touched-key counts yield both signed residuals, and strict positive representation yields exact list equality. The complete theorem assumes only an admitted Program, a well-formed running predecessor, and successful preparation; it derives successor position validity and delta equality. It does not assume an intermediate invariant, successor preparation, final-state equality, or publication equality. Complete Lean and focused source/document gates pass on 2026-09-08.

The [complete Lean preparation](../BpmnSemantics/SemanticProcess/InternalLocalControlPreparation.lean) retains the five-family selection, canonical state footprint, exact static lifecycle identity, logical time, and index-free publication template. It checks live ownership, exact operation selection, owned provenance, available consumed units, safe resulting multiplicity, and exact owner/key removal before accepting a preparation. Checked application re-derives the whole retained value. Quantified refinement realizes the patch through actual `fire?` under the existing no-snapshot-declaration boundary; symbolic guards reject excess repeated consumption, a second owner/key record with different inputs, and altered publication time. The [complete preparation frame](../BpmnSemantics/SemanticProcess/InternalLocalControlPreparationFrames.lean) preserves the entire artifact from exact predecessor read populations without an intermediate readiness, preparation, or validity premise. The lifecycle identity uses immutable Program ancestry and an exact live owner; the validity and accepted-publication owners below consume this preparation. Complete Lean and focused source/document gates pass on 2026-09-08.

The [prepared-step validity bridge](../BpmnSemantics/SemanticProcess/InternalLocalControlPreparationValidity.lean) derives aggregate runtime well-formedness from an admitted Program, a well-formed running predecessor, and successful complete preparation. Successful provenance supplies exact output declarations and scope bindings; static lifecycle identity supplies actual operation membership; selected split ownership and admitted declaration uniqueness supply the inserted record's validity. The actual optional open-occurrence projection is framed exactly. No intermediate validity, output-binding, operation-membership, or successor-preparation hypothesis is assumed. Complete Lean and focused source/document gates pass on 2026-09-08.

The [accepted lifecycle bridge](../BpmnSemantics/SemanticProcess/InternalLocalControlLifecyclePublication.lean) derives exact owner selection, transition record, candidate lifecycle, and actual accepted singleton start/completion from complete preparation at arbitrary command and transition indices. Its predecessor validity, running identity, and successful open projector derive the unchanged successor open set; projector success already supplies Program validity. The [instantaneous fold algebra](../BpmnSemantics/SemanticProcess/FlowNodeOccurrenceInstantaneousPublication.lean) derives fresh transient anchors and canonical open-set preservation and rejects reused anchors and missing completion. Complete Lean and focused source/document gates pass on 2026-09-08.

The [footprint-derived pair law](../BpmnSemantics/SemanticProcess/InternalLocalControlFootprintCommutation.lean) derives preservation of both complete preparations and exact raw state commutation from predecessor footprint independence and canonical collection order. The five local-control families use equality-keyed state atoms: read/write separation frames full input-owner censuses and owned token buckets, including every same-key selected-join record's other-owner readiness buckets; write/write separation frames complete selected-key populations. No intermediate preparation, readiness, validity, or final equality is assumed. This theorem covers local-control patches; the actual pair-publication and mixed-batch owners below consume it. General region overlap remains open.

The [actual pair-publication composition](../BpmnSemantics/SemanticProcess/InternalLocalControlPairPublication.lean) connects both execution orders to all four actual `fire?` steps, one exact final RuntimeState, and each original template's accepted transition record, lifecycle delta, position delta, and logical time. It derives intermediate well-formedness, running identity, open projection, and complete preparation from the original admitted running predecessor and footprint separation. Each operation keeps its arbitrary assigned transition index across both orders; a symbolic unequal-index anchor discriminator prevents confusing state commutation with execution-order numbering. The existing no-snapshot-declaration boundary remains explicit. Complete Lean and focused source/document gates pass on 2026-09-08.

The [mixed arming frame helpers](../BpmnSemantics/SemanticProcess/InternalLocalControlArmingFrames.lean) preserve complete ordinary and composed Activity-data preparations after local control when the arming input census is untouched. The ordinary law covers correlated Message selection and effect-local freshness through the same actual preparation function. Token-patch algebra proves exact raw mixed commutation from canonical token order and absence of production at the arming input. These are explicit read-frame helpers consumed by the mixed footprint pair and publication proofs below. Complete Lean and focused source/document gates pass on 2026-09-08.

The [mixed footprint pair law](../BpmnSemantics/SemanticProcess/InternalLocalControlArmingCommutation.lean) derives both complete preparations and exact canonical final-state equality for every local-control/arming pair from computed state-footprint separation. Ordinary and composed-data arms use their existing read/write sets; their actual token removal frames all local input censuses and owned buckets, including competing selected-join owners. A symbolic conflict law requires no owner equality: writing any token at an arming input conflicts with its complete owner census. No successor preparation, readiness, validity, or final equality is assumed. Complete Lean and focused source/document gates pass on 2026-09-08.

The [finite mixed preparation owner](../BpmnSemantics/SemanticProcess/InternalPreparedTransition.lean) classifies the existing arming and five local-control families, retains every exact operation in caller order, and requires at least two members, unique operation alternatives, and all-pairs independence. Arming pairs retain their existing publication conflicts; local-involved pairs use exact state footprints. Quantified soundness derives each complete predecessor preparation, operation binding, list multiplicity, unique alternatives, and separation from successful classification. Snapshot-declaring execution remains refused, and checked application requires the retained complete preparation. Complete Lean and focused source/document gates pass on 2026-09-08.

The classifier's dependency closure excludes [trace execution](../BpmnSemantics/SemanticProcess/TransitionTrace.lean). Moving the unchanged arming-independence symmetry law beside its relation in [InternalPreparedArming](../BpmnSemantics/SemanticProcess/InternalPreparedArming.lean) removes its incidental import of the arming batch's trace-publication proof dependencies. The import-graph discriminator previously reached trace and now excludes it across the classifier's complete dependency closure. This is a non-material module extraction; theorem names, propositions, proof terms, and behavior are unchanged.

The [publication execution owner](../BpmnSemantics/SemanticProcess/InternalTransitionPublication.lean) places the existing templates, canonical numbering, checked prefix projection, and accepted batch function below trace. [TransitionRecord](../BpmnSemantics/SemanticProcess/TransitionRecord.lean) owns the unchanged operation metadata and strict replay; [ControlPositionProjection](../BpmnSemantics/SemanticProcess/ControlPositionProjection.lean) owns the unchanged public-position projection and delta algebra. Their previous owners retain the closure and proof consumers. All six moved definition blocks are byte-identical, and the complete publication dependency graph excludes trace, allowing production closure to consume the proved functions without a cycle. This separate extraction changes neither execution nor the proof account.

The [finite mixed execution proof](../BpmnSemantics/SemanticProcess/InternalTransitionBatch.lean) lifts the proved local, arming, and mixed pairs to arbitrary finite prefixes and multiplicity-preserving permutations. Every prefix derives complete remaining preparations, aggregate runtime well-formedness, running identity, projectability, and canonical collection order. Both the checked prepared fold and the actual `fire?` operation fold succeed under the existing no-snapshot-declaration boundary, with the exact raw final RuntimeState equal in every independent permutation. No list-length bound, intermediate invariant, successor preparation, or final equality is a premise. Complete Lean and focused source/document gates pass on 2026-09-08.

The [unified publication template](../BpmnSemantics/SemanticProcess/InternalTransitionPublicationTemplate.lean) retains exact transition records, logical time, position deltas, and either durable wait starts or uninstantiated local identities. Construction reads only prepared data and predecessor owner ancestry. The actual optional record/lifecycle/position projector accepts the instantiated value at arbitrary assigned indices; the ordinary arming index frame extends its existing index-zero acceptance, and composed-data and local families use their quantified acceptance bridges. Complete templates frame exactly across every supported prepared update because time and owner ancestry remain unchanged. No successor projection constructs a template and no acceptance or delta-equality premise is supplied. Complete Lean and focused source/document gates pass on 2026-09-08.

At this prerequisite's checkpoint, the selected-join footprint was incomplete: its selector required exactly one ready record across every owner of the same selection key, while its reads protected only the chosen record and buckets. On 2026-09-08, two structurally admitted, runtime-well-formed, open-set-projectable child-occurrence states separated that gap. A producer made an existing second record ready, and a split inserted a second record whose expected token already existed; both old footprint pairs were independent while the join preparation changed from present to absent. Neither state is claimed reachable through a registered source profile. The implemented [selected-join dependency checkpoint](#selected-join-dependency-checkpoint) repairs that account without restricting the existing runtime invariant to one owner per selection key; batch integration remains open.

The [mixed publication ordering proof](../BpmnSemantics/SemanticProcess/InternalTransitionPublicationOrder.lean) sorts complete templates by unique operation identity before assigning consecutive lifecycle indices. Arbitrary finite input permutations retain the exact canonical template list and instantiated publication list, including multiplicity and every payload field. Sorting actual publications instantiated at canonical ranks yields that same consecutively numbered list; membership proves the assigned index lies inside the batch. These are natural-number ordering laws, not an extension of wire capacity. Complete Lean and focused source/document gates pass on 2026-09-09.

The [finite-prefix publication bridge](../BpmnSemantics/SemanticProcess/InternalTransitionBatchPublication.lean) executes the checked prepared update and actual optional record/lifecycle/position projection at every prefix. For arbitrary finite independent preparations, it derives all intermediate runtime and preparation invariants and proves every actual publication equals its complete original template at any fixed operation-index assignment. Template identities are exactly the prepared operation identities, so classifier uniqueness and input permutations transfer to complete templates. Complete Lean and focused source/document gates pass on 2026-09-09.

The [canonical accepted mixed-batch theorem](../BpmnSemantics/SemanticProcess/InternalTransitionCanonicalBatchPublication.lean) composes complete predecessor templates, canonical rank assignment, checked execution, and actual prefix projection. Every arbitrary finite independent permutation succeeds with one exact final RuntimeState and one exact accepted publication list. It derives final aggregate validity, running identity, projectability, and agreement with both the checked fold and actual `fire?` fold. No intermediate validity, acceptance, successor preparation, or final equality is a premise. The [review receipt](#independent-cold-review-receipt) records independent proof and production-integration approval; the proof checkpoint's clean complete gate passed on 2026-09-09.

The [mixed kernel witness](../BpmnSemantics/FiniteInternalTransitionConformance.lean) admits a structural claim-assessment Program and running three-member frontier containing a local routing fork, ordinary human review, and composed-data coverage assessment. It instantiates the arbitrary-permutation theorem and binds actual accepted publication to complete original templates, exact records/time/position/lifecycle, and consecutive indices 37–39. Stale same-ID publication time and an individually prepared input writer/reader conflict refuse. Numbering before sorting changes the local anchor across reverse order; canonical template sorting preserves it. This proves structural Program/runtime admission, not registered XML admission or source reachability. The first completed witness build and [immutable calibration](CAPSULE-COST-LEDGER.md#finite-mixed-local-control-calibration) pass under the unchanged 3 GiB controller with zero controlled memory events, pressure totals, and swap. The complete Lean library and all 841 TypeScript semantic-core tests pass on 2026-09-09.

The [TypeScript mixed batch owner](../packages/semantic-core/src/internal-transition-batch.ts) integrates the five local-control families with ordinary/composed-data arming into production closure. It prepares every candidate against the same predecessor, requires unique alternatives and every pair's sufficient independence, and applies retained complete preparations. Publication sorts complete templates before assigning indices, then checks equality with actual accepted record/lifecycle projections at every execution prefix. The [production matrix](../packages/semantic-core/test/internal-transition-local-control-batch.test.ts) covers all five local families beside all seven arming families through six three-member orders, and four distinct families through all 24 orders. Committed Start and later fuel exhaustion bind successful batching and whole-command rollback; unsupported members, nonadjacent conflicts, stale preparations, and numbering-before-sort are separating refusals. These are admitted internal Program fixtures, not registered XML reachability evidence. The complete 886-test semantic-core gate passes on 2026-09-09; the [review receipt](#independent-cold-review-receipt) records joint production approval.

The [Lean production closure](../BpmnSemantics/SemanticProcess/TransitionTrace.lean) now classifies the complete enabled frontier through the proved mixed preparation owner and calls the actual accepted mixed-batch function after checking fuel for every member. It retains the function's canonical records and lifecycle rows, recomputes the frontier, and uses the existing whole-command rollback on later refusal. The [production kernel witness](../BpmnSemantics/MixedLocalControlClosureConformance.lean) starts a structurally admitted concurrent-review Program, reaches a local fork beside a human wait and deadline, and commits three human waits and one timer with exact publication and lifecycle indices. One fewer fuel unit restores the pre-command state and removes every publication. Reversed and rotated Program storage fail the existing structural admission rule; arbitrary prepared-batch permutations remain covered by the separate quantified theorem. This is internal Program evidence, with no new checked-XML or general fuel theorem claim. The first successful fixture build and [immutable production calibration](CAPSULE-COST-LEDGER.md#mixed-local-control-production-calibration) pass under the unchanged 3 GiB controller with zero controlled memory events, pressure totals, and swap. The complete Lean gate, independent production review, and clean [complete repository verification](PLAN.md#current-evidence) pass on 2026-09-09.

### Representation and reuse assessment, 2026-09-20

The E2 locality pilot preserved the exact hypotheses and conclusion of `prepared_local_control_pair_commutes`, including both complete successor preparations and literal RuntimeState equality. Its inhabited certificate proves write/read frames, exact collection reconstruction, and residual-state equality without consuming the old pair theorem or assuming commutation. Root Lean execution and the isolated feasibility review found only the existing `propext`, `Classical.choice`, and `Quot.sound` dependencies. The local 750-line prototype is `/tmp/BpmnLocalControlPilot.lean`, SHA-256 `0b6608de4f817c38e6b357e60657f0b7a58ae807bc511c4769d3bcff03c228db`; `/tmp/bpmn-locality-final-pilot` retains the successful command and axiom report. It could replace roughly 130–140 same-family proof lines, while other consumers still require the shared frame owners. Retain production pair proofs: this demonstrates feasibility, not net simplification or cross-family savings. Reopen migration only when a shared implementation reduces the complete maintained proof machinery without weakening canonical order, multiplicity, residual state, or preparation preservation. The [retained locality counterexamples](../packages/semantic-core/test/internal-transition-local-control-preparation.test.ts) remain executable discriminators.

The consumer audit retains the Boolean `runtimeStateWellFormed` account and its named predicates/accessors. A second Prop structure would duplicate the invariant inventory unless its equivalence and maintenance benefit were established; an accessor can remove repeated ordinal unpacking without that migration. RuntimeState values and [state atoms](../BpmnSemantics/SemanticProcess/InternalCommutationCore.lean) have distinct responsibilities: atoms describe conflicts, including derived owner censuses and absence-sensitive reads, rather than independent stored values. The [cross-target census guard](../scripts/internal-commutation-census.test.ts) deliberately compares independently maintained representations. The audit supplies no evidence for replacing them with one generated store.

Concrete consolidation instead moves three byte-identical collection lemmas, each previously copied three times, to [CollectionOrder](../BpmnSemantics/SemanticProcess/CollectionOrder.lean): `orderedBy_erase`, `all_occursOnce_erase`, and `all_occursOnce_filter`. Existing Message, composed Activity-data, and Parallel Multi-Instance consumers keep their exact hypotheses and conclusions. Parallel progress also reuses `activityIdentitiesUnique_member_eq` from [the invariant owner](../BpmnSemantics/SemanticProcess/RuntimeStateWellFormed.lean). This removes duplicate proofs without changing evaluator definitions, aggregate invariants, or the kernel fixture lane. Scalar-order wrappers and wait-ID projections remain consumer-local: their extraction has not demonstrated a saving across the affected proof consumers and import boundaries.

The extraction is classified non-material: it changes no theorem contract, evaluator, or assurance boundary. The complete Lean gate checks its proof consumers; the complete infrastructure gate checks source contracts, classifications, and owner inventories. The assessment above authorizes no broader representation migration.

### Regional preparation prerequisite

The 2026-09-20 E1 investigation establishes a source-grounded exclusion argument for independent regional frontiers in current registered execution-admitted Programs, starting from initial state and using fully drained commands. This is a reviewed capability-level argument, not a Lean reachability theorem, a singleton classification, or approval to retire the regional obligations below. It excludes private checkpoints and arbitrary constructed intermediate states.

The [admission guard](../packages/semantic-core/test/regional-frontier-admission.test.ts) derives each registered capability's actual operation multiset and graph policy. The argument also uses [graph admission](../packages/semantic-core/src/semantic-process-graph-admission.ts), [scope quiescence](../packages/semantic-core/src/semantic-process-scope-runtime.ts), and [complete-frontier closure](../packages/semantic-core/src/semantic-process-closure.ts):

- Initial execution has one path; committed commands drain internal work, and rejected or rolled-back commands preserve the preceding state.
- Single-scope capabilities have only root completion among regional operations; quiescence excludes concurrent live work. Call and bounded-scope capabilities have no fork; bounded interruption removes its child atomically.
- Other nested capabilities have one fork and one child entry, with no additional internal forwarding mechanism. With the fork inside the child, parent execution is suspended; completion requires quiescence, and Error/Terminate conflicts with remaining child work. With the fork outside, its peer parks or is consumed before child regional work becomes enabled. At the E1 checkpoint, an unsupported earlier End batch could instead roll back the command; the [ordinary End outcome](#ordinary-end-batch-outcome) removes that limitation. The retained source witness now checks committed End/arming state and publication, with rollback for insufficient fuel; it adds no independent regional frontier.

The guard's separating Program adds exactly one Duplicate and one None End to the Terminate capability. Generic structure accepts it, execution admission rejects it, and complete earlier frontiers reach an independent Terminate/two-task frontier. All six orders preserve complete preparations and exact runtime state; current batching refuses and erases the command's state and publications. The retained [Error conflict](../packages/bpmn-source/test/subprocess-error-lowering.test.ts) and [delayed-pair/End witnesses](../packages/bpmn-source/test/embedded-subprocess-lowering.test.ts) distinguish actual closure from hypothetical interleavings. Admission widening reopens the argument; narrowing RC scope still requires semantic-checkpoint review.

The in-progress regional checkpoint groups directed closure agreement, complete preparation, preservation, and pair laws before production integration. [Call-instance closure](../BpmnSemantics/SemanticProcess/CallInstanceClosure.lean) shares the unchanged forward evaluator used by Call association and scope cancellation and proves saturation, leastness, and membership preservation under independent deletion with each graph's own record-count fuel. [Occurrence-region laws](../BpmnSemantics/SemanticProcess/InternalOccurrenceRegionLaws.lean) establish finite saturation, the least live parent/forward-Call closure under the existing exact graph check, and literal canonical-region equality after independent deletion. Complete preparation, aggregate preservation, and quantified regional pair commutation remain open.

[Scope ancestry laws](../BpmnSemantics/SemanticProcess/ScopeAncestryLaws.lean) connect cancellation's unchanged parent walker to a finite directed ancestry relation. Unique complete IDs and live parent references suffice to derive population-fuel adequacy, leastness, transitivity, and independent-deletion equality; cycles require no assumed acyclicity. Reversed chains, backward queries, distinct activations, and a disconnected cycle separate the account.

[Instance ancestry](../BpmnSemantics/SemanticProcess/InternalRegionalInstanceAncestry.lean) derives each live scope's unique parentless Process root and duplicate-free Call-instance seeds from executable predecessor validity. Static ancestor rank and actual Call reachability supply these facts without an assumed connectivity premise. [Removal agreement](../BpmnSemantics/SemanticProcess/InternalRegionalRemovalAgreement.lean) then proves exact equality between prepared-region membership and cancellation's subtree-plus-Calls mask or Call return's instance traversal. Actual cancellation scope/token filters equal the prepared-region filters; issuance laws preserve all eight activation collections, Process bindings, time, and prior End count. These quantified laws do not assume successor validity or equality.

[Activity removal agreement](../BpmnSemantics/SemanticProcess/InternalRegionalActivityRemoval.lean) binds actual Activity, attached Timer/Message, User Task, effect, incident, branch-selection, and event-race filtering to the prepared region. Owner and child-body liveness follow from the predecessor aggregate, so handlers outside the scope region are removed through the exact withdrawn Activity population. Controller and Activity-local data laws use that same population and retain the evaluator's separate called-instance test. [Removal validity](../BpmnSemantics/SemanticProcess/InternalRegionalRemovalValidity.lean) preserves exact live-owner censuses, the implemented identity bounds, and wait/hidden-record Program declarations for either cancellation disposition. [Retained Activity validity](../BpmnSemantics/SemanticProcess/InternalRegionalActivityValidity.lean) preserves exact task-body multiplicity, child-scope liveness, and tagged Timer/Message attachments using only predecessor Activity validity and attachment uniqueness. Symbolic competing-claimant discriminators show why ownership alone does not preserve a handler.

[Call cleanup agreement](../BpmnSemantics/SemanticProcess/InternalRegionalCallRemoval.lean) covers every RuntimeState field of the actual cleanup: live work uses the scope region, while its derived complete-instance projection handles controllers, local data, and Compensation history without assuming historical scope owners remain live. The selected Call-ID exclusion, both parent-context coordinates, all issuance records, hosting control, Process bindings, time, and End history retain their existing meaning. The actual quiescence predicate makes a successfully prepared completion region a singleton. [Compensation cancellation agreement](../BpmnSemantics/SemanticProcess/InternalRegionalCompensationRemoval.lean) completes cancellation's field binding with the live region plus the existing called-instance test, both parent-context coordinates, and trigger-owned handler withdrawal. A dead-owner discriminator separates that full mask from a live-region-only filter. Narrow Lean gates pass on 2026-09-20. Aggregate validity, complete preparation frames, and pair commutation remain open; these field laws do not admit new snapshot or Compensation compositions.

[Canonical removal order](../BpmnSemantics/SemanticProcess/InternalRegionalRemovalOrder.lean) preserves all 21 collection-order conjuncts for actual cancellation and Call cleanup, using [comparator composition](../BpmnSemantics/SemanticProcess/InternalRegionalOrderFacts.lean) to justify the new adjacent pairs exposed by filtering. A symbolic counterexample shows why adjacency alone is insufficient. Both removals also preserve Timer/Message attachment uniqueness and disjoint Activity-body claims.

[Controller validity](../BpmnSemantics/SemanticProcess/InternalRegionalControllerValidity.lean) preserves the complete SMI/PMI Activity lookup census for surviving cancellation controllers, SMI singleton ownership, and retained progress. Its SMI forward Program-binding law derives exact task and lifetime-Timer survival from predecessor Activity validity and handler uniqueness. [Sequential binding preservation](../BpmnSemantics/SemanticProcess/InternalRegionalSequentialBinding.lean) adds reverse operation-wide completeness under both cancellation dispositions: predecessor uniqueness, complete bindings, and equal finite populations establish an exhaustive controller/Activity/task/Timer correspondence whose members share actual retention. The no-owner case follows from predecessor emptiness.

[Call SMI binding preservation](../BpmnSemantics/SemanticProcess/InternalRegionalCallSequentialBinding.lean) reuses the finite correspondence law for actual called-instance cleanup. Predecessor binding aligns controller identity with Activity/task/Timer owner instances; the actual filters retain complete tuples together. The ownership-closure condition supplies the surviving forward references. Together these laws preserve both halves of complete SMI Program binding without assuming successor validity, selection, or retained-count equality.

[Parallel forward binding preservation](../BpmnSemantics/SemanticProcess/InternalRegionalParallelBinding.lean) derives exact pending-child and lifetime-Timer censuses for every surviving controller after cancellation or Call cleanup. It frames Process bindings and task/Activity/Timer high-water reads, including partially completed slot lists. Call cleanup additionally consumes predecessor wait Process/owner agreement: Lean's narrower PMI validator does not itself relate those identities. The symbolic three-slot discriminator rejects a lost pending wait through the actual validator, and any withdrawn identity claimant removes its controller.

[Parallel reverse binding preservation](../BpmnSemantics/SemanticProcess/InternalRegionalParallelReverseBinding.lean) completes the actual PMI validator for both cancellation dispositions and Call cleanup. The [weighted correspondence laws](../BpmnSemantics/SemanticProcess/InternalRegionalParallelCensus.lean) derive exhaustive controller/Activity/Timer and pending-child populations from predecessor binding and unique claims, then transport their actual retention. Completed slots contribute zero; controller identity and order survive filtering. Both complete wrappers consume predecessor wait Process/owner agreement because controller identity and Activity-owner filters must agree; this is an existing flow-node fact, not a conclusion of the narrower PMI validator. Call cleanup also uses predecessor ownership closure. The root narrow gate passes on 2026-09-20, without successor-validity or count premises.

At the 2026-09-20 prerequisite checkpoint, [TypeScript regional preparation](../packages/semantic-core/src/internal-transition-regional-preparation.ts) retained exact Return, Complete, Error, and Terminate selections, including bounded completion's parent-owned Activity/deadline withdrawal, before applying any transition. [Publication preparation](../packages/semantic-core/src/internal-transition-regional-publication.ts) derives token/scope deltas and terminal/instantaneous lifecycle templates from predecessor ownership and selected continuation facts. Execution re-derives and compares the complete artifact before reusing the existing operation. Snapshot declarations remain excluded; these helpers are not in production batch dispatch and have no independent checkpoint approval yet.

The 2026-09-20 [regional pair matrix](../packages/semantic-core/test/internal-transition-regional-pair.test.ts) checked all sixteen ordered family pairs, bounded completion, and all seven independent arming kinds, including complete preparation preservation, exact opposite-order state, accepted publication at nonzero indices, and retained activation counters. Dependency-omission mutations expose false independence against regional cancellation. Every tested prefix is runtime-valid and projectable; the constructed Programs are explicitly outside profile admission. The [containing-region cases](../packages/semantic-core/test/internal-transition-regional-removal.test.ts) add directed Call removal, parent-owned bounded deadlines, live child work, and descendant-omission refusal. The complete semantic-core gate passes all 1054 tests on 2026-09-20. Sixteen executed mutations that clear an outside Timer, Message, effect, or composed Activity population after each regional operation are rejected by the mixed matrix. This finite evidence does not close the remaining Lean obligations.

The [controller cancellation witnesses](../packages/semantic-core/test/internal-transition-regional-controller.test.ts) retain two activations of the same SMI operation in the same definition scope. Error and Terminate each remove one controller/Activity/task/Timer group and preserve the exact other binding and issuance counters. Every prefix passes runtime and projection checks; incomplete successor groups fail the binding oracle. The second scope is constructed outside raw entry's admitted topology, so this establishes neither source reachability nor a broader entry contract.

The [Call controller witnesses](../packages/semantic-core/test/internal-transition-regional-call-controller.test.ts) arm three SMI or PMI lifetimes in the caller and two called Process instances. The PMI case completes the caller's middle child before cleanup. Actual caller-selected cleanup removes both called groups and preserves the complete caller binding and all eight issuance collections. Wrong caller identities remove nothing; five injected PMI faults lose a pending child, controller, Timer, or completed slot, or retain removed controllers, and all fail the oracle. Live called work still refuses normal Return preparation, so these witnesses establish cleanup preservation rather than expanded Return capability.

The [parallel controller witnesses](../packages/semantic-core/test/internal-transition-regional-parallel-controller.test.ts) reuse the [two-activation fixture](../packages/semantic-core/test/internal-regional-controller-fixture.ts) and complete one middle child through the actual runtime. Error and Terminate remove three pending children in one activation while preserving two pending children and one completed slot in the other. Full binding, exact state, and all eight issuance collections are checked; losing a pending child, controller, Timer, or completed slot is detected by eight executed mutations. This finite evidence does not establish the quantified PMI binding theorem or source reachability.

Open composition gap `AOO-RETAINED-BODY-01`: changing the bounded-scope fixture's `ChildEnd` to a containing-scope Terminate yields a structurally valid, runtime-well-formed predecessor. The existing cancellation account withdraws the parent-owned Activity and deadline while retaining the child scope; its next bounded completion consequently refuses the absent pair. Current runtime well-formedness and open-set projection accept that successor because their record obligations do not require a missing bounded-body record to exist. This is outside registered profile admission and does not refute independent-pair commutation, but no generalized Terminate-to-completion liveness claim follows. Before admitting that composition, a separately reviewed amendment must resolve the retained-body lifetime against [`AOO-CANCEL-01`](ACTIVITY-OCCURRENCE-OWNERSHIP-SPEC.md#stable-semantic-rules); weakening bounded completion's missing-pair refusal is not an accepted resolution.

[Identity preservation](../BpmnSemantics/SemanticProcess/InternalRegionalIdentityValidity.lean) derives the four wait-family, Activity, and sequential-controller uniqueness predicates after actual cancellation or Call cleanup from predecessor uniqueness. Its sublist law preserves multiplicity; a symbolic duplicated-record witness refutes membership inclusion as a substitute. This does not establish identity issuance or the remaining aggregate conjuncts.

The independently approved [ownership-closure amendment](INTERNAL-REGIONAL-OWNERSHIP-CLOSURE-PROPOSAL.md) adds a preparation condition for retained references into selected deletion sets. The private TypeScript preparation checks predecessor-derived masks; [separating tests](../packages/semantic-core/test/internal-transition-regional-ownership.test.ts) retain valid/projectable predecessors whose raw Return/Complete loses an Activity body or whose raw Error/Terminate loses a retained race's Message/Timer member. Predicate-only states separately isolate direct tagged handlers and identity coordinates without claiming whole-state validity. Positive cases retain an unrelated Activity beside a removed one, permit whole-race removal, and preserve existing pairs.

On 2026-09-20 the tests reject thirteen executed mutations: omitted child body, either handler, either race member, Process instance, activation, retained root, closure recheck, blanket refusal, a one-record restriction, and either Activity/race removal-source exemption. Actual raw fields independently settle all six TypeScript masks in the [preparation tests](../packages/semantic-core/test/internal-transition-regional-preparation.test.ts).

The [Lean closure owner](../BpmnSemantics/SemanticProcess/InternalRegionalOwnershipClosure.lean) defines the same reference criterion, proves exact retained-target census preservation, and binds Call/cancellation masks to actual removal fields. Its deletion law preserves closure for fixed masks. Its monotonicity law also permits retained source populations to narrow and target retention to widen; re-derived cancellation masks require this weaker relation because removing an Activity can remove its claim on a deleted handler.

The [retained local-data correction](INTERNAL-REGIONAL-OWNERSHIP-CLOSURE-PROPOSAL.md#retained-local-data-correction) closes an omitted ownership edge under the existing Activity-data lifetime account. Root Complete previously accepted an orphan local scope; bounded Complete could withdraw its live Activity while retaining its local scope. Both targets now require every retained Activity-tagged local scope to have exactly one surviving predecessor owner. [Local-data retention](../BpmnSemantics/SemanticProcess/InternalRegionalLocalDataRetention.lean) and [selection integration](../BpmnSemantics/SemanticProcess/InternalRegionalSelectedRetention.lean) prove the seventh actual field mask, selected-step owner preservation, and a fixed-mask deletion frame using both operations' closure. Complete preparation rechecks the condition. The complete core gate passes 1067 tests on 2026-09-20, including identity/tag and disjoint-retention discriminators; narrow Lean consumers pass. Re-derived masks, insertion frames, and successor open-projection correspondence remain open. Raw operations, global validators, and profile admission are unchanged.

[Completion retention](../BpmnSemantics/SemanticProcess/InternalRegionalCompletionRetention.lean) binds ordinary root/child scope removal and bounded Activity/deadline withdrawal to the actual Lean evaluators. Predecessor Timer uniqueness justifies expressing the evaluator's single-record erase as a retention filter and as complete Timer-key withdrawal. The exact child-body selector plus Activity identity uniqueness likewise equates body-based and identity-based Activity removal. These laws settle the mask comparisons for selected records; they do not equate the targets' complete selection domains.

[Reference preservation](../BpmnSemantics/SemanticProcess/InternalRegionalReferencePreservation.lean) derives retained Activity-body, task-owner, tagged-handler, and event-race association predicates from predecessor validity, ownership closure, and actual reference-field agreement. Complete matching lists preserve multiplicity; sublist uniqueness preserves all three race association censuses.

[Incident preservation](../BpmnSemantics/SemanticProcess/InternalRegionalIncidentValidity.lean) derives the complete incident-association predicate after actual Call cleanup and both cancellation dispositions from predecessor incident validity alone. A survivor retains its exact scope and local-data censuses; the incident's identity/owner agreement joins the Process and scope filters, the tagged local-data owner excludes Activity-owned deletion, and the existing singleton incident rule excludes a second cancelled claimant. Removed incidents impose no retained-reference obligation, and deleting other open effects preserves same-occurrence exclusion. A symbolic discriminator rejects a missing local scope through the actual validator; the narrow Lean gate passes on 2026-09-20.

The [incident removal witnesses](../packages/semantic-core/test/internal-transition-regional-incident.test.ts) check outside-incident preservation for Return/Complete/Error/Terminate and contained-incident removal for Error/Terminate, including exact local data, open-effect exclusion, projection, Process data, and all eight issuance collections. Six runtime-valid constructed cases reject sixteen executed faults affecting local-data multiplicity, reopened effects, or retained cancelled records. The incident state is deliberately constructed after actual effect arming; the wider Program does not admit incident reporting or a source profile.

[Child-cancellation position preservation](../BpmnSemantics/SemanticProcess/InternalRegionalPositionValidity.lean) proves the complete position predicate for both actual cancellation dispositions from predecessor position validity and a live nonparentless selected scope. Every valid Call endpoint is parentless, so child cancellation seeds no called instance and preserves the exact Call graph and root census. Static forest rank excludes the selected child's parent from its subtree; forward parent closure protects every other surviving parent, and token filtering preserves exact owner censuses. The [position frame](../BpmnSemantics/SemanticProcess/RuntimePositionValidity.lean) composes those derived facts without assuming successor validity. Four [runtime witnesses](../packages/semantic-core/test/internal-transition-regional-position.test.ts) retain independent Call or child work and reject missing roots, broken parent links, orphaned tokens, duplicate scopes, and missing Call records. The narrow Lean gate passes on 2026-09-20. Arbitrary root deletion fails the position oracle, and these constructed Programs establish no profile reachability.

[Call-tree position preservation](../BpmnSemantics/SemanticProcess/InternalRegionalCallPositionValidity.lean) derives the full Call association validator and complete position predicate after actual removal of a present Call's forward tree. A forward-closed deletion set preserves each outside target's incoming edge and reachability with the retained graph's own fuel. The actual selected-ID condition cannot delete an edge with a different target; retained singleton Call-anchor censuses, hosting-root exclusion, and owner Process binding supply the remaining facts. The shared [forest-removal frame](../BpmnSemantics/SemanticProcess/RuntimePositionValidity.lean) composes those facts while allowing removed called roots; child cancellation remains its unchanged-Call specialization. The narrow Lean gate passes on 2026-09-20. Two [nested Call witnesses](../packages/semantic-core/test/internal-transition-regional-call-position.test.ts) remove either branch's two nested Calls and retain the other's complete three-edge path; missing links, duplicate Calls, and dangling removed references fail the independent association or position oracle. Their constructed runtime-valid graphs do not claim source or invocation reachability. Integration into complete regional preparation remains separate.

[Terminate position preservation](../BpmnSemantics/SemanticProcess/InternalRegionalRootTerminationValidity.lean) proves the complete position predicate after every successful actual Terminate selection. Predecessor Call reachability and scope ancestry establish that hosting-root cancellation covers every live scope and transitively called Process; its actual result retains exactly the hosting root, no tokens, and no Call records. Child selections reuse child-cancellation preservation, and the End-history increment preserves the position fields. The selector's running-instance condition excludes called roots, whose raw retained cancellation would orphan them. The narrow Lean gate passes on 2026-09-20 without successor-validity or coverage premises. Preparation-derived continuation bindings and aggregate preservation remain open; this position result adds no profile admission or bounded-Terminate completion claim.

[Return continuation preservation](../BpmnSemantics/SemanticProcess/InternalRegionalReturnPositionValidity.lean) proves complete position after actual successful Return from valid running position, a uniquely declared output, and its static ownership binding to the selected caller. Parentless endpoint censuses lift selected-root quiescence to every caller in that Process instance, so the actual Call traversal contains only its seed and cannot remove the caller. Cleanup preservation and token insertion then compose without assuming intermediate validity or caller survival. The [admission bridge](../BpmnSemantics/SemanticProcess/InternalRegionalContinuationAdmission.lean) removes the extra static binding premises for a declared Return: predecessor structural Call validity supplies an invocation owned by the recorded caller, the [paired Call account](../BpmnSemantics/SemanticProcess/CallActivityAdmission.lean) binds its unique Return output to that scope, and complete [graph ownership](../BpmnSemantics/SemanticProcess/GraphValidation.lean) supplies exact declaration and binding censuses. The narrow Lean gate passes on 2026-09-20. Integration into complete regional preparation remains open.

[Completion position preservation](../BpmnSemantics/SemanticProcess/InternalRegionalCompletionPositionValidity.lean) proves actual ordinary and bounded child completion with a uniquely declared parent-owned output, and actual hosting-root completion with the selected root bound to the hosting instance. Quiescence excludes child edges and owned tokens. Predecessor ancestry and Call coverage then establish that a quiescent hosting root is the only live scope and that tokens and Calls are empty. The [selected-update law](../BpmnSemantics/SemanticProcess/ScopeCompletion.lean) exposes the unchanged evaluator's root and child rewrites; bounded completion's actual Activity/deadline withdrawal preserves their position fields and missing-pair refusal. Narrow Lean gates pass on 2026-09-20. Three [continuation witnesses](../packages/semantic-core/test/internal-transition-regional-continuation-position.test.ts) reject foreign-scope or undeclared output tokens and substituted called-root completion through position projection; the separate defect-list validator intentionally excludes token binding. The containing-region witnesses additionally execute hosting-root completion after Terminate. These constructed cases do not admit a profile, and complete preparation integration and aggregate preservation remain open.

The [completion admission bridge](../BpmnSemantics/SemanticProcess/InternalRegionalCompletionAdmission.lean) derives those output and hosting-identity facts for actual declared ordinary and bounded Complete operations from predecessor position and structural open-occurrence validity. Graph ownership joins the live child's static parent to its output declaration. The [scope lifecycle account](../BpmnSemantics/SemanticProcess/CompensationEventSubProcessSnapshotDeclaration.lean) permits a parentless Complete only at the entry scope, while paired Call admission and the structural Call census exclude a called occurrence using that hosting definition. The exact runtime selector supplies the scope census; no successor validity, surviving-parent assertion, or assumed hosting identity enters the wrapper theorem. A quantified discriminator rejects every nonhosting parentless Complete through the actual lifecycle validator. The narrow Lean gate passes on 2026-09-20; full regional preparation, aggregate preservation, and commutation remain open.

[Error continuation preservation](../BpmnSemantics/SemanticProcess/InternalRegionalErrorPositionValidity.lean) proves complete position after actual successful declared Error from valid running position. The raw selector supplies the exact child and parent. Static parent rank excludes that parent from child cancellation, and valid Call endpoints make the child's called-instance deletion closure empty. Checked graph ownership then binds the handler output to that same parent's definition scope and supplies exact declaration and binding censuses. The wrapper assumes neither successor validity nor continuation ownership or parent survival. The narrow Lean gate and two additional [Error witnesses](../packages/semantic-core/test/internal-transition-regional-continuation-position.test.ts) pass on 2026-09-20; they reject foreign or undeclared continuation places and wrong handler attachment while preserving valid parent continuations. Complete regional preparation, aggregate preservation, and commutation remain open.

[Bounded withdrawal selection](../BpmnSemantics/SemanticProcess/InternalRegionalCompletionSelection.lean) retains a unique bounded declaration, exact child/parent, unique child-body Activity, and exactly one tagged Timer with a unique live identity before owner filtering. Quantified positive and ambiguous-declaration/Timer discriminators accompany a proof that accepted selection supplies the unchanged raw helper's lookups. Selection composes with actual ordinary completion to derive the bounded result and its complete position validity from the declared-completion law; no additional state-writing helper is introduced. The narrow Lean gate and [TypeScript selection discriminators](../packages/semantic-core/test/internal-transition-regional-preparation.test.ts) pass on 2026-09-20. The [declaration-binding laws](../BpmnSemantics/SemanticProcess/InternalRegionalCompletionBinding.lean) recover the unique operation from a validated Timer declaration, and independently recover its identity from a declared output's sole producer. This discharges the declaration-identity prerequisite without adding the Activity-to-Program restriction excluded by the approved ownership contract. Full cross-target preparation integration and equality of raw refusal domains remain unproved; neither representation, raw evaluator, nor source admission changes.

[Lean regional selection](../BpmnSemantics/SemanticProcess/InternalRegionalSelection.lean) now retains Return, Complete, Error, and Terminate predecessor choices without executing a transition. It proves original-operation retention, running control, actual evaluator availability under snapshot exclusion, and complete position preservation for a declared operation from predecessor position and structural occurrence validity. Positive laws preserve running raw Return, ordinary completion with exact bounded withdrawal, matching Error, and existing Terminate selection. Quantified negatives require exactly one Error input token and one parent occurrence; the new preparation checks match TypeScript's existing counts while the raw Lean Error helper remains unchanged. Terminate retains the Lean evaluator's existing profile and declaration restrictions. The narrow Lean gate passes on 2026-09-20. These are selection and position laws, not complete preparation, cross-target domain equality, aggregate preservation, or pair commutation.

[Selected ownership closure](../BpmnSemantics/SemanticProcess/InternalRegionalSelectedRetention.lean) connects all four retained choices to the independently defined reference masks. A quantified discriminator separates Terminate's retained root from Error's removed root. The masks agree with the actual evaluator's six reference collections, including bounded completion's Activity and Timer withdrawal; predecessor wait-identity uniqueness justifies expressing single Timer erasure as a filter. The ownership-closed selection stage refuses stranded references and preserves every closed selection. Its proof derives retained Activity-body/handler liveness and complete event-race associations from their predecessor predicates, then combines them with declared position preservation for the same evaluator result. The narrow Lean gate passes on 2026-09-20. These facts require no successor validity or assumed mask agreement. Full preparation/publication integration, selected-domain equality, remaining aggregate predicates, and preparation frames remain open.

[Regional dependencies](../BpmnSemantics/SemanticProcess/InternalRegionalDependencies.lean) extend the existing atom type through a wrapper, following the measured [snapshot resource boundary](CAPSULE-COST-LEDGER.md#compensation-snapshot-resource-ceiling-incident). Quantified checks protect parent insertion, owned waits, shared Activity identities, task bodies, tagged handlers, and legacy data-arming keys. They preserve legacy key conflicts and global activation counters while allowing shared Process reads and relative End increments to commute. [Predecessor footprints](../BpmnSemantics/SemanticProcess/InternalRegionalFootprint.lean) cover every selected Return/Complete/Error/Terminate branch, continuation and removed-token/selection censuses, bounded deadlines, and every actually withdrawn Activity association. Symbolic red/green checks reject omitted parent dependencies, accidental End-increment conflicts, missing Activity writes, and missed legacy Activity aliases; the narrow Lean gate passes on 2026-09-20. Complete preparation binds their region argument to the exact predecessor-derived region; commutation remains unproved.

[Assembled regional preparation](../BpmnSemantics/SemanticProcess/InternalRegionalPreparation.lean) binds exact operation declaration, snapshot exclusion, safe logical time, ownership-closed selection, derived region, footprint, and predecessor [publication](../BpmnSemantics/SemanticProcess/InternalRegionalPublication.lean). Execution rechecks the complete preparation before invoking the unchanged evaluator. Quantified red/green checks reject an altered region; the same guard rejects altered footprints and publication, while successful preparation proves actual execution availability. Publication construction retains positions, open anchors, and instantaneous identities without observing a successor or assigning command/index numbers. Cancellation checks preserve Terminate's root and outside waits while cancelling Error's root and owned waits. The assembled preservation theorem derives predecessor position from successful publication projection and combines exact declaration with the selected removal laws to preserve position, Activity liveness, and race associations. The narrow Lean gate passes on 2026-09-20. Actual accepted-publication correspondence, cross-target selected-domain agreement, complete preparation frames, remaining aggregate predicates, and pair commutation remain open.

[Open projection ownership](../BpmnSemantics/SemanticProcess/InternalRegionalOpenOwnership.lean) derives live owners and exact scope-parent edges for every projected occurrence. [Lifecycle correspondence](../BpmnSemantics/SemanticProcess/InternalRegionalLifecycleCorrespondence.lean) uses those facts and region closure to equate the region classifier with the evaluator's subtree-plus-Calls classifier, including the retained-root exception. Successful complete preparation supplies all premises for exact equality with the actual Error and Terminate cancellation-end selectors; no successor projection or assumed mask equality enters that theorem. The narrow Lean gate passes on 2026-09-20. Complete lifecycle acceptance and pair publication remain open. Compiling the lifecycle consumer also exposed a pending namespace collision: the newly public [graph key-census lemma](../BpmnSemantics/SemanticProcess/GraphValidation.lean) shared an existing lifecycle lemma's name but had a different signature. Renaming the new lemma and its new callers restores that consumer without changing either proof or runtime behavior.

[Lifecycle selection](../BpmnSemantics/SemanticProcess/InternalRegionalLifecycleSelection.lean) proves that all four regional selections supply the actual lifecycle selector's owner and exact Call/scope/parent lookup facts. It composes them with cancellation correspondence to prove literal equality of the complete lifecycle candidate and retained template for every command ID, transition index, and successor argument. Return/Complete endings and Error/Terminate instantaneous identities are included; the arbitrary successor argument establishes that candidate construction uses only the predecessor. A successful complete preparation supplies every premise, and its exact declaration and owner also determine the actual transition record's operation ID, kind, origin, and owner. The narrow Lean gate passes on 2026-09-20. Candidate equality does not establish acceptance: folding the delta must still equal the actual successor's independent open projection.

[Lifecycle fold](../BpmnSemantics/SemanticProcess/InternalRegionalLifecycleFold.lean) derives acceptance of Return/Complete templates by the actual fold from successful complete preparation alone, for every command ID and transition index. The retained selection supplies unique ending anchors and their predecessor membership; the independent predecessor projection supplies unique regular anchors and canonical order. Sorting endings preserves their permutation, so the fold returns exactly the predecessor with those anchors removed. The narrow Lean gate passes on 2026-09-20.

[Instantaneous lifecycle fold](../BpmnSemantics/SemanticProcess/InternalRegionalInstantaneousFold.lean) extends that result to all four regional families. The generic fold law accepts arbitrary unique transient starts that close in the same delta, proves their separation from regular predecessor anchors, and removes exactly the retained endings. The actual numbering proof covers the complete regional surface of zero, one, or two instantaneous identities, including both identity-sort orders and equal identities; distinct local indices provide freshness. Successful preparation derives the identity bound, ending census, and exact template, so every regional fold succeeds for arbitrary command IDs and transition indices without an acceptance premise. This proves fold acceptance; the accepted-publication results below separately establish actual successor correspondence. The narrow Lean gate passes on 2026-09-20.

[Regional position algebra](../BpmnSemantics/SemanticProcess/InternalRegionalPositionAlgebra.lean) proves that owner filtering commutes with token projection and that deletion publishes the full multiplicity of each removed bucket, with no produced token. The insertion lemma requires strict canonical order, which the existing projector supplies; arbitrary unordered lists do not satisfy that law. A quantified repeated-unit discriminator retains a consumed count of two. The deletion-plus-continuation law handles an output outside the removed region even when its bucket already contains tokens; it publishes only the added multiplicity. Exact projection invariance under token permutation binds that law to the evaluator's existing `addToken`, without an empty-output premise. [Scope projection](../BpmnSemantics/SemanticProcess/ScopePositionInsertion.lean) proves exact retained payload membership and removed/entered differences without assuming an order for the raw scope list.

[Actual Terminate publication](../BpmnSemantics/SemanticProcess/InternalRegionalTerminationPositionPublication.lean) combines these laws with the selected evaluator's removal fields and derived successor position validity to produce exactly the region's consumed tokens and exited scopes, retaining the selected scope and producing neither tokens nor scopes. Its complete-preparation theorem derives actual execution and equality with the retained position template from successful preparation alone; selected ownership, running control, snapshot exclusion, and predecessor projection are extracted rather than assumed separately. The narrow Lean gate passes on 2026-09-20.

[Actual Error publication](../BpmnSemantics/SemanticProcess/InternalRegionalErrorPositionPublication.lean) derives parent survival from static scope rank and the empty called-instance closure of a child, then applies the continuation law to actual cancellation and token insertion. Complete preparation alone determines the evaluator's selected child and parent, the exact output declaration and public origin, successor position validity, and equality with the retained position template. The output bucket may already contain tokens: the delta still produces exactly one unit while consuming all region tokens and exiting exactly its scopes. The narrow Lean gate passes on 2026-09-20.

[Actual Return publication](../BpmnSemantics/SemanticProcess/InternalRegionalReturnPositionPublication.lean) connects the actual selected Call cleanup to the prepared region using predecessor position validity. Quiescence and Call endpoint ownership exclude the caller from removal. Successful complete preparation supplies the exact Call and root census, output declaration and public origin, actual execution, and successor validity; their composition proves literal equality with the retained position template, including populated caller output buckets. The narrow Lean gate passes on 2026-09-20.

[Actual Complete publication](../BpmnSemantics/SemanticProcess/InternalRegionalCompletionPositionPublication.lean) derives root completion's empty predecessor tokens from quiescence and hosting ancestry, and child completion's one-unit continuation and exact exited scope from the actual update. Bounded Activity/deadline withdrawal preserves these position fields. Successful complete preparation supplies structural validity through its independent predecessor open projection, the declared operation and selected scope census, actual execution, successor position validity, and the output's exact public origin. [Composed regional publication](../BpmnSemantics/SemanticProcess/InternalRegionalPositionPublication.lean) closes position correspondence for all four families from successful preparation alone, with no successor-validity or delta-equality premise. The narrow Lean gate passes on 2026-09-20. The individual lifecycle-acceptance results below compose with this position account; pair publication remains open.

[Root-completion projection](../BpmnSemantics/SemanticProcess/InternalRegionalCompletionOpenProjection.lean) closes actual accepted lifecycle publication for Complete without a parent continuation. Successful complete preparation and predecessor runtime validity derive the hosting root, singleton scope population, exact raw execution, and empty predecessor/successor open projections. [Quiescence laws](../BpmnSemantics/SemanticProcess/InternalRegionalQuiescence.lean) establish emptiness of all eight live-owner collections and exclude retained Activities through body liveness plus ownership closure; a quantified child-root discriminator shows why liveness alone is insufficient. The existing Effect-local reverse census and the new Activity-local closure then exclude remaining local scopes. The actual candidate, fold, and independent successor projection agree at arbitrary command IDs and transition indices. No successor-validity, execution-emptiness, or publication-acceptance premise is assumed; historical and issuance records may remain. The narrow Lean gate passes on 2026-09-20. [Error/Terminate accepted publication](#error-and-terminate-accepted-publication) follows below; complete pair publication remains open.

[Projection removal](../BpmnSemantics/SemanticProcess/InternalRegionalProjectionRemoval.lean) transports successful optional projection through independent source/output filters without losing multiplicity; a quantified duplicate witness rejects set-based collapse. The actual lifecycle sort commutes with filtering through its proved strict-order permutation invariance, and canonical wait/Scope/Call concatenation removes exactly the specified ending anchors. [Child-completion frames](../BpmnSemantics/SemanticProcess/InternalRegionalProjectionFrames.lean) derive the actual evaluator's scope removal, unchanged control/Calls, and quiescence from complete preparation. They preserve surviving owner/Process lookups, all three start constructors, and the actual structural Program validator. The composed prepared-step theorem projects exactly the surviving Scope and Call entries by complete scope anchor, including bounded deadline withdrawal; it assumes only the original structural validity and successful component projections. The narrow Lean gate passes on 2026-09-20. The complete child projection and acceptance theorem below composes these results.

[Child wait validity](../BpmnSemantics/SemanticProcess/InternalRegionalChildWaitValidity.lean) derives preservation of the actual User Task and Effect validators from complete preparation, predecessor validity, and wait-identity uniqueness. Quiescence protects surviving owners; the selected evaluator retains tasks, effects, incidents, and local data. [Child Timer binding](../BpmnSemantics/SemanticProcess/InternalRegionalChildTimerBinding.lean) identifies the selected deadline through the Program's unique declarer and proves it was already private. Exact Activity body selection and its singleton tagged attachment exclude every surviving Timer from the removed Activity, without relating independent activation counters. [Child Timer frames](../BpmnSemantics/SemanticProcess/InternalRegionalChildTimerFrames.lean) preserve the complete surviving host census and private/public classification through actual prepared execution. Scope-host filtering follows the record's full child identity; Task and Multi-Instance host censuses reject a removed child-scope body. The narrow targets pass on 2026-09-20, including actual Complete/Return accepted-publication consumers. Complete selected-domain agreement remains open.

[Complete child wait preservation](../BpmnSemantics/SemanticProcess/InternalRegionalChildWaitPreservation.lean) closes every wait-family validator and the composite Program predicate, including retained selected-branch and event-race owners. [Projection association validity](../BpmnSemantics/SemanticProcess/InternalRegionalChildProjectionValidity.lean) preserves Call associations through unchanged parentless roots, incident associations through exact retained owners/local data, event-race associations through regional reference closure, and the actual Message-boundary pairing predicate. [Exact wait projection](../BpmnSemantics/SemanticProcess/InternalRegionalChildWaitProjection.lean) preserves the complete ordered optional wait projection, including failed lookup results, because completion removes only an already-private Timer and retains every surviving lookup. All three narrow Lean targets pass on 2026-09-20. The named successor predicates and exact wait projection are conclusions, not preparation premises. The child acceptance theorem below composes them with Scope/Call filtering.

[Child-completion accepted publication](../BpmnSemantics/SemanticProcess/InternalRegionalChildOpenProjection.lean) composes the actual successor's full open projection, deleting exactly the selected scope anchor and preserving all other payloads, order, and multiplicity. Anchor uniqueness comes from the predecessor projection and filtering, not a successor premise. The selected publication template supplies exactly that scope ending, so its lifecycle fold agrees with independent successor projection and the actual acceptor succeeds at every command ID and transition index. The theorem assumes Program well-formedness, predecessor wait-identity uniqueness, and successful complete preparation; it derives the open predecessor from preparation and does not assume runtime preservation or publication acceptance. The narrow Lean gate passes on 2026-09-20. Root and child Complete publication are now proved; cross-operation preparation, pair commutation, and the remaining regional families stay open.

[Return projection frames](../BpmnSemantics/SemanticProcess/InternalRegionalReturnProjectionFrames.lean) preserve complete Process lookup and wait/Scope/Call start results for owners outside the selected called-instance closure. The incoming Call census is preserved by target Process, including every matching record; the existing association invariant rules out an equal selected Call identity with a different target. These are equalities of optional results, including failed lookups, and require no successor validity. The narrow Lean gate passes on 2026-09-20.

[Return quiescence](../BpmnSemantics/SemanticProcess/InternalRegionalReturnQuiescence.lean) derives the actual successor's cleanup and continuation fields from complete preparation and predecessor runtime validity. The selected region contains only the parentless called root, so cleanup preserves every token, wait, incident, branch selection, and event race with its order and multiplicity; Return then adds the caller's continuation token. Scope and Activity records retain their explicit root-identity and owner-removal filters. Activity emptiness and unchanged local data are not assumed. The narrow Lean gate passes on 2026-09-20.

[Return Effect preservation](../BpmnSemantics/SemanticProcess/InternalRegionalReturnEffectValidity.lean) derives exact Effect-local retention from actual wait/incident survival and complete tagged identity. Filtering local data preserves both directions of the existing Effect bijection without assuming that Activity-tagged data survives. [Wait validity](../BpmnSemantics/SemanticProcess/InternalRegionalReturnWaitValidity.lean) preserves every existing wait-family predicate and complete boundary-Timer candidate census: removed Activities cannot match surviving wait owners, and the parentless removed root cannot match a bounded-scope Timer host. [Wait projection](../BpmnSemantics/SemanticProcess/InternalRegionalReturnWaitProjection.lean) then proves exact equality of the actual successor's optional wait projection, including failed lookups, order, multiplicity, and private-Timer exclusion. Complete preparation and predecessor runtime validity supply the cleanup/projection premises; wait-validity preservation additionally assumes the predecessor wait predicate. The narrow Lean gate passes on 2026-09-20.

[Return projection validity](../BpmnSemantics/SemanticProcess/InternalRegionalReturnProjectionValidity.lean) preserves the complete Program and Call/incident/race/Message-boundary predicates from their predecessor accounts. The structural law covers actual Call-tree cleanup generally, preserving exact incoming-Call and live-parent censuses. The shared Message-boundary lemma now derives each selected Activity's Task body and owner from the independent reverse record census under the `MBP-EMPTY-TASK-01` correction below; Complete and Return need no assumed first Task.

[Return Scope/Call projection](../BpmnSemantics/SemanticProcess/InternalRegionalReturnScopeCallProjection.lean) preserves the complete optional child-scope projection and deletes exactly the selected Call anchor. Quiescence restricts actual Call traversal to its seed; called-instance identity injection excludes removal of a different Call anchor, while parent-instance binding preserves every surviving scope lookup. [Accepted Return publication](../BpmnSemantics/SemanticProcess/InternalRegionalReturnOpenProjection.lean) composes these laws with wait projection and every independent guard. Program well-formedness, predecessor runtime validity, and successful complete preparation suffice: the predecessor open set comes from preparation, canonical filtering preserves anchor uniqueness, and the actual successor projection agrees with the retained lifecycle fold at every command ID and transition index. No successor-validity, projection-equality, or acceptance premise is assumed. The narrow Lean gate passes on 2026-09-20, including the shared Complete consumers. [Error/Terminate accepted publication](#error-and-terminate-accepted-publication) follows below; cross-operation preparation, aggregate preservation, and pair commutation remain open.

[Cancellation projection frames](../BpmnSemantics/SemanticProcess/InternalRegionalCancellationProjectionFrames.lean) preserve the full optional Process lookup outside a cancelled child region under either retain/remove disposition. The actual scope filter preserves the complete owner census, including unsuccessful lookups, and the current parentless Call endpoint account retains every Call record. Wait and Scope start frames follow; the complete optional Call projection is unchanged. These laws do not assume successor validity or establish full open projection. The narrow Lean gate passes on 2026-09-20.

[Cancellation Timer frames](../BpmnSemantics/SemanticProcess/InternalRegionalCancellationTimerFrames.lean) preserve every surviving Timer's complete Activity-host census and private/public classification under either root disposition, including duplicate and failed censuses. Actual attachment withdrawal forces every claimant of a surviving Timer to survive; Task and child-scope body censuses then remain exact. [Wait preservation](../BpmnSemantics/SemanticProcess/InternalRegionalCancellationWaitValidity.lean) frames surviving owners and Event Race lookups for every wait family. [Effect preservation](../BpmnSemantics/SemanticProcess/InternalRegionalCancellationEffectValidity.lean) derives both directions of local-data retention: the predecessor projection's singleton Effect identity excludes cancelled aliases, and incident association binds the cleanup identity to its suspended wait. The composed complete wait validator requires only predecessor wait validity and incident association, with no successor-validity premise. Narrow Lean gates pass on 2026-09-20. [Error/Terminate accepted publication](#error-and-terminate-accepted-publication) below composes exact published-list removal with the remaining full-projection guards.

The shared [Boundary Timer matcher](../BpmnSemantics/SemanticProcess/FlowNodeOccurrenceWaitProgramValidity.lean) is available through the existing internal proof namespace. Cancellation, Complete, and Return each prove operation-level matching preservation once; classification and wait validity reuse that result. This removes their repeated five-family host analyses while preserving every existing theorem statement and the matcher body. It changes proof composition, not the selected assurance boundary. The complete gate exposed a heartbeat failure in the [data-arming consumer](../BpmnSemantics/SemanticProcess/InternalDataArmingFrames.lean); explicit operation-level matcher equality replaces implicit whole-state conversion, preserving its theorem statements and passing the unchanged limit on 2026-09-20.

`BTP-LEGACY-HOST-01` was reproduced in [Lean's Boundary Timer matcher](../BpmnSemantics/SemanticProcess/FlowNodeOccurrenceWaitProgramValidity.lean). Its bounded-scope arm joined a live child by parent and activation without requiring an Activity record or its tagged Timer attachment. Actual cancellation withdraws a parent-owned deadline through that attachment, so the old matcher admitted states whose cancellation stranded the deadline. The correction replaces that join and the analogous ordinary/monitored Task joins with exact Activity body/attachment censuses. It changes no global runtime predicate, raw removal, source capability, or Multi-Instance account.

The [scope/Error kernel witnesses](../BpmnSemantics/InternalRegionalCancellationProjectionConformance.lean) independently remove all Activity records or only their attachments from an armed bounded scope. The pre-correction run accepted both predecessors and complete Error preparations, then produced unprojectable successors under a structurally valid mixed Program. After correction, the same mutations retain their otherwise-valid runtime state but refuse projection and preparation. The intact Error step remains projectable; an exact Timer attachment with a divergent activation preserves the original open set. All nineteen kernel assertions pass on 2026-09-20. The four [Task witness theorems](../BpmnSemantics/BoundaryTimerProjectionConformance.lean) also pass for both ordinary/monitored arms, and the [nine TypeScript controls](../packages/semantic-core/test/internal-regional-timer-projection.test.ts) independently cover all three families. These constructed states establish neither registered-profile reachability nor a CIB deviation.

The governing rule already exists: [`AOO-JOIN-03`](ACTIVITY-OCCURRENCE-OWNERSHIP-SPEC.md#stable-semantic-rules) prohibits cross-family activation joins in either language. The [join guard](../scripts/activity-occurrence-join.test.ts) omitted the Lean projection owner. Extending its census first reproduced both offending source sites; all four guard checks pass after correction. Standalone catches and empty-handler monitored Activities remain permitted. Scope-insertion and Complete/Return proof consumers are green, and the complete core gate passes 1076 tests on 2026-09-20. Resource calibration, complete integration, and independent review remain obligations of the existing regional semantic checkpoint before dependent batching integration; this correction creates no helper commit or separate semantic account.

`MBP-EMPTY-TASK-01` is reproduced on 2026-09-20 through the complete Lean runtime validator and open projector. Starting with the registered Message-boundary fixture's armed state, removing both Tasks and Activities leaves an orphan Message; removing Tasks and substituting the live hosting scope as the Activity body creates a second malformed state. Both retain runtime well-formedness and previously projected the Message. [TypeScript regressions](../packages/semantic-core/test/activity-boundary-message-occurrence.test.ts) accept the same narrow runtime validity but reject both projections. These are constructed malformed states under an admitted Program, not execution-reachability witnesses.

The root cause is lambda scope in [the Lean validator](../BpmnSemantics/SemanticProcess/FlowNodeOccurrenceLifecycle.lean): Message and Activity checks were inside the Task universal, so empty Tasks bypassed both. Explicitly parenthesizing all three population checks restores the existing [atomic ownership contract](capsules/ACTIVITY-BOUNDARY-MESSAGE-SPEC.md#stable-semantic-rules). The [general empty-population law](../BpmnSemantics/SemanticProcess/InternalRegionalChildProjectionValidity.lean) derives empty selected Messages and Activities from empty selected Tasks and successful validation for arbitrary Programs and states. Complete/Return preservation uses the independent reverse record census and no longer assumes an existing Task. The same complete Lean probes now reject both malformed projections; valid arming and both victory fixtures remain kernel-checked. This corrects malformed-state publication without changing raw transitions or profile capabilities. The [resource record](CAPSULE-COST-LEDGER.md#message-boundary-projection-correction-2026-09-20) and [correction receipt](#independent-cold-review-receipt) own the bounded measurement, independent approval, and complete integration evidence.

`RHP-HANDLER-01` reproduces a publication gap on 2026-09-20 using the [retention witness's](../packages/semantic-core/test/internal-transition-regional-retention-frame.test.ts) cross-region Activity shape. Both runtime states are valid and projectable, and preparation succeeds, but cancelling the child-body region removes the other owner's handler while regional templates omit its ending; actual lifecycle projection refuses. Eight TypeScript Error/Terminate × Message/Timer cases and two complete Lean Error probes reproduced the omission before correction. These constructed states establish no execution-reachability claim; the TypeScript Programs fail profile admission.

Publication follows predecessor Activity withdrawal and the live handler family in [Lean](../BpmnSemantics/SemanticProcess/FlowNodeOccurrenceWaitProgramValidity.lean) and [TypeScript](../packages/semantic-core/src/flow-node-occurrence-lifecycle.ts). The private-Timer alias correction below additionally classifies handlers before matching already-open anchors. The [state-free cancellation relation](../packages/semantic-core/src/flow-node-occurrence-publication-external-completeness.ts) uses retained body-handler associations, including a Terminate root whose anchor survives. Tests compare accepted actual publication with the retained template, retain an unrelated outside Task, and require the complete Timer E1 checker to reject an omitted ending. The current retained Message account still rejects child-scope hosts; its admission boundary is unchanged.

The quantified preparation-to-candidate and cancellation-correspondence theorems retain their original assumptions. The former owner-only `regionalCancellation_preserves_outside_wait` classifier law now additionally excludes attached-handler withdrawal, matching `AOO-CANCEL-01`; it never established raw wait survival. No runtime validator, raw transition, schema, or profile changes. Four kernel-decided Error/Terminate × Message/Timer witnesses prove exact accepted publication; a fifth preserves private-Timer withdrawal without a public ending. General Error/Terminate accepted-publication preservation and regional trial closure remain open.

The changed conformance target passed its cold-target, warm-dependency resource gate on 2026-09-20 in 102.70 seconds at 2,794,292 KiB RSS and 2,883,141,632 cgroup bytes under the unchanged 3 GiB/no-additional-swap, one-CPU/thread image. All memory-event counters and swaps were zero; `some` and `full` pressure totals were 51,810 microseconds each. The asserted receipt is `/tmp/bpmn-handler-publication-resource/cold-receipt-fixed`; [the cost row](../scripts/lean-module-cost.ts) binds its exact mounted source and receipt digests. All 513 archived source/build inputs match the implementation target; manifest SHA-256 is `67b01676237044ad97a05e92b83a52ef135534f53aeb243d7c6d8cb5cd3976fc`. The full 482-target Lean gate, 1087 semantic-core tests, semantic type checks, and 572 infrastructure checks pass. The [review receipt](#independent-cold-review-receipt) owns independent approval and clean committed integration.

The `AOO-JOIN-03` actual Timer-publication correction reproduces the residual ordinal join with Task/Timer activations `(2, 1)` and `(1, 2)`: both predecessors are well-formed and projectable, raw deadline admission commits, and accepted lifecycle publication returns `none`. The helper now reads the exact attached Activity record and its body identity, retaining the declared Task element and owner checks. [The census](../scripts/activity-occurrence-join.test.ts) now includes the omitted lifecycle owner; stored EventRace wait identities remain same-family comparisons. Its Lean exemption now matches the complete identity operand, so a legitimate comparison cannot hide a forbidden join on the same line; mixed-comparison controls reproduce that former hole. The census remains enumerated, and the separate TypeScript pattern retains its documented lexical limitation.

The [kernel witnesses](../BpmnSemantics/BoundaryTimerProjectionConformance.lean) use existing body replacement and a separately advanced attached deadline to require exact accepted cancellation of the record-named Task plus one instantaneous Boundary Event. The [TypeScript turnover control](../packages/semantic-core/test/activity-body-turnover.test.ts) independently checks actual traced publication. Neither witness admits ordinary Task repetition or establishes registered-schedule reachability. Raw transition writers and complete runtime-invariant preservation are unchanged; a projectable successor is not claimed to be well-formed. The full 482-target Lean gate, 1087-test core gate, and 572 infrastructure checks pass. The [review receipt](#independent-cold-review-receipt) owns independent approval and clean complete integration.

On 2026-09-21 the changed fixture passed the cold-target, warm-dependency 3 GiB gate in 12.64 seconds, at 1,218,336 KiB RSS and 892,489,728 cgroup bytes, with zero swaps, controlled memory events, and pressure totals. The one-CPU/thread, read-only, network-disabled conditions are unchanged. `/tmp/bpmn-timer-pairing-resource/inputs.json` binds 513 source/build inputs with SHA-256 `449623336cbb336ce28c8dd5dfdce489e43699d44e63d2a8ce44dd5935d409c9`; [the cost row](../scripts/lean-module-cost.ts) binds the exact target source and asserted `cold-receipt`. This is acceptance evidence, not a performance improvement claim.

A separate control on the original, unmodified armed state exposed an existing `AOO-BODY-01`/`AOO-ATTACH-01` preservation gap: [BoundedTask's `commitVictory`](../BpmnSemantics/SemanticProcess/BoundedTask.lean) withdrew Task and Timer waits but retained their Activity record. `/tmp/bpmn-timer-pairing-stale-record-control` reports `(beforeValid, afterValid, beforeRecords, afterRecords, afterTasks, afterTimers) = (true, false, 1, 1, 0, 0)` through raw deadline admission. Complete commands at `0ba40aef` reproduce invalid successors after both victories in `/tmp/bpmn-activity-retirement-command-control`. The sibling [monitored writer](../BpmnSemantics/SemanticProcess/MonitoredTask.lean) likewise retained the ended Activity on completion and its consumed attachment on firing; `/tmp/bpmn-activity-retirement-monitored-control-fixed` reproduces both and completion after firing. These defects predate the publication correction.

The bounded repair removes records by the exact ended Task body and filters only the consumed tagged Timer attachment on monitored firing. Other records, body claims, identities, and counters retain their existing account; no representation, profile, public route, or timing rule changes. Evaluator soundness and the original quantified transition premises remain intact, with the same record maintenance in their declarative successors. The writer census now classifies all eight added update sites and binds their identity/claim evidence. The strengthened [interrupting](../BpmnSemantics/ActivityBoundaryTimerConformance.lean) and [non-interrupting](../BpmnSemantics/NonInterruptingBoundaryTimerConformance.lean) fixtures require exact record lifetime and full runtime validity across the original complete commands. Separate constructed frame controls preserve another activation and a same-coordinate Message attachment; they establish no additional profile reachability. The [review receipt](#independent-cold-review-receipt) owns gate and approval evidence. Reassess before any substantial proof prerequisite; no general all-transition preservation theorem is claimed.

Both strengthened fixtures passed cold-target measurements on 2026-09-21 against the corrected warm dependencies, under the unchanged 3 GiB/no-additional-swap, one-CPU/thread, read-only, network-disabled controller. `ActivityBoundaryTimerConformance` passed in 16.38 seconds at 1,216,932 KiB RSS and 888,365,056 cgroup bytes; `NonInterruptingBoundaryTimerConformance` passed in 14.94 seconds at 1,238,276 KiB RSS and 900,255,744 cgroup bytes. Swaps, memory events, and pressure totals were zero. `/tmp/bpmn-activity-retirement-resource/inputs-fixed.json` binds all 513 source/build inputs with SHA-256 `c0366662c48c8400598759ae3b0427aabc85f71d4ab9f6cf8841729994cea43f`; [the cost rows](../scripts/lean-module-cost.ts) bind their measured source and successful `bounded-receipt-fixed`/`monitored-receipt`. The initial negative fixtures, one later fixture-syntax failure, and their snapshots remain retained. The full 482-target Lean gate, complete 1087-test core gate, and focused writer/census checks pass; the [review receipt](#independent-cold-review-receipt) owns independent approval and clean integration. These are resource acceptance measurements, not a like-for-like performance improvement.

The declaration comparison exposed a material projection correction in [Boundary Timer binding](../packages/semantic-core/src/flow-node-occurrence-open-set.ts): the Activity's `operationId` selected an operation whose output was checked, but Timer element, operation ownership, and the declared host were not all checked. Two bounded scopes reproduce both directions of a forged operation-key/output pair, and swapping their child bodies reproduces the host mismatch. The predecessor ownership and position validators accept those states; their narrower predicates do not justify projection. The common Timer-declaration check now validates element, output, and operation scope before family-specific host binding, with exact owner agreement and no activation-counter equality. All 1058 core tests pass on 2026-09-20. This corrects malformed-state projection under the existing contract, introduces no runtime writer or profile, and belongs to the pending regional semantic checkpoint review.

[Call retention frames](../BpmnSemantics/SemanticProcess/InternalRegionalCallFrames.lean) derive literal equality of all six masks and recheck ownership closure after actual removal of a disjoint called-instance tree. The actual association predicate excludes an equal-ID edge with a different target; each graph uses its own record-count fuel. [Cancellation-to-Call frames](../BpmnSemantics/SemanticProcess/InternalRegionalCancellationCallFrames.lean) derive the same mask and closure preservation under either cancellation disposition: disjoint prepared regions protect both endpoints of every reachable Call edge. Narrow Lean gates pass on 2026-09-20. Selected-domain cross-target agreement, remaining preparation frames, and checkpoint review remain open. Raw operation behavior and profile admission are unchanged.

[Cancellation retention frames](../BpmnSemantics/SemanticProcess/InternalRegionalCancellationFrames.lean) preserve the re-derived ownership condition after disjoint Call cleanup and every pair of cancellation dispositions. The owner classifier is equal for arbitrary historical IDs, while handler masks may widen. A retained parent-graph sandwich and exact Call-edge survival preserve reachability at each actual population's fuel bound; no successor-validity or left-closure premise is used. The root narrow gate passes on 2026-09-20; this does not establish complete preparation equality.

The [retention-frame regression](../packages/semantic-core/test/internal-transition-regional-retention-frame.test.ts) constructs an Activity owned in one region whose child body lies in another. Both cancellations remove that Activity. Eight Error/Terminate and Timer/Message cases preserve ownership closure while changing a deleted handler's retention mask. The cases exposed false independence in the pending preparation code: region atoms alone omitted the shared Activity write. Preparation now unions every actually selected Activity removal into its existing typed write set. Omitting those writes reproduces the defect, forged preparation refuses execution, and the independent pair matrix remains green. These constructed states pass runtime validity and projection but remain outside profile admission.

#### Error and Terminate accepted publication

[Cancellation acceptance](../BpmnSemantics/SemanticProcess/InternalRegionalCancellationAcceptance.lean) proves actual Error and Terminate lifecycle acceptance at arbitrary command IDs and transition indices. Successful complete preparation supplies the running predecessor, valid position, open projection, selected raw execution, exact region, reference closure, and publication template. Both require predecessor Message-attachment uniqueness; Terminate additionally requires live predecessor wait owners. Neither theorem assumes successor validity, projection equality, or acceptance. The [independent checkpoint receipt](#independent-cold-review-receipt) records approval and complete verification.

[Wait projection](../BpmnSemantics/SemanticProcess/InternalRegionalCancellationWaitProjection.lean) derives public identity uniqueness from the predecessor projection after private Timer classification, preserving the valid Task/Message alias discriminators. [Scope and Call projection](../BpmnSemantics/SemanticProcess/InternalRegionalCancellationScopeProjection.lean) uses the existing surviving-parent ancestry proof and current parentless Call account. [Program validity](../BpmnSemantics/SemanticProcess/InternalRegionalCancellationProgramValidity.lean) composes actual retained-owner censuses and existing wait preservation. [Message-boundary validity](../BpmnSemantics/SemanticProcess/InternalRegionalCancellationMessageValidity.lean) preserves all three exact pairing censuses through synchronized retention, reusing the existing Message-claim uniqueness proof; its generic filter law retains list multiplicity.

The [complete child projector](../BpmnSemantics/SemanticProcess/InternalRegionalCancellationOpenProjection.lean) derives every successor guard and filters the predecessor's canonically ordered open set. Error removes the selected child; Terminate retains it. [Hosting-root projection](../BpmnSemantics/SemanticProcess/InternalRegionalHostingCancellationProjection.lean) derives an empty public set from complete live-owner coverage while retaining the parentless Process root. These results connect the existing candidate and lifecycle fold to independently projected raw successors. Runtime predicates, raw execution, registered admission, the Message child-scope exclusion, and `AOO-RETAINED-BODY-01` remain unchanged. This checkpoint closes individual Error/Terminate lifecycle acceptance; complete preparation frames, aggregate runtime preservation, pair commutation/publication, regional batching, and the pending regional trial are separate obligations.

#### Error and Terminate runtime preservation

[Aggregate cancellation preservation](../BpmnSemantics/SemanticProcess/InternalRegionalCancellationRuntimeValidity.lean) proves `runtimeStateWellFormed` for the actual prepared Error and Terminate successors from that unchanged predicate on the predecessor and successful complete preparation. Preparation supplies the selected operation, owner, running identity, publication-derived wait ownership, and closed references. Existing collection, identity, ordering, Activity, and bidirectional Multi-Instance proofs supply the aggregate components. Error's parent continuation preserves canonical token multiplicity; Terminate's End increment preserves the remaining components. Neither theorem assumes intermediate validity, successor validity, or final equality.

[Compensation preservation](../BpmnSemantics/SemanticProcess/InternalRegionalCancellationCompensationValidity.lean) supplies the missing retained-state cases. Child cancellation preserves parentless owner censuses, the required root Activity-retention register, and root-owned triggers and handler waits; completed trigger owners need not still be live. The existing declaration prohibition on Terminate with Activity retention, together with preparation's snapshot exclusion, forces Terminate's Compensation execution collections to be empty. No predicate, declaration rule, transition, or admission boundary changes. The retained root/child and private-Timer alias controls pass; a separate constructed Compensation control retains live handlers and rejects incorrectly purging the required register. These controls establish no new profile reachability. The [review receipt](#independent-cold-review-receipt) records independent approval and clean integration; the broader regional trial remains pending.

#### Complete and Return runtime preservation

[Return](../BpmnSemantics/SemanticProcess/InternalRegionalReturnRuntimeValidity.lean), [hosting-root Complete](../BpmnSemantics/SemanticProcess/InternalRegionalRootCompletionRuntimeValidity.lean), and [ordinary/bounded child Complete](../BpmnSemantics/SemanticProcess/InternalRegionalChildCompletionRuntimeValidity.lean) derive the complete unchanged `runtimeStateWellFormed` predicate for their actual prepared successors. Each public theorem assumes only predecessor well-formedness and successful complete preparation. The proofs reuse existing position, reference, identity, ordering, and publication facts; no intermediate or successor-validity premise is added. The [review receipt](#independent-cold-review-receipt) records independent approval, concrete Lean controls, and clean complete integration on 2026-09-21.

[Call removal](../BpmnSemantics/SemanticProcess/InternalRegionalCallRemovalValidity.lean) retains live owners, issuance bounds, declarations, and controller ownership. [Completion bindings](../BpmnSemantics/SemanticProcess/InternalRegionalCompletionMultiInstanceValidity.lean) reuse existing bidirectional Sequential/Parallel Multi-Instance laws: child completion removes a scope body, while retained Task bodies keep their exact controller and Timer censuses. [Compensation completion](../BpmnSemantics/SemanticProcess/InternalRegionalCompletionCompensationValidity.lean) derives successful terminal history from root quiescence and preserves parentless retained owners during child removal. Return's existing declaration exclusions force its Compensation execution state empty. Snapshot exclusion and all runtime predicates remain unchanged.

The [Activity-owner correction](INTERNAL-REGIONAL-OWNERSHIP-CLOSURE-PROPOSAL.md#retained-activity-owner-correction) is required by child preservation: both targets previously accepted ordinary and bounded completion that stranded a retained Activity's owning scope despite retaining its body. The new predecessor-mask check applies to every regional family; a quantified Lean refusal law covers the omitted edge, and both TypeScript regressions preserve valid surviving-owner siblings. Constructed runtime-valid witnesses establish no profile reachability. The complete core gate passes 1091 tests. This checkpoint supplies individual Complete/Return aggregate preservation; complete preparation frames, pair laws, production regional batching, `AOO-RETAINED-BODY-01`, and the regional trial remain open.

The changed preparation guard reaches two kernel-decided consumers. Both were remeasured on 2026-09-21 from the read-only snapshot at `/tmp/bpmn-completion-owner-resource/source`, with all 530 archived Lean/build inputs matching the worktree. The existing pinned image, network isolation, one CPU/thread, warm dependency closure, cold exact target, 3 GiB memory ceiling, and zero additional swap were retained. `InternalRegionalCancellationProjectionConformance` passed in 102.47 seconds at 2,767,292 KiB GNU RSS and 2,888,040,448 cgroup bytes; `InternalRegionalPrivateTimerAliasConformance` passed in 16.21 seconds at 2,689,160 KiB and 2,535,141,376 bytes. Every controlled memory-event counter and swap reading was zero; pressure totals were 121,671 and 13 microseconds respectively. The executable cost rows bind each source and complete receipt digest. The first warmup failed before Lean started because the temporary read-only snapshot lacked the `.lake` mountpoint; creating that directory fixed the setup. These are bounded correctness/resource results, not an uncontended speed comparison.

#### Local-control preservation of regional preparation

The [complete preparation frame](../BpmnSemantics/SemanticProcess/InternalRegionalLocalControlPreparationFrame.lean) proves `prepareInternalRegional_after_independent_localControl` for every successful regional and local-control preparation. Its premises are predecessor `runtimeStateWellFormed`, both predecessor preparations, and independence of the regional footprint from the lifted local-control footprint. Selection, ownership closure, occurrence region, exact dependency census, and the complete numbering-free publication template are derived unchanged after applying the local-control patch. No intermediate-validity or retained-preparation premise is assumed.

The [dependency laws](../BpmnSemantics/SemanticProcess/InternalRegionalLocalControlDependencies.lean) extract protected occurrence-region and token-owner reads from successful regional preparation. The [selection frame](../BpmnSemantics/SemanticProcess/InternalRegionalLocalControlSelectionFrame.lean) reuses token and selected-branch filter laws to preserve quiescence and Error/Terminate owner selection. Publication reuses local-control position validity and the existing open-occurrence frame; all equalities preserve exact list order and multiplicity.

The [TypeScript pair witnesses](../packages/semantic-core/test/internal-transition-regional-local-control.test.ts) cover all four regional families beside all five local-control operations, with additional bounded-completion cases. Each prefix checks runtime and projection validity; both execution orders retain complete preparation and agree exactly on the final state. Two contained-fork negatives remove occurrence-region and token-census dependencies to expose false independence, changed preparation, and stale-artifact refusal in both directions. These constructed Programs explicitly fail profile admission. The [review receipt](#independent-cold-review-receipt) records independent approval and clean integration of the forward frame; the reverse checkpoint follows below.

#### Regional preservation of local-control preparation

The [reverse complete frame](../BpmnSemantics/SemanticProcess/InternalLocalControlRegionalPreparationFrame.lean) proves `prepareInternalLocalControl_after_independent_regional` for actual prepared Return, Complete, Error, and Terminate successors. It assumes only predecessor well-formedness, both successful preparations, footprint independence, and the actual regional execution result. The unchanged [local-control read frame](../BpmnSemantics/SemanticProcess/InternalLocalControlPreparationFrames.lean) then retains the entire artifact, including selected-join competing-owner readiness, whole-key branch populations, exact token multiplicity/order, and publication. No successor validity, readiness, or preparation is assumed.

The [reverse dependency laws](../BpmnSemantics/SemanticProcess/InternalLocalControlRegionalDependencies.lean) derive scope/bucket exclusion and census protection. The [regional observation filters](../BpmnSemantics/SemanticProcess/InternalRegionalControlFields.lean) reuse actual Call removal, cancellation masks, and bounded-completion field laws. Quiescence excludes removed token/branch populations for normal completion; its hosting-root case conflicts through the shared runtime-control atom. The executable pair matrix and dependency-omission negatives remain the separators. The [review receipt](#independent-cold-review-receipt) records approval and clean integration; quantified pair commutation, accepted pair publication, production regional batching, and regional trial closure remain open.

#### Regional/local-control commutation and accepted publication

The [pair law](../BpmnSemantics/SemanticProcess/InternalRegionalLocalControlCommutation.lean) derives both retained preparations and one exact final RuntimeState for every prepared regional/local-control pair from Program validity, predecessor runtime validity, both preparations, and footprint independence. The [publication law](../BpmnSemantics/SemanticProcess/InternalRegionalLocalControlPairPublication.lean) composes individual preservation and acceptance into all four actual steps. Both orders accept the same per-operation record, lifecycle, position delta, and logical time at that operation's assigned transition index. Neither intermediate validity nor successor equality/publication is a premise. The [patch algebra](../BpmnSemantics/SemanticProcess/InternalLocalControlRegionPatch.lean) preserves canonical order and multiplicity through filtering, continuation insertion, and bounded completion; it reuses the existing RuntimeState updates.

The existing [pair matrix](../packages/semantic-core/test/internal-transition-regional-local-control.test.ts) checks actual accepted publication in both orders for all 25 family combinations, including bounded completion. Nonzero distinct assigned indices remain attached to their operations; renumbering local control changes its lifecycle, separating state commutation from publication numbering. The two dependency-omission negatives still refuse stale preparations. A constructed Lean Error/Synchronize predecessor jointly satisfies Program/runtime validity, preparation, independence, and exact final equality. The main theorem axiom reports contain only `propext`, `Classical.choice`, and `Quot.sound`. The [independent checkpoint review](#independent-cold-review-receipt) approved without findings. This closes the local-control pair proof obligation only; other family pairs, finite regional batching, source admission, and the regional trial remain open.

#### Regional/arming outcome

The next complete outcome covers prepared ordinary and composed-data arming beside Return, Complete, Error, and Terminate under the existing footprint independence account. Its obligations are:

1. Preserve both complete preparations from predecessor facts.
2. Derive successful execution in both orders and literally equal canonical RuntimeState, including collection order and multiplicity.
3. Derive intermediate and final runtime well-formedness from predecessor validity and the individual preservation laws.
4. Derive actual accepted publication in both orders, with equal per-operation records and deltas at their assigned indices and agreement after canonical ordering and numbering.

Intermediate validity, retained preparation, final equality, and publication acceptance must be conclusions rather than added premises. The existing snapshot and Message child-scope exclusions and `AOO-RETAINED-BODY-01` remain unchanged. [PLAN.md](PLAN.md#exact-resume-point) owns the unresolved work; [the cost ledger](CAPSULE-COST-LEDGER.md#regional-pair-review-cycle-baseline-2026-09-21) owns the review-cycle comparison. This is one outcome under the existing [commit boundaries](TESTING-SPEC.md#commit-boundaries), not a new process rule.

The [pair theorem](../BpmnSemantics/SemanticProcess/InternalRegionalArmingCommutation.lean) derives complete preparation in both directions and exact successor equality from predecessor validity and the existing independence predicate. The [publication theorem](../BpmnSemantics/SemanticProcess/InternalRegionalArmingPairPublication.lean) composes actual execution, all successor validity, and accepted per-operation publication at arbitrary assigned indices. Ordinary and composed-data arming share the owner/retention frames; Call return, cancellation, and bounded completion retain exact canonical collections rather than sorting away differences. No intermediate invariant or post-execution equality is assumed.

At this pair-level checkpoint, the [TypeScript pair matrix](../packages/semantic-core/test/internal-transition-regional-pair.test.ts) covered all four regional families and bounded completion beside seven arming variants. It checked actual publications with distinct nonzero assigned indices and canonical ordering, complete preparations, and exact states in both orders. Constructed Lean Error/ordinary-Task and Error/composed-data-Task predecessors jointly satisfy the theorem hypotheses and check actual state/publication equality at indices 43 and 44; renumbering the arming publication changes it. Both main theorem axiom reports contain only `propext`, `Classical.choice`, and `Quot.sound`. Independent review and clean integration approved this pair-level proof checkpoint. It does not implement regional batching or establish public-profile reachability or regional-trial closure.

#### Regional/regional outcome

The [pair consumer](../BpmnSemantics/SemanticProcess/InternalRegionalPairCommutation.lean) covers every ordered pair of prepared Return, Complete, Error, and Terminate operations under the existing regional independence predicate. From a valid running predecessor and both complete preparations, it derives both preparation directions, successful execution in either order, literally equal canonical RuntimeState, all successor validity, and actual accepted per-operation publication at fixed assigned indices. Intermediate validity, retained preparation, final equality, and publication acceptance are conclusions. This is one complete pair outcome under the existing [commit boundaries](TESTING-SPEC.md#commit-boundaries).

The proof reuses the individual execution, preservation, and publication laws. Its [preparation frame](../BpmnSemantics/SemanticProcess/InternalRegionalPairPublicationFrame.lean) preserves selection, ownership closure, region, footprint, and the entire publication template. Disjoint removal preserves exact retained collection order; canonical token insertion commutes with the other operation's filter because write/write independence protects each continuation. End counters are additive. Cancellation lifecycle frames distinguish private Timers and retain exact Call, Activity, effect, and incident populations; bounded completion also withdraws its parent-owned deadline and Activity body.

The existing [TypeScript matrix](../packages/semantic-core/test/internal-transition-regional-pair.test.ts) checks all sixteen ordered regional pairs and four bounded-completion pairs against actual transitions and publications, including canonical ordering at distinct nonzero indices. The temporary Lean probe `/tmp/bpmn-regional-pair-joint-probe.lean` independently constructs all twenty predecessors through actual entry, fork, arming, and completion operations and checks joint hypotheses, both preparations, exact states, successor validity, accepted publications at indices 43/44, self-conflict, and changed-index inequality. It explicitly uses the existing Terminate profile tag required by Lean selection; it is not a cross-target refusal-domain equivalence claim. Both consumer axiom reports contain only `propext`, `Classical.choice`, and `Quot.sound`. These are constructed validation-domain witnesses, not source/profile reachability. Independent review and clean integration approved the pair. Finite regional batching, scheduled choice, and regional-trial closure remain open; snapshot and Message child-scope exclusions and `AOO-RETAINED-BODY-01` are unchanged.

#### Regional/scope-creation outcome

The [pair consumer](../BpmnSemantics/SemanticProcess/InternalRegionalScopeCreationCommutation.lean) covers Return, Complete, Error, and Terminate beside ordinary child entry or Call invocation, including bounded completion. Predecessor validity, both complete preparations, and existing footprint independence imply both preparation directions, successful opposite orders with literally equal canonical RuntimeState, all successor validity, and actual accepted per-operation publication at assigned indices. Intermediate validity, preparation, equality, and acceptance are conclusions.

The [creation frame](../BpmnSemantics/SemanticProcess/InternalScopeCreationRegionalFrame.lean) retains protected token and owner populations through removal. The [regional frame](../BpmnSemantics/SemanticProcess/InternalRegionalScopeCreationFrame.lean) preserves arbitrary-ID ancestry and Call reachability with each state's own fuel, ownership checks, exact region, footprint, and publication through insertion. Existing Call structural admission places invocation owners in the hosting Process; [the caller lemma](../BpmnSemantics/SemanticProcess/InternalRegionalScopeCreationCalls.lean) derives that restriction rather than adding a new theorem premise. [Collection algebra](../BpmnSemantics/SemanticProcess/InternalScopeCreationRegionalAlgebra.lean) preserves token multiplicity and literal scope/Call order when retained insertion is exchanged with filtering.

The [TypeScript matrix](../packages/semantic-core/test/internal-regional-scope-creation-pair.test.ts) covers ten family/withdrawal combinations, five repeated child-input cases, and two containing-region negatives using the [shared fixture](../packages/semantic-core/test/internal-regional-scope-creation-fixture.ts). Omitting region and changed-token-census dependencies falsely admits child creation inside Error/Terminate removal. Actual execution either removes the creation owner or enlarges the selected region; the old artifact refuses. The temporary Lean probe `/tmp/bpmn-regional-scope-creation-probe.lean` reconstructs all seventeen cases through actual operations, checks joint premises, preparation, state, validity, publication at indices 43/44, multiplicity, and changed-index refusal. It uses the existing Terminate profile tag required by Lean selection, without claiming cross-target refusal-domain equivalence. Both consumer axiom reports contain only `propext`, `Classical.choice`, and `Quot.sound`. Independent review and clean integration approved the pair. No executable reduction body or kernel fixture changes; public admission, snapshot/Message exclusions, `AOO-RETAINED-BODY-01`, finite regional batching, scheduled choice, and regional-trial closure are unchanged.

#### Finite regional batch outcome

The next consumer integrates the existing regional preparations with arming, local control, and scope creation in finite reject-mode batches. From predecessor validity, complete preparations, and pairwise independence, it must derive every prefix's retained preparations and validity, successful execution, literal canonical final-state equality under permutation, and actual accepted publication after canonical ordering and numbering. The existing pair laws own collection preservation; the finite proof composes them rather than reopening per-field removal proofs. Production must classify the complete frontier and retain whole-command rollback on conflict, unsupported preparation, stale artifacts, or insufficient fuel.

The separating witnesses are three same-family regional members through all six orders, four mixed families through all twenty-four orders, nonadjacent region conflicts, complete-artifact tampering, publication numbering before sorting, and exact fuel exhaustion before a batch executes. Root completion's control write cannot coexist with another independent operation's control read; preserve its ordinary singleton execution and derive this exclusion rather than weakening running-state preservation. Existing snapshot, Message child-scope, and bounded-Terminate limitations remain unchanged; these constructed internal witnesses widen no profile or host admission.

Reassess before implementation expands beyond composing the approved pairs, adapting the existing prepared executor/publication interfaces, and those discriminators. A missing prefix invariant, a new atom or semantic account, or failure of the unchanged 3 GiB fixture protocol requires a named smaller approach or representation decision before adding prerequisites. Finite batching, scheduled choice, and final closure remain separate obligations; this outcome closes only the first.

The 2026-09-22 implementation adds Return, Complete, Error, and Terminate to the existing [Lean prepared dispatcher](../BpmnSemantics/SemanticProcess/InternalPreparedTransition.lean) and independently written [TypeScript dispatcher](../packages/semantic-core/src/internal-transition-batch.ts). Both reuse complete regional preparation and revalidation. The [finite induction](../BpmnSemantics/SemanticProcess/InternalTransitionBatch.lean) composes the four approved regional pair laws with the existing ordinary-family laws. Every member of a batch of at least two has an independent partner whose hosting-control read excludes terminal root completion; this derives the control condition once before induction, including singleton residual prefixes. The [canonical consumer theorem](../BpmnSemantics/SemanticProcess/InternalTransitionCanonicalBatchPublication.lean) therefore retains successful actual execution, predecessor-derived successor validity, literal final-state equality, and accepted canonical publication without an intermediate-validity or final-equality premise. Its cardinality premise is supplied by the existing batch classifier; ordinary singleton execution is unchanged.

Publication values and individual acceptance laws now have [dispatcher-independent owners](../BpmnSemantics/SemanticProcess/InternalTransitionPublicationCore.lean), so approved pair laws remain reusable below the finite consumer without an import cycle. The [TypeScript matrix](../packages/semantic-core/test/internal-transition-regional-batch.test.ts) checks five three-member variants through six orders and ten four-family variants through twenty-four orders, including bounded completion, exact raw states, every retained preparation, actual lifecycle/position publication, nonzero numbering, complete-artifact tampering, nonadjacent region conflicts, and unsupported members. Whole-command witnesses retain exact rollback and no publication after admitted external completion when conflict or fuel exhaustion prevents closure. The complete core gate passes 1,166 tests. Independent review and clean integration approve this finite outcome. These constructed Programs remain outside public profile admission; regional-trial closure remains pending.

The temporary Lean probe `.cache/finite-regional-batch-20260922/probe.lean` decodes the same fifteen positive Program fixtures and two containing-region conflicts, independently constructs predecessors through actual operations, and passes all prefixes, permutations, exact states, and accepted publications. As with the accepted pair probes, it selects the existing Terminate profile tag required by Lean selection; it claims neither equal cross-target refusal domains nor byte-identical starting states. The asserted `/tmp/bpmn-finite-regional-probe-v1` receipt records output SHA-256 `405ede42919925e5e83ce4c3aa5839034beb6617e6061b3840049c6a0354b50d`. The pair, derived-control, and canonical consumer axiom reports contain only `propext`, `Classical.choice`, and `Quot.sound`.

#### Ordinary End batch outcome

The next complete consumer integrates `reachNoneEnd` into finite reject-mode batching. Its predecessor preparation retains the exact selected owner, one consumed token, the relative End increment, complete footprint, and numbering-free publication. Both preparations retain the [existing End selector's single-unit input restriction](../packages/semantic-core/src/semantic-process-control-flow-runtime.ts); a repeated-unit input is refused rather than silently broadening the executable boundary. Existing token-patch laws must derive canonical token storage and runtime validity; the End counter composes by addition rather than recording an absolute successor count. All currently prepared families must preserve the complete preparation in both orders, and the finite consumer must retain exact state equality, successor validity, accepted publication, and whole-command rollback. No profile, snapshot, Message, or bounded-Terminate admission changes.

The separating witnesses are three ordinary Ends through every order; ordinary End beside arming, local control, scope creation, and each regional kind; two independent End increments retained exactly; a same-input producer or containing-region removal that invalidates the End selection; altered retained publication or patch; and exact-fit versus insufficient whole-command fuel. Reassess before adding per-field preservation machinery, a new atom domain, or weakening a consumer premise: the existing `TokenPatch` validity/commutation laws and relative-increment account are the selected reuse boundary. Exclusive Merge retains its exact selected-input alternative and belongs to the separate unified-frontier obligation.

The implementation uses [End preparation](../BpmnSemantics/SemanticProcess/InternalEndPreparation.lean) and the independently written [TypeScript preparation](../packages/semantic-core/src/internal-transition-end-preparation.ts). Both bind the complete artifact before applying one token removal and a relative increment. [End publication](../BpmnSemantics/SemanticProcess/InternalEndPublication.lean) derives accepted instantaneous lifecycle and exact public token deltas. Pair laws cover [End](../BpmnSemantics/SemanticProcess/InternalEndCommutation.lean), [arming](../BpmnSemantics/SemanticProcess/InternalEndArmingCommutation.lean), [local control](../BpmnSemantics/SemanticProcess/InternalEndLocalControlCommutation.lean), [scope creation](../BpmnSemantics/SemanticProcess/InternalEndScopeCreationCommutation.lean), and [regional operations](../BpmnSemantics/SemanticProcess/InternalEndRegionalCommutation.lean). They reuse token-patch validity, existing regional selection/retention frames, and fixed regional token actions; no new aggregate invariant or atom domain is introduced. The unchanged statement of the [canonical finite consumer](../BpmnSemantics/SemanticProcess/InternalTransitionCanonicalBatchPublication.lean) now includes ordinary End through the prepared union.

The [18 TypeScript checks](../packages/semantic-core/test/internal-transition-end-batch.test.ts) cover all six orders of three Ends, ten mixed regional/scope-creation variants, complete preparation and actual publication at every tested prefix, changed artifacts, wrong owners, repeated input units, conflicting production, containing Error/Terminate removal, and exact-fit versus insufficient command fuel. Same-input End alternatives form an intentionally inadmissible Program used only as a defensive conflict check; the producer-conflict Program is structurally valid. All 1,184 package tests pass. These constructed states establish no registered-profile reachability. The [review receipt](#independent-cold-review-receipt) records approval and clean integration.

### Exact Merge frontier outcome

The next consumer closes the existing [complete-frontier](#arbitrary-complete-frontier-batches) and [choice](#explicit-observable-choice) contracts for exact Merge inputs together with already prepared families. It must retain every offered owner/input alternative when the legacy unique-offer evaluator has no successor, carry the complete value-bearing token patch and numbering-free accepted publication, derive successor validity and independent preparation/state/publication laws, and recompute the complete frontier after each selected step. Universal normalization excludes every member of a multi-alternative operation even when one member happens to commute with the other operations. Canonical alternative order encodes the presented choice and publication; it never selects a conflicting member.

The separating consumer witnesses are an admitted-Program multi-input Merge that currently appears falsely stable, a Merge with another operation conflicting with exactly one alternative, a universally independent member beside a choice, multiplicity within one exact input bucket, stale complete preparations, both valid schedule directions, every schedule-failure discriminant and precedence, and exact-fit versus insufficient whole-command fuel. The existing registered cyclic source must retain its unique-offer results and publication. Constructed broader Programs establish no registered-source or Temporal admission; snapshot exclusions and the pending regional trial remain unchanged. Complete frontier, scheduled consumption, exact canonical state/publication, and atomic refusal form one outcome rather than separately reviewed helper checkpoints. Reassess before new state atoms or per-field preservation machinery; token-patch laws and the existing finite batch consumer are the reuse boundary.

The implemented [Lean frontier](../BpmnSemantics/SemanticProcess/InternalExecutionFrontier.lean) and [TypeScript frontier](../packages/semantic-core/src/internal-transition-preparation.ts) retain every exact Merge alternative. [Merge preparation](../BpmnSemantics/SemanticProcess/InternalMergePreparation.lean) proves declarative-step refinement and successor validity; mixed frames reuse the existing token-patch and regional laws. The finite state/publication theorem now executes exact alternatives, with an ordinary-operation bridge preserving existing raw-evaluator fixture statements. Strict replay retains the selected input privately and leaves the public transition wire unchanged. Its owner-selection soundness statement explicitly uses exact-alternative selection rather than the legacy unique-offer selector.

The [scheduled closure](../BpmnSemantics/SemanticProcess/InternalScheduledClosure.lean) normalizes only universally independent singleton alternatives, revalidates complete preparations at each prefix, and proves successful schedule consumption against the actual selected execution. Its successful checked batch refines the existing finite publication executor. [Command evaluation](../BpmnSemantics/SemanticProcess/TransitionTrace.lean) proves empty-schedule erasure and exact atomic refusal with no publication. These are prepared-family, consumption, and rollback guarantees; they are not general graph progress, fuel adequacy, or a proof of cross-language equivalence. Admission remains visible at the result-only interface; existing command-admission theorem statements are retained.

Singleton execution retains the result-only/publication separation exercised by the Configured Task wrong-lowering and User Task metadata theorems: unavailable publication erases the trace rather than adding an admission condition to the selected step. End discovery explicitly retains the approved single-unit selector; the broader raw Lean End relation is not the executable frontier rule. Selecting both Merge inputs before End therefore leaves two output units and a stable running state in both targets. This private structural witness demonstrates observable choice and the retained End limitation, not registered-profile progress or a general resumability result.

Twenty [shared scheduling witnesses](../packages/differential/test/internal-choice-schedule-lean-core.integration-test.ts) pass identical structurally valid Program/stimulus/directive bytes to both interpreters and compare outcomes, flags, failure precedence, exact rollback, replay, enabled-frontier counts, and actual E1 publication/current positions. They cover both Merge directions, universal normalization beside choice, late refusal, admission-first refusal, fuel precedence, and unused directives. The witness is part of the complete verification pipeline. Existing TypeScript tests separately cover multiplicity, stale preparation, and conflict permutations. Full Lean, complete core/source gates, shared parity, infrastructure, and [resource calibration](CAPSULE-COST-LEDGER.md#exact-merge-frontier-integration-2026-09-22) pass. The [review receipt](#independent-cold-review-receipt) records semantic-checkpoint approval and successful completion of the selected integration lanes after browser provisioning. This outcome is accepted; final proposal closure and new registered profile or Temporal scheduling support are not claimed.

### Remaining final-closure discriminators

The region separator uses two enabled operations in disjoint sibling occurrences and a second state that moves one operation into the other's descendant occurrence. The sibling case must batch; the ancestor/descendant case must conflict even when every concrete pre-existing collection key differs. A mutation that compares only region-root equality must accept the wrong case and fail the oracle.

The explicit-choice separator has two conflicting operation alternatives with reverse lexical IDs. Under `rejectObservableChoice`, both Program orders report ambiguity. Under `requireChoiceSchedule`, two exact schedules select opposite alternatives and reach the corresponding distinct states, while swapping Program order changes neither result. Missing, stale-frontier, non-member, duplicate, skipped-ordinal, and unused directives roll back to the exact pre-command state and publish nothing. An overlapping-defect oracle supplies one directive with the wrong ordinal, a stale alternatives list, and a selected member absent from both the stale and actual lists, and requires exactly `ordinalMismatch`; a companion with the correct ordinal but the same two remaining defects requires exactly `alternativesMismatch`. A late-failure oracle first commits external admission and one scheduled internal choice, then supplies a stale second directive; any exposed intermediate state or publication fails it. Separate precedence oracles cover rejected or unsupported admission with a nonempty schedule, schedule forbidden under reject mode, fuel exhaustion with remaining directives, and trailing directives discovered only at stable closure.

A unified-frontier state contains one `mergeExclusive` with two offered input-owner/place pairs plus another enabled operation that conflicts with exactly one merge alternative. The producer must return all three exact prepared alternatives even though the legacy single-successor merge evaluator returns `none`; each valid directive fires its selected member and recomputes the other two rather than discarding them. A second graph has one universally independent member plus two conflicting members and proves that the independent work is normalized before the directive. A three-node conflict chain retains two valid directive sequences as explicit semantic input rather than falsely claiming a unique schedule.

The numbering oracle batches at least two instantaneous publishers. All permutations must yield equal RuntimeState and exact E1/E2 publication after canonical template sorting and late index instantiation. A mutation that writes transition index zero into a prepared lifecycle, numbers before sorting, changes equal-key local multiplicity order, or separates a transition record from its lifecycle template must fail outside the production ordering branch.

The census guards are mutation-tested by adding one synthetic operation variant and one synthetic RuntimeState field to their parsed owner shapes. Each omission must fail before semantic tests. These guards establish exhaustiveness only; the per-family transition and field mutations establish that each classification is semantically complete.

The Temporal oracle rejects `requireChoiceSchedule` before Workflow start, proves every existing profile lowers to `rejectObservableChoice`, and mutates one Program by dropping or changing the required field. Exact-fit and one-byte-over tests cover both the Program argument and complete six-argument continuation aggregate. A real-service witness starts only new-byte history, crosses permitted pre-arming rollover, replaces the Worker, recovers an accepted result, completes, and replays every Run. No test claims compatibility with a pre-field history.

The final complete gate retains every checkpoint witness and existing semantic result and public-observation byte result. Semantic Process artifact bytes change only by the required closed scheduling-mode field and receive exact decoder, lowering, fixture, content-binding, and capacity updates rather than a compatibility reader. Because the amendment changes the Semantic Process Program representation, pure evaluator input, transition proof boundary, multiple-enabled behavior, and Temporal serialization preflight, the first green final implementation requires a new semantic-checkpoint review before any adapter or downstream lane continues.

### Private Timer alias correction

`RHP-PRIVATE-01` was reproduced on 2026-09-21 before extending Error/Terminate accepted-publication proofs. A structurally valid internal Program can declare a private boundary Timer and a public Task or Message with the same element and occurrence identity. Cancellation removes the Timer and preserves the unrelated public wait, but the former handler selector erased the Timer's privacy before matching the untagged public anchor. Both actual and prepared publication incorrectly ended the survivor; actual acceptance refused. The independent retained checker repeated the same mistake. Two complete Lean execution probes and [two TypeScript regressions](../packages/semantic-core/test/internal-regional-private-timer-alias.test.ts) reproduce the class with valid predecessors and successors. These are constructed validation-domain witnesses, not registered-profile or XML-reachability claims.

The publication selectors now exclude private Timers using the existing live-host classifier before erasing family identity. The retained checker independently identifies public Timer declarations from the immutable Program; it still retains private handler associations for their existing completion/continuation role. Raw cancellation, runtime predicates, admission, and the Message child-scope exclusion remain unchanged. Existing correspondence proofs take the Program needed by that classifier without adding successor-validity or final-equality assumptions. [The separate Lean witness module](../BpmnSemantics/InternalRegionalPrivateTimerAliasConformance.lean) keeps new kernel fixtures out of the previously near-cap cancellation module. Resource acceptance and complete affected gates precede the implementation commit; independent checkpoint review and clean integration precede further proof work. Timer-firing regressions also require the complete retained checker to accept the correct cancellation and reject a forged completion of the aliased public wait.

Both kernel witnesses passed the cold-target, warm-dependency 3 GiB/no-additional-swap gate in 16.02 seconds at 2,694,608 KiB RSS and 2,483,884,032 cgroup bytes, with all memory events and pressure totals zero. `/tmp/bpmn-private-alias-resource/receipt-fixed` is the asserted receipt; `inputs-fixed.json` beside it binds all 514 source/build inputs with SHA-256 `8bf14f2a95c62518d0101be5eead70dc8b26852096a69afbeb8bcd0cd8fabf9a`. The original fixture type-error receipt and snapshot remain retained. [The cost row](../scripts/lean-module-cost.ts) binds the measured source and completed receipt; this is acceptance evidence, not a performance improvement claim.

## Final owner, oracle, and headroom inventory

The implementation uses new bounded owners for the large new mechanisms and keeps existing modules as adapters or dispatchers. The `OWNER` measurements below are the nonblank counts reported by `node scripts/what-binds.ts` at proposal target `242c72f8`; implementation re-runs that command before every file grows.

| Reviewed account | Primary producer/consumer owner | Separating guard or oracle | Proposal-target headroom |
|---|---|---|---|
| Required scheduling field and exact JSON | [`semantic-process-contract.ts`](../packages/semantic-core/src/semantic-process-contract.ts), [`semantic-process.schema.json`](../contracts/schemas/semantic-process.schema.json), [`Program.lean`](../BpmnSemantics/SemanticProcessJson/Program.lean), and [source lowering](../packages/bpmn-source/src/semantic-process-lowering.ts) | [`contract-schema-coverage.test.ts`](../scripts/contract-schema-coverage.test.ts), a new exhaustive profile-lowering test, exact decoder mutations, and existing artifact consistency gates | contract `451/800`; Lean decoder `563/800`; lowering `564/800` |
| Operation and state census | new `scripts/internal-commutation-census.test.ts` generated from the TypeScript/Lean unions and this proposal's typed tables | add/rename one operation or state field independently in either parsed fixture; missing, duplicate, or cross-language-unmatched classification fails | new bounded owner; [`RuntimeState.lean`](../BpmnSemantics/SemanticProcess/RuntimeState.lean) remains type owner at `548/800` |
| Pre-frontier production, exact alternatives, schedule result, and rollback | new bounded semantic-core `internal-transition-preparation.ts` and `internal-choice-schedule.ts`; [`semantic-process-runtime.ts`](../packages/semantic-core/src/semantic-process-runtime.ts) remains dispatcher | new `internal-commutation-final.test.ts` covers multi-offer merge plus another conflict, all schedule failures, late rollback, precedence, universal normalization, and permitted schedule non-uniqueness | new bounded owners; dispatcher `589/800` |
| Footprints, regions, and arbitrary batches | [`internal-transition-footprint.ts`](../packages/semantic-core/src/internal-transition-footprint.ts) plus new bounded per-family preparation modules | census-key mutations, sibling/ancestor region separator, three-arm six-permutation and mixed four-family oracle; production never calls the permutation oracle | footprint `428/800`; new family owners bounded separately |
| Numbering-free publication | new bounded `internal-publication-template.ts`; [`semantic-transition-trace.ts`](../packages/semantic-core/src/semantic-transition-trace.ts) and [`flow-node-occurrence-lifecycle.ts`](../packages/semantic-core/src/flow-node-occurrence-lifecycle.ts) only adapt and instantiate | two-instantaneous-publisher permutations and mutations for pre-sorting numbering, transition/local index, pair separation, and actual accepted lifecycle | trace `390/800`; lifecycle `595/800`; new owner bounded separately |
| Lean proof and executable account | new responsibility-split `InternalPreparation`, `InternalChoiceSchedule`, `InternalPublicationTemplate`, `InternalBatch`, and conformance modules; [`TransitionTrace.lean`](../BpmnSemantics/SemanticProcess/TransitionTrace.lean) remains facade/consumer | narrow module builds, kernel-decided discriminators, relation soundness, complete-value frame, pairwise commutation, adjacent-swap theorem, schedule consumption, rollback, and publication instantiation | facade `555/800`; every new module starts below the soft target |
| Host admission and strict version boundary | [`host-admission.ts`](../packages/temporal-adapter/protocol/src/host-admission.ts), [pre-release policy](PROJECT-DESIGN.md#pre-release-evolution-policy), and [contract evolution policy](../contracts/README.md#evolution-policy) | new scheduled-mode host-admission test, exhaustive existing-profile reject-mode test, missing/unknown/changed-field mutations, and no compatibility reader | host admission `233/800` |
| Continuation capacity and durable refinement | [`workflow-continuation.ts`](../packages/temporal-adapter/protocol/src/workflow-continuation.ts), [Workflow lifetime contract](TEMPORAL-PROCESS-LIFECYCLE-SPEC.md#workflow-lifetime-contract), and [Workflow-chain production contract](TEMPORAL-PROCESS-LIFECYCLE-SPEC.md#workflow-chain-production-contract) | existing aggregate-capacity owner extended with exact Program and aggregate fit/over mutations; new real-service Worker-replacement, accepted-result recovery, pre-arming Continue-As-New, terminal receipt, and every-Run replay witness | continuation `506/800`; real-service witness is a new bounded owner |
| Same-change status and reproducibility | the four routed implementation maps, source registries/module graph, [PLAN](PLAN.md), and capsule cost ledger | focused package gates, exact Lean targets, `test:infrastructure`, every path-selected clean-commit pre-push entry point, `git diff --check`, checkpoint review, and closure review | each existing source owner is remeasured before delegation |

## Same-change owners and reopen conditions

Implementation updates [Semantic Process internal scheduling](SEMANTIC-PROCESS-IL-SPEC.md#internal-scheduling), its [Lean proof obligations](SEMANTIC-PROCESS-IL-SPEC.md#lean-specification-and-proof-obligations), the [scoped runtime data specification](capsules/SCOPED-DATA-SPEC.md#runtime-replacement), the [pre-release evolution policy](PROJECT-DESIGN.md#pre-release-evolution-policy), the [contract evolution policy](../contracts/README.md#evolution-policy), the [Workflow lifetime contract](TEMPORAL-PROCESS-LIFECYCLE-SPEC.md#workflow-lifetime-contract), the [Workflow-chain production contract](TEMPORAL-PROCESS-LIFECYCLE-SPEC.md#workflow-chain-production-contract), [`implementation-status-owner:ENGINE-CONTRACTS-SOURCE`](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [`implementation-status-owner:ENGINE-RUNTIME-PROOF`](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [`implementation-status-owner:TEMPORAL-HOSTING`](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md), [`implementation-status-owner:ASSURANCE-ADOPTION`](ASSURANCE-AND-ADOPTION-IMPLEMENTATION-MAP.md), the semantic-core source registry, the Lean module graph, and [PLAN](PLAN.md). The scoped-data owner must retain complete occurrence-identity order and carry the same producer, consumer, well-formedness, fixture, proof, observation, and replay preservation obligation. The Semantic Process schema and exact JSON decoders gain the closed scheduling mode, while the existing public stimulus and engine command schemas stay byte-identical. No semantic-family or platform map changes unless implementation discovers a fact in that owner's boundary.

Reopen before adding an atom domain or operation family, changing the region-overlap relation, changing the exact choice-point or schedule representation, exporting scheduled choice through a public engine contract, changing a wire or publication order, changing the actual flow-node open-set projectability premises, using state-diff equality as the production rule, or admitting a source/profile/host composition on the strength of this closure.

A future admitted topology that exposes an interrupting transition beside an unarmed sibling also reopens the account: reject mode may encounter an observable choice. The current admitted Error/Terminate fixtures do not establish that frontier, and this proposal selects no preemption policy.

## Independent cold-review receipt

The RC completion scope amendment received an independent proposal review at `4d7da502ba0f408034238e19e727af6fca216da1`, against baseline `2a12990b416e5327d19d37440a95e556d3642c02`, on 2026-09-23. Isolation: `fork-turns-none`. Verdict: `approve`; correction audit: `not-required`. The reviewer regenerated packet `f81eb2e1adcdb11f9188ff1d61b9f62acd6b35c36d51e356160084b27fd83dc0`, passed all 30 focused document/RC/census/link guards, and independently reconstructed both retained patches and their source hashes/counts. The one nonblocking advisory was applied: the experiment's total Lean count now explicitly excludes the frozen adoption snapshot. No semantic exclusion, RC closure, unfinished bounded-entry acceptance, or experimental adoption follows from this review.

The complete committed-target infrastructure gate passed all 582 tests at that target in 49.568 seconds, asserted receipt `/tmp/commutation-review-committed-infrastructure`, output SHA-256 `eeb9668271ca7fd763f945b961691453439127efe0e93b7b1584c2a53de88863`. Production Lean, TypeScript, contracts, and profiles are byte-unchanged from the baseline; no production implementation or Lean rebuild was part of this documentation increment. The [experiment report](experiments/INTERNAL-COMMUTATION-PROOF-ARCHITECTURE-EXPERIMENT.md) owns the closed investigation's findings and evidence limits; this receipt approves only the RC scope amendment.

The Timer-task arming outcome received a context-cold semantic-checkpoint review at `e8c1aa724d65cd88cd56c555d2b9f57ab0b42be7`, against baseline `bbaeb866ef6a8c4da4eb05d947437fa737b9e26f`. The isolated reviewer (`fork-turns-none`) approved with no required corrections, independently passed 109 focused tests, and regenerated packet `c08028c998fea199ebb7f9a999c7fb39f3ec1cfb559b70c1bb6cc87c13012a18`. All seven gate-output hashes, ten complete resource-receipt digests, and 618 measured inputs matched. Approval covers complete preparation for both Timer-task variants, predecessor-derived successor validity, exact mixed-family state equality, actual accepted publication, and inherited finite permutation laws. The advisory identified an asserted string literal in the publication template; the acceptance follow-up replaces that pattern in all four preparation owners with the existing enum value. This behavior-preserving correction is non-material under [the review gate](TESTING-SPEC.md#independent-cold-review-gate), covered by the complete semantic-core and infrastructure gates; it changes no proof or resource input.

Clean complete `./scripts/pnpm.sh run test:pre-push` passed at the reviewed target on 2026-09-22 in 2,363.728 seconds, with all 72 pipeline cases agreeing, 83 replayed histories, all 83 browser checks passing, and `dirty: false`. The asserted receipt is `/tmp/bpmn-timer-task-clean-integration-1`, output SHA-256 `aa1a681ba33f3bcfa2413e5554556b1a1993e89e25b891187abd642ca166506b`. The umbrella selected engine, platform, PostgreSQL, showcase, and UI gates from the entire unpushed range. Applying the same selector to the 52-path Timer increment selects only `test:pre-push:verify`; [the cost reflection](CAPSULE-COST-LEDGER.md#timer-task-arming-2026-09-22) records that avoidable duplication. These receipts accept the Timer-task arming outcome. Snapshot-enabled batching, broader source/profile admission, Timer firing/completion preservation, public/hosted scheduling, remaining families, final proposal closure, and regional-trial closure remain open.

The standalone Activity-data arming outcome received a context-cold semantic-checkpoint review at `46f9d1f6a55bb77da6e7e52fdac70214d9020740`, against baseline `7c509b9627ef75da85fa6981366dfd5cceee8414`. The isolated reviewer (`fork-turns-none`) required scoping the input specification's stale batching refusal, then approved documentation-only correction `486ac42fb16758c3b5e830bea554a66b9aec49f4` in one same-thread audit with no remaining findings. Original packet `57cb7b60e9aa9cf98ea98f0aabb0e7532d1557a78ce3258ec57b66cd8c67bf70` and correction packet `513ab27644e91d354d9a1ed91f39ac6264106d2e15535907f59c3dc9c528542c` matched their generated bodies and capsule hashes. All 61 independently run focused tests passed; the reviewer verified all 599 archived inputs, ten isolated cold measurements, and three warm/axiom receipts. Approval covers complete standalone preparation and inherited finite/mixed state, validity, and accepted-publication laws. Constructed witnesses establish no new profile reachability, Temporal admission, or general cross-target equivalence.

Clean complete `./scripts/pnpm.sh run test:pre-push:verify` passed at the implementation target in 2,109.207 seconds, with all 72 pipeline cases agreeing, 83 replayed histories, and `dirty: false`. Its asserted receipt is `/tmp/bpmn-standalone-arming-clean-integration-1`, output SHA-256 `f6416aae95667cadcd68350964dbc33d6f0dea481a51bf41fe0e29552671ca46`. The changed-path selector requires only that Product 1 gate for this increment. The correction leaves every implementation and resource input unchanged and passes all 582 infrastructure checks; its reviewer independently validated both receipts. The 55.032-second pipeline exceeded the 36-second soft target, so this is correctness evidence, not a performance claim. Together these receipts accept the standalone outcome. [The cost record](CAPSULE-COST-LEDGER.md#standalone-activity-data-arming-2026-09-22) retains resource failures and reflection; remaining families, final proposal closure, and regional-trial closure remain open.

The exact Merge/private scheduling outcome received a context-cold semantic-checkpoint review at `cd114dd672795f5898daf4dcb46a5fd539e629e2`, against baseline `146fd0f88db0e976ae6a3e1688ea2dae483a8e44`. Required findings were stale current-owner descriptions (R1) and an omitted changed section in the review packet (R2). The same reviewer audited `469d69b1664d7dde9b91e1bf7563279d9d845e60`, then approved `52ed7496fa1f46d21c7240f822674ed793d7b0a9` in the second correction audit with no remaining findings. These corrections change documentation only. Original packet `b44f4d85c8195daca00b5be521280b5d691c28313118969b18214b221df9ea41`, first correction `9ce2008b943d27a46f532b32bccf68c9f7bb1467d072ee3f765a623cd76d930e`, and final correction `bc7ecfc549781b2b8433d3163846872b2a6b899e706fdc9d6c6278172cea869a` retain the exact targets and routed sections. The reviewer independently passed 28 focused tests and checked the bounded measurement inputs and axiom reports. The requested fresh twenty-case shared Lean/core parity passed at the implementation commit in `/tmp/bpmn-exact-merge-review-parity-1`, output SHA-256 `1eb4b5549b6cea0ac0a314a7fce3e81f0f8696b04b426f98b1ce4c7d8ba61987`. Final correction infrastructure passed all 573 checks in `/tmp/bpmn-exact-merge-doc-correction-infrastructure-3`.

The first clean `./scripts/pnpm.sh run test:pre-push` at `cd114dd6` exited 1 after 2,103.108 seconds; its asserted failed receipt is `/tmp/bpmn-exact-merge-clean-integration-1`, output SHA-256 `563edf5560d6851da32787fc7007952e3f7fea22c730bb557d73b4a71dab4068`. Engine verification, platform, local PostgreSQL, and showcase lanes passed, including 72 agreeing pipeline cases, 83 replayed histories, and `dirty: false`. All 83 UI cases failed before browser launch because the pinned Chromium executable was absent. Provisioning through the manifest-pinned Playwright installer stalled, and a bounded connection check to `cdn.playwright.dev` timed out; `/tmp/bpmn-exact-merge-browser-provision-1` retains the stopped installer's exit 1. No browser behavior was exercised by those failures.

The owner-requested retry on 2026-09-22 installed pinned Chromium successfully in `/tmp/bpmn-exact-merge-browser-provision-retry-1`. Clean `./scripts/pnpm.sh run test:pre-push:ui` then passed at `28bac9007f4e2be8467923e756420de755c74593`: 19 feedback-policy checks, 147 web package tests, and all 83 functional browser tests. The asserted receipt `/tmp/bpmn-exact-merge-ui-integration-retry-1` took 124.759 seconds and has output SHA-256 `36848771e27060bb14eeacde9e0001ba0a55d40c4930277c7466498e7f50d3b2`. Only documentation differs from the original integration target; successful engine/platform/PostgreSQL/showcase and bounded Lean evidence remains applicable, with the handoff infrastructure receipt covering the later documentation. Together these receipts complete the selected integration lanes and accept this outcome. The original umbrella command remains recorded as failed; this is no claim that it was rerun successfully. Public/hosted scheduling, remaining families, final proposal closure, and regional-trial closure remain open.

The ordinary End batch outcome received a context-cold semantic-checkpoint review at `aa5ab5add1f06f569e23984f9776699901a41329`, against baseline `71151bcce06756aa3b2790446d8f6904955836a6`. The reviewer required correction of the source test's superseded rollback expectation and its linked limitation wording, then approved `ae78a5341b7ba98908741b250c02c168512af87b` in one same-thread correction audit with no remaining findings. Original packet `8bfc02d8f65ffb91c71e853866a8d4b3e9a158fef7beed08ae51430a2ed34810` and correction packet `d9804ae3541e5172bffa8b26164a3d3bc1e78e22680b4fd72ce5102e9bb08698` regenerated exactly. The reviewer independently passed eighteen End tests and five source tests, checked every packet gate-output hash, all six complete resource-receipt digests, and all 581 measured inputs. Approval covers predecessor-derived complete preparation, exact mixed-family state and accepted publication, successor validity, and command rollback; the source witness demonstrates admitted End/arming progress without extending the regional reachability argument.

Clean complete `./scripts/pnpm.sh run test:pre-push` passed at correction target `ae78a534` on 2026-09-22 in 557.643 seconds, including the selected showcase gate, all 72 pipeline cases agreeing, 83 replayed histories, and `dirty: false`. Its asserted receipt is `/tmp/bpmn-end-batch-prepush-corrected`, output SHA-256 `41df12317f023f6904f90461c61878f82a55c0ed226b74bb1bc396c98986ec47`. The pipeline took 53.715 seconds against its 36-second soft target. The [cost record](CAPSULE-COST-LEDGER.md#ordinary-end-batch-integration-2026-09-22) retains the failed first integration and correction. Production and Lean bytes are unchanged by that correction; existing snapshot/Message exclusions, scheduled choice, remaining families, final closure, and regional-trial closure remain intact. Constructed mixed-family fixtures establish neither general BPMN correctness nor cross-target equivalence or general profile reachability.

The finite regional batch outcome received a context-cold semantic-checkpoint review at `63b20e136031a9566c3b900d1f4bfe32a9ca701e`, against baseline `587ec3ddb951b078b64d85d270915bc00719287f`. The isolated reviewer approved with no required or advisory findings. Packet `d4fb570e53f873cdf926645ddb25fcd59b0339d8d4036c279c8086fedaae75c1` regenerated exactly; all five gate-output hashes, six complete resource-receipt digests, and 577 measured inputs matched. At the reviewer's request, the root ran all 27 focused TypeScript tests, the narrow canonical-publication Lean build, and the unchanged seventeen-case bounded probe. The reviewer independently asserted the exact-target probe receipt `/tmp/bpmn-finite-regional-review-probe-v1`, output SHA-256 `e878e166ed0505bb8ce70716999afb5c3e7377e17b554a333c2b9bf6e70f14e0`; all cases passed and axiom reports contain only `propext`, `Classical.choice`, and `Quot.sound`. Approval covers predecessor-derived complete preparation, successful finite execution, successor validity, literal canonical state equality, and actual accepted canonical publication.

Clean complete `./scripts/pnpm.sh run test:pre-push` passed at that exact target on 2026-09-22 in 826.062 seconds, including the selected showcase gate, all 72 pipeline cases agreeing, 83 replayed histories, and `dirty: false`. Its asserted receipt is `/tmp/bpmn-finite-regional-prepush`, output SHA-256 `fa88b259b70af1f8a11c0660fd9d01b5d1a7d15f31b698f670fd06b67aedf363`. The [cost record](CAPSULE-COST-LEDGER.md#finite-regional-batch-integration-2026-09-22) retains bounded measurements, failed iterations, and reported external CPU contention; timings establish no performance trend. Constructed probes establish neither independent BPMN correctness, registered-profile reachability, equal cross-target refusal domains, nor byte-identical starting states. Snapshot/Message exclusions, `AOO-RETAINED-BODY-01`, scheduled choice, remaining families, final closure, and regional-trial closure remain unchanged.

The regional/scope-creation outcome received a context-cold semantic review at `2c4a7bb594ae01a7c2e49ca62f11e45ca304cee6`, against baseline `23388fc2e82dd7496f574ac812e7686370d6acf9`. The isolated reviewer approved with no required or advisory findings, independently passed all seventeen retained TypeScript cases, and regenerated packet `e8932c5548327a661931d22efb52c4395f65c5755a28110007cad48c0daaab29` exactly. All four gate-output hashes matched. The reviewer inspected the seventeen-case Lean probe and inputs and independently asserted its exact-target receipt `/tmp/bpmn-regional-scope-creation-review-probe`; output SHA-256 is `259cb4e2fea73969cf360e1a1cd42cf6bdedea2ac5fab3aebf28bc6fa39f7e92`. Both consumer axiom reports contain only `propext`, `Classical.choice`, and `Quot.sound`. Approval covers complete preparation in both directions, literal canonical successor equality, successor validity, and actual accepted publication from predecessor facts. Constructed witnesses share Program inputs and the existing Lean validators/projections; they establish neither registered-profile reachability, general cross-target equivalence, independent BPMN correctness, production regional batching, nor final closure.

Clean complete `./scripts/pnpm.sh run test:pre-push` passed at that exact target on 2026-09-22 in 545.155 seconds, including the selected showcase gate, all 72 pipeline cases agreeing, 83 replayed histories, and `dirty: false`. Its asserted receipt is `/tmp/bpmn-regional-scope-creation-pre-push-after-smi-diagnostic`; output SHA-256 is `632b61e7601f4cd1659cce76c817dd638ad9b0b468f53c8ad8c7c31de3b61669`. The pipeline took 57.211 seconds against its 36-second soft target, so this is correctness evidence, not a performance improvement. The [cost record](CAPSULE-COST-LEDGER.md#regionalscope-creation-pair-preparation-2026-09-22) retains the first timing-margin failure and unchanged isolated diagnostic. Snapshot/Message exclusions, `AOO-RETAINED-BODY-01`, and regional-trial closure remain unchanged.

The regional/regional outcome received a context-cold semantic review at `cafc8018a25f0cceb6a265829f8928989ea0662c`, against baseline `72612d6c2e3927e1762f0304b0e7ddd75552add4`. The reviewer approved with one required ledger-rank correction and no semantic implementation finding, then approved correction `aa497c932710bdc71cb338170be14b7666a23a25` in the same thread. Original packet `b296a4537f4e05ec1fe3f70129dfbd7ce7d70e390d71e41f565c463645424307` and correction packet `c0fb70c1d9c2ef2877541a131a96e54f66cbdc3d3d25d253cf2d61ec3d2c7446` regenerated exactly. All 57 independently run pair tests and five correction cost guards passed. The reviewer independently asserted exact-target Lean receipts and requested a stronger twenty-case multiplicity probe: duplicate outside tokens remain exactly twice, while two cancellation-owned sibling-end units are removed in both orders. Every original joint premise, preparation, state, validity, publication, conflict, and numbering check remains. Its receipt `/tmp/bpmn-regional-pair-review-multiplicity-corrected` and the original exact-target probe `/tmp/bpmn-regional-pair-review-probe` share output SHA-256 `1ad0971cd3ebe72dadff40bbc82166da442304621e46e9a91a700b65b54e6cd5`; the separate probe sources carry the differing assertions. A preliminary probe duplicating an Error enabling input correctly refused preparation under its exact-one selector and is retained as a failed diagnostic.

Clean complete `./scripts/pnpm.sh run test:pre-push` passed at `aa497c93` on 2026-09-21 in 524.261 seconds, including the selected showcase gate, all 72 pipeline cases agreeing, 83 replayed histories, and `dirty: false`. The asserted receipt is `/tmp/bpmn-regional-pair-clean-integration-corrected`; output SHA-256 is `52b305e4cd5a306c5b6ab0aabb5916675860c673bb9a0feacc8da8351a56fa10`. The correction changed only the cost ledger; semantic bytes match `cafc8018`. The [cost record](CAPSULE-COST-LEDGER.md#regionalregional-pair-preparation-2026-09-21) retains the failed first integration and the author's final-document guard omission. Acceptance covers this pair obligation, not production regional batching, registered-profile reachability, cross-target equivalence, final proposal closure, or regional-trial closure.

The regional/arming pair received a context-cold semantic review at `1f490780152e1f542f621398707ac6374a01fc08`, against baseline `eba2eb817bfa3747e7b764a8d4526b60c701a5c6`. The isolated reviewer approved with no required or advisory findings, independently passed all 57 focused checks, and regenerated packet `5f7608c21b1a3c6e3e42f913a862b498fd424a8108bfbc88d197fb1c0c217a2f`. The exact-target Lean probe at `/tmp/bpmn-regional-arming-review-probe` passed and was independently asserted; output SHA-256 is `b7df44de11c0ae6d97c696cbeb0f97bd7ff9bfbdf7014779c8fcd8fed1fcfa4a`. Both constructed predecessors satisfy the hypotheses, actual state/publication checks pass, and both theorem axiom reports contain only `propext`, `Classical.choice`, and `Quot.sound`. Approval covers complete preparations, literal successor equality, successor validity, and accepted per-operation publication from predecessor facts, without intermediate-state or acceptance premises. The [cost record](CAPSULE-COST-LEDGER.md#regionalarming-outcome-preparation-2026-09-21) retains integration failures and their subsequent corrections. This approval establishes neither production regional batching, registered-profile reachability, nor final closure. Existing snapshot/Message exclusions and `AOO-RETAINED-BODY-01` remain intact.

Clean complete `./scripts/pnpm.sh run test:pre-push` verification passed at `4234d02967bc89ab21dae30ad7861949f6530e67` on 2026-09-21 in 545.752 seconds: all 72 pipeline cases agree, 83 histories replay, and `dirty: false`. The independently asserted receipt is `/tmp/bpmn-regional-arming-clean-integration-corrected`; output SHA-256 is `9febded2842264af8ab8c19aab3b2e952de4e3c51cb23aac6bc3cfd629d3e80e`. Lean and semantic-core trees are byte-identical to the reviewed `1f490780` target. The intervening commits fix only testkit Worker cleanup and update documentation; complete Temporal and infrastructure gates passed before each correction commit. This accepts the regional/arming pair obligation within the existing account. Remaining pairs, finite regional batching, and regional-trial closure stay open.

The regional/local-control pair checkpoint received a context-cold semantic review at `ab29d3caec9e94eb17386e2634cb0beb29d9e926`, against baseline `9656fd3305715a720f6744b6eae2cc936b0acaed`. The isolated reviewer approved with no required or advisory findings. All 80 independent focused checks passed, including 25 family combinations, dependency-omission negatives, and distinct assigned publication indices. Packet regeneration matched `c096b2ee03e61db949ce04386797829130ae3cff1ee1608a619a1fd6b4247b97`. The exact-target Lean probe at `/tmp/bpmn-regional-local-pair-review-probe` passed and was independently asserted: jointly satisfiable predecessor hypotheses derive both retained preparations, exact canonical state equality, successor validity, and accepted publication in both orders. Axiom reports contain only `propext`, `Classical.choice`, and `Quot.sound`.

Clean complete `./scripts/pnpm.sh run test:pre-push` verification passed at that target on 2026-09-21 in 499.859 seconds, with all 72 pipeline cases agreeing and `dirty: false`. Its asserted receipt is `/tmp/bpmn-regional-local-pair-clean-integration`; output SHA-256 is `44946e7d0db09f0ebd37a49dbf711ce5442f200cb5ac406f6f4f274bade5c1df`. The complete Lean/core gate and 573 infrastructure checks passed before commit. No executable reduction body or kernel fixture changed. [The cost record](CAPSULE-COST-LEDGER.md#regionallocal-control-pair-commutation-2026-09-21) retains failed iterations and reassessment. Approval covers this pair obligation within the existing account; it establishes no independent BPMN correctness, general cross-target equivalence, registered-profile reachability, production regional batching, or regional-trial closure. Snapshot and Message child-scope exclusions and `AOO-RETAINED-BODY-01` remain intact.

The reverse regional/local-control preparation-frame checkpoint received a context-cold semantic review at `ecd535a0d0302d654d47ce0ef90f976e7c4d2eb7`, against baseline `b23f68a3ab802347a706684cc8767c8b29baba17`. The isolated reviewer approved with no required or advisory findings. All 67 independent focused checks passed, including the pair matrix, both-direction dependency-omission refusals, and selected-join separators. Packet regeneration matched `8cc6476bf3c18d81a208e87f0a40b14b889f4048a463f4536fe8db32a9f7a31c`. The unchanged requested Lean probe passed at the exact target and was independently asserted at `/tmp/bpmn-local-regional-reverse-review-probe`: an Error/Synchronize predecessor jointly satisfies the theorem's premises, actual execution, and complete retained preparation. The axiom report contains only `propext`, `Classical.choice`, and `Quot.sound`.

Clean complete `./scripts/pnpm.sh run test:pre-push` verification passed at that target on 2026-09-21 in 503.824 seconds, with all 72 pipeline cases agreeing and `dirty: false`. Its asserted receipt is `/tmp/bpmn-local-regional-reverse-clean-integration`; output SHA-256 is `b52aa8ea5d2b0dc238ba6f1349f7e260c539e3e449984064d32b59573e09697c`. The complete Lean/core gate and 573 infrastructure checks passed before commit; clean integration overlapped review. No executable reduction body or kernel fixture changed. [The cost record](CAPSULE-COST-LEDGER.md#regional-preservation-of-local-control-preparation-2026-09-21) retains failures and reassessment. Approval covers complete preparation in the reverse direction, without claiming profile reachability, general cross-target equivalence, quantified pair commutation/publication, production regional batching, or regional-trial closure.

The local-control/regional preparation-frame checkpoint received a context-cold semantic review at `e185caca644679b2f3720cc9ce11116951311e1d`, against baseline `dadd06d3f3debf5754d97ad62edc5de6dfeb5244`. The isolated reviewer approved with no required or advisory findings after checking the complete predecessor hypotheses, exact equality, all-family coverage, and proof reuse. All 56 independent focused tests/guards passed. Packet regeneration matched `914086ca651436a9e7768fd763b35c70da4c70533b9bfde94be68b51a292b9e7`. The requested unchanged Lean probe passed at that exact target and was independently asserted at `/tmp/bpmn-regional-local-control-review-probe`: a constructed Error/Synchronize predecessor jointly satisfies runtime validity, both preparations, independence, and exact retained preparation. The theorem's axiom report contains only `propext`, `Classical.choice`, and `Quot.sound`.

Clean complete `./scripts/pnpm.sh run test:pre-push` verification passed at that target on 2026-09-21 in 504.681 seconds, with all 72 pipeline cases agreeing and `dirty: false`. Its independently asserted receipt is `/tmp/bpmn-regional-local-control-clean-integration`; output SHA-256 is `706066d1f663a812197694df4a5323b2bcf377183b863ac6ef438ee31e913bd9`. The complete Lean/core gate and 573 infrastructure checks passed before commit; integration overlapped review. No executable reduction body or kernel fixture changed. [The cost record](CAPSULE-COST-LEDGER.md#local-controlregional-preparation-frame-2026-09-21) retains failures and reassessment. Approval covers the one-direction preparation frame, with no profile-reachability, cross-target-equivalence, reverse-frame, pair-publication, production-batching, or regional-trial closure claim.

| Stage | Review target | Isolation | Verdict | Correction audit |
|---|---|---|---|---|
| Proposal | `f7b59b08c0441416f036ad0753e6cba88fe18629` | `fork-turns-none` | `approve` | `not-required` |
| Semantic checkpoint | `299eea89a73c54c50491c50a1091f5e4646bfb74` | `fork-turns-none` | `approve-with-required-edits` | `bd1ac87bd4a0596fe8a96684d0bc6d0f5374ad3d` |
| Closure | `not-applicable` | `not-applicable` | `not-reached` | `not-applicable` |

The first checkpoint proposal review targeted `95ee893fc7efef561d579c9c2ecd164eccae1187` and closed its required edits at `e65fa4fbd2b4303794398061d94c0602e54a4714`. Its context-cold checkpoint review targeted `f4b09ba48054a2c059f06b92b3b4d2b4675a6117` and closed the required correction at `a34df385863d706f36785282201703604720013f`; those immutable results continue to own the implemented Beta checkpoint.

The materially amended final-closure account received a cold proposal review at `242c72f81bdeb2b6df28b4f2fc39e78c20573ef6`, followed by correction audits at `280716b667da387c04356f43d40123f10e0e6aab`, `905fba45833daafd2994f11dd23c0476419f64b9`, and `543430f7ebc560572515dd2b3317a7a0e954ae9c`. The owner authorized the third round, and the same reviewer approved its target with every required finding closed. Its context-cold final-implementation checkpoint review targeted `f6da5db574c7d2733dd06c7a13e4122cc1415fe1`; the same reviewer approved correction target `fb998985daa5ecc94af313b78bc6485a352dd213` with no remaining findings after the unrelated Temporal harness limitation was corrected and the clean complete gate passed at `9bbc99e5f830f4dea5157ed944f4d54b461ec6cc`.

The Activity-data projection prerequisite received a context-cold checkpoint review at `94033af12a0efc0e2be584b295888503b4e69c21`. The same reviewer approved correction target `13dfe438898bd3dc95283de9cf9fa32f984e54e9` with no remaining findings after the PLAN routing correction, 114 independent focused checks, and the clean complete `./scripts/pnpm.sh run test:pre-push` gate. Its output SHA-256 is `a8d7e33152fd35de2dda5658204da2b946a6d5bff24f96e90bf41ce5a2cd850f`; all 72 pipeline cases agree. That review covered the projection contract only.

The composed Activity-data preparation prerequisite received a context-cold checkpoint review at `08f075b3acd1bea96b93406177613442299dd68d`, against baseline `a96683eb3bc99de5bb38d63484b73e4bbb509eec`. The isolated reviewer approved with no required or advisory findings after independently checking the proof and helper claims, routed digests, and seven focused TypeScript tests. That target excluded the complete footprint contract and production batching integration.

The composed arming footprint and accepted-publication checkpoint received a context-cold review at `5eae9923646633f47e7ba80b0a2f86c325245dd0`, against baseline `539ec9ac6ba72f17dcf599fd9f6728c0cee48c38`. The isolated reviewer approved with no required or advisory findings after checking predecessor-derived pair laws, Activity-counter invariants and writers, snapshot public-definition preservation and kernel equalities, 27 independent TypeScript checks, and all 14 calibration logs. The regenerated routing packet matched SHA-256 `4a2413274f8f21ba44232578ba5787b1fe53fdf75fbac80e608de5fe514d5c55`. That review excluded production Activity-data batching, arbitrary finite mixed batches, snapshot-declaring execution, and the broader final-closure obligations.

The finite prepared arming checkpoint received a context-cold review at `0a382dc6766a210bd83e27437250b1bad20f9c12`, against baseline `c1a690eb602751bbd5796d5a85a61456bcabccda`. The reviewer required corrections to stale implementation-map claims and overstated permutation-fixture coverage. The same reviewer approved correction target `4dee9ae65c7bf97b91cf64b79f82a6b46f5bd842` with both findings closed and no remaining required or advisory findings. The correction audit also checked the restored ordinary-only TypeScript snapshot path, composed/mixed exclusion, complete-preparation revalidation, and snapshot-refusal precedence; all 37 independently executed focused tests passed. The expanded routing packet regenerated exactly to SHA-256 `6a94b34fe4a84f6bfec41e8b3f1b551f6295a39bc38be57a4ad0fa72d254a6ee`. Lean code and theorem premises are unchanged by the correction. Approval adds no snapshot-path correspondence theorem, regional batching, scheduled choice, or final-closure claim; the complete repository gate remains a separate requirement.

The place-wide token-owner census checkpoint received a context-cold review at `dcb0e79e64a4b002571c5bf0a92dc96ca7c62a67`, against baseline `ec93ba11322879808ab2512ce48e6340e4ecc841`. The isolated reviewer approved with no required or advisory findings after checking the preparation-family matrix, transitive regional ownership, quantified Lean claims, 37 independent TypeScript tests, and all six calibration logs. All 354 archived Lean/build inputs match the immutable source and remain unchanged through the target. The routing packet regenerated exactly to SHA-256 `2c3fb3d0b5159e948a065d39f314f22d74b7b3020569d093cfab8c62f8f1ee3a`. The separate clean complete `./scripts/pnpm.sh run test:pre-push` gate passed at that target with all 72 pipeline cases agreeing; output SHA-256 is `007defaf06e4d81f0113878a681d27a5b3346ef1deba1a575ea68c4d08f0ebbb`. At that target, canonical token storage and producer/regional batch integration remained open.

The canonical token-storage checkpoint received a context-cold review at `e07943d5ceb3bcf3640b6a4719c17adfd8205ad9`, against baseline `48e3a04c96e00ca4a7d6479c640e4f9f15e827a8`. The isolated reviewer approved with no required findings after inspecting the complete-key storage algorithm, quantified premises, preservation consumers, 33 independent TypeScript checks, and all ten calibration records. All 357 archived Lean/build inputs match the immutable calibration source and the target. The packet regenerated exactly to SHA-256 `8088810401b53d5725033f81e22d0fb970e0900c450caaa0d0330108ce74dc2d`. The sole advisory identifies a pre-existing stale composed-footprint sentence in the Semantic Process specification; it does not block this checkpoint.

The clean complete `./scripts/pnpm.sh run test:pre-push` gate passed at that target with all 72 pipeline cases agreeing; output SHA-256 is `4f3f6944e4ca69ec249b1668a38131e2393cb86ecd1cc660b6d4f5cc587c3ded`. A preceding run failed when Node crashed in V8 garbage collection during the managed-wait queue-pressure witness, with the same native crash signature present in local reports predating this change; its output SHA-256 is `489b17e37889f1eac30afb024cfc1449fce8257ae1d7f87de46c06fa6ba4adea`. The isolated witness and all 332 concurrent Temporal checks then passed before the unchanged complete rerun. No runtime flag, dependency pin, or test was weakened, and no native-crash fix is claimed. Producer/regional batching, scheduled choice, and final Internal Commutation closure remain open.

The local-control preparation prerequisite received a context-cold review at `f5a4ecbe463206c54645520abef635ae548111a5`, against baseline `db5de8ca09aa44399d6218508a09e6192bccbdbf`. The isolated reviewer approved with no required findings after inspecting the retained patch and publication contract, quantified token-patch premises, routed claims, and all 27 independently executed local-control preparation tests. The packet regenerated exactly to SHA-256 `cd8dc966688e690167125804ad777c4d92a0a159d29766a6a0136a740516a0a4`. Its sole advisory concerns a literal-to-enum cast in the new preparation owner. Approval covers the prerequisite's stated helper and algebra boundary; complete selected-join dependencies, production local-control batching, and broader preparation, validity, and publication proofs remain open.

The clean complete `./scripts/pnpm.sh run test:pre-push` gate passed at that target on 2026-09-08 with all 72 pipeline cases agreeing; output SHA-256 is `67a52331d91a56d7591d70e7f37043f38da5ba6fb8a65d76c8ecd22d57f0ad8a`. The preceding sandboxed invocation stopped at a denied loopback listener (`EPERM`); the unchanged rerun used the local-service access required by the gate. The contended pipeline exceeded its soft feedback target and is correctness evidence, not a comparable performance measurement.

The advisory's local enum-reference cleanup is non-material: it changes no semantic claim. Type checking, the existing local-control preparation matrix, source-hygiene guards, and the complete semantic-core gate cover that correction.

The selected-join readiness amendment received a context-cold proposal review at `f7b59b08c0441416f036ad0753e6cba88fe18629`, against baseline `5a23ff2f562bda62d5e31cf9168402e061e8afb6`. The isolated reviewer approved with no required findings after independently reproducing both admitted-state counterexamples, dependency-omission mutations, and the different-owner same-place and unrelated-key controls. All 51 existing focused checks passed, and the packet regenerated exactly to SHA-256 `96ee9e0383d88458352e75687813bb9dfe62a3fdc26e96e8e0ad457ff34b761f`. Its advisory corrects the Semantic Process specification's wording to require exactly one ready same-key record, as both selectors already implement. That clarification changed no selected account; the proposal review established no implementation or preservation evidence.

The selected-join dependency checkpoint received a context-cold review at `c37ea46436c43bcb647b0f136294359635be1e30`, against baseline `2b29a0a752bbd16a665720596a4b378fd3c06a1f`. The same reviewer approved correction target `3b6938feab8c5d513a0105e98a5b40c61887469e` with no remaining findings after the required Lean module document and changed-section route were restored and stale prerequisite wording was corrected. All 54 independent semantic-core tests and 71 guards passed; the correction packet regenerated exactly to SHA-256 `ed1c0414888c33652ee8ede10437db04a933b34e557b86bf748154a898759e8c`. Approval covers the dependency helpers and token-patch readiness frames, not complete local-control preparation, runtime validity, accepted publication, or production batching.

The clean complete `./scripts/pnpm.sh run test:pre-push` gate passed at the correction target on 2026-09-08 with all 72 pipeline cases agreeing; output SHA-256 is `28759f157f399f6fcf7b6906ccf7419dd631021d8c810f8a78ddb94b810a571c`. The preceding invocation failed the missing-module-document guard before its library/integration lanes; its output SHA-256 is `3251bbe7363b23be9c0f42a3119a19a561a77d5fcc09b4ed0309a4d4dc76f698`. The corrected run included the shared-type-dependent Lean rebuild and overlapped review. Its pipeline exceeded the soft feedback target under heavy host load; these timings are correctness evidence and not comparable performance measurements.

The selected-record patch and Inclusive declaration-pairing checkpoint received a context-cold review at `565ff325eec927f5367be6fe16d160c578ab70ae`, against baseline `336a7894`. The same reviewer approved correction target `33b2ef7eca2475fe43a091ba0f37a7de28ef3290` with no remaining findings after the required immutable resource evidence and complete gate were supplied. All 360 archived inputs match the measurement source; six measurements remain below the unchanged 3 GiB ceiling with zero controlled events, pressure totals, and swap. All 45 independently executed TypeScript checks passed. The correction packet regenerated exactly to SHA-256 `bec73f48de3a1c5d36e9e39c049a0f3c93d3aff382d441e7318928882a9c10d3`. Approval covers token-patch runtime validity, selected-record algebra, and unique-key declaration admission; complete local-control preparation, selected-record validity, accepted publication, and production batch integration remain open.

The clean complete `./scripts/pnpm.sh run test:pre-push` gate passed at that correction target on 2026-09-08 with all 72 pipeline cases agreeing; output SHA-256 is `525f746f31fdacfc8777ee88290042e7409cdbd97639babe49b46ed409e1692d`. Its timing is contended correctness evidence, not performance calibration.

The local-control proof checkpoint received a context-cold review at `d0aa19657be4403f1057fafdee24def9ff4d7e28`, against baseline `8ef988c65f7d87341ae978f451b1b2f864f8292d`. The isolated reviewer approved with no required corrections after checking complete preparation, arbitrary finite prefix invariants, actual execution agreement, and canonical accepted publication. All 39 independently executed focused TypeScript tests passed. All 394 calibration inputs match the archived, measured, and target sources; ten log digests match, and five bounded cold target builds have zero controlled memory events. The packet regenerated exactly to SHA-256 `46a19817daaaaf51ed6947e230fc601fb2254d3146c2b6f7a591e0bcebd3ca37`. Approval covers this sufficient helper/proof account, not production local-control batching, arbitrary regions, scheduled choice, fuel behavior, or general wire-capacity guarantees.

The sole advisory identifies a pre-existing TypeScript preparation precision gap: duplicate same-key records belonging to another owner emit duplicate readiness dependency atoms. Two valid, projectable runtime states execute the selected join successfully but return no preparation. Dependency atoms represent sets while runtime records retain multiplicity. Correct that helper separately before production integration; no general registered-model reachability or cross-language correspondence theorem is claimed.

The separate advisory correction deduplicates readiness atoms by the existing complete atom identity before footprint validation. Its two separating regressions retain repeated and distinct-input same-key records, compare checked patch application with the actual join, preserve the predecessor, and reject every affected dependency writer. The change is non-material under [the negative case](TESTING-SPEC.md#independent-cold-review-gate): it repairs the existing dependency-set contract without changing operation meaning, runtime validity, public observation, or a proof claim. The 41 focused checks, complete 843-test semantic-core gate, and source-hygiene, canonical-ordering, and operation/commutation census guards pass on 2026-09-09. Production local-control batching remains open.

The clean complete `./scripts/pnpm.sh run test:pre-push` gate passed at the approved target on 2026-09-09 with all 72 pipeline cases agreeing; output SHA-256 is `2ece06317f508aaef4ed918d9577a0d578c5f75fbf9627e80a10b7e5af474d01`. Its 595,708 ms elapsed time is contended correctness evidence, not performance calibration.

The joint mixed local-control production checkpoint received a context-cold review at `caa56e4814c822fb699e7859e8cff627efc8ddc0`, against baseline `c5d0107c94f47e809cfa52eb4b6c33c005fa834b`. The same reviewer approved correction `34e38132efe34edbbbbbb33829a221a1dea3e348` after the required current-contract and runtime-map correction and the pre-existing composed-footprint advisory were closed. No implementation defect or remaining finding was reported. All 73 independently executed focused tests and ten correction-audit documentation/link checks passed. The six extracted definition blocks are byte-identical, both relevant import closures exclude trace, and all 394 archived inputs and nine calibration digests match. The original packet regenerated to `cece511c18b6a6b5106da10e1f652d0e4b4a1c59a81dd4c5cee1ab82a6b4054b`; the bounded correction packet regenerated to `62fea2e50cd3f9328b8cd932ccf499b53258da8102ffdf3e540082c3abe7a687`. Approval covers the implemented mixed production boundary. The clean complete `./scripts/pnpm.sh run test:pre-push` gate passes at `9fddbacaebeb6ae107fe6f2437e7e84af9b0da89`, with all 72 pipeline cases agreeing; [the verification chronology](CAPSULE-COST-LEDGER.md#mixed-local-control-production-verification) retains its output digest and earlier failed attempts. No regional, scheduled-choice, registered-XML, general fuel, or new Temporal-refinement claim follows.

The canonical scope and counter-storage checkpoint received a context-cold review at `da9657e0485e0cfc1b168bc6524ab8127524e9c6`, against baseline `669f2bf9e4bf63a0f9bb5bed4fdf9f1750ed398a`. The isolated reviewer approved with no required or advisory findings after checking complete-key storage, quantified collection hypotheses, unchanged preservation contracts, and ordering-only fixture corrections. The root executed the reviewer's 41 focused TypeScript checks and opposite-order actual-writer probes to coordinate shared build outputs; the reviewer independently inspected the successful receipts. All 396 archived inputs and twelve calibration records match the immutable source and target. The packet regenerated exactly to SHA-256 `ccc835da7086356dad42c67643382fcf4c21f32fee00f3c4f0066e0f808867db`. Approval establishes neither general regional transition preservation nor cross-language predicate equivalence, source reachability, scheduled choice, or final closure.

The separate clean complete `./scripts/pnpm.sh run test:pre-push` gate passed at that exact target on 2026-09-18, with all 72 pipeline cases agreeing and `dirty: false`. Its separately asserted receipt reports success; output SHA-256 is `ce0bcbc2a4adbdd9122fa9574b13c961fc1e7bfd924e32f3d6c95d72fed0c5a8`. Total elapsed time was 571.48 seconds; this run is correctness evidence, not an uncontended performance baseline.

The TypeScript scope-creation prerequisite received a context-cold checkpoint review at `fc5fc6044a4c89b017026b8804ddb9e8a23b3d27`, against baseline `6526eb990ac90423ec1a52cc9c2edf866c9392a9`. The same reviewer approved correction `d8661a201772a76081097ba68ad9a4b7ec7f8801`, which removes one surplus fixture blank line and passes the complete baseline-to-correction whitespace check. No semantic or advisory findings remain. The root ran the reviewer's 23 focused tests to coordinate shared build outputs; the reviewer independently checked their exact-target receipt, retained patch/runtime and public-projection agreement, identity/provenance refusal, and unchanged fixture extraction. The packet regenerated exactly to `662f33b3119b6c236963746ee1388e48281c48d82fb63cd7411d467dca2c014a`. Finite constructed sibling witnesses establish neither general preservation nor source reachability; Lean preparation laws and production regional batching remain open.

The clean complete `./scripts/pnpm.sh run test:pre-push` gate passed at `fc5fc604` on 2026-09-18 before the whitespace-only correction, with all 72 pipeline cases agreeing and `dirty: false`. Its separately asserted receipt reports success; output SHA-256 is `6072c39c5d099fec5c7ccd6faf08c8872337820d42d8ef2841bd46c6db1ca00f`. Total elapsed time was 566.03 seconds; it is correctness evidence, not a performance baseline.

The Lean scope-creation selection and retained-patch refinement checkpoint received a context-cold review at `3b19a96dbb45a74f2d28dfc3424567a40395b97a`, against baseline `2ce2eb5eb9123b2403b0d0f6b59ca0e3b4dfafa9`. The isolated reviewer approved with no required or advisory findings after checking the quantified refinement premises, unchanged raw evaluator bodies, identity/counter/token discriminators, and immutable resource evidence. The root ran both requested narrow Lean builds to preserve serialization; the reviewer independently inspected and asserted their exact-target receipts. All 398 archived Lean/build inputs match the implementation and reviewed target. The packet regenerated exactly to `149cb99eefe8f521c8420ffa919a9528f19404a43f7279d881ba5d7d8f11067b`. Shared helper agreement establishes neither independent BPMN correctness nor complete preparation, aggregate validity, commutation, accepted publication, or regional batching.

The clean complete `./scripts/pnpm.sh run test:pre-push` gate passed at that exact target on 2026-09-18, with all 72 pipeline cases agreeing and `dirty: false`. Its separately asserted receipt reports success; output SHA-256 is `826d2803ba877f1190f7f36952a89d7250447e0ffeefbc4c6b555a4a235ee461`. Total elapsed time was 542.30 seconds; it is correctness evidence, not a performance baseline.

The Lean complete scope-creation preparation checkpoint received a context-cold review at `c3510b4893453b59b7db6273853c4a0c0392cefc`, against baseline `d7e911189ceaa4741ce3dab86a2a0733d0fad27c`. The isolated reviewer approved with no required or advisory findings after checking predecessor refinement, identity/provenance refusals, complete artifact revalidation, typed dependency ordering, the unchanged TypeScript boundary, and the explicit proof exclusions. The root ran both requested focused Lean builds to preserve serialization; the reviewer independently inspected and asserted their exact-target receipts. All 402 archived Lean/build inputs match the implementation and reviewed target, and all nine cold-target resource records were independently checked. The packet regenerated exactly to `8b40df8f26def5dd19ea29906b6a1f70fd112476b50a6491a1c63e2ced16d350`. Aggregate validity, complete preparation frames, commutation, accepted-publication correspondence, and production scope/region batching remain open.

The clean complete `./scripts/pnpm.sh run test:pre-push` gate passed at that exact target on 2026-09-18, with all 72 pipeline cases agreeing and `dirty: false`. Its separately asserted receipt reports success; output SHA-256 is `0d533feb376fc1c133a4fca762e2584831a723592451ea673652ee480c3ae076`. Total elapsed time was 539.66 seconds; it is correctness evidence, not a performance baseline.

The Lean fresh-ownership and bounded Call-reachability prerequisite received a context-cold review at `8e6fe02349478ace303d821d4848c00fe0eb3d4f`, against baseline `fcbd1a38ca7512acfc62e11a8b80567f65b6c651`. The same reviewer approved correction `aef2d619468d0aa3a420b1260d4619b2fdad1e76`, which updates fourteen cost-table denominators after the new measurement; no semantic or advisory findings remain. The root ran the requested focused Lean build to preserve serialization, and the reviewer independently asserted its exact-target receipt, checked all 405 archived Lean/build inputs, and regenerated the packet byte-for-byte to `dbb9123fb36a62f9db7d11591dff178c7807d1d1ac97b728cca21ef22297365a`. Aggregate validity, complete preparation frames, commutation, accepted publication, and production scope/region batching remain open.

The clean complete `./scripts/pnpm.sh run test:pre-push` gate passed at the correction target on 2026-09-18, with all 72 pipeline cases agreeing and `dirty: false`. Its separately asserted receipt reports success; output SHA-256 is `814672eb1ef6a4c4459a533a216b278b3bd44a4b19aa475bbf2d571ebdbb1b45`. Total elapsed time was 512.22 seconds; it is correctness evidence, not a performance baseline. The preceding invocation stopped at the stale-denominator guard before the remaining verification lanes.

The Lean Call-association preservation, admission, and sorting prerequisite received a context-cold review at `00210a781876b839537544ace1b5cf4aefe2bf13`, against baseline `598f736537a73fe8fa718990e972fed8deec5d95`. The isolated reviewer approved with no required or advisory findings. The root ran the requested three-module focused build to preserve serialization, and the reviewer independently asserted its exact-target receipt, checked all 408 archived Lean/build inputs, reproduced the cost row, and regenerated the packet byte-for-byte to `bbb08a0261e7ebf0d6bc69645454abf2f3f2ce1972bfca7410ae7966f0d8a2b6`. The private proof characterization is equivalent to the existing predicate; preservation assumes no successor validity. This establishes preservation of the existing Lean account, not independent BPMN correctness. Aggregate validity, complete scope-position and Compensation preservation, commutation, accepted publication, paired-operation binding of every retained Call record, and cross-target correspondence remain open.

The clean complete `./scripts/pnpm.sh run test:pre-push` gate passed at that exact target on 2026-09-18, with all 72 pipeline cases agreeing and `dirty: false`. Its separately asserted receipt reports success; output SHA-256 is `d27b14f8f7c42c00b1740a97829369bb9603b4f01868467cc275f616037ac722`. Total elapsed time was 509.85 seconds; it is correctness evidence, not a performance baseline.

The Lean scope-creation position-preservation checkpoint received a context-cold review at `20fece845df5a55f6132e9a8f3c3727971529584`, against baseline `d17b0bd34e8b3dab7f1a9c83eeb7d0b7bd39f3e0`. The isolated reviewer approved with no required or advisory findings after checking the quantified predicate equivalence, actual preparation relation, and admitted positive and negative witnesses. The root ran the requested two-module focused build to preserve serialization; the reviewer independently asserted its exact-target receipt, checked all 410 archived Lean/build inputs and the bounded resource record, reproduced the cost row, and regenerated the packet exactly to `2d01cf379b8d8106765f768cf874faedeff06ad409f256681466d1e1c7fd4b4d`. Preservation of the existing Lean account establishes neither independent BPMN correctness nor source reachability. Aggregate validity, Compensation/incident preservation, cross-target correspondence, commutation, accepted publication, and regional batching remain open.

The clean complete `./scripts/pnpm.sh run test:pre-push` gate passed at that exact target on 2026-09-18, with all 72 pipeline cases agreeing and `dirty: false`. Its separately asserted receipt reports success; output SHA-256 is `44389f71a56c8861ff2a7a43a45efd34ad4670e14fb5a597bd54ce8eb0ca8d67`. Total elapsed time was 507.55 seconds; it is correctness evidence, not a performance baseline.

The Lean scope-creation aggregate-validity checkpoint received a context-cold review at `4f71f86132fda40182759e08b985002307526d4d`, against baseline `23e6b81d65f9ac949cd4adbb23152772e350a2aa`. The isolated reviewer approved with no required findings after checking every aggregate conjunct, actual preparation premises, exact Compensation owner distinctions, and nonempty witnesses. The root ran the requested two-module focused build to preserve serialization; the reviewer independently asserted its exact-target receipt, checked all 413 archived Lean/build inputs and the bounded resource record, reproduced the cost row, and regenerated the packet byte-for-byte to `5eb7e4b43cabd3e63a0cef49f767f941b99a3e48dc3054839f2d187d505e0d0c`. The sole advisory was stale pending-calibration wording in PLAN and the invariant map; this status record reconciles both. The theorem preserves the existing predicate's disclosed identity-bound and Call-declaration coverage. It establishes neither independent BPMN correctness nor cross-target correspondence, source reachability, complete preparation frames, commutation, accepted publication, or production batching.

The clean complete `./scripts/pnpm.sh run test:pre-push` gate passed at that exact target on 2026-09-18, with all 72 pipeline cases agreeing and `dirty: false`. Its separately asserted receipt reports success; output SHA-256 is `93d9b70b9029186ec58d0d5ff43dc424637b33666e8bc5b358184866c29b3c9f`. Total elapsed time was 506.75 seconds; it is correctness evidence, not a performance baseline.

The Lean scope-creation preparation-frame checkpoint received a context-cold review at `b8213b4106b2e0b62ca9ecf352ed637d001196fb`, against baseline `f111c37ce0c5c837092ac6dc26cb267f3337ff93`. The isolated reviewer approved with no required or advisory findings after checking exact complete-preparation preservation for all four child/Call pairings from unchanged predecessor premises. The root ran the requested two-module focused build to preserve serialization; the reviewer independently asserted its exact-target receipt, verified all 417 archived inputs and bounded resource evidence, reproduced the cost row, and regenerated the packet byte-for-byte to `c676ccc01306eb35b5417e2645a2a40976f57e831ec7ba9b5714c297443f6ce0`. The constructed witnesses satisfy structural and runtime premises while explicitly failing profile admission. Preservation of the existing Lean account establishes neither independent BPMN correctness nor source reachability, cross-target correspondence, commutation, accepted publication, or production batching.

The clean complete `./scripts/pnpm.sh run test:pre-push` retry passed at that exact target on 2026-09-18 in 543.49 seconds, with all 72 pipeline cases agreeing and `dirty: false`; its separately asserted output SHA-256 is `52121d961663e5c537268b3b46afcad84e5ae40cb7aadb5c55c04f36a128a919`. The preceding full invocation failed the parallel Multi-Instance natural-recovery timing margin at 2902 ms of a 5000 ms Timer, exceeding the unchanged 50% limit; its output SHA-256 is `0a46035dcbc8e6fcb8c8a19f5bcd3475b1732960482be567797845642eb10953`. The isolated witness then passed in 17.66 seconds before the complete retry, with output SHA-256 `ffd5fe2fec4f97a0c96248252c576169b996761352b08afd43a7cdfbb8dc2525`. No runtime source, dependency, flag, Timer, or threshold changed. These receipts establish correctness for the successful runs, not a timing-defect fix or performance baseline.

The Lean scope-creation complete-state commutation checkpoint received a context-cold review at `cdada7eeda18b0cbca2c91a01f50287602f0f8ed`, against baseline `0b8a5544d9d3c375dd77a84e9dfa8cbf400fd5e1`. The isolated reviewer approved with no required or advisory findings after checking all four child/Call pairings, complete-state equality, derived preparation/validity conclusions, and the partial-key Call comparator's payload distinction. The root ran the three-module focused build to preserve serialization; the reviewer independently asserted its exact-target receipt, verified all 419 archived inputs against the measured and reviewed commits, checked bounded resource evidence, reproduced the cost row, and regenerated the packet exactly to `04f2b2a4ed6fa16e2f5db57325bcc6a0bbec91110c67b2d8c77d5a1b107ede7b`. The constructed fixtures establish structural/runtime premises and explicitly reject profile admission. The existing Lean account and predicate coverage remain the common basis; independent BPMN correctness, cross-target correspondence, source reachability, accepted publication, production batching, and final closure are not established.

The clean complete `./scripts/pnpm.sh run test:pre-push` gate passed at that exact target on 2026-09-18 in 537.80 seconds, with all 72 pipeline cases agreeing and `dirty: false`. Its separately asserted output SHA-256 is `d7773c341542b713e872dbaa22ea5f3138b4061610a37d27dfd878bfecd5e11f`. This is correctness evidence, not a performance baseline.

The Lean scope-creation control-position publication checkpoint received a context-cold review at `f9d1e91084a6b84a8c5adf886616c2e7e7d26836`, against baseline `f6b99ac688a696f5d29e2621b1d69c7c0bbf8447`. The isolated reviewer approved with no required or advisory findings after checking predecessor-derived actual position correspondence, token multiplicity and distinct owners, and complete scope payload insertion. The root ran the three-module focused build to preserve serialization; the reviewer independently asserted its exact-target receipt, verified all 421 archived inputs and bounded resource evidence, reproduced the cost row, and regenerated the packet exactly to `5eb39a90ef3a561f489a5466ade22e04e86ee1521520065648bd07bbe8298eef`. Constructed witnesses establish structural/runtime premises while explicitly rejecting profile admission. The existing Lean preparation, validity, and projection account remains the common basis; independent BPMN correctness, source reachability, cross-target equivalence, accepted lifecycle publication, production scope/region batching, and final closure are not established.

The clean complete `./scripts/pnpm.sh run test:pre-push` gate passed at that exact target on 2026-09-18 in 547.26 seconds, with all 72 pipeline cases agreeing and `dirty: false`. Its separately asserted output SHA-256 is `5379513747967ada44b3a146ae74fb5feeec9c104498fa2c758e4771e121fe75`. This is correctness evidence, not a performance baseline.

The Lean scope-creation accepted-lifecycle publication checkpoint received a context-cold review at `026c690f249064f360fa92cb5a364b4382b12bcb`, against baseline `be60e283089bbbac629c25cc5757d5b48ebccf3a`. The isolated reviewer approved with no required or advisory findings after checking predecessor-derived actual acceptance, both independent execution orders, complete payload preservation, private boundary-Timer classification, and adversarial start witnesses. The root ran the requested two-module focused build (161 jobs) and 42 focused guards; the reviewer independently verified their successful exact-target receipts, all 426 archived inputs, bounded resource evidence, and the packet digest `a78992bb134688767f38255af309ab73416682a9a03b319e7d077427e8c5ed9d`. Constructed witnesses satisfy structural/runtime premises while explicitly failing profile admission. The shared Lean preparation, validators, and projection account remain the basis; independent BPMN correctness, source reachability, cross-target equivalence, general wire capacity, production scope/region batching, scheduled choice, and final closure are not established.

The clean complete `./scripts/pnpm.sh run test:pre-push` gate passed at that exact target on 2026-09-19 in 522.07 seconds. Its terminal, separately asserted receipt reports `exit 0` with output SHA-256 `d4457a6c71cf50099b4a5aa4f83eaf80978eb3def082bf314aa157855dab834b`. This is correctness evidence, not a performance baseline.

The scope-creation mixed-batch checkpoint received a context-cold review at `299eea89a73c54c50491c50a1091f5e4646bfb74`, against baseline `7613956c60ccc8c4f869404e61df04a6f3c25f5c`. The reviewer found no implementation defect and required the omitted Semantic Process contract and proof descriptions to be updated. The same reviewer approved correction `bd1ac87bd4a0596fe8a96684d0bc6d0f5374ad3d` with no remaining findings. Original and correction packets regenerated exactly to `e88286fded3aa9c88b928b17286d328dd38aae4d10275b1c14199dde5c9163e2` and `b6ea834207b3ca3daf13a7ffade1db6802524c47ef2549e8c5ef9c786c40fd23`.

The reviewer independently checked 94 requested TypeScript tests, the three-module Lean gate, all 430 archived inputs, and both bounded measurements. The correction changed only documentation; 557 infrastructure tests passed before its commit and 10 requested documentation tests passed at its exact target. Prior executable and resource evidence therefore remains applicable. Mixed witnesses establish structurally valid internal Programs outside profile capability admission; registered-source reachability, cross-target equivalence, general fuel and wire-capacity theorems, regional batching, scheduled choice, and additional Temporal refinement remain unestablished.

The clean complete `./scripts/pnpm.sh run test:pre-push` gate passed at `299eea89` on 2026-09-20 in 534.98 seconds with `dirty: false`. Its separately asserted output SHA-256 is `12ee463dca275eebb8f96649e21ccf6098d95f8225d28704e12ceeb2431b0e94`. This complete implementation gate and the correction-target documentation checks are distinct evidence. Grouping the mixed proofs, dispatcher, and witnesses avoided helper commits; the review correction exposed a missed same-change contract owner, so the next unit checks that existing owner list before committing. This is correctness evidence, not a performance baseline.

The Message projection correction received a context-cold semantic-checkpoint review at `8c5b28189c76a8c1caf515c5396fac2d4ef91096`, against baseline `ec217b5095fb6e0cc4aab2e77d71d91001fa5c15`. The isolated reviewer approved with no required findings after checking independent population validation, the quantified empty-population and record-binding laws, and unchanged Complete/Return acceptance conclusions. Root-executed reviewer probes passed both complete Lean negatives and all seven TypeScript occurrence checks. The reviewer reproduced packet `4e6271f6cd65c98d1269665e594a0860bc8e69ca97f2fc78c275b2cb47c50d2e`, all seven output digests, all 513 archived inputs against the snapshot and target, and the combined resource receipt. This approval covers `MBP-EMPTY-TASK-01`, not regional closure or general cross-target domain equality.

The clean complete `./scripts/pnpm.sh run test:pre-push:verify` gate passed at that exact target on 2026-09-20 in 565.882 seconds, with all 72 pipeline cases agreeing and `dirty: false`. The separately asserted receipt is `/tmp/bpmn-message-projection-clean-integration`; output SHA-256 is `8a6d88071671cc5d647dccaa41fcc44034f7d281f85f98f5a0807a135fb92ed0`. Focused proofs, the full 482-target Lean gate, 1087 semantic-core checks, all 572 infrastructure checks, and the isolated 3 GiB measurement passed before the implementation commit. The integration gate overlapped review and is correctness evidence, not a performance baseline. [The cost record](CAPSULE-COST-LEDGER.md#message-boundary-projection-correction-2026-09-20) preserves the failed pre-commit attempts and reflection.

The handler-publication correction received a context-cold semantic-checkpoint review at `40ce8c00299c923a9a2682321c27f6feac803284`, against baseline `2e4197146e5423d462fbd66adb6102edd61e79a5`. The isolated reviewer approved with no required findings, independently checked 22 TypeScript tests, and verified all 513 archived inputs and bounded resource evidence against the immutable target. Packet SHA-256 is `9d5e4878ed5118f933c239fc6518abdddac66b75034de8e5e06b6b8f15e32c1d`. Approval covers `RHP-HANDLER-01`: original quantified preparation assumptions, exact attached-handler withdrawal, canonical endings, and private-Timer exclusion remain intact. Retained associations are producer-derived; the finite witnesses establish neither registered-profile reachability nor general cross-target equivalence or regional closure.

The clean complete `./scripts/pnpm.sh run test:pre-push:verify` gate passed at that target in 502.519 seconds, with all 72 pipeline cases agreeing and `dirty: false`. The asserted receipt is `/tmp/bpmn-handler-clean-integration-after-v8-diagnostic`; output SHA-256 is `5cf868c0af66c99ae2fccab7eafb22abaf455607f81a2200cc4f68ab6de06da4`. The earlier failed integration attempt, native V8 crash evidence, unchanged-settings diagnostic, and cost reflection remain in [the cost record](CAPSULE-COST-LEDGER.md#regional-handler-publication-correction-2026-09-20).

The reviewer separately identified a pre-existing `AOO-JOIN-03` violation: [Lean actual Timer publication](../BpmnSemantics/SemanticProcess/FlowNodeOccurrenceLifecycle.lean) joined Task and Timer activation ordinals in `taskForBoundaryTimer?`, contrary to the [stable ownership rule](ACTIVITY-OCCURRENCE-OWNERSHIP-SPEC.md#stable-semantic-rules). The subsequent correction and its own review boundary are recorded under the [regional prerequisite](#regional-preparation-prerequisite). This advisory and `AOO-RETAINED-BODY-01` are separate from the approved handler-withdrawal correction.

The actual Timer-publication correction received a context-cold semantic-checkpoint review at `0ba40aef650efc1294d113a8d36f62acad2a3d2b`, against baseline `faf33f52167f8581de873afd6f3a11264cdb5c27`. The isolated reviewer approved with no required edits, independently passed 31 focused tests, and verified all 513 archived inputs, five gate-output digests, and bounded resource evidence. Packet SHA-256 is `974fc0d5d066172f7d6b693007fe66b24879ddfdc0947ec820ba2910d8729d13`. The sole advisory was a comment describing the superseded Lean guard exemption; this acceptance record corrects that historical wording. Approval covers exact record-bound accepted publication for the two divergent-counter witnesses, without a general preservation, registered-schedule reachability, new admission, or regional-closure claim.

The clean complete `./scripts/pnpm.sh run test:pre-push:verify` gate passed at that exact target on 2026-09-21 in 512.136 seconds, with all 72 pipeline cases agreeing and `dirty: false`. Its asserted receipt is `/tmp/bpmn-timer-pairing-clean-integration`; output SHA-256 is `7b3d57745ccd305dfe040fccc1552674b306af1ccfdf27365d672a2fb98954a2`. Focused, complete affected-package, and cold resource gates passed before the implementation commit; clean integration overlapped the independent review. [The cost record](CAPSULE-COST-LEDGER.md#actual-timer-publication-correction-2026-09-21) retains pre-commit failures and reassessment. Activity-record retirement was the next separate obligation, reviewed below.

The Activity-retirement correction received a context-cold semantic-checkpoint review at `44ae1775bfabf62a46a082b2f20251859e1bcda5`, against baseline `441b740078fa1c850abea26a453eb146e85fdf33`. The isolated reviewer approved with no required or advisory findings, independently passed 32 focused tests, and verified all 513 archived inputs, six gate-output digests, both resource receipts, and their controlled measurements. Packet SHA-256 is `0c04dde2a84125007da6d70b2054c6cdc525fd82ab47263da7f21c99605a9ea5`. Exact Task-body retirement and tagged Timer withdrawal preserve the original transition premises and evaluator soundness; all eight added writer sites have applicable evidence. Whole-command validity is established for the original fixtures. The additional frame controls establish neither validity nor reachability; general preservation, cross-target domain equality, broader handler withdrawal, and regional closure remain unproved.

The clean complete `./scripts/pnpm.sh run test:pre-push:verify` gate passed at that exact target on 2026-09-21 in 508.031 seconds, with all 72 pipeline cases agreeing and `dirty: false`. Its asserted receipt is `/tmp/bpmn-activity-retirement-clean-integration`; output SHA-256 is `25be3535ff9e2ce4a519f505474b125b6d51c76385eb460dbe6373a9433fe8a7`. The full Lean, complete core, infrastructure, and both cold resource checks passed before the implementation commit; clean integration overlapped independent review. [The cost record](CAPSULE-COST-LEDGER.md#activity-record-retirement-correction-2026-09-21) retains pre-commit failures and reassessment. Regional trial closure remains pending.

The private Timer alias correction received a context-cold semantic-checkpoint review at `df253c4ec199ada57465344d09d6b9b0fe9f621e`, against baseline `cd2c28e9011c4a4adca9c4ba558f89e4cd9e3e83`. The reviewer independently passed 14 focused tests, verified all 514 measured inputs and five gate-output digests, and found no semantic implementation defect. The sole required edit replaced a log-only hash with the complete resource-receipt digest and corrected its description. The same reviewer approved correction `5bc50736abb7f1b347edde87efcb445a1b3f3e9b` with no remaining findings. Original and correction packets regenerated exactly to `9b6537d0461b7b4596a9305f38e259637a8db98bd430c5005c574245c7f69d97` and `09e94606f397e7e959b53741e5bcbc3186af14f3228ef68e85734d298725ae16`. Approval covers `RHP-PRIVATE-01`; the constructed witnesses establish neither profile reachability nor general cross-target equivalence or regional closure. Prepared and actual publication share selectors, and retained associations originate in producer records.

The clean complete `./scripts/pnpm.sh run test:pre-push:verify` gate passed at the original target on 2026-09-21 in 520.415 seconds, with all 72 pipeline cases agreeing and `dirty: false`. Its asserted receipt is `/tmp/bpmn-private-alias-clean-integration`; output SHA-256 is `33253646e65f764703d75174b96a8dc9ce20bc5ae2cd4aa2db9dd1bbff2ef13b`. The full Lean, complete core, infrastructure, and cold resource gates passed before implementation was committed; integration overlapped review. The metadata correction passed all 573 infrastructure checks before commit and all 20 cost checks at its committed target. All measured and executable inputs remain identical, so prior semantic and resource results remain applicable. [The cost record](CAPSULE-COST-LEDGER.md#private-timer-alias-correction-2026-09-21) retains failures and reassessment; regional trial closure remains pending.

The Error/Terminate accepted-publication checkpoint received a context-cold semantic review at `5c3570c70cdb21e2bd98a630062b467160e747eb`, against baseline `7d0e0a5358b17db5c02a6d23f7c55bd245956ad6`. The isolated reviewer approved with no required or advisory findings after checking the complete capsule, quantified hypotheses, actual successor projections, canonical filtering, and 37 independently executed focused tests. The packet regenerated exactly to `c13e7a4497f18cf3cd0b0f2513bc40b175bd722daed4f81ec105244846d302d3`. The root executed the reviewer's joint-hypothesis probe at the target: hosting-root Terminate and both private-Timer alias controls satisfy preparation and predecessor validity together. Both theorem axiom reports contain only `propext`, `Classical.choice`, and `Quot.sound`. Its asserted receipt is `/tmp/bpmn-cancellation-acceptance-review-probe`. No executable reduction body or kernel fixture changed; historical resource receipts are not measurements of the new proof owners.

The clean complete `./scripts/pnpm.sh run test:pre-push` gate passed at that target on 2026-09-21 in 508.772 seconds, with all 72 pipeline cases agreeing and `dirty: false`. Its asserted receipt is `/tmp/bpmn-regional-cancellation-acceptance-clean-integration`; output SHA-256 is `9625bdf70fa8ed1d47df365004cf03d0425984a1655010a3459eb9fda53d5db3`. The full Lean/core and infrastructure gates passed before commit; integration overlapped review. The pipeline exceeded its soft feedback target under rising host load, so this is correctness evidence, not a performance improvement. [The cost record](CAPSULE-COST-LEDGER.md#error-and-terminate-accepted-publication-2026-09-21) retains failures and reassessment. Approval covers individual lifecycle acceptance only; aggregate preservation, complete preparation frames, pair commutation/publication, regional batching, and regional trial closure remain open.

The Error/Terminate aggregate runtime-preservation checkpoint received a context-cold semantic review at `0f4e8a51b42e8bda6faf1c4fff0642cef6a91210`, against baseline `cdd5bc11813e1e10e4ecc80bc8af67e075fd2594`. The isolated reviewer approved with no required or advisory findings after checking the complete capsule, quantified hypotheses, actual successors, Compensation cases, canonical multiplicity, and 29 independently executed focused guards. The packet regenerated exactly to `a090dd4fbc6660fb882f8327da20c527d6e5db3d671086f9538b820374c8b778`. The requested exact-target probe passed and was independently asserted at `/tmp/bpmn-cancellation-runtime-review-probe`: root/child Terminate and private-Timer controls jointly satisfy preparation and runtime validity; incorrectly purging the required Compensation register fails. The nonempty Compensation control checks its named predicates, not complete preparation or source reachability. Both aggregate theorem axiom reports contain only `propext`, `Classical.choice`, and `Quot.sound`.

The clean complete `./scripts/pnpm.sh run test:pre-push` gate passed at that target on 2026-09-21 in 500.552 seconds, with all 72 pipeline cases agreeing and `dirty: false`. Its asserted receipt is `/tmp/bpmn-cancellation-runtime-clean-integration`; output SHA-256 is `d2f91f80cb1a5e44ea03d95653a4a237566d4411eeb02b68524ab02b87721767`. All 492 Lean targets, 1089 core tests, and 573 infrastructure checks passed before commit; integration overlapped independent review. The pipeline exceeded its soft feedback target, so this is correctness evidence, not a performance claim. [The cost record](CAPSULE-COST-LEDGER.md#error-and-terminate-runtime-preservation-2026-09-21) retains failures and reassessment. Approval preserves the existing Lean account without independently establishing BPMN correctness or cross-target equivalence; other families' aggregate validity, preparation frames, pair laws, batching, and regional trial closure remain open.

The Complete/Return aggregate runtime-preservation checkpoint and retained Activity-owner correction received a context-cold semantic review at `f725745d464719f2f429813cfb311915ab88ab32`, against baseline `f49f6fd7e427f8fa30fa2ecb9402cc27d8a3de2b`. The isolated reviewer approved with no required or advisory findings after checking actual successor validity, the existing ownership authority, Compensation cases, bidirectional Multi-Instance bindings, and canonical multiplicity. All 85 distinct independently executed guards and ownership/preparation/retention-frame checks passed. Packet regeneration matched `18b0d2a2a31b518859321efd281e4889f9e451c0786aa14a4fc9a316dbc9f70e`; the reviewer verified all six packet output hashes, both complete resource receipts, and all 530 archived inputs against the snapshot and target. The unchanged requested probes passed at that exact target, with separately asserted receipts `/tmp/bpmn-return-completion-review-probe` and `/tmp/bpmn-child-completion-review-probe`. They cover joint preparation/validity controls, owner-versus-body survival, terminal retention cleanup, and separating refusals; the nonempty Compensation execution control establishes its named predicates rather than full preparation. Aggregate theorem axiom reports contain only `propext`, `Classical.choice`, and `Quot.sound`.

The clean complete `./scripts/pnpm.sh run test:pre-push` gate passed at that target on 2026-09-21 in 503.253 seconds, with all 72 pipeline cases agreeing and `dirty: false`. Its independently asserted receipt is `/tmp/bpmn-completion-clean-integration`; output SHA-256 is `09bb8d8dcb865e0367667f5e7a0f6abb4481335b998bffd3a75a4e29af63bc56`. All 499 Lean targets, 1091 core tests, 573 infrastructure checks, and both affected fixed-3-GiB measurements passed before commit; clean integration overlapped review. The pipeline exceeded its soft feedback target, so this is correctness evidence, not a performance claim. [The cost record](CAPSULE-COST-LEDGER.md#complete-and-return-runtime-preservation-2026-09-21) retains failures and reassessment. Approval covers individual preservation of the existing Lean account and the owner correction; constructed probes establish neither profile reachability nor general cross-target equivalence. Complete preparation frames, pair laws, production regional batching, `AOO-RETAINED-BODY-01`, and regional trial closure remain open.
