# Temporal test evidence map

## Status

Implemented living evidence map.

This document is the feature-by-feature lookup for the witnesses, mutations, durable-history checks, and replay coverage exercised by [the focused Temporal gate](TESTING-SPEC.md#current-temporal-gate). The testing specification owns the command, gate-selection policy, calibration lane, pre-release history policy, and release requirements. This map records what the gate covers; current implementation remains in the [`implementation-status-owner:TEMPORAL-HOSTING`](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md), and latest measured status remains in [PLAN.md](PLAN.md).

## Temporal witness and mutation inventory

The registered Terminate End profile is part of this gate through its exact source, passive User Task Update hosting, containing-scope cancellation, Worker replacement, history, mutation, and replay witnesses.

The gate starts fresh in-memory Temporal servers, compiles exact BPMN before Workflow start, and runs the semantic-lifetime Workflow over the retained sequential, Boolean User Task completion, Message Start, Timer Start, parallel, Intermediate Catch Timer, Timer/User Task composition, Intermediate Catch Message, direct-Message Receive Task, Service Task, configured Task effect, data-mapping, boundary-error, Simple Boolean Exclusive Gateway, structured Inclusive Gateway, Event-Based Gateway, resumption-bounded User Task cycle, ordinary embedded Sub-Process, and Sub-Process Error-propagation probes. It checks separate semantic and host-capability admission with typed pre-start rejection, the shared Workflow-safe typed-tuple encoder, fixed existing Process-address/Update/timer encodings and digests, deterministic SHA-256 known-answer vectors across padding boundaries, supplementary-plane UTF-8, a multi-block native-crypto cross-check outside Workflow code, content-bound Update and timer command IDs, collision-resistant Process-address Workflow IDs, duplicate logical delivery, accepted-handler draining, typed adapter lifecycle results, chain-relative recovery, Query evidence reconciliation, closed v1 terminal receipts, private recovery entries, durable timer and Message Signal history, and replay before cleanup. Product 1 starts carry the exact production Initial host envelope and expose no SDK handle; testkit reacquires handles privately by Workflow ID. The ordinary disposable test host compiles its Workflow bundle once per Node process and passes that exact bundle to initial Workers, replacement Workers, and replay Workers; a focused concurrent-request guard prevents duplicate builds, and successful Webpack progress is suppressed while compilation errors remain visible. The production external runtime and each bypass mutation retain separate bundle ownership because they are different consumers or deliberately different Workflow programs. A focused product-runtime witness loads the maintained accepted command config, starts the server only as test infrastructure, points the production command orchestration at its published address, polls an explicit caller-owned task queue, starts the compiled Process, reports the stable canonical wait, reads one exact selected-input User Task detail, observes that the same sole task remains active across a real host delay, submits configured form values through Update ingress, and reports the completed receipt before orderly shutdown. Dependency-free command tests prove exact config validation, pnpm argument forwarding, typed unsupported-model rejection, and zero connection attempts on source rejection. Pure actor tests refuse zero/multiple/changing tasks and malformed or widened configuration. The production runtime, command, and dummy actor never start a server or bind a port.

The sequential exact completion submits canonical string/null Process-variable bindings, requires the committed result, terminal Query state, and completed receipt to retain the same merged bindings, and replays the history. The sequential stale schedule awaits the completed receipt before submitting the distinct stale command. The gate requires CIB Seven, Lean, and the pure core to retain exact semantic rejection, Temporal to agree exactly through semantic completion, and the adapter to return `processClosed` separately. The parallel live-sibling schedule completes A and then repeats A while B remains active, requiring exact four-target semantic rejection. A retained concurrent same-occurrence race goes through production ingress and requires exactly one committed semantic result. If both requests are accepted, the other result is semantic rejection and both final observations are identical. If Workflow closure wins the acceptance race, the other result is `processClosed` carrying the same completed receipt. `processUnknown` fails the harness. Complete User Task Update identity includes the entire canonical submitted patch, so reuse under changed data reaches the identity-conflict boundary. A separately bundled completion-data bypass fabricates the final variables outside the core and omits the core command result; durable Query/Update reconciliation rejects it even though the terminal state alone was forged to match.

The registered Boolean completion witness reuses the same User Task Update and starts with string/null Process data. It replaces the Worker while the task wait is active, submits tagged Boolean `true`, and requires the Update request in Event History, committed Query state, closed terminal state, and replay to retain the primitive value. The Update result and private recovery entry remain the existing command ID/digest and semantic outcome only. A second execution under the old profile rejects the same Boolean with exact wait/state preservation and then accepts a valid string completion. Test-owned stringification and outside-core binding mutations fail the durable relation, while the history contains no new Signal, Timer, Activity, Child Workflow, cancellation, or Search Attribute family.

The structured Human Work witness reuses that passive User Task Update mechanism for one assignment-only review task and one String-equality gateway. It replaces the Worker before completion, refuses a wrong occurrence while preserving the exact task, proves that reordering an ordered String list under the same command ID is a content-identity conflict, commits an integer/Boolean/String/duplicate-preserving String-list patch, recovers an identical retry after closure, checks exact terminal data and accepted/rejected outcome history, and replays the fetched history. Protocol guards bind all new variants and their byte ceilings while effect transport still refuses integer and list values. No form catalog, validation rule, action, Zod schema, or new Temporal primitive enters Workflow state.

The resumption-bounded cycle witness completes activation `1` through the first back-edge, replaces the Worker, refuses a stale activation-`1` command while preserving the activation-`2` Query, traverses the second back-edge, exits from activation `3`, recovers the first accepted Update result, inspects the exact finite Event History, and replays it. Test-only direct-VM Workflows respectively collapse occurrence identity to BPMN element ID and reset the current activation to `1`; each wrongly commits the stale command and therefore separates the production identity-preserving Workflow. Every observed Workflow Task keeps `suggestContinueAsNew` false, without making an unbounded-history or cross-Run claim.

The Workflow-chain first-green witness starts the registered cyclic program through the production v1 host-input shape in two lower-only configurations. The count case sets four Events and proves exactly three Runs and two Continue-As-New boundaries. The byte case keeps the production count limit, sets 48 KiB, and uses accepted bounded stale completions to grow only host history before proving two Runs and one boundary. Both retain the exact open User Task occurrence, semantic completion through all three genuine activations, exact prior-Run Update recovery, conflicting-content rejection, the closed public receipt plus ordered private recovery entries, semantic-core final-state equality, and replay of every Run. History inspection proves no SDK suggestion, the selected count or byte trigger, and overshoot within the executable activation reserve below Temporal's warning margins. Its focused continuation-validator gate rejects terminal carried state, negative or widened publication metadata, foreign Message identity, oversized successor RuntimeState with exact capacity details, and oversized host metadata. A separate enum-fence guard applies both User Task completion and incident retry to prove that only a nonterminal rollover fence emits `BpmnWorkflowRolloverInProgress`, while the terminal fence delegates to the existing Process-closure lifecycle. Ordinary Product 1 starts use both production limits; retained direct two-argument starts alone stay outside the branch. Event History never becomes BPMN or CIB evidence.

The aggregate-continuation witness measures the actual six separate Temporal arguments. A constructed boundary distributes bytes across the start, Program, host, RuntimeState, recovery wrapper, and publication values while each stays within its own row: exactly 458,752 bytes succeeds, 458,753 reports aggregate capacity, and shifting one byte beyond the start-stimulus row proves that individual limits still take precedence. The prior synthetic-array implementation fails both aggregate cases by seven bytes, locking the root correction rather than one fixture. Incoming and successor call-site tests retain fail-closed continuation versus typed-capacity classification, and the live cyclic cases retain the ordinary replay witness.

The Run-capacity witness builds the real publication and stable RuntimeState fixture at current ordinal 127 with 126 contiguous zero-length predecessor descriptors, emits the complete 128th-Run continuation, and re-admits its state, recovery, and publication. At current ordinal 128, an adversarial throwing segment-directory getter proves that the typed capacity failure for observed ordinal 129 happens before segment closure. A mutated incoming ordinal 129 remains a nonretryable continuation defect. The existing cyclic live cases supply ordinary multi-Run replay; this boundary witness adds no public Run fact or BPMN/CIB behavior.

The stop-the-world deployment witness hashes the exact executable old and candidate bundles, binds their fleet-visible Worker identities to those digests, and runs the required ingress-fence, old-stop, zero-poller, candidate-replay, candidate-start, candidate-only-poller, reopen sequence. It replays one retained two-argument legacy history and all three v1 cycle Runs under the candidate before polling. The old bundle fails the closed v1 Run at the non-deprecated chain patch marker, and substituting those old bytes under the candidate digest is rejected before any operational effect. After admission, the candidate resumes the open legacy User Task through all three registered cycle completions and reaches the same semantic-core terminal state. Pure adversaries reject mixed old/new pollers, a missing Activity poller, malformed bundle-bound identities, and replay failure while keeping ingress fenced and stopping a started candidate. Deployment receipt and failures remain private infrastructure facts.

The recovery-capacity witness uses the production 512-entry limit without a test override. It commits 512 distinct cyclic User Task completions in one Run, requires the filling command's semantic result before failure, then requires the retained failed Run to resolve that exact identity, reject changed content as conflict, and return the bounded typed capacity details for unseen work. Product 1 must map the same unseen command to `BpmnWorkflowChainCapacityExhausted`; the details expose no Run ID or candidate payload. The history contains one Workflow failure, no Continue-As-New or Workflow retry, and replays under the production bundle. Pure lowered-bound tests additionally prove byte-first exhaustion and terminal-over-capacity precedence.

The terminal-result-capacity witness preserves the registered resumption-bounded cycle profile and compiled semantic operations while padding only test-owned definition and command identity strings. Sixteen real User Task completions traverse the existing Exclusive Merge and default exit, commit through Product 1, and fill the private recovery bytes without crossing the Program, stimulus, RuntimeState, paired-publication, or ledger sub-bounds. The exact private receipt-plus-recovery envelope is then 196,609 canonical UTF-8 bytes. The closing command returns committed before the Workflow fails with bounded nonretryable terminal-envelope capacity details; the retained failed Run resolves that exact command, rejects changed content under its ID, returns the exact terminal receipt for unseen work, exposes no private Run or candidate fact, contains no Continue-As-New or Workflow retry, and replays. A focused pure Workflow test returns an unchanged envelope at exactly 196,608 bytes and applies exact-fit/one-byte-over lowering to both completed and cancelled terminal states. The identity padding is host-capacity evidence, not a new BPMN source or CIB-cycle claim.

The Message Start witness submits the exact resolved Process, Start Event, Message, Interface, and Interface Operation identity through ordinary Workflow creation while no Worker polls. Service acceptance precedes the later Worker, exact first User Task, completion Update, terminal receipt, history inspection, and replay. The history contains no Signal Event. A test-owned Signal-With-Start start creates an otherwise plausible execution whose Signal Event fails the same durable-history discriminator, and a wrong Interface Operation is rejected before Workflow creation.

The forced Intermediate Catch Message rollover witness uses the exact registered BPMN source and standards-only profile with the existing trailing User Task CIB premise. Run 1 accepts a wrong-channel Signal, reduces it exactly once to semantic rejection, and closes with the original subscription unchanged. Product publication then proves the complete occurrence and operation-addressed channel in Run 2 before the valid delivery is sent. That delivery commits once and the trailing User Task appears in Run 3. A post-boundary exact duplicate returns the retained commit, changed content under the same command ID remains conflict, and neither creates another semantic transition. The closed v1 receipt, ordered recovery entries, four exact Signal payloads, concatenated per-Run diagnostic trace, pure semantic-core oracle, absence of Timer/Activity/Child/cancellation events, and replay of all three Runs agree without using Event History as Message meaning.

The forced Intermediate Catch Timer rollover witness uses the exact registered BPMN source and standards-only `PT1S` profile. Run 1 commits the complete Timer occurrence, deadline 1,000, and logical time zero, then closes before scheduling a durable Timer. Fresh Run 2 defers the still-triggered low Event threshold until it retains work, schedules one 1,000 ms Timer, derives the exact content-bound firing once, and reaches the same pure semantic-core terminal trace. The history contains exactly one matched Timer-started/Timer-fired pair in Run 2, no Timer in Run 1, no Signal or Activity, a closed v1 receipt with no recovery entries, and replay of both Runs. Event History proves hosting only and does not redefine the registered Timer meaning or its `CIB-AGR-0004`/`CIB-CFG-0001` classifications.

The forced Service Task effect rollover witness uses the exact registered BPMN source and success-only CIB extension profile. Run 1 commits the complete effect occurrence and neutral descriptor, then closes before any Activity is scheduled. Run 2 derives the same content-bound request, exposes the unchanged semantic wait while the one probe Activity is held in flight, records one schedule/start/completion lifecycle and one external mutation, and derives the exact `completeEffect` command once. The concatenated per-Run trace equals the pure semantic-core result; the closed v1 receipt has no recovery ledger entry for the derived command; no Timer or Signal exists; and both Runs replay. Activity History proves hosting only and does not redefine Service Task success or the `CIB-EXT-0001`/`CIB-CFG-0002` classifications.

The Timer Start witness performs semantic and host admission before creating one exact one-action Temporal Schedule. A wrong Process or Start Event creates neither Schedule nor Workflow. The Schedule stores the semantic-instance-derived configured Workflow-ID base and fires at one UTC due instant resolved from `PT1S` while no Worker polls. After the due action, the witness reads one opaque execution Workflow ID and first Run ID from `ScheduleHandle.describe()` before starting the Worker or performing any semantic Query or Update; treating the configured base as that execution identity fails. Every later Workflow operation, exact User Task completion, terminal result, ten-event history inspection, and replay uses the returned identity. The Schedule reports one exhausted action, the history has no Workflow Timer or Signal family, and its service-reported byte size is asserted only as a positive safe integer because server-generated metadata makes the exact byte count run-specific. Test-owned stored-action replacement and direct Workflow-start mutations separately detect post-admission program drift and bypass of the Schedule service.

The Workflow must enqueue its admitted start stimulus before registering externally addressable handlers. Update handlers may run as soon as they are registered, including during replay after Worker restart; the focused restart witness guards against completion overtaking start. The harness-only post-completion Query trace must reconcile every completion-command outcome with its completed Update result in Event History and its terminal state with the validated completed receipt. A failed Update is classified as harness infrastructure failure rather than parsed as a malformed semantic outcome. The start command is excluded from durable Update-result reconciliation because it is a Workflow argument rather than an Update. Intermediate Query state remains independently checked against the core and does not become the production observation API.

For the timer capsule, the runner validates but never delivers the scenario's explicit `fireTimer` stimulus. The Workflow derives the identical stimulus only from committed `openTimers` state, schedules the exact remaining duration, and records one matching timer-started/timer-fired pair. The mandatory full-server witness stops the Worker before the due boundary, waits beyond due time without a poller, starts a replacement Worker, reconciles the completed receipt and history, and replays the history. A separately bundled mutation bypasses `Workflow.sleep`; the pure result stays equal while the durable-history assertion fails.

For the Timer/User Task composition, the runner waits for host-driven Timer progress before delivering the later User Task completion, but preserves immediate negative delivery for wrong/stale User Task scenarios that have no preceding host-driven stimulus. The focused witness requires exact core agreement, one durable Timer pair, the later open User Task, completion through Update ingress, and live replay.

For the Intermediate Catch Message capsule, the Signal handler only validates, records, and enqueues well-formed delivery; the main loop alone calls the core. The focused primary witness starts through Product 1, locks the production chain patch, accepts a wrong-channel Signal as transport but returns semantic rejection, stops the Worker, accepts the exact Signal, restarts the Worker, observes the committed delivery and later User Task, proves stale and exact-duplicate behavior, records a conflicting-identity request failure without a Workflow Task failure, completes the Process, reconciles the ordered private recovery entries against each original stimulus digest, requires the closed host-ledger-free public receipt and five exact Signal payloads, fails a seeded payload substitution, and replays the history. The conflicting reuse leaves the original entry unchanged. The reverse User Task/Message order supplies independent host execution and replay under the same passive-ingress capability. Malformed input fails before Signal submission.

For the Message-addressed Receive Task, the same test environment starts the independently admitted direct-channel Process through Product 1, locks the production chain patch, and pins its complete direct subscription/interaction Query. Malformed direct ingress emits no Signal, while an operation-addressed channel carrying the same Message ID reaches the core, rejects, and preserves the wait. The test stops the Worker, submits the exact direct Signal, verifies durable acceptance before replacement, recovers one committed result from the private recovery entry, requires the closed host-ledger-free public receipt, exact empty completed state, and absence of Timer, Activity, Child Workflow, and cancellation events, removes one Signal to prove the exact-history check fails, and replays the live history. A test-only admitted-program mutation replaces the direct arm with a complete `operationMessage` arm: the pre-delivery Query diverges, the exact direct delivery rejects, and only the substituted arm completes, proving that Workflow hosting does not erase the selected channel.

The focused Service Task refinement witnesses derive the Activity request and content-bound completion only from committed `openEffects` state. They lock the exact two-attempt timeout/retry policy, one-invocation plain success, two-invocation/one-mutation fail-after-mutation reconciliation, two-instance shared-store separation, field omission and host-over-inclusion mutations, typed exhaustion with unchanged committed intent, Worker replacement after start-to-close expiry, live replay, and a separately bundled inline-bypass mutation that preserves the pure trace but lacks Activity history. The CreateDocument witnesses extend that same boundary with immutable typed arguments and one typed local result patch, require semantic-core output mapping to the final Process variable under retry and Worker replacement, and retain an Activity-bypass mutation whose final semantic result remains equal while durable evidence fails. The pinned server summarizes a successful retry as one final `ActivityTaskStarted` with `attempt: 2` and `lastFailure`; probe-service invocation evidence independently observes both attempts.

The Service Task incident witnesses route the host-only `technicalFailure` Activity result without changing the semantic success or BPMN Error result arms. Runnable configuration accepts that handler only for the registered incident profile, while the compiled program selects a one-attempt Activity policy inside the Workflow and every predecessor profile retains its existing policy. The primary history commits one `reportEffectFailure` command, replaces the Worker while the generation-1 incident is open, recovers the exact retained retry Update, completes the same restored effect, verifies the Activity, Update, Query, and terminal receipt relation, and replays. A second history returns `technicalFailure` after the retry and produces a typed host failure with no second semantic incident while preserving the last committed restored wait for Query and replay. A concurrent retry race commits one exact command, rejects the other, preserves one restored wait, and replays its history.

The incident-cancellation witness uses the same one-attempt policy, reaches the exact generation-1 incident, stops the Worker, submits the exact published root/incident cancellation Update before replacement, and recovers its committed result after replacement. It requires typed cancelled terminal state and receipt, preserved committed Process data, one technical Activity attempt, a distinct late `processClosed` carrying the same receipt, ordinary Workflow completion, zero Workflow cancellation or termination events, and replay. Test-owned native termination and completed-receipt substitutions must fail the relation.

The incident-operations Query witness runs both graduated incident profiles through the production Workflow bundle and the Product 1 process-operations client. It requires the exact current Retry-only and Retry-then-Cancel sequences, Retry recovery to a running zero-incident snapshot, completed and cancelled terminal receipt corroboration, no Query-created history event, and replay of both exact histories. Trace-derived state, malformed interaction pairing, private locator exposure, Schedule-base substitution, and terminal-status substitution must each fail. Product 2 consumes this boundary only through the representation-free engine gateway.

The committed-execution lane requires one immutable revision-zero accumulator beside RuntimeState, one complete numbered batch only for a committed publishable step, unchanged accumulator identity for rejected and closure-bound steps, append-before-command-result ordering with no `await` boundary, and every later append to fold from the accumulator's stored current position without logical-time regression. A changed current multiplicity or backward cross-batch logical time must reject before advancing the accumulator. Its unconditional Query is registered before semantic evaluation, returns `notReady` at head zero for every valid cursor, serves only complete batch-boundary pages under the requested/default limit, returns `gap` for inside/ahead cursors, includes current only at head, validates every producer result against the exact Program, and is pure across repeats. The Product 1 client uses the representation-free decoder and the engine API accepts only public identity plus an opaque locator. The transport decoder validates the complete position fold only from revision zero; Product 2 validates positive-cursor suffixes from its transactionally retained prior head before atomic advance. The real-server witness locks exact start and terminal revisions, positive cursors, Worker replacement, retained terminal Query, Query-history purity, repeated-activation identity, and exact replay. Product 2 tests lock byte-identical overlap, changed-overlap refusal, gap/unavailable suppression, restart continuation, revision-zero rebuild, canonical export, and fresh-only History and Diagram surfaces.

The flow-node occurrence Temporal lane requires a separate immutable revision-zero accumulator and unconditional Query beside E1. Only a committed step with complete, transition-aligned E1 and lifecycle publications may sample the deterministic Workflow clock, and it samples exactly once per complete command batch before deriving both immutable successors and recording the existing command outcome. Rejected, bounded, ambiguous, recovered, or malformed publication paths sample no time and advance neither head. Public occurrence IDs are assigned from hosting Process instance, start revision, and start index; the exact private anchor map remains in Workflow state and must match the public open head. Every batch preserves the E1 command ID, range, transition count, and revisions exactly. Revision-zero validation folds the complete lifecycle from an empty open set, while positive-cursor transport validation checks only its visible suffix and Workflow validation anchors it to retained open occurrences and prior committed time. Before numbering or clock exposure, a separate Workflow relation reconstructs the complete lifecycle of every E1 transition from the exact Program operation or stimulus and retained private anchors, then compares the whole canonical delta. The Query reuses E1 complete-batch cursor and limit behavior, includes `currentOpen` only at head, returns immutable copies, and creates no history event. The completed Product 1 evidence drives this production Query through the strict client, Worker replacement, terminal retrieval, repeated pure Queries, exact E1 comparison, and replay. Test-owned Event History head/count substitution enters the production Query relation and is rejected by E1 head alignment; an independently derived before/after-open-state substitute enters command-publication integration and is rejected by lifecycle completeness before clock access or accumulator advance.

The sequential Multi-Instance refinement lane starts the exact registered batch-review profile through the production Workflow-chain entry, forces one pre-arming Continue-As-New, and preserves one semantic lifetime Timer through inner-task turnover. The natural history replaces the Worker, recovers one accepted completion, publishes three ordered inner occurrences and the exact output collection, validates the terminal receipt against the pure trace, remains inside the independent capacity envelope, and replays both Runs. The interrupted history completes one item, lets the original Timer cancel the second generated task, reaches the escalation task in Run 3 only after callback reduction, rejects the stale task without state change, publishes no partial output, validates E1/E2 and the terminal receipt, remains inside the interrupted capacity envelope, and replays all three Runs.

The Workflow-chain publication-segment witness drives the unchanged public E1 and E2 clients through three retained Runs. It proves exact paired segments `0..8`, `8..12`, and `12..16`; pages at cursors `0`, `4`, `8`, `12`, and `16` that never cross a Run; the exact current and open-occurrence snapshot only at the global head; two occurrence identities that start before and end after distinct Continue-As-New boundaries; a closed terminal receipt with no private segment or Run field; and replay of every Run. Focused protocol, Workflow, and client separators reject malformed directories, noncontiguous ranges, descriptor or Run substitution, changed paired alignment, unbounded responses, and generic latest-selection failures entering the legacy fallback. The Query-capacity separator derives two aligned E1/E2 batches by advancing the real semantic core over its maintained publication fixture, proves the exact inclusive limit, returns only the largest complete aligned prefix, withholds both current snapshots before head, retrieves the unchanged remainder deterministically, refuses a one-byte-lower no-progress bound, and makes the client reject an oversized private response before public projection. A missing exact selected Run maps to public `unavailable`; only `QueryNotRegisteredError` on latest selection reaches the retained pre-chain direct Query.

The production Workflow-chain gate contains the concurrent Temporal suite and decisive serial capacity witnesses. Product starts carry the production Initial envelope; raw results are opaque; public receipts contain no Message ledger; and Product 1 preserves retry/conflict precedence. The reviewed checkpoint proves recursive continuation validation, terminal fencing, and replay. Event History evidence proves count and byte rollover independently, no earlier SDK suggestion, exact current history metrics, conservative activation reserves below both Temporal warning margins, unchanged registered-cycle semantics, three-Run and two-Run boundaries, and replay. Aggregate continuation evidence proves exact 448 KiB sum accounting over the separate payloads, one-byte overflow, individual-bound precedence, both production call sites, and ordinary live replay. Run-capacity evidence proves exact Run-128 admission, pre-segment Run-129 typed failure, incoming ordinal rejection, and private Run identity. Paired publication evidence covers Run-local ranges `0..8`, `8..12`, and `12..16`, boundary cursors, cross-Run occurrence identity, private Run identity, and replay. Recovery and semantic-candidate witnesses cover the exact 512-entry, 64 KiB state, and 64 KiB paired-batch boundaries without exposing speculative state. Command-ingress evidence proves an exact 64 KiB client stimulus, pre-acceptance oversized Update refusal, accepted oversized-Signal failure, the 65th queued Signal failure before revision advance, bounded failure details, and replay. Effect evidence proves exact 64 KiB request/result accounting, pre-schedule request refusal, pre-return Worker result replacement, deterministic Workflow revalidation, a fixed 16 KiB-bounded exhaustion projection with no cause graph, typed live failure at the last public revision, and replay. Retained-Run evidence proves exact accounting, a hard 2 MiB ceiling, two-candidate headroom, rollover from rejected-command trace growth, preserved occurrence/recovery/terminal facts, bounded history, and two-Run replay. Timer evidence proves 64/65 pre-scheduler refusal; admitted profiles allow one live Timer. Query-response evidence proves the 192 KiB producer/client ceiling, largest complete paired prefix, exact inclusive fit, one-byte-lower refusal, and unchanged real BPMN cycle traversal/replay. Terminal-result evidence proves exact production fit, typed one-byte overflow after committed closure, retained identity-first recovery, one failed Run, and replay. Horizon 1 closure target `36c3c75` passed after correction `07c68d5`; M6 target `c1ee125` passed after correction `7c35fd1`. Product 2 evidence covers strict catalog projection, durable definitions, exact retry/conflict, HTTP, server composition, the retained expense-exception model, and the 1280/1600 Chromium journey.

The boundary-error witnesses carry the business error as a successful typed Activity result rather than an Activity failure. The Workflow derives `completeEffect` from committed intent, the core selects the exact route and null mapping, caller delivery completes the resulting User Task, and the primary history replays. A returned unmatched code becomes typed adapter failure without retrying the completed Activity. An `ApplicationFailure` never opens the boundary route, and the separately bundled Activity-bypass mutation must preserve the canonical semantic result while failing the durable Activity-history assertion.

The Simple Boolean Exclusive Gateway witness starts the standards-profile Process, observes only `Task_First`, completes that occurrence, reaches semantic completion, and replays the live history without an evaluator Activity or expression-specific Temporal Event. A separately bundled schema-valid Workflow mutation swaps the two conditional routes while leaving the conditions intact; it exposes `Task_Second` at the public observation boundary and proves that bypassing the semantic core's selected route is detectable. Worker-restart safety is inherited from the existing User Task wait/Update mechanism and its focused restart witness because the gateway adds no durable host mechanism.

The structured Inclusive Gateway witnesses start the one-true, both-true, and default cases and pin the exact initial wait set. Both completion orders retain the sibling-only intermediate state and reach equal terminal state after semantic instance identity is normalized. The both-true durability witness completes A, stops the Worker, recovers the accepted result and B-only state under a replacement Worker, completes B, verifies exactly two accepted Updates and zero unrelated Signal, Timer, Activity, Child Workflow, or cancellation events, and replays the history. All four histories replay. A separately bundled `selectMany` mutation forces one true candidate false and exposes A without B at the initial public observation boundary. Exhaustive host classification recognizes `selectMany` as token-splitting, rejects its combination with Timer or effect waits as `concurrentHostDrivenWaits`, and is mutation-guarded against omitting that operation kind.

The Event-Based Gateway witnesses pin one `MessageCatch` activation-`1` subscription and one `TimerCatch` activation-`1` deadline-`1000` Timer in the armed Query. One history accepts Message ingress while the Worker is absent, commits the Message winner under replacement, cancels the Timer, and exposes only the Message-path User Task. One lets the Timer become ready while the Worker is absent, commits the Timer winner under replacement, rejects the late Message, and exposes only the Timer-path User Task. A third delivers a wrong Message in a separate activation, preserves the exact armed Query and original Timer, then lets that Timer fire once without deadline drift. A coalesced same-activation Signal/Timer history fails with typed `BpmnEventRaceOrderingUnavailable` before core advancement and replays. The scheduler's deterministic activation tag and one-turn drain are pinned to the installed SDK's single-batch activation behavior by a focused job-level witness; barrier removal, single-batch-premise removal, fixed-priority selection, Timer cancel/restart, loser retention, and core-bypass mutations must each invalidate their owning assertion. No Event-race history contains an Activity, Child Workflow, effect, or Workflow-cancellation event.

The bounded Call Activity witness starts one caller/root Workflow, exposes only the called User Task under its derived semantic Process-instance identity, and submits that task through the existing API that separates Workflow address from semantic task address. After called completion it stops the Worker; replacement must recover the committed Update result and the unchanged caller-only Query. Caller completion produces the ordinary terminal receipt. The history contains exactly two accepted/completed Updates, no Signal, Timer, Activity, Child Workflow, effect, or cancellation event, and replays. Separate mutation Workflows expose simultaneous called/caller tasks after forged early return and invert Query identity plus completion acceptance after called-identity erasure.

The ordinary embedded Sub-Process witness exposes the two child User Tasks through the existing passive Update mechanism, completes the first, replaces the Worker, recovers that accepted result, and proves that the sibling remains live while the outer task remains absent. Completing the sibling makes the child region quiescent, emits one outer continuation, and permits the trailing User Task and root completion. The history replays and contains zero Signals, Timers, Activities, Child Workflows, or cancellation events. A separately bundled scope-bypass mutation uses the real first core completion and then fabricates the outer continuation outside the core; the retained Update/state relation rejects it because the genuine observation still contains the sibling.

The Sub-Process Error-propagation witness commits Trigger Error through the existing passive Update mechanism, stops the Worker immediately after the core's atomic throw/catch/cancel closure, and requires a replacement Worker to recover both the accepted Update result and the Recover-only wait set. A fresh stale Sibling Work Update is then rejected without changing Recover, Recover completes the Process, and the history replays with zero Signals, Timers, Activities, Child Workflows, or cancellation events. A separately bundled bypass fabricates the identical post-cancellation prefix without invoking the core; because it retains pre-throw semantic state, the next stale Sibling Work Update durably commits and creates a canonical suffix that differs from the genuine rejection and preserved Recover state.

The Terminate End witness commits Trigger through the existing passive Update mechanism, stops the Worker immediately after the core's containing-scope termination and ordinary child completion, and requires a replacement Worker to recover the accepted result and the Outer-only wait set. A fresh stale Sibling Update is rejected with exact state preservation, Outer completes the Process, and the 20-event history replays with zero Signals, Timers, Activities, Child Workflows, or cancellation events. A test-owned Workflow applies the realistic wrong global-cancellation account to the same admitted definition and closes after Trigger instead of exposing Outer, producing a durable public discriminator without copying production's stale-command implementation.
