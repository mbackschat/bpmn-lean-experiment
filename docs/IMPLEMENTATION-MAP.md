# Implementation map

This document is the sole detailed owner of current implementation, proof, evidence, and absence status. It describes the repository now, not milestone history.

## Current claim

The repository has an evidence-closed **draft** semantic capsule for one private executable `None Start Event → User Task → None End Event` Process. Exact completion and wrong activation agree across pinned CIB Seven, the Lean reference interpreter, the independent TypeScript semantic core, and the Temporal adapter. For sequential stale completion, CIB Seven, Lean, and the core agree on semantic rejection; Temporal agrees exactly through completion and separately returns adapter-owned `processClosed` under the explicit post-terminal schedule.

The parallel fork/join contract is evidence-closed as a draft under normative per-incoming-Sequence-Flow behavior. The bounded [Semantic Process IL spec](SEMANTIC-PROCESS-IL-SPEC.md), asymmetric runtime representations, and decision not to claim broad CIB parallel compatibility own the maintained definition boundary. Current schemas freeze the checked graph and Semantic Process wire shapes, and adversarial contract tests cover reference, arity, identity, canonical-order, and raw-to-canonical projection failures. The bounded source path produces canonical checked graphs and Semantic Process programs for the sequential model, exact two-branch parallel structure, and exact `PT1S` Intermediate Catch Timer structure. Lean strictly decodes both artifacts, validates the checked graph and a structural subset of the program contract, recomputes canonical lowering, rejects inequality before evaluation, executes the received program through a generic relation/evaluator, proves evaluator soundness and structural lowering properties, and checks the bounded parallel and timer laws and non-laws. Reachability, acyclicity, and producer/consumer shape are guaranteed for admitted artifacts only transitively through checked-source validation plus exact lowering equality; `programWellFormed` does not establish them independently and must be strengthened before program admission widens beyond that binding. Lean's scenario evaluator closes the admitted independent two-task activation pair only under the checked disjointness condition and rejects every other multiple-enabled state as an unresolved semantic choice. The independent TypeScript semantic core executes the same parallel and timer observable contracts through its own explicit runtime representation and enum-based evaluator. The topology-specific representation and evaluator path are absent. Temporal delegates current-task and timer projection, stimulus well-formedness, command identity, and same-stimulus comparison to semantic-core operations. The [production lifecycle](TEMPORAL-PROCESS-LIFECYCLE-SPEC.md) hosts the program under ordered, duplicate, concurrent, post-terminal, Worker-restart, retained-result, durable-timer, replay, and cleanup evidence. The CIB adapter projects multiple distinct active tasks with semantic sorting and per-element wait multiplicity and realizes the timer through controlled-clock, eligibility-gated job execution. The seven-case prepared pipeline establishes exact ordinary four-target agreement including the exact timer, the explicit sequential stale target relation, intermediate Query projections, live replay, projection sensitivity, durable-timer discrimination, and rejection of erased parallel Sequence-Flow provenance. The schema-valid negative probe separately establishes candidate deviation `CIB-DEV-0001`.

The [Intermediate Catch Timer contract](capsules/INTERMEDIATE-CATCH-TIMER-SPEC.md) is evidence-closed as a draft. The exact literal remains in the checked graph and independently lowers to `awaitTimer` duration 1000; Lean and TypeScript own full timer occurrence identity, exact-deadline eligibility, logical-time advancement, refusal, and `openTimers` observation. The scenario's answer-free `fireTimer` is applied directly by Lean and the core, realized by controlled-clock CIB scheduling, and never delivered to Temporal; the Workflow derives the identical content-bound stimulus exclusively from committed core state after one durable timer. Full-server evidence covers Worker absence at due time, exact history, receipt reconciliation, replay, and a separately bundled sleep-bypass mutation. Time skipping is a separately named optional calibration lane.

This is not a general BPMN engine, an OMG conformance result, or an immutable CIB compatibility profile.

## Implemented and absent surfaces

| Surface | Implemented | Explicitly absent |
|---|---|---|
| Project foundation | Lean 4.31.0/Lake 5.0; Node 24.18.0; pnpm 11.17.0; TypeScript 7.0.2; Ajv 8.20.0; Java 21 with explicit, environment-first portable home resolution; Temporal SDK 1.21.0 and CLI v1.8.1; MIT licensing for project-authored material; shared `CLAUDE.md`/`AGENTS.md`; A12-aligned document roles and proposal/spec lifecycle; enforced documentation index, filename-role, and local-link guards; source-synchronized walkthrough; focused and full gates; GitHub Actions verification on Ubuntu and macOS with the warm feedback budget unchanged | Release packaging, published libraries, production deployment |
| Wire contracts | One current structural schema per semantic profile, scenario, canonical result, CIB evidence, checked BPMN graph, and Semantic Process program; stable document kinds; semantic profile/source/compiler identity; exact scenario/profile content binding; seven answer-free target scenarios; produced checked-process and Semantic Process artifacts; raw CIB task-query and timer-job observations with independently verified canonical projections; required `openTimers`; typed `fireTimer`; cross-artifact definition identity and source-origin checks; reference, arity, identity, canonical-order, evidence, and projection mutations; pre-release guard against embedded format counters, retired representation names, and milestone compatibility paths | Parallel legacy schemas, migration readers, compatibility switches, general assertion language |
| Semantic profile | Draft `cibseven-2.2.0-user-task-draft`, `parallel-fork-join-draft`, and `cibseven-2.2.0-intermediate-catch-timer-draft` profiles pin their oracle revision, environment, selected features, observation boundary, exclusions, and reviewed CIB–BPMN relationship IDs; the parallel profile assigns normative balanced fork/join meaning without claiming general CIB parallel compatibility; the timer profile claims only exact `PT1S` wait/due agreement under controlled scheduling; the central register records agreements `CIB-AGR-0003` and `CIB-AGR-0004` and candidate deviation `CIB-DEV-0001` separately | Separate CIB parallel-compatibility profile, immutable compatibility profile, full requirement classification, approved gap interpretations, selected CIB extensions, confirmed deviations |
| Semantic Process IL | Implemented draft spec for a checked source-facing graph; current JSON Schemas and boundary validators for typed `initiate`, `awaitUserTask`, `awaitTimer`, `duplicate`, `synchronize`, and `terminate` operations; deterministic TypeScript lowerer with exact source/profile/compiler identity and source origins; independent sequential, bounded-parallel, and exact-timer TypeScript evaluator; strict Lean artifact decoders and validators; independent Lean lowerer and exact equality gate; generic Lean relation/evaluator with explicit operation choice, evaluator soundness, structural lowering laws, parallel and timer operation semantics, laws, and non-laws; bounded growth and stop criteria; separately gated, frozen five-operation checked-source token game and fixture-coincidental positional-lowering discriminator; focused Temporal hosting/refinement and complete four-target evidence | Adopted checked-source operational relation and full observational checked-source-to-program-run preservation proof; the bounded experiment stopped before the correspondence induction and may reopen only under its recorded triggers |
| BPMN source | Exact byte capture and SHA-256; UTF-8/security preflight; private `bpmn-moddle@10.0.0` import; warning/reference-loss rejection; bounded compiler for the sequential, exact balanced two-branch parallel, and exact linear `PT1S` Intermediate Catch Timer structures; canonical project-owned checked graph and Semantic Process program with source/profile/compiler identity, node/flow origins, User Task metadata, and retained timer literal; checked partial CMOF facts including Parallel Gateway direction and TimerEventDefinition containment; optional MIWG admission observation | General BPMN compiler, arbitrary graph admission, timer forms beyond exact `PT1S`, synchronous parser CPU isolation, non-UTF-8 decoder, source locations, extension semantics, DI-preserving export, complete CMOF binding, deployment store |
| Lean | Strict checked-graph, Semantic Process, and external scenario JSON decoders with exact-key rejection; independent checked-graph validation and structural program validation; canonical lowerer with definition-identity and Sequence-Flow-origin preservation laws; exact per-artifact lowering equality before evaluation; generic flow-identified token-multiplicity runtime; declarative `OperationStep`/`ProgramStep`, executable operation-identified `step`, and universal evaluator soundness; semantic task and timer occurrences; bounded closure for one enabled operation or the exact disjoint two-task activation pair, with every other multiple-enabled state rejected; User Task exact-completion and quantified mismatch laws; parallel laws and duplicate-left/no-right non-law; independent `PT1S` normalization and exact lowering; exact timer firing trace; quantified full occurrence-identity/logical-time mismatch rejection with state preservation; early firing named checked non-law and late refusal derived from the same theorem; generic seven-scenario result emitter that consumes and echoes the same admitted scenario documents; experiment-only frozen direct checked-node relation and renamed public divergence | Independent `programWellFormed` checks for reachability, acyclicity, and permitted producer/consumer shape; adopted checked-source semantics and run-level observational lowering-preservation proof; enabled-transition, supported-closure, admission, observation, and stimulus-list correspondence remain unresolved; general multiple-enabled closure without an explicit semantic choice or checked commutation argument; general scope/variable/effect model; TypeScript or Temporal correspondence proof; arbitrary BPMN XML parsing |
| TypeScript semantic core | Dependency-free Semantic Process contracts; strict structural program admission for exactly the sequential, balanced two-branch, and exact timer operation graphs; pure enum-based execution of `initiate`, `awaitUserTask`, `awaitTimer`, `duplicate`, per-incoming-flow `synchronize`, and `terminate`; operation-ID-stable internal closure independent of program collection order; explicit flow-identified token multiplicity and sorted semantic task/timer occurrences; parallel completion-order, excess-token, and non-law witnesses; exact timer wait, deadline, firing, full mismatch, stale refusal, and `openTimers` projection; adapter-facing current-task/current-timer projection, exact structural stimulus validation, command identity, and same-stimulus comparison; incremental deploy/advance and full scenario evaluation; malformed topology rejection | I/O, parser, Temporal SDK, CIB dependency, general BPMN state model, effects, timer forms or races beyond the exact capsule, semantically material nondeterministic scheduling, arbitrary graph execution |
| CIB oracle | Pinned CIB Seven `2.2.0` embedded runner; exact deploy/start/query/complete; generated-task-to-semantic-occurrence mapping; multiple distinct active task projection with semantic sorting and per-element wait multiplicity; exact/wrong/sequential-stale, balanced A-then-B/B-then-A, live-sibling stale, and exact timer witnesses; controlled epoch clock; raw timer-job due date and executable projection; engine-observed pre-due ineligibility and due-time eligibility before execution; bounded engine-level rejection probe for a generated task ID after completion; schema-valid duplicate-same-flow Parallel Gateway probe for candidate `CIB-DEV-0001`; content-bound evidence with raw producer task-query/timer-job observations and independently reconstructed canonical projection; meaningful task, sibling, and timer-deadline projection mutations; explicit evidence replacement; persistent seven-scenario JSON-lines batch; PVM diagnostic projection including normal-flow event-scope recursion guard; timings and full cleanup | Repeated live instances of one BPMN element and engine-derived activation ordinals; full raw derivation of timer logical identity/deadline; immutable negative-probe evidence; reused CIB PVM algorithms/types as project semantics; broad CIB corpus adoption; general compatibility claim |
| Temporal adapter | One semantic-lifetime Workflow receiving the start stimulus and admitted Semantic Process program; collision-resistant SHA-256 Workflow ID derived from semantic Process address in production; canonical typed stimulus encoding and SHA-256 content-bound Update/timer command IDs; one semantic loop with start queued before handlers; core-owned command policy; a typed `semantic`/`processClosed`/`processUnknown` ingress result kept outside semantic outcomes; retained-Update-first closure recovery; validated address-bound completed receipt; `REJECT_DUPLICATE`; conflicting-identity failure; accepted-handler draining; explicit ordered, post-terminal, accepted-batch, concurrent, and Worker-down-at-timer-due harness schedules; unordered one-commit/one-rejection race witness; committed-state-derived exact timer duration and firing with no runner delivery; harness-only Query reconciliation with durable Update, timer-history, and receipt facts; exact one-pair durable-timer assertion; Worker replacement after timer due; replay and cleanup; command-ID-only and separately bundled timer-bypass mutation guards; optional time-skipping calibration outside default verification | Retained result beyond Temporal retention; production canonical-observation API; protocol that imposes caller order on concurrent distinct commands; semantic policy copies in the Workflow; Signal compatibility path; committed Event History fixtures; patch branches; legacy representation fallback; production history baseline; general Worker versioning; Activities; timer forms/races/cancellation beyond the exact capsule; Search Attributes; Continue-As-New; task inbox |
| Differential pipeline | Seven answer-free cases; one CIB batch; one generic Lean result emitter; one core batch; fourteen isolated Temporal executions; exact ordinary four-target agreement including parallel live-sibling stale rejection and exact timer firing; sequential stale three-way semantic agreement plus exact Temporal completion prefix and separate `processClosed`; retained-CIB comparison; Query/Update/durable-timer evidence; definition binding; activation, initial-task, live-sibling, timer-deadline, and provenance mutations; seven-history replay; isolation, cleanup, timings, and budgets | Universal equivalence, majority voting, general conformance suite; uncorrelated Lean and TypeScript account failure |
| BPMN conformance | Ultimate Process Execution Conformance target and evidence-lane discipline are explicit; a seeded disposition-only requirement ledger records reviewed scope without an exhaustive denominator; MIWG and CIB/Betsy adoption strategies are researched | Any implemented OMG conformance point, exhaustive requirement coverage, percentage-complete claim, or conformance claim |

## Current evidence

Each rule's CIB evidence carries an explicit `engine-observed`, `adapter-derived`, or `adapter-decided` fidelity in [the User Task capsule](capsules/USER-TASK-INTERACTION-SPEC.md#oracle-evidence-fidelity) and [the Timer capsule](capsules/INTERMEDIATE-CATCH-TIMER-SPEC.md#cib-fidelity-by-rule). Wrong-activation refusal is adapter-decided, while timer occurrence/deadline projection is adapter-derived; neither four-target agreement is misreported as four independent derivations of those project representations.

The complete prepared pipeline requires:

- exact agreement for `user-task-discovery-completion` and `user-task-wrong-activation`;
- exact CIB Seven/Lean/core semantic agreement for `user-task-stale-completion`, exact core/Temporal prefix agreement through completion, and separate adapter-owned `processClosed`;
- exact agreement for `parallel-fork-join-a-then-b` and `parallel-fork-join-b-then-a`, including the symmetric one-task intermediate projections;
- exact four-target rejection for `parallel-fork-join-stale-a-while-b-active` with B unchanged;
- exact four-target agreement for `intermediate-catch-timer-pt1s`, including one wait at deadline 1000, identical content-bound firing command observation, completed logical time 1000, and no caller-enabled interaction;
- equality between current CIB execution and content-bound retained CIB evidence;
- the same waiting projection for successful and wrong-activation inputs before completion;
- exact state preservation after wrong and stale completion rejection;
- equality between the pure core and both isolated Temporal results under each case's explicit target relation;
- correct Query projection and Update outcomes;
- proof that the Temporal runner never delivers `fireTimer`, the Workflow derives it from committed `openTimers`, and Event History contains one exact 1000-millisecond timer pair;
- duplicate logical-command stability;
- classified disagreement after activation `1` is mutated to `2`;
- a rejected Lean scenario document with an injected extra answer field, plus exact equality between the admitted scenario and Lean's decoded echo;
- exact Lean definition identity and lowering equality, plus rejection of a schema-valid operation-origin mutation;
- rejection of erased parallel control-place Sequence-Flow provenance, detection of an omitted parallel open task, detection of a dropped live sibling after stale A, and detection of a one-millisecond timer-deadline mutation;
- seven fetched live histories replayed before server shutdown;
- clean CIB and Temporal teardown.

The complete pipeline remains subject to the 15-second warm and 45-second cold budgets. Exact latest measurements and commands belong in [PLAN.md](PLAN.md), not in this inventory.

## Nearest unsupported claim

The nearest planned adapter-feasibility claim is a bounded Service Task/Temporal Activity composition: effect occurrence and intent, stable idempotency, Activity result ingress, external-side-effect reconciliation, Temporal retry versus CIB-visible retry/incident separation, cancellation, and replay remain absent and require a proposal before implementation.

The nearest structurally important proof gap remains full observational checked-source-to-program-run preservation. The [frozen bounded experiment](experiments/CHECKED-SOURCE-RELATION-EXPERIMENT.md) retained a provisional direct account and renamed positional-lowering discriminator but stopped before enabled-transition, closure, admission, observation, and run-level correspondence at the approved effort boundary. It is not extended by the timer capsule and reopens only under its recorded triggers. The exact proof framing remains in the [Semantic Process IL spec](SEMANTIC-PROCESS-IL-SPEC.md#lean-specification-and-proof-obligations).
