# Implementation map

This document is the sole detailed owner of current implementation, proof, and evidence status.

| Surface | Implemented | Explicitly absent |
|---|---|---|
| Project contract | Pinned Lean 4.31.0/Lake 5.0, Node 24.18.0, pnpm 11.17.0, TypeScript 7.0.2, Temporal SDK 1.21.0, and Temporal CLI v1.8.1 setup; nvm/asdf files and Homebrew fallback wrapper; MIT license for project-authored material; shared `CLAUDE.md`/`AGENTS.md` guidance; project and documentation ownership; command-versus-harness-versus-infrastructure outcome vocabulary; dependency-controlled verification script | Release process, CI, packaging |
| Milestone 0 pipeline | M0.0 through M0.6 complete; shared runner and observation contracts; provisional JSON-lines CIB boundary; one-way Lean canonical-result emitter; CIB Surefire result bridge; pure exact-result comparator with typed first-disagreement classification; one command builds and concurrently runs CIB, Lean, semantic core, and two isolated Temporal executions; live and retained replay, provenance, phase timings, cleanup, and warm/cold budget enforcement | General Lean/TypeScript JSON-lines runners, multi-scenario batching, extended assurance gate |
| BPMN 2.0.2 source | Official 532-page PDF downloaded; digital-first Markdown conversion with 333 linked figures; eleven normative machine-readable files; hashes, provenance, conformance digest, and Git-ignore boundary | Requirement-level coverage ledger, errata register, formalized metamodel, XML ingestion |
| BPMN conformance | Process Execution Conformance identified as the ultimate normative target; modeling/BPEL/choreography scope separated; import/admission and external-suite claim boundaries recorded | Any implemented conformance point or conformance claim |
| Semantic profile | Versioned draft Milestone 0 artifact pins CIB Seven `v2.2.0`, Java/H2/history/job/clock environment, feature surface, observations, comparison, and exclusions; first canonical trace calibrated; artifact policy in [profiles/README.md](../profiles/README.md) | Immutable identity, complete interpretations, deviations, and unsupported-feature register |
| Lean | Profile-independent outcomes and scenario contract; capsule-specific immutable model and runtime control state for None Start → User Task → None End; external command admission separated from bounded internal closure; closure exhaustion classified as harness failure without exposing a committed command; stable canonical state projection; interpreter-derived calibrated trace; proofs that start reaches one User Task wait, matching completion terminates, and a non-matching completion is rejected without state change; compact one-way JSON emitter for the content-addressed capsule result; separately gated experiment-local source/IR, dual-scope, edge-provenance, and richer runtime candidates | General BPMN source model, parser, executable IR, static semantics, general token/activation/scope model, concurrency, variables, effects, model checking, general JSON-lines transport |
| TypeScript semantic core | Strict dependency-free `@bpmn-lean/semantic-core` package; shared outcome, stimulus, status, wait, observation, scenario, and result types; immutable sequential model/runtime state; external admission and bounded internal closure; pure transition application; core-owned deployment and incremental stable-observation steps; scenario runner; exact calibrated-trace, poisoned-calibration, lifecycle, rejection/state-preservation, and no-speculative-commit tests | General BPMN model/IR, XML ingestion, variables, scopes, concurrency, timers, messages, effects, jobs, CIB/Temporal dependencies, JSON transport |
| Temporal adapter | Private `@bpmn-lean/temporal-adapter` pins the coordinated SDK 1.21.0 set and full local CLI v1.8.1 server; one generic Workflow hosts the semantic core; synchronous Signal handler deduplicates and queues stimuli; one main loop alone advances semantic state; diagnostic Query exposes the current projection; runner starts isolated executions with explicit per-call deadlines; live result equals the pure core result; live and committed CLI-exported histories replay through `Worker.runReplayHistory`; dependency/license/install-script audit recorded in [SOURCES.md](SOURCES.md) | Arbitrary BPMN model/IR input, Activities, timers, Search Attributes, Continue-As-New, fault injection, cache-eviction/Worker-restart tests, duplicate-delivery test, production Update/Signal API, production deployment/versioning |
| Formal methods and behavioral refinement | Question-driven capability map, relation hierarchy, risks, alternative-tool fits, candidate experiment design, and claim boundaries recorded in [TLA-AND-BISIMULATION-RESEARCH.md](TLA-AND-BISIMULATION-RESEARCH.md); no auxiliary tool selected; observational stuttering refinement remains an unimplemented adapter design target | Auxiliary dependency or model, model-checker result, counterexample projection, formal simulation relation, bisimulation checker or proof, implementation correspondence |
| Reference instrumentation | Pristine evidence lane, experimental source-lane policy, profiling targets, acceleration limits, branch provenance, and shadow-equivalence gate recorded in [REFERENCE-INSTRUMENTATION.md](REFERENCE-INSTRUMENTATION.md) | Experimental branch, source patch, execution profile, seeded-mutation comparator guard, shadow result |
| Semantic representation research | CIB Model API, deployment parse tree, PVM definition/runtime, fUML activation/offer model, PSSM activation/configuration model, code examples, transfer limits, and provisional project consequences recorded in [research/SEMANTIC-REPRESENTATIONS.md](research/SEMANTIC-REPRESENTATIONS.md); four executable discriminators and the first read-only PVM definition projection recorded in [experiments/SEMANTIC-REPRESENTATION-SPIKES.md](experiments/SEMANTIC-REPRESENTATION-SPIKES.md); project-level BPMN XML → source model → versioned data IR → TypeScript interpreter boundary adopted in [PROJECT-DESIGN.md](PROJECT-DESIGN.md) | Exact production source model or executable IR schema, scope algebra, token/activation model, parser comparison, or general compiler implementation |
| CIB Seven oracle | Java 21 runner pins CIB Seven `2.2.0`, H2 `2.3.232`, audit history with `P180D` default TTL, disabled automatic job executor, and explicit logical epoch; public services deploy/start/query/complete; canonical trace omits generated IDs; read-only command-context PVM diagnostic recurses scopes; repeated and JSON-lines tests prove cleanup; Maven wrapper and focused gate are repository-local | Negative semantic/harness/infrastructure classification, jobs and explicit scheduler commands, broader BPMN cases, pristine-versus-modified shadow lane |
| Scenarios and observations | Draft machine-readable spike profile; standard BPMN XML fixture validated against the official XSD; content-addressed neutral scenario with two stimuli, observation requests, provenance, and calibrated canonical trace; corresponding Lean, Java, and TypeScript types; pure exact-result comparator | JSON Schema or generated cross-language codec, general process transports, regression corpus |
| Assurance | Red/green Lean scaffold and sequential lifecycle tests; Lean and TypeScript equality with the independently stored CIB-calibrated trace; poisoned-calibration guard proves the semantic core derives rather than reads its answer; no-speculative-commit guards in both implementations; red/green CIB calibration and persistent-boundary tests; Temporal live refinement equality plus live and retained-history replay; direct concurrent external comparison across CIB, Lean, semantic core, and Temporal; two isolated Temporal identities; clean CIB projection; typed injected state disagreement; provenance and required phase timings; enforced warm/cold budgets; BPMN MIWG interchange corpus and Betsy historical execution benchmark pinned read-only | Temporal fault injection, broad semantic-capsule corpus, BPMN conformance evidence, immutable compatibility report |

The Lean examples prove the profile-independent outcome distinctions and three properties of only the Milestone 0 sequential User Task capsule. The TypeScript tests independently reproduce that trace and the matching negative cases. The complete pipeline establishes exact four-target agreement, detects one seeded observation mutation, repeats Temporal execution under isolated identities, verifies CIB cleanup, and replays live plus retained Temporal histories within the feedback budgets. This is bounded evidence for the declared slice; it is not a BPMN conformance proof or a general CIB compatibility result.
