# Implementation map

This document is the sole detailed owner of current implementation, proof, evidence, and absence status. It describes the repository now, not milestone history.

## Current claim

The repository has an evidence-closed **draft** semantic capsule for one private executable `None Start Event → User Task → None End Event` Process. Exact completion and wrong activation agree across pinned CIB Seven, the Lean reference interpreter, the independent TypeScript semantic core, and the Temporal adapter. For sequential stale completion, CIB Seven, Lean, and the core agree on semantic rejection; Temporal agrees exactly through completion and separately returns adapter-owned `processClosed` under the explicit post-terminal schedule.

The parallel fork/join contract is evidence-closed as a draft under normative per-incoming-Sequence-Flow behavior. The bounded [Semantic Process IL spec](SEMANTIC-PROCESS-IL-SPEC.md), asymmetric runtime representations, and decision not to claim broad CIB parallel compatibility own the maintained definition boundary. Current schemas freeze the checked graph and Semantic Process wire shapes, and adversarial contract tests cover reference, arity, identity, canonical-order, and raw-to-canonical projection failures. The bounded source path produces canonical checked graphs and Semantic Process programs for the sequential model and exact two-branch parallel structure. Lean strictly decodes both artifacts, validates the checked graph and a structural subset of the program contract, recomputes canonical lowering, rejects inequality before evaluation, executes the received program through a generic relation/evaluator, proves evaluator soundness and structural lowering properties, and checks the bounded parallel laws and non-law. Reachability, acyclicity, and producer/consumer shape are guaranteed for admitted artifacts only transitively through checked-source validation plus exact lowering equality; `programWellFormed` does not establish them independently and must be strengthened before program admission widens beyond that binding. Lean's scenario evaluator closes the admitted independent two-task activation pair only under the checked disjointness condition and rejects every other multiple-enabled state as an unresolved semantic choice. The independent TypeScript semantic core executes the same parallel observable contract through its own explicit multiplicity representation and enum-based evaluator. The topology-specific representation and evaluator path are absent. Temporal delegates current-task projection, stimulus well-formedness, command identity, and same-stimulus comparison to semantic-core operations. The [production lifecycle](TEMPORAL-PROCESS-LIFECYCLE-SPEC.md) hosts the program under ordered, duplicate, concurrent, post-terminal, Worker-restart, retained-result, replay, and cleanup evidence. The CIB adapter projects multiple distinct active tasks with semantic sorting and per-element wait multiplicity, and the two balanced completion orders plus the live-sibling stale witness have content-bound raw and canonical CIB evidence under the dedicated normative draft profile. The six-case prepared pipeline establishes exact ordinary four-target agreement, the explicit sequential stale target relation, intermediate Query projections, live replay, projection sensitivity, and rejection of erased parallel Sequence-Flow provenance. The schema-valid negative probe separately establishes candidate deviation `CIB-DEV-0001`.

This is not a general BPMN engine, an OMG conformance result, or an immutable CIB compatibility profile.

## Implemented and absent surfaces

| Surface | Implemented | Explicitly absent |
|---|---|---|
| Project foundation | Lean 4.31.0/Lake 5.0; Node 24.18.0; pnpm 11.17.0; TypeScript 7.0.2; Ajv 8.20.0; Java 21 with explicit, environment-first portable home resolution; Temporal SDK 1.21.0 and CLI v1.8.1; MIT licensing for project-authored material; shared `CLAUDE.md`/`AGENTS.md`; A12-aligned document roles and proposal/spec lifecycle; enforced documentation index, filename-role, and local-link guards; source-synchronized walkthrough; focused and full gates; GitHub Actions verification on Ubuntu and macOS with the warm feedback budget unchanged | Release packaging, published libraries, production deployment |
| Wire contracts | One current structural schema per semantic profile, scenario, canonical result, CIB evidence, checked BPMN graph, and Semantic Process program; stable document kinds; semantic profile/source/compiler identity; exact scenario/profile content binding; six answer-free target scenarios; produced checked-process and Semantic Process artifacts; raw CIB task-query observations with independently verified canonical projections; cross-artifact definition identity and source-origin checks; reference, arity, identity, canonical-order, evidence, and projection mutations; pre-release guard against embedded format counters, retired representation names, and milestone compatibility paths | Parallel legacy schemas, migration readers, compatibility switches, general assertion language |
| Semantic profile | Draft `cibseven-2.2.0-user-task-draft` and `parallel-fork-join-draft` profiles pin their oracle revision, environment, selected features, observation boundary, exclusions, and reviewed CIB–BPMN relationship IDs; the parallel profile assigns normative balanced fork/join meaning without claiming general CIB parallel compatibility; the central register records positive agreement `CIB-AGR-0003` and candidate deviation `CIB-DEV-0001` separately | Separate CIB parallel-compatibility profile, immutable compatibility profile, full requirement classification, approved gap interpretations, selected CIB extensions, confirmed deviations |
| Semantic Process IL | Implemented draft spec for a checked source-facing graph; current JSON Schemas and boundary validators for typed `initiate`, `awaitUserTask`, `duplicate`, `synchronize`, and `terminate` operations; deterministic TypeScript lowerer with exact source/profile/compiler identity and source origins; independent sequential and bounded-parallel TypeScript evaluator; strict Lean artifact decoders and validators; independent Lean lowerer and exact equality gate; generic Lean relation/evaluator with explicit operation choice, evaluator soundness, structural lowering laws, parallel operation semantics, laws, and non-law; bounded growth and stop criteria; separately gated, frozen five-operation checked-source token game and fixture-coincidental positional-lowering discriminator; focused parallel Temporal hosting/refinement and complete balanced four-target evidence | Adopted checked-source operational relation and full observational checked-source-to-program-run preservation proof; the bounded experiment stopped before the correspondence induction and may reopen only under its recorded triggers |
| BPMN source | Exact byte capture and SHA-256; UTF-8/security preflight; private `bpmn-moddle@10.0.0` import; warning/reference-loss rejection; bounded compiler for the sequential and exact balanced two-branch parallel structures; canonical project-owned checked graph and Semantic Process program with source/profile/compiler identity, node/flow origins, and User Task metadata; checked partial CMOF facts including Parallel Gateway direction; optional MIWG admission observation | General BPMN compiler, arbitrary graph admission, synchronous parser CPU isolation, non-UTF-8 decoder, source locations, extension semantics, DI-preserving export, complete CMOF binding, deployment store |
| Lean | Strict checked-graph, Semantic Process, and external scenario JSON decoders with exact-key rejection; independent checked-graph validation and structural program validation; canonical lowerer with definition-identity and Sequence-Flow-origin preservation laws; exact per-artifact lowering equality before evaluation; generic flow-identified token-multiplicity runtime; declarative `OperationStep`/`ProgramStep`, executable operation-identified `step`, and universal evaluator soundness; duplicate and per-incoming-flow synchronize semantics; semantic task occurrences and external command admission; bounded closure for one enabled operation or the exact disjoint two-task activation pair, with every other multiple-enabled state rejected; exact-completion theorem; quantified mismatch rejection/state-preservation theorem; wrong-activation corollary; element-only identity non-law; parallel exact-wait, task-activation observation-order, named-completion, completion-order, live-sibling stale, excess-token, storage-permutation laws and duplicate-left/no-right non-law; generic six-scenario result emitter that consumes and echoes the same admitted scenario documents; experiment-only direct checked-node relation, evaluator soundness, six green mutation controls, and renamed public divergence | Independent `programWellFormed` checks for reachability, acyclicity, and permitted producer/consumer shape; adopted checked-source semantics and run-level observational lowering-preservation proof; enabled-transition, supported-closure, admission, observation, and stimulus-list correspondence remain unresolved; general multiple-enabled closure without an explicit semantic choice or checked commutation argument; general scope/variable/effect model; TypeScript or Temporal correspondence proof; arbitrary BPMN XML parsing |
| TypeScript semantic core | Dependency-free Semantic Process contracts; strict structural program admission for exactly the sequential and balanced two-branch operation graphs; pure enum-based execution of `initiate`, `awaitUserTask`, `duplicate`, per-incoming-flow `synchronize`, and `terminate`; operation-ID-stable internal closure independent of program collection order; explicit flow-identified token multiplicity and sorted semantic task occurrences; exact two-wait fork projection; both completion orders with equivalent final state/observation; duplicate-left/no-right non-law; excess-token retention; storage-order-independent task projection; adapter-facing current-task projection, exact structural stimulus validation, command identity, and same-stimulus comparison; incremental deploy/advance and full scenario evaluation; malformed topology rejection | I/O, parser, Temporal SDK, CIB dependency, general BPMN state model, effects, timers, semantically material nondeterministic scheduling, arbitrary graph execution |
| CIB oracle | Pinned CIB Seven `2.2.0` embedded runner; exact deploy/start/query/complete; generated-task-to-semantic-occurrence mapping; multiple distinct active task projection with semantic sorting and per-element wait multiplicity; exact/wrong/sequential-stale, balanced A-then-B/B-then-A, and live-sibling stale witnesses; bounded engine-level rejection probe for a generated task ID after completion; schema-valid duplicate-same-flow Parallel Gateway probe for candidate `CIB-DEV-0001`; content-bound sequential and balanced-parallel evidence with raw producer task-query observations and independently reconstructed canonical projection; meaningful initial-task and live-sibling projection mutations; explicit evidence replacement; persistent six-scenario JSON-lines batch; PVM diagnostic projection; timings and full cleanup | Repeated live instances of one BPMN element and engine-derived activation ordinals; immutable negative-probe evidence; reused CIB PVM algorithms/types as project semantics; broad CIB corpus adoption; general compatibility claim |
| Temporal adapter | One semantic-lifetime Workflow receiving the start stimulus and admitted Semantic Process program; collision-resistant SHA-256 Workflow ID derived from semantic Process address in production; canonical typed stimulus encoding and SHA-256 content-bound Update IDs; one semantic loop with start queued before handlers; core-owned command policy; a typed `semantic`/`processClosed`/`processUnknown` ingress result kept outside semantic outcomes; retained-Update-first closure recovery; validated address-bound completed receipt; `REJECT_DUPLICATE`; conflicting-identity failure; accepted-handler draining; explicit ordered, post-terminal, accepted-batch, and concurrent harness schedules; unordered one-commit/one-rejection race witness; harness-only Query reconciliation of completion Updates with Event History and receipt, with failed Updates classified as infrastructure and start explicitly excluded; Worker restart, live replay, cleanup, and command-ID-only mutation guard | Retained result beyond Temporal retention; production canonical-observation API; protocol that imposes caller order on concurrent distinct commands; semantic policy copies in the Workflow; Signal compatibility path; committed Event History fixtures; patch branches; legacy representation fallback; production history baseline; general Worker versioning or fault injection; Activities; timers; Search Attributes; Continue-As-New; task inbox |
| Differential pipeline | Six answer-free cases; one CIB batch; one generic Lean result emitter; one core batch; twelve isolated Temporal executions; exact ordinary four-target agreement including parallel live-sibling stale rejection; sequential stale three-way semantic agreement plus exact Temporal completion prefix and separate `processClosed`; retained-CIB comparison; Query/Update/durable-history evidence; definition binding; activation, initial-task, live-sibling, and provenance mutations; six-history replay; isolation, cleanup, timings, and budgets | Universal equivalence, majority voting, general conformance suite; uncorrelated Lean and TypeScript account failure |
| BPMN conformance | Ultimate Process Execution Conformance target and evidence-lane discipline are explicit; a seeded disposition-only requirement ledger records reviewed scope without an exhaustive denominator; MIWG and CIB/Betsy adoption strategies are researched | Any implemented OMG conformance point, exhaustive requirement coverage, percentage-complete claim, or conformance claim |

## Current evidence

Each rule's CIB evidence carries an explicit `engine-observed`, `adapter-derived`, or `adapter-decided` fidelity in [the User Task capsule](capsules/USER-TASK-INTERACTION-SPEC.md#oracle-evidence-fidelity). The wrong-activation refusal is adapter-decided, so its four-target agreement is not four independent derivations of the ordinal rule.

The complete prepared pipeline requires:

- exact agreement for `user-task-discovery-completion` and `user-task-wrong-activation`;
- exact CIB Seven/Lean/core semantic agreement for `user-task-stale-completion`, exact core/Temporal prefix agreement through completion, and separate adapter-owned `processClosed`;
- exact agreement for `parallel-fork-join-a-then-b` and `parallel-fork-join-b-then-a`, including the symmetric one-task intermediate projections;
- exact four-target rejection for `parallel-fork-join-stale-a-while-b-active` with B unchanged;
- equality between current CIB execution and content-bound retained CIB evidence;
- the same waiting projection for successful and wrong-activation inputs before completion;
- exact state preservation after wrong and stale completion rejection;
- equality between the pure core and both isolated Temporal results under each case's explicit target relation;
- correct Query projection and Update outcomes;
- duplicate logical-command stability;
- classified disagreement after activation `1` is mutated to `2`;
- a rejected Lean scenario document with an injected extra answer field, plus exact equality between the admitted scenario and Lean's decoded echo;
- exact Lean definition identity and lowering equality, plus rejection of a schema-valid operation-origin mutation;
- rejection of erased parallel control-place Sequence-Flow provenance, detection of an omitted parallel open task, and detection of a dropped live sibling after stale A;
- six fetched live histories replayed before server shutdown;
- clean CIB and Temporal teardown.

The complete pipeline remains subject to the 15-second warm and 45-second cold budgets. Exact latest measurements and commands belong in [TESTING-SPEC.md](TESTING-SPEC.md) and [PLAN.md](PLAN.md), not in this inventory.

## Nearest unsupported claim

The nearest structurally important unsupported claim is full observational checked-source-to-program-run preservation. The [bounded experiment](experiments/CHECKED-SOURCE-RELATION-EXPERIMENT.md) retained a provisional direct account and renamed positional-lowering discriminator but stopped before enabled-transition, closure, admission, observation, and run-level correspondence at the approved effort boundary. The exact proof framing remains in the [Semantic Process IL spec](SEMANTIC-PROCESS-IL-SPEC.md#lean-specification-and-proof-obligations). The production Temporal lifecycle is implemented under the [lifecycle specification](TEMPORAL-PROCESS-LIFECYCLE-SPEC.md); retained result lookup beyond Temporal retention remains explicitly absent.
