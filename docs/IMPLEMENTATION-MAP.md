# Implementation map

This document is the sole detailed owner of current implementation, proof, evidence, and absence status. It describes the repository now, not milestone history.

## Current claim

The repository has an evidence-closed **draft** semantic capsule for one private executable `None Start Event → User Task → None End Event` Process. Exact completion and wrong activation agree across pinned CIB Seven, the Lean reference interpreter, the independent TypeScript semantic core, and the Temporal adapter. For sequential stale completion, CIB Seven, Lean, and the core agree on semantic rejection; Temporal agrees exactly through completion and separately returns adapter-owned `processClosed` under the explicit post-terminal schedule.

The parallel fork/join contract is evidence-closed as a draft under normative per-incoming-Sequence-Flow behavior. The bounded [Semantic Process IL spec](SEMANTIC-PROCESS-IL-SPEC.md), asymmetric runtime representations, and decision not to claim broad CIB parallel compatibility own the maintained definition boundary. Current schemas freeze the checked graph and Semantic Process wire shapes, and adversarial contract tests cover reference, arity, identity, canonical-order, and raw-to-canonical projection failures. The bounded source path produces canonical checked graphs and Semantic Process programs for the sequential model, exact two-branch parallel structure, exact `PT1S` Intermediate Catch Timer structure, and the exact success-only Service Task binding. Lean strictly decodes both artifacts, validates the checked graph and a structural subset of the program contract, recomputes canonical lowering, rejects inequality before evaluation, executes the received program through a generic relation/evaluator, proves evaluator soundness and structural lowering properties, and checks the bounded parallel, timer, and effect laws and non-laws. Reachability, acyclicity, and producer/consumer shape are guaranteed for admitted artifacts only transitively through checked-source validation plus exact lowering equality; `programWellFormed` does not establish them independently and must be strengthened before program admission widens beyond that binding. Lean's scenario evaluator closes the admitted independent two-task activation pair only under the checked disjointness condition and rejects every other multiple-enabled state as an unresolved semantic choice. The independent TypeScript semantic core executes the same parallel, timer, and effect observable contracts through its own explicit runtime representation. The topology-specific representation and evaluator path are absent. Temporal delegates current projection, stimulus well-formedness, command identity, and same-stimulus comparison to semantic-core operations. The [production lifecycle](TEMPORAL-PROCESS-LIFECYCLE-SPEC.md) hosts the program under ordered, duplicate, concurrent, post-terminal, Worker-restart, retained-result, durable-timer, effect-Activity, replay, and cleanup evidence. The CIB adapter projects multiple active tasks, realizes the timer through controlled-clock job execution, and realizes the selected Service Task through an explicitly released async-before continuation job. The eight-case prepared pipeline establishes the case-specific target relations, intermediate Query projections, live replay, projection sensitivity, timer and Activity mechanism discrimination, and rejection of erased parallel Sequence-Flow provenance. The schema-valid negative probe separately establishes candidate deviation `CIB-DEV-0001`.

The [Intermediate Catch Timer contract](capsules/INTERMEDIATE-CATCH-TIMER-SPEC.md) is evidence-closed as a draft. The exact literal remains in the checked graph and independently lowers to `awaitTimer` duration 1000; Lean and TypeScript own full timer occurrence identity, exact-deadline eligibility, logical-time advancement, refusal, and `openTimers` observation. The scenario's answer-free `fireTimer` is applied directly by Lean and the core, realized by controlled-clock CIB scheduling, and never delivered to Temporal; the Workflow derives the identical content-bound stimulus exclusively from committed core state after one durable timer. Full-server evidence covers Worker absence at due time, exact history, receipt reconciliation, replay, and a separately bundled sleep-bypass mutation. Time skipping is a separately named optional calibration lane.

The [Service Task effect spec](capsules/SERVICE-TASK-EFFECT-SPEC.md) is evidence-closed as a draft. Exact namespace-aware source admission lowers the paired protocol/handler binding through a checked Service Task to `awaitEffect`; Lean and the pure TypeScript core implement effect occurrence, structured intent, matching completion, full-identity and stale refusal, and the separate `openEffects` observation with no caller interaction. The shared wire contract uses one occurrence shape across tasks, timers, and effects. The semantic core projects the explicit definition-field group plus occurrence and descriptor from admitted program data and committed intent. The adapter renders domain-separated transport and completion-command digests and schedules one non-local Activity exclusively from that material. Evidence covers plain success, fail-after-mutation reconciliation, cross-instance separation, omission collisions, host-identity over-inclusion, Worker replacement, typed exhaustion with unchanged committed intent, durable history, replay, Activity bypass, ordinary CIB execution, raw retry facts, content-bound retained evidence, and the complete differential matrix. CIB's effect wait remains adapter-decided and CIB does not claim the project transport key or a semantic effect-in-flight state.

The production semantic realization remains one pure TypeScript core hosted by the TypeScript Temporal Workflow. The [dual semantic-core account](DUAL-SEMANTIC-CORE-ARCHITECTURE-PROPOSAL.md) is rejected; there is no Java semantic core, Java Workflow, or Java-core experiment. JVM integration is an approved architecture boundary for future Activity Workers and client façades, not an implemented surface.

This is not a general BPMN engine, an OMG conformance result, or an immutable CIB compatibility profile.

## Implemented and absent surfaces

| Surface | Implemented | Explicitly absent |
|---|---|---|
| Project foundation | Lean 4.31.0/Lake 5.0; Node 24.18.0; pnpm 11.17.0 with a repository-pinned CI-oriented local virtual-store projection, shared content-addressable store, and ordinary/CI bare-wrapper regression guard; TypeScript 7.0.2; Ajv 8.20.0; direct MIT-licensed Node 24 declarations for strict no-emit checking; substantial pipeline, artifact, evidence-replacement, Java-resolution, strict-JSON, and bounded-process harnesses executed directly as TypeScript under Node's erasable-type stripping; Java 21 with explicit, environment-first portable home resolution; Temporal SDK 1.21.0 and CLI v1.8.1; MIT licensing for project-authored material; shared `CLAUDE.md`/`AGENTS.md`; A12-aligned document roles and proposal/spec lifecycle; enforced documentation index, filename-role, and local-link guards; dependency-free source-hygiene enforcement over tracked and pending Lean/TypeScript/JavaScript/Java source with a 600-nonblank-line review target, 1000-line hard ceiling, exact import-only Lean umbrellas, and no current reviewed exceptions; responsibility-owned Lean semantics, Temporal lifecycle, differential pipeline, contract verification, and CIB runner collaborators; source-synchronized walkthrough; focused and full gates; GitHub Actions verification on Ubuntu and macOS with the warm feedback budget unchanged | Release packaging, published libraries, production deployment; automatic proof of semantic cohesion or function/class responsibility; Semgrep or tree-sitter dependency |
| A12 Workflows product compatibility | Defined `release/2025.06` denominator of 62 physical and 50 distinct exact-byte BPMN models; namespace-aware element/extension/expression census; production delegate, listener, plugin, REST/JMS façade, and downstream-template inventory; bounded CIB Seven `2.0.0`/`2.2.0` assessment; distinct immutable `cibseven-2.0.0-a12-create-document-draft` profile; exact admission of the registered maintained `CreateDocument.bpmn` bytes when the checkout is available; mandatory project-authored equivalent fixture; exact delegate token, literal input, simple local output reference, and modeler metadata preservation; hostile expression, attribute, parameter, and mapping rejection; checked mapping data and exact TypeScript/Lean lowering | Runtime string scopes, evaluated arguments, typed result patch, Process-variable projection, CIB `2.0.0` execution/evidence, Temporal refinement, and a closed unchanged-model admission count; general target expression subset, `BpmnError` semantics, Java delegate Worker bridge, A12 façade adapter, script/listener runtime, or materialized Workflows-enabled full-stack template integration |
| Wire contracts | One current structural schema per semantic profile, scenario, canonical result, CIB evidence, checked BPMN graph, and Semantic Process program; stable document kinds; semantic profile/source/compiler identity; exact scenario/profile content binding; eight answer-free target scenarios; produced checked-process and Semantic Process artifacts; one shared occurrence-ID shape reused by User Tasks, timers, and effects; raw CIB task-query, timer-job, effect-job, and effect-execution observations with independently verified canonical projections; required `openTimers` and separate `openEffects`; typed `fireTimer` and `completeEffect`; JavaScript-safe non-negative integer maxima; exact non-normalized Unicode-scalar identifier order; byte-aware duplicate-key and unpaired-surrogate rejection; TypeScript/Lean edge locks for unknown and missing fields, closed enums, explicit null versus absence, unsafe and fractional numbers, and canonical arrays; matching CIB scalar sorting and safe numeric carriers; cross-artifact definition identity and source-origin checks; reference, arity, identity, canonical-order, evidence, and projection mutations; pre-release guard against embedded format counters, retired representation names, and milestone compatibility paths | Parallel legacy schemas, migration readers, compatibility switches, general assertion language; wider or decimal numeric domain; identifier normalization or locale-sensitive ordering |
| Semantic profile | Draft `cibseven-2.2.0-user-task-draft`, `parallel-fork-join-draft`, `cibseven-2.2.0-intermediate-catch-timer-draft`, and `cibseven-2.2.0-service-task-effect-draft` profiles plus the distinct immutable `cibseven-2.0.0-a12-create-document-draft` profile pin their oracle revision, environment, selected features, observation boundary, exclusions, and reviewed CIB–BPMN relationship IDs; the A12 profile selects exact delegate/input-output extension `CIB-EXT-0002`, synchronous-to-durable operational mapping `CIB-OP-0002`, and pinned `2.0.0` environment `CIB-CFG-0003` | Executable CIB Seven `2.0.0` CreateDocument evidence; separate CIB parallel-compatibility profile; first production compatibility baseline; full requirement classification; approved gap interpretations beyond the reviewed slices; confirmed deviations |
| Semantic Process IL | Implemented draft spec for a checked source-facing graph; current JSON Schemas and boundary validators for typed `initiate`, `awaitUserTask`, `awaitTimer`, `awaitEffect`, `duplicate`, `synchronize`, and `terminate` operations; `awaitEffect` now carries typed input/output mappings with exact empty legacy and one-pair CreateDocument contracts; deterministic TypeScript lowerer and independent Lean decoder/lowerer preserve the admitted mapping data and exact source bodies; independent sequential, bounded-parallel, exact-timer, and payload-free effect evaluator; generic Lean relation/evaluator, laws, and non-laws; separately gated frozen checked-source experiment | Runtime evaluation of the new mappings and effect patch; adopted checked-source operational relation and full observational preservation proof; general effect payload or fault semantics |
| BPMN source | Exact byte capture and SHA-256; UTF-8/security preflight; private `bpmn-moddle@10.0.0` import; warning/reference-loss rejection; bounded compilers for the sequential User Task, balanced parallel, exact `PT1S` timer, payload-free Service Task, and exact A12 CreateDocument shape; the A12 projector preserves exact bytes and excludes BPMNDI/modeler metadata from semantics while retaining the exact expanded delegate attribute and input/output bodies in the checked graph; registered external checkout and project-authored fixture pass unchanged; method/property expressions, extra executable attributes/elements, altered parameters, and altered mapping bodies reject | General BPMN compiler, arbitrary graph admission, Service Task/data bindings beyond the approved exact pairs, timer forms beyond exact `PT1S`, synchronous parser CPU isolation, non-UTF-8 decoder, source locations, general extension semantics, DI-preserving export, complete CMOF binding, deployment store |
| Lean | Project-owned strict JSON parser with duplicate-key, unpaired-surrogate, and safe-integer rejection; strict checked-graph, Semantic Process, and external scenario decoders with exact-key and closed-variant rejection; executable JSON edge-case and Unicode scalar-order locks; independent checked-graph validation and structural program validation; canonical lowerer with definition-identity and Sequence-Flow-origin preservation laws; exact per-artifact lowering equality before evaluation; generic flow-identified token-multiplicity runtime; declarative `OperationStep`/`ProgramStep`, executable operation-identified `step`, and universal evaluator soundness; semantic task, timer, and effect occurrences; bounded closure for one enabled operation or the exact disjoint two-task activation pair, with every other multiple-enabled state rejected; User Task exact-completion and quantified mismatch laws; parallel laws and duplicate-left/no-right non-law; independent `PT1S` normalization and exact lowering; exact timer firing trace; quantified full timer mismatch refusal and early-firing non-law; exact Service Task lowering and success trace; exact start-prefix projection of one structured effect intent; quantified full effect-identity mismatch rejection with state preservation; a contrasting accept-arbitrary-result non-law where the wrong account advances and `applyStimulus` rejects; generic eight-scenario result emitter that consumes and echoes the same admitted scenario documents; experiment-only frozen direct checked-node relation and renamed public divergence | Independent `programWellFormed` checks for reachability, acyclicity, and permitted producer/consumer shape; adopted checked-source semantics and run-level observational lowering-preservation proof; enabled-transition, supported-closure, admission, observation, and stimulus-list correspondence remain unresolved; general multiple-enabled closure without an explicit semantic choice or checked commutation argument; replay/host-attempt stability as a Lean proposition; general scope/variable/effect-result-payload model; TypeScript or Temporal correspondence proof; arbitrary BPMN XML parsing |
| TypeScript semantic core | Dependency-free Semantic Process contracts; shared safe-string admission and Unicode scalar-value comparator; strict structural program admission for exactly the sequential, balanced two-branch, exact timer, and exact effect operation graphs; pure enum-based execution of `initiate`, `awaitUserTask`, `awaitTimer`, `awaitEffect`, `duplicate`, per-incoming-flow `synchronize`, and `terminate`; operation-ID-stable internal closure independent of program collection order; explicit flow-identified token multiplicity and sorted semantic task/timer/effect occurrences; parallel completion-order, excess-token, and non-law witnesses; exact timer wait, deadline, firing, full mismatch, stale refusal, and `openTimers` projection; exact effect wait, structured protocol/handler intent, matching completion, full mismatch and stale refusal, and separate `openEffects` projection with no caller interaction; pure effect-transport material projection from admitted definition fields plus one committed `openEffects` entry without hashing or host identity; adapter-facing current task/timer projection, exact structural stimulus validation including safe integers and scalar strings, command identity, and same-stimulus comparison; incremental deploy/advance and full scenario evaluation; malformed topology rejection | I/O, byte-level parser, Temporal SDK, CIB dependency, general BPMN state model, effect execution or transport digest, effect results with payload/failure, timer forms or races beyond the exact capsule, semantically material nondeterministic scheduling, arbitrary graph execution |
| CIB oracle | Pinned CIB Seven `2.2.0` embedded runner; exact deploy/start/query/complete; generated-task-to-semantic-occurrence mapping; multiple distinct active task projection with semantic sorting and per-element wait multiplicity; exact/wrong/sequential-stale, balanced A-then-B/B-then-A, live-sibling stale, exact timer, and exact Service Task success witnesses; controlled epoch clock; raw timer-job due date/executability plus raw effect-job binding/retry and effect-execution observations; engine-observed pre-due timer eligibility; bounded engine-level stale-task rejection probe; schema-valid duplicate-same-flow Parallel Gateway probe for candidate `CIB-DEV-0001`; warning-free exact Service Task binding, prefix-independent expanded QNames, bean resolution, immediately executable async-before job with no due date, public job-definition/deployed-model projection, independent protocol/handler controls, public retry decrement `3 → 2`, two invocations/one test-local mutation, clean re-execution, and no administrative retry mutation; content-bound evidence with independently reconstructed canonical projection; meaningful task, sibling, timer-deadline, and effect-handler projection mutations; explicit evidence replacement; persistent eight-scenario JSON-lines batch; PVM diagnostic projection; timings and full cleanup | A CIB semantic effect-in-flight state, project transport key, or engine-derived effect activation ordinal; repeated live instances of one BPMN element and engine-derived activation ordinals; full raw derivation of timer logical identity/deadline; immutable negative-probe evidence; reused CIB PVM algorithms/types as project semantics; broad CIB corpus adoption; general compatibility claim |
| Temporal adapter | One semantic-lifetime Workflow receiving the start stimulus and admitted Semantic Process program; collision-resistant SHA-256 Workflow ID derived from semantic Process address in production; one shared Workflow-safe canonical typed-tuple encoder over exact scalar strings and non-negative safe integers for Process addresses, stimuli, timer firings, effect transport, and effect completion; dependency-free deterministic SHA-256 with padding-boundary, supplementary-plane, multi-block, and native-crypto equality locks; fixed literal encodings/digests with distinct domain prefixes; content-bound Update/timer/effect command IDs; adapter-rendered effect transport key with complete field-variation, under-inclusion, cross-instance, and host-over-inclusion witnesses; isolated harness probe stores; one non-local effect Activity derived exclusively from committed intent with two-second start-to-close, ten-second schedule-to-close, two attempts, 100-millisecond configured fixed backoff, no heartbeat, and closed success result; fail-after-mutation reconciliation; typed `BPMN_EFFECT_EXECUTION_EXHAUSTED` Workflow failure with unchanged last committed intent; Worker replacement after an unacknowledged mutation; exact Activity request/policy/final-attempt/result history checks; replay and separately bundled Activity-bypass mutation; one semantic loop with start queued before handlers; core-owned command policy; a typed `semantic`/`processClosed`/`processUnknown` ingress result kept outside semantic outcomes; retained-Update-first closure recovery; validated address-bound completed receipt; `REJECT_DUPLICATE`; conflicting-identity failure; accepted-handler draining; explicit ordered, post-terminal, accepted-batch, concurrent, Worker-down-at-timer-due, and Worker-down-at-effect-pending harness schedules; unordered one-commit/one-rejection race witness; committed-state-derived exact timer duration and firing with no runner delivery; harness-only Query reconciliation with durable Update, timer, Activity, and receipt facts; replay and cleanup; command-ID-only, timer-bypass, and Activity-bypass mutation guards; optional time-skipping calibration outside default verification | Retained result beyond Temporal retention; production canonical-observation API; protocol that imposes caller order on concurrent distinct commands; semantic policy copies in the Workflow; Signal compatibility path; committed Event History fixtures; patch branches; legacy representation fallback; production history baseline; general Worker versioning; effect payloads or faults; Activity heartbeats; cancellation recovery; timer forms/races/cancellation beyond the exact capsule; Search Attributes; Continue-As-New; task inbox |
| Differential pipeline | Eight answer-free cases; one ordinary CIB batch plus one raw-only Service Task fail-once execution; one generic Lean result emitter; one core batch; sixteen isolated Temporal executions; exact case-specific target relations including parallel live-sibling stale rejection, exact timer firing, and Service Task success; sequential stale three-way semantic agreement plus exact Temporal completion prefix and separate `processClosed`; retained-CIB comparison; Query/Update/durable-timer/Activity evidence; definition binding; activation, initial-task, live-sibling, timer-deadline, effect-handler, and provenance mutations; nine-history replay; isolation, cleanup, timings, and budgets | Universal equivalence, majority voting, general conformance suite; uncorrelated Lean and TypeScript account failure |
| BPMN conformance | Ultimate Process Execution Conformance target and evidence-lane discipline are explicit; a seeded disposition-only requirement ledger records reviewed scope without an exhaustive denominator; MIWG and CIB/Betsy adoption strategies are researched | Any implemented OMG conformance point, exhaustive requirement coverage, percentage-complete claim, or conformance claim |

## Current evidence

Each rule's CIB evidence carries an explicit `engine-observed`, `adapter-derived`, or `adapter-decided` fidelity in [the User Task capsule](capsules/USER-TASK-INTERACTION-SPEC.md#oracle-evidence-fidelity), [the Timer capsule](capsules/INTERMEDIATE-CATCH-TIMER-SPEC.md#cib-fidelity-by-rule), and [the Service Task capsule](capsules/SERVICE-TASK-EFFECT-SPEC.md#cib-fidelity-labels). Wrong-activation refusal and the effect wait are adapter-decided, while timer occurrence/deadline projection is adapter-derived; target agreement is never misreported as independent derivation of those project representations.

The complete prepared pipeline requires:

- exact agreement for `user-task-discovery-completion` and `user-task-wrong-activation`;
- exact CIB Seven/Lean/core semantic agreement for `user-task-stale-completion`, exact core/Temporal prefix agreement through completion, and separate adapter-owned `processClosed`;
- exact agreement for `parallel-fork-join-a-then-b` and `parallel-fork-join-b-then-a`, including the symmetric one-task intermediate projections;
- exact four-target rejection for `parallel-fork-join-stale-a-while-b-active` with B unchanged;
- exact four-target agreement for `intermediate-catch-timer-pt1s`, including one wait at deadline 1000, identical content-bound firing command observation, completed logical time 1000, and no caller-enabled interaction;
- exact canonical agreement for `service-task-effect-success`, including one structured effect intent, the content-bound success command, completed state, and no caller-enabled interaction; CIB contributes an explicitly adapter-decided host-realization check rather than an independent semantic effect-in-flight state;
- equality between current CIB execution and content-bound retained CIB evidence;
- exact raw-only CIB retry evidence for one fail-once execution with retries `3 → 2`, two delegate invocations, one test-local mutation, and canonical equality to the retained plain-success execution;
- the same waiting projection for successful and wrong-activation inputs before completion;
- exact state preservation after wrong and stale completion rejection;
- equality between the pure core and both isolated Temporal results under each case's explicit target relation;
- correct Query projection and Update outcomes;
- proof that the Temporal runner never delivers `fireTimer` or `completeEffect`, the Workflow derives each from committed semantic state, and Event History contains the exact timer or Activity mechanism;
- duplicate logical-command stability;
- classified disagreement after activation `1` is mutated to `2`;
- a rejected Lean scenario document with an injected extra answer field, plus exact equality between the admitted scenario and Lean's decoded echo;
- exact Lean definition identity and lowering equality, plus rejection of a schema-valid operation-origin mutation;
- rejection of erased parallel control-place Sequence-Flow provenance, detection of an omitted parallel open task, detection of a dropped live sibling after stale A, detection of a one-millisecond timer-deadline mutation, and detection of a mutated Service Task handler;
- nine fetched live histories replayed before server shutdown;
- clean CIB and Temporal teardown.

The complete pipeline remains subject to the 15-second warm and 45-second cold budgets. Exact latest measurements and commands belong in [PLAN.md](PLAN.md), not in this inventory.

## Nearest unsupported claim

The target inventory is now defined by the [A12 Workflows compatibility ledger](research/A12-WORKFLOWS-COMPATIBILITY-LEDGER.md). It identifies A12 Workflows `release/2025.06` as the product target and the A12 Full Stack Project Template as the canonical downstream blueprint. The exact `2.0.0` target-profile projector admits one of 50 distinct exact-byte A12 Workflows models unchanged at the static source/lowering boundary; zero models have closed runtime, CIB, Temporal, and integration evidence. The nearest unsupported product claims are typed variable and input/output mapping execution, successful CIB Seven `2.0.0`/Temporal refinement, `BpmnError`, and a Java-friendly delegate Activity bridge. General JUEL, scripts, listeners, forms/assignment, message correlation, A12 façade adaptation, engine-plugin behavior, cancellation recovery, faults, incidents as semantic outcomes, and retry exhaustion semantics remain unimplemented with their target-backed priorities recorded in that ledger.

The cross-language wire-hardening prerequisite is closed: schemas cap every current integer at `9007199254740991`, canonical identifiers use exact Unicode scalar-value ordering with no normalization, strict byte readers reject duplicate decoded keys and unpaired surrogates, and the TypeScript, Lean, and CIB boundary guards exercise their applicable representations. `awaitEffect` is part of the checked program, pure semantic accounts, and production Temporal refinement under the exact bounded specification.

The nearest structurally important proof gap remains full observational checked-source-to-program-run preservation. The [frozen bounded experiment](experiments/CHECKED-SOURCE-RELATION-EXPERIMENT.md) retained a provisional direct account and renamed positional-lowering discriminator but stopped before enabled-transition, closure, admission, observation, and run-level correspondence at the approved effort boundary. It is not extended by the timer capsule and reopens only under its recorded triggers. The exact proof framing remains in the [Semantic Process IL spec](SEMANTIC-PROCESS-IL-SPEC.md#lean-specification-and-proof-obligations).
