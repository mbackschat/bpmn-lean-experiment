# Implementation map

This document is the sole detailed owner of current implementation, proof, and evidence status.

| Surface | Implemented | Explicitly absent |
|---|---|---|
| Project contract | Pinned Lean 4.31.0/Lake 5.0, Node 24.18.0, pnpm 11.17.0, TypeScript 7.0.2, Ajv 8.20.0, Temporal SDK 1.21.0, and Temporal CLI v1.8.1 setup; nvm/asdf files and Homebrew fallback wrapper; MIT license for project-authored material; shared `CLAUDE.md`/`AGENTS.md` guidance; project and documentation ownership; command-versus-harness-versus-infrastructure outcome vocabulary; semantic-capsule ownership and epistemic-closure review; dependency-controlled verification script | Release process, CI, packaging |
| Milestone 0 pipeline | M0.0 through M0.6 complete; shared runner and observation contracts; provisional JSON-lines CIB boundary; one-way Lean canonical-result emitter; CIB Surefire result bridge; pure exact-result comparator with typed first-disagreement classification; case registry and reusable single-case harness; one command compiles exact BPMN XML and concurrently runs CIB, Lean, semantic core, and two isolated Temporal executions; executable-IR identity, live and retained replay, provenance, ingestion and execution timings, process-group cleanup, prepared-build mode, and warm/cold budget enforcement | General Lean/TypeScript JSON-lines runners, one-process multi-case full-pipeline batching, extended assurance gate |
| BPMN 2.0.2 source | Official 532-page PDF and eleven normative machine-readable files retained in the ignored research corpus with tracked provenance; isolated `@bpmn-lean/bpmn-source` pins `bpmn-moddle@10.0.0`, captures defensive copies plus SHA-256 before decoding, enforces a caller byte limit and parser Promise-settlement deadline, blocks DOCTYPE, unsupported encodings, all parser warnings, unresolved-reference loss, and unsupported behavior; `bpmn-source-sequential-user-task@0.2.0` compiles the exact sequential Process to project IR with admitted User Task ID and optional name; bounded CMOF manifest records twelve class/generalization and eight property facts and is checked against exact `BPMN20.cmof`; optional pinned MIWG gate classifies all 21 external models without copying them | General BPMN source model/compiler, synchronous parser CPU isolation, non-UTF-8 decoder, stable source locations, warning allowlist, extension semantics, DI-preserving projection/export, complete CMOF binding, deployment store |
| BPMN conformance | Process Execution Conformance identified as the ultimate normative target; modeling/BPEL/choreography scope separated; import/admission and external-suite claim boundaries recorded | Any implemented conformance point or conformance claim |
| Semantic profile | Versioned draft Milestone 0 and User Task interaction artifacts pin CIB Seven `v2.2.0`, Java/H2/history/job/clock environment, feature surfaces, observations, comparison, and exclusions; lifecycle and task-instance traces calibrated; artifact policy in [profiles/README.md](../profiles/README.md) | Immutable identity, complete interpretations, deviations, and unsupported-feature register |
| Lean | Profile-independent outcomes and scenario contract; separate profile/scenario/trace version fields; capsule-specific immutable User Task definition and runtime activation ordinal for None Start → User Task → None End; external command admission separated from bounded internal closure; closure exhaustion classified as harness failure without exposing a committed command; version-routed lifecycle and interaction projections; interpreter-derived lifecycle plus successful, wrong-activation, and stale-completion traces; concrete exact-completion theorem, universal wrong-activation rejection/state-preservation law, and checked element-only identity non-law; compact retained-M0 JSON emitter; separately gated experiment-local source/IR, dual-scope, edge-provenance, and richer runtime candidates | General BPMN source model, parser, executable IR, static semantics, general token/activation/scope model, concurrency, variables, effects, model checking, interaction JSON runner, general JSON-lines transport |
| TypeScript semantic core | Strict dependency-free `@bpmn-lean/semantic-core` package; retained lifecycle IR v0.1 plus named-task IR v0.2 with source/profile/compiler identity and explicit topology; immutable runtime state with activation ordinal; deployment rejects identity, version, task metadata, or topology mismatch; exact task-instance completion admission; bounded internal closure; pure transition application; state-derived open-task and command-ID-free interaction projection; independently derived exact, wrong-activation, and stale-completion results plus malformed-IR and no-speculative-commit tests | General BPMN IR, XML ingestion, variables, scopes, concurrency, timers, messages, effects, jobs, CIB/Temporal dependencies, JSON transport |
| Temporal adapter | Private `@bpmn-lean/temporal-adapter` pins the coordinated SDK 1.21.0 set and full local CLI v1.8.1 server; deployment-time caller passes compiled project IR into one generic Workflow; no parser enters the Workflow bundle; version marker requires IR for new histories while a narrow legacy constructor preserves committed pre-IR replay; synchronous Signal handler deduplicates and queues stimuli; one main loop advances semantic state; diagnostic Query exposes the current projection; live result equals the pure core result; live and committed histories replay | General BPMN IR behavior, Activities, timers, Search Attributes, Continue-As-New, fault injection, cache-eviction/Worker-restart tests, duplicate-delivery test, production Update/Signal API, deployment store and production versioning |
| Formal methods and behavioral refinement | Question-driven capability map, relation hierarchy, risks, alternative-tool fits, candidate experiment design, and claim boundaries recorded in [TLA-AND-BISIMULATION-RESEARCH.md](TLA-AND-BISIMULATION-RESEARCH.md); no auxiliary tool selected; observational stuttering refinement remains an unimplemented adapter design target | Auxiliary dependency or model, model-checker result, counterexample projection, formal simulation relation, bisimulation checker or proof, implementation correspondence |
| Reference instrumentation | Pristine evidence lane, experimental source-lane policy, profiling targets, acceleration limits, branch provenance, and shadow-equivalence gate recorded in [REFERENCE-INSTRUMENTATION.md](REFERENCE-INSTRUMENTATION.md) | Experimental branch, source patch, execution profile, seeded-mutation comparator guard, shadow result |
| Semantic representation research | CIB Model API, deployment parse tree, PVM definition/runtime, fUML activation/offer model, PSSM activation/configuration model, code examples, transfer limits, and provisional project consequences recorded in [research/SEMANTIC-REPRESENTATIONS.md](research/SEMANTIC-REPRESENTATIONS.md); four executable discriminators and the first read-only PVM definition projection recorded in [experiments/SEMANTIC-REPRESENTATION-SPIKES.md](experiments/SEMANTIC-REPRESENTATION-SPIKES.md); project-level BPMN XML → source model → versioned data IR → TypeScript interpreter boundary adopted and its first bounded production schema implemented | General executable IR schema, scope algebra, token/activation model, or general compiler implementation |
| Lean process transfer | Bounded sibling study recorded in [research/A12-KERNEL-LEAN-PROCESS-TRANSFER.md](research/A12-KERNEL-LEAN-PROCESS-TRANSFER.md); semantic definitions, examples, laws, checked non-laws, finite CIB evidence, independent implementation, and Temporal refinement treated as separate claim lanes; immutable evidence and mutation-sensitive projection rules added to contributor and testing guidance | Generic evidence-bundle framework, tracked language-neutral `spec/` body, cross-project sync ledger, broad Lean module reorganization, trust-zone audit machinery |
| CIB Seven oracle | Java 21 runner pins CIB Seven `2.2.0`, H2 `2.3.232`, audit history with `P180D` default TTL, disabled automatic job executor, and explicit logical epoch; public services deploy/start/query/complete; the interaction profile projects one generated CIB task to a structured semantic occurrence and BPMN name, derives command-ID-free enabled interactions from current state, commits exact completion, rejects wrong activation and stale completion without state change, and omits generated IDs; one warm runner batches the three interaction scenarios; read-only command-context PVM diagnostic recurses scopes; repeated and JSON-lines tests prove cleanup; Maven wrapper and focused gate are repository-local | Assignment, authorization, forms, variables, multiple simultaneous or repeated task occurrences, negative infrastructure classification, jobs and explicit scheduler commands, broader BPMN cases, pristine-versus-modified shadow lane |
| Scenarios and observations | Two draft machine-readable spike profiles; language-neutral Draft 2020-12 schemas for profile documents, answer-free scenarios, canonical results, and retained CIB evidence; independent profile-document, semantic-profile, scenario-document, trace, evidence-envelope, IR, and replay version dimensions; standard BPMN XML fixture validated against the official XSD; four content-addressed target scenarios physically separated from immutable producer/projection/result evidence; structured task-instance identity, exact open-task projection, and state-derived command-ID-free enabled interactions; Java, Lean, and TypeScript contract implementations plus pure exact-result comparator | User Task interaction in Temporal/differential pipeline, generated cross-language codec, general process transports, regression corpus |
| Assurance | Red/green source identity, security, warning, lost-reference, unsupported-model, IR identity/version/topology/task metadata, Lean lifecycle and interaction, TypeScript lifecycle and interaction equality, no-speculative-commit, exact/wrong/stale state preservation, CIB lifecycle/task-discovery/exact/wrong/stale completion, answer-smuggling rejection, exact scenario/evidence digest binding, Draft 2020-12 schema evaluation, invalid task-projection mutation, observation-purity metamorphism, retained-shape, process-group timeout cleanup, and persistent-boundary tests; live CIB-versus-retained-evidence comparison; Temporal live refinement equality plus new and retained-history replay; direct exact-source compilation and concurrent comparison across CIB, Lean, semantic core, and Temporal; two isolated Temporal identities; clean CIB projection; typed injected state disagreement; provenance and required phase timings; enforced prepared-pipeline budget; optional mutation-sensitive MIWG classification | User Task interaction closure in Temporal/differential/replay, Temporal fault injection, broad semantic-capsule corpus, BPMN conformance evidence, immutable compatibility report |

The Lean examples prove the profile-independent outcome distinctions, the retained Milestone 0 lifecycle properties, and the bounded User Task occurrence laws listed above. CIB independently supplies three finite interaction traces, and TypeScript now independently derives each of them; Temporal and the complete differential pipeline still implement only the retained lifecycle scenario. The complete M0 pipeline establishes exact four-target agreement, detects one seeded observation mutation, repeats Temporal execution under isolated identities, verifies CIB cleanup, and replays new plus retained Temporal histories within the prepared feedback budget. This is bounded evidence for the declared slices; it is not a BPMN conformance proof or a general CIB compatibility result.
