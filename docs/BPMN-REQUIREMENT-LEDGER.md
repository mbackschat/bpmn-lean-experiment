# BPMN 2.0.2 requirement-disposition ledger

## Boundary

This ledger owns the disposition of reviewed BPMN 2.0.2 Process Execution requirements. It records what the standard requires and how the project currently classifies that requirement; it does not describe implementation inventory, which belongs in the applicable detail maps routed by [`implementation-status-router`](IMPLEMENTATION-MAP.md).

The ledger is deliberately seeded from the requirements already reviewed for the sequential lifecycle, User Task interaction, import boundary, parallel fork/join, and Intermediate Catch Timer specs. It is not an exhaustive extraction of BPMN 2.0.2 and supplies no denominator for Process Execution Conformance. Counts here measure reviewed scope only.

The initial seed avoids reconstructing closed capsule decisions. The owner has selected BPMN coverage as the primary engine roadmap and CIB Seven `2.2.0` executable breadth as the primary near-term ordering baseline, so the two-pass growth rule below expands the construct/mechanism map and then prioritizes its uncovered executable CIB-backed surface without pretending that the line-by-line conformance corpus is already complete. The present ledger makes reviewed scope countable; it still does not make the distance to full Process Execution Conformance measurable.

This is the BPMN engine-coverage ledger. CIB profile relationships belong in the [CIB–BPMN register](CIB-BPMN-RELATION-REGISTER.md), and downstream A12 model/delegate/API adoption belongs in the [A12 Workflows compatibility ledger](research/A12-WORKFLOWS-COMPATIBILITY-LEDGER.md). A requirement may link to both as evidence or priority, but the three denominators are never combined.

## Dispositions

| Disposition | Meaning |
|---|---|
| `supported` | The current reviewed profile assigns bounded executable meaning to the requirement. Evidence can still be draft or incomplete, as linked in the row. |
| `rejected` | Current execution admission deliberately refuses the stated document or construct shape rather than silently assigning behavior. |
| `unsupported` | The requirement is part of the ultimate conformance target but the current reviewed profile has no complete executable disposition for it. |
| `undecided` | A normative conflict, ambiguity, observation boundary, or profile choice remains unresolved. |

These are requirement dispositions, not implementation statuses. A `supported` row does not imply general conformance, and an `unsupported` row does not remove the requirement from the ultimate target.

A row is **structural** or **operational**, and that distinction is not a fifth disposition. A structural row disposes what the source compiler admits and retains; an operational row disposes what the engine executes. One construct can carry a `supported` structural row and an `unsupported` operational one at the same time, which is how retained notation becomes countable without reading as executable support. [Why `preserved` is not a disposition](PRESERVE-ONLY-ADMISSION-SPEC.md#why-preserved-is-not-a-disposition) owns that decision. A structural row names the operational requirement that stays open, and every `BPMN-STRUCT-` row below is bounded to one profile's admitted source.

## Process Execution mechanism-family map

This first-pass map is the planning denominator for reusable BPMN Process Execution mechanism families. It is derived from the Process Execution conformance boundary in Clauses 2.3 and 13, the supporting Process metamodel in Clauses 8–10, and interchange closure in Clause 15. A family row is deliberately broader than a capsule: its disposition remains `unsupported` until the family has a complete reviewed account, while the “closed slice” column names any narrower requirements already supported below.

The thirteen rows make roadmap scope visible without claiming line-by-line corpus completeness or a percentage. Deep review can split a family when two parts have different semantics, dependencies, or evidence; it must not silently remove a family or count a bounded slice as closing the family.

| Family ID | Normative source and machine-readable anchors | Reusable mechanism obligation | Depends on or is co-defined with | Family disposition | Closed reviewed slice |
|---|---|---|---|---|---|
| `BPMN-MECH-DEFINITION-01` | Clauses 8.2, 8.3, 15.1–15.3; CMOF/XSD `Definitions`, `Import`, `RootElement`, QName/ID references | Resolve a self-contained definition set, imports, namespaces, IDs, and cross-file references before executable admission. | — | `unsupported` | Exact-byte, one-file, one-private-Process admission only; see `BPMN-PROCESS-EXECUTABLE-01`. |
| `BPMN-MECH-PROCESS-01` | Clauses 10.1, 13.1–13.2, 13.5.1, 13.5.6; CMOF/XSD `Process`, `StartEvent`, `EndEvent` | Instantiate the selected Process, create and consume control tokens, retain active Activities, and complete or terminate at the defined boundary. | `BPMN-MECH-DEFINITION-01`, `BPMN-MECH-FLOW-01`, `BPMN-MECH-EVENT-01` | `unsupported` | One None Start/None End lifecycle; see `BPMN-PROCESS-SEQUENTIAL-01`. |
| `BPMN-MECH-FLOW-01` | Clauses 8.4.13, 10.1, 13.3.1; CMOF/XSD `FlowNode`, `SequenceFlow`, incoming/outgoing references | Preserve Sequence Flow identity and multiplicity, propagate tokens, and implement uncontrolled incoming flow plus conditional/default outgoing flow. | `BPMN-MECH-PROCESS-01`, `BPMN-MECH-EXPRESSION-01` for conditional flow | `unsupported` | Unconditional flow in exact sequential and balanced-parallel graphs; one exact upstream-returning Sequence Flow cycle is implemented and evidence-closed. General Sequence Flow composition remains unsupported. |
| `BPMN-MECH-ACTIVITY-01` | Clauses 10.3, 13.3.1–13.3.2; CMOF/XSD `Activity`, `Task`, `SubProcess`, data associations | Implement the shared Activity lifecycle, activation cardinality, completion, failure, withdrawal, interruption, and token/data propagation. | `BPMN-MECH-FLOW-01`, `BPMN-MECH-DATA-01`, `BPMN-MECH-SCOPE-01` | `unsupported` | Bounded User Task and successful/typed-error Service Task transitions. |
| `BPMN-MECH-TASK-01` | Clauses 10.3.3, 10.7, 13.3.3; CMOF/XSD Task subclasses, `Operation`, `ResourceRole` | Supply type-specific execution for Service, Send, Receive, User, Business Rule, Script, Manual, and abstract Tasks without duplicating the Activity lifecycle. | `BPMN-MECH-ACTIVITY-01`; task-specific dependencies on messages, resources, expressions, and data | `unsupported` | Bounded User Task interaction and Service Task effect execution; see `BPMN-UTASK-LIFECYCLE-01` and `BPMN-SERVICE-TASK-01`. |
| `BPMN-MECH-GATEWAY-01` | Clauses 10.6, 13.4; CMOF/XSD `Gateway`, `ParallelGateway`, `ExclusiveGateway`, `InclusiveGateway`, `EventBasedGateway`, `ComplexGateway` | Implement token splitting, merging, synchronization, condition choice, deferred choice, and gateway-local state without inheriting evaluator iteration order. | `BPMN-MECH-FLOW-01`, `BPMN-MECH-EXPRESSION-01`, `BPMN-MECH-EVENT-01` | `unsupported` | Exact balanced two-branch Parallel Gateway, exact two-condition-plus-default divergent Exclusive Gateway, one structured selected-branch Inclusive split/join, one bounded operation-addressed Message-versus-`PT1S` Event-Based Gateway deferred choice, and one identity-only Exclusive Merge are evidence-closed. Closed slices remain `BPMN-PARALLEL-GATEWAY-01`, `BPMN-EXCLUSIVE-GATEWAY-01`, `BPMN-EXCLUSIVE-MERGE-01`, `BPMN-INCLUSIVE-GATEWAY-01`, and `BPMN-EVENT-BASED-GATEWAY-01`; general Gateway composition, reachability, trigger sets, executable concurrent Multi-Merge choice, and Complex Gateway behavior remain unsupported. |
| `BPMN-MECH-EVENT-01` | Clauses 10.5, 13.5; CMOF/XSD `Event`, `EventDefinition`, Start/Catch/Throw/Boundary/End variants | Register, wait for, correlate, consume, throw, interrupt, and route Event occurrences across their legal positions and trigger types. | `BPMN-MECH-PROCESS-01`, `BPMN-MECH-SCOPE-01`; trigger-specific dependencies | `unsupported` | None Start/End, exact `PT1S` Intermediate Catch Timer, one directly addressed payload-free Intermediate Catch Message, one exact-code Error Boundary Event, and one interrupting exact-`PT1S` Timer Boundary Event on a User Task; see `BPMN-BOUNDARY-TIMER-01`. |
| `BPMN-MECH-SCOPE-01` | Clauses 10.3.5–10.3.6, 10.5, 13.3.4, 13.5.3–13.5.4; CMOF/XSD `SubProcess`, `CallActivity`, `BoundaryEvent`, `EventSubProcess` | Own nested execution context, propagation, interruption/cancellation, handler lifetime, and Sub-Process/Call Activity completion. | `BPMN-MECH-ACTIVITY-01`, `BPMN-MECH-EVENT-01`, `BPMN-MECH-DATA-01` | `unsupported` | Closed bounded slices include one-level embedded Sub-Process entry/ordinary completion, direct-parent exact-code Error propagation with regional cancellation, and one distinct in-document called Process with normal empty-data return. This does not establish arbitrary nesting, cancellation, or general Call Activity lifecycle; see `BPMN-SUBPROCESS-ENTRY-01`, `BPMN-SUBPROCESS-COMPLETION-01`, `BPMN-ERROR-PROPAGATION-01`, and `BPMN-CALL-ACTIVITY-01`. |
| `BPMN-MECH-DATA-01` | Clauses 8.4, 10.3–10.4, 13.3.2; CMOF/XSD `ItemDefinition`, `DataObject`, `Property`, `DataInput`, `DataOutput`, `DataAssociation`, `InputOutputSpecification` | Preserve typed values and scope, select input/output sets, evaluate associations/mappings, and distinguish absence, null, and other admitted values. | `BPMN-MECH-DEFINITION-01`, `BPMN-MECH-EXPRESSION-01`, owning Activity/scope | `unsupported` | Bounded string/null Process/local variables and literal input/output mapping in two Service Task slices. |
| `BPMN-MECH-EXPRESSION-01` | Clauses 8.3.3, 8.4.13, 10.3–10.6; CMOF/XSD `Expression`, `FormalExpression`, condition and loop expressions | Define expression languages, evaluation context, typing/coercion, determinism, failure, and use in conditions, mappings, loops, assignments, and gateway activation. | `BPMN-MECH-DATA-01` | `unsupported` | Exact literal mapping bodies, exact delegate-expression tokens, and the closed Simple Boolean v1 condition language; no general expression evaluator. |
| `BPMN-MECH-LOOP-01` | Clauses 10.3.6–10.3.8 and 13.3.6–13.3.7; CMOF/XSD `LoopCharacteristics`, `StandardLoopCharacteristics`, `MultiInstanceLoopCharacteristics` | Create, identify, schedule, complete, and cancel standard-loop and sequential/parallel multi-instance Activity instances with their data and completion conditions. | `BPMN-MECH-ACTIVITY-01`, `BPMN-MECH-DATA-01`, `BPMN-MECH-EXPRESSION-01`, `BPMN-MECH-SCOPE-01` | `unsupported` | One bounded collection-driven sequential User Task with exact data mediation and lifetime-Timer interruption; see `BPMN-SEQUENTIAL-MULTI-INSTANCE-01`. |
| `BPMN-MECH-COMPENSATION-01` | Clauses 10.3.5, 10.5, 13.5.5; CMOF/XSD compensation Event definitions, handlers, Transactions | Register completed work, snapshot required context, trigger compensation, and respect dependency/reverse-order rules and transaction cancellation. | `BPMN-MECH-SCOPE-01`, `BPMN-MECH-EVENT-01`, `BPMN-MECH-DATA-01`, `BPMN-MECH-LOOP-01` | `unsupported` | None. |
| `BPMN-MECH-COLLABORATION-01` | Clauses 9, 10.3.3, 13.2–13.3; CMOF/XSD `Collaboration`, `Participant`, `MessageFlow`, `Message`, `CorrelationKey`, `CorrelationSubscription` | Relate executable Processes to Participants, route Messages without treating Message Flow as control flow, and correlate starts/catches/receives to semantic instances. | `BPMN-MECH-DEFINITION-01`, `BPMN-MECH-PROCESS-01`, `BPMN-MECH-EVENT-01`, `BPMN-MECH-DATA-01` | `unsupported` | One external caller may directly address an already-known Process and exact payload-free subscription; no Collaboration, Participant, Message Flow, modeled throw, or key-based routing is operationally closed. One profile imports and retains a definitional Collaboration with its Participants and Message Flows without executing any of it, which is a structural slice of this family and closes none of its mechanism obligation; see `BPMN-STRUCT-COLLABORATION-01`. |

The dependency graph deliberately exposes why “another BPMN element” is often not a small unit. Cycles identify semantics that must be reviewed together, such as Process/Event instantiation and Activity/scope interruption; they are not an implementation order. Exclusive Gateway work depends on exact expression and Sequence Flow decisions; Message Events depend on Collaboration/correlation and Process identity; Sub-Processes depend on Activity, Event, scope, and data lifetime; compensation depends on nearly every stateful family below it.

`BPMN-MECH-EXPRESSION-01` does not require one universal project grammar, AST, or evaluator. The vendor-neutral account owns the evaluation point, visible semantic context, typed result or failure, and consuming BPMN transition; each language profile selects its exact grammar and runtime. The first standards profile is the dependency-free [Simple Boolean expression language](SIMPLE-BOOLEAN-EXPRESSION-DECISION.md), implemented independently in Lean and TypeScript under one explicit URI. The [JUEL architecture](JUEL-EVALUATION-ARCHITECTURE-DECISION.md) remains a deferred CIB compatibility direction. This closes only the exact Simple Boolean expression provider and its Exclusive Gateway consumer; general `FormalExpression`, XPath, and Common Executable expression coverage remain unsupported.

## Reviewed requirements

| Requirement ID | Normative source | Machine-readable anchor | Requirement or conflict | Disposition | Owning semantic decision | Evidence status |
|---|---|---|---|---|---|---|
| `BPMN-CONF-EXEC-SEM-01` | Clauses 2.3.1 and 13 | BPMN20 CMOF Process package and executable FlowElement subclasses | A Process Execution conforming tool implements the operational execution semantics and Activity lifecycle for the applicable Process metamodel. | `unsupported` | [Conformance target](BPMN-CONFORMANCE-TARGET.md#process-execution-boundary); bounded supported slices are listed separately below. | [`implementation-status-root-owner`](IMPLEMENTATION-MAP.md#current-claim) explicitly excludes general Process Execution Conformance. |
| `BPMN-CONF-EXEC-IMPORT-01` | Clause 2.3.2 | `tDefinitions`, `tProcess`, and `tCollaboration`; CMOF `Process` and `Collaboration` | A Process Execution conforming tool imports BPMN Process diagram types including their definitional Collaboration. | `unsupported` | [Import and admission policy](BPMN-CONFORMANCE-TARGET.md#import-and-admission-policy) | [`implementation-status-owner:ENGINE-CONTRACTS-SOURCE`](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md#bpmn-source) executes one self-contained private Process and closes no import. One profile now imports a definitional Collaboration structurally and retains it unexecuted; the six `BPMN-STRUCT-` rows below bound exactly what that reaches, and none of it makes this operational import requirement supported. |
| `BPMN-CONF-IMPORT-EXPORT-01` | Clause 2 summary table versus Clause 2.3.2 | BPMN document root and Diagram Interchange schemas | The summary says Process Execution exchange is Import/Export, while the detailed conformance clause explicitly requires import only. | `undecided` | [Recorded source conflict](BPMN-CONFORMANCE-TARGET.md#import-and-admission-policy) | No export or round-trip execution claim exists. |
| `BPMN-IMPORT-LOCATION-01` | Clause 15 and the Infrastructure metamodel/XSD | CMOF `Import.location` versus XSD `tImport.location` | The metamodel prose permits an omitted import location while the XSD requires one. | `undecided` | [Recorded source conflict](BPMN-CONFORMANCE-TARGET.md#formalization-consequences) | General imports are absent from the current source boundary. |
| `BPMN-PROCESS-EXECUTABLE-01` | Clause 10 Process attributes and Clause 13 execution preconditions | CMOF `Process.isExecutable`; XSD optional `tProcess.isExecutable` | The sources conflict over the default of omitted `isExecutable`; the current conservative execution boundary admits only a private Process with explicit `isExecutable="true"`. | `rejected` | [Conservative admission policy](BPMN-CONFORMANCE-TARGET.md#import-and-admission-policy) | [Compiler admission](../packages/bpmn-source/src/checked-process-compiler.ts) and its focused tests reject the omitted, false, or public-Process shapes for the current executable slice. |
| `BPMN-PROCESS-SEQUENTIAL-01` | Clauses 13.2 and 13.3 | CMOF `Process`, `StartEvent`, `SequenceFlow`, `UserTask`, and `EndEvent`; corresponding `t*` XSD types | For the admitted private sequential model, Process start reaches one User Task wait and exact task completion reaches Process completion through the None End Event. | `supported` | `CIB-AGR-0001` in the [CIB–BPMN register](CIB-BPMN-RELATION-REGISTER.md#cib-agr-0001--sequential-process-and-user-task-lifecycle) and the [User Task capsule](capsules/USER-TASK-INTERACTION-SPEC.md) | [`implementation-status-owner:ASSURANCE-ADOPTION`](ASSURANCE-AND-ADOPTION-IMPLEMENTATION-MAP.md#evidence-owners) bind the Lean, CIB, TypeScript, and Temporal lanes. |
| `BPMN-SEQUENTIAL-MULTI-INSTANCE-01` | Clauses 8.4.10, 10.3.8, 10.4.1–10.4.2, 10.5.5, 10.5.6, 13.3.2, 13.3.7, and 13.5.3; Tables 8.47, 10.29, 10.30, 10.51–10.64, 10.91, 10.92, 10.101, and 10.122 | CMOF/XSD `MultiInstanceLoopCharacteristics`, `InputOutputSpecification`, `InputSet`, `OutputSet`, `ItemDefinition`, `DataObject`, `DataObjectReference`, `DataInputAssociation`, `DataOutputAssociation`, `BoundaryEvent-cancelActivity`, `TimerEventDefinition.timeDuration`, runtime instance attributes, and exact loop input/output item references | For the bounded collection-driven sequential User Task slice, evaluate the desired collection cardinality once, generate one inner instance at a time in collection order, preserve distinct outer and inner identity, transfer one scalar input and output per iteration through one exact set and association graph, publish the complete ordered output collection only on natural completion, and cancel every generated inner instance when the exact interrupting Timer Boundary Event cancels the outer Activity. | `supported` | [Sequential Multi-Instance User Task specification](capsules/SEQUENTIAL-MULTI-INSTANCE-SPEC.md), reusing the reviewed [Activity boundary Timer account](capsules/ACTIVITY-BOUNDARY-TIMER-SPEC.md#normative-basis) only for its trigger and interruption shape | Exact source admission and lowering, declarative Lean relations and evaluator soundness, the independently written TypeScript core, registered natural and interrupted differential cases, production Workflow-chain refinement and replay, exact capacity evidence, E1/E2 occurrence publication, and one exact retained whole-model BPMN source under one digest with two retained schedules and corpus entries implement the bounded slice. CIB evidence remains separately limited to `CIB-AGR-0011`, `CIB-INT-0002`, and `CIB-LIM-0001`; the broad loop family remains unsupported. |
| `BPMN-PARALLEL-MULTI-INSTANCE-01` | Clauses 8.4.10, 10.3.8, 10.4.1–10.4.2, 10.5.5, 10.5.6, 13.3.2, 13.3.7, and 13.5.3; Tables 8.47, 10.29, 10.30, 10.51–10.64, 10.91, 10.92, 10.101, and 10.122 | CMOF/XSD `MultiInstanceLoopCharacteristics` including default-false `isSequential`, default-All `behavior`, `completionCondition`, exact loop input/output item references, runtime instance attributes, the existing direct data-association graph, and exact interrupting boundary Timer fields | For the bounded collection-driven parallel User Task slice, evaluate cardinality once, atomically generate one distinct inner occurrence per ordered input, preserve indexed scalar results independently of completion order, publish the complete ordered output only after every instance completes, publish no partial output when the exact completion condition terminates remaining instances, and cancel all remaining instances when the exact outer interrupting Timer fires. | `unsupported` | [Parallel Multi-Instance User Task proposal](capsules/PARALLEL-MULTI-INSTANCE-PROPOSAL.md), reusing the implemented sequential data graph and Activity boundary Timer trigger only where their reviewed propositions are unchanged | Proposal drafted; no source admission, runtime execution, proof, differential, retained corpus, capacity, or production Temporal refinement evidence exists yet. No CIB parallel Multi-Instance target is selected, and the broad loop family remains unsupported. |
| `BPMN-UTASK-LIFECYCLE-01` | Clauses 10.7.3, 13.3.2, and 13.3.3 | CMOF `UserTask`; XSD `tUserTask` | The admitted User Task becomes active, is discoverable through the selected task-manager boundary, and completing the exact active occurrence permits outgoing control to continue. | `supported` | Rules `UTASK-ACTIVATE-01`, `UTASK-DISCOVER-01`, and `UTASK-COMPLETE-01` in the [User Task capsule](capsules/USER-TASK-INTERACTION-SPEC.md#stable-semantic-rules-and-evidence) | The capsule matrix keeps the finite Lean, CIB, TypeScript, and Temporal evidence lanes separate. |
| `BPMN-UTASK-REFUSAL-01` | Clauses 10.7.3, 13.3.2, and 13.3.3 plus project operational mapping `CIB-OP-0001` | CMOF `UserTask`; host task identity has no BPMN machine-readable equivalent | A command for a non-active semantic occurrence does not complete the active User Task or reactivate a completed occurrence. | `supported` | Rules `UTASK-REFUSE-01` and `UTASK-REFUSE-02` in the [User Task capsule](capsules/USER-TASK-INTERACTION-SPEC.md#stable-semantic-rules-and-evidence) | Wrong activation remains adapter-decided for the ordinal clause; stale host-task refusal has the adjacent [engine consistency probe](../runners/cibseven/src/test/java/org/bpmnlean/cibseven/CibSevenConsistencyProbeTest.java). |
| `BPMN-UTASK-POTENTIAL-OWNER-01` | Clauses 10.3.1 and 10.3.4 plus Tables 10.3 and 10.5 | CMOF/XSD `ResourceRole`, `Performer`, `HumanPerformer`, `PotentialOwner`, `Resource`, and assignment-expression path | A User Task may identify potential owners through the standard Resource Role hierarchy; potential owners are persons who can claim and work on the task. | `unsupported` | The [User Task assignment and form metadata specification](capsules/USER-TASK-ASSIGNMENT-FORM-METADATA-SPEC.md#authority-and-forward-compatible-boundary) keeps this portable account conforming but deferred while selecting a separately classified literal-group CIB extension. | No standard Resource identity, assignment-expression language, person/group distinction, identity directory, claim transition, or public projection is implemented. The pinned CIB control does not turn a Potential Owner `resourceRef` into a candidate link. |
| `BPMN-UTASK-RENDERING-01` | Clause 10.3.4 and Table 10.13 | CMOF/XSD `UserTask.renderings` and `Rendering` | A User Task may contain zero or more Rendering extension hooks whose content is deliberately opaque to BPMN. | `supported` | The bounded structural rule in the [structured Human Work specification](BPM-PLATFORM-STRUCTURED-HUMAN-WORK-SPEC.md#source-admission-and-rendering-projection) admits `UserTask.renderings` only for the M6 profile, retains its standard subtree opaquely in the exact source, and strips it before checked projection. | Exact source admission and twin lowering evidence cover one optional standard `Rendering` child and warning containment inside that subtree. Rendering content has no semantic or public projection. Other profiles, WSHumanTask rendering, portable schemas, renderers, validation, and submission mapping remain unsupported. |
| `BPMN-PARALLEL-GATEWAY-01` | Clauses 10.6.4 and 13.4.1, Figure 13.3, and Table 13.1 | CMOF `ParallelGateway` extends `Gateway`; XSD `tParallelGateway`; incoming/outgoing `SequenceFlow` references | A Parallel Gateway waits for at least one token on every incoming Sequence Flow, consumes exactly one from each incoming flow, retains excess tokens, and produces exactly one token on every outgoing flow. | `supported` | The [parallel fork/join spec](capsules/PARALLEL-FORK-JOIN-SPEC.md) owns `PAR-FORK-01`, `PAR-JOIN-READY-01`, and `PAR-JOIN-CONSUME-01` under normative per-incoming-flow semantics. | The bounded capsule is evidence-closed as a draft across checked source/lowering, Lean, TypeScript, balanced and live-sibling pinned-CIB evidence, semantic-lifetime Temporal refinement/replay, and the declared-target differential. The [count/provenance discriminator](experiments/SEMANTIC-REPRESENTATION-EXPERIMENT.md#experiment-3-join-arrival-count-versus-edge-provenance) and bounded CIB probe separately establish candidate deviation `CIB-DEV-0001`; immutable negative evidence remains open. |
| `BPMN-EXCLUSIVE-GATEWAY-01` | Clause 13.4.2 and Table 13.2 | CMOF `ExclusiveGateway` extends `Gateway`; XSD `tExclusiveGateway`; conditional/default `SequenceFlow` | A divergent Exclusive Gateway evaluates outgoing conditions in a defined order, selects the first true conditional Sequence Flow, and selects the default Sequence Flow only when every condition is false. | `supported` | The [Exclusive Gateway condition specification](capsules/EXCLUSIVE-GATEWAY-CONDITION-SPEC.md) owns the exact two-condition-plus-default Simple Boolean slice; `CIB-AGR-0006` and `CIB-INT-0001` remain separate CIB calibration facts. | Strict checked source and generic `choose` lowering, independent Lean and TypeScript parsers/evaluators, targeted closure and reachability guards, answer-free standards evidence, Temporal replay/bypass discrimination, and the 13-case declared-target differential close this bounded slice. Pinned CIB probes establish declaration order and first-true/default behavior for JUEL but are not Simple Boolean truth evidence. |
| `BPMN-EXCLUSIVE-MERGE-01` | Clauses 10.6.2 and 13.4.2, Figure 13.4, and Table 13.2 | CMOF `ExclusiveGateway` extends `Gateway`; XSD `tExclusiveGateway`; incoming/outgoing `SequenceFlow` references and `gatewayDirection` | A converging Exclusive Gateway passes each arriving alternative token to its outgoing Sequence Flow without synchronizing with the other incoming paths. | `supported` | Rule `CYCLE-MERGE-01` in the [resumption-bounded cyclic control flow specification](capsules/CYCLIC-CONTROL-FLOW-SPEC.md) selects a general per-offered-token declarative relation while its exact profile proves only one offered occurrence reachable; executable concurrent Multi-Merge selection remains unsupported. | Identity-only checked source, reusable nonempty `mergeExclusive`, exact checked-to-IL binding, independent Lean and TypeScript semantics, profile-local exact-three admission, a registered answer-free profile/scenario, differential discrimination, durable Temporal execution/replay, and real candidate-output and occurrence-identity mutations close the exact selected slice. |
| `BPMN-SEQUENCE-FLOW-CYCLE-01` | Table 7.2 plus Clauses 13.2, 13.3.2, and 13.4.2 | CMOF/XSD `FlowNode`, `SequenceFlow`, `ExclusiveGateway`, and `UserTask` | A Sequence Flow may connect to an upstream Flow Node, so an executable Process graph may revisit the same Flow Node and create a fresh Activity occurrence until a selected route exits the cycle. | `supported` | Rules `CYCLE-ADMIT-01`, `CYCLE-WAIT-01`, `CYCLE-REPEAT-01`, `CYCLE-EXIT-01`, and `CYCLE-CLOSURE-01` in the [resumption-bounded cyclic control flow specification](capsules/CYCLIC-CONTROL-FLOW-SPEC.md) select one root-scope User Task cycle whose every directed cycle crosses an explicit resumption boundary. | Full-graph reachability/co-reachability, profile-selected User Task continuation cuts, strict source/lowering, quantified Lean repeat/exit and actual-closure results, an independent TypeScript runtime, passive host admission, registered answer-free evidence, differential discrimination, Worker replacement, stale refusal, exact history inspection, replay, and real occurrence-identity mutations close this slice. Standard Loop Characteristics, Multi-Instance, concurrent tokens, unconditional termination, and unbounded durable history remain unsupported. |
| `BPMN-TIMER-DURATION-01` | Clause 10.5.5 and Tables 10.101 and 10.122 | CMOF `TimerEventDefinition.timeDuration`; XSD `tTimerEventDefinition.timeDuration` | The admitted Intermediate Catch Timer Event contains exactly one relative-duration expression whose exact lexical value is `PT1S`; the bounded profile normalizes only that reviewed literal to 1000 logical milliseconds. | `supported` | The [Intermediate Catch Timer spec](capsules/INTERMEDIATE-CATCH-TIMER-SPEC.md#source-profile) owns the lexical subset and independent lowering check. | Checked source retains `PT1S`; Lean and TypeScript lower it independently to `1000`; all other duration values and timer-expression forms are rejected by this profile rather than assigned semantics. |
| `BPMN-TIMER-CATCH-01` | Clause 10.5.4 and Table 10.89 | CMOF `IntermediateCatchEvent` and `TimerEventDefinition`; XSD `tIntermediateCatchEvent` and `tTimerEventDefinition` | A normal-flow catching Intermediate Timer Event retains the token until its trigger occurs and then permits outgoing Sequence Flow progress. | `supported` | Rules `TIMER-WAIT-01`, `TIMER-FIRE-01`, `TIMER-REFUSE-01`, and `TIMER-OBSERVE-01` in the [Intermediate Catch Timer spec](capsules/INTERMEDIATE-CATCH-TIMER-SPEC.md#stable-semantic-rules) | The bounded exact-deadline capsule is evidence-closed as a draft across Lean, TypeScript, controlled-clock CIB evidence, durable Temporal timer/restart/replay evidence, and the declared-target differential. The logical-millisecond deadline projection is project-owned and CIB adapter-derived, not a normative BPMN representation. |
| `BPMN-MESSAGE-DEFINITION-01` | Clauses 8.4.11, 8.5, 10.5.5, and Table 10.99 | CMOF/XSD `MessageEventDefinition.messageRef`, `MessageEventDefinition.operationRef`, `Interface.operation`, and `Operation.inMessageRef` | The admitted executable payload-free Message catch resolves one Message and one Interface Operation whose input Message is the same referenced Message. | `supported` | The exact source profile and reference-selection rule in the [Intermediate Catch Message spec](capsules/INTERMEDIATE-CATCH-MESSAGE-SPEC.md#selected-source-profile) | Strict source admission and CMOF-bound facts reject unresolved, inconsistent, payload-bearing, implemented, or additional definition chains; TypeScript and Lean independently preserve the selected channel through lowering. |
| `BPMN-MESSAGE-START-01` | Clauses 8.4.11, 8.5, 10.5.2, 10.5.5, 13.2, and 13.5.1 plus Tables 10.84, 10.87, and 10.99 | CMOF/XSD `StartEvent`, `MessageEventDefinition`, `Message`, `Interface`, `Operation`, `SequenceFlow`, `messageRef`, `operationRef`, and `inMessageRef` | A top-level Message Start Event with one resolved Message definition generates a new Process instance when its exact Message trigger occurs and produces one token on each outgoing Sequence Flow. | `supported` | The [Message Start Event specification](capsules/MESSAGE-START-EVENT-SPEC.md) owns exact source admission, checked and IL identity, Lean/core initiation and closure, strict wires, a registered answer-free scenario, and direct Temporal Workflow-start hosting. | The exact selected slice is closure-reviewed and evidence-closed. Product routing remains absent. Multiple starts, payload, correlation, Message Flow execution, broker behavior, CIB compatibility, and Event Sub-Process start remain unsupported. |
| `BPMN-TIMER-START-01` | Clauses 10.5.2, 10.5.5, 10.5.6, 13.2, and 13.5.1 plus Tables 10.84 and 10.101 | CMOF/XSD `StartEvent`, `TimerEventDefinition`, `FormalExpression`, `SequenceFlow`, `timeDate`, `timeCycle`, and `timeDuration` | A top-level Timer Start Event generates a new Process instance when its declared timer occurrence is triggered and produces one token on each outgoing Sequence Flow. | `supported` | The [Timer Start Event specification](capsules/TIMER-START-EVENT-SPEC.md) is closure-reviewed and evidence-closed, with exact source admission, checked and IL identity, Lean/core initiation and closure, strict wires, a registered standards-only profile/scenario/example/differential case, and live one-action Temporal Schedule history/replay evidence through the opaque service-returned execution identity. | Support is limited to one exact top-level `PT1S` Timer Start and one resolved occurrence. Product 2 scheduling, recurrence, calendar forms, multiple starts, and definition-version activation remain unsupported. |
| `BPMN-TERMINATE-END-01` | Clauses 10.5.6, 13.2, 13.3.4, and 13.5.6 plus Table 12.22 | CMOF/XSD `EndEvent`, `ThrowEvent`, `TerminateEventDefinition`, `FlowElementsContainer`, `SubProcess`, and `SequenceFlow` | Reaching a Terminate End Event terminates every remaining active owner in its containing scope; Clause 13.5.6 makes a nested occurrence local to that Sub-Process instance, while root placement completes the Process. | `supported` | The [Terminate End Event specification](capsules/TERMINATE-END-EVENT-SPEC.md) implements one exact nested parallel witness and a reusable no-output containing-scope termination operation. | Exact source/CMOF/XSD admission, checked and IL shapes, strict schemas and artifact binding, independent Lean/core transitions, closure/refusal discriminators, a registered standards-only profile with three answer-free scenarios and differential mutations, passive host admission, Worker replacement, stale refusal, a durable global-cancellation mutation, history inspection, and replay close the selected evidence boundary. Event Sub-Process, Transaction, compensation, Call Activity propagation, arbitrary nesting, CIB compatibility, and Product 2 cancellation remain outside the specification. |
| `BPMN-ABSTRACT-TASK-01` | Clauses 10.3.3.1 and 13.3.3 plus Table 10.4 | CMOF/XSD `Task` and `tTask` | A Task with no further type specification is an Abstract Task; upon activation it completes as a conceptual model and is never executed by an IT system. | `unsupported` | The [configured Task extension specification](capsules/CONFIGURED-GENERIC-TASK-SPEC.md) preserves this meaning but defers plain Abstract Task admission from its bounded profile. | Existing executable profiles reject plain `bpmn:task`; no checked node, immediate-completion lowering, Lean/core behavior, registered scenario, or Temporal evidence currently implements the standard shape. |
| `BPMN-TASK-EXTENSION-01` | Clauses 7.7, 8.3.3, and 10.3.3.1 | CMOF/XSD `Task`, `BaseElement.extensionElements`, and the extension wildcard | BPMN adopters may define additional Task types without changing the standard element's semantics; the bounded profile selects one exact versioned configured external-effect Task. | `supported` | Rules `CTASK-SOURCE-01`, `CTASK-BIND-01`, `CTASK-LOWER-01`, and `CTASK-WAIT-01` in the [configured Task extension specification](capsules/CONFIGURED-GENERIC-TASK-SPEC.md) own the exact implemented slice. | Source, checked identity, lowering, Lean/core execution, strict wires, registered answer-free evidence, differential mutation, and live Activity/Worker-replacement/history/replay evidence close the exact selected slice. Pinned CIB pass-through remains an exclusion oracle only. Plain Abstract Task, arbitrary extensions, dynamic handlers, data, Error routes, retries as BPMN semantics, CIB compatibility, and Product 2 work queues remain excluded. |
| `BPMN-MESSAGE-CATCH-01` | Clauses 8.4.2, 10.5.1, and 10.5.4 | CMOF/XSD `IntermediateCatchEvent`, `MessageEventDefinition`, `Message`, `Interface`, and `Operation` | A normal-flow catching Intermediate Message Event retains its token until the addressed Message arrives, consumes the active subscription once, and then permits outgoing Sequence Flow progress. | `supported` | Rules `MSG-WAIT-01`, `MSG-DELIVER-01`, `MSG-REFUSE-01`, and `MSG-OBSERVE-01` in the [Intermediate Catch Message spec](capsules/INTERMEDIATE-CATCH-MESSAGE-SPEC.md#stable-semantic-rules) | The bounded direct-address capsule is evidence-closed across checked source/lowering, Lean, TypeScript, durable Temporal Signal/result-ledger/restart/replay evidence, and the declared-target differential. Payload, key-based/global correlation, modeled throw, Message Flow, CIB compatibility, and A12 façade adoption remain unsupported. |
| `BPMN-RECEIVE-TASK-01` | Clauses 8.4.2, 10 Receive Task and Table 10.10, and 13.3.3 | CMOF/XSD `ReceiveTask` and `Message`; optional `messageRef`, `operationRef`, and `instantiate` properties | An activated non-instantiating Receive Task with an associated direct Message waits for that Message and completes the Activity when it arrives. | `supported` | The implemented [Message-addressed Receive Task specification](capsules/RECEIVE-TASK-MESSAGE-SPEC.md) selects one payload-free direct-Message slice and reuses the existing subscription transition. Content-bound CIB Seven `2.2.0` evidence establishes bounded public subscription and completion agreement under `CIB-AGR-0009`/`CIB-OP-0005`; exact checked source/lowering and independent Lean/core execution establish the semantic checkpoint. | The declared-target differential establishes four-target agreement and a closed-arm mutation; the focused Temporal witness establishes direct Query projection, live wrong-kind refusal, Worker-absence Signal recovery, terminal receipt, exact mutation-sensitive history, and replay. Addressless signal, operation binding, instantiation, payload, and general correlation remain excluded. |
| `BPMN-RECEIVE-TASK-IMPLEMENTATION-01` | Clause 10 Receive Task and Table 10.10 | XSD `tReceiveTask.implementation` default `##WebService`; the installed moddle descriptor does not materialize that default | Omitting `implementation` semantically selects the BPMN default Web-service technology rather than leaving transport unspecified. | `unsupported` | The [Receive Task specification](capsules/RECEIVE-TASK-MESSAGE-SPEC.md) admits the omitted/defaulted CIB-corpus shape but records Web-service realization as a bounded profile limitation; Temporal Signal hosts only the selected semantic input. | No Web-service binding, WSDL operation, endpoint, transport conformance, or non-default implementation is admitted or evidenced. |
| `BPMN-SERVICE-TASK-01` | Clause 10.3.3.1, Table 10.8, and Clause 13.3.3 | CMOF `ServiceTask` and its `implementation`/`operationRef` properties; XSD `tServiceTask` | Under the admitted bounded success subsets, a Service Task invokes its selected service on activation and completes after successful service completion. | `supported` | Rules `EFFECT-WAIT-01`, `EFFECT-INTENT-01`, `EFFECT-RESULT-01`, `EFFECT-REFUSE-01`, and `EFFECT-OBSERVE-01` in the [Service Task effect spec](capsules/SERVICE-TASK-EFFECT-SPEC.md), plus the bounded data/mapping rules in the [CreateDocument spec](capsules/CREATE-DOCUMENT-DATA-SPEC.md) | The bounded capsules are evidence-closed as drafts across checked source/lowering, Lean, TypeScript, pinned-CIB host realization, Temporal Activity refinement/replay, and the declared-target differential. CIB host waits and synchronous mapping transactions are not independent derivations of the project effect-in-flight state. |
| `BPMN-SERVICE-TASK-FAULT-01` | Clause 10.3.3.1 and Clause 13.3.3 | CMOF `ServiceTask`, `Operation`, and `Error`; XSD `tServiceTask`, `tOperation`, and `tError` | Under the admitted typed-business-error slice, a matching service fault interrupts the active Service Task instead of completing its normal output. | `supported` | Rules `BERROR-CATCH-01`, `BERROR-INTERRUPT-01`, `BERROR-MESSAGE-01`, and `BERROR-REFUSE-01` in the [boundary-error spec](capsules/BOUNDARY-ERROR-SPEC.md) | Lean and TypeScript implement the typed result and exact-code interruption; Temporal carries it as a successful Activity result and keeps exhausted Activity attempts as adapter failure. General service faults, incidents, retry exhaustion as semantics, propagation, and unmatched-Error semantics remain unsupported. |
| `BPMN-BOUNDARY-ERROR-01` | Clause 13.5.3 and Tables 10.86, 10.91, and 10.92 | CMOF `BoundaryEvent`, `ErrorEventDefinition`, and `Error`; XSD `tBoundaryEvent`, `tErrorEventDefinition`, and `tError` | For the admitted same-Process exact-code case, an attached interrupting Error Boundary Event catches the matching Error, abandons the normal Activity route, and continues only through its boundary Sequence Flow. | `supported` | Rules `BERROR-CATCH-01`, `BERROR-INTERRUPT-01`, `BERROR-REFUSE-01`, and `BERROR-OBSERVE-01` in the [boundary-error spec](capsules/BOUNDARY-ERROR-SPEC.md); profile-scoped caught-path mapping is separately owned by `BERROR-CIBMAP-01`. | Checked source/lowering, Lean, TypeScript, synchronous CIB host realization, Temporal refinement/replay, and the declared-target differential close this bounded route. Catch-all handlers, multiple handlers, nested propagation, Error End Events, Error throws, and general cancellation remain unsupported. |
| `BPMN-SUBPROCESS-ENTRY-01` | Clauses 10.3.5 and 13.3.4 | CMOF/XSD `SubProcess`, `FlowElementsContainer`, `FlowNode`, and `SequenceFlow` | Reaching the admitted one-level embedded Sub-Process instantiates one child scope occurrence and starts its unique empty Start Event while keeping child runtime ownership distinct from the enclosing Process. | `supported` | Rule `SUBPROC-ENTER-01` in the [ordinary embedded Sub-Process completion specification](capsules/EMBEDDED-SUBPROCESS-COMPLETION-SPEC.md) | Exact scoped-source admission and lowering, independent Lean and TypeScript scope-entry semantics, answer-free four-target scenarios, retained CIB agreement, Temporal Worker replacement, and replay establish the bounded entry claim. |
| `BPMN-SUBPROCESS-COMPLETION-01` | Clauses 10.3.5 and 13.3.4 | CMOF/XSD `SubProcess`, `FlowElementsContainer`, `EndEvent`, `SequenceFlow`, and `UserTask` | An admitted ordinary embedded Sub-Process completes only after no token and no active Activity remains in its scope occurrence; completion removes that occurrence and continues exactly once through the Sub-Process's normal outgoing Sequence Flow. | `supported` | Rules `SUBPROC-END-01` and `SUBPROC-QUIESCE-01` in the [ordinary embedded Sub-Process completion specification](capsules/EMBEDDED-SUBPROCESS-COMPLETION-SPEC.md) | The two-child fixture, both external completion orders, checked premature-exit non-law, seeded early-exit mutation, CIB public-task evidence, and Temporal restart/replay discriminate one child End from true child quiescence. |
| `BPMN-ERROR-PROPAGATION-01` | Clauses 10.5.1, 10.5 event handling, 13.3.4, and 13.5.3 plus the Error Event Definition rules | CMOF/XSD `SubProcess`, `EndEvent`, `BoundaryEvent`, `ErrorEventDefinition`, and `Error` | In the approved direct-parent exact-code slice, an Error End Event propagates to the matching interrupting boundary Error attached to the enclosing embedded Sub-Process, cancels live runtime state owned by that child scope while preserving monotonic counters, and continues only through the boundary route. | `supported` | Rules `SUBERR-THROW-01`, `SUBERR-PROPAGATE-01`, `SUBERR-INTERRUPT-01`, `SUBERR-NORMAL-01`, and `SUBERR-OBSERVE-01` in the [embedded Sub-Process Error propagation specification](capsules/SUBPROCESS-ERROR-PROPAGATION-SPEC.md) | Checked Error nodes, exact direct-parent lowering, regional cancellation, independent Lean and TypeScript behavior, both child-command orders, global-cancellation non-law, sibling-retention and bypass mutations, retained CIB `2.2.0` lifecycle agreement under `CIB-AGR-0008`, stale host-task mapping under `CIB-OP-0001`, and post-throw Temporal Worker replacement/replay close this bounded slice. Catch-all, unmatched, multi-handler, arbitrary-nesting, Event Sub-Process, payload, and A12 claims remain unsupported. |
| `BPMN-INCLUSIVE-GATEWAY-01` | Clauses 10.6.3 and 13.4.3, Figure 13.5, and Table 13.3 | CMOF `InclusiveGateway` extends `Gateway`; XSD `tInclusiveGateway`; conditional/default `SequenceFlow` | A divergent Inclusive Gateway evaluates all outgoing conditions and activates every true branch, using the default only when none is true; a converging Inclusive Gateway synchronizes only the subset of branches that can still contribute to that occurrence. | `supported` | The implemented [Inclusive Gateway selected-branch specification](capsules/INCLUSIVE-GATEWAY-SPEC.md) owns one structured slice and does not settle general topology. Semantic checkpoint correction audit `ea8b019` and closure correction audit `15ebadc` passed without a material redesign. | Checked source, `selectMany`/`synchronizeSelected`, the hidden selected subset, Lean, the TypeScript core, split-aware host admission, four registered standards-only scenarios, differential mutations, and Temporal Worker-replacement/replay evidence implement the bounded slice. No Inclusive-specific CIB relationship or evidence lane is selected; profile metadata reuses only the existing User Task relationships. The current `choose` and `synchronize` contracts remain unchanged. |
| `BPMN-EVENT-BASED-GATEWAY-01` | Clauses 10.6.6 and 13.4.4, Figure 13.6 | CMOF `EventBasedGateway` extends `Gateway`; CMOF/XSD `instantiate` and `eventGatewayType`; configured catch targets and `SequenceFlow` | For a non-instantiating exclusive Event-Based Gateway, the first configured subsequent Task or Event to complete selects exactly one outgoing path and withdraws every other configured branch. | `supported` | The implemented [Event-Based Gateway Message/Timer specification](capsules/EVENT-BASED-GATEWAY-SPEC.md) owns one exact operation-addressed Message-versus-`PT1S` slice. Semantic-checkpoint correction audit `b7c52ca` and closure correction audit `a62a51a` passed without a material redesign. | Checked source, lowering, Lean, the independent TypeScript core, shared race identity, exact immutable-definition binding, atomic arming and winner transitions, loser withdrawal, stale-loser laws, two registered standards-only schedules, exact differential agreement, and bounded Temporal scheduling/replay are implemented. Coalesced dual readiness fails closed without selecting a portable winner; other trigger families, instantiation, repetition, nesting, and general cancellation remain excluded. |
| `BPMN-CALL-ACTIVITY-01` | Clauses 10.3.6 and 13.3.4, Table 10.23 | CMOF `CallActivity.calledElementRef : CallableElement [0..1]`; XSD `tCallActivity.calledElement` QName | Reaching a Call Activity transfers control to the referenced global Process or Global Task, and the wrapper completes according to the called element's lifecycle while preserving the distinct called instance. | `supported` | The implemented [bounded called-Process specification](capsules/CALL-ACTIVITY-SPEC.md) closes one exact namespace-qualified in-document Process, distinct called semantic identity, empty-data normal return, standards-only differential case, and one-Workflow durability. | Imports, Global Tasks, mappings, deployment/version/tenant resolution, recursion, repetition/concurrency, exceptional completion, cancellation, Child Workflow identity, and CIB compatibility remain unsupported. |
| `BPMN-BOUNDARY-TIMER-01` | Clause 10.5.6, Clause 13.5.3, and Tables 10.91, 10.92, 10.101, and 10.122, with the Activity lifecycle in Clause 13.3.2 | CMOF `BoundaryEvent-cancelActivity` and `TimerEventDefinition.timeDuration`; XSD `tBoundaryEvent.cancelActivity` and `tTimerEventDefinition.timeDuration`, both with `cancelActivity` defaulting to `true` | For the admitted interrupting exact-`PT1S` Timer Boundary Event on one User Task, firing the deadline consumes the Timer occurrence, cancels the attached Activity, and continues only through the boundary Sequence Flow; completing the Activity first withdraws the deadline and continues only through the normal flow. | `supported` | Rules `ABTIMER-ARM-01`, `ABTIMER-COMPLETE-01`, `ABTIMER-INTERRUPT-01`, `ABTIMER-REFUSE-01`, and `ABTIMER-OBSERVE-01` in the [interrupting Activity boundary Timer specification](capsules/ACTIVITY-BOUNDARY-TIMER-SPEC.md#proposed-semantic-rules). An omitted `cancelActivity` is admitted because the machine-readable default resolves it to `true`, not as a tolerated variant; Table 10.92 admits `true` and `false` for a Timer trigger, so excluding non-interrupting behavior is a scope decision about a legal shape. | Exact checked source and lowering, a Lean declarative victory relation with evaluator-soundness bridges and quantified off-deadline and wrong-identity refusal, an independently implemented semantic core with its own focused test, two registered standards-only schedules with seeded mutations, and a durable host deadline that survives Worker absence and refuses only a shared Workflow activation under its own typed identity. Non-interrupting `cancelActivity="false"`, non-User-Task attachment hosts, other boundary triggers, cycle and date timer forms, any duration other than `PT1S`, multiple Boundary Events on one Activity, repetition, general Activity cancellation, completion data, CIB compatibility, and any conformance claim remain unsupported. Quantified stale-identity state preservation and two scenarios excluded for stated adapter reasons are recorded in the specification. |
| `BPMN-SUBPROCESS-BOUNDARY-TIMER-01` | Clause 13.3.4, Clause 13.5.3, Clause 13.3.2's Activity lifecycle, and Tables 10.91, 10.92, 10.101, and 10.122 | CMOF `BoundaryEvent-cancelActivity`, `SubProcess`, and `TimerEventDefinition.timeDuration`; XSD `tBoundaryEvent.attachedToRef`, `tSubProcess.triggeredByEvent`, and `tTimerEventDefinition.timeDuration` | An interrupting Timer Boundary Event attached to an embedded Sub-Process consumes the Timer occurrence, cancels the attached Sub-Process Activity, and continues only through the boundary Sequence Flow; the Sub-Process's own normal completion, which requires no remaining token and no still-active contained Activity, instead withdraws the Timer. What cancelling that Activity does to the work nested inside it is the unresolved part of this requirement, not a settled clause. | `supported` | [The interrupting Sub-Process boundary Timer specification](capsules/SUBPROCESS-BOUNDARY-TIMER-SPEC.md) selects a bounded slice and records the conflict: Clause 13.3.2's nested-termination sentence literally excludes `Ready` and `Active` nested Activities from termination while the same clause's Figure 13.2 gives both states an interruption edge reaching `Terminating`. The specification resolves it as all non-final nested work being cancelled, the owner approved that account on 2026-08-04, and rules `SPTIMER-ARM-01`, `SPTIMER-QUIESCE-01`, `SPTIMER-INTERRUPT-01`, `SPTIMER-REFUSE-01`, and `SPTIMER-OBSERVE-01` own the resulting meaning. | Exact checked source and lowering; a Lean declarative victory relation with an evaluator-soundness bridge per arm, the counter-and-history preservation law, two per-evaluator logical-time laws, a quantified off-deadline refusal, and the checked non-law that interruption does not preserve child-scope-owned state; an independently written TypeScript semantic core with its own focused test; two registered answer-free schedules whose seeded mutations each reach a public observation; durable Temporal arming, both victories, Worker absence across the due instant, replay, and a shared-activation refusal under its own identity. The disposition covers exactly one embedded Sub-Process with one child task and one interrupting `PT1S` boundary Timer: nesting beyond one level, more than one child task, non-interrupting boundaries, other triggers, other durations, multiple boundaries on one Sub-Process, Transaction and Cancel semantics, Event Sub-Processes, child-local data, incidents, CIB compatibility, and any conformance claim remain unsupported. The pre-due firing and the post-victory stale refusal are excluded from the registry for stated structural reasons and are covered in Lean and the focused core test instead. |
| `BPMN-NON-INTERRUPTING-BOUNDARY-TIMER-01` | Clause 10.5.6, Clause 13.5.3, Clause 13.2's Process-completion condition, Clause 13.3.2's Activity lifecycle, and Tables 10.91, 10.92, 10.101, and 10.122 | CMOF `BoundaryEvent-cancelActivity` and `TimerEventDefinition.timeDuration`; XSD `tBoundaryEvent.cancelActivity` and `tTimerEventDefinition.timeDuration` | A Timer Boundary Event with `cancelActivity="false"` consumes its Event occurrence and generates a token on its boundary Sequence Flow while the attached Activity continues to be active, so the boundary branch runs in parallel with the host rather than replacing it, and the enclosing scope completes only once both have finished. | `supported` | The [non-interrupting Activity boundary Timer capsule](capsules/NON-INTERRUPTING-BOUNDARY-TIMER-SPEC.md) is owner-approved and evidence-closed for exactly one User Task host carrying one `PT1S` deadline whose `cancelActivity` names *false*. Repetition, concurrent handler instances, other hosts, other triggers, and other Timer expressions stay outside the row. | Source admission, lowering, the semantic core, Lean, two registered answer-free schedules with seeded mutations, and the Temporal refinement, replay, Worker-absence, and shared-activation lanes, with the exact boundary in the [`implementation-status-owner:ENGINE-SEMANTIC-FAMILY`](ENGINE-SEMANTIC-FAMILY-IMPLEMENTATION-MAP.md#non-interrupting-boundary-timer). |
| `BPMN-STRUCT-DEFINITIONS-METADATA-01` | Clause 8.2.1 and Table 8.1 | CMOF `Definitions-name`, `Definitions-exporter`, and `Definitions-exporterVersion`; XSD `tDefinitions` attributes `name`, `exporter`, and `exporterVersion` | The selected profile admits the standard `Definitions` name and exporter provenance attributes and retains their exact source bytes without projecting them into the checked graph, Semantic Process program, runtime state, or public observation. Structural only: these attributes acquire no execution meaning, and `BPMN-MECH-DEFINITION-01` stays `unsupported`. | `supported` | D1 and D2 of [the preserve-only admission specification](PRESERVE-ONLY-ADMISSION-SPEC.md#d2--a-closed-recursive-classifier-not-a-flat-list), declared exactly by [the preserve-enabled profile README](../profiles/bpmn-2.0.2-user-task-preserved-notation-draft/README.md) while its immutable artifact remains byte-identical to the cyclic-control-flow baseline | The registered source carries `exporter` and `exporterVersion`; the admission suite adds `name` and requires the same identity-normalized execution projection as the source without that added attribute. Each compilation retains its own distinct source digest. |
| `BPMN-STRUCT-DIAGRAM-INTERCHANGE-01` | Clauses 2.3.2, 12.1, and 12.2 | `BPMNDI.cmof` and `BPMNDI.xsd` `BPMNDiagram`, `BPMNPlane`, `BPMNShape`, `BPMNEdge`, `BPMNLabel`, and `BPMNLabelStyle`; `DC.cmof` `Bounds`, `Point`, and `Font`, reached through the `DI.cmof` and `DI.xsd` `Shape`, `Label`, and `Edge` waypoint properties that admit them; the `bpmndi:BPMNDiagram` child `BPMN20.xsd` admits on the document root | The selected profile imports the Diagram Interchange a modeler saved beside a Process and retains it in the exact source bytes, with no diagram element reaching the checked graph, the Semantic Process program, or any public observation. Structural only: no diagram element acquires token, data, or lifecycle meaning, Clause 12's rendering obligations are not claimed, and `BPMN-CONF-EXEC-SEM-01` stays `unsupported`. | `supported` | D1 and D2 of [the preserve-only admission specification](PRESERVE-ONLY-ADMISSION-SPEC.md#d2--a-closed-recursive-classifier-not-a-flat-list), realized as the `retained-diagram-interchange` feature of [the preserve-enabled profile](../profiles/bpmn-2.0.2-user-task-preserved-notation-draft/README.md) | [The registered scenario](../scenarios/user-task-preserved-notation/README.md) carries a diagram with six shapes, three edges, a label, bounds, and waypoints, and [the admission suite](../packages/bpmn-source/test/preserve-only-admission.test.ts) adds a label style with its font. Each admitted source reaches the executed-only twin's execution projection and program, a preserved set seeded with an executable type is rejected, and an unresolvable `bpmnElement`, one resolving to a plane, and a foreign attribute on a shape each refuse. |
| `BPMN-STRUCT-COLLABORATION-01` | Clauses 2.3.2, 9.1, 9.3, 9.3.1, and 9.4 | CMOF `Collaboration`, `Participant`, and `MessageFlow`; XSD `tCollaboration`, `tParticipant`, and `tMessageFlow` | Clause 2.3.2 requires importing a Process diagram *including its definitional Collaboration*. The selected profile admits that Collaboration with its Participants and Message Flows and retains it in the exact source, while execution continues to see only the one entry Process a Participant's `processRef` names. | `supported` | D2 and D4 of [the preserve-only admission specification](PRESERVE-ONLY-ADMISSION-SPEC.md#d4--one-executable-entry-process-for-generic-profiles-without-breaking-existing-ones), realized as the `retained-collaboration-and-participant` feature of [the preserve-enabled profile](../profiles/bpmn-2.0.2-user-task-preserved-notation-draft/README.md) | The registered scenario carries a Collaboration with one Participant bound to the entry Process, and the admission suite adds a second Participant with a Message Flow between them. `BPMN-MECH-COLLABORATION-01` stays `unsupported`: no Participant is executed, no Message Flow delivers, and no correlation exists. A `participantMultiplicity` descendant refuses because it carries execution meaning, and a `processRef` resolving to a User Task refuses. |
| `BPMN-STRUCT-LANE-01` | Clauses 2.3.2, 9.3.2, and 10.8 | CMOF `LaneSet`, `Lane`, and `Lane-flowNodeRefs`; XSD `tLaneSet`, `tLane`, and its `flowNodeRef` element | The selected profile admits a Process's Lane Sets and Lanes, including each `flowNodeRef` resolving to an executed flow node, and retains them in the exact source without lane membership reaching execution. | `supported` | D2 of [the preserve-only admission specification](PRESERVE-ONLY-ADMISSION-SPEC.md#d2--a-closed-recursive-classifier-not-a-flat-list), realized as the `retained-lane-set` feature of [the preserve-enabled profile](../profiles/bpmn-2.0.2-user-task-preserved-notation-draft/README.md) | The registered scenario carries one Lane Set whose Lane names the executed User Task, and the admitted source reaches the executed-only twin's execution projection. Lane-based assignment stays outside every profile and is excluded by name; a `flowNodeRef` resolving to a Process rather than a flow node refuses. |
| `BPMN-STRUCT-ARTIFACT-01` | Clauses 2.3.2 and 8.4.1 | CMOF `Artifact` with `Association`, `Group`, and `TextAnnotation`; XSD `tAssociation`, `tGroup`, and `tTextAnnotation` | Clause 8.4.1 defines Artifacts as additional information not directly related to Sequence Flows or Message Flows. The selected profile admits a Process's Associations, Groups, and Text Annotations and retains them in the exact source, and Clause 13 gives them no execution semantics to claim. | `supported` | D2 of [the preserve-only admission specification](PRESERVE-ONLY-ADMISSION-SPEC.md#d2--a-closed-recursive-classifier-not-a-flat-list), realized as the `retained-artifacts` feature of [the preserve-enabled profile](../profiles/bpmn-2.0.2-user-task-preserved-notation-draft/README.md) | The registered scenario carries a Text Annotation and the Association linking it to the executed User Task, and the admission suite adds a Group. Each admitted source reaches the executed-only twin's execution projection and program, so no Artifact reaches the IL, the runtime state, or the canonical observation. |
| `BPMN-STRUCT-DOCUMENTATION-01` | Clauses 2.3.2 and 8.3.2 | CMOF `Documentation` and `BaseElement-documentation` at `upper="*"`; XSD `tDocumentation` and the `documentation` element every `tBaseElement` admits | BPMN declares `documentation` on `BaseElement`, so a modeler may write it on any element, executed ones included. The selected profile admits it at every such locus and retains it in the exact source without its text reaching the checked graph or the program. | `supported` | D2 of [the preserve-only admission specification](PRESERVE-ONLY-ADMISSION-SPEC.md#d2--a-closed-recursive-classifier-not-a-flat-list), realized as the `retained-documentation` feature of [the preserve-enabled profile](../profiles/bpmn-2.0.2-user-task-preserved-notation-draft/README.md) | The registered scenario documents the Process, and the admission suite documents a User Task, a Start Event, and a Sequence Flow, each of which still reaches the executed-only twin's execution projection. Retention on `Definitions` and `Process` alone was the narrower account the semantic-checkpoint review found, corrected at `ab29717`. |
| `BPMN-ADHOC-01` | Clause 13 non-operational inventory versus the operational Ad-Hoc Sub-Process description | CMOF `AdHocSubProcess`; XSD `tAdHocSubProcess` | The standard's non-operational “Ad-Hoc Process” wording conflicts with its operational description of Ad-Hoc Sub-Processes. | `undecided` | [Recorded source conflict](BPMN-CONFORMANCE-TARGET.md#elements-that-process-execution-conformance-may-ignore) | No current profile admits Ad-Hoc Process or Ad-Hoc Sub-Process execution. |

## Growth rule

Grow the ledger in two passes. First enumerate Process Execution-relevant construct and lifecycle families from Clauses 8, 9, 10, 13, and 15 plus their CMOF/XSD anchors, assign stable requirement IDs, record dependency edges, and keep unreviewed details explicitly open. Then deepen one family into precise propositions when its semantic capsule begins. Link to capsule rule IDs only after the capsule owns them, and link to evidence rather than copying implementation details.

Group work by reusable semantic mechanism rather than XML element count or downstream model: Process lifecycle and graph flow; Activity lifecycle; branching/merging; scopes and interruption; event subscription/consumption; variables/data/mapping; looping/multi-instance; compensation; and import/reference closure. After the M0 engine-runner floor, order uncovered mechanisms primarily by whether CIB Seven `2.2.0` executes them, then by prerequisite closure and Temporal feasibility. This priority does not alter the normative disposition, import CIB product APIs into the mechanism, or imply coverage of Collaboration, Human Resources, forms, Tasklist, or identity facilities that the selected executable baseline does not establish.

An exhaustive percentage-complete claim remains prohibited until the applicable corpus extraction has a reviewed completeness argument. Until then, report exact implemented mechanisms and explicitly unmeasured remainder rather than a guessed percentage.
