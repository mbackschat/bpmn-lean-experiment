# BPMN 2.0.2 requirement-disposition ledger

## Boundary

This ledger owns the disposition of reviewed BPMN 2.0.2 Process Execution requirements. It records what the standard requires and how the project currently classifies that requirement; it does not describe implementation inventory, which belongs only in [IMPLEMENTATION-MAP.md](IMPLEMENTATION-MAP.md).

The ledger is deliberately seeded from the requirements already reviewed for the sequential lifecycle, User Task interaction, import boundary, parallel fork/join, and Intermediate Catch Timer specs. It is not an exhaustive extraction of BPMN 2.0.2 and supplies no denominator for Process Execution Conformance. Counts here measure reviewed scope only.

The initial seed avoids reconstructing closed capsule decisions. The owner has now selected BPMN coverage as the primary engine roadmap, so the two-pass growth rule below expands the construct/mechanism map before deep capsule work without pretending that the line-by-line conformance corpus is already complete. The present ledger makes reviewed scope countable; it still does not make the distance to full Process Execution Conformance measurable.

This is the BPMN engine-coverage ledger. CIB profile relationships belong in the [CIB–BPMN register](CIB-BPMN-RELATION-REGISTER.md), and downstream A12 model/delegate/API adoption belongs in the [A12 Workflows compatibility ledger](research/A12-WORKFLOWS-COMPATIBILITY-LEDGER.md). A requirement may link to both as evidence or priority, but the three denominators are never combined.

## Dispositions

| Disposition | Meaning |
|---|---|
| `supported` | The current reviewed profile assigns bounded executable meaning to the requirement. Evidence can still be draft or incomplete, as linked in the row. |
| `rejected` | Current execution admission deliberately refuses the stated document or construct shape rather than silently assigning behavior. |
| `unsupported` | The requirement is part of the ultimate conformance target but the current reviewed profile has no complete executable disposition for it. |
| `undecided` | A normative conflict, ambiguity, observation boundary, or profile choice remains unresolved. |

These are requirement dispositions, not implementation statuses. A `supported` row does not imply general conformance, and an `unsupported` row does not remove the requirement from the ultimate target.

## Reviewed requirements

| Requirement ID | Normative source | Machine-readable anchor | Requirement or conflict | Disposition | Owning semantic decision | Evidence status |
|---|---|---|---|---|---|---|
| `BPMN-CONF-EXEC-SEM-01` | Clauses 2.3.1 and 13 | BPMN20 CMOF Process package and executable FlowElement subclasses | A Process Execution conforming tool implements the operational execution semantics and Activity lifecycle for the applicable Process metamodel. | `unsupported` | [Conformance target](BPMN-CONFORMANCE-TARGET.md#process-execution-boundary); bounded supported slices are listed separately below. | [Current implementation boundary](IMPLEMENTATION-MAP.md#current-claim) explicitly excludes general Process Execution Conformance. |
| `BPMN-CONF-EXEC-IMPORT-01` | Clause 2.3.2 | `tDefinitions`, `tProcess`, and `tCollaboration`; CMOF `Process` and `Collaboration` | A Process Execution conforming tool imports BPMN Process diagram types including their definitional Collaboration. | `unsupported` | [Import and admission policy](BPMN-CONFORMANCE-TARGET.md#import-and-admission-policy) | [BPMN source boundary](IMPLEMENTATION-MAP.md#implemented-and-absent-surfaces) supports one self-contained private Process, not import closure or definitional Collaboration. |
| `BPMN-CONF-IMPORT-EXPORT-01` | Clause 2 summary table versus Clause 2.3.2 | BPMN document root and Diagram Interchange schemas | The summary says Process Execution exchange is Import/Export, while the detailed conformance clause explicitly requires import only. | `undecided` | [Recorded source conflict](BPMN-CONFORMANCE-TARGET.md#import-and-admission-policy) | No export or round-trip execution claim exists. |
| `BPMN-IMPORT-LOCATION-01` | Clause 15 and the Infrastructure metamodel/XSD | CMOF `Import.location` versus XSD `tImport.location` | The metamodel prose permits an omitted import location while the XSD requires one. | `undecided` | [Recorded source conflict](BPMN-CONFORMANCE-TARGET.md#formalization-consequences) | General imports are absent from the current source boundary. |
| `BPMN-PROCESS-EXECUTABLE-01` | Clause 10 Process attributes and Clause 13 execution preconditions | CMOF `Process.isExecutable`; XSD optional `tProcess.isExecutable` | The sources conflict over the default of omitted `isExecutable`; the current conservative execution boundary admits only a private Process with explicit `isExecutable="true"`. | `rejected` | [Conservative admission policy](BPMN-CONFORMANCE-TARGET.md#import-and-admission-policy) | [Compiler admission](../packages/bpmn-source/src/checked-process-compiler.ts) and its focused tests reject the omitted, false, or public-Process shapes for the current executable slice. |
| `BPMN-PROCESS-SEQUENTIAL-01` | Clauses 13.2 and 13.3 | CMOF `Process`, `StartEvent`, `SequenceFlow`, `UserTask`, and `EndEvent`; corresponding `t*` XSD types | For the admitted private sequential model, Process start reaches one User Task wait and exact task completion reaches Process completion through the None End Event. | `supported` | `CIB-AGR-0001` in the [CIB–BPMN register](CIB-BPMN-RELATION-REGISTER.md#cib-agr-0001--sequential-process-and-user-task-lifecycle) and the [User Task capsule](capsules/USER-TASK-INTERACTION-SPEC.md) | [Evidence-closed draft boundary](IMPLEMENTATION-MAP.md#current-evidence) across Lean, CIB, TypeScript, and Temporal. |
| `BPMN-UTASK-LIFECYCLE-01` | Clauses 10.7.3, 13.3.2, and 13.3.3 | CMOF `UserTask`; XSD `tUserTask` | The admitted User Task becomes active, is discoverable through the selected task-manager boundary, and completing the exact active occurrence permits outgoing control to continue. | `supported` | Rules `UTASK-ACTIVATE-01`, `UTASK-DISCOVER-01`, and `UTASK-COMPLETE-01` in the [User Task capsule](capsules/USER-TASK-INTERACTION-SPEC.md#stable-semantic-rules-and-evidence) | The capsule matrix keeps the finite Lean, CIB, TypeScript, and Temporal evidence lanes separate. |
| `BPMN-UTASK-REFUSAL-01` | Clauses 10.7.3, 13.3.2, and 13.3.3 plus project operational mapping `CIB-OP-0001` | CMOF `UserTask`; host task identity has no BPMN machine-readable equivalent | A command for a non-active semantic occurrence does not complete the active User Task or reactivate a completed occurrence. | `supported` | Rules `UTASK-REFUSE-01` and `UTASK-REFUSE-02` in the [User Task capsule](capsules/USER-TASK-INTERACTION-SPEC.md#stable-semantic-rules-and-evidence) | Wrong activation remains adapter-decided for the ordinal clause; stale host-task refusal has the adjacent [engine consistency probe](../runners/cibseven/src/test/java/org/bpmnlean/cibseven/CibSevenConsistencyProbeTest.java). |
| `BPMN-PARALLEL-GATEWAY-01` | Clauses 10.6.4 and 13.4.1, Figure 13.3, and Table 13.1 | CMOF `ParallelGateway` extends `Gateway`; XSD `tParallelGateway`; incoming/outgoing `SequenceFlow` references | A Parallel Gateway waits for at least one token on every incoming Sequence Flow, consumes exactly one from each incoming flow, retains excess tokens, and produces exactly one token on every outgoing flow. | `supported` | The [parallel fork/join spec](capsules/PARALLEL-FORK-JOIN-SPEC.md) owns `PAR-FORK-01`, `PAR-JOIN-READY-01`, and `PAR-JOIN-CONSUME-01` under normative per-incoming-flow semantics. | The bounded capsule is evidence-closed as a draft across checked source/lowering, Lean, TypeScript, balanced and live-sibling pinned-CIB evidence, semantic-lifetime Temporal refinement/replay, and the current ten-case differential. The [count/provenance discriminator](experiments/SEMANTIC-REPRESENTATION-EXPERIMENT.md#experiment-3-join-arrival-count-versus-edge-provenance) and bounded CIB probe separately establish candidate deviation `CIB-DEV-0001`; immutable negative evidence remains open. |
| `BPMN-TIMER-DURATION-01` | Clause 10.5.5 and Tables 10.101 and 10.122 | CMOF `TimerEventDefinition.timeDuration`; XSD `tTimerEventDefinition.timeDuration` | The admitted Intermediate Catch Timer Event contains exactly one relative-duration expression whose exact lexical value is `PT1S`; the bounded profile normalizes only that reviewed literal to 1000 logical milliseconds. | `supported` | The [Intermediate Catch Timer spec](capsules/INTERMEDIATE-CATCH-TIMER-SPEC.md#source-profile) owns the lexical subset and independent lowering check. | Checked source retains `PT1S`; Lean and TypeScript lower it independently to `1000`; all other duration values and timer-expression forms are rejected by this profile rather than assigned semantics. |
| `BPMN-TIMER-CATCH-01` | Clause 10.5.4 and Table 10.89 | CMOF `IntermediateCatchEvent` and `TimerEventDefinition`; XSD `tIntermediateCatchEvent` and `tTimerEventDefinition` | A normal-flow catching Intermediate Timer Event retains the token until its trigger occurs and then permits outgoing Sequence Flow progress. | `supported` | Rules `TIMER-WAIT-01`, `TIMER-FIRE-01`, `TIMER-REFUSE-01`, and `TIMER-OBSERVE-01` in the [Intermediate Catch Timer spec](capsules/INTERMEDIATE-CATCH-TIMER-SPEC.md#stable-semantic-rules) | The bounded exact-deadline capsule is evidence-closed as a draft across Lean, TypeScript, controlled-clock CIB evidence, durable Temporal timer/restart/replay evidence, and the current ten-case differential. The logical-millisecond deadline projection is project-owned and CIB adapter-derived, not a normative BPMN representation. |
| `BPMN-SERVICE-TASK-01` | Clause 10.3.3.1, Table 10.8, and Clause 13.3.3 | CMOF `ServiceTask` and its `implementation`/`operationRef` properties; XSD `tServiceTask` | Under the admitted bounded success subsets, a Service Task invokes its selected service on activation and completes after successful service completion. | `supported` | Rules `EFFECT-WAIT-01`, `EFFECT-INTENT-01`, `EFFECT-RESULT-01`, `EFFECT-REFUSE-01`, and `EFFECT-OBSERVE-01` in the [Service Task effect spec](capsules/SERVICE-TASK-EFFECT-SPEC.md), plus the bounded data/mapping rules in the [CreateDocument spec](capsules/CREATE-DOCUMENT-DATA-SPEC.md) | The bounded capsules are evidence-closed as drafts across checked source/lowering, Lean, TypeScript, pinned-CIB host realization, Temporal Activity refinement/replay, and the current ten-case differential. CIB host waits and synchronous mapping transactions are not independent derivations of the project effect-in-flight state. |
| `BPMN-SERVICE-TASK-FAULT-01` | Clause 10.3.3.1 and Clause 13.3.3 | CMOF `ServiceTask`, `Operation`, and `Error`; XSD `tServiceTask`, `tOperation`, and `tError` | Under the admitted typed-business-error slice, a matching service fault interrupts the active Service Task instead of completing its normal output. | `supported` | Rules `BERROR-CATCH-01`, `BERROR-INTERRUPT-01`, `BERROR-MESSAGE-01`, and `BERROR-REFUSE-01` in the [boundary-error spec](capsules/BOUNDARY-ERROR-SPEC.md) | Lean and TypeScript implement the typed result and exact-code interruption; Temporal carries it as a successful Activity result and keeps exhausted Activity attempts as adapter failure. General service faults, incidents, retry exhaustion as semantics, propagation, and unmatched-Error semantics remain unsupported. |
| `BPMN-BOUNDARY-ERROR-01` | Clause 13.5.3 and Tables 10.86, 10.91, and 10.92 | CMOF `BoundaryEvent`, `ErrorEventDefinition`, and `Error`; XSD `tBoundaryEvent`, `tErrorEventDefinition`, and `tError` | For the admitted same-Process exact-code case, an attached interrupting Error Boundary Event catches the matching Error, abandons the normal Activity route, and continues only through its boundary Sequence Flow. | `supported` | Rules `BERROR-CATCH-01`, `BERROR-INTERRUPT-01`, `BERROR-REFUSE-01`, and `BERROR-OBSERVE-01` in the [boundary-error spec](capsules/BOUNDARY-ERROR-SPEC.md); profile-scoped caught-path mapping is separately owned by `BERROR-CIBMAP-01`. | Checked source/lowering, Lean, TypeScript, synchronous CIB host realization, Temporal refinement/replay, and the ten-case differential close this bounded route. Catch-all handlers, multiple handlers, nested propagation, Error End Events, Error throws, and general cancellation remain unsupported. |
| `BPMN-ADHOC-01` | Clause 13 non-operational inventory versus the operational Ad-Hoc Sub-Process description | CMOF `AdHocSubProcess`; XSD `tAdHocSubProcess` | The standard's non-operational “Ad-Hoc Process” wording conflicts with its operational description of Ad-Hoc Sub-Processes. | `undecided` | [Recorded source conflict](BPMN-CONFORMANCE-TARGET.md#elements-that-process-execution-conformance-may-ignore) | No current profile admits Ad-Hoc Process or Ad-Hoc Sub-Process execution. |

## Growth rule

Grow the ledger in two passes. First enumerate Process Execution-relevant construct and lifecycle families from Clauses 8, 9, 10, 13, and 15 plus their CMOF/XSD anchors, assign stable requirement IDs, record dependency edges, and keep unreviewed details explicitly open. Then deepen one family into precise propositions when its semantic capsule begins. Link to capsule rule IDs only after the capsule owns them, and link to evidence rather than copying implementation details.

Group work by reusable semantic mechanism rather than XML element count or downstream model: Process lifecycle and graph flow; Activity lifecycle; branching/merging; scopes and interruption; event subscription/consumption; variables/data/mapping; looping/multi-instance; compensation; and import/reference closure. A downstream corpus may prioritize a family, but it does not alter the normative disposition.

An exhaustive percentage-complete claim remains prohibited until the applicable corpus extraction has a reviewed completeness argument. Until then, report exact implemented mechanisms and explicitly unmeasured remainder rather than a guessed percentage.
