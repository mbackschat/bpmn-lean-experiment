# Plan

This file owns immediate execution order, blockers, current measured evidence, and the exact resume action. Durable decisions, implementation detail, semantic meaning, and test procedure belong in their linked owners. Completed work leaves this file after its current consequence has an owner; Git retains history.

## Current checkpoint

M0 through M6 and Horizons 1 and 2 are closed. Horizon 2 Product 1 Workflow-chain production enrollment, handle-free start, content-bound recovery, closed v1 terminal receipts, private paired E1/E2 traversal, every approved capacity row, stop-the-world Worker deployment admission, and forced Intermediate Catch Message, Intermediate Catch Timer, and Service Task effect rollover are implemented under the [production lifecycle specification](TEMPORAL-PROCESS-LIFECYCLE-SPEC.md#workflow-chain-production-contract) and [`implementation-status-owner:TEMPORAL-HOSTING`](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md). The complete gate passed at `c2f9a5d`; the same isolated reviewer approved the opaque Schedule-locator and cost-attribution correction at `5b9e028`. Those corrections changed hosting/API representation and persistence only, leaving the registered BPMN/CIB accounts, semantic-core traces, replay evidence, and published locator meaning unchanged.

The non-binding [engine maturity ladder](PROJECT-DESIGN.md#engine-maturity-roadmap-labels) retrospectively names this Product 1 floor Engine `v0.1`, **Runnable MVP**, and uses Engine `v0.2`, **Minimum Useful Engine (MUE)**, as the current direction. Annotated milestone tag `engine/v0.1` points at closed Horizon 2 documentation commit `72b11cf`; it is an evidence snapshot, not a package version or release. The eight named MUE content IDs remain the complete candidate programme and are not a frozen release or version assignment; Sequential and Parallel Multi-Instance are the first two to close, and the other six remain ordered below. `MUE-PREVIEW-ALPHA` and `MUE-PREVIEW-BETA` are product-delivery checkpoints, not MUE content, closure evidence, or support claims. `CONFORMANCE-CLOSURE` is later content. Engine `v0.9`, **Conformance Candidate**, and reserved Engine `v1.0`, **Conformant Engine**, remain conditional future planning labels rather than present claims.

The exact [Sequential Multi-Instance User Task specification](capsules/SEQUENTIAL-MULTI-INSTANCE-SPEC.md) is implemented, independently closure-reviewed, evidence-closed, and execution-registered across source, Lean, the TypeScript core, E1/E2 publication, differential evidence, Product 1, and the retained corpus. Its natural and interrupted production witnesses cover pre-arming rollover, one lifetime Timer, Worker replacement, recovery, stale-task refusal, exact output behavior, production-bound comparison, and replay. The closure corrections passed the complete clean committed gate at `d814cac4`; the closure-reviewed [MUE Preview Alpha specification](MUE-PREVIEW-ALPHA-SPEC.md) consumes this published contract without adding or simulating BPMN semantics.

The MUE acceleration procedure is classified non-material under [the negative case](TESTING-SPEC.md#independent-cold-review-gate): the eight named MUE content IDs, their `Maps`, and every semantic and evidence obligation are unchanged. The increment adds only lane-aware explicit staging, write-free concurrent-gate measurement, and an owner-authorized bound on unpushed accumulation. Existing delegation, review, Lean, and verification owners remain authoritative; [the documentation control plane](../scripts/document-control-plane.test.ts), [independent-review policy](../scripts/independent-review-policy.test.ts), [verification-entrypoint guard](../scripts/verification-entrypoint.test.ts), and [process-ledger escalation guard](../scripts/document-reviewability.test.ts) cover adoption.

The owner selected the [Alpha, Beta, Release Candidate, and MUE delivery ladder](PROJECT-DESIGN.md#mue-delivery-checkpoints) under the existing [breadth and risk ordering](PROJECT-DESIGN.md#cib-seven-220-breadth-ordering) and [showcase acceptance model](SHOWCASE-MILESTONE-LADDER-DECISION.md#showcase-milestone-ladder). Alpha is implemented and closure-reviewed: the exact natural and interrupted Sequential Multi-Instance examples run through the production browser and Temporal host, publish visible committed progress and interruption, and replay every actual Run without simulated semantics. The pushed annotated tag `phase/mue-preview-alpha` points at completely verified closure head `51e59a6d167d2d8dd15fbf7d19355080f5dd493c`. Beta is active and takes one separately reviewed risk-bearing vertical checkpoint through every remaining MUE content ID before integrating that breadth into one coherent preview. Named mechanism boundaries retain separate capsules and reviews. After Beta the same MUE IDs return to the queue for full closure; the Release Candidate begins only after that scope is implemented and feature-frozen, and final MUE remains the separate release-qualified `engine/v0.2` boundary.

For the current MUE programme, the owner authorizes pushes at the triggers in [the three-level verification policy](TESTING-SPEC.md#three-level-verification-policy) after every selected pre-push entry point passes. This authorizes neither pushing an unverified target nor bypassing a required review or gate.

The [runtime-state identity bound](RUNTIME-STATE-INVARIANT-SPEC.md#layer-1-lifecycle-and-structure) and Activity-family issuing discipline supply the published outer-identity premises. User Task, Timer, and Activity identities are bounded; Message, Effect, Event race, Call, ordinary Scope, general preservation, and issuing disciplines outside Activity remain open without weakening this slice.

The owner-approved [internal commutation account](INTERNAL-COMMUTATION-PROPOSAL.md) has passed its first green final-implementation semantic checkpoint. The Semantic Process Program requires the closed `InternalSchedulingMode`, and every existing source lowerer emits `rejectObservableChoice`. The TypeScript core retains the proved exact-pair rule and atomically executes a complete finite frontier of ordinary User Task, Message, Timer, and effect arming operations when every unordered pre-state footprint pair is independent; it refuses a batch that exceeds remaining fuel before mutation and rolls a disappearing member back to the pre-batch state. Lean defines the same complete-frontier classifier and a three-User-Task discriminator whose six permutations reach one raw state. Both semantic accounts now classify every operation family and RuntimeState field under the proposal's exact cross-language mapping; the proposal-derived guard rejects independent additions and classification drift. The census enables no transition and defines no footprint. The context-cold reviewer approved correction target `fb998985` with no remaining findings, and the clean complete gate passed at `9bbc99e5`. Scheduled-choice consumption, region footprints, remaining operation-family preparations, the arbitrary-batch theorem, and Temporal scheduled-mode admission remain unimplemented. This checkpoint changes Program representation but adds no BPMN source shape, admitted profile, public command, or host capability.

The [Parallel Multi-Instance specification](capsules/PARALLEL-MULTI-INSTANCE-SPEC.md) and supporting [`AOO-CLAIM-01`](ACTIVITY-OCCURRENCE-OWNERSHIP-SPEC.md#stable-semantic-rules) invariant are implemented, independently closure-reviewed, and evidence-closed. Source, Lean, the independently written TypeScript core, public progress, E1/E2 publication, three registered differential schedules, retained corpus coverage, strict Product 2 copied-contract compatibility, and production Temporal refinement are green. Lean proves unconditional entry, progress, final, early, and Timer preservation plus all-policy commutation and the first-policy trace non-law. The production witness covers zero/all/first and both task/Timer schedules, pre-arming rollover, Worker replacement, content-bound recovery, terminal receipts, paired publication, exact-16 capacity, and replay. Reproducible cost is bound to `25dd98cd..17e23590`; the cold closure review targeted `a45ee384`, and the same reviewer closed its two documentation-only audit rounds at `10a013ab` and `0b060ab8` without semantic redesign.

The time-bounded `MUE-LIVE-DEMO-DELIVERY` product-delivery increment is closed at `bedd7276`. It changed no MUE content, BPMN meaning, support claim, engine contract, platform authorization rule, or retained example. The source-built delivery binds all four project images to one clean committed source, reaches one healthy exact public origin without a rebuild or pull, and passes its audience-paced expense-exception and Alpha rehearsals. The published Docker-only bundle adds immutable multi-platform GHCR images, exact digest pins, source-provenance labels, SBOMs, fallback captures, and a one-command presenter launcher. Its hosted workflow logged out before successfully pulling and starting the complete stack from those exact public digests, so a demo machine needs Docker but no Git, Node, pnpm, repository checkout, or image build.

## Ordered work

Exactly one stable work ID is active. Required maps are part of the routing contract, not descriptive tags.

After the time-bounded live-demo delivery, the remaining MUE content IDs resume at `INTERNAL-COMMUTATION`. Each Action below remains that ID's final closure obligation; reaching a Beta checkpoint neither closes the ID nor permits a broader claim.

1. `INTERNAL-COMMUTATION` · **active** · Owner: [internal commutation proposal](INTERNAL-COMMUTATION-PROPOSAL.md), [Semantic Process internal scheduling](SEMANTIC-PROCESS-IL-SPEC.md#internal-scheduling), [Lean proof obligations](SEMANTIC-PROCESS-IL-SPEC.md#lean-specification-and-proof-obligations) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md), [assurance/adoption](ASSURANCE-AND-ADOPTION-IMPLEMENTATION-MAP.md) · Action: Generalize the proved exact-pair checkpoint into complete operation-family classification, arbitrary finite independent batches over disjoint semantic and region footprints, canonical state and publication order, and exact semantic input for genuinely observable choice.
2. `MECHANISM-MATURITY-EVIDENCE` · **queued** · Owner: [requirement-ledger mechanism-family map](BPMN-REQUIREMENT-LEDGER.md#process-execution-mechanism-family-map), [testing specification](TESTING-SPEC.md) · Maps: [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [assurance/adoption](ASSURANCE-AND-ADOPTION-IMPLEMENTATION-MAP.md) · Action: Generate a non-conformance mechanism-maturity vector for invariant coverage, shared Activity occurrence adoption, multiple-enabled closure classification, and profile-local versus reusable composition evidence; do not infer proof quality from theorem syntax or combine BPMN, CIB, and platform denominators.
3. `DATA-AND-TASK-MECHANISMS` · **queued** · Owner: [requirement ledger](BPMN-REQUIREMENT-LEDGER.md), [minimal engine research](research/MINIMAL-USEFUL-BPMN-ENGINE-RESEARCH.md) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md) · Action: Broaden standard data lifetime and mappings before selecting executable Task profiles and host effects.
4. `EVENT-SUBSCRIPTIONS` · **queued** · Owner: [requirement ledger](BPMN-REQUIREMENT-LEDGER.md) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md) · Action: Generalize subscription lifetime, correlation, payload, cancellation, deterministic races, and scope handlers.
5. `COMPENSATION-TRANSACTIONS` · **queued** · Owner: [requirement ledger](BPMN-REQUIREMENT-LEDGER.md) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md) · Action: Specify completed-work registration, snapshots, handler ordering, cancellation, failure, and replay.
6. `H3-WORKLOAD-ISOLATION` · **queued** · Owner: [scalability roadmap](TEMPORAL-BPMN-EXECUTION-SCALABILITY-PROPOSAL.md) · Maps: [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md), [platform](BPM-PLATFORM-IMPLEMENTATION-MAP.md) · Action: Prove queue and Worker isolation, backpressure, fairness, failover, observability, capacity, and cost.
7. `MUE-PREVIEW-BETA` · **queued** · Owner: [engine maturity ladder](PROJECT-DESIGN.md#engine-maturity-roadmap-labels), [requirement ledger](BPMN-REQUIREMENT-LEDGER.md), [showcase ladder](SHOWCASE-MILESTONE-LADDER-DECISION.md#showcase-milestone-ladder) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md), [platform](BPM-PLATFORM-IMPLEMENTATION-MAP.md), [assurance/adoption](ASSURANCE-AND-ADOPTION-IMPLEMENTATION-MAP.md) · Action: Integrate the separately reviewed breadth checkpoints into one coherent example catalog and Product 2 journey, disclose every open limit, then resequence all MUE content IDs for full closure.
8. `CONFORMANCE-CLOSURE` · **later** · Owner: [requirement ledger](BPMN-REQUIREMENT-LEDGER.md), [conformance target](BPMN-CONFORMANCE-TARGET.md) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [assurance/adoption](ASSURANCE-AND-ADOPTION-IMPLEMENTATION-MAP.md) · Action: Continue Process Execution closure by normative dependency, semantic risk, practical reach, and the adopted [showcase ladder](SHOWCASE-MILESTONE-LADDER-DECISION.md#showcase-milestone-ladder).

## Current evidence

- Internal Commutation final-implementation semantic checkpoint. Command: `./scripts/pnpm.sh run test:pre-push:verify`. Status: `exit 0`. Date: `2026-08-28`. Commit: `9bbc99e5f830f4dea5157ed944f4d54b461ec6cc`. The context-cold review targeted `f6da5db574c7d2733dd06c7a13e4122cc1415fe1`, closed both required findings at `fb998985daa5ecc94af313b78bc6485a352dd213`, and approved the checkpoint with no semantic redesign. The clean gate output SHA-256 is `3c7adb53c7cbe0423dfa37ab64df8bedf72ffcfa911071ac1c95219de3a24b25`; it recorded `dirty:false`, 57 retained pipeline cases, 68 replay histories, and zero failures. Its `33.08` second warm pipeline timing exceeded the soft target under `loadPerCore 3.23`, so it is correctness evidence rather than a comparable performance measurement.
- Parallel Multi-Instance closure. Command: `./scripts/pnpm.sh run test:pre-push:verify`, `./scripts/pnpm.sh run test:pre-push:showcase`, and `./scripts/pnpm.sh run test:pre-push:ui`. Status: `exit 0`. Date: `2026-08-28`. Commit: `0b060ab8f1d01d5a35f2bc8af569d4d859574adb`. The Product 1 pipeline retained all 57 cases and 68 replay histories with zero failures and `dirty:false`; the path-selected showcase compatibility lane passed, and the headless Chromium UI lane passed 83/83. The cold paired closure review of `a45ee3842c7457980abdbdca078f71249370597d` returned documentation-only required edits; the same reviewer closed every finding across correction targets `10a013ab38fbe01962b2fc13eb184b9f03d62f38` and `0b060ab8f1d01d5a35f2bc8af569d4d859574adb` without semantic redesign.

## Exact resume point

Active work ID: `INTERNAL-COMMUTATION`.

Next action: implement the [proposal's occurrence-region ownership and destructive/completing transition footprints](INTERNAL-COMMUTATION-PROPOSAL.md#prepared-transition-and-region-footprints) as the next Red/green slice, retaining disjoint-sibling composition and ancestor/descendant, called-root, concurrent-insertion, and parent/caller-output conflicts as separating oracles. Keep scheduled-choice consumption, Temporal scheduled-mode admission, and downstream work stopped until the pure semantic prerequisites are green.

Separate open changes outside the active item: Activity issuing-discipline closure and graduation, cross-family Activity wait withdrawal, PostgreSQL migration evidence.

Oracle: the existing two-operation checkpoint remains byte-identical while the final account classifies every enabled operation by a complete semantic footprint, proves canonical state and publication equivalence for independent sets, and represents any remaining observable choice explicitly rather than selecting by program order.

Stop if an operation family cannot expose its complete reads, writes, and publication keys without successor-state inference; if pairwise independence is insufficient for a larger enabled set; if canonical ordering changes public meaning; or if an observable choice lacks a reviewed semantic representation and Temporal preservation account.
