# Plan

This file owns immediate execution order, blockers, current measured evidence, and the exact resume action. Durable decisions, implementation detail, semantic meaning, and test procedure belong in their linked owners. Completed work leaves this file after its current consequence has an owner; Git retains history.

## Current checkpoint

M0 through M6 and Horizons 1 and 2 are closed. Horizon 2 Product 1 Workflow-chain production enrollment, handle-free start, content-bound recovery, closed v1 terminal receipts, private paired E1/E2 traversal, every approved capacity row, stop-the-world Worker deployment admission, and forced Intermediate Catch Message, Intermediate Catch Timer, and Service Task effect rollover are implemented under the [production lifecycle specification](TEMPORAL-PROCESS-LIFECYCLE-SPEC.md#workflow-chain-production-contract) and [`implementation-status-owner:TEMPORAL-HOSTING`](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md). The complete gate passed at `c2f9a5d`; the same isolated reviewer approved the opaque Schedule-locator and cost-attribution correction at `5b9e028`. Those corrections changed hosting/API representation and persistence only, leaving the registered BPMN/CIB accounts, semantic-core traces, replay evidence, and published locator meaning unchanged.

The non-binding [engine maturity ladder](PROJECT-DESIGN.md#engine-maturity-roadmap-labels) retrospectively names this Product 1 floor Engine `v0.1`, **Runnable MVP**, and uses Engine `v0.2`, **Minimum Useful Engine (MUE)**, as the current direction. Annotated milestone tag `engine/v0.1` points at closed Horizon 2 documentation commit `72b11cf`; it is an evidence snapshot, not a package version or release. Items 1 through 9 below are the remaining `v0.2 MUE` candidate content, not a frozen release or version assignment. Engine `v0.3`, **Scalable Engine**, currently points at Horizon 3; Engine `v0.9`, **Conformance Candidate**, and reserved Engine `v1.0`, **Conformant Engine**, remain conditional future planning labels rather than present claims.

The first sequential Multi-Instance checkpoint now compiles one exact schema-valid batch-review source into a closed checked node and `awaitSequentialMultiInstanceUserTask` operation with deterministic data, route, Timer, and limit facts. The reserved profile is intentionally not execution-registered, so no runtime, public observation, Lean, Temporal, or conformance claim follows from this checkpoint.

Commit `9ba76d8` had added `awaitSequentialMultiInstanceUserTask` to the operation union without its cross-package exhaustiveness obligations, so the harness type check and the publication coverage guard were both red at clean `da3e87a`. Commit `3032828` restores them: the source test narrows its union access, both publication-validation predicates and host classification answer the reserved kind, a directly reachable `assertNever` throw becomes a typed host refusal, and the coverage guard's union-wide denominator becomes a partition requiring each kind to be either enumerated in the publication schema or refused by the completeness producer. Both partition directions were seeded and detected. The increment is classified non-material under [the negative case](TESTING-SPEC.md#independent-cold-review-gate) and opens no review cycle: no BPMN meaning, profile, CIB relationship, checked-source or IL representation, runtime or public observation, transition family, or proof boundary changes, and no reachable production outcome changes because profile admission already refuses the reserved profile before host capability is assessed. The guards that cover it are [the publication coverage guard](../scripts/execution-publication-contract-coverage.test.ts) and [the host refusal test](../packages/temporal-adapter/testkit/test/host-admission-unhostable.test.ts), and its measured gate results belong to `3032828` rather than to this section.

## Ordered work

Exactly one stable work ID is active. Required maps are part of the routing contract, not descriptive tags.

1. `ACTIVITY-OCCURRENCE-OWNERSHIP` · **active** · Owner: [Semantic Process runtime state](SEMANTIC-PROCESS-IL-SPEC.md#runtime-state), [sequential Multi-Instance proposal](capsules/SEQUENTIAL-MULTI-INSTANCE-PROPOSAL.md) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md) · Action: Add one closed Activity occurrence identity and ownership mechanism for current task/effect execution, attached handlers, and loop controllers; migrate the three existing boundary-Timer families without broadening their profiles or introducing a universal flag bag.
2. `SEQUENTIAL-MULTI-INSTANCE` · **queued** · Owner: [sequential Multi-Instance proposal](capsules/SEQUENTIAL-MULTI-INSTANCE-PROPOSAL.md), [requirement ledger](BPMN-REQUIREMENT-LEDGER.md) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md) · Action: Resume the proved Lean account and independent TypeScript runtime/public-observation checkpoint over the closed source/IL representation and shared Activity occurrence mechanism.
3. `INTERNAL-COMMUTATION` · **queued** · Owner: [Semantic Process internal scheduling](SEMANTIC-PROCESS-IL-SPEC.md#internal-scheduling), [Lean proof obligations](SEMANTIC-PROCESS-IL-SPEC.md#lean-specification-and-proof-obligations) · Maps: [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [assurance/adoption](ASSURANCE-AND-ADOPTION-IMPLEMENTATION-MAP.md) · Action: Replace the exact two-User-Task closure exception with a proved sufficient commutation rule over disjoint semantic footprints, canonical state and publication order, and explicit semantic choice for genuinely observable nondeterminism.
4. `PARALLEL-MULTI-INSTANCE` · **queued** · Owner: [requirement ledger](BPMN-REQUIREMENT-LEDGER.md) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md) · Action: Add bounded concurrent ownership, completion conditions, deterministic aggregation, races, cancellation, replacement, and replay over the shared occurrence and commutation mechanisms.
5. `MECHANISM-MATURITY-EVIDENCE` · **queued** · Owner: [requirement-ledger mechanism-family map](BPMN-REQUIREMENT-LEDGER.md#process-execution-mechanism-family-map), [testing specification](TESTING-SPEC.md) · Maps: [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [assurance/adoption](ASSURANCE-AND-ADOPTION-IMPLEMENTATION-MAP.md) · Action: Generate a non-conformance mechanism-maturity vector for invariant coverage, shared Activity occurrence adoption, multiple-enabled closure classification, and profile-local versus reusable composition evidence; do not infer proof quality from theorem syntax or combine BPMN, CIB, and platform denominators.
6. `DATA-AND-TASK-MECHANISMS` · **queued** · Owner: [requirement ledger](BPMN-REQUIREMENT-LEDGER.md), [minimal engine research](research/MINIMAL-USEFUL-BPMN-ENGINE-RESEARCH.md) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md) · Action: Broaden standard data lifetime and mappings before selecting executable Task profiles and host effects.
7. `EVENT-SUBSCRIPTIONS` · **queued** · Owner: [requirement ledger](BPMN-REQUIREMENT-LEDGER.md) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md) · Action: Generalize subscription lifetime, correlation, payload, cancellation, deterministic races, and scope handlers.
8. `COMPENSATION-TRANSACTIONS` · **queued** · Owner: [requirement ledger](BPMN-REQUIREMENT-LEDGER.md) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md) · Action: Specify completed-work registration, snapshots, handler ordering, cancellation, failure, and replay.
9. `H3-WORKLOAD-ISOLATION` · **queued** · Owner: [scalability roadmap](TEMPORAL-BPMN-EXECUTION-SCALABILITY-PROPOSAL.md) · Maps: [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md), [platform](BPM-PLATFORM-IMPLEMENTATION-MAP.md) · Action: Prove queue and Worker isolation, backpressure, fairness, failover, observability, capacity, and cost.
10. `CONFORMANCE-CLOSURE` · **later** · Owner: [requirement ledger](BPMN-REQUIREMENT-LEDGER.md), [conformance target](BPMN-CONFORMANCE-TARGET.md) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [assurance/adoption](ASSURANCE-AND-ADOPTION-IMPLEMENTATION-MAP.md) · Action: Continue Process Execution closure by normative dependency, semantic risk, practical reach, and the adopted [showcase ladder](SHOWCASE-MILESTONE-LADDER-DECISION.md#showcase-milestone-ladder).

## Current evidence

- Complete Horizon 2 closure gate. Command: `env CI=true ./scripts/verify.sh`. Status: `exit 0`. Date: `2026-08-18`. Commit: `c2f9a5dc5369b73ff61b363c53bcac74ab2b05fe`. It covered Lean, the semantic core, BPMN source, shared contracts and canonical bytes, CIB targets, differential comparison, Temporal adapter tests and history replay, Product 2 package evidence, the complete Workflow-chain contract, and the registered 51-case pipeline; this establishes correctness, not production scalability. Two performance baselines remain because catalog changes break comparability: an uncontended, owner-confirmed idle-host 30-case run measured 15,986.670 ms at `13cdec8`, and the uncontended fastest 28-case run measured 13,476 ms at `ac2813c`. No uncontended measurement exists at 32, 34, or 35 cases, and none exists for the current 52-case catalog, so compare a new figure with the per-case trend rather than either baseline and treat a contended run only as correctness evidence. Two independent 34-case runs on 2026-08-07 measured 20,406.710 ms and 20,036 ms, agreeing within 2 percent; this is weak evidence that the catalog genuinely costs about twenty seconds rather than that both runs were contended, so treat roughly 20 seconds as unexplained rather than a regression until an idle-host run settles it. The H2 target's complete gate passed in 190.000 seconds and its registered pipeline passed in 26,426.960 ms under external host contention, so the new run is correctness-only.
- Interchange complete closure gate. Command: `env CI=true JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home BPMN_JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home ./scripts/verify.sh`. Status: `exit 0`. Date: `2026-08-19`. Commit: `42e62e5e87ee14529fb81c850a5e557019a1aa91`. Elapsed: `214300` ms. Output SHA-256: `f60882cd7e1ff6a86b473e753e591db0417e17bc7a8fe42a08bf2a1fa5a1ec7e`. The final run proves all prior failure classes corrected: Lean and source admission complete, the main Temporal suite passes 229/229, the capacity suite passes 7/7, and the registered 52-case release pipeline passes 5/5 with 63 live histories. This is correctness evidence under the explicit Java 21 toolchain, not a performance baseline. The same checkpoint reviewer approved immutable closure target `c763d1a` with no required or advisory findings; the maintained contract is the [interchange admission specification](INTERCHANGE-ADMISSION-SPEC.md).
## Exact resume point

Active work ID: `ACTIVITY-OCCURRENCE-OWNERSHIP`.

Next action: propose one closed Activity occurrence identity and ownership mechanism covering current task and effect execution, attached handlers, and loop controllers, then migrate the three existing boundary-Timer families onto it without broadening their profiles. It builds on [the runtime-state invariant](RUNTIME-STATE-INVARIANT-SPEC.md#the-deliberately-open-lane), whose quantified Lean preservation is deliberately open; the two boundary-Timer deferrals waiting on `waitIdentitiesUnique` are standing unmet demand this item does not discharge.

Oracle: one occurrence identity admits every current consumer, each migrated boundary-Timer family keeps its admitted models, accepted transitions, and canonical projections unchanged, and a state reusing a retired occurrence identity is refused rather than silently rebound.

Stop if the mechanism needs a per-family flag bag or a profile-specific exception, if migration changes an existing BPMN result rather than its representation, if the identity cannot be stated without reading host or Temporal facts, or if a family's ownership can only be expressed by widening `RuntimeState` with a field no conjunct constrains.
