# Plan

This document owns the current checkpoint, ordered next work, unresolved decisions, and exact resume point. Durable architecture belongs in [PROJECT-DESIGN.md](PROJECT-DESIGN.md); exact supported and absent surfaces belong in [IMPLEMENTATION-MAP.md](IMPLEMENTATION-MAP.md); test procedure belongs in [TESTING-SPEC.md](TESTING-SPEC.md).

## Current checkpoint

The hygiene review work is implemented: the differential harness declares and uses its Temporal adapter development dependency, `CLAUDE.md` contains only durable Mission guidance, portable Java 21 resolution replaces the Homebrew-only default, and the approved Ubuntu/macOS verification workflow preserves the existing feedback budgets.

The approved [production Temporal lifecycle](TEMPORAL-PROCESS-LIFECYCLE-SPEC.md) is implemented without a finite scenario-count lifetime or compatibility branch. It uses a semantic Process address, content-bound Update identity, retained-Update-first result resolution, adapter-owned lifecycle results, accepted-handler draining, explicit schedules, replay, and cleanup.

The sequential stale case now records the honest target relation: CIB Seven, Lean, and the TypeScript core retain semantic rejection; Temporal agrees through completion and returns adapter-owned `processClosed` for the explicitly post-terminal command. The parallel live-sibling stale case supplies exact four-target semantic rejection while the Process remains addressable, and the concurrent reversal is retained as an unordered one-commit/one-rejection race witness.

Harness-only Query extraction reconciles completion-command outcomes with completed Update results in Event History and the terminal state with the completed receipt. Failed Updates are named infrastructure failures, the start command is explicitly outside Update-result reconciliation, and intermediate Query observations remain independently compared with the pure core.

Lean now strictly decodes the same twenty-eight answer-free scenario documents consumed by the declared targets and echoes the decoded values. The extra-field mutation guards answer smuggling; retained CIB content binding owns disk-content drift detection for the eighteen CIB-backed cases, while the ten standards-only cases deliberately have no CIB expected result.

The exact implementation and absence boundary is recorded in [IMPLEMENTATION-MAP.md](IMPLEMENTATION-MAP.md). The [profile-parameterized admission specification](PROFILE-PARAMETERIZED-ADMISSION-SPEC.md) now separates topology-independent checked-source and Semantic Process graph validation from exact profile mechanism/cardinality capability. Lean and TypeScript independently reject unknown or mismatched profiles, and the source compiler no longer selects among complete topology predicates. The first composed standards profile admits one finite acyclic linear `PT1S` Timer plus User Task witness. Its executable gate covers closure-bound exhaustion, single-enabledness, stable wait resumability, stranded-state rejection, typed pre-start Temporal host capability, durable Timer-to-User-Task execution, replay, and a deadline mutation. The frozen experiment lane remains separately gated and not adopted as semantic authority.

The approved [Intermediate Catch Timer specification](capsules/INTERMEDIATE-CATCH-TIMER-SPEC.md) is implemented under its exact `PT1S` account. The checked graph retains the literal for independent Lean normalization; the IL, Lean relation/evaluator, semantic core, controlled-clock CIB runner, and Temporal adapter implement the same timer occurrence and exact-deadline firing. CIB proves pre-due ineligibility and due-time eligibility before execution. Temporal derives the content-bound firing exclusively from committed core state, durably waits while the Worker is absent at the due boundary, completes after Worker replacement, and replays one exact timer pair. The declared-target differential pipeline and retained deadline/bypass mutations preserve the capsule's required evidence. The time-skipping lane remains optional because the full local-server witness is the mandatory refinement gate.

The family-level [CIB Seven extension research](research/CIB-SEVEN-EXTENSIONS-RESEARCH.md) shows that Service Task source binding, project handler registration, JUEL/beans, Java delegate APIs, scripts, FEEL, and external-task protocols are distinct compatibility claims. The owner-approved [compatibility scope proposal](CIB-SEVEN-COMPATIBILITY-SCOPE-PROPOSAL.md) selects the exact bounded source pair. The [archived dual semantic-core architecture proposal](archived/DUAL-SEMANTIC-CORE-ARCHITECTURE-PROPOSAL.md) is owner-rejected because no non-Temporal JVM semantic-execution product exists; the single TypeScript semantic core and Workflow remain the production account, with JVM code behind Activity Worker or client-façade boundaries. The [Service Task effect spec](capsules/SERVICE-TASK-EFFECT-SPEC.md) closes namespace-aware source admission, profile registration to a neutral effect descriptor, neutral checked-graph lowering, the shared occurrence/descriptor/`openEffects` contract, Lean and TypeScript semantic behavior, core-owned transport material, domain-separated adapter identities, Activity execution from committed intent, retry reconciliation, typed exhaustion, Worker replacement, bypass detection, cross-instance separation, ordinary CIB execution, content-bound evidence, and live replay. CIB's phase-zero and ordinary runner derive the raw activity/implementation/handler binding from deployment state, reject independent raw binding perturbations, map it through the shared profile registration, retain the adapter-decided activation classification, and expose retry decrement only as raw host evidence.

The durable architecture now separates three concerns. BPMN Process Execution coverage is the primary engine roadmap; selected CIB Seven behavior is a classified compatibility overlay added when a standards ambiguity, extension, behavioral promise, or host-realization risk requires it; A12 Workflows `release/2025.06` is the downstream adoption target and prioritization lens. The [A12 Workflows compatibility ledger](research/A12-WORKFLOWS-COMPATIBILITY-LEDGER.md) defines 62 physical and 50 distinct exact-byte BPMN models, the maintained extension/delegate/API surface, and the A12 Full Stack Project Template as the canonical downstream blueprint. Its counts remain separate from BPMN and CIB coverage. The exact target-profile projector admits one of those 50 models unchanged at the static source/lowering boundary, while exact-model product execution remains zero of 50 because no external A12 model and delegate have run through a separately bounded adoption adapter.

The BPMN coverage assessment exposes thirteen reusable Process Execution mechanism families and their dependency edges without claiming an exhaustive line-by-line denominator or percentage. The formerly owner-approved [archived compositional BPMN admission proposal](archived/COMPOSITIONAL-BPMN-ADMISSION-PROPOSAL.md) is superseded as a staged production-admission programme. Its completed experimental results remain: ordering and bounded selector correspondence; finite graph predicates and stronger standalone Lean program validation; graph-derived tail and whole-process decomposition; saturation-certified path completeness and declarative acyclicity; and single- and two-token frontier localization. Stage 3b is accepted and frozen at 298 new or materially rewritten nonblank Lean lines against commit `362f91f`, excluding 11 relocated lines. No source admission, profile, CIB evidence, execution semantics, runtime state, observation, or production selector changed. The former universal preservation prerequisite is intentionally replaced by the targeted preservation gate approved below; the closure-limit and multiple-enabledness safeguards move into that gate rather than disappearing with the staged programme.

The same review exposed a canonical-projection defect independent of widened admission: TypeScript sorted mixed active waits by element ID while the `PAR-PROJECT-01` contract orders them by semantic kind and then element ID. Red/green TypeScript evidence locked the kind-order correction. At that stage Lean still grouped by kind while inheriting program operation order within each group, so it relied on ID-sorted admitted programs and unreachable mixed or repeated same-kind waits. The later Message wait kind fired the recorded reopen trigger: the Intermediate Catch Message capsule added element-ID sorting within all four kind groups and a four-kind Lean fixture whose reverse-ordered same-kind User Task pair distinguishes the element-order rule. All four projectors now order active waits by semantic kind rank and then element ID; the former ID-sorted-program premise no longer applies.

The [CreateDocument data and mapping specification](capsules/CREATE-DOCUMENT-DATA-SPEC.md) is implemented as an evidence-closed draft. The immutable `2.0.0` profile and relationship records remain distinct from every `2.2.0` account; the exact registered source bytes must admit unchanged when the optional adoption lane is selected, with missing external A12 source then classified as an infrastructure failure; and the mandatory project-authored MIT fixture supplies default runtime evidence without copying A12 source. Lean and TypeScript execute the same literal input, committed arguments, exact local patch, output mapping, scope cleanup, and Process-variable contract. Fresh packaged CIB `2.0.0` evidence observes synchronous mapped input/output and final state, while Temporal derives the Activity from committed intent and preserves the result under retry, Worker replacement, replay, and bypass discrimination. The CIB relation is deliberately a synchronous final-state host check, not an independent semantic intent or failure/rollback account.

The owner-approved [typed BPMN Error specification](capsules/BOUNDARY-ERROR-SPEC.md) stopped correctly during development when its packaged-CIB-Seven-`2.0.0` phase-zero probe found a new semantic conflict. CIB executes the Service Task output mapping on a matching caught Error, so the pre-error local sentinel becomes Process `relationshipLinkId = "must-not-map"` before the boundary User Task; the target-shaped null local write creates a present null-valued Process variable. On the mapped unmatched path, later output-mapping evaluation cannot resolve the destroyed local binding and rolls the start command back, leaving no runtime or historic Process state. The owner approved and the capsule implements revised option 2: a CIB-specific Error overlay with a validated pre-error local patch, discriminated null, program-owned output mapping, explicit independence limits, a mapping-free unmatched control, and atomic patch → mapping → cleanup → boundary ordering.

Every project-authored module now executes as strict TypeScript: the pipeline, contract-artifact, retained-evidence replacement, Java-resolution, strict-JSON, bounded-process, documentation, metamodel, and calibration harnesses plus all package tests. A mandatory strict no-emit gate resolves workspace packages to their source trees rather than ignored build output, and the source-hygiene guard rejects every hand-written `.js`, `.cjs`, or `.mjs` module without an allowlist. Direct MIT-licensed Node declarations provide the host API types; their type-only `undici-types` dependency adds no runtime component.

Temporal and pipeline scheduling inputs use named closed variants rather than optional-boolean mode bags. The adapter contract separates completion delivery, one execution schedule, and one explicit nullable effect schedule; the differential catalog separately names its primary/isolation effect pair, CIB retry schedule, and replay selection. Directly executed TypeScript uses zero-emit `as const` unions, while compiled package APIs may use native enums. The source-hygiene gate invokes TypeScript's `erasableSyntaxOnly` diagnostic over direct harness roots so a construct that Node cannot strip fails before runtime.

The source-hygiene root fix is implemented. The former 1800-line Temporal runner, 1600-line differential harness, oversized contract artifact owners, 900-line Temporal test, 690-line CIB runner, and monolithic Semantic Process Lean module are split by lifecycle or semantic responsibility. A dependency-free default gate now checks tracked and pending Lean, TypeScript, JavaScript, and Java source before commit, rejects files above 1000 nonblank lines, requires owner review above the 600-line target, and keeps the two Lean umbrella modules import-only. Every current hand-written source is at or below 600 nonblank lines and there are no reviewed exceptions.

The Lean comment-discipline enabling increment is implemented and closure-reviewed. [CLAUDE.md](../CLAUDE.md#comments--document-semantic-surplus) owns the semantic-surplus and anti-boilerplate instructions, [TESTING-SPEC.md](TESTING-SPEC.md#default-verification) owns the composed module-document and named-conformance-fact guard, and [IMPLEMENTATION-MAP.md](IMPLEMENTATION-MAP.md) records the exact implemented and absent surface. The guard contains no density, word-count, heading, or declaration-coverage metric; its sparse-valid conformance fixture must remain valid through the same assessment as the live worktree.

The `../oss` root has been inspected and the project-created external research checkouts are explicitly navigable from [SOURCES.md](SOURCES.md). Unknown or unregistered repositories and folders are ignored. A12 Workflows is the downstream adoption target, A12 Full Stack Project Template is its canonical downstream blueprint, `cibseven/cibseven` is the pinned engine oracle, and the remaining registered trees are standards, SDK, corpus, or semantic-workbench references.

The [Simple Boolean expression language](SIMPLE-BOOLEAN-EXPRESSION-DECISION.md) and [Exclusive Gateway conditional-routing specification](capsules/EXCLUSIVE-GATEWAY-CONDITION-SPEC.md) are implemented as the first standards-only expression/branching slice. Exact source admission rejects implicit XPath and every non-v1 body; checked source retains exact bodies and declaration order; Lean and TypeScript independently parse, evaluate, and route through generic `choose`; the exact three-step start closure and fixed operation graph protect the targeted preservation obligations; and Temporal completes, replays, and detects a route-substitution bypass without an evaluator Activity. The standards profile declares BPMN normative authority and explicitly omits CIB truth evidence. The separately audited JUEL dependency graph remains unadopted.

The admission-successor checkpoint is implemented. Exact whole-topology execution-surface predicates are removed and prohibited by an executable architecture guard. Profile capability, scoped structural graph validity, semantic execution admission, and Temporal host capability are distinct checks. The production start API returns `started | rejected` before Workflow creation, while Workflow-side wait-cardinality checks remain unreachable invariant defenses. The twenty-eight-case pipeline runs fifty-six isolated Temporal executions and replays thirty histories; CIB remains absent from the Timer/User Task composition, Intermediate Catch Message, Simple Boolean Exclusive Gateway, structured Inclusive Gateway, bounded Event-Based Gateway, and bounded Call Activity target sets.

The [bounded Call Activity specification](capsules/CALL-ACTIVITY-SPEC.md) is implemented and evidence-closed. Its closure review target `cefb56d` returned APPROVE WITH REQUIRED EDITS, and the same reviewer passed correction audit target `bb66c8c` without a material redesign. Exact source admission and QName resolution, the two-root checked definition forest, paired `invokeProcess`/`returnProcess` IL, independent Lean lowering and relations, the independently written TypeScript runtime, owner-derived called task identity, empty-data/refusal rules, quiescence/interruption guards, exact 3/3/2 closures, schemas, and exhaustive internal host classification are implemented. The exact profile and answer-free scenario are registered with a called-identity mutation; durable Temporal evidence separates root Workflow address from called task identity, rejects a caller-owned substitute while preserving the called wait, survives Worker replacement, recovers an exact committed retry without another accepted Update, reaches the terminal receipt, asserts three distinct Update pairs and zero unrelated host mechanisms, replays, and detects early return plus identity erasure.

The owner-approved [Intermediate Catch Message specification](capsules/INTERMEDIATE-CATCH-MESSAGE-SPEC.md) is implemented across exact Message/Interface/Operation source admission, profile-parameterized both-order graph admission, `awaitMessage`, independent Lean and TypeScript subscription semantics, the eleven-field canonical state replacement, explicit retained-CIB evidence replacement with Message fidelity `not-claimed`, passive Temporal Signal/result-ledger ingress, malformed/conflicting request classification, Worker absence, exact Signal history, replay, and the declared-target differential. Modeled throw, Message Flow, payload, key-based/global correlation, CIB Message compatibility, and A12 façade adoption remain excluded.

The owner-approved [ordinary embedded Sub-Process completion specification](capsules/EMBEDDED-SUBPROCESS-COMPLETION-SPEC.md) is implemented on the anchored baseline `5b34977`. It atomically adds definition scopes and ownership, replaces root-specific termination with scope-aware end and quiescent completion, closes Lean/core behavior, admits one exact one-level source profile, retains four `CIB-AGR-0007` schedules, and preserves the same passive Temporal Update lifecycle through Worker replacement, replay, and an early-exit bypass. Arbitrary nesting and exceptional propagation beyond the direct-parent follow-on remain excluded.

The [embedded Sub-Process Error-propagation specification](capsules/SUBPROCESS-ERROR-PROPAGATION-SPEC.md) is implemented on baseline `be7845d`. It atomically adds explicit boundary Error and Error End nodes, direct exact-code handler lowering, `throwError`, regional child-subtree cancellation with monotonic counter preservation, three retained schedules whose recovery lifecycle is classified by `CIB-AGR-0008` and whose stale host-task mapping uses `CIB-OP-0001`, post-throw Worker replacement/replay, and a semantic-core bypass guard without changing runtime-state or public-observation shapes. The bypass matches the genuine recovery prefix but is rejected by the next stale-command state/result discriminator. Catch-all, unmatched, multi-handler, ancestor-search, Event Sub-Process, payload, and concurrent-command semantics remain excluded.

The CIB Seven `2.2.0` breadth inventory is now a tested dependency-free research instrument rather than a one-off tag count. Its refreshed candidate split selected the 17-file/18-occurrence Message-addressed Receive Task subset because it reuses the implemented Message subscription and Temporal Signal seam. The 104-file addressless Receive Task subset remains explicitly excluded as CIB legacy execution-signal behavior. The first semantic checkpoint has now resolved the former Interface/Operation/Message-only channel as an atomic closed `operationMessage | directMessage` union across all current producers and consumers.

On 2026-07-31 the owner changed the immediate product order. CIB Seven `2.2.0` executable BPMN breadth now orders the near-term standards schedule, while BPMN remains normative and A12 `2.0.0` adoption remains separate. Ordered-work item 33 delivered the [runnable Temporal MVP](RUNNABLE-TEMPORAL-MVP-SPEC.md). Its implemented [User Task completion-data specification](capsules/USER-TASK-COMPLETION-DATA-SPEC.md) interprets “return data” as deterministic simulated form input committed through the real exact-task completion command after a realistic configured host delay while the task remains durable on Temporal; UI, rendering, identity, authorization, human-resource semantics, and a task inbox remain excluded. The pinned `2.2.0` evidence selects `CIB-EXT-0005`: task completion creates, overwrites, preserves, and retains explicit null Process variables before continuation, while no-data, unknown-ID, and stale-ID controls apply no unintended write.

The active implementation boundary is [IMPLEMENTATION-MAP.md](IMPLEMENTATION-MAP.md).

## Last verified baseline

The compositional-admission Stage 3b twice-amended checkpoint was accepted and frozen on 2026-07-30. Against commit `362f91f`, the Lean change contains 298 new or materially rewritten nonblank lines under its approved 340-line ceiling: 62 added and 28 removed in `CheckedSourceFrontier.lean` less 11 byte-identical relocated lines, 113 in the new `CheckedSourceParallelFrontier.lean`, and 134 added and 2 removed in `CheckedSourceFrontierConformance.lean`. The excluded 11 lines are the relocated `disabledOffFrontier` docstring, signature, and `sourceGraphFacts` binding, deducted under the same moved-code convention Stage 1 stated and Stage 2d applied; the raw added figure is 309 and both figures stay under the ceiling. The shared refactor preserves both accepted Stage 3a public theorem statements and their `[propext, Quot.sound]` axiom footprints; the new two-token theorem characterizes the enabled list up to `List.Perm` with `[propext, Classical.choice, Quot.sound]`, and its kernel witnesses refute exact equality under node reordering and anchor exchange while covering both-contribution, exactly one live contribution at the half-ready frontier, same-target, and initiation-pending cases.

The first independent review required the half-ready witness to state both its singleton enabled list and live sibling contribution; those two added `by decide` conjuncts are axiom-free, while `halfReadyJoinContributesNothing` as a whole retains `[propext, Classical.choice, Quot.sound]`. The second independent review accepted the change set with required amendments, all applied: `nodeDisabled`'s docstring now names its load-bearing settled-initiation hypothesis, which a kernel probe refutes the earlier wording against; the frontier module header now declares the token-count-agnostic laws it shares with the two-token account; `excludedTwoTokenShapes` now refutes the theorem's own `List.Perm` conclusion at both hypothesis-violating instances instead of comparing list lengths, which moves that witness from axiom-free to the refuted theorem's own `[propext, Classical.choice, Quot.sound]` footprint; the two deliberately unreferenced audit facts and the synthetic reordered graph's deliberately shared identity are annotated in source; and the line figure applies the project's moved-code convention. The module is committed, so the stage delta no longer depends on an unversioned working tree.

Forced elaboration of all four affected modules reports zero diagnostics at 2.93 seconds user, 0.48 seconds system, and 1.98 seconds wall, and the cold focused experiment build consumed 2.55 seconds user; both stay below the unchanged five-second stop. `lake build`, `lake test`, the focused experiment build and executable, eight source-hygiene guards, forty-eight infrastructure guards, and `git diff --check` on both the worktree and the index pass. Source hygiene reported 8/8 in this environment; under a shell exporting `FORCE_COLOR`, `scripts/source-hygiene.test.ts` reports a spurious 7/8 because its literal `error TS1294:` filter breaks on ANSI codes, which is a pre-existing untouched-helper defect and not attributable to this change set. No wall-time budget or assertion was relaxed and the aggregate wrapper was not re-run for this documentation-and-witness amendment. The `closeSupported` head choice remains explicitly outside the accepted result.

The latest green complete `./scripts/verify.sh` gate passed on 2026-08-02 at `d31c44a`. Its complete 25-case prepared pipeline completed in 12.606 seconds warm with every declared target relation and retained-CIB comparison in agreement, 27 live histories replayed, 50 isolated Temporal executions, and all cleanup complete. Eighteen cases retain CIB comparisons; the seven normative cases deliberately compare only Lean, the TypeScript semantic core, and Temporal. The gate passed 39 contract-artifact tests, 90 semantic-core tests, 51 BPMN-source tests, 85 Temporal-adapter tests, 112 infrastructure tests, 9 differential tests, and the 20-case source-hygiene guard. Its complete CIB gate reported 50 default-`2.2.0` tests with 18 expected release-profile skips, and exact CIB `2.0.0` passed all 23 tests. No feedback budget or assertion was relaxed.

At Event-Based Gateway implementation commit `f92b26b`, the 27-case pipeline reached every semantic comparison, both standards-only Event-race mutations, 29 history replays, 54 isolated Temporal executions, and clean teardown. The surrounding complete gate passed contracts, semantic core, BPMN source, Lean, CIB, infrastructure, differential, and all 91 then-current Temporal-adapter tests. Only the unchanged 15-second prepared-pipeline wall-clock assertion failed under the owner's reported unrelated sustained CPU load, first at 15.087 seconds and then at 18.382 seconds. After commit `273e747` added three direct-SDK activation witnesses, one root `./scripts/verify.sh` correctness run with the existing environment-only `BPMN_PIPELINE_WARM_BUDGET_MS=40000` override passed all gates, including 94 Temporal-adapter tests and the complete 27-case pipeline. That run confirms semantic completeness under contention but is not a performance baseline: the repository default remains unchanged, and the uncontended `d31c44a` result above remains the latest comparable warm measurement.

At Call Activity correction commit `4eaa0eb`, the same correctness-only complete gate passed 41 contract tests, 111 semantic-core tests, 62 BPMN-source tests, 98 Temporal-adapter tests, 124 infrastructure tests, 14 differential tests, both CIB release lanes, Lean builds and tests, and the complete 28-case pipeline. The pipeline compared the bounded Call Activity across Lean, the semantic core, and Temporal, detected the called-identity erasure mutation, replayed 30 histories, used 56 isolated Workflow IDs, and completed in 12.977 seconds warm under the existing `BPMN_PIPELINE_WARM_BUDGET_MS=40000` contention override. This is correctness evidence only and does not replace the uncontended `d31c44a` performance baseline or change the repository's 15-second default.

The reviewer Proto-MVP exit gate passed at clean graduation commit `4f2fe61`. The complete 28-case catalog reached every declared Lean, semantic-core, Temporal, selected CIB, and retained-evidence comparison; every case's seeded semantic mutation produced the required disagreement; 30 disposable histories replayed; 56 Workflow IDs remained isolated; and cleanup completed. The measured warm total was 14.620 seconds, numerically below the unchanged 15-second default, under the explicitly declared `BPMN_PIPELINE_WARM_BUDGET_MS=40000` allowance while unrelated CPU-heavy programs were active. It is therefore recorded conservatively as correctness evidence and does not replace the uncontended `d31c44a` performance baseline.

The same-model/same-effort sub-agent review and delegated-implementation protocol is implemented. It defines the exact valid warm-review cases, preserves context-cold governed reviews, requires three safe-boundary reports, keeps implementation lanes on disjoint owners, and reserves integration/docs plus the one complete gate for root. Review cost is bounded further by stage-specific focus, static findings before CPU-heavy gates, target-bound neutral review packets, and one combined cold checkpoint/closure review only for a genuinely single-lane atomic closure. The executable policy guard locks receipt equality for that exception; no generic worker tier may replace the root model or effort.

The independent cold-review policy guard reads approval from the owned Status section, resolves receipt SHAs as `HEAD` ancestors, derives the closed directory-independent pre-policy set from baseline `f1ef362`, validates local Markdown anchors, and enforces proposal, checkpoint, correction-audit, closure, and graduation boundaries. Immutable transition baseline `b361681` preserves older and already-started external receipts while requiring every later review target to use a context-cold sub-agent that inherits the author agent's model and effort; the same reviewer sub-agent owns warm correction-audit continuity. The temporary Receive Task pending-lane barrier was deleted after its checkpoint correction audit passed; the general closure-review guard now owns the remaining pause.

The portable-input boundary now has four explicit scopes. `verify` is complete for the MIT engine and CI with the hash-bound OMG corpus and no A12 checkout; `adoption` selects only pinned pristine A12 Workflows for its two exact-source facts; `research` selects the registered research trees; and `all` is their deliberate union. Every selected lane remains fail-closed. The default CIB runner preserves all six boundary-error engine methods under `2.0.0` while excluding the frozen class under `2.2.0`, where those methods are release-skipped; the A12-source-only method is superseded by the root-aware adoption harness. Default doctor and adoption evidence both pass under a custom external root. A partial non-Git target fails with a distinct diagnostic, and OMG fetch staging now occurs in the target parent so installation is a same-filesystem rename. The full registered `all` preflight verifies 13 repositories and four submodules, while the doctor continues to list every pin, dependency owner, and cache regardless of selected scope.

The complete `./scripts/verify.sh` gate passed on 2026-08-04 at `db0360e` with 539 tests passing and none failing, over a 30-case pipeline in which every declared target relation agreed, all 30 seeded mutations produced their required disagreement, and 32 live histories replayed. Its warm total was 16285ms against the unchanged 15000ms soft target, and an earlier run the same day at `56afcaa` plus the toolchain-pin change measured 21311ms with 534 tests. The owner reports additional CPU load on the machine during this period, so both figures are contended and neither is a performance baseline or evidence of a regression; the comparable uncontended measurement remains `d31c44a` at 12606ms over the smaller 25-case catalog. Both runs are correctness evidence only. Its 28-case prepared pipeline reached agreement on every declared target relation, rejected all three Lean mutations (`operationOrigin`, `scenarioExtraField`, `parallelControlPlaceProvenanceErasure`) and every case's seeded semantic disagreement, replayed 30 live histories across 56 isolated Workflow IDs, and completed cleanup. This run exists to gate-verify the pin change and is correctness evidence only: its 21.311-second warm total exceeded the unchanged 15-second soft feedback target, with CIB Seven accounting for 18.760 of the 18.984-second scenario-execution phase including 7.156 seconds of engine startup. No agent competed for cores, so contention does not explain the overage, but the uncontended `d31c44a` measurement covered 25 cases and remains the latest comparable warm baseline; treat the difference as unexplained rather than as a regression or a new baseline until a same-case-count uncontended run exists.

The Node and pnpm pins now have one owner each in [package.json](../package.json), read by [`pinned-toolchain.sh`](../scripts/pinned-toolchain.sh) and derived by the pnpm wrapper, the doctor, and the CI setup steps. pnpm moved from `11.18.0` to `11.20.0` under that mechanism after a host Homebrew upgrade blocked the wrapper's exact-version check. An executable guard fails when the shell reader disagrees with a real parse of the manifest, when a Node selector or `engines` entry drifts, when a derived consumer restates a literal instead of deriving, or when contributor-facing prose quotes a superseded version; it was confirmed red on each of those states before the bump landed.

## Approved decisions

The owner approved the following on 2026-07-26:

- **Parallel meaning:** implement normative BPMN per-incoming-Sequence-Flow fork/join behavior under the approved bounded capsule.
- **CIB relationship:** retain `CIB-DEV-0001` as a visible candidate deviation; do not expand the current CIB profile to claim parallel compatibility.
- **Representation:** share the checked BPMN graph and Semantic Process program, while allowing Lean and TypeScript to use asymmetric internal runtime representations.
- **Definition boundary:** lower exact admitted source through a checked project-owned BPMN graph to the bounded Semantic Process IL; do not extend the topology-specific executable IR.
- **R5 — Temporal semantic-policy ownership:** completed by moving current-state task projection, stimulus well-formedness, command identity, and same-stimulus comparison behind semantic-core-owned operations; the Workflow now delegates instead of scanning trace history or maintaining policy copies.
- **R6 — multiple-task CIB observation:** before canonical parallel CIB evidence, remove the single-active-task guard together with deterministic semantic task sorting and per-element wait multiplicity.
- **R8 — default Temporal history gate:** retain `test:temporal` in `verify.sh` so Update acceptance/completion, zero-Signal, and live-replay assertions remain mandatory.
- **Production Temporal lifecycle:** use the semantic-lifetime Workflow, collision-resistant Process address mapping, content-bound Update identity, retention-bounded accepted-result recovery, typed adapter lifecycle results, and accepted-handler drain rule in [the production lifecycle specification](TEMPORAL-PROCESS-LIFECYCLE-SPEC.md).
- **Production-lifecycle evidence correction:** retain sequential stale rejection as three-way CIB/Lean/core semantic evidence, require Temporal prefix agreement through completion plus a separate adapter-owned `processClosed` result under an explicit post-terminal delivery schedule, and add a parallel live-sibling stale scenario for exact four-target semantic rejection. Keep `UTASK-REFUSE-02`, update both capsule evidence rows, retain the concurrent reversal as an unordered race witness, and reconcile harness-only Query extraction with durable Update results and the completed receipt.
- **C2 — independent checked-source relation experiment:** execute the bounded five-operation source token game, renamed positional-lowering divergence witness, and projection-level observational preservation attempt under the owner conditions below. The relation is a transcription-correspondence account for reviewed meaning, not an independent BPMN authority. The experiment ended not adopted at the effort stop. The later 2026-07-30 decision selected a targeted production proof boundary while retaining the experiment and its countermodel; it did not adopt a source-to-run theorem.
- **C2 freeze:** accept the not-adopted result as the intended stop outcome. Keep the five-operation experiment compiling without extending its source semantics. Reopen only before admission widens beyond the two fixture-pinned topologies, after another fixture-coincidental lowering defect surfaces, or when a future capsule independently needs source-level semantics.
- **C1 next-capsule sequence:** the approved bounded Intermediate Catch Timer and Service Task/Temporal Activity capsules are complete under their exact specifications.
- **Intermediate Catch Timer capsule:** the exact approved `PT1S` account is implemented and graduated to [the specification](capsules/INTERMEDIATE-CATCH-TIMER-SPEC.md). The answer-free scenario carries `fireTimer` for Lean, semantic core, and controlled-clock CIB realization; Temporal derives the identical content-bound stimulus exclusively from committed semantic state. Per-rule CIB fidelity labels, quantified full-identity/time refusal, the early-firing non-law, explicit retained-evidence replacement, mandatory full-server durability evidence, and optional time-skipping calibration are retained.
- **D1 sequencing:** defer the optional dependency-free Lean/core fuzz experiment until after the timer preflight is reviewed so it does not delay the dominant adapter-feasibility discriminator.
- **Semantic-core runtime language:** reject a second Java semantic core and retain one TypeScript semantic core hosted by the TypeScript Temporal Workflow. A JVM Worker or Spring/client façade is not a semantic-core consumer. Reopen only for a named non-Temporal embedded JVM product mode that must own and advance semantic Process state in-process.
- **Migration product goal:** optimize the bounded profile and compatibility work for replacing A12 Workflows `release/2025.06` as the A12 product layered on CIB Seven, using defined inventory-based measures for unchanged model admission, unmodified delegate bridging, supported API adaptation, downstream-blueprint integration, and classified migration of the remainder. Do not claim generic drop-in engine compatibility.
- **Layered coverage strategy:** make BPMN Process Execution coverage the primary engine roadmap, add CIB Seven profile work only for a named ambiguity, extension, compatibility promise, host-realization risk, or downstream blocker, and keep A12 Workflows adoption as a later on-top adapter/component. A12 may prioritize reusable mechanisms but may not define lower-layer semantics. `CreateDocument` and boundary Error are accepted first-round vertical feasibility slices; do not repeat that implementation shape for every A12 model.
- **Cross-language wire hardening:** close the JavaScript-safe integer and strict JSON boundary gaps across current TypeScript and Lean lanes without introducing a Java core or compatibility reader. Canonical identifiers use lexicographic Unicode scalar-value order over well-formed exact strings with no normalization; reject unpaired surrogate encodings and exercise both BMP and supplementary-plane cases.
- **CreateDocument data and mapping capsule:** approve all eight selections now recorded in [the specification](capsules/CREATE-DOCUMENT-DATA-SPEC.md): distinct CIB Seven `2.0.0` target identity; unchanged source with an exact raw binding registered to a neutral Activity/mapped-success descriptor; string-only Process/Activity-local variables; literal/simple-reference mappings; committed arguments and typed local patch; content-bound argument/result identities; success-only transaction refinement; license-safe external-source evidence; and typed `BpmnError` immediately afterward.
- **Typed BPMN Error and interrupting boundary-error capsule:** approve all seven amended selections in the [specification](capsules/BOUNDARY-ERROR-SPEC.md), including revised option 2 after the complete packaged-CIB-Seven-`2.0.0` phase-zero probe exposed the original mapping conflict.
- **Revised caught-Error mapping account:** after the phase-zero stop, select revised option 2 with all six reviewer conditions. Classify caught-path output mapping as the profile-scoped `CIB-EXT-0004` extension and visible candidate deviation from the BPMN-only cancellation reading; represent null as a closed variable-value variant; validate success and Error patches identically; do not count CIB as independent evidence for the rule it supplies; isolate default unmatched behavior with a mapping-free control; and implement one atomic patch → mapping → cleanup → boundary transition.

The owner approved the following review-cost and feedback-budget decisions on 2026-08-03:

- **No governed cycle for non-material work:** a product, adapter, command-line, harness, or documentation increment that changes none of the governed claims does not open a proposal, checkpoint, or closure review, and is governed by the executable guards plus the applicable complete gate. The negative case is owned by [TESTING-SPEC.md](TESTING-SPEC.md#independent-cold-review-gate) and its trigger by [CLAUDE.md](../CLAUDE.md#independent-cold-review). Evidence for the decision: of ten closure-stage findings on the product-surface increment, two were material and eight were documentation consistency, at roughly 570,000 reviewer tokens across four sub-agent runs.
- **Recurring drift becomes a guard, not a review finding:** the pre-release architecture guard now rejects retired product vocabulary in maintained documentation and reader-facing READMEs as well as active source. It was verified by a deliberate violation, and it immediately caught two live defects that had shipped — the top-level and adapter READMEs still instructed users to run a deleted example configuration.
- **Two-tier CIB Maven deadline:** every CIB Maven invocation resolves one ceiling of 120000ms from a single owner and reports a 30000ms workstation soft target after success. The pipeline export bridge previously hardcoded a stricter 30000ms deadline that `BPMN_CIB_MAVEN_TIMEOUT_MS` could not reach, which is how host load average 79 failed a documentation-and-guard commit; unifying the call sites is part of that fix.
- **Waived full-gate re-run:** the owner waived re-running the complete gate on levers commit `162966f` once the host quietened. Its three changes are the pipeline budget split, the documentation vocabulary guard, and documentation, and the focused gates owning exactly those passed — differential 14/14, infrastructure 126/126, documentation and review-policy guards 18/18, plus a planted-violation check of the new guard. The last green complete gate remains `4a12629`, and the lanes that failed afterwards are untouched by `162966f`.
- **Two-tier warm-pipeline feedback measure:** the 15000ms figure becomes a reported soft target and the hard ceiling becomes 40000ms, because unrelated host CPU load failed a documentation-only commit at 15210ms while the identical pipeline had measured 13520ms an hour earlier. Automatic regression detection therefore moves from the assertion to the reported `phaseMs.warmTotal` figure, which must be compared against the last uncontended measurement; "the gate passed" is no longer evidence about speed.

The owner approved the [interrupting Activity boundary Timer capsule](capsules/ACTIVITY-BOUNDARY-TIMER-PROPOSAL.md) on 2026-08-03 after its cold proposal review and two audited correction rounds. All four decisions were approved as recommended: the selection itself; one new `awaitBoundedUserTask` operation rather than a widened `awaitEventRace` or sibling waits; no runtime occurrence record, accepting that a later repetition capsule may add one; and a standards-only boundary registering no CIB relationship. Implementation must pause at the conditional semantic checkpoint after the first green Lean and semantic-core result.

The owner approved the following product-surface decisions on 2026-08-03, now owned by the graduated [runnable Temporal MVP specification](RUNNABLE-TEMPORAL-MVP-SPEC.md):

- **Full-MVP breadth:** lift the product command to every registered profile through one driver keyed to published enabled interactions, a deterministic product effect handler over the complete `EffectExecutionResult` union, and atomic removal of the former single-task actor configuration field. This adds no BPMN semantics, profile, IL operation, or deployment claim.
- **Graduation destination:** replace the bounded contract inside [RUNNABLE-TEMPORAL-MVP-SPEC.md](RUNNABLE-TEMPORAL-MVP-SPEC.md), keep that filename, and carry the review receipt into it voluntarily because it belongs to the closed pre-policy grandfather set.
- **Host capability:** a profile needing a wait-set shape the current predicate rejects is a stop condition routed to an owner capability decision, never absorbed into driver logic. All fifteen registered profiles are admitted today.
- **Negative controls:** no synthetic program is added for the host-capability rejection, whose two failure codes already have direct adapter coverage.

The owner approved the following review-programme decisions on 2026-07-30:

1. **Targeted preservation gate:** supersede the staged universal checked-source preservation programme. This intentionally revises the preservation prerequisite approved in the compositional-admission proposal; it does not correct a nonexistent prerequisite. Every capsule that widens admission or replaces lowering, runtime representation, or public observation must state the exact source-to-result claim at risk, retain a separating lowering or representation discriminator, and close the smallest reusable theorem or executable guard that protects that claim. The gate must additionally prove or executable-check that every newly reachable internal closure stays within `semanticProcessClosureLimit`; every newly reachable multiple-enabled state is either an approved independent/order-invariant pair, carries an explicit semantic choice, or is rejected consistently by Lean and the TypeScript core; and every newly reachable stable `running` state is terminally complete or exposes an explicit semantic resumption surface such as a User Task, timer, effect, interaction, or subscription. Tokens alone do not establish progress: a half-ready join beside a live wait is valid because the wait is the resumption surface, while a stable running state with tokens and no possible semantic ingress is a failed preservation check. A general preservation theorem becomes mandatory only when a second capsule needs the same proposition or the targeted proof cannot isolate the risk without recreating the general bridge.
2. **Atomic layer repair:** remove Camunda namespaces, A12 bean names, and target-specific discriminator literals from the checked graph, Semantic Process IL, Lean, and the pure TypeScript core in one pre-release replacement. Generic mapping names and values remain source-derived semantic data, never lower-layer admission discriminators. Retain `awaitEffect`; no new IL operation is authorized. The lower layer uses profile-registered opaque protocol and operation identities validated as safe strings. Exact Camunda source bindings remain in source/profile admission evidence, the CIB profile owns source-token-to-neutral-operation translation, and the A12 adoption layer owns neutral-operation-to-bean or Worker binding. The repair must preserve the exact existing admission set rather than replacing literal validation with a wider shape rule. Because the canonical effect descriptor changes, all producers, consumers, schemas, fixtures, mutations, and retained CIB evidence are replaced atomically. Lean continues to recompute neutral checked-graph-to-program lowering, but no claim may imply that it independently derives the raw Camunda-to-neutral translation.
3. **Atomic scoped-data replacement:** replace the flat runtime variable representation inside one scoped-data capsule rather than adding a root-scope wrapper. Process and Activity-local ownership are explicit in every affected Lean theorem and TypeScript transition. Activity-local state is internal and does not enter the canonical public observation; `variables` continues to expose Process-scope bindings only. Activity-local scope is keyed by the complete semantic effect occurrence identity, never by a bare BPMN element or activation ordinal. The capsule must migrate or restate all affected theorems over explicit scope ownership and complete its required Temporal hosting/refinement preflight.
4. **Dependency-first, target-weighted dual roadmap:** maintain one vendor-neutral mechanism spine and separate A12 adoption checkpoints. The implementation order is the atomic layer repair, raw-observation/fidelity repair, scoped-data replacement, typed mapping/expression work, Exclusive Gateway with conditional/default Sequence Flow, Message Event subscription/correlation, then broader Sub-Process and event propagation. This gives structural BPMN progression a majority of the semantic spine while resolving the data dependencies that make conditional routing sound. A12 frequency may prioritize or split a capsule but never defines BPMN meaning. Census claims remain separate and exact: `camunda:inputOutput` occurs in 43 of 50 target models, `camunda:inputParameter` in 41 of 50, `camunda:formKey` in 35 of 50, Exclusive Gateway in 9 of 50, and Message Event Definition in 8 of 50. The ledger records 233 expression occurrences and 79 distinct exact strings but no per-model expression percentage. Sub-Process/scope prevalence is unmeasured; Boundary Event at 7 of 50 and Error Event Definition at 3 of 50 are nearby structural anchors, not a scope denominator.
5. **Raw-observation and fidelity repair:** classify every field of `scenario.schema.json#/$defs/stateObservation`—ten top-level fields—and every nested occurrence, wait, timer, effect, and variable field as engine-observed, adapter-derived, adapter-decided, or not claimed. Add a raw observation only where the pinned engine exposes the fact; otherwise retain the honest fidelity label. Do not add a second parser or count the raw verifier as another independent evidence lane when it shares projection rules. Record explicitly how the layer repair changes what Lean independently recomputes about source binding.
6. **W3 classification retained:** accept W3's semantic-refusal-versus-harness-inability classification and its supplied wording unchanged. Activation-ordinal derivation remains unsupported rather than fabricated. The canonical wait-order rule must reopen if a later admission makes mixed or repeated same-kind waits reachable, changes the wait-kind domain, or invalidates the ID-sorted-program premise; that capsule must implement element-ID sorting in Lean or prove that program order and canonical order coincide. The adjacent Lean/TypeScript multiple-enabledness divergence is owned by the targeted preservation gate, not reclassified by W3.
7. **Qualitative Temporal history-cost boundary:** document only that pure semantic-core transitions may close to quiescence inside one Workflow Task without one Temporal Event per semantic step. Event History growth follows Workflow Task lifecycle and durable operations such as Updates, timers, Activities, and completion, while CPU and replay work can still grow with internal closure. The closure is bounded by the semantic core's checked closure limit and exceeding it remains a harness failure. This decision neither introduces a numeric cost model nor authorizes Continue-As-New.
8. **Standards-first expression sequence:** implement the dependency-free [Simple Boolean language](SIMPLE-BOOLEAN-EXPRESSION-DECISION.md) first under its explicit immutable URI and the bounded [Exclusive Gateway capsule](capsules/EXCLUSIVE-GATEWAY-CONDITION-SPEC.md). Lean and TypeScript independently parse and evaluate the complete five-form total language over Process-scope string/null bindings; omitted language remains BPMN's XPath default and is rejected. The source boundary retains exact text, the generic `choose` operation owns first-true/default routing, and Temporal hosts pure internal closure without an evaluator Activity. The separate [JUEL architecture](JUEL-EVALUATION-ARCHITECTURE-DECISION.md) remains the selected CIB compatibility approach but is deferred; its exact approved dependency graph is audited but unadopted, and no Java evaluator module is retained. The existing exact `MappingExpression.localVariable` lookup remains bounded to the two implemented mapping capsules and must not grow into either language.
9. **Compositional admission successor:** exact whole-program execution-surface predicates are a provisional boundary, not the accepted steady state. The successor is profile-parameterized structural admission: one reusable graph/program validator owns topology-independent reference, producer/consumer, reachability, co-reachability, acyclicity, closure, and stable-state obligations, while a selected profile supplies typed mechanism/cardinality capabilities. Do not add a seventh whole-topology disjunct. Before the Message Event capsule can be approved for implementation, its proposal must select the smallest production admission slice that composes already implemented mechanisms plus the new subscription mechanism without recognizing the complete model as one named shape.
10. **Host capability is not semantic admission:** every capsule that makes a new wait-set combination reachable must define the adapter capability predicate separately from semantic program well-formedness and executable-check it before Workflow start. The current Temporal host accepts exactly one committed timer or one committed effect wait at a time and rejects a mixed timer/effect set; this is an adapter limitation accidentally protected by current whole-topology admission, not BPMN meaning. A capsule must either preserve that reachable wait-set bound or implement and evidence the required multi-wait scheduler. Violation must be a deterministic pre-start adapter admission result, never a non-retryable Workflow crash.

## Current owner decision

The independently reviewed [Message-addressed Receive Task specification](capsules/RECEIVE-TASK-MESSAGE-SPEC.md) owns the implemented selection. The project-authored CIB Seven `2.2.0` phase-zero probe passed before any Message-channel replacement: one public subscription exposed the expected Receive Task and Message name, exact public delivery removed it, and the Process completed. This selected `cibseven-2.2.0-message-addressed-receive-task-draft` and bounded agreement `CIB-AGR-0009`; the standards-only fallback was not needed. The root Message name remains source-only and never enters checked, program, runtime, or canonical identity.

The original bundled Stage 3 and the staged compositional-admission programme are superseded. Revised Stage 3a and Stage 3b are accepted experimental results and remain frozen; Stage 3b's only claim is graph-derived two-token enabled-list localization up to `List.Perm`. It excludes either target firing as a law, exactly-two enabledness, closure or selector soundness, commutation, closure fuel, the four-step bound, Timer/effect source semantics, source-to-program correspondence, production admission, profiles, evidence, runtime, observation, and selector changes. Stages 3c–3e, optional Stage 2e, Stage 4, and the composed profile are not scheduled. Their production-relevant closure-limit, multiple-enabledness, and stable-state-resumption obligations now belong to the targeted preservation gate above. Widened production admission is no longer ownerless: decision 9 requires the profile-parameterized structural successor before the Message Event implementation and rejects another complete-topology predicate.

The atomic neutral effect-boundary repair under decision 2 is implemented as one pre-release replacement. It preserves the exact admitted source set, retains `awaitEffect`, maps exact source/profile bindings to registered neutral protocol/operation descriptors, replaces every producer and consumer of the canonical descriptor, and explicitly replaces all retained CIB evidence. Lean independently checks neutral checked-graph-to-program lowering but does not derive the raw Camunda-to-neutral profile translation. The subsequent raw-observation repair is also implemented: its exhaustive [field-depth fidelity table](TESTING-SPEC.md#canonical-cib-observation-fidelity), raw state-query snapshots, schema guard, and seeded mutations make the resulting source/observation independence boundary explicit. The [atomic scoped-data replacement](capsules/SCOPED-DATA-SPEC.md) is implemented without changing source admission, canonical wire contracts, effect transport, or retained evidence. Decision 8 is also implemented: the [Simple Boolean language](SIMPLE-BOOLEAN-EXPRESSION-DECISION.md) and [Exclusive Gateway condition specification](capsules/EXCLUSIVE-GATEWAY-CONDITION-SPEC.md) close strict source admission, checked conditions, generic `choose` lowering, independent Lean and TypeScript parsing/evaluation, targeted closure and reachability guards, answer-free standards evidence, Temporal completion/replay, and a Workflow route-substitution discriminator. The standards scenario explicitly omits CIB; the separately approved JUEL architecture remains deferred.

The [Service Task effect spec](capsules/SERVICE-TASK-EFFECT-SPEC.md) records the implemented owner decision. Its seven selections remain binding with the layer-repair amendment: the success-only account and exclusions; green phase-zero `asyncBefore` delegate-expression binding; namespace-aware admission of exactly two Camunda attributes without general JUEL; `awaitEffect` with shared occurrence identity, committed neutral protocol/operation intent, separate `openEffects`, and adapter-rendered transport key; the exact two-attempt Activity policy and typed adapter failure; separate host schedules with isolated stores and the eight-scenario/sixteen-execution/nine-replay matrix; and the named retry/fault/cancellation/external-task reopen conditions. Raw source binding is owned by source/profile admission and the CIB overlay, while downstream bean or Worker binding remains outside the semantic core.

Implementation also must name the transport tuple's non-`SemanticProcessIdentity` definition group, retain a `processId`-drop mutation, keep storage constraints outside `EFFECT-INTENT-01`, record the non-independent CIB activation projection, and add a delegate-expression negative control. Stop at the first green Lean/core checkpoint for semantic review before beginning the Temporal lane.

The research corrects three tempting assumptions: CIB Seven retains the Camunda namespace rather than defining `cibseven:*` attributes; the async-continuation job is immediately executable rather than timer-like eligibility-gated; and FEEL is not the language used by CIB Service Task delegate expressions, which use JUEL. Manual public job execution uses the failed-job listener and default retry decrement in both pinned source and packaged `2.2.0` evidence. Exact bean resolution, prefix-independent namespace handling, immediate executability, null due date, deployment-derived binding projection, fail-once retry decrement, and clean public re-execution are locked by the phase-zero probe.

The owner-approved [CreateDocument data and mapping specification](capsules/CREATE-DOCUMENT-DATA-SPEC.md) is implemented and evidence-closed as a draft under its exact scope. It authorizes no Java Worker, general JUEL, `BpmnError`, failure/rollback equivalence, source redistribution, or new dependency. The [typed BPMN Error and interrupting boundary-error specification](capsules/BOUNDARY-ERROR-SPEC.md) bounds the implemented product-forced business-error decision without changing successful-result semantics. Its target review establishes that the three maintained Error Event Definitions contain one matching boundary catch and two Error End Events, so the specification selects only the exact-code catch.

## Approved next-capsule sequence

### C1 — next semantic capsules

**Owner decision:** approved for a timer capsule followed by a service-task Activity capsule. Both capsules are complete under their exact specifications.

**Timer first:** use one bounded `None Start Event → Intermediate Catch Timer Event → None End Event` discriminator with one reviewed literal duration form. The semantic core must own the timer occurrence, logical deadline, readiness, duplicate/stale firing behavior, and public observations. The adapter may translate one typed semantic timer wait into a durable Temporal timer and feed its recorded wakeup back as an explicit typed stimulus; Temporal time must not become implicit BPMN state or decide semantic firing. The mandatory preflight must settle the admitted BPMN timer-expression subset, logical-time input, timer identity, deadline comparison, Workflow completion, duplicate wakeup, Worker restart, replay, and whether the full-server plus time-skipping evidence can remain inside the feedback budgets. The nearest adapter counterexample is deriving the deadline independently in Workflow code or advancing the core merely because a Temporal timer fired.

**Service task second:** the bounded `None Start Event → Service Task → None End Event` discriminator is implemented in the [Service Task effect spec](capsules/SERVICE-TASK-EFFECT-SPEC.md). The semantic core owns the Service Task occurrence, structured effect intent, stable idempotency material, and closed success result while a Temporal Activity remains only the effect executor. Exact finite timeouts and retries, idempotent reconciliation, typed adapter failure on attempt exhaustion, and the boundary between hidden Temporal attempts and CIB-visible retry evidence are fixed; general JUEL, Java API compatibility, supported cancellation recovery, semantic faults, and incidents remain excluded.

**Why this order:** the timer introduces the first durable host Command driven by semantic waiting without also introducing nondeterministic I/O. The Service Task then reuses the proven wakeup/input loop while adding external effects and retry separation. Another pure-topology capsule would exercise the existing Query/Update host composition and would not reduce either risk.

**Disposition:** Stage 3b is accepted and frozen. The compositional-admission programme is superseded; later proof stages and its composed profile are not the next work.

## BPMN coverage program through and after the reviewer proto-MVP

The boundary-error capsule is closed. The reusable BPMN mechanism-coverage programme remains the long-term route to Process Execution Conformance. Three new breadth increments—Inclusive Gateway, Event-Based Gateway, and Call Activity—are now scheduled into the reviewer proto-MVP below. A12 remains a prioritization and acceptance input, and CIB remains an on-demand overlay.

### Work unit 1 — establish the coverage denominator and mechanism map

Expand the [BPMN requirement ledger](BPMN-REQUIREMENT-LEDGER.md) in bounded clause families across the Process Execution-relevant parts of Clauses 8, 9, 10, 13, and 15 plus their CMOF/XSD anchors. First enumerate executable construct and lifecycle families with stable requirement IDs and dependency edges; then deepen each family when its capsule begins. Do not publish a completion percentage until that extraction is demonstrably exhaustive.

Group requirements by reusable mechanism rather than by BPMN XML element or A12 model: Process lifecycle and graph flow; Activity lifecycle; branching/merging; scopes and interruption; event subscription/consumption; variables/data/mapping; looping/multi-instance; compensation; import/reference closure. Each group records the closest implemented slice and the next missing standard proposition.

In the same work unit, assess a bounded compositional source-admission/lowering step for acyclic private Processes made only from already implemented mechanisms. The objective is to stop adding exact-topology compilers when no new semantics are present. It must retain strict rejection for unsupported constructs and must not become an unreviewed general BPMN compiler.

### Work unit 2 — repair the layer and evidence boundaries

First replace the vendor- and target-shaped effect contract atomically with the neutral profile-registered operation contract selected above. Preserve the exact admitted source set, regenerate the content-bound evidence explicitly, and record the resulting source-binding independence boundary.

The subsequent raw-observation repair classifies every canonical observation field and nested field by raw-producer fidelity. It adds retained state-query facts for Process status, controlled engine time, and Process variables while preserving adapter-derived or adapter-decided labels where the engine supplies no semantic fact; it manufactures neither activation ordinals nor an additional evidence lane.

### Work unit 3 — scoped data and profile-selected expressions

The flat runtime variable representation is replaced by explicit Process and Activity-local ownership under the [scoped-data specification](capsules/SCOPED-DATA-SPEC.md). Activity-local state stays outside the public observation and is keyed by complete semantic effect occurrence identity. This closes the representation dependency for expressions that read variables in a semantically meaningful scope.

The expression ledger records 233 occurrences and 79 distinct exact strings without inventing a per-model percentage. The conditional-expression census isolates 16 JUEL occurrences, 11 distinct strings, and 10 divergent decisions across 8 of 50 models. The deferred read-only JUEL context could serve 9 of 16 occurrences, 5 of 11 strings, and 4 of 8 models at the language/context level only, but none uses the active Simple Boolean URI and no unchanged A12 adoption follows. The standards-first language implements exactly five total Boolean forms over complete Process-scope string/null bindings in Lean and TypeScript. Mutation, bean, `execution`, method, function, and engine-service access remain separate effect or adoption-capability work. The census-backed mapping anchors remain `camunda:inputOutput` in 43 of 50 models and `camunda:inputParameter` in 41 of 50.

### Work unit 4 — structural BPMN progression

The implemented [Exclusive Gateway condition specification](capsules/EXCLUSIVE-GATEWAY-CONDITION-SPEC.md) fixes one explicit language URI, two ordered conditional Sequence Flows, one conditionless default, one typed generic `choose` operation, and one selected outgoing route. Lean and TypeScript independently parse and evaluate expression truth; the semantic core owns evaluation timing, XML Sequence Flow declaration order, default selection, and token routing. Temporal persists the pure core result without a new evaluator Activity, suspension, or receipt. Exclusive Gateway occurs in 9 of 50 target models, but this standards profile claims zero unchanged A12 adoption because no retained A12 condition uses its language URI.

Follow it with one bounded standard Message Event lifecycle with explicit subscription identity, correlation input, consumption, duplicate/stale refusal, and Temporal delivery/replay. Message Event Definition occurs in 8 of 50 target models. Prefer the smallest catch/throw pair that establishes reusable semantics before start Events, boundary messages, or event races.

Then add broader nested execution scope, Sub-Process structure, interruption, and event propagation. Scope/Sub-Process prevalence has not been measured in the A12 census; Boundary Event at 7 of 50 and Error Event Definition at 3 of 50 are nearby anchors only. Reuse the bounded boundary-error result only where its standard proposition survives without the A12/CIB mapping extension.

### Subsequent mechanism queue

After the proto-MVP gateway and Call Activity slices, prioritize User Task performer/assignment and data behavior, additional start/intermediate/end Events, loop and multi-instance Activities, compensation, Business Rule Tasks, and Script Tasks according to normative dependency and implementation leverage. A12 frequency breaks ties; it does not remove standard families from the eventual conformance target.

### CIB on-demand gate

For every BPMN work unit, answer these questions before adding CIB-specific implementation:

1. Does BPMN leave a material choice that this profile must resolve?
2. Does admitted source require a `camunda:*` extension?
3. Does the compatibility claim require a pinned engine observation not already covered?
4. Can CIB configuration or host behavior change the canonical or adoption-visible result?
5. Does a concrete downstream blocker remain after the standard mechanism is implemented?

If all answers are no, no new CIB profile surface is added. If any answer is yes, add the smallest relationship-register entry, profile delta, probe, fidelity label, and mutation that establishes that fact. Do not make every standard capsule pay for a new CIB extension lane.

### Vertical-slice limit

One first-round vertical witness is appropriate for a genuinely new host seam such as durable timers, external effects, or typed business faults. Once the seam exists, later models and source variants should reuse it through generic admission, profile configuration, and downstream regression tests. Do not add model-specific IL operations, Lean evaluators, semantic-core branches, or Temporal Workflows for each A12 model.

## Deferred assessment

### D1 — seeded Lean/core fuzz lane

**Deferred behind the next adapter-feasibility preflight.** A hand-rolled seeded generator could compare well-formed stimulus sequences over the admitted topologies in batches and report the exact seed on failure. Malformed documents require an explicit per-case admission-result envelope or isolated Lean invocations because the strict Lean decoder currently rejects an entire invocation while the TypeScript core classifies structural admission separately; do not coerce either into a semantic outcome. Estimated cost is 220–340 lines of JavaScript and protocol/test support with no new dependency. Its value compounds as capsules grow, but it is not the nearest Temporal-adapter risk.

## Approved C2 experiment

### Scope and recommendation

Define a declarative token-game relation directly over `CheckedProcess`, in BPMN vocabulary with tokens on Sequence Flows and per-incoming-Sequence-Flow synchronization, then relate its projected observations to the existing Semantic Process execution for every admitted source and supported stimulus sequence. Prove equality of projected public observations after each supported stimulus, not runtime-state equality. The source relation must not use `lowerCheckedProcess`, the IL evaluator, or IL operations.

The competing accounts are:

1. a direct checked-graph relation authored from the capsule clauses, which supplies an independent source-side account;
2. a source relation defined by lowering to or executing the Semantic Process program, which is circular and cannot support the claimed preservation result;
3. a permanent proof-boundary decision that retains artifact equality and structural lowering theorems but explicitly declines source-to-run preservation.

The separating witness must be quantified rather than fixture-specific. Mutate `lowerNode` so User Tasks are paired positionally with sorted input/output flow lists instead of selecting flows by source and target. Existing fixtures can keep passing when their task and flow identifier orders happen to align. An admitted renamed graph whose task-ID and flow-ID orders disagree then produces divergent behavior, and a universally quantified preservation theorem detects the defect. The earlier proposal to route Task A to Task B's join input is rejected as a witness because the fixture-defined Lean program laws and CIB differential already detect it before a preservation theorem contributes any independent assurance.

The expected experiment is approximately 400–700 lines of Lean: a source runtime, about six transition constructors, the `flowControlPlaceId` renaming relation and prefix-injectivity lemma, per-operation correspondence, and run-level stimulus/closure alignment. The difficult boundary is `enabledTransitions`/`closeSupported` list-order alignment; prove equality of public projections per stimulus rather than identical internal states.

The buy-now rationale is structural, not present mutation coverage: the direct graph account is the missing transcription-correspondence side of the stated proof obligation, the run-level induction skeleton becomes substantially more expensive after timers or Activities enlarge runtime state, and its universal quantifier gains further value as admission widens. Normative/profile review and CIB evidence remain the authorities for the BPMN account.

### Binding conditions

- Do not change `closeSupported`, `enabledTransitions`, observation projection, or any wire contract for proof convenience; stop and route any such need through the semantic-change process.
- Retain the positional-lowering counter-model as executable divergence in the separately gated experiments lane. Empirically require all retained fixture locks to remain green under the mutation and use theorem failure only during development, not as retained red evidence.
- Stop and record the precise unresolved boundary if the run-level proof does not close within approximately 700 lines of Lean or requires restructuring beyond the slice.
- On adoption, update the IL specification proof boundary, implementation map, and rule-to-evidence rows while retaining the witness. On non-adoption, record the unresolved boundary in the experiment document. In either end state, the experiment records all competing accounts and why the permanent-boundary account was rejected.
- Stage 2 later added independent executable reachability, acyclicity, and exact producer/consumer-shape checks to Lean `programWellFormed`. The corresponding profile-specific checks and stronger TypeScript program validator remain separate compositional-admission work; the original C2 result neither proves nor depends on them.

### Result

The experiment ended **not adopted** at the binding effort stop. Approximately 700 lines of separately gated Lean implement the direct checked-node relation and sound executable selector, source admission/closure/observation runner, six retained-fixture mutation controls, correct endpoint-lowering agreement on the renamed graph, and renamed public divergence under positional lowering. A flow-only branch permutation proved observationally symmetric at the current public boundary; the retained positional-record mutation additionally reads task metadata through the wrongly paired Flow target, which swaps task names only under the renamed graph.

The removed correspondence attempt reached the Sequence-Flow/control-place state mapping, prefix injectivity, and token-list lemmas. It had not yet proved nonempty-Flow facts from checked admission, wait and activation mapping, complete enabled-transition list correspondence, parallel supported closure, admission, observation, or the final stimulus induction. Continuing would exceed the owner-approved effort boundary or require proof-facing restructuring of production internals. [The experiment record](experiments/CHECKED-SOURCE-RELATION-EXPERIMENT.md) owns the precise unresolved boundary and red/green evidence. No production semantics, wire contract, observation projection, or rule-to-evidence row changed.

## Implemented discriminator slices

The bounded parallel discriminator is:

```text
None Start
    ↓
parallel fork
   ↙   ↘
User A User B
   ↘   ↙
parallel join
    ↓
None End
```

The bounded timer discriminator is:

```text
None Start
    ↓
Intermediate Catch Timer PT1S
    ↓
None End
```

The [parallel fork/join spec](capsules/PARALLEL-FORK-JOIN-SPEC.md) and [Intermediate Catch Timer spec](capsules/INTERMEDIATE-CATCH-TIMER-SPEC.md) own their semantic rules, observations, laws, non-laws, witnesses, runtime-information invariants, evidence matrices, assurance boundaries, and exclusions. The [Semantic Process IL spec](SEMANTIC-PROCESS-IL-SPEC.md) owns the checked source contract, current operation families, lowering, well-formedness, exact Lean proof boundary, event-growth policy, maintained obligations, and stop criteria.

## Reviewer proto-MVP milestone

The next product milestone is a reviewer-facing end-to-end demonstration of every implemented bounded executable BPMN profile, not a curated subset. Its scope is derived from the guarded artifact catalogs: every implemented profile artifact must be referenced by a registered answer-free scenario, every registered scenario must have exactly one pipeline case, and every retained CIB projection must be content-bound to its scenario. [IMPLEMENTATION-MAP.md](IMPLEMENTATION-MAP.md) remains the human-readable claim boundary; the [complete differential/refinement pipeline](TESTING-SPEC.md#complete-differentialrefinement-pipeline) is the executable demonstration and emits the exact cases, profiles, target relations, mutations, executions, and replay results it actually ran.

The proto-MVP exit gate adds three separately reviewed mechanisms to that complete catalog:

| Planned demonstration | New proposition | Required architectural seam |
|---|---|---|
| bounded Inclusive Gateway split/join | select every true outgoing condition and synchronize only the branches activated for that gateway occurrence | reuse typed condition evaluation and token multiplicity while preserving the selected branch set needed by the converging join |
| bounded Event-Based Gateway race | arm competing external catches, commit exactly one winner, and cancel every losing subscription or timer without exposing a host scheduling choice as BPMN semantics | compose the existing Message and Timer wait mechanisms with explicit race identity, atomic winner selection, cancellation, Worker replacement, and replay |
| bounded Call Activity | resolve one called Process definition, create a separately owned called Process instance, and complete the caller only after normal called-instance completion | preserve definition/instance identity, parent-child lifecycle, durable invocation, result recovery, and replay without equating a BPMN Call Activity with a Temporal Child Workflow |

Those rows define planning outcomes, not approved profiles. Each needs its own normative/CIB classification, proposal review, Temporal preflight, semantic checkpoint if triggered, implementation evidence, closure review, artifact registration, pipeline case, and exact exclusion set before it enters the executable catalog.

The demonstration must show the same boundary in order: exact BPMN XML bytes → checked project-owned graph → Semantic Process IL → Lean reference behavior and laws → independent TypeScript semantic-core result → Temporal durability → differential comparison → retained CIB evidence only where the selected profile claims it. Each catalog entry must state its exact admission profile, public observation, nearest unsupported claim, and whether CIB is an oracle lane or deliberately absent.

The milestone excludes general model composition, general expression languages, loops and multi-instance Activities, Event-Based Gateway races beyond the one reviewed discriminator, Call Activity mappings or lifecycle variants beyond the one reviewed discriminator, Collaborations and Message Flows, payload and correlation, compensation and transactions, assignment and authorization, a task-list UI, migration, and production Event History compatibility. The reviewer path uses the existing complete artifact and pipeline catalogs; it adds no second runner, curated case list, demo-only semantic mode, or hand-maintained expected result.

Proto-MVP success requires approved and closed Inclusive Gateway, Event-Based Gateway, and Call Activity capsules; complete artifact-catalog registration for their scenarios and evidence; and a green `./scripts/pnpm.sh run test:pipeline` over the entire implemented catalog from exact source through every declared semantic, compatibility, durability, mutation, and replay lane. The generated report and implementation map must prevent success in one lane from being presented as BPMN conformance or broad CIB compatibility. Only then does work resume on the remaining BPMN breadth queue and exhaustive denominator programme.

## Ordered work

1. **Completed — direct-parent Sub-Process Error propagation:** checked source, schemas, generic graph admission, exact lowering, Lean relation/evaluator/laws, independent TypeScript semantics, three answer-free CIB-backed schedules, differential mutations, post-throw Temporal restart/replay, and bypass evidence are closed under the [implemented specification](capsules/SUBPROCESS-ERROR-PROPAGATION-SPEC.md).
2. **Completed — reproducible CIB Seven breadth refresh:** `pnpm research:cib-breadth` inventories the pinned 1,144-file corpus with fixture-tested, namespace-insensitive structural classification and records both file and occurrence counts without treating them as conformance evidence.
3. **Completed — Message-addressed Receive Task semantic review checkpoint:** the project-authored phase-zero fixture passed against packaged CIB Seven `2.2.0` before any Message-channel change, selecting `cibseven-2.2.0-message-addressed-receive-task-draft` and `CIB-AGR-0009`. The atomic closed-channel replacement, exact source admission/lowering, combined declaration-order discriminator, and reused Lean/semantic-core Message behavior are implemented at checkpoint `7226733`; its independent review required checked-source and observation-evidence corrections, then passed correction audit at `5a74bad`.
4. **Completed — Message-addressed Receive Task closure:** external fresh-session review target `3881a7a` returned required edits, and the same reviewer passed correction audit target `f5f9caf` without a material redesign. The implemented capsule has graduated to [specification](capsules/RECEIVE-TASK-MESSAGE-SPEC.md) without admitting addressless legacy signal, operation-addressed invention, payload, correlation, repetition, Collaboration, or adjacent Event loci.
5. **Completed — complete current-catalog reviewer path:** `./scripts/pnpm.sh run test:pipeline` and its generated report demonstrate every registered implemented profile. The human walkthrough and its fragment machinery were removed so no curated catalog or second explanatory artifact can become a competing scope authority.
6. **Completed — prepare the proto-MVP expansion:** the requirement denominator and dependency map for Inclusive Gateway, Event-Based Gateway, and Call Activity are committed at `68c5633`, with no new or Inclusive-specific CIB relationship selected before a project-owned probe. The bounded Inclusive Gateway proposal is committed as immutable review target `16904dd`; its already-started external fresh-session review remains valid because the target predates the sub-agent transition baseline. This planning decision does not approve its semantic account.
7. **Completed — bounded Inclusive Gateway capsule:** the [implemented specification](capsules/INCLUSIVE-GATEWAY-SPEC.md) closes exact structured checked source, generic `selectMany`/`synchronizeSelected` operations, occurrence-owned selected-subset synchronization, premise-bearing Lean laws, independent TypeScript semantics, four registered standards-only scenarios, differential mutations, exact Temporal Queries, both completion orders, Worker replacement, accepted-result recovery, zero unrelated host mechanisms, selection-bypass detection, and four-history replay. Proposal correction audit `0b52d15`, semantic-checkpoint correction audit `ea8b019`, and closure correction audit `15ebadc` passed without a material redesign. The complete 25-case gate is green and cost boundary `263433e..24d726b` is recorded.
8. **Completed — bounded Event-Based Gateway capsule:** the [implemented specification](capsules/EVENT-BASED-GATEWAY-SPEC.md) closes exact checked source and lowering, occurrence-owned atomic Message/Timer arming, explicit winner order, complete loser withdrawal, wrong/stale refusal, Lean and independent TypeScript laws, two standards-only differential cases, one cancellable durable Timer, activation-batch fail-closed detection, both Worker-down winner directions, wrong-ingress Timer continuity, direct pinned-SDK batching evidence, hostile scheduler mutations, and replay. Proposal correction audit `acff781`, semantic-checkpoint correction audit `b7c52ca`, and closure correction audit `a62a51a` passed without a material redesign. Cost boundary `8efec58..378a0c8` is recorded; the contended correctness run does not replace the last uncontended performance baseline.
9. **Completed — bounded Call Activity capsule:** the [implemented specification](capsules/CALL-ACTIVITY-SPEC.md) closes namespace-qualified in-document Process resolution, distinct called semantic identity, quiescent exactly-once return, empty-data refusal, independent Lean/core behavior, registered differential agreement, and single-Workflow durability without CIB or Child Workflow identity claims. Closure target `cefb56d` returned APPROVE WITH REQUIRED EDITS; same-reviewer correction audit `bb66c8c` passed without a material redesign. The production wrong-ID refusal preserves the derived called wait, exact committed retry after Worker replacement adds no accepted Update, the lifecycle owner separates host and semantic task addresses, the history inventory says thirty, and cost boundary `dee189c..bd0f0d3` is recorded.
10. **Completed — close the reviewer proto-MVP:** the clean `4f2fe61` complete-catalog run passed all 28 registered cases, declared target and retained-evidence comparisons, seeded semantic mutations, 30 history replays, 56 isolated Workflow IDs, and cleanup. Its 14.620-second warm measurement under the declared contention allowance is correctness evidence, not a replacement performance baseline.
11. **Completed — full-profile product surface:** the runnable command now runs every registered profile through one driver keyed to published enabled interactions, with occurrence identity taken from the publication, declared-order disambiguation, typed refusals, a deterministic product effect Activity, and one example configuration per profile. Its independent cold proposal review at `df44937` and closure review at `1e6f88f` both returned APPROVE WITH REQUIRED EDITS and passed same-reviewer correction audits at `482bbd6`, `7b0c46b`, and `9e66b5e`; the contract graduated into the [runnable Temporal MVP specification](RUNNABLE-TEMPORAL-MVP-SPEC.md), which carries the receipt voluntarily.
12. **Completed — refresh the breadth evidence and repair the denominator:** the CIB Seven breadth instrument now decomposes Boundary Event, the largest family with no closed reviewed slice, by trigger, interruption, and attachment host, with three independently summing dimensions over the pinned corpus. The stale `BPMN-INCLUSIVE-GATEWAY-01` disposition is corrected and an executable guard now fails whenever the mechanism-family map cites a requirement as a closed reviewed slice while its own row stays undecided.
13. **Blocked on owner approval — interrupting Activity boundary Timer capsule:** the [proposal](capsules/ACTIVITY-BOUNDARY-TIMER-PROPOSAL.md) selects one interrupting exact-`PT1S` Timer Boundary Event on one User Task and states four required owner decisions. Its independent cold proposal review at target `fcdd7fb` returned APPROVE WITH REQUIRED EDITS across ten findings. Corrections ran in two same-reviewer rounds: `109c212` closed all ten and introduced four new defects, and audited correction `e37c018` closed those four. The proposal review is complete; **owner approval of the four decisions remains outstanding and is the only thing blocking implementation.**
14. **Completed — reach the Event-Based Gateway timer arm from the product surface:** the product-example oracle no longer requires exactly one example per profile, because a single declared plan can answer the published Message or decline it so the timer wins, never both. It now binds in both useful directions instead, and a second Event-Based Gateway example plus a seventh live mechanism exercise the driver's keep-waiting precedence branch. This also removes a prerequisite the boundary-timer capsule would otherwise have hit, since that capsule obligates two example configurations over one definition.
15. **Then — resume the remaining BPMN breadth queue:** continue the exhaustive requirement denominator and select subsequent capsules by reusable Process Execution leverage, using CIB breadth and A12 prevalence only to order equal-value candidates.

Completed commit-bounded capsule measurements live in the [capsule cost ledger](CAPSULE-COST-LEDGER.md); implemented and absent claims live in [IMPLEMENTATION-MAP.md](IMPLEMENTATION-MAP.md). Git and the owning specifications retain completed implementation history.

A merely strategic assessment does not authorize implementation; the explicit owner decisions and capsule approvals above do.

## Explicitly deferred

- assignment, users/groups, authorization, rendered forms, task-list UI, and BPMN data associations; the runnable MVP admits only the explicit dummy actor proposal and implemented selected Process-variable completion patch;
- global task discovery or Search Attributes;
- timer forms or races beyond the exact Intermediate Catch Timer capsule, Activities and retries beyond the bounded Service Task specification, host/general cancellation, incidents, compensation, and Event Sub-Processes; Message payload, key-based/global correlation, modeled throw, Message Flow, and other Message Event loci remain unimplemented beyond the direct payload-free catch;
- multi-instance, loops, migration, and Continue-As-New;
- a universal BPMN IL, general BPMN compiler, or general semantic assertion language;
- any expansion of the exact Simple Boolean v1 grammar or use outside its Exclusive Gateway capsule; JUEL, capability-bearing expressions, variable mutation, XPath, DMN/FEEL, Groovy, FreeMarker, and JavaScript remain separate profile/runtime decisions;
- a separate CIB parallel-compatibility profile until it has a concrete consumer;
- a classified four-quadrant project-admission × CIB-deployment lane over the pinned MIWG corpus; schedule it with or after the admission-successor checkpoint, report all four quadrants without asserting set equality, and do not count it as execution evidence;
- immutable profile or production Event History compatibility;
- public BPMN conformance or broad CIB compatibility claims.

## Stop conditions

Stop for owner direction if:

- a new normative or pinned-CIB observation reopens the approved semantic account;
- the profile feature or observation boundary would expand beyond the approved capsule;
- a dependency addition, removal, replacement, or upgrade is required;
- the Simple Boolean language would expand beyond its exact URI, grammar, total result, Process-scope string/null context, or read-only capability boundary, or any Workflow code, Lean, or TypeScript core path would parse or evaluate JUEL;
- lowering performs runtime scheduling, activation, completion, propagation, or other semantic work that the IL claims to own;
- an IL operation merely selects a retained topology-specific evaluator;
- a new operation mirrors a BPMN surface class without a reusable semantic mechanism and separating witness;
- required neutral semantic distinctions are erased before Lean can check lowering, or a source/profile translation is claimed as Lean-independent evidence when Lean receives only its normalized result;
- structural invalidity would become an ordinary semantic outcome;
- a second production semantic core or Workflow language is introduced without the exact reopen trigger and fresh owner approval recorded in [PROJECT-DESIGN.md](PROJECT-DESIGN.md#cib-compatibility-and-polyglot-effect-execution);
- the preservation obligation cannot be stated without assuming the desired result;
- a Temporal preflight cannot map a required public semantic outcome without adding host-defined semantics;
- source and executable CIB revisions diverge;
- the feedback budget cannot be met without weakening independence or evidence.

## Exact resume point

The normal and exceptional embedded Sub-Process capsules are closed under their implemented specifications. The Error follow-on reuses definition scopes, runtime occurrence ownership, passive Update hosting, artifact roundtrip, and shared Worker bundles; the public observation, stimulus, result, outcome, and runtime-state shapes remain unchanged. The source-hygiene closure gate forced cohesive extraction of typed operation admission and Error definition admission/lowering instead of accepting new 600–999-line exceptions.

The Message-addressed Receive Task checkpoint and closure reviews both passed their same-thread correction audits. The exact profile/scenario/result roundtrip, content-bound CIB projection under `CIB-AGR-0009`/`CIB-OP-0005`, direct-channel differential mutation, and Receive-Task-specific Temporal refinement/replay witness are implemented under the graduated [specification](capsules/RECEIVE-TASK-MESSAGE-SPEC.md). Preserve the selected `cibseven-2.2.0-message-addressed-receive-task-draft` identity and all declared exclusions.

The [Inclusive Gateway selected-branch specification](capsules/INCLUSIVE-GATEWAY-SPEC.md) is implemented and evidence-closed. Proposal correction audit `0b52d15`, semantic-checkpoint correction audit `ea8b019`, and closure correction audit `15ebadc` passed without a material redesign; cost boundary `263433e..24d726b` is recorded. No Inclusive-specific CIB relationship or execution lane was added, while the profile retains only the existing User Task relationship metadata.

The [Event-Based Gateway Message/Timer specification](capsules/EVENT-BASED-GATEWAY-SPEC.md) is implemented and evidence-closed. Proposal correction audit `acff781`, semantic-checkpoint correction audit `b7c52ca`, and closure correction audit `a62a51a` passed without a material redesign; cost boundary `8efec58..378a0c8` is recorded. The exact profile remains standards-only with CIB deliberately absent, and coalesced host readiness fails closed without selecting a portable BPMN winner.

The bounded [Call Activity specification](capsules/CALL-ACTIVITY-SPEC.md) is implemented and evidence-closed. Its first green semantic checkpoint at `5cb7b54` implements one private caller and one distinct in-document executable called Process, separate semantic instance identity, caller continuation only after normal called quiescence, empty called-instance data, extracted source/graph/runtime/Lean owners, and exhaustive passive host classification while retaining the explicit exclusions for imports, Global Tasks, mappings, version/tenant resolution, recursion, exceptional completion, cancellation, repeated or concurrent calls, and Temporal Child Workflow identity. Checkpoint correction audit `0148592` passed. The registered standards-only profile and scenario compare the called/caller/terminal trace across Lean, the core, and Temporal with a public identity-erasure mutation. The durable witness separates root Workflow address from called task identity, rejects a caller-owned substitute without losing the called wait, survives Worker replacement, recovers an exact committed retry without another accepted Update, reaches the terminal receipt, asserts exact Update-only history, replays, and exposes early-return plus identity-erasure bypasses. The pinned CIB simple-call test remains feasibility evidence rather than an oracle lane; the profile reuses only existing User Task relationship metadata. Closure review target `cefb56d` required the production refusal/retry witness and a corrected history count; same-reviewer correction audit `bb66c8c` closed both findings without material redesign. Cost boundary `dee189c..bd0f0d3` is recorded.

The reviewer Proto-MVP is closed at clean implementation revision `4f2fe61`: all three expansion capsules are implemented specifications, and the complete 28-case catalog passed every declared target relation, retained-evidence comparison, seeded mutation, 30 disposable history replays, 56 Workflow-isolation checks, and cleanup. Resume by deepening the BPMN requirement denominator and selecting the next bounded mechanism capsule; do not treat this milestone as broad BPMN conformance or broad CIB compatibility.

The compositional-admission experiment remains accepted and frozen; do not begin another stage without a documented reopen trigger and owner approval. The runnable MVP remains implemented and owns no Temporal server or port lifecycle.

The runnable product surface is closed and graduated. Ordered-work item 12 is complete: the breadth instrument now decomposes Boundary Event by trigger, interruption, and attachment host, and the requirement ledger's closed-slice claims are locked to its own dispositions by an executable guard.

Resume at ordered-work item 13. Two of the three required **behavior-preserving extractions have landed** as their own commits: `packages/semantic-core/src/semantic-process-runtime.ts` went from 595 to 528 of 600 nonblank lines by giving control-flow token transitions their own owner, and the combined Lean definition decoder at 588 became three modules whose program half now sits at 360. The adapter's `runner.ts` at 592 remains and is needed for this family's scheduler. Run `node scripts/what-binds.ts <path>...` before each edit; [the proposal's owner and guard inventory](capsules/ACTIVITY-BOUNDARY-TIMER-PROPOSAL.md#versioning-consequences) records current headroom for all eight owners and the eight oracles the implementation must satisfy.

The extraction target is the four pure control-flow token operations in that runtime module — `reachNoneEnd`, `duplicate`, `synchronize`, and `choose` — which depend only on `addToken` and `removeToken` and move cleanly into one cohesive owner, freeing roughly seventy-five lines. Each managed wait family already owns its own module, as `semantic-process-event-race-runtime.ts` shows, so the boundary-timer family follows that precedent rather than growing the dispatcher: the new family module owns the atomic arm, both victory transitions, and the two program predicates, while the runtime module gains only dispatch. One real refactor remains inside the dispatcher, because `CompleteUserTaskInstance` is still handled inline while `DeliverMessage` already routes through a named function for its managed case, and the completion arm now needs the same routable shape.

The first implementation attempt was reverted deliberately rather than left half-applied; its 172-line slice, the schema-validated BPMN fixture, and the source-admission red test are preserved outside the repository. The contract addition, the adapter host class with its own typed failure code, and the graph-admission projections were all complete and correct, and the reverted slice additionally closed a hole where a managed race beside a bounded Activity wait would have been admitted, since the two classes were checked independently.

**Source admission, lowering, the transition family, the profile registration, and the product example are implemented and green on `main`**, and the Lean lane covers the arming relation with its soundness bridge plus finite `by decide` locks. The Lean lane is **not complete**: the capsule additionally requires a two-constructor victory relation, an evaluator-to-relation bridge for both victories, and quantified exclusivity, interruption-with-counter-preservation, and state-preservation laws, none of which exist yet, with the complete gate passing 525/0 at `7a8164d`. The remaining lanes are the `profiles/` artifact, the three scenarios, the differential catalog case with its seeded mutation, and Temporal refinement. **These four are one atomic commit and Temporal is the blocking prerequisite**, because [each pipeline case](../packages/differential/test/event-based-gateway-pipeline-cases.ts) declares a Temporal relation, wait-trace length, completion delivery, execution schedule, and replay selection that [the pipeline](../packages/differential/test/pipeline.test.ts) actually runs. The adopter's bounded host-capability class in [host admission](../packages/temporal-adapter/src/host-admission.ts) is admission-only: it admits one isolated bounded User Task with an exact `PT1S` deadline, but no scheduler arms that deadline durably or races it against the completion Update. Start there, and land the `runner.ts` extraction as its own behavior-preserving commit first, because `node scripts/what-binds.ts` reports that owner at 592 of 600 nonblank lines and the scheduler cannot be added under that squeeze. Two lane rules learned the hard way apply to them: a profile registration and its live example under `examples/temporal-mvp/` are **one atomic change**, because [the product oracle](../packages/temporal-adapter/test/product-example-configs.test.ts) requires every registered profile to have an example and that invariant is not negotiable; and the `profiles/` artifact cannot land before its scenarios, because [the roundtrip guard](../scripts/capsule-roundtrip.test.ts) rejects a profile no registered scenario references while [the pipeline catalog](../packages/differential/test/pipeline-catalog.test.ts) requires exactly one case per registered scenario. Plan those four as a single commit.

Always run `./scripts/pnpm.sh run build:differential` before believing any test result about a source change: package tests execute built `dist/` output, and narrower targets such as `build:temporal` leave `bpmn-source` stale. Invoke the full gate as a bare `./scripts/verify.sh > log 2>&1` without a trailing `; echo "EXIT=$?"`, which makes the wrapper always exit 0 and hides a failing run from the task notification.

Branch `wip/activity-boundary-timer` is a superseded checkpoint whose content is squash-merged into `main`; `git branch -d` refuses it because a squash leaves no ancestry, and it can be removed with `-D` whenever the owner wants.

The semantic-core transition family for this capsule was **implemented on `main`** at `eb43040`: `packages/semantic-core/src/semantic-process-bounded-task-runtime.ts` owns atomic arming, both victory transitions, and the program predicates that route `CompleteUserTaskInstance` and `FireTimer`, following the `FireTimer`/event-race routing shape rather than extracting the generic inline paths. The source, Lean, and wire-schema lanes have since landed on `main` and `packages/bpmn-source/test/activity-boundary-timer-source.test.ts` is green; branch `wip/activity-boundary-timer` was removed. The `profiles/` artifact, three scenarios, and the differential case with its seeded mutation remain absent, and they are one atomic change gated on the Temporal lane because `PipelineCase.temporalRelation` is non-nullable while `cib` is nullable, so every registered scenario runs through Temporal. Two empirical facts from that branch are worth keeping: `bpmn-moddle` exposes a Boundary Event's own keys as `$type`/`id`/`name`/`eventDefinitions` only, with `attachedToRef` reachable through the prototype, and it **defaults `cancelActivity` to `true`** when the attribute is omitted, which confirms the capsule's XSD/CMOF reading empirically rather than by citation. Note also that package tests execute built `dist/` output, so a source edit needs `build:differential` before any test result about it is meaningful. The same trap has a Lean form: `lake env lean <file>` type-checks against already-built `.olean` files, so a registry edit in another module stays invisible until `lake build` runs, and reading a stale `false` that way cost one wrong diagnosis. Two further build facts are load-bearing: plain `lake build` does **not** build the `BpmnSemantics/Experiments` tree that `./scripts/verify.sh` also builds, so a new `CheckedNode` or `SemanticOperation` variant can leave the complete gate red while every focused gate passes; and `./scripts/verify.sh` must be invoked bare, because any trailing `; echo` or `; grep` replaces its exit status with the wrapper's.

The adapter `runner.ts` extraction landed at `d14570b`, and the reason was size rather than the scenario path: the file stood at 592 of 600 nonblank lines with 136 of them forwarding nine probe methods to free functions, so no tenth probe could be added without breaching the target. `packages/temporal-adapter/src/mutation-probes.ts` now owns those nine behind a narrow `TemporalProbeHost`, reached as `runner.probes.*` from twelve call sites in eight test files; the runner is at 448.

Both halves of the Update-arm coalescing premise now exist. `47e1abd` extends the source lock from the *use* of `!hasSignals` to its *definition*, because widening that predicate to count `doUpdate` would have split the batch the barrier depends on while every existing assertion kept matching; a mutation confirms it. `17fb551` adds the behavioral half: one probe module with the single-batch SdkFlag withheld by replay splits on a Signal and coalesces on an Update, so the unconditional licence has a lane that does not share the source lock's failure mode. That witness also settles that the coalesced case is reachable rather than hypothetical, so the scheduler must handle it.

The Temporal lane is implemented and the atomic artifact change has landed. `26a9e6d` replaced the unconditional refusal with `packages/temporal-adapter/src/bounded-activity-deadline-scheduler.ts`, which owns the deadline in a cancellation scope so the semantic loop is never parked, and refuses only a completion and deadline sharing one activation — under an identity distinct from the Event-race one, because this capsule defines no winner for host simultaneity. `2cfcd26` first extracted the direct-VM activation harness, whose second user is the bounded witness. `db0360e` registered the profile artifact, two answer-free scenarios, and two differential cases with seeded mutations.

One wiring fact is worth keeping: the completion Update's handler already accepts its stimulus, so the scheduler's returned completion must be pushed straight onto the pending queue. Routing it through `enqueueStimulus` re-accepts it and silently drops it from the queue, which presented as a missing deadline withdrawal rather than as a lost command. The Event-race path already distinguishes these two cases and the shape was copied without the distinction.

Resume at the capsule's cold closure review, which is its last governed boundary. Two things must be stated to that reviewer rather than discovered by it. The third scenario — a stale completion of the abandoned Activity — is deliberately absent: `Ordered` delivery waits for a User Task the deadline victory removed, and `AcceptedBatch` documents itself as an acceptance race, so neither preserves the ordering that case needs; the refusal itself is already checked in Lean and the semantic core, so only its Temporal agreement lane is missing. And `packages/differential/test/pipeline.test.ts` now selects the last caller wait state for `activity-boundary-timer-deadline-wins`, because `openUserTasksAtWait` is queried after the runner waits for the first completion's task while the assertion compared it against the first state with any open task; that proxy holds only while host progress precedes any open task, and the root fix is for the runner to record the state it paused at.

Item 13 is **blocked on nothing**. The [interrupting Activity boundary Timer proposal](capsules/ACTIVITY-BOUNDARY-TIMER-PROPOSAL.md) is written, independently reviewed, corrected under an audited follow-up, and owner-approved on 2026-08-03; its four decisions were the selection itself, one new `awaitBoundedUserTask` operation rather than a widened `awaitEventRace` or sibling waits, the deliberate absence of a runtime occurrence record, and the standards-only boundary with no CIB relationship. The mandatory conditional semantic-checkpoint review is complete: it returned `approve-with-required-edits` at immutable target `a176edb`, and the same reviewer audited the final correction at `d41861b` with no new defect. The remaining review boundary for this capsule is its closure review.

The review changed three facts that implementation must carry. BPMN 2.0.2 does not fix when a boundary Event's waiting begins — Clause 13.5.2 starts waiting when an Intermediate Event is *reached* and a Boundary Event never is — so arming on Activity activation is a recorded project interpretation, and the pre-due-firing witness is the only thing that discriminates it. The Update-arm coalescing barrier is licensed by `hasSignals === false` making single-batch processing unconditional, **not** by the Event-Based Gateway's SDK flag, so that premise needs its own direct-VM witness and a source lock over the `hasSignals` predicate. Separately, omitting `cancelActivity` is admitted only because the XSD and CMOF default it to `true`; Clause 13.5.3's "if the attribute is not set" must be read as "not set to `true`", since the literal reading would make an omitted attribute non-interrupting for a Timer.

The complete `./scripts/verify.sh` gate passed at `6ef14b6` with a documentation-only dirty tree on 2026-08-03. All 28 registered cases reached agreement across every declared target, all 28 seeded semantic mutations produced the required disagreement, all 18 retained-CIB evidence comparisons agreed, 30 live histories replayed, and cleanup completed. Per-gate counts were 41 contract, 20 source-hygiene, 111 semantic-core, 62 BPMN-source, 14 differential, 129 infrastructure, 124 Temporal-adapter, and 4 pipeline-catalog tests with zero failures.

That run's warm total was 15282ms, which exceeded the 15000ms soft target and reported the fact without failing, exactly as the two-tier measure intends. A second complete gate on a clean tree at `a7e0b24` then measured 13805ms with no soft-target report, 108405ms of total wall time, and the same 28 agreements, 28 seeded mutations, 18 retained-evidence comparisons, and 30 replays. The two measurements together settle the question: 13805ms sits between the comparable 28-case figures of 13520ms at `f56f5b4` and 13788ms at `4a12629`, so the earlier excursion was host load and not a regression. No semantic-core, BPMN-source, or Temporal-adapter source file changed since `4a12629`, which independently agrees. The last uncontended figure over the smaller 25-case catalog remains `d31c44a` at 12606ms and is not directly comparable.

**`ac2813c` at 13476ms is the current comparable 28-case baseline.** That closing gate passed all eight lanes with no soft-target report at host load average 4.31, replayed 30 histories, and left a clean tree; it is the fastest recorded 28-case measurement, ahead of `a7e0b24` at 13805ms, `f56f5b4` at 13520ms, and `4a12629` at 13788ms. The pipeline's case count is unchanged, so the comparison is direct and nothing this session regressed it.

Two intervening runs are deliberately recorded as not comparable. `283182b` measured 15341ms while a review sub-agent ran, and a later run measured 25276ms at host load average 17.83 from unrelated work. Overlapping the gate with a review contends the host by construction, as [the testing specification](TESTING-SPEC.md#default-verification) records; both remain correctness evidence only. The 25276ms excursion is also the clearest case for the two-tier measure, since a hard 15000ms assertion would have failed a documentation-only commit.

The complete `./scripts/verify.sh` gate passed at `f56f5b4` after host port-binding authorization. Its 28-case pipeline reached agreement on every case across all declared targets, every one of the 28 seeded semantic mutations produced the required disagreement, all three Lean mutation controls rejected, 30 live histories replayed, 56 Workflow IDs stayed isolated, and cleanup completed; warm total was 13.520 seconds against the unchanged 15-second default, with CIB Seven at 7.468 seconds and Temporal at 12.390 seconds. Both CIB release lanes passed, Lean built and replayed, and the adapter gate passed 120 tests. After the live acceptance lane, `test:temporal` passes 121 tests including six live product runs in 4.695 seconds, and `check:source-hygiene` remains 20/20.
