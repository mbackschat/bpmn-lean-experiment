# Plan

This file owns immediate execution order, blockers, current measured evidence, and the exact resume action. Durable decisions, implementation detail, semantic meaning, and test procedure belong in their linked owners. Completed work leaves this file after its current consequence has an owner; Git retains history.

## Current checkpoint

M0 through M6 and Horizons 1 and 2 are closed. Horizon 2 Product 1 Workflow-chain production enrollment, handle-free start, content-bound recovery, closed v1 terminal receipts, private paired E1/E2 traversal, every approved capacity row, stop-the-world Worker deployment admission, and forced Intermediate Catch Message, Intermediate Catch Timer, and Service Task effect rollover are implemented under the [production lifecycle specification](TEMPORAL-PROCESS-LIFECYCLE-SPEC.md#workflow-chain-production-contract) and [`implementation-status-owner:TEMPORAL-HOSTING`](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md). The complete gate passed at `c2f9a5d`; the same isolated reviewer approved the opaque Schedule-locator and cost-attribution correction at `5b9e028`. Those corrections changed hosting/API representation and persistence only, leaving the registered BPMN/CIB accounts, semantic-core traces, replay evidence, and published locator meaning unchanged.

The non-binding [engine maturity ladder](PROJECT-DESIGN.md#engine-maturity-roadmap-labels) retrospectively names this Product 1 floor Engine `v0.1`, **Runnable MVP**, and uses Engine `v0.2`, **Minimum Useful Engine (MUE)**, as the current direction. Annotated milestone tag `engine/v0.1` points at closed Horizon 2 documentation commit `72b11cf`; it is an evidence snapshot, not a package version or release. Items 1 through 8 below are the remaining `v0.2 MUE` candidate content, not a frozen release or version assignment. Item 9 is `v0.3` content and is listed here only because it is sequenced after them: Engine `v0.3`, **Scalable Engine**, points at Horizon 3, and item 9 restates that row's obligation almost verbatim, so counting it as MUE assigned one work item to two labels; Engine `v0.9`, **Conformance Candidate**, and reserved Engine `v1.0`, **Conformant Engine**, remain conditional future planning labels rather than present claims.

The first sequential Multi-Instance checkpoint now compiles one exact schema-valid batch-review source into a closed checked node and `awaitSequentialMultiInstanceUserTask` operation with deterministic data, route, Timer, and limit facts. The reserved profile is intentionally not execution-registered, so no runtime, public observation, Lean, Temporal, or conformance claim follows from this checkpoint.

Commit `9ba76d8` had added `awaitSequentialMultiInstanceUserTask` to the operation union without its cross-package exhaustiveness obligations, so the harness type check and the publication coverage guard were both red at clean `da3e87a`. Commit `3032828` restores them: the source test narrows its union access, both publication-validation predicates and host classification answer the reserved kind, a directly reachable `assertNever` throw becomes a typed host refusal, and the coverage guard's union-wide denominator becomes a partition requiring each kind to be either enumerated in the publication schema or refused by the completeness producer. Both partition directions were seeded and detected. The increment is classified non-material under [the negative case](TESTING-SPEC.md#independent-cold-review-gate) and opens no review cycle: no BPMN meaning, profile, CIB relationship, checked-source or IL representation, runtime or public observation, transition family, or proof boundary changes, and no reachable production outcome changes because profile admission already refuses the reserved profile before host capability is assessed. The guards that cover it are [the publication coverage guard](../scripts/execution-publication-contract-coverage.test.ts) and [the host refusal test](../packages/temporal-adapter/testkit/test/host-admission-unhostable.test.ts), and its measured gate results belong to `3032828` rather than to this section.

The MUE acceleration procedure is classified non-material under [the negative case](TESTING-SPEC.md#independent-cold-review-gate): PLAN items 1 through 8, their `Maps`, and every semantic and evidence obligation are unchanged. The increment adds only lane-aware explicit staging, write-free concurrent-gate measurement, and an owner-authorized bound on unpushed accumulation. Existing delegation, review, Lean, and verification owners remain authoritative; [the documentation control plane](../scripts/document-control-plane.test.ts), [independent-review policy](../scripts/independent-review-policy.test.ts), [verification-entrypoint guard](../scripts/verification-entrypoint.test.ts), and [process-ledger escalation guard](../scripts/document-reviewability.test.ts) cover adoption.

For the current MUE programme, the owner authorizes pushes at the triggers in [the three-level verification policy](TESTING-SPEC.md#three-level-verification-policy) after every selected pre-push entry point passes. This authorizes neither pushing an unverified target nor bypassing a required review or gate.

The [runtime-state identity bound](RUNTIME-STATE-INVARIANT-SPEC.md#layer-1-lifecycle-and-structure) is implemented, independently closure-reviewed, and graduated into its stable owner. Cost selected the consumer-bound subset: User Task, Timer, and Activity identities are bounded in Lean and TypeScript, the defect gates command and continuation admission, and body turnover derives its wait-key freshness premise from well-formedness. The additive Activity-family issuing discipline is now implemented across a guarded production-writer census, both Lean issuer roots, exact preservation/removal proofs, and an independent TypeScript pair oracle, with its governed semantic-checkpoint verdict pending. Message, Effect, Event race, Call, and ordinary Scope remain open; called roots remain excluded; and general preservation, issuing disciplines outside Activity, non-reissue outside Activity, and Multi-Instance execution registration remain open. The identity-bound review chronology stays with the [archived amendment](archived/RUNTIME-STATE-IDENTITY-BOUND-PROPOSAL.md#independent-cold-review-receipt), and the Activity issuing chronology stays with [its amendment](RUNTIME-STATE-ACTIVITY-ISSUING-DISCIPLINE-PROPOSAL.md#independent-cold-review-receipt).

## Ordered work

Exactly one stable work ID is active. Required maps are part of the routing contract, not descriptive tags.

1. `SEQUENTIAL-MULTI-INSTANCE` · **active** · Owner: [sequential Multi-Instance proposal](capsules/SEQUENTIAL-MULTI-INSTANCE-PROPOSAL.md), [Activity issuing-discipline proposal](RUNTIME-STATE-ACTIVITY-ISSUING-DISCIPLINE-PROPOSAL.md), [Activity occurrence ownership](ACTIVITY-OCCURRENCE-OWNERSHIP-SPEC.md), [requirement ledger](BPMN-REQUIREMENT-LEDGER.md) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [semantic family](ENGINE-SEMANTIC-FAMILY-IMPLEMENTATION-MAP.md), [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md) · Action: Close the governed semantic-checkpoint review of the implemented additive Activity issuing discipline without narrowing `ActivityOccurrenceId`, then resume the Sequential Multi-Instance Temporal/refinement and measured-capacity lanes before execution registration.
2. `INTERNAL-COMMUTATION` · **queued** · Owner: [Semantic Process internal scheduling](SEMANTIC-PROCESS-IL-SPEC.md#internal-scheduling), [Lean proof obligations](SEMANTIC-PROCESS-IL-SPEC.md#lean-specification-and-proof-obligations) · Maps: [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [assurance/adoption](ASSURANCE-AND-ADOPTION-IMPLEMENTATION-MAP.md) · Action: Replace the exact two-User-Task closure exception with a proved sufficient commutation rule over disjoint semantic footprints, canonical state and publication order, and explicit semantic choice for genuinely observable nondeterminism.
3. `PARALLEL-MULTI-INSTANCE` · **queued** · Owner: [requirement ledger](BPMN-REQUIREMENT-LEDGER.md) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md) · Action: Add bounded concurrent ownership, completion conditions, deterministic aggregation, races, cancellation, replacement, and replay over the shared occurrence and commutation mechanisms.
4. `MECHANISM-MATURITY-EVIDENCE` · **queued** · Owner: [requirement-ledger mechanism-family map](BPMN-REQUIREMENT-LEDGER.md#process-execution-mechanism-family-map), [testing specification](TESTING-SPEC.md) · Maps: [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [assurance/adoption](ASSURANCE-AND-ADOPTION-IMPLEMENTATION-MAP.md) · Action: Generate a non-conformance mechanism-maturity vector for invariant coverage, shared Activity occurrence adoption, multiple-enabled closure classification, and profile-local versus reusable composition evidence; do not infer proof quality from theorem syntax or combine BPMN, CIB, and platform denominators.
5. `DATA-AND-TASK-MECHANISMS` · **queued** · Owner: [requirement ledger](BPMN-REQUIREMENT-LEDGER.md), [minimal engine research](research/MINIMAL-USEFUL-BPMN-ENGINE-RESEARCH.md) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md) · Action: Broaden standard data lifetime and mappings before selecting executable Task profiles and host effects.
6. `EVENT-SUBSCRIPTIONS` · **queued** · Owner: [requirement ledger](BPMN-REQUIREMENT-LEDGER.md) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md) · Action: Generalize subscription lifetime, correlation, payload, cancellation, deterministic races, and scope handlers.
7. `COMPENSATION-TRANSACTIONS` · **queued** · Owner: [requirement ledger](BPMN-REQUIREMENT-LEDGER.md) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md) · Action: Specify completed-work registration, snapshots, handler ordering, cancellation, failure, and replay.
8. `H3-WORKLOAD-ISOLATION` · **queued** · Owner: [scalability roadmap](TEMPORAL-BPMN-EXECUTION-SCALABILITY-PROPOSAL.md) · Maps: [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md), [platform](BPM-PLATFORM-IMPLEMENTATION-MAP.md) · Action: Prove queue and Worker isolation, backpressure, fairness, failover, observability, capacity, and cost.
9. `CONFORMANCE-CLOSURE` · **later** · Owner: [requirement ledger](BPMN-REQUIREMENT-LEDGER.md), [conformance target](BPMN-CONFORMANCE-TARGET.md) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [assurance/adoption](ASSURANCE-AND-ADOPTION-IMPLEMENTATION-MAP.md) · Action: Continue Process Execution closure by normative dependency, semantic risk, practical reach, and the adopted [showcase ladder](SHOWCASE-MILESTONE-LADDER-DECISION.md#showcase-milestone-ladder).

## Current evidence

- Complete Horizon 2 closure gate. Command: `env CI=true ./scripts/verify.sh`. Status: `exit 0`. Date: `2026-08-18`. Commit: `c2f9a5dc5369b73ff61b363c53bcac74ab2b05fe`. It covered Lean, the semantic core, BPMN source, shared contracts and canonical bytes, CIB targets, differential comparison, Temporal adapter tests and history replay, Product 2 package evidence, the complete Workflow-chain contract, and the registered 51-case pipeline; this establishes correctness, not production scalability. Two performance baselines remain because catalog changes break comparability: an uncontended, owner-confirmed idle-host 30-case run measured 15,986.670 ms at `13cdec8`, and the uncontended fastest 28-case run measured 13,476 ms at `ac2813c`. No uncontended measurement exists at 32, 34, or 35 cases, and none exists for the current 52-case catalog, so compare a new figure with the per-case trend rather than either baseline and treat a contended run only as correctness evidence. Two independent 34-case runs on 2026-08-07 measured 20,406.710 ms and 20,036 ms, agreeing within 2 percent; this is weak evidence that the catalog genuinely costs about twenty seconds rather than that both runs were contended, so treat roughly 20 seconds as unexplained rather than a regression until an idle-host run settles it. The H2 target's complete gate passed in 190.000 seconds and its registered pipeline passed in 26,426.960 ms under external host contention, so the new run is correctness-only.
- Runtime-state identity-bound closure gate. Command: `env CI=true ./scripts/pnpm.sh run test:pre-push:verify`. Status: `exit 0`. Date: `2026-08-24`. Commit: `e47fe38b15d4e1502e1500777cfc7fcb9f9c5422`. Output SHA-256: `1a81719b6229439365c85574bf724b38f4195b52a28339b407337e349ab98874`. The clean committed target recorded `"dirty":false`, and the exact checkpoint reviewer approved closure after the documentation-only correction audit at `0b435cc759532a7fbe29de150c0a3a71ed2e0349`. The complete gate covered the infrastructure guards, Lean, semantic core, BPMN source, shared contracts, differential comparison, Temporal adapter and history replay, and the registered 52-case pipeline. Its warm phase was `26985.423` ms at `loadPerCore` `2.33953857421875`, so the timing is contended correctness evidence and not a comparable baseline.

## Exact resume point

Active work ID: `SEQUENTIAL-MULTI-INSTANCE`.

Next action: commit the complete first-green [Activity issuing discipline](RUNTIME-STATE-ACTIVITY-ISSUING-DISCIPLINE-PROPOSAL.md) target after its focused and complete applicable gates, generate its review packet mechanically, and open the required context-cold semantic-checkpoint review against that immutable commit while the exact-target complete gate runs concurrently.

The Activity-family issuing account is selected and implemented rather than narrowing `ActivityOccurrenceId`; registration still waits for its governed checkpoint approval and the capsule's Temporal host class, refinement witnesses, measured capacity owner, complete gates, and closure review.

Separate open changes: cross-family Activity wait withdrawal, scope-cancellation controller cleanup, Temporal hosting/refinement/capacity, and PostgreSQL migration evidence.

Oracle: every evaluator-produced transition is permitted by its relation, iteration preserves the outer deadline and its counters, interruption publishes nothing, and both count identities are arithmetic over one published sum.

Stop if a hypothesis is discharged by weakening a conjunct rather than at an arming call site.
