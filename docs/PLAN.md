# Plan

This file owns immediate execution order, blockers, current measured evidence, and the exact resume action. Durable decisions, implementation detail, semantic meaning, and test procedure belong in their linked owners. Completed work leaves this file after its current consequence has an owner; Git retains history.

## Current checkpoint

M0 through M6 and Horizons 1 and 2 are closed. Horizon 2 Product 1 Workflow-chain production enrollment, handle-free start, content-bound recovery, closed v1 terminal receipts, private paired E1/E2 traversal, every approved capacity row, stop-the-world Worker deployment admission, and forced Intermediate Catch Message, Intermediate Catch Timer, and Service Task effect rollover are implemented under the [production lifecycle specification](TEMPORAL-PROCESS-LIFECYCLE-SPEC.md#workflow-chain-production-contract) and [`implementation-status-owner:TEMPORAL-HOSTING`](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md). The complete gate passed at `c2f9a5d`; the same isolated reviewer approved the opaque Schedule-locator and cost-attribution correction at `5b9e028`. Those corrections changed hosting/API representation and persistence only, leaving the registered BPMN/CIB accounts, semantic-core traces, replay evidence, and published locator meaning unchanged.

The non-binding [engine maturity ladder](PROJECT-DESIGN.md#engine-maturity-roadmap-labels) retrospectively names this Product 1 floor Engine `v0.1`, **Runnable MVP**, and uses Engine `v0.2`, **Minimum Useful Engine (MUE)**, as the current direction. Annotated milestone tag `engine/v0.1` points at closed Horizon 2 documentation commit `72b11cf`; it is an evidence snapshot, not a package version or release. The seven named MUE content IDs remain the complete candidate programme and are not a frozen release or version assignment; Sequential and Parallel Multi-Instance are the first two to close, and the other five remain ordered below. The owner moved `H3-WORKLOAD-ISOLATION` out of MUE to Engine `v0.3` on 2026-08-29: it carries none of [the standing high-risk semantic families](PROJECT-DESIGN.md#cib-seven-220-breadth-ordering), the ladder already assigns workload isolation to `v0.3`, and its capacity evidence would be measured against a corpus every remaining Beta checkpoint still changes. `MUE-PREVIEW-ALPHA` and `MUE-PREVIEW-BETA` are product-delivery checkpoints, not MUE content, closure evidence, or support claims. `CONFORMANCE-CLOSURE` is later content. Engine `v0.9`, **Conformance Candidate**, and reserved Engine `v1.0`, **Conformant Engine**, remain conditional future planning labels rather than present claims.

The exact [Sequential Multi-Instance User Task specification](capsules/SEQUENTIAL-MULTI-INSTANCE-SPEC.md) is implemented, independently closure-reviewed, evidence-closed, and execution-registered across source, Lean, the TypeScript core, E1/E2 publication, differential evidence, Product 1, and the retained corpus. Its natural and interrupted production witnesses cover pre-arming rollover, one lifetime Timer, Worker replacement, recovery, stale-task refusal, exact output behavior, production-bound comparison, and replay. The closure corrections passed the complete clean committed gate at `d814cac4`; the closure-reviewed [MUE Preview Alpha specification](MUE-PREVIEW-ALPHA-SPEC.md) consumes this published contract without adding or simulating BPMN semantics.

The owner selected the [Alpha, Beta, Release Candidate, and MUE delivery ladder](PROJECT-DESIGN.md#mue-delivery-checkpoints) under the existing [breadth and risk ordering](PROJECT-DESIGN.md#cib-seven-220-breadth-ordering) and [showcase acceptance model](SHOWCASE-MILESTONE-LADDER-DECISION.md#showcase-milestone-ladder). Alpha is implemented and closure-reviewed: the exact natural and interrupted Sequential Multi-Instance examples run through the production browser and Temporal host, publish visible committed progress and interruption, and replay every actual Run without simulated semantics. The pushed annotated tag `phase/mue-preview-alpha` points at completely verified closure head `51e59a6d167d2d8dd15fbf7d19355080f5dd493c`. Beta is active and takes one separately reviewed risk-bearing vertical checkpoint through every remaining MUE content ID before integrating that breadth into one coherent preview. Named mechanism boundaries retain separate capsules and reviews. After Beta the same MUE IDs return to the queue for full closure; the Release Candidate begins only after that scope is implemented and feature-frozen, and final MUE remains the separate release-qualified `engine/v0.2` boundary.

For the current MUE programme, the owner authorizes pushes at the triggers in [the three-level verification policy](TESTING-SPEC.md#three-level-verification-policy) after every selected pre-push entry point passes. This authorizes neither pushing an unverified target nor bypassing a required review or gate.

The [runtime-state identity bound](RUNTIME-STATE-INVARIANT-SPEC.md#layer-1-lifecycle-and-structure) and Activity-family issuing discipline supply the published outer-identity premises. User Task, Timer, and Activity identities are bounded; Message, Effect, Event race, Call, ordinary Scope, general preservation, and issuing disciplines outside Activity remain open without weakening this slice.

The owner-approved [internal commutation account](INTERNAL-COMMUTATION-PROPOSAL.md) has passed its first green final-implementation semantic checkpoint. The Semantic Process Program requires the closed `InternalSchedulingMode`, and every existing source lowerer emits `rejectObservableChoice`. The TypeScript core retains the proved exact-pair rule and atomically executes a complete finite frontier of ordinary User Task, Message, Timer, and effect arming operations when every unordered pre-state footprint pair is independent; it refuses a batch that exceeds remaining fuel before mutation and rolls a disappearing member back to the pre-batch state. Lean defines the same complete-frontier classifier and a three-User-Task discriminator whose six permutations reach one raw state. Both semantic accounts now classify every operation family and RuntimeState field under the proposal's exact cross-language mapping; the proposal-derived guard rejects independent additions and classification drift. The census enables no transition and defines no footprint. The context-cold reviewer approved correction target `fb998985` with no remaining findings, and the clean complete gate passed at `9bbc99e5`. Scheduled-choice consumption, region footprints, remaining operation-family preparations, the arbitrary-batch theorem, and Temporal scheduled-mode admission remain unimplemented. This checkpoint changes Program representation but adds no BPMN source shape, admitted profile, public command, or host capability.

The generated mechanism-maturity vector derives its family set from the requirement ledger and rejects missing, stale, or evidence-less cells. Compensation and collaboration are the only families with open cells, and compensation's recorded dependencies keep Data and Task mechanisms first in the breadth queue.

The owner-approved [Activity data-input mediation specification](capsules/ACTIVITY-DATA-INPUT-MEDIATION-SPEC.md) selects the first `DATA-AND-TASK-MECHANISMS` Beta risk checkpoint and is implemented, closure-reviewed, and graduated. Its context-cold review targeted `3332a92f` and approved the account with no findings. The discriminated-owner foundation landed first, replacing the flat effect-only local-scope owner with distinct complete effect and Activity occurrence arms while preserving existing effect semantics and bytes. The semantic checkpoint then added the producer: one required direct Process-Property-to-User-Task DataInput mapping makes source availability part of Activity readiness, distinguishes absent data from explicit null, gives the copied value occurrence-owned local lifetime, and publishes only the selected task input. Source admission, checked graph, IL, both semantic accounts, wire schemas, the registered profile, three answer-free scenarios, one retained invoice-review model, and the three registered differential cases running Lean, the core, and Temporal in agreement are in place; the production Temporal refinement witness runs all three scenarios against one live service with Worker replacement, termination, and replay; and Product 2 now strictly decodes the published collection through its copied contract. The governed closure review returned `approve-with-required-edits` at `cad23498`; its two required findings, that the freshness invariant was anchored where no family transition is enabled and that no recorded gate executed the platform changes, were closed at `fb42ab1b`. Reaching the host exposed one cross-cutting correction: the first stable state that is Running with a token and no ingress separated publication from progress, so committed publication and the incident projection now turn on structural soundness while Run-boundary continuation keeps the stricter resumability question. Output mediation and every new executable Task host remain separate mechanism boundaries.

The [Parallel Multi-Instance specification](capsules/PARALLEL-MULTI-INSTANCE-SPEC.md) and supporting [`AOO-CLAIM-01`](ACTIVITY-OCCURRENCE-OWNERSHIP-SPEC.md#stable-semantic-rules) invariant are implemented, closure-reviewed, and evidence-closed across source, Lean, the independently written core, publication, differential schedules, retained corpus, Product 2 contract compatibility, and production Temporal refinement. Reproducible cost is bound to `25dd98cd..17e23590`.

## Ordered work

Exactly one stable work ID is active. Required maps are part of the routing contract, not descriptive tags.

Beta breadth now advances at `DATA-AND-TASK-MECHANISMS`. The approved Internal Commutation checkpoint already satisfies that content ID's Beta risk boundary, while its broader final-closure Action remains queued after Beta and no broader claim follows from the checkpoint.

1. `DATA-AND-TASK-MECHANISMS` · **active** · Owner: [requirement ledger](BPMN-REQUIREMENT-LEDGER.md), [minimal engine research](research/MINIMAL-USEFUL-BPMN-ENGINE-RESEARCH.md) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md), [assurance/adoption](ASSURANCE-AND-ADOPTION-IMPLEMENTATION-MAP.md) · Action: Broaden standard data lifetime and mappings before selecting executable Task profiles and host effects.
2. `EVENT-SUBSCRIPTIONS` · **queued** · Owner: [requirement ledger](BPMN-REQUIREMENT-LEDGER.md) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md) · Action: Generalize subscription lifetime, correlation, payload, cancellation, deterministic races, and scope handlers. The owner placed this before compensation on 2026-08-29: the ledger makes it a prerequisite of both compensation and collaboration, it carries its own concurrency, cancellation, and correlation risk, and three closed Message capsules already supply machinery to build on.
3. `COMPENSATION-TRANSACTIONS` · **queued** · Owner: [requirement ledger](BPMN-REQUIREMENT-LEDGER.md) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md) · Action: Specify completed-work registration, snapshots, handler ordering, cancellation, failure, and replay. [The retention experiment](experiments/COMPENSATION-RETENTION-EXPERIMENT.md) leaves two bounds to discharge here: retention disposal attaches to scope close rather than Activity completion, and the profile owes an explicit retained-record bound stated against the 64 KiB committed-state budget, which Continue-As-New carries forward rather than sheds.
4. `MUE-PREVIEW-BETA` · **queued** · Owner: [engine maturity ladder](PROJECT-DESIGN.md#engine-maturity-roadmap-labels), [requirement ledger](BPMN-REQUIREMENT-LEDGER.md), [showcase ladder](SHOWCASE-MILESTONE-LADDER-DECISION.md#showcase-milestone-ladder) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md), [platform](BPM-PLATFORM-IMPLEMENTATION-MAP.md), [assurance/adoption](ASSURANCE-AND-ADOPTION-IMPLEMENTATION-MAP.md) · Action: Integrate the separately reviewed breadth checkpoints into one coherent example catalog and Product 2 journey, disclose every open limit, then resequence all MUE content IDs for full closure.
5. `INTERNAL-COMMUTATION` · **queued** · Owner: [internal commutation proposal](INTERNAL-COMMUTATION-PROPOSAL.md), [Semantic Process internal scheduling](SEMANTIC-PROCESS-IL-SPEC.md#internal-scheduling), [Lean proof obligations](SEMANTIC-PROCESS-IL-SPEC.md#lean-specification-and-proof-obligations) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md), [assurance/adoption](ASSURANCE-AND-ADOPTION-IMPLEMENTATION-MAP.md) · Action: Generalize the proved exact-pair checkpoint into complete operation-family classification, arbitrary finite independent batches over disjoint semantic and region footprints, canonical state and publication order, and exact semantic input for genuinely observable choice.
6. `CONFORMANCE-CLOSURE` · **later** · Owner: [requirement ledger](BPMN-REQUIREMENT-LEDGER.md), [conformance target](BPMN-CONFORMANCE-TARGET.md) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [assurance/adoption](ASSURANCE-AND-ADOPTION-IMPLEMENTATION-MAP.md) · Action: Continue Process Execution closure by normative dependency, semantic risk, practical reach, and the adopted [showcase ladder](SHOWCASE-MILESTONE-LADDER-DECISION.md#showcase-milestone-ladder).

## Current evidence

- Activity data-input closure corrections verified. Command: `./scripts/pnpm.sh run test:pre-push:verify`. Status: `exit 0`. Date: `2026-08-30`. Commit: `fb42ab1b`, recording `dirty:false`. Elapsed `1328000` ms with output SHA-256 `e5a97ce5c1f4fdd041519c5b7bd94009730004a59a899d9a5f47d53de8ad007e`; the longer run is the cold rebuild of the corrected Lean module and its downstream conformance modules. `./scripts/pnpm.sh run test:pre-push:platform` also exited zero in `69000` ms with output SHA-256 `eb7a72d8593b9f8a7353550b48bac8ac851445c595ebf1cbdf9b54f13c7e8799`; the closure review found that `verify.sh` builds no platform package, so the Product 2 lane and the removed-process-weight guard had no run against this target until that entry point was added.
- Activity data-input mediation semantic checkpoint. Command: `./scripts/pnpm.sh run test:pre-push:verify`. Status: `exit 0`. Date: `2026-08-29`. Commit: `13a8de20`. Elapsed `286072` ms with output SHA-256 `29ec0bec1d3ceffdb7568115296165616bf7eb09186d0dad8e649d23e3aeb606`, run through the durable receipt wrapper against a clean committed head. The context-cold review targeted `adb23217f7e9248620435019c904ac8ba915e6f9` and returned `approve-with-required-edits`; the same reviewer closed the required finding and every blocking advisory in a warm audit of correction target `ede20831`, then confirmed this gate at `13a8de20`. The pipeline retained 60 cases and 71 replay histories.

## Exact resume point

Active work ID: `DATA-AND-TASK-MECHANISMS`.

Next action: propose output mediation as the second `DATA-AND-TASK-MECHANISMS` checkpoint, taking [the closed input capsule](capsules/ACTIVITY-DATA-INPUT-MEDIATION-SPEC.md)'s named nearest unsupported claim: one DataOutput and DataOutputAssociation copying a completed Activity value back to Process scope. The active item broadens data lifetime and mappings before executable Task profiles, so the input half does not close it; `EVENT-SUBSCRIPTIONS` follows.

Separate open changes outside the active item: Internal Commutation final closure, Activity issuing-discipline closure and graduation, cross-family Activity wait withdrawal, PostgreSQL migration evidence, and workload isolation now held at Engine `v0.3`. Host load no longer blocks this capsule's refinement lane, because the raised deadline and its margin assertion are green and recorded by [`implementation-status-owner:TEMPORAL-HOSTING`](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md).

Oracle: one write-back proposition at the Activity completion boundary, an exact OutputSet and DataOutputAssociation contract, the ordering of that write against local-scope disposal, and a Temporal account still using only existing completion ingress. The input capsule supplies the discriminated Activity-occurrence owner, the occurrence-local scope and its disposal law, and the copy law to run in reverse.

Stop if the write-back cannot be ordered against disposal without making disposal observable, an output can reach Process scope from a state the account does not admit, the local scope must outlive its Activity to supply it, an expression runtime becomes semantic authority, the locked empty-OutputSet meaning would change, or a no-output profile's retained bytes would move.
