# Plan

This file owns immediate execution order, blockers, current measured evidence, and the exact resume action. Durable decisions, implementation detail, semantic meaning, and test procedure belong in their linked owners. Completed work leaves this file after its current consequence has an owner; Git retains history.

## Current checkpoint

M0 through M6 and Horizons 1 and 2 are closed. Horizon 2 Product 1 Workflow-chain production enrollment, handle-free start, content-bound recovery, closed v1 terminal receipts, private paired E1/E2 traversal, every approved capacity row, stop-the-world Worker deployment admission, and forced Intermediate Catch Message, Intermediate Catch Timer, and Service Task effect rollover are implemented under the [production lifecycle specification](TEMPORAL-PROCESS-LIFECYCLE-SPEC.md#workflow-chain-production-contract) and [`implementation-status-owner:TEMPORAL-HOSTING`](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md). The complete gate passed at `c2f9a5d`; the same isolated reviewer approved the opaque Schedule-locator and cost-attribution correction at `5b9e028`. Those corrections changed hosting/API representation and persistence only, leaving the registered BPMN/CIB accounts, semantic-core traces, replay evidence, and published locator meaning unchanged.

The non-binding [engine maturity ladder](PROJECT-DESIGN.md#engine-maturity-roadmap-labels) retrospectively names this Product 1 floor Engine `v0.1`, **Runnable MVP**, and uses Engine `v0.2`, **Minimum Useful Engine (MUE)**, as the current direction. Annotated milestone tag `engine/v0.1` points at closed Horizon 2 documentation commit `72b11cf`; it is an evidence snapshot, not a package version or release. Items 1 through 6 below are the `v0.2 MUE` candidate content, not a frozen release or version assignment. Engine `v0.3`, **Scalable Engine**, currently points at Horizon 3; Engine `v0.9`, **Conformance Candidate**, and reserved Engine `v1.0`, **Conformant Engine**, remain conditional future planning labels rather than present claims.

`INTERCHANGE-ADMISSION` preflight selected `BPMN-STRUCT-DIAGRAM-INTERCHANGE-01` with consequence `preserve`. The first reviewed [interchange proposal](INTERCHANGE-ADMISSION-PROPOSAL.md) closed its original findings through `f773977`, and the owner authorized implementation. The fail-closed profile-by-surface dispatch then exposed a proposal defect at the complete BPMN-source gate: adding declaration atoms in place changes four value-domain profile digests and the preserved-notation predecessor digest even though the cyclic-control-flow preservation baseline binds those artifacts exactly and has no replacement path. Context-cold review approved redesigned immutable target `adec37e` with no findings. Commit `2e617c2` restored all five frozen artifacts, bound the four legacy value domains to baseline digests plus exact README declarations, and recorded standard Definitions metadata without changing checked source or execution. The executable checkpoint registers the composed profile, answer-free scenario, content-bound CIB evidence, independently authored source/twin discriminator, semantic-core reuse, and differential pipeline case. The context-cold semantic-checkpoint review of `55e31d4` required one evidence-integrity correction; `dbf5fe7` independently binds every registered profile artifact's standard-notation declaration to actual compiler dispatch, and the same reviewer approved it without changing BPMN/CIB meaning or production behavior. Commit `234c284` registers the same existing execution account in Lean, `3654b43` proves the unchanged Product 1 path through Worker replacement, history, terminal result, and replay, and `e9714a8` reclassifies the five external CIB rows under the closest profile while all remain rejected. Owner-authorized `ed121e3` inventories the one existing Structured Human Work topology theorem whose imported String parser cannot reduce under kernel decision; the clean narrow module build and trusted-path guard are green. The first complete-gate attempt at `7bb3d22` exposed one stale Call Activity proof oracle that still treated an invented profile as implicitly String-capable after admission became fail-closed. Commit `1f8d2af` replaces it with separate registered-positive and unregistered-negative witnesses, and the narrow Lean and source-policy gates are green. The owner-authorized second complete-gate attempt at `2c0024e` passed the corrected Lean, source, core, CIB, and most live Temporal lanes before the Product 1 catalog guard found the new profile lacked its required runnable example. Commit `8d8a286` adds the existing-host example with exact nonempty start and completion data plus an executable composition assertion, while its exhaustive registered-profile check prevents recurrence. The owner-authorized third complete-gate attempt at `116a408` passed the complete main Temporal suite at 229/229 and exposed two final capacity witnesses that still fabricated Process-start data under the cycle profile after that surface became fail-closed. Commit `35459aa` moves those data-heavy capacity measurements to the existing registered Process-data User Task profile while preserving the cycle profile's completion-only data contract; the exact previously failing final batch now passes 7/7. The owner-authorized fourth complete-gate attempt at `377fcdc` passed both Temporal batches at 229/229 and 7/7 before the release-bound pipeline found its exact expected-ID oracle still described the previous 51-case catalog. Commit `a0e444b` adds the already registered interchange case to that order oracle, and the complete 52-case pipeline now passes 5/5 across its declared targets. The complete gate passed at `42e62e5` with the repository-required Java 21 path: the main Temporal suite passed 229/229, the capacity suite passed 7/7, and the registered pipeline passed 5/5 with 63 live histories. Closure remains open only for same-reviewer warm continuity.

## Ordered work

Exactly one stable work ID is active. Required maps are part of the routing contract, not descriptive tags.

1. `INTERCHANGE-ADMISSION` · **active** · Owner: [requirement ledger](BPMN-REQUIREMENT-LEDGER.md), [corpus research](research/EXECUTABLE-BPMN-MODEL-CORPUS-RESEARCH.md) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [assurance/adoption](ASSURANCE-AND-ADOPTION-IMPLEMENTATION-MAP.md) · Action: Admit high-leverage standard DI, lanes, Collaboration presentation, documentation, and safe inert metadata as execute, preserve, or reject.
2. `SEQUENTIAL-MULTI-INSTANCE` · **queued** · Owner: [requirement ledger](BPMN-REQUIREMENT-LEDGER.md) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md) · Action: Specify bounded sequential iteration, identity, data, completion, cancellation, progress, replay, and output aggregation.
3. `PARALLEL-MULTI-INSTANCE` · **queued** · Owner: [requirement ledger](BPMN-REQUIREMENT-LEDGER.md) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md) · Action: Add bounded concurrent ownership, completion conditions, deterministic aggregation, races, cancellation, replacement, and replay.
4. `DATA-AND-TASK-MECHANISMS` · **queued** · Owner: [requirement ledger](BPMN-REQUIREMENT-LEDGER.md), [minimal engine research](research/MINIMAL-USEFUL-BPMN-ENGINE-RESEARCH.md) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md) · Action: Broaden standard data lifetime and mappings before selecting executable Task profiles and host effects.
5. `EVENT-SUBSCRIPTIONS` · **queued** · Owner: [requirement ledger](BPMN-REQUIREMENT-LEDGER.md) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md) · Action: Generalize subscription lifetime, correlation, payload, cancellation, deterministic races, and scope handlers.
6. `COMPENSATION-TRANSACTIONS` · **queued** · Owner: [requirement ledger](BPMN-REQUIREMENT-LEDGER.md) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md) · Action: Specify completed-work registration, snapshots, handler ordering, cancellation, failure, and replay.
7. `H3-WORKLOAD-ISOLATION` · **queued** · Owner: [scalability roadmap](TEMPORAL-BPMN-EXECUTION-SCALABILITY-PROPOSAL.md) · Maps: [Temporal](TEMPORAL-HOSTING-IMPLEMENTATION-MAP.md), [platform](BPM-PLATFORM-IMPLEMENTATION-MAP.md) · Action: Prove queue and Worker isolation, backpressure, fairness, failover, observability, capacity, and cost.
8. `CONFORMANCE-CLOSURE` · **later** · Owner: [requirement ledger](BPMN-REQUIREMENT-LEDGER.md), [conformance target](BPMN-CONFORMANCE-TARGET.md) · Maps: [contracts/source](ENGINE-CONTRACTS-AND-SOURCE-IMPLEMENTATION-MAP.md), [runtime/proof](ENGINE-RUNTIME-AND-PROOF-IMPLEMENTATION-MAP.md), [assurance/adoption](ASSURANCE-AND-ADOPTION-IMPLEMENTATION-MAP.md) · Action: Continue Process Execution closure by normative dependency, semantic risk, practical reach, and the adopted [showcase ladder](SHOWCASE-MILESTONE-LADDER-DECISION.md#showcase-milestone-ladder).

## Current evidence

- Complete Horizon 2 closure gate. Command: `env CI=true ./scripts/verify.sh`. Status: `exit 0`. Date: `2026-08-18`. Commit: `c2f9a5dc5369b73ff61b363c53bcac74ab2b05fe`. It covered Lean, the semantic core, BPMN source, shared contracts and canonical bytes, CIB targets, differential comparison, Temporal adapter tests and history replay, Product 2 package evidence, the complete Workflow-chain contract, and the registered 51-case pipeline; this establishes correctness, not production scalability. Two performance baselines remain because catalog changes break comparability: an uncontended, owner-confirmed idle-host 30-case run measured 15,986.670 ms at `13cdec8`, and the uncontended fastest 28-case run measured 13,476 ms at `ac2813c`. No uncontended measurement exists at 32, 34, or 35 cases, and none exists for the current 52-case catalog, so compare a new figure with the per-case trend rather than either baseline and treat a contended run only as correctness evidence. Two independent 34-case runs on 2026-08-07 measured 20,406.710 ms and 20,036 ms, agreeing within 2 percent; this is weak evidence that the catalog genuinely costs about twenty seconds rather than that both runs were contended, so treat roughly 20 seconds as unexplained rather than a regression until an idle-host run settles it. The H2 target's complete gate passed in 190.000 seconds and its registered pipeline passed in 26,426.960 ms under external host contention, so the new run is correctness-only.
- Interchange complete closure gate. Command: `env CI=true JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home BPMN_JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home ./scripts/verify.sh`. Status: `exit 0`. Date: `2026-08-19`. Commit: `42e62e5e87ee14529fb81c850a5e557019a1aa91`. Elapsed: `214300` ms. Output SHA-256: `f60882cd7e1ff6a86b473e753e591db0417e17bc7a8fe42a08bf2a1fa5a1ec7e`. The final run proves all four prior failure classes corrected: Lean and source admission complete, the main Temporal suite passes 229/229, the capacity suite passes 7/7, and the registered 52-case release pipeline passes 5/5 with 63 live histories. This is correctness evidence under the explicit Java 21 toolchain, not a performance baseline. The context-cold checkpoint reviewer approved `dbf5fe7`; same-reviewer warm closure continuity remains pending.

## Exact resume point

Active work ID: `INTERCHANGE-ADMISSION`.

Next action: generate the checkpoint-continuity manifest from `dbf5fe7` to immutable closure target `c763d1a833febbce7e1b35d9ff492e6d8b2d8b28`, then return that target and its neutral review packet to the same semantic-checkpoint reviewer for warm closure review. Do not rerun the complete gate or claim interchange closure before review approval.

Oracle: the admitted interchange subset preserves exact source bytes and project-owned checked representations, does not make DI, lane, Collaboration-presentation, documentation, or inert metadata executable, and keeps BPMN requirement coverage, selected CIB compatibility, and corpus reach independently accounted.

Stop if a gate or review requires broadening the trusted path beyond the one inventoried theorem, if review requires a material redesign, if admission would reinterpret executable BPMN behavior, if a new CIB relationship is required, if checked Process meaning changes, or if presentation or documentation metadata can influence runtime execution.
