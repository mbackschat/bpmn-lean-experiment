# CreateDocument data fixture

The tracked [BPMN fixture](process.bpmn) is a project-authored MIT-licensed model with the same admitted executable shape as the registered A12 Workflows `CreateDocument.bpmn`: one synchronous delegate-expression Service Task, one literal string input mapping, and one local-variable output mapping.

It is not a copy or redistribution of the EUPL-or-commercial A12 source. IDs, namespace, descriptive metadata, layout, and source identity are project-authored. The focused BPMN-source gate also admits the exact registered checkout bytes unchanged when that checkout is available at the relative path recorded in [SOURCES.md](../../docs/SOURCES.md).

The answer-free scenario and retained CIB Seven `2.0.0` evidence are added only when the semantic and oracle lanes close. Until then this directory is a source-admission fixture, not a compatibility result.
