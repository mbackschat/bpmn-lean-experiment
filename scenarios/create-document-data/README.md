# CreateDocument data fixture

The tracked [BPMN fixture](process.bpmn) is a project-authored MIT-licensed model with the same admitted executable shape as the registered A12 Workflows `CreateDocument.bpmn`: one synchronous delegate-expression Service Task, one literal string input mapping, and one local-variable output mapping.

It is not a copy or redistribution of the EUPL-1.2 A12 source. IDs, namespace, descriptive metadata, layout, and source identity are project-authored. The focused BPMN-source gate also admits the exact registered checkout bytes unchanged when that checkout is available at the relative path recorded in [SOURCES.md](../../docs/SOURCES.md).

The [answer-free scenario](scenario.json) and [retained CIB Seven `2.0.0` evidence](cibseven-evidence.json) close the project-authored equivalent fixture under the case-specific relation in the [CreateDocument specification](../../docs/capsules/CREATE-DOCUMENT-DATA-SPEC.md). They do not redistribute the A12 model, prove execution of the external EUPL-1.2 file, or establish Java delegate compatibility.
