# User Task assignment and form metadata scenario

The [answer-free scenario](scenario.json) uses the byte-identical approved [User Task metadata source](process.bpmn). It starts the Process with string data, observes one open User Task carrying candidate group `reviewers` and Boolean field `approved`, completes the exact occurrence with primitive Boolean `true`, and reaches ordinary Process completion.

The target contains no expected result. Retained CIB evidence is a separate content-bound artifact, while Lean, the TypeScript semantic core, the differential pipeline, and Temporal derive their observations independently. The [User Task assignment and form metadata specification](../../docs/capsules/USER-TASK-ASSIGNMENT-FORM-METADATA-SPEC.md) owns the selected extension shape, passive metadata meaning, Boolean-completion composition, and exclusions.
