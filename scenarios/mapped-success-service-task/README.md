# Mapped-success Service Task fixture

The tracked [BPMN fixture](process.bpmn) is a project-authored MIT-licensed model containing one synchronous delegate-expression Service Task, one literal string input mapping, and one local-variable output mapping.

The [answer-free scenario](scenario.json) and [retained CIB Seven `2.0.0` evidence](cibseven-evidence.json) close the project-neutral fixture under the mapped-success profile. They do not establish arbitrary expression, delegate, value, or mapping compatibility.
