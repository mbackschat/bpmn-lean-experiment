# Service Task effect scenario

The answer-free [scenario](scenario.json) starts the exact [BPMN Process](process.bpmn), observes one project-owned effect intent, and supplies an explicit content-bound `completeEffect` semantic input. Lean and the TypeScript semantic core apply that input directly. CIB Seven realizes it through explicit harness release of the async-before continuation job and exact delegate-expression bean. The Temporal adapter must derive the identical stimulus from committed intent after its Activity succeeds and must not receive it through scenario delivery.

Expected observations remain outside the scenario in content-bound retained CIB evidence and verifier-side assertions. The retained CIB result is bound to the plain-success schedule; retry decrement and re-execution facts remain raw host evidence.
