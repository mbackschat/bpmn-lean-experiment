# Artifact store

This directory will own exact artifact-byte storage and retrieval through content identity. It does not compile BPMN, assign definition versions, or decide deployment policy.

No artifact store is implemented yet. See [the architecture](../../../docs/ARCHITECTURE.md#foundation-packages) and the [definition module](../../modules/definitions/README.md).
