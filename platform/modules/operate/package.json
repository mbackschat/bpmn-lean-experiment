{
  "name": "@bpmn-lean/platform-operate",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "migrations"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test:built": "node --test --test-concurrency=1 test/*.test.ts",
    "test": "pnpm build && pnpm test:built",
    "test:postgresql:built": "node --test --test-concurrency=1 test/postgresql/*.test.ts",
    "test:postgresql": "pnpm --filter @bpmn-lean/platform-operate... build && pnpm test:postgresql:built"
  },
  "dependencies": {
    "@bpmn-lean/platform-contracts": "workspace:*",
    "@bpmn-lean/platform-identity-policy": "workspace:*",
    "@bpmn-lean/platform-postgresql-runtime": "workspace:*"
  }
}
